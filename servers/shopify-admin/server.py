#!/usr/bin/env python3
"""
Shopify Admin MCP Server
Generated: 2026-04-25 19:22:51 UTC
Generator: MCP Blacksmith v1.1.0 (https://mcpblacksmith.com)
"""

from __future__ import annotations

import argparse
import asyncio
import collections
import contextlib
import json
import logging
import os
import random
import sys
import time
import uuid
from dataclasses import dataclass
from enum import Enum
from pathlib import Path
from typing import Any, cast

try:
    from dotenv import load_dotenv
    _env_path = Path(__file__).parent / '.env'
    if _env_path.exists():
        load_dotenv(_env_path)
except ImportError:
    pass

import _auth
import _models
import httpx
import pydantic
from fastmcp import FastMCP
from fastmcp.server.middleware import Middleware
from fastmcp.tools import ToolResult
from pydantic import Field

# Server variables (from OpenAPI spec, overridable via SERVER_* env vars)
_SERVER_VARS = {
    "store_name": os.getenv("SERVER_STORE_NAME", ""),
}
BASE_URL = os.getenv("BASE_URL", "https://{store_name}.myshopify.com/admin/api/2024-01".format_map(collections.defaultdict(str, _SERVER_VARS)))
SERVER_NAME = "Shopify Admin"
SERVER_VERSION = "1.0.0"

CONNECTION_POOL_SIZE = int(os.getenv("CONNECTION_POOL_SIZE", "100"))
MAX_KEEPALIVE_CONNECTIONS = int(os.getenv("MAX_KEEPALIVE_CONNECTIONS", "20"))

LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO").upper()
if LOG_LEVEL not in ("DEBUG", "INFO", "WARNING", "ERROR"):
    LOG_LEVEL = "INFO"

HTTPX_TIMEOUT = httpx.Timeout(
    connect=float(os.getenv("HTTPX_CONNECT_TIMEOUT", "10.0")),
    read=float(os.getenv("HTTPX_READ_TIMEOUT", "60.0")),
    write=float(os.getenv("HTTPX_WRITE_TIMEOUT", "30.0")),
    pool=float(os.getenv("HTTPX_POOL_TIMEOUT", "5.0"))
)

DEFAULT_TIMEOUT = float(os.getenv("TOOL_EXECUTION_TIMEOUT", "90.0"))

MAX_RETRIES = int(os.getenv("MAX_RETRIES", "3"))
RETRY_BACKOFF_FACTOR = float(os.getenv("RETRY_BACKOFF_FACTOR", "2.0"))
CIRCUIT_BREAKER_FAILURE_THRESHOLD = int(os.getenv("CIRCUIT_BREAKER_FAILURE_THRESHOLD", "5"))
CIRCUIT_BREAKER_TIMEOUT_SECONDS = float(os.getenv("CIRCUIT_BREAKER_TIMEOUT_SECONDS", "60.0"))
RATE_LIMIT_REQUESTS_PER_SECOND = int(os.getenv("RATE_LIMIT_REQUESTS_PER_SECOND", "10"))
client: httpx.AsyncClient | None = None

@dataclass
class RetryConfig:
    """Retry with exponential backoff configuration."""
    max_attempts: int = 3
    base_delay: float = 1.0
    max_delay: float = 60.0
    exponential_base: float = 2.0
    jitter: bool = True
    retry_on_status_codes: list[int] | None = None

    def __post_init__(self):
        if self.retry_on_status_codes is None:
            self.retry_on_status_codes = [429, 500, 502, 503, 504]

class TokenBucket:
    """Token bucket rate limiter."""

    def __init__(self, rate: float, capacity: int):
        self.rate = rate
        self.capacity = capacity
        self.tokens = float(capacity)
        self.last_update = time.monotonic()

    def _refill(self):
        now = time.monotonic()
        elapsed = now - self.last_update
        self.tokens = min(self.capacity, self.tokens + (elapsed * self.rate))
        self.last_update = now

    def consume(self, tokens: int = 1) -> tuple[bool, float | None]:
        self._refill()
        if self.tokens >= tokens:
            self.tokens -= tokens
            return True, None
        tokens_needed = tokens - self.tokens
        return False, tokens_needed / self.rate

class CircuitState(Enum):
    """Circuit breaker states."""
    CLOSED = "closed"      # Normal operation
    OPEN = "open"          # Blocking requests
    HALF_OPEN = "half_open"  # Testing recovery

@dataclass
class CircuitBreakerConfig:
    """Circuit breaker configuration."""
    failure_threshold: int = 5
    success_threshold: int = 2
    timeout: float = 60.0

class CircuitBreakerError(Exception):
    """Circuit breaker is open, request blocked."""
    def __init__(self, message: str, retry_after: float | None = None):
        super().__init__(message)
        self.retry_after = retry_after

class CircuitBreaker:
    """Circuit breaker to prevent cascading failures."""

    def __init__(self, config: CircuitBreakerConfig):
        self.config = config
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        self.last_failure_time: float | None = None

    def before_call(self):
        """Check if circuit allows request."""
        if self.state == CircuitState.OPEN:
            if self._should_attempt_reset():
                self._transition_to_half_open()
            else:
                retry_after = self._time_until_recovery()
                raise CircuitBreakerError(
                    "Circuit breaker OPEN due to repeated failures",
                    retry_after=retry_after
                )

    def on_success(self):
        """Handle successful request."""
        if self.state == CircuitState.HALF_OPEN:
            self.success_count += 1
            if self.success_count >= self.config.success_threshold:
                self._transition_to_closed()
        elif self.state == CircuitState.CLOSED:
            if self.failure_count > 0:
                self.failure_count = 0

    def on_failure(self, error: Exception):
        """Handle failed request."""
        logging.debug(f"Circuit breaker recorded failure: {error}")
        if self.state == CircuitState.HALF_OPEN:
            self._transition_to_open()
        elif self.state == CircuitState.CLOSED:
            self.failure_count += 1
            if self.failure_count >= self.config.failure_threshold:
                self._transition_to_open()

    def _should_attempt_reset(self) -> bool:
        """Check if timeout elapsed for recovery."""
        if self.last_failure_time is None:
            return True
        return (time.monotonic() - self.last_failure_time) >= self.config.timeout

    def _time_until_recovery(self) -> float:
        """Time remaining until recovery attempt."""
        if self.last_failure_time is None:
            return 0.0
        elapsed = time.monotonic() - self.last_failure_time
        return max(0.0, self.config.timeout - elapsed)

    def _transition_to_open(self):
        """Transition to OPEN state."""
        self.state = CircuitState.OPEN
        self.last_failure_time = time.monotonic()
        self.success_count = 0
        logging.error(f"Circuit breaker OPEN after {self.failure_count} failures")

    def _transition_to_half_open(self):
        """Transition to HALF_OPEN state."""
        self.state = CircuitState.HALF_OPEN
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker HALF_OPEN - testing recovery")

    def _transition_to_closed(self):
        """Transition to CLOSED state."""
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker CLOSED - recovered")

# Global resilience configuration
retry_config: RetryConfig | None = None
rate_limiter = None  # Initialized based on mode in main()
circuit_breaker: CircuitBreaker | None = None

# ============================================================================
# Error Formatting
# ============================================================================

def _format_api_error_message(
    tool_name: str | None,
    method: str,
    path: str,
    status_code: int,
    request_id: str | None,
    error_data: dict
) -> str:
    """
    Format API error for LLM consumption with structured, parseable details.

    Args:
        tool_name: MCP tool function name
        method: HTTP method (GET, POST, etc.)
        path: API endpoint path
        status_code: HTTP status code
        request_id: Request UUID
        error_data: Sanitized error response from API

    Returns:
        Structured error message for LLM with available details
    """
    parts = []

    # Add tool name if available
    if tool_name:
        parts.append(f"Tool: {tool_name}")

    parts.append(f"Status: {status_code}")

    # Extract error message from various API response formats
    error_message = None
    if isinstance(error_data, dict):
        # Format 1: {"message": "..."}
        if 'message' in error_data:
            error_message = error_data['message']
        # Format 2: {"error": {"message": "...", "code": ..., "errors": [...]}}  (Google APIs)
        elif 'error' in error_data:
            err = error_data['error']
            if isinstance(err, dict):
                if 'message' in err:
                    error_message = err['message']
                # Also include error code if available
                if 'code' in err and error_message:
                    error_message = f"[{err['code']}] {error_message}"
            elif isinstance(err, str):
                error_message = err
        # Format 3: {"detail": "..."} (FastAPI/OpenAPI)
        elif 'detail' in error_data:
            detail = error_data['detail']
            if isinstance(detail, str):
                error_message = detail
            elif isinstance(detail, list) and detail:
                # Validation errors: [{"loc": [...], "msg": "...", "type": "..."}]
                msgs = [d.get('msg', str(d)) if isinstance(d, dict) else str(d) for d in detail[:3]]
                error_message = "; ".join(msgs)
                if len(detail) > 3:
                    error_message += f" (+{len(detail) - 3} more)"
        # Format 4: {"errors": [...]} (GraphQL-style)
        elif 'errors' in error_data and isinstance(error_data['errors'], list):
            errors = error_data['errors']
            if errors:
                msgs = [e.get('message', str(e)) if isinstance(e, dict) else str(e) for e in errors[:3]]
                error_message = "; ".join(msgs)
                if len(errors) > 3:
                    error_message += f" (+{len(errors) - 3} more)"

    if error_message:
        parts.append(f"Error: {error_message}")

    parts.append(f"Request: {method} {path}")

    # Add request ID if available
    if request_id:
        parts.append(f"ID: {request_id}")

    return "\n".join(parts)

# ============================================================================
# Response Sanitization
# ============================================================================

# Sanitization level configuration
SANITIZATION_LEVEL = os.getenv("SANITIZATION_LEVEL", "DISABLED").upper()
if SANITIZATION_LEVEL not in ("DISABLED", "LOW", "MEDIUM", "HIGH"):
    SANITIZATION_LEVEL = "DISABLED"

# Pattern sets by level
_PATTERNS_LOW = [
    'password', 'passwd', 'pwd',
    'token', 'secret', 'private_key', 'priv_key',
]
_PATTERNS_MEDIUM = _PATTERNS_LOW + [
    'auth_token', 'access_token', 'refresh_token', 'bearer',
    'credentials', 'authorization',
]
_PATTERNS_HIGH = _PATTERNS_MEDIUM + [
    'session_id', 'sessionid', 'cookie',
    'api_key', 'apikey', 'api-key',
    'user_id', 'userid', 'account_id', 'accountid', 'internal_id',
    'ip_address', 'ipaddress', 'ip_addr',
    'hostname', 'host_name', 'server_name', 'servername',
    'internal_ip', 'private_ip',
]

def _get_sensitive_patterns() -> list:
    """Get patterns based on current sanitization level."""
    if SANITIZATION_LEVEL == "DISABLED":
        return []
    if SANITIZATION_LEVEL == "LOW":
        return _PATTERNS_LOW
    if SANITIZATION_LEVEL == "MEDIUM":
        return _PATTERNS_MEDIUM
    return _PATTERNS_HIGH

def sanitize_response(response_data: Any) -> Any:
    """Remove sensitive fields from API responses before returning to LLM.

    Controlled by SANITIZATION_LEVEL env var:
    - DISABLED: No filtering (fastest)
    - LOW: Basic secrets (password, token, secret, private_key)
    - MEDIUM: LOW + Auth fields
    - HIGH: MEDIUM + Session/IDs

    Args:
        response_data: API response (dict, list, or primitive)

    Returns:
        Sanitized response with sensitive fields removed (based on level)
    """
    sensitive_patterns = _get_sensitive_patterns()

    # Skip sanitization if disabled
    if not sensitive_patterns:
        return response_data

    def should_filter(key: str) -> bool:
        """Check if field name matches sensitive patterns."""
        key_lower = key.lower()
        return any(pattern in key_lower for pattern in sensitive_patterns)

    def sanitize_dict(data: dict) -> dict:
        """Recursively sanitize dictionary."""
        sanitized: dict[str, Any] = {}
        for key, value in data.items():
            if should_filter(key):
                # Skip sensitive fields
                logging.debug(f"Filtered sensitive field: {key}")
                continue

            # Recursively sanitize nested structures
            if isinstance(value, dict):
                sanitized[key] = sanitize_dict(value)
            elif isinstance(value, list):
                sanitized[key] = sanitize_list(value)
            else:
                sanitized[key] = value

        return sanitized

    def sanitize_list(data: list[Any]) -> list[Any]:
        """Recursively sanitize list."""
        sanitized: list[Any] = []
        for item in data:
            if isinstance(item, dict):
                sanitized.append(sanitize_dict(item))
            elif isinstance(item, list):
                sanitized.append(sanitize_list(item))
            else:
                sanitized.append(item)
        return sanitized

    # Handle different response types
    if isinstance(response_data, dict):
        return sanitize_dict(response_data)
    if isinstance(response_data, list):
        return sanitize_list(response_data)
    # Primitive types (str, int, bool, None) pass through
    return response_data

def get_safe_error_response(
    error_type: str,
    status_code: int | None = None,
    retry_after: int | None = None,
    request_id: str | None = None
) -> dict:
    """Generate safe error response for client (LLM).

    Returns generic, actionable error messages without leaking:
    - API keys, tokens, credentials
    - Internal server details, IP addresses, hostnames
    - Stack traces, exception messages
    - User data from other users

    Args:
        error_type: Type of error (auth, rate_limit, timeout, etc.)
        status_code: HTTP status code
        retry_after: Retry-After header value (seconds)
        request_id: Request correlation ID for debugging

    Returns:
        Safe error response dictionary
    """
    error_messages = {
        'AUTHENTICATION_ERROR': {
            'error': 'AUTHENTICATION_ERROR',
            'message': 'Authentication failed. Please check your credentials.',
            'retry': False
        },
        'RATE_LIMIT_ERROR': {
            'error': 'RATE_LIMIT_ERROR',
            'message': 'Rate limit exceeded. Please retry after the specified delay.',
            'retry': True
        },
        'TIMEOUT_ERROR': {
            'error': 'TIMEOUT_ERROR',
            'message': 'Request timed out. The API did not respond in time.',
            'retry': True
        },
        'VALIDATION_ERROR': {
            'error': 'VALIDATION_ERROR',
            'message': 'Invalid request parameters. Please check your input.',
            'retry': False
        },
        'NOT_FOUND': {
            'error': 'NOT_FOUND',
            'message': 'The requested resource was not found.',
            'retry': False
        },
        'PERMISSION_DENIED': {
            'error': 'PERMISSION_DENIED',
            'message': 'You do not have permission to access this resource.',
            'retry': False
        },
        'API_ERROR': {
            'error': 'API_ERROR',
            'message': 'An error occurred while calling the external API.',
            'retry': True
        },
        'INTERNAL_ERROR': {
            'error': 'INTERNAL_ERROR',
            'message': 'An internal error occurred. Please try again later.',
            'retry': False
        }
    }

    # Get base error response
    response = error_messages.get(error_type, error_messages['INTERNAL_ERROR']).copy()

    # Add optional fields
    if status_code:
        response['status_code'] = status_code

    if retry_after:
        response['retry_after_seconds'] = retry_after

    if request_id:
        response['request_id'] = request_id

    return response

class UpstreamAPIError(Exception):
    """Expected upstream API error that should not become a server traceback."""

    def __init__(
        self,
        *,
        status_code: int,
        request_id: str | None,
        method: str,
        path: str,
        tool_name: str | None,
        error_data: dict[str, Any],
        error_message: str,
    ) -> None:
        super().__init__(error_message)
        self.status_code = status_code
        self.request_id = request_id
        self.method = method
        self.path = path
        self.tool_name = tool_name
        self.error_data = error_data
        self.error_message = error_message


def _resolve_request_url(base_url: str, path: str) -> str:
    """Resolve request URL without duplicating absolute API prefixes.

    Some specs provide a base URL with a path prefix (for example
    ``https://host/admin/api/2024-01``) while operation paths are already
    absolute from the API root (for example ``/admin/api/2020-01/shop.json``).
    Passing that path directly to an httpx client with ``base_url`` would
    incorrectly produce ``.../admin/api/2024-01/admin/api/2020-01/...``.

    When the request path already starts with the same absolute API root (or
    the base URL path itself), send it against the origin instead.
    """
    if not path or "://" in path or not path.startswith("/"):
        return path

    try:
        base = httpx.URL(base_url)
    except Exception:
        return path

    if not base.host:
        return path

    if base.port is not None:
        origin = f"{base.scheme}://{base.host}:{base.port}"
    else:
        origin = f"{base.scheme}://{base.host}"

    base_path = (base.path or "").rstrip("/")
    if not base_path:
        return path

    base_parent = base_path.rsplit("/", 1)[0] if "/" in base_path else ""
    if path == base_path or path.startswith(base_path + "/"):
        return f"{origin}{path}"
    if base_parent and base_parent != "/" and (path == base_parent or path.startswith(base_parent + "/")):
        return f"{origin}{path}"
    return path


async def _make_request(
    method: str,
    path: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    multipart_file_fields: list[str] | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    tool_name: str | None = None,
    request_id: str | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any], int]:
    """Make HTTP request with retry, rate limiting, and circuit breaker."""
    global client, retry_config, rate_limiter, circuit_breaker

    if client is None:
        client = httpx.AsyncClient(
            base_url=BASE_URL,
            timeout=HTTPX_TIMEOUT,
            limits=httpx.Limits(max_keepalive_connections=MAX_KEEPALIVE_CONNECTIONS, max_connections=CONNECTION_POOL_SIZE),
            cookies=None,
            follow_redirects=True,
        )

    if headers is None:
        headers = {}
    headers.setdefault("Accept", "application/json")
    if (
        body is not None
        and method.upper() in ("POST", "PUT", "PATCH")
        and (body_content_type is None or body_content_type == "application/json")
    ):
        headers.setdefault("Content-Type", "application/json")


    if rate_limiter is not None:
        allowed, wait_time = rate_limiter.consume()
        if not allowed:
            return {
                "error": "Rate limit exceeded",
                "retry_after": wait_time,
                "message": f"Rate limit exceeded. Wait {wait_time:.1f}s"
            }, 429

    if circuit_breaker is not None:
        try:
            circuit_breaker.before_call()
        except CircuitBreakerError as e:
            return {
                "error": "Circuit breaker open",
                "retry_after": e.retry_after,
                "message": str(e)
            }, 503

    if retry_config is not None:
        max_attempts = retry_config.max_attempts
        retry_status_codes = retry_config.retry_on_status_codes or []
    else:
        max_attempts = 1
        retry_status_codes = []

    # Append raw querystring before retry loop to avoid repeated appending
    # (OAS 3.2 in: querystring — already serialized per media type)
    if raw_querystring:
        _qs_sep = "&" if "?" in path else "?"
        path = f"{path}{_qs_sep}{raw_querystring}"
    _request_url = _resolve_request_url(BASE_URL, path)

    last_error: httpx.HTTPStatusError | Exception | None = None
    _auth_retried = False  # Guard: only attempt one auth refresh per request
    for attempt in range(max_attempts):
        try:
            # Dispatch body to correct httpx kwarg based on content type
            _json = body if body_content_type is None or body_content_type == "application/json" else None
            _form_content = None
            if body_content_type == "application/x-www-form-urlencoded":
                _data = body if isinstance(body, dict) else None
                if isinstance(body, bytearray):
                    _form_content = bytes(body)
                elif isinstance(body, (bytes, str)):
                    _form_content = body
                elif body is not None and not isinstance(body, dict):
                    _form_content = str(body)
                else:
                    _form_content = None
            else:
                _data = None
            _files = None
            if body_content_type == "multipart/form-data":
                _multipart_parts: list[tuple[str, tuple[str | None, Any] | tuple[str, Any, str]]] = []
                _file_fields = set(multipart_file_fields or [])
                if isinstance(body, dict):
                    for _key, _value in body.items():
                        if _value is None:
                            continue
                        if _key in _file_fields:
                            _file_values = _value if isinstance(_value, (list, tuple)) else [_value]
                            for _file_item in _file_values:
                                if _file_item is None:
                                    continue
                                if isinstance(_file_item, str):
                                    _file_content = _file_item.encode("utf-8")
                                elif isinstance(_file_item, (bytes, bytearray)):
                                    _file_content = bytes(_file_item)
                                else:
                                    raise ValueError(
                                        f"Unsupported multipart file field '{_key}': "
                                        "expected str, bytes, or list of str/bytes, got "
                                        f"{type(_file_item).__name__}"
                                    )
                                _multipart_parts.append(
                                    (_key, (f"{_key}.bin", _file_content, "application/octet-stream"))
                                )
                        else:
                            if isinstance(_value, (dict, list)):
                                _part_value = json.dumps(_value)
                            elif isinstance(_value, bool):
                                _part_value = "true" if _value else "false"
                            else:
                                _part_value = str(_value)
                            _multipart_parts.append((_key, (None, _part_value)))
                elif body is not None:
                    if isinstance(body, str):
                        _file_content = body.encode("utf-8")
                    elif isinstance(body, (bytes, bytearray)):
                        _file_content = bytes(body)
                    else:
                        raise ValueError(
                            "Unsupported multipart file body: expected str or bytes "
                            f"for file part, got {type(body).__name__}"
                        )
                    _field_name = next(iter(_file_fields), "file")
                    _multipart_parts.append(
                        (_field_name, (f"{_field_name}.bin", _file_content, "application/octet-stream"))
                    )
                _files = _multipart_parts
            _content = None
            if body_content_type is not None and body_content_type not in ("application/json", "application/x-www-form-urlencoded", "multipart/form-data"):
                _raw = body
                if isinstance(_raw, (dict, list)):
                    _content = json.dumps(_raw).encode()
                elif isinstance(_raw, bytearray):
                    _content = bytes(_raw)
                else:
                    _content = _raw
            elif _form_content is not None:
                _content = _form_content
            response = await client.request(
                method=method,
                url=_request_url,
                params=params,
                json=_json,
                data=_data,
                files=_files,
                content=cast(Any, _content),
                headers=headers,
                cookies=cookies
            )

            # Get status code and data before raise_for_status
            status_code = response.status_code

            # Handle empty response body (204 No Content, 205 Reset Content, or empty body)
            if status_code in (204, 205) or not response.content:
                response_data = {"status": status_code, "message": "Success"}
            else:
                # Parse response JSON (graceful fallback to text)
                try:
                    response_data = response.json()
                except Exception:
                    response_data = {"text": response.text}

            # Check for HTTP errors
            if status_code >= 400:
                last_error = httpx.HTTPStatusError(
                    message=f"HTTP {status_code}",
                    request=response.request,
                    response=response
                )

                # On 401: clear stale tokens and retry once with fresh auth
                if status_code == 401 and not _auth_retried and tool_name:
                    _auth_retried = True
                    logging.warning(f"401 Unauthorized for {tool_name} - clearing token and re-authorizing")
                    _on_auth_failure()
                    _fresh_auth = await _get_auth_for_operation(tool_name)
                    if _fresh_auth.get("headers"):
                        headers = {**headers, **_fresh_auth["headers"]}
                    if _fresh_auth.get("params") and params is not None:
                        params = {**params, **_fresh_auth["params"]}
                    continue  # Retry immediately with fresh auth (no backoff)

                # Check if should retry
                if status_code not in retry_status_codes or attempt == max_attempts - 1:
                    # Don't retry or last attempt - notify circuit breaker of failure
                    if circuit_breaker is not None and status_code in [500, 502, 503, 504]:
                        circuit_breaker.on_failure(last_error)

                    # Log error information - WARNING for client errors (4xx), ERROR for server errors (5xx)
                    log_level = logging.WARNING if 400 <= status_code < 500 else logging.ERROR
                    logging.log(
                        log_level,
                        f"API request failed: {method} {path} (status: {status_code})",
                        extra={
                            "status_code": status_code,
                            "endpoint": path,
                            "method": method,
                            "attempt": attempt + 1,
                            "max_attempts": max_attempts,
                            "error_detail": str(response_data)[:500]
                        },
                        exc_info=False
                    )

                    # Sanitize response before returning/raising
                    sanitized_data = sanitize_response(response_data)

                    # Raise exception with structured error message for LLM
                    error_message = _format_api_error_message(
                        tool_name=tool_name,
                        method=method,
                        path=path,
                        status_code=status_code,
                        request_id=request_id,
                        error_data=sanitized_data
                    )
                    raise UpstreamAPIError(
                        status_code=status_code,
                        request_id=request_id,
                        method=method,
                        path=path,
                        tool_name=tool_name,
                        error_data=sanitized_data,
                        error_message=error_message,
                    )

                # Will retry - continue to backoff logic below

            else:
                # Success - notify circuit breaker
                if circuit_breaker is not None:
                    circuit_breaker.on_success()

                # Sanitize response before returning to client
                sanitized_data = sanitize_response(response_data)
                return sanitized_data, status_code

            # Exponential backoff with jitter (for retry)
            # retry_config is guaranteed non-None here since max_attempts > 1
            assert retry_config is not None
            base_delay = retry_config.base_delay
            max_delay = retry_config.max_delay
            exponential_base = retry_config.exponential_base
            use_jitter = retry_config.jitter

            # Honor Retry-After header for 429 responses
            delay: float
            if status_code == 429:
                retry_after = response.headers.get('Retry-After')
                if retry_after:
                    try:
                        delay = float(retry_after)
                    except ValueError:
                        delay = min(base_delay * (exponential_base ** attempt), max_delay)
                else:
                    delay = min(base_delay * (exponential_base ** attempt), max_delay)
            else:
                delay = min(base_delay * (exponential_base ** attempt), max_delay)

            # Add jitter
            if use_jitter:
                delay = delay * (0.5 + random.random() * 0.5)  # noqa: S311

            logging.warning(
                f"Request failed with {status_code} (attempt {attempt + 1}/{max_attempts}). "
                f"Retrying in {delay:.2f}s"
            )
            await asyncio.sleep(delay)

        except httpx.HTTPStatusError:
            # Already handled above - shouldn't reach here
            continue

        except UpstreamAPIError:
            # Expected upstream HTTP error — already logged above.
            raise

        except Exception as e:
            last_error = e

            # Network/connection errors - notify circuit breaker
            if circuit_breaker is not None:
                circuit_breaker.on_failure(e)

            # Log detailed error information for debugging (internal only)
            logging.error(
                f"Request failed with exception: {method} {path}",
                extra={
                    "endpoint": path,
                    "method": method,
                    "attempt": attempt + 1,
                    "max_attempts": max_attempts,
                    "error_type": e.__class__.__name__,
                    "error_detail": str(e)
                },
                exc_info=True
            )

            # Don't retry on non-HTTP errors - raise immediately with details
            # This ensures the LLM sees the actual error message
            error_message = (
                f"Tool: {tool_name}\n"
                f"Error: {e.__class__.__name__}\n"
                f"Request: {method} {path}\n"
                f"ID: {request_id}\n"
                f"Details: {str(e)}"
            )
            raise ConnectionError(error_message) from e

    # Log retry exhaustion with full context (internal only)
    logging.error(
        f"All retry attempts exhausted: {method} {path}",
        extra={
            "endpoint": path,
            "method": method,
            "total_attempts": max_attempts,
            "last_error_type": last_error.__class__.__name__ if last_error else "Unknown",
            "last_error_detail": str(last_error) if last_error else "Unknown"
        },
        exc_info=True
    )

    # Raise exception with error details
    if isinstance(last_error, httpx.HTTPStatusError):
        try:
            error_data = last_error.response.json()
        except Exception:
            error_data = {"error": last_error.response.text[:1000]}

        sanitized_error = sanitize_response(error_data)
        error_message = _format_api_error_message(
            tool_name=tool_name,
            method=method,
            path=path,
            status_code=last_error.response.status_code,
            request_id=request_id,
            error_data=sanitized_error
        )
        raise UpstreamAPIError(
            status_code=last_error.response.status_code,
            request_id=request_id,
            method=method,
            path=path,
            tool_name=tool_name,
            error_data=sanitized_error,
            error_message=error_message,
        )

    # Network/connection error - structured format for consistency
    error_message = (
        f"Tool: {tool_name}\n"
        f"Error: Network error after {max_attempts} attempts\n"
        f"Request: {method} {path}\n"
        f"ID: {request_id}\n"
        f"Details: {str(last_error)}"
    )
    raise ConnectionError(error_message)

# ============================================================================
# MCP Input Coercion Middleware
# ============================================================================
# Defensive middleware: some MCP clients (including Claude) may send dict/list
# arguments as JSON strings instead of native objects. This violates the MCP spec
# but is a known, widespread client-side issue. This middleware transparently
# parses stringified JSON before Pydantic validation, preventing tool call failures.
# See: https://github.com/PrefectHQ/fastmcp/issues/932

class _JsonCoercionMiddleware(Middleware):
    async def on_call_tool(self, context, call_next):
        if context.message.arguments:
            for key, value in context.message.arguments.items():
                if isinstance(value, str) and len(value) > 1 and value[0] in ('{', '['):
                    with contextlib.suppress(json.JSONDecodeError, ValueError):
                        context.message.arguments[key] = json.loads(value)
        return await call_next(context)


# ============================================================================
# Helper Functions
# ============================================================================


def _log_tool_invocation(tool_name: str, method: str, path: str, request_id: str, _redact: str = "") -> None:
    """Log tool invocation. If _redact is set, that value is partially masked in the logged path."""
    log_path = path
    if _redact:
        log_path = log_path.replace(_redact, _redact[:4] + "***" if len(_redact) > 4 else "***")

    logging.info(
        f"Tool invoked: {tool_name}",
        extra={
            "request_id": request_id,
            "tool": tool_name,
            "method": method,
            "path": log_path,
            "timeout": DEFAULT_TIMEOUT
        }
    )

def _build_path(
    template: str,
    path_params: dict[str, Any],
) -> str:
    """Build path from template and parameters.

    Args:
        template: Path template with {param} placeholders
        path_params: Dictionary of parameter names to values

    Returns:
        Path with all  placeholders replaced

    Example:
        _build_path("/files/{fileId}/copy", {"fileId": "abc123"})
        Returns: "/files/abc123/copy"
    """
    result = template
    for key, value in path_params.items():
        result = result.replace("{" + key + "}", str(value))
    # Normalize double slashes from path param substitution (e.g. "/{path}" + "/foo")
    # but preserve "://" in URL-valued params (e.g. siteUrl="https://example.com")
    while "//" in result:
        cleaned = result.replace("://", ":%SCHEME%")
        cleaned = cleaned.replace("//", "/")
        cleaned = cleaned.replace(":%SCHEME%", "://")
        if cleaned == result:
            break
        result = cleaned
    return result

async def _execute_tool_request(
    tool_name: str,
    method: str,
    path: str,
    request_id: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    multipart_file_fields: list[str] | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any] | ToolResult, int]:
    """
    Execute tool request with timeout handling and metrics recording.

    Returns:
        Tuple of (normalized_response_data_or_tool_result, status_code).
        Successful responses are normalized to dict format for Pydantic validation.
        Status code: HTTP status code from the API response.
    """
    start_time = time.time()
    try:
        # Wrap API call with timeout to prevent hanging
        async with asyncio.timeout(DEFAULT_TIMEOUT):
            # Call unified _make_request() which handles all resilience + security features
            result, status_code = await _make_request(
                method=method,
                path=path,
                params=params,
                body=body,
                body_content_type=body_content_type,
                multipart_file_fields=multipart_file_fields,
                headers=headers,
                cookies=cookies,
                tool_name=tool_name,
                request_id=request_id,
                raw_querystring=raw_querystring,
            )

        latency_ms = (time.time() - start_time) * 1000.0

        logging.info(f"Tool completed: {tool_name}", extra={"request_id": request_id, "latency_ms": f"{latency_ms:.2f}"})

        # Normalize response to dict and return with status code
        # Use "value" key for consistency with generated response models
        if isinstance(result, dict):
            return result, status_code
        return {"value": result}, status_code

    except asyncio.TimeoutError as e:
        latency_ms = (time.time() - start_time) * 1000.0

        # Request timed out
        logging.error(
            f"Tool timeout: {tool_name}",
            extra={
                "request_id": request_id,
                "tool": tool_name,
                "method": method,
                "path": path,
                "timeout": DEFAULT_TIMEOUT,
                "latency_ms": f"{latency_ms:.2f}"
            }
        )

        # Raise exception with structured error message for consistency
        # FastMCP will catch this and set isError: true in MCP response
        timeout_message = (
            f"Tool: {tool_name}\n"
            f"Error: Request timed out after {DEFAULT_TIMEOUT} seconds\n"
            f"ID: {request_id}"
        )
        raise asyncio.TimeoutError(timeout_message) from e

    except UpstreamAPIError as e:
        latency_ms = (time.time() - start_time) * 1000.0
        return ToolResult(
            content=e.error_message,
            structured_content={
                "ok": False,
                "status": e.status_code,
                "request_id": e.request_id,
                "method": e.method,
                "path": e.path,
                "error": e.error_message,
                "details": e.error_data,
            },
        ), e.status_code

    except ValueError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except ConnectionError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except Exception:
        latency_ms = (time.time() - start_time) * 1000.0
        raise
# ============================================================================
# Authentication
# ============================================================================

# Authentication scheme priority (most secure first)
AUTH_SCHEME_PRIORITY = [
    'OAuth2Auth',
    'CustomAppAccessToken',
]

# Initialize authentication handlers at server startup
_auth_handlers: dict[str, Any] = {}
try:
    _auth_handlers["OAuth2Auth"] = _auth.OAuth2Auth()
    logging.info("Authentication configured: OAuth2Auth")
except ValueError as e:
    # Extract credential names from error message (first sentence before "Leave empty")
    error_msg = str(e).split("Leave empty")[0].strip()
    logging.warning(f"Credentials for OAuth2Auth not configured: {error_msg}")
    _auth_handlers["OAuth2Auth"] = None
try:
    _auth_handlers["CustomAppAccessToken"] = _auth.APIKeyAuth(env_var="API_KEY", location="header", param_name="X-Shopify-Access-Token")
    logging.info("Authentication configured: CustomAppAccessToken")
except ValueError as e:
    # Extract credential names from error message (first sentence before "Leave empty")
    error_msg = str(e).split("Leave empty")[0].strip()
    logging.warning(f"Credentials for CustomAppAccessToken not configured: {error_msg}")
    _auth_handlers["CustomAppAccessToken"] = None

# Warn only if NO auth handlers were successfully configured
if all(handler is None for handler in _auth_handlers.values()):
    logging.warning("Server will attempt unauthenticated requests (may fail if API requires auth)")

def _on_auth_failure() -> None:
    """Clear tokens on all auth handlers that support it (called on 401 response)."""
    for scheme_name, handler in _auth_handlers.items():
        if handler is not None and hasattr(handler, 'clear_token'):
            handler.clear_token()
            logging.info(f"Cleared token for auth scheme '{scheme_name}' after 401")

async def _get_auth_for_operation(operation_id: str) -> dict[str, dict[str, str]]:
    """Get authentication for specific operation (handles multi-auth with OR/AND logic).

    Args:
        operation_id: The operation ID (tool name) to get auth for

    Returns:
        Dictionary with 'headers', 'params', 'cookies', 'path_params' keys containing auth data
    """
    result: dict[str, dict[str, str]] = {"headers": {}, "params": {}, "cookies": {}, "path_params": {}}

    # Get auth requirements for this operation from auth module
    # Map structure: operation_id -> [[scheme1], [scheme2, scheme3]] (OR of AND groups)
    required_schemes = _auth.OPERATION_AUTH_MAP.get(operation_id)

    if required_schemes is None:
        # CRITICAL: Operation missing from OPERATION_AUTH_MAP
        # This indicates a bug in auth config generation - operation should be in map
        # Do NOT fall back to global security - this masks bugs and creates security gaps
        logging.error(
            f"SECURITY ERROR: Operation '{operation_id}' not found in OPERATION_AUTH_MAP. "
            f"This indicates a bug in auth config generation. "
            f"All operations should be explicitly mapped. "
            f"Proceeding with no authentication - REQUEST WILL LIKELY FAIL."
        )
        # Return empty auth (will likely fail at API, but surfaces the bug)
        return result

    # Sort OR groups by security priority (most secure first)
    # Each OR group is sorted internally by scheme priority
    def get_scheme_priority(scheme_name: str) -> int:
        """Get priority index for scheme (lower = higher priority)."""
        try:
            return AUTH_SCHEME_PRIORITY.index(scheme_name)
        except ValueError:
            # Unknown scheme - put at end
            return len(AUTH_SCHEME_PRIORITY)

    # Sort each OR group by priority, then sort OR groups by their best (lowest) priority
    sorted_schemes = []
    for scheme_list in required_schemes:
        # Sort schemes within AND group by priority (most secure first)
        sorted_group = sorted(scheme_list, key=get_scheme_priority)
        sorted_schemes.append(sorted_group)

    # Sort OR groups by the priority of their first (most secure) scheme
    sorted_schemes.sort(key=lambda group: get_scheme_priority(group[0]) if group else float('inf'))

    # Try each OR group in priority order (most secure first)
    for scheme_list in sorted_schemes:
        headers = {}
        params = {}
        cookies = {}
        path_params = {}
        all_succeeded = True

        # Handle AND group (multiple schemes in same list - all must succeed)
        for scheme_name in scheme_list:
            handler = _auth_handlers.get(scheme_name)
            if not handler:
                # Handler not configured (env vars missing)
                all_succeeded = False
                logging.debug(f"Auth scheme '{scheme_name}' not configured for {operation_id}")
                break

            try:
                # Try all injection methods (headers, params, cookies, path_params)
                # OAuth2 methods are async (token refresh/authorize); others are sync.
                import inspect as _inspect
                if hasattr(handler, 'get_auth_headers'):
                    _h = handler.get_auth_headers()
                    headers.update(await _h if _inspect.isawaitable(_h) else _h)
                if hasattr(handler, 'get_auth_params'):
                    _p = handler.get_auth_params()
                    params.update(await _p if _inspect.isawaitable(_p) else _p)
                if hasattr(handler, 'get_auth_cookies'):
                    _c = handler.get_auth_cookies()
                    cookies.update(await _c if _inspect.isawaitable(_c) else _c)
                if hasattr(handler, 'get_auth_path_params'):
                    _pp = handler.get_auth_path_params()
                    path_params.update(await _pp if _inspect.isawaitable(_pp) else _pp)
            except Exception as e:
                logging.debug(f"Auth scheme '{scheme_name}' failed for {operation_id}: {e}")
                all_succeeded = False
                break

        # If all schemes in AND group succeeded, use this auth
        if all_succeeded and (headers or params or cookies or path_params):
            result["headers"] = headers
            result["params"] = params
            result["cookies"] = cookies
            result["path_params"] = path_params
            logging.debug(f"Using auth for {operation_id}: schemes={scheme_list}")
            return result

    # No auth configured or all OR groups failed
    if required_schemes and required_schemes != [[]]:
        logging.warning(f"No configured auth handler found for {operation_id} (requires: {required_schemes})")

    return result

# ============================================================================
# FastMCP Server Initialization
# ============================================================================

mcp = FastMCP("Shopify Admin", middleware=[_JsonCoercionMiddleware()])

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def list_application_charges_202001() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all application charges for the store. This endpoint returns historical and current charges associated with the application."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_charges_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_charges_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_charges_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def get_application_charge_202001(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific application charge by its ID. Use this to fetch information about a charge created for your app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.Deprecated202001GetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def list_application_credits_202001() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits for the store. This endpoint returns a collection of credits that have been issued to the application."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, assignedfulfillmentorder, shipping-and-fulfillment/assignedfulfillmentorder
@mcp.tool()
async def list_assigned_fulfillment_orders_legacy(
    assignment_status: Any | None = Field(None, description="Filter results by the fulfillment request status: use 'cancellation_requested' for orders where the merchant requested cancellation, 'fulfillment_requested' for pending fulfillment requests, or 'fulfillment_accepted' for approved requests ready for fulfillment creation."),
    location_ids: Any | None = Field(None, description="Restrict results to specific assigned locations by providing their location IDs as a comma-separated list or array. Omit to retrieve fulfillment orders from all assigned locations."),
) -> dict[str, Any] | ToolResult:
    """Retrieves fulfillment orders assigned to your app on the shop, optionally filtered by assignment status and location. Use this to monitor fulfillment requests, acceptances, and cancellations across your assigned locations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetAssignedFulfillmentOrdersRequest(
            query=_models.Deprecated202001GetAssignedFulfillmentOrdersRequestQuery(assignment_status=assignment_status, location_ids=location_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_assigned_fulfillment_orders_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/assigned_fulfillment_orders.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_assigned_fulfillment_orders_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_assigned_fulfillment_orders_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_assigned_fulfillment_orders_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, blog, online-store/blog
@mcp.tool()
async def get_blog(blog_id: str = Field(..., description="The unique identifier of the blog to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve a single blog by its ID from the Shopify online store. Returns detailed information about the specified blog."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetBlogsParamBlogIdRequest(
            path=_models.Deprecated202001GetBlogsParamBlogIdRequestPath(blog_id=blog_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_blog: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_blog")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_blog", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_blog",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, blog, online-store/blog
@mcp.tool()
async def delete_blog(blog_id: str = Field(..., description="The unique identifier of the blog to delete. This is a required string value that specifies which blog resource to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently delete a blog from the online store. This action cannot be undone and will remove all associated blog posts and content."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteBlogsParamBlogIdRequest(
            path=_models.Deprecated202001DeleteBlogsParamBlogIdRequestPath(blog_id=blog_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_blog: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_blog")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_blog", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_blog",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_articles_for_blog(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="Maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: 'published' for published articles only, 'unpublished' for unpublished articles only, or 'any' for all articles regardless of status (defaults to 'any')."),
    handle: Any | None = Field(None, description="Retrieve a single article by its URL-friendly handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those authored by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Results are paginated using link headers in the response; use the provided links for navigation rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202001GetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.Deprecated202001GetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_articles_for_blog: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_articles_for_blog")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_articles_for_blog", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_articles_for_blog",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_count_for_blog_2020_01(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to count articles."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specific blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetBlogsParamBlogIdArticlesCountRequest(
            path=_models.Deprecated202001GetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.Deprecated202001GetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_count_for_blog_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_count_for_blog_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_count_for_blog_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_count_for_blog_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_202001(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article."),
    article_id: str = Field(..., description="The unique identifier of the article to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single article from a blog by its ID. Use this to fetch detailed information about a specific article in the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202001GetBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def delete_article_202001(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to delete."),
    article_id: str = Field(..., description="The unique identifier of the article to delete."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes an article from a blog. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202001DeleteBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_article_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_article_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_article_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_article_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def update_carrier_service_202001(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service configuration. Only the application that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202001UpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def delete_carrier_service_2020_01(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a carrier service from the store. This action cannot be undone and will remove the carrier service configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202001DeleteCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_carrier_service_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_carrier_service_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_carrier_service_2020_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_carrier_service_2020_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts
@mcp.tool()
async def get_abandoned_checkouts_count_202001(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCheckoutsCountRequest(
            query=_models.Deprecated202001GetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, checkout, sales-channels/checkout
@mcp.tool()
async def list_shipping_rates_for_checkout_2020_01(token: str = Field(..., description="The unique identifier for the checkout. This token is used to retrieve shipping rates specific to that checkout.")) -> dict[str, Any] | ToolResult:
    """Retrieves available shipping rates for a checkout. Poll this endpoint until rates become available, then use the returned rates to display updated pricing (subtotal, tax, total) or apply a rate by updating the checkout's shipping line."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCheckoutsParamTokenShippingRatesRequest(
            path=_models.Deprecated202001GetCheckoutsParamTokenShippingRatesRequestPath(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shipping_rates_for_checkout_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/checkouts/{token}/shipping_rates.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/checkouts/{token}/shipping_rates.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_rates_for_checkout_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_rates_for_checkout_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_rates_for_checkout_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def delete_collection_listing_202001(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to delete.")) -> dict[str, Any] | ToolResult:
    """Remove a collection listing to unpublish a collection from your sales channel or app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCollectionListingsParamCollectionListingIdRequest(
            path=_models.Deprecated202001DeleteCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_collection_listing_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_collection_listing_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_collection_listing_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_collection_listing_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def list_collection_product_ids_legacy(
    collection_listing_id: str = Field(..., description="The unique identifier of the collection listing whose published product IDs you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of product IDs to return per request, ranging from 1 to 1000 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve the product IDs that are published to a specific collection. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectionListingsParamCollectionListingIdProductIdsRequest(
            path=_models.Deprecated202001GetCollectionListingsParamCollectionListingIdProductIdsRequestPath(collection_listing_id=collection_listing_id),
            query=_models.Deprecated202001GetCollectionListingsParamCollectionListingIdProductIdsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_product_ids_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collection_listings/{collection_listing_id}/product_ids.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collection_listings/{collection_listing_id}/product_ids.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_product_ids_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_product_ids_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_product_ids_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def get_collection_202001(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID. Use this to fetch collection information such as name, description, and other metadata."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectionsParamCollectionIdRequest(
            path=_models.Deprecated202001GetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def list_collection_products_202001(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, ranging from 1 to 250 products (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted according to the collection's sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectionsParamCollectionIdProductsRequest(
            path=_models.Deprecated202001GetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.Deprecated202001GetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def list_collects_2020_01(limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250 items. Defaults to 50 if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of collects (product-to-collection associations). Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectsRequest(
            query=_models.Deprecated202001GetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collects_count_202001(collection_id: int | None = Field(None, description="Filter the count to only include collects belonging to a specific collection. If omitted, returns the total count of all collects across the store.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects, optionally filtered by a specific collection. Useful for understanding the size of a collection or the total number of product-to-collection associations in the store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectsCountRequest(
            query=_models.Deprecated202001GetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collect_202001(collect_id: str = Field(..., description="The unique identifier of the collect resource to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCollectsParamCollectIdRequest(
            path=_models.Deprecated202001GetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def remove_product_from_collection_2020_01(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the link between a product and a collection.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCollectsParamCollectIdRequest(
            path=_models.Deprecated202001DeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection_2020_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection_2020_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, comment, online-store/comment
@mcp.tool()
async def approve_comment(comment_id: str = Field(..., description="The unique identifier of the comment to approve. This ID must correspond to an existing comment in the system.")) -> dict[str, Any] | ToolResult:
    """Approves a comment in the online store, making it visible to customers. This operation is used to moderate and publish pending comments."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCommentsParamCommentIdApproveRequest(
            path=_models.Deprecated202001CreateCommentsParamCommentIdApproveRequestPath(comment_id=comment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for approve_comment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/comments/{comment_id}/approve.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/comments/{comment_id}/approve.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("approve_comment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("approve_comment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="approve_comment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, comment, online-store/comment
@mcp.tool()
async def mark_comment_not_spam(comment_id: str = Field(..., description="The unique identifier of the comment to mark as not spam.")) -> dict[str, Any] | ToolResult:
    """Marks a specific comment as not spam, removing any spam classification it may have had. This operation is used to correct false-positive spam flagging in the online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCommentsParamCommentIdNotSpamRequest(
            path=_models.Deprecated202001CreateCommentsParamCommentIdNotSpamRequestPath(comment_id=comment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for mark_comment_not_spam: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/comments/{comment_id}/not_spam.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/comments/{comment_id}/not_spam.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("mark_comment_not_spam")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("mark_comment_not_spam", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="mark_comment_not_spam",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, comment, online-store/comment
@mcp.tool()
async def restore_comment(comment_id: str = Field(..., description="The unique identifier of the comment to restore. This must be a valid comment ID that was previously removed.")) -> dict[str, Any] | ToolResult:
    """Restores a previously removed comment in the online store. This operation reactivates a comment that was previously deleted or hidden."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCommentsParamCommentIdRestoreRequest(
            path=_models.Deprecated202001CreateCommentsParamCommentIdRestoreRequestPath(comment_id=comment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for restore_comment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/comments/{comment_id}/restore.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/comments/{comment_id}/restore.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("restore_comment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("restore_comment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="restore_comment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, comment, online-store/comment
@mcp.tool()
async def mark_comment_as_spam(comment_id: str = Field(..., description="The unique identifier of the comment to mark as spam.")) -> dict[str, Any] | ToolResult:
    """Marks a specific comment as spam in the online store. This action flags the comment for moderation and helps maintain comment quality."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCommentsParamCommentIdSpamRequest(
            path=_models.Deprecated202001CreateCommentsParamCommentIdSpamRequestPath(comment_id=comment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for mark_comment_as_spam: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/comments/{comment_id}/spam.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/comments/{comment_id}/spam.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("mark_comment_as_spam")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("mark_comment_as_spam", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="mark_comment_as_spam",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def list_countries_202001() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all countries available in the Shopify system. This endpoint provides country data for store configuration and regional settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_countries_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_countries_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_countries_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def get_countries_count_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of countries available in the Shopify store. This is useful for understanding the scope of country data without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/countries/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_countries_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_countries_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_countries_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def get_country_legacy(country_id: str = Field(..., description="The unique identifier for the country to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a specific country, including its provinces and tax settings available in the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCountriesParamCountryIdRequest(
            path=_models.Deprecated202001GetCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_country_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_country_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_country_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_country_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def delete_country_202001(country_id: str = Field(..., description="The unique identifier of the country to delete. This is a required string value that specifies which country record to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a country from the store. This action cannot be undone and will remove the country from all applicable store configurations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCountriesParamCountryIdRequest(
            path=_models.Deprecated202001DeleteCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_country_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_country_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_country_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_country_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def list_provinces_for_country_202001(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve provinces. This ID must correspond to a valid country in the Shopify system.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of provinces or states for a specified country. Use this to populate province/state selectors in address forms or location-based workflows."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCountriesParamCountryIdProvincesRequest(
            path=_models.Deprecated202001GetCountriesParamCountryIdProvincesRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_provinces_for_country_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/countries/{country_id}/provinces.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/countries/{country_id}/provinces.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_provinces_for_country_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_provinces_for_country_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_provinces_for_country_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def get_province_count_for_country_202001(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve the province count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of provinces or states for a specified country. Useful for understanding administrative divisions within a country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCountriesParamCountryIdProvincesCountRequest(
            path=_models.Deprecated202001GetCountriesParamCountryIdProvincesCountRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_count_for_country_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/countries/{country_id}/provinces/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/countries/{country_id}/provinces/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_count_for_country_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_count_for_country_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_count_for_country_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency
@mcp.tool()
async def list_currencies_202001() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies that are enabled and available on the shop for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def count_custom_collections_2020_01(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain this specific product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' for all collections regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomCollectionsCountRequest(
            query=_models.Deprecated202001GetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customersavedsearch, customers/customersavedsearch
@mcp.tool()
async def list_customers_for_saved_search_2020_01(
    customer_saved_search_id: str = Field(..., description="The unique identifier of the customer saved search to retrieve customers for."),
    order: Any | None = Field(None, description="Specifies the field and direction to sort results by (e.g., 'last_order_date DESC'). Defaults to sorting by last order date in descending order."),
    limit: Any | None = Field(None, description="The maximum number of customer records to return per request. Defaults to 50 and cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all customers matching the criteria defined in a customer saved search. Results can be ordered and paginated."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequest(
            path=_models.Deprecated202001GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestPath(customer_saved_search_id=customer_saved_search_id),
            query=_models.Deprecated202001GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestQuery(order=order, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_for_saved_search_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customer_saved_searches/{customer_saved_search_id}/customers.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customer_saved_searches/{customer_saved_search_id}/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_for_saved_search_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_for_saved_search_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_for_saved_search_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customers_202001(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer IDs."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 and cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the store. Results are paginated using link headers in the response; use the provided links for navigation rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomersRequest(
            query=_models.Deprecated202001GetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customers_count_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the Shopify store. Useful for understanding customer base size without fetching individual customer records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customer_202001(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single customer by their unique identifier from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomersParamCustomerIdRequest(
            path=_models.Deprecated202001GetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def update_customer_202001(customer_id: str = Field(..., description="The unique identifier of the customer to update. This ID is required to specify which customer record should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in Shopify. Modify customer details such as email, name, phone, and other profile attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateCustomersParamCustomerIdRequest(
            path=_models.Deprecated202001UpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def delete_customer_v202001(customer_id: str = Field(..., description="The unique identifier of the customer to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. Note that a customer cannot be deleted if they have any existing orders associated with their account."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCustomersParamCustomerIdRequest(
            path=_models.Deprecated202001DeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_v202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_v202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_v202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_v202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def generate_customer_account_activation_url_2020_01(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the account activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.Deprecated202001CreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def list_customer_addresses_202001(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202001GetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def create_customer_address(customer_id: str = Field(..., description="The unique identifier of the customer for whom the address is being created.")) -> dict[str, Any] | ToolResult:
    """Creates a new address for a customer. The address is added to the customer's address list and can be set as their default billing or shipping address."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202001CreateCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_customer_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_customer_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_customer_address", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_customer_address",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def bulk_update_customer_addresses_202001(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to target for the bulk operation. The order may be significant depending on the operation type being performed."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., delete, set as default). Refer to API documentation for valid operation values."),
) -> dict[str, Any] | ToolResult:
    """Performs bulk operations on multiple customer addresses, allowing you to update, delete, or modify address records in a single request for a specific customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.Deprecated202001UpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.Deprecated202001UpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def get_customer_address_202001(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific customer address by customer ID and address ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202001GetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def update_customer_address_2020_01(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the specific address to update for the customer."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing customer address by customer ID and address ID. Use this to modify address details such as street, city, postal code, or other address information for a specific customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202001UpdateCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_address_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_address_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_address_2020_01", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_address_2020_01",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def delete_customer_address_202001(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific address from a customer's address list. This operation permanently deletes the address record associated with the given customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202001DeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def set_customer_default_address_202001(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address will be updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Sets a specific address as the default address for a customer. This operation updates the customer's default address preference in their address book."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.Deprecated202001UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_customer_default_address_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_customer_default_address_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_customer_default_address_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_customer_default_address_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customer_orders_202001(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports filtering and sorting through standard Order resource query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetCustomersParamCustomerIdOrdersRequest(
            path=_models.Deprecated202001GetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def send_customer_invite_202001(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation email to a customer, allowing them to create or access their account on the store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.Deprecated202001CreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def list_events_202001(
    limit: Any | None = Field(None, description="Maximum number of events to return per request, between 1 and 250 (defaults to 50)."),
    filter_: Any | None = Field(None, alias="filter", description="Filter events by specific criteria to narrow results to matching events."),
    verb: Any | None = Field(None, description="Filter events by type or action verb to show only events of a certain category."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetEventsRequest(
            query=_models.Deprecated202001GetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_events_count_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. This endpoint provides a quick way to determine the number of events without fetching the full event list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_event_2020_01(event_id: str = Field(..., description="The unique identifier of the event to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single event by its ID from the Shopify admin. Use this to fetch detailed information about a specific event that occurred in your store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetEventsParamEventIdRequest(
            path=_models.Deprecated202001GetEventsParamEventIdRequestPath(event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def get_fulfillment_order_202001(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID. Use this to fetch current status, line items, and fulfillment information for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.Deprecated202001GetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def cancel_fulfillment_order_2020_01(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation is useful when a fulfillment order must be abandoned before it's been fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def send_fulfillment_order_cancellation_request_2020_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order in the system."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request. This provides context to the fulfillment service about why the order is being cancelled."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider that the order should be cancelled if not yet fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def accept_fulfillment_order_cancellation_request_2020_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to accept the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, confirming that the fulfillment service should cancel the specified order. Optionally include a reason message for the acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def reject_fulfillment_order_cancellation_request_2020_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason may be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this to deny a cancellation and keep the fulfillment order active."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def close_fulfillment_order_2020_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or note explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def send_fulfillment_request_202001(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request, such as special instructions or notes for the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional array of fulfillment order line items to request for fulfillment. If omitted, all line items in the fulfillment order will be requested."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally specifying which line items to fulfill and including an optional message."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def accept_fulfillment_request_2020_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def reject_fulfillment_request_202001(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202001CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove
@mcp.tool()
async def list_locations_for_fulfillment_order_move_2020_01(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves an alphabetically sorted list of locations where a fulfillment order can be moved. Use this to determine valid destination locations before initiating a fulfillment order transfer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.Deprecated202001GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice
@mcp.tool()
async def list_fulfillment_services_legacy_202001() -> dict[str, Any] | ToolResult:
    """Retrieve all fulfillment services available for the store. This endpoint returns a list of fulfillment service configurations that can be used to manage order fulfillment."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/fulfillment_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_services_legacy_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_services_legacy_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_services_legacy_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice
@mcp.tool()
async def delete_fulfillment_service_202001(fulfillment_service_id: str = Field(..., description="The unique identifier of the fulfillment service to delete. This ID is required to specify which fulfillment service should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently delete a fulfillment service from your Shopify store. This operation removes the fulfillment service and its associated configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteFulfillmentServicesParamFulfillmentServiceIdRequest(
            path=_models.Deprecated202001DeleteFulfillmentServicesParamFulfillmentServiceIdRequestPath(fulfillment_service_id=fulfillment_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_fulfillment_service_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillment_services/{fulfillment_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillment_services/{fulfillment_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_fulfillment_service_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_fulfillment_service_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_fulfillment_service_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_202001(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This ID references a specific fulfillment order that has been previously created.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment order. This operation marks the specified fulfillment as cancelled and updates its status accordingly."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.Deprecated202001CreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_tracking_2020_01(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates the tracking information for a fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.Deprecated202001CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def list_gift_cards_2020_01(
    status: Any | None = Field(None, description="Filter results to gift cards with a specific status: enabled to show only active gift cards, or disabled to show only inactive gift cards."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of gift cards, optionally filtered by status. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetGiftCardsRequest(
            query=_models.Deprecated202001GetGiftCardsRequestQuery(status=status, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_gift_cards_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/gift_cards.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_gift_cards_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_gift_cards_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_gift_cards_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def count_gift_cards(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetGiftCardsCountRequest(
            query=_models.Deprecated202001GetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_gift_cards: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_gift_cards")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_gift_cards", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_gift_cards",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def search_gift_cards_202001(
    order: Any | None = Field(None, description="The field and direction to sort results by. Defaults to most recently disabled gift cards first. Common sortable fields include created_at, updated_at, disabled_at, balance, initial_value, amount_spent, email, and last_characters."),
    query: Any | None = Field(None, description="The search query text to match against indexed gift card fields such as email, balance, initial value, amount spent, and last characters."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for gift cards using indexed fields like balance, email, creation date, and more. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetGiftCardsSearchRequest(
            query=_models.Deprecated202001GetGiftCardsSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_gift_cards_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/gift_cards/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_gift_cards_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_gift_cards_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_gift_cards_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def update_gift_card_202001(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Update an existing gift card's expiry date, note, or template suffix. Note that the gift card's balance cannot be modified through the API."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateGiftCardsParamGiftCardIdRequest(
            path=_models.Deprecated202001UpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def disable_gift_card_202001(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.Deprecated202001CreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def list_inventory_items_202001(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request, between 1 and 250 items (defaults to 50)."),
    ids: int | None = Field(None, description="Filter results to only inventory items with the specified IDs. Provide as a comma-separated list of integers."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items. Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetInventoryItemsRequest(
            query=_models.Deprecated202001GetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def update_inventory_item_legacy_202001(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to update. This ID is required to specify which inventory item should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing inventory item in your Shopify store. Use this to modify inventory item properties such as tracked status, cost, and other attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateInventoryItemsParamInventoryItemIdRequest(
            path=_models.Deprecated202001UpdateInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_inventory_item_legacy_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_inventory_item_legacy_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_inventory_item_legacy_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_inventory_item_legacy_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def list_inventory_levels_202001(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across your store's locations. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetInventoryLevelsRequest(
            query=_models.Deprecated202001GetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def delete_inventory_level_202001(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted."),
    location_id: int | None = Field(None, description="The unique identifier of the location from which the inventory item should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This operation disconnects the inventory item from that location; note that every inventory item must maintain at least one inventory level, so you must connect the item to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteInventoryLevelsRequest(
            query=_models.Deprecated202001DeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_locations_202001() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. Use this to get a complete list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_locations_count_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. This is useful for understanding inventory distribution across fulfillment locations without fetching full location details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_location_202001(location_id: str = Field(..., description="The unique identifier of the location to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single location by its ID from the Shopify inventory system. Use this to fetch detailed information about a specific store location."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetLocationsParamLocationIdRequest(
            path=_models.Deprecated202001GetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_inventory_levels_for_location_202001(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves all inventory levels for a specific location. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.Deprecated202001GetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafields_count_2020_01() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields associated with a resource. Use this to determine how many custom metadata fields exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def update_metafield_legacy(metafield_id: str = Field(..., description="The unique identifier of the metafield to update. This ID is returned when a metafield is created and is required to target the specific metafield for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing metafield by its ID. Use this to modify metafield properties such as namespace, key, value, and value type."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateMetafieldsParamMetafieldIdRequest(
            path=_models.Deprecated202001UpdateMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_metafield_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_metafield_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_metafield_legacy", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_metafield_legacy",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def list_orders(
    ids: Any | None = Field(None, description="Filter results to only orders with the specified IDs. Provide as a comma-separated list of order IDs."),
    limit: Any | None = Field(None, description="Maximum number of orders to return per page. Defaults to 50; maximum allowed is 250."),
    processed_at_min: Any | None = Field(None, description="Show only orders processed at or after this date. Use ISO 8601 format with timezone offset."),
    processed_at_max: Any | None = Field(None, description="Show only orders processed at or before this date. Use ISO 8601 format with timezone offset."),
    attribution_app_id: Any | None = Field(None, description="Filter orders by the app that created them. Use the app's ID, or set to 'current' to show orders created by the app making this request."),
    status: Any | None = Field(None, description="Filter orders by fulfillment status: open (default), closed, cancelled, or any (including archived orders)."),
    financial_status: Any | None = Field(None, description="Filter orders by payment status: authorized, pending, paid, partially_paid, refunded, voided, partially_refunded, unpaid, or any (default)."),
    fulfillment_status: Any | None = Field(None, description="Filter orders by shipment status: shipped (fulfilled), partial (partially shipped), unshipped (not yet shipped), unfulfilled (unshipped or partial), or any (default)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of orders from the store. Results are paginated using link headers in the response; use the provided links for navigation rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersRequest(
            query=_models.Deprecated202001GetOrdersRequestQuery(ids=ids, limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, attribution_app_id=attribution_app_id, status=status, financial_status=financial_status, fulfillment_status=fulfillment_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_orders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/orders.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_orders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_orders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_orders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def get_orders_count(
    status: Any | None = Field(None, description="Filter orders by their fulfillment state. Defaults to 'open' if not specified. Valid values are 'open' (unfulfilled orders), 'closed' (completed orders), or 'any' (all orders regardless of state)."),
    financial_status: Any | None = Field(None, description="Filter orders by their payment state. Defaults to 'any' if not specified. Valid values are 'authorized', 'pending', 'paid', 'refunded', 'voided', or 'any' (all payment states)."),
    fulfillment_status: Any | None = Field(None, description="Filter orders by their shipping state. Defaults to 'any' if not specified. Valid values are 'shipped' (fully fulfilled), 'partial' (partially shipped), 'unshipped' (not yet shipped), 'unfulfilled' (not shipped or partially shipped), or 'any' (all shipping states)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the count of orders filtered by status, financial status, and fulfillment status. Useful for obtaining order statistics without fetching full order details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersCountRequest(
            query=_models.Deprecated202001GetOrdersCountRequestQuery(status=status, financial_status=financial_status, fulfillment_status=fulfillment_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_orders_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/orders/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_orders_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_orders_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_orders_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def get_order_legacy(order_id: str = Field(..., description="The unique identifier of the order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific order by its ID. Use this to fetch complete order information including items, customer details, and fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def update_order(order_id: str = Field(..., description="The unique identifier of the order to update. This is a required string value that specifies which order will be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing order in Shopify. Use this operation to modify order details such as status, customer information, or other order attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateOrdersParamOrderIdRequest(
            path=_models.Deprecated202001UpdateOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def delete_order(order_id: str = Field(..., description="The unique identifier of the order to delete. This is a required string value that identifies which order should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes an order from the store. Note that orders connected to online payment gateways cannot be deleted due to payment processing requirements."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteOrdersParamOrderIdRequest(
            path=_models.Deprecated202001DeleteOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def cancel_order(
    order_id: str = Field(..., description="The unique identifier of the order to cancel."),
    amount: Any | None = Field(None, description="The monetary amount to refund. When provided, Shopify attempts to void or refund the payment based on its current status. For multi-currency orders, the currency parameter is required when this is set."),
    currency: Any | None = Field(None, description="The currency code for the refund amount in multi-currency orders. Required whenever the amount parameter is provided for orders with multiple currencies."),
    reason: Any | None = Field(None, description="The reason for cancellation. Valid options are: customer, inventory, fraud, declined, or other. Defaults to other if not specified."),
    email: Any | None = Field(None, description="Whether to send a cancellation notification email to the customer. Defaults to false if not specified."),
    refund: Any | None = Field(None, description="Refund transaction details for complex refund scenarios. Use this for advanced refund operations beyond simple amount-based refunds. See the Refund API documentation for structure details."),
) -> dict[str, Any] | ToolResult:
    """Cancels an order and optionally processes a refund. Orders with fulfillment objects cannot be canceled. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdCancelRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdCancelRequestPath(order_id=order_id),
            query=_models.Deprecated202001CreateOrdersParamOrderIdCancelRequestQuery(amount=amount, currency=currency, reason=reason, email=email, refund=refund)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/cancel.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def close_order(order_id: str = Field(..., description="The unique identifier of the order to close. This is the Shopify order ID.")) -> dict[str, Any] | ToolResult:
    """Closes an order, preventing further modifications. This operation marks the order as closed in the system."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdCloseRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdCloseRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/close.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def list_fulfillment_orders_for_order_2020_01(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order. Fulfillment orders represent the items in an order that need to be fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders_for_order_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders_for_order_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders_for_order_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders_for_order_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def list_fulfillments_for_order(
    order_id: str = Field(..., description="The unique identifier of the order whose fulfillments you want to retrieve."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the limit parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_for_order_legacy_202001(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for specified line items in an order. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order_legacy_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order_legacy_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order_legacy_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order_legacy_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_count_for_order(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding fulfillment status and logistics tracking without fetching full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_count_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_count_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_count_for_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_count_for_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order. Use this to access fulfillment status, tracking information, and line item details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202001UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def complete_fulfillment_2020_01(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped and the order fulfillment process is finished."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent
@mcp.tool()
async def list_fulfillment_events_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all events associated with a specific fulfillment for an order. Events track status changes and milestones throughout the fulfillment lifecycle."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_events_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_events_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_events_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_events_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def reopen_order(order_id: str = Field(..., description="The unique identifier of the order to re-open. This must be a valid order ID from your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Re-opens a previously closed order, allowing it to be modified and processed again. This operation restores a closed order to an active state."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdOpenRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdOpenRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reopen_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reopen_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reopen_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reopen_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def list_order_refunds_202001(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="Maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays refund amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of refunds for a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202001GetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def create_order_refund_202001(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Specifies shipping refund details. Use full_refund to refund all remaining shipping, or provide a specific amount to refund (which takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each specifying the line item ID, quantity to refund, restock type (no_restock, cancel, or return), and location ID for restocking (required for cancel and return types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Should be calculated using the calculate endpoint before submission."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transactions to submit. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202001CreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def calculate_order_refund_2020_01(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping cost. The amount property takes precedence over full_refund."),
    refund_line_items: Any | None = Field(None, description="List of line items to refund, each specifying the line item ID, quantity to refund, restock instructions (no_restock, cancel, or return), and optionally the location ID where items should be restocked. If location_id is omitted for return or cancel restocks, the endpoint will suggest an appropriate location."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required whenever a shipping amount is specified, particularly important for multi-currency orders."),
) -> dict[str, Any] | ToolResult:
    """Calculate refund transactions for an order based on line items and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund, including any necessary adjustments to restock instructions."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.Deprecated202001CreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_order_refund_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_order_refund_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_order_refund_2020_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_order_refund_2020_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def get_refund_for_order(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's native currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific refund associated with an order. Use this to view refund information including amounts, line items, and status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.Deprecated202001GetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund_for_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund_for_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def list_order_risks_2020_01(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve risk assessments.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def create_order_risk_202001(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202001CreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def get_order_risk_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk assessment."),
    risk_id: str = Field(..., description="The unique identifier of the specific risk assessment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single risk assessment associated with a specific order. Use this to view fraud or security risk details that Shopify has flagged for an order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202001GetOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_risk_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_risk_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_risk_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_risk_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def update_order_risk_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk for a specific order. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202001UpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def delete_order_risk_202001(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be deleted."),
    risk_id: str = Field(..., description="The unique identifier of the risk assessment to be deleted from the order."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific risk assessment from an order. Note that you can only delete risks created by your application; risks created by other applications cannot be deleted."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202001DeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, policy, store-properties/policy
@mcp.tool()
async def list_policies_2020_01() -> dict[str, Any] | ToolResult:
    """Retrieves all policies configured for the shop, including return, privacy, shipping, and other store policies."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/policies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_policies_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_policies_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_policies_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_codes_batch_202001(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation job object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.Deprecated202001CreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_codes_batch_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_codes_batch_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_codes_batch_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_codes_batch_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_batch_202001(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the batch job to retrieve status and results for."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule. Use this to check the progress and results of bulk discount code generation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.Deprecated202001GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_batch_202001(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the batch job that generated the discount codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a batch creation job for a specific price rule. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.Deprecated202001GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_price_rule_2020_01(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.Deprecated202001GetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_for_price_rule(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific discount code associated with a price rule. Use this to fetch details about an individual discount code within a price rule configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202001GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_for_price_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_for_price_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_for_price_rule", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_for_price_rule",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def update_discount_code_202001(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code being updated."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing discount code associated with a price rule. Modify discount code properties such as code value, usage limits, and other settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202001UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_discount_code_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_discount_code_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_discount_code_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_discount_code_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def delete_discount_code_202001(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific discount code associated with a price rule. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202001DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, productlisting, sales-channels/productlisting
@mcp.tool()
async def update_product_listing(product_listing_id: str = Field(..., description="The unique identifier of the product listing to update.")) -> dict[str, Any] | ToolResult:
    """Update a product listing to publish or modify a product's presence on your sales channel app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateProductListingsParamProductListingIdRequest(
            path=_models.Deprecated202001UpdateProductListingsParamProductListingIdRequestPath(product_listing_id=product_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_listing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/product_listings/{product_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/product_listings/{product_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_listing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_listing", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_listing",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, productlisting, sales-channels/productlisting
@mcp.tool()
async def delete_product_listing(product_listing_id: str = Field(..., description="The unique identifier of the product listing to delete.")) -> dict[str, Any] | ToolResult:
    """Remove a product listing to unpublish a product from your sales channel app. This operation deletes the listing relationship without affecting the product itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteProductListingsParamProductListingIdRequest(
            path=_models.Deprecated202001DeleteProductListingsParamProductListingIdRequestPath(product_listing_id=product_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_listing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/product_listings/{product_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/product_listings/{product_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_listing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_listing", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_listing",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def list_products_2020_01(
    ids: Any | None = Field(None, description="Filter results to only include products with IDs matching this comma-separated list of product identifiers."),
    limit: Any | None = Field(None, description="Maximum number of products to return per page, between 1 and 250 (defaults to 50)."),
    title: Any | None = Field(None, description="Filter results to only include products whose title contains this text."),
    vendor: Any | None = Field(None, description="Filter results to only include products from this vendor."),
    handle: Any | None = Field(None, description="Filter results to only include products with this handle (URL-friendly identifier)."),
    product_type: Any | None = Field(None, description="Filter results to only include products of this type."),
    status: Any | None = Field(None, description="Filter results by product status: active (default) shows only active products, archived shows only archived products, or draft shows only draft products."),
    collection_id: Any | None = Field(None, description="Filter results to only include products in this collection, specified by collection ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published shows only published products, unpublished shows only unpublished products, or any (default) shows all products regardless of publication status."),
    presentment_currencies: Any | None = Field(None, description="Return presentment prices in only the specified currencies, provided as a comma-separated list of ISO 4217 currency codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of products from the store, with support for filtering by various attributes and sorting options. Uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetProductsRequest(
            query=_models.Deprecated202001GetProductsRequestQuery(ids=ids, limit=limit, title=title, vendor=vendor, handle=handle, product_type=product_type, status=status, collection_id=collection_id, published_status=published_status, presentment_currencies=presentment_currencies)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_products_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_products_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_products_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_products_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_products_count_202001(
    vendor: Any | None = Field(None, description="Filter the count to include only products from a specific vendor."),
    product_type: Any | None = Field(None, description="Filter the count to include only products of a specific product type."),
    collection_id: Any | None = Field(None, description="Filter the count to include only products belonging to a specific collection by its ID."),
    published_status: Any | None = Field(None, description="Filter products by their publication status: use 'published' for only published products, 'unpublished' for only unpublished products, or 'any' to include all products regardless of status (default is 'any')."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of products in the store, with optional filtering by vendor, product type, collection, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetProductsCountRequest(
            query=_models.Deprecated202001GetProductsCountRequestQuery(vendor=vendor, product_type=product_type, collection_id=collection_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_products_count_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/products/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_products_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_products_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_products_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_product_202001(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetProductsParamProductIdRequest(
            path=_models.Deprecated202001GetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def delete_product_202001(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteProductsParamProductIdRequest(
            path=_models.Deprecated202001DeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def list_product_images_202001(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of image resources for the given product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetProductsParamProductIdImagesRequest(
            path=_models.Deprecated202001GetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def create_product_image_202001(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a specific product. The image will be associated with the product and can be used to display product visuals in the storefront."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateProductsParamProductIdImagesRequest(
            path=_models.Deprecated202001CreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_images_count_202001(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for determining how many product images exist without fetching the full image data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetProductsParamProductIdImagesCountRequest(
            path=_models.Deprecated202001GetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def update_product_image_202001(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be updated."),
    image_id: str = Field(..., description="The unique identifier of the specific image within the product to be updated."),
) -> dict[str, Any] | ToolResult:
    """Update the properties of an existing product image, such as alt text, position, or other image metadata."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202001UpdateProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_image_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_image_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_image_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_image_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def delete_product_image_202001(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be deleted."),
    image_id: str = Field(..., description="The unique identifier of the image to be deleted from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image associated with a product. This removes the image from the product's image gallery."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202001DeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def list_recurring_application_charges_202001() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your Shopify app. Use this to view subscription charges that have been set up for the merchant."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def get_recurring_application_charge_202001(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific recurring application charge by its ID. Use this to fetch the current status, pricing, and other metadata for a recurring charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_recurring_application_charge_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_recurring_application_charge_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_recurring_application_charge_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_recurring_application_charge_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def delete_recurring_application_charge_2020_01(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to cancel. This ID is returned when the charge is created.")) -> dict[str, Any] | ToolResult:
    """Cancels an active recurring application charge for the app. This prevents future billing cycles for the specified charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202001DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_recurring_application_charge_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_recurring_application_charge_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_recurring_application_charge_2020_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_recurring_application_charge_2020_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def update_recurring_application_charge_capped_amount_2020_01(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to update."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount limit for the recurring charge, specified as a whole number representing the maximum billable amount."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount limit for an active recurring application charge. This allows you to modify the maximum amount that can be charged to the merchant for this recurring billing plan."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.Deprecated202001UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.Deprecated202001UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount_2020_01", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount_2020_01",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def list_usage_charges_for_recurring_application_charge_2020_01(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to retrieve associated usage charges.")) -> dict[str, Any] | ToolResult:
    """Retrieves all usage charges associated with a specific recurring application charge. Usage charges represent metered billing events tracked against a recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_usage_charges_for_recurring_application_charge_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_usage_charges_for_recurring_application_charge_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_usage_charges_for_recurring_application_charge_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_usage_charges_for_recurring_application_charge_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge_202001(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to create the usage charge.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill customers for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202001CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def get_usage_charge_for_recurring_application_charge_202001(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge that contains the usage charge you want to retrieve."),
    usage_charge_id: str = Field(..., description="The unique identifier of the specific usage charge to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single usage charge associated with a recurring application charge. Use this to fetch details about a specific metered billing charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequest(
            path=_models.Deprecated202001GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id, usage_charge_id=usage_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_usage_charge_for_recurring_application_charge_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_usage_charge_for_recurring_application_charge_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_usage_charge_for_recurring_application_charge_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_usage_charge_for_recurring_application_charge_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def list_redirects_202001(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250 (defaults to 50)."),
    path: Any | None = Field(None, description="Filter results to show only redirects with a specific source path."),
    target: Any | None = Field(None, description="Filter results to show only redirects pointing to a specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of URL redirects configured in the online store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRedirectsRequest(
            query=_models.Deprecated202001GetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def create_redirect_202001() -> dict[str, Any] | ToolResult:
    """Creates a URL redirect for your store. Full URLs provided in the path property are automatically converted to absolute paths (domain removed). For example, a path of 'http://www.johns-apparel.com/springwear' will be saved as 'springwear'."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/redirects.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_redirect_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_redirect_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_redirect_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirects_count_2020_01(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRedirectsCountRequest(
            query=_models.Deprecated202001GetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirects_count_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirects_count_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirects_count_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirects_count_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirect_202001(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store. Use this to fetch details about a specific URL redirect configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202001GetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def update_redirect_v202001(redirect_id: str = Field(..., description="The unique identifier of the redirect to update. This ID is required to locate and modify the specific redirect.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect in the online store. Modify redirect settings such as the target path or status by providing the redirect's unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202001UpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect_v202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect_v202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect_v202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect_v202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def delete_redirect_v202001(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202001DeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect_v202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect_v202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect_v202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect_v202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def create_report_legacy_202001(
    name: Any | None = Field(None, description="The display name for the report. Must not exceed 255 characters."),
    shopify_ql: Any | None = Field(None, description="The ShopifyQL query string that defines what data the report will retrieve and analyze."),
) -> dict[str, Any] | ToolResult:
    """Creates a new custom report in the Shopify admin. Reports can be configured with a name and ShopifyQL query to analyze store data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001CreateReportsRequest(
            query=_models.Deprecated202001CreateReportsRequestQuery(name=name, shopify_ql=shopify_ql)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_report_legacy_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_report_legacy_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_report_legacy_202001", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_report_legacy_202001",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def get_report_202001(report_id: str = Field(..., description="The unique identifier of the report to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single report that was created by your app. Use this to fetch details about a specific report by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetReportsParamReportIdRequest(
            path=_models.Deprecated202001GetReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_report_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_report_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_report_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_report_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def delete_report_202001(report_id: str = Field(..., description="The unique identifier of the report to delete. This is a required string value that identifies which report should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific report from the Shopify admin. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteReportsParamReportIdRequest(
            path=_models.Deprecated202001DeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def get_script_tags_count_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the store. Useful for understanding the scope of script tag usage without fetching individual records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone
@mcp.tool()
async def list_shipping_zones_202001() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop
@mcp.tool()
async def get_shop_202001() -> dict[str, Any] | ToolResult:
    """Retrieves the current shop's configuration and metadata, including store name, currency, timezone, and other essential shop-level settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance
@mcp.tool()
async def get_shopify_payments_balance_2020_01() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, including available funds and pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, transaction, shopify_payments/transaction
@mcp.tool()
async def list_shopify_payments_balance_transactions(
    test: Any | None = Field(None, description="Filter results to include only transactions placed in test mode when set to true."),
    payout_id: Any | None = Field(None, description="Filter results to transactions associated with a specific payout by providing the payout ID."),
    payout_status: Any | None = Field(None, description="Filter results to transactions with a specific payout status (e.g., pending, paid, failed)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of Shopify Payments balance transactions ordered by processing time, with the most recent first. Use cursor-based pagination via response header links."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetShopifyPaymentsBalanceTransactionsRequest(
            query=_models.Deprecated202001GetShopifyPaymentsBalanceTransactionsRequestQuery(test=test, payout_id=payout_id, payout_status=payout_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_balance_transactions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shopify_payments/balance/transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_balance_transactions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_balance_transactions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_balance_transactions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, dispute, shopify_payments/dispute
@mcp.tool()
async def list_shopify_payments_disputes(
    status: Any | None = Field(None, description="Filter disputes by their current status (e.g., won, lost, under_review). Only disputes matching the specified status will be returned."),
    initiated_at: Any | None = Field(None, description="Filter disputes by their initiation date in ISO 8601 format. Only disputes initiated on the specified date will be returned."),
) -> dict[str, Any] | ToolResult:
    """Retrieve all Shopify Payments disputes ordered by initiation date (most recent first). Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetShopifyPaymentsDisputesRequest(
            query=_models.Deprecated202001GetShopifyPaymentsDisputesRequestQuery(status=status, initiated_at=initiated_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_disputes: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shopify_payments/disputes.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_disputes")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_disputes", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_disputes",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def list_shopify_payments_payouts_2020_01(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after this date (inclusive). Specify as an ISO 8601 formatted date."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before this date (inclusive). Specify as an ISO 8601 formatted date."),
    status: Any | None = Field(None, description="Filter payouts by their current status (e.g., pending, paid, failed, cancelled). Only payouts matching the specified status will be returned."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all payouts from Shopify Payments, ordered by payout date with the most recent first. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetShopifyPaymentsPayoutsRequest(
            query=_models.Deprecated202001GetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_payouts_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_payouts_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_payouts_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_payouts_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def get_shopify_payments_payout_2020_01(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.Deprecated202001GetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_shopify_payments_payout_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_payout_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_payout_2020_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_payout_2020_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def list_smart_collections_202001(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Must be between 1 and 250; defaults to 50."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by smart collection handle (the URL-friendly identifier)."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished only, or 'any' for all collections. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from your store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetSmartCollectionsRequest(
            query=_models.Deprecated202001GetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def count_smart_collections_202001(
    title: Any | None = Field(None, description="Filter to only count smart collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter to only count smart collections that include the specified product."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' to include all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetSmartCollectionsCountRequest(
            query=_models.Deprecated202001GetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def update_smart_collection_202001(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing smart collection by its ID. Use this to modify smart collection properties such as title, rules, and other configuration settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202001UpdateSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_202001", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_202001",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def delete_smart_collection_202001(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202001DeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tendertransaction
@mcp.tool()
async def list_tender_transactions_202001(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Filter to show only tender transactions processed on or after this date (ISO 8601 format)."),
    processed_at_max: Any | None = Field(None, description="Filter to show only tender transactions processed on or before this date (ISO 8601 format)."),
    order: Any | None = Field(None, description="Sort results by processed_at timestamp in either ascending or descending order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of tender transactions. Results are paginated using link headers in the response; use the provided links to navigate between pages."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetTenderTransactionsRequest(
            query=_models.Deprecated202001GetTenderTransactionsRequestQuery(limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tender_transactions_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/tender_transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tender_transactions_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tender_transactions_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tender_transactions_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def list_themes_202001() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store, including both published and unpublished themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def create_theme_from_zip_url() -> dict[str, Any] | ToolResult:
    """Creates a new unpublished theme from a ZIP file hosted at a public URL. Optionally publish the theme immediately by including role: main in the request; publication completes after Shopify extracts and stores all theme files, which may take a few minutes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_theme_from_zip_url")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_theme_from_zip_url", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_theme_from_zip_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def get_theme_202001(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single theme by its ID from the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetThemesParamThemeIdRequest(
            path=_models.Deprecated202001GetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def update_theme_deprecated(theme_id: str = Field(..., description="The unique identifier of the theme to update. This is a required string value that specifies which theme will be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing theme in the Shopify online store. Modify theme settings and properties by providing the theme ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001UpdateThemesParamThemeIdRequest(
            path=_models.Deprecated202001UpdateThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_theme_deprecated: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_theme_deprecated")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_theme_deprecated", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_theme_deprecated",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def delete_theme_2020_01(theme_id: str = Field(..., description="The unique identifier of the theme to delete, returned as a string by the Shopify API.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the Shopify store. The theme must not be the currently active theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteThemesParamThemeIdRequest(
            path=_models.Deprecated202001DeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_2020_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_2020_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_2020_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_2020_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def delete_theme_asset_202001(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key (file path) of the asset to delete from the theme. This identifies which specific asset file to remove."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key within the theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001DeleteThemesParamThemeIdAssetsRequest(
            path=_models.Deprecated202001DeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.Deprecated202001DeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-01/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-01/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset_202001", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset_202001",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def list_webhooks_legacy_202001(
    address: Any | None = Field(None, description="Filter webhooks by the URI endpoint where they send POST requests."),
    limit: Any | None = Field(None, description="Maximum number of webhooks to return per request, between 1 and 250 (defaults to 50)."),
    topic: Any | None = Field(None, description="Filter webhooks by topic name to show only subscriptions for specific events."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of webhook subscriptions configured for your Shopify store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetWebhooksRequest(
            query=_models.Deprecated202001GetWebhooksRequestQuery(address=address, limit=limit, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_webhooks_legacy_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/webhooks.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_webhooks_legacy_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_webhooks_legacy_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhooks_legacy_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def count_webhooks_202001(
    address: Any | None = Field(None, description="Filter the webhook count to only subscriptions that deliver POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter the webhook count to only subscriptions listening to this specific event topic. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the Shopify store, with optional filtering by delivery address or event topic."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202001GetWebhooksCountRequest(
            query=_models.Deprecated202001GetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_webhooks_202001: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-01/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_webhooks_202001")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_webhooks_202001", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_webhooks_202001",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def list_application_charges_202004() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all application charges for the store. This endpoint returns historical and current charges associated with the Shopify app."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_charges_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_charges_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_charges_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def get_application_charge_202004(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific application charge by its ID. Use this to fetch the current status and information of a billing charge associated with your app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.Deprecated202004GetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def list_application_credits_202004() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits for the store. This endpoint returns a collection of credits that have been issued to the application by Shopify."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_articles_for_blog_v202004(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="Maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: 'published' for published articles only, 'unpublished' for unpublished articles only, or 'any' for all articles regardless of status (defaults to 'any')."),
    handle: Any | None = Field(None, description="Retrieve a single article by its URL-friendly handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those written by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202004GetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.Deprecated202004GetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_articles_for_blog_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_articles_for_blog_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_articles_for_blog_v202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_articles_for_blog_v202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def create_article_for_blog_202004(blog_id: str = Field(..., description="The unique identifier of the blog where the article will be created.")) -> dict[str, Any] | ToolResult:
    """Creates a new article for a specified blog. The article will be added to the blog's collection of articles."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202004CreateBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_article_for_blog_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_article_for_blog_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_article_for_blog_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_article_for_blog_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_count_for_blog_2020_04(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to count articles."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specific blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetBlogsParamBlogIdArticlesCountRequest(
            path=_models.Deprecated202004GetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.Deprecated202004GetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_count_for_blog_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_count_for_blog_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_count_for_blog_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_count_for_blog_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_202004(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article."),
    article_id: str = Field(..., description="The unique identifier of the article to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single article from a blog by its ID. Use this to fetch detailed information about a specific article in the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202004GetBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def update_article_202004(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to update."),
    article_id: str = Field(..., description="The unique identifier of the article to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing article within a blog. Modify article content, metadata, and other properties by providing the blog and article identifiers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202004UpdateBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_article_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_article_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_article_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_article_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def delete_article_202004(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to delete."),
    article_id: str = Field(..., description="The unique identifier of the article to delete."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes an article from a blog. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202004DeleteBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_article_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_article_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_article_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_article_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def create_carrier_service_legacy() -> dict[str, Any] | ToolResult:
    """Creates a new carrier service that allows Shopify to calculate shipping rates for orders. This operation registers a carrier service endpoint that will be called to provide real-time shipping rate quotes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/carrier_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_carrier_service_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_carrier_service_legacy", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_carrier_service_legacy",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def update_carrier_service_202004(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service configuration. Only the application that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202004UpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def delete_carrier_service_2020_04(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a carrier service from the store. This action cannot be undone and will remove the carrier service configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202004DeleteCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_carrier_service_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_carrier_service_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_carrier_service_2020_04", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_carrier_service_2020_04",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts
@mcp.tool()
async def get_abandoned_checkouts_count_202004(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCheckoutsCountRequest(
            query=_models.Deprecated202004GetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, checkout, sales-channels/checkout
@mcp.tool()
async def list_shipping_rates_for_checkout_2020_04(token: str = Field(..., description="The unique identifier for the checkout. This token is used to retrieve shipping rates specific to that checkout.")) -> dict[str, Any] | ToolResult:
    """Retrieves available shipping rates for a checkout. Poll this endpoint until rates become available, then use the returned rates to display updated pricing (subtotal, tax, total) or apply a rate by updating the checkout's shipping line."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCheckoutsParamTokenShippingRatesRequest(
            path=_models.Deprecated202004GetCheckoutsParamTokenShippingRatesRequestPath(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shipping_rates_for_checkout_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/checkouts/{token}/shipping_rates.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/checkouts/{token}/shipping_rates.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_rates_for_checkout_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_rates_for_checkout_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_rates_for_checkout_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def delete_collection_listing_202004(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to delete.")) -> dict[str, Any] | ToolResult:
    """Remove a collection listing to unpublish a collection from your sales channel or app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCollectionListingsParamCollectionListingIdRequest(
            path=_models.Deprecated202004DeleteCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_collection_listing_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_collection_listing_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_collection_listing_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_collection_listing_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def get_collection_202004(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID, including metadata such as name, description, and product associations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCollectionsParamCollectionIdRequest(
            path=_models.Deprecated202004GetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def list_collection_products_202004(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, ranging from 1 to 250 products (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted according to the collection's configured sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCollectionsParamCollectionIdProductsRequest(
            path=_models.Deprecated202004GetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.Deprecated202004GetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def list_collects_2020_04(limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250 items. Defaults to 50 if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of collects (product-to-collection associations). Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCollectsRequest(
            query=_models.Deprecated202004GetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collects_count_202004(collection_id: int | None = Field(None, description="Filter the count to only include collects within a specific collection. When omitted, returns the count of all collects across the store.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects, optionally filtered by a specific collection. Useful for understanding the size of a collection or the total number of product-to-collection associations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCollectsCountRequest(
            query=_models.Deprecated202004GetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collect_202004(collect_id: str = Field(..., description="The unique identifier of the collect record to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection by its ID. Use this to fetch details about how a product is organized within a collection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCollectsParamCollectIdRequest(
            path=_models.Deprecated202004GetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def remove_product_from_collection_2020_04(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the link between a product and a collection.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCollectsParamCollectIdRequest(
            path=_models.Deprecated202004DeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection_2020_04", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection_2020_04",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def list_countries_202004() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all countries available in the Shopify system. This endpoint provides country data for store configuration and regional settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_countries_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_countries_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_countries_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def create_country_202004() -> dict[str, Any] | ToolResult:
    """Creates a new country in the store. Note: The tax field is deprecated as of API version 2020-10 and should not be used."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_country_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_country_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_country_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def get_countries_count_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of countries available in the Shopify store. This is useful for understanding the scope of country data without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/countries/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_countries_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_countries_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_countries_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def update_country_202004(country_id: str = Field(..., description="The unique identifier of the country to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing country's configuration. Note: The tax field is deprecated as of API version 2020-10 and should not be used."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCountriesParamCountryIdRequest(
            path=_models.Deprecated202004UpdateCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_country_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_country_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_country_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_country_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def delete_country_202004(country_id: str = Field(..., description="The unique identifier of the country to delete. This is a required string value that specifies which country record to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a country from the store's configuration. This operation removes the country and all associated settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCountriesParamCountryIdRequest(
            path=_models.Deprecated202004DeleteCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_country_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_country_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_country_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_country_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def list_provinces_for_country_202004(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve provinces. This ID corresponds to a valid country in the Shopify system.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of provinces or states for a specified country. Use this to populate province/state selectors in address forms or location-based workflows."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCountriesParamCountryIdProvincesRequest(
            path=_models.Deprecated202004GetCountriesParamCountryIdProvincesRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_provinces_for_country_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/countries/{country_id}/provinces.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/countries/{country_id}/provinces.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_provinces_for_country_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_provinces_for_country_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_provinces_for_country_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def get_province_count_for_country_202004(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve the province count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of provinces or states for a specified country. Useful for understanding administrative divisions within a country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCountriesParamCountryIdProvincesCountRequest(
            path=_models.Deprecated202004GetCountriesParamCountryIdProvincesCountRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_count_for_country_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/countries/{country_id}/provinces/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/countries/{country_id}/provinces/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_count_for_country_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_count_for_country_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_count_for_country_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def update_province_for_country_2020_04(
    country_id: str = Field(..., description="The unique identifier of the country containing the province to update."),
    province_id: str = Field(..., description="The unique identifier of the province to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing province for a specified country. Note: The tax field is deprecated as of API version 2020-10."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCountriesParamCountryIdProvincesParamProvinceIdRequest(
            path=_models.Deprecated202004UpdateCountriesParamCountryIdProvincesParamProvinceIdRequestPath(country_id=country_id, province_id=province_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_province_for_country_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/countries/{country_id}/provinces/{province_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/countries/{country_id}/provinces/{province_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_province_for_country_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_province_for_country_2020_04", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_province_for_country_2020_04",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency
@mcp.tool()
async def list_currencies_202004() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies that are enabled and available on the shop for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def list_custom_collections_202004(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 if not specified."),
    ids: Any | None = Field(None, description="Filter results to only collections with IDs matching this comma-separated list of collection IDs."),
    title: Any | None = Field(None, description="Filter results to only collections with an exact title match."),
    product_id: Any | None = Field(None, description="Filter results to only collections that contain a specific product, identified by product ID."),
    handle: Any | None = Field(None, description="Filter results by the collection's URL-friendly handle identifier."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of custom collections with optional filtering and pagination. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomCollectionsRequest(
            query=_models.Deprecated202004GetCustomCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_custom_collections_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/custom_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_custom_collections_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_custom_collections_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_custom_collections_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def count_custom_collections_2020_04(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain this specific product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' for all collections regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomCollectionsCountRequest(
            query=_models.Deprecated202004GetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def update_custom_collection_202004(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing custom collection in your Shopify store. Modify collection details such as title, description, image, and other properties."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.Deprecated202004UpdateCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_custom_collection_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_custom_collection_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_custom_collection_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_custom_collection_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customers_202004(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer IDs."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 and cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the store. Results are paginated using link headers in the response; use the provided links for navigation rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersRequest(
            query=_models.Deprecated202004GetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customers_count_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the Shopify store. Useful for understanding customer base size and monitoring growth."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def search_customers_202004(
    order: Any | None = Field(None, description="Specify which field to sort results by and the sort direction (ascending or descending). Defaults to sorting by last order date in descending order."),
    query: Any | None = Field(None, description="Text query to search across customer data fields such as name, email, phone, and address."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for customers in the shop by query text. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersSearchRequest(
            query=_models.Deprecated202004GetCustomersSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_customers_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/customers/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_customers_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_customers_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_customers_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customer_202004(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single customer by their unique identifier from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersParamCustomerIdRequest(
            path=_models.Deprecated202004GetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def update_customer_202004(customer_id: str = Field(..., description="The unique identifier of the customer to update. This ID is required to specify which customer record should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in Shopify. Modify customer details such as email, name, phone, and other profile attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCustomersParamCustomerIdRequest(
            path=_models.Deprecated202004UpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def delete_customer_v202004(customer_id: str = Field(..., description="The unique identifier of the customer to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. Note that a customer cannot be deleted if they have any existing orders."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCustomersParamCustomerIdRequest(
            path=_models.Deprecated202004DeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_v202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_v202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def generate_customer_account_activation_url_2020_04(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the account activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.Deprecated202004CreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def list_customer_addresses_202004(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202004GetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def create_customer_address_v202004(customer_id: str = Field(..., description="The unique identifier of the customer for whom the address is being created.")) -> dict[str, Any] | ToolResult:
    """Creates a new address for a customer. The address is added to the customer's address list and can be set as their default billing or shipping address."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202004CreateCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_customer_address_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_customer_address_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_customer_address_v202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_customer_address_v202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def bulk_update_customer_addresses_202004(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to target for the bulk operation. The order may affect processing sequence depending on the operation type."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., delete, set as default). Consult API documentation for valid operation values."),
) -> dict[str, Any] | ToolResult:
    """Performs bulk operations on multiple customer addresses, allowing you to update, delete, or modify address records in a single request."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.Deprecated202004UpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.Deprecated202004UpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def get_customer_address_202004(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific customer address by customer ID and address ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202004GetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def update_customer_address_2020_04(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the specific address to update for the customer."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing customer address by customer ID and address ID. Use this to modify address details such as street, city, postal code, or other address information for a specific customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202004UpdateCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_address_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_address_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_address_2020_04", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_address_2020_04",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def delete_customer_address_202004(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific address from a customer's address list. This operation permanently deletes the address record associated with the given customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202004DeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def set_customer_default_address_202004(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Sets a specific address as the default address for a customer. This updates the customer's primary address used for shipping and billing purposes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.Deprecated202004UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_customer_default_address_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_customer_default_address_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_customer_default_address_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_customer_default_address_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customer_orders_202004(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports filtering and sorting through standard Order resource query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetCustomersParamCustomerIdOrdersRequest(
            path=_models.Deprecated202004GetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def send_customer_invite_202004(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation email to a customer, allowing them to create or access their account on the store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.Deprecated202004CreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def list_events_202004(
    limit: Any | None = Field(None, description="Maximum number of events to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
    filter_: Any | None = Field(None, alias="filter", description="Filters events to only those matching the specified criteria. Use this to narrow results to specific event types or conditions."),
    verb: Any | None = Field(None, description="Filters events to only those of a specific action type (e.g., create, update, delete). Narrows results to events matching the specified verb."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetEventsRequest(
            query=_models.Deprecated202004GetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_events_count_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. Use this to get a quick metric of how many events exist without fetching individual event details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_event_2020_04(event_id: str = Field(..., description="The unique identifier of the event to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single event by its ID from the Shopify admin. Use this to fetch detailed information about a specific event that occurred in your store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetEventsParamEventIdRequest(
            path=_models.Deprecated202004GetEventsParamEventIdRequestPath(event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def get_fulfillment_order_202004(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID. Use this to fetch current status, line items, and fulfillment information for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.Deprecated202004GetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def cancel_fulfillment_order_2020_04(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation is used to prevent a fulfillment order from being fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def send_fulfillment_order_cancellation_request_2020_04(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider that the order should be cancelled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def accept_fulfillment_order_cancellation_request_2020_04(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to accept the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, confirming that the fulfillment service should cancel the specified order. Optionally include a reason message for the acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def reject_fulfillment_order_cancellation_request_2020_04(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason will be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this when you need to decline a cancellation and optionally provide a reason to the fulfillment service."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def close_fulfillment_order_2020_04(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or note explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def send_fulfillment_request_202004(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request, such as special instructions or notes for the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional array of fulfillment order line items to request for fulfillment. If omitted, all line items in the fulfillment order will be requested."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally specifying which line items to fulfill and including an optional message."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def accept_fulfillment_request_2020_04(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def reject_fulfillment_request_202004(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove
@mcp.tool()
async def list_locations_for_fulfillment_order_move_2020_04(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves an alphabetically sorted list of locations where a fulfillment order can be moved. Use this to determine valid destination locations before initiating a fulfillment order transfer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.Deprecated202004GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def move_fulfillment_order_legacy(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to be moved."),
    new_location_id: Any | None = Field(None, description="The unique identifier of the destination location where the fulfillment order will be transferred. Must be a merchant-managed location."),
) -> dict[str, Any] | ToolResult:
    """Relocates a fulfillment order to a different merchant-managed location, enabling inventory redistribution across fulfillment centers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequest(
            path=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202004CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequestQuery(new_location_id=new_location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for move_fulfillment_order_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_orders/{fulfillment_order_id}/move.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("move_fulfillment_order_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("move_fulfillment_order_legacy", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="move_fulfillment_order_legacy",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice
@mcp.tool()
async def delete_fulfillment_service_202004(fulfillment_service_id: str = Field(..., description="The unique identifier of the fulfillment service to delete. This ID is required to specify which fulfillment service should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently delete a fulfillment service from your Shopify store. This operation removes the fulfillment service and its associated configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteFulfillmentServicesParamFulfillmentServiceIdRequest(
            path=_models.Deprecated202004DeleteFulfillmentServicesParamFulfillmentServiceIdRequestPath(fulfillment_service_id=fulfillment_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_fulfillment_service_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillment_services/{fulfillment_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillment_services/{fulfillment_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_fulfillment_service_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_fulfillment_service_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_fulfillment_service_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_202004(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This ID references a specific fulfillment order that has been previously created.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment order. This operation marks the specified fulfillment as cancelled in the system."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.Deprecated202004CreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_tracking_2020_04(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates the tracking information for a fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.Deprecated202004CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def list_gift_cards_2020_04(
    status: Any | None = Field(None, description="Filter results to gift cards with a specific status: enabled to show only active gift cards, or disabled to show only inactive gift cards."),
    limit: Any | None = Field(None, description="Maximum number of gift cards to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of gift cards, optionally filtered by status. Results are paginated using link headers in the response rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetGiftCardsRequest(
            query=_models.Deprecated202004GetGiftCardsRequestQuery(status=status, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_gift_cards_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/gift_cards.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_gift_cards_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_gift_cards_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_gift_cards_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def count_gift_cards_v202004(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetGiftCardsCountRequest(
            query=_models.Deprecated202004GetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_gift_cards_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_gift_cards_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_gift_cards_v202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_gift_cards_v202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def search_gift_cards_202004(
    order: Any | None = Field(None, description="The field and direction to sort results by. Defaults to most recently disabled gift cards first. Common fields include created_at, updated_at, disabled_at, balance, initial_value, amount_spent, email, and last_characters."),
    query: Any | None = Field(None, description="The search query text to match against indexed gift card fields such as email, balance, or last characters."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for gift cards using indexed fields like balance, email, or creation date. Results are paginated and returned via link headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetGiftCardsSearchRequest(
            query=_models.Deprecated202004GetGiftCardsSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_gift_cards_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/gift_cards/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_gift_cards_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_gift_cards_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_gift_cards_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def update_gift_card_202004(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Update an existing gift card's expiry date, note, or template suffix. Note that the gift card's balance cannot be modified through the API."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateGiftCardsParamGiftCardIdRequest(
            path=_models.Deprecated202004UpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def disable_gift_card_202004(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.Deprecated202004CreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def list_inventory_items_202004(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request, between 1 and 250 items (defaults to 50)."),
    ids: int | None = Field(None, description="Filter results to specific inventory items by their numeric IDs. Provide one or more comma-separated integer values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items. Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetInventoryItemsRequest(
            query=_models.Deprecated202004GetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def update_inventory_item_legacy_202004(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to update. This ID is required to specify which inventory item should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing inventory item in your store's inventory system. Use this to modify inventory item properties such as tracked status, cost, and other item-level settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateInventoryItemsParamInventoryItemIdRequest(
            path=_models.Deprecated202004UpdateInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_inventory_item_legacy_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_inventory_item_legacy_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_inventory_item_legacy_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_inventory_item_legacy_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def list_inventory_levels_202004(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request. Defaults to 50 if not specified, with a maximum of 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across locations and inventory items. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetInventoryLevelsRequest(
            query=_models.Deprecated202004GetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def delete_inventory_level_202004(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted."),
    location_id: int | None = Field(None, description="The unique identifier of the location from which the inventory item should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This operation disconnects the inventory item from that location; note that every inventory item must maintain at least one inventory level, so you must connect the item to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteInventoryLevelsRequest(
            query=_models.Deprecated202004DeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_locations_202004() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. This endpoint returns a list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_locations_count_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. This is useful for understanding inventory distribution across fulfillment locations without fetching full location details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_location_202004(location_id: str = Field(..., description="The unique identifier of the location to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single location by its ID from the Shopify inventory system. Use this to fetch detailed information about a specific store location."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetLocationsParamLocationIdRequest(
            path=_models.Deprecated202004GetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_inventory_levels_for_location_202004(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves all inventory levels for a specific location. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.Deprecated202004GetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafields_count_2020_04() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields associated with a resource. Use this to determine how many custom metadata fields exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def list_orders_v202004(
    ids: Any | None = Field(None, description="Filter results to only orders with the specified IDs, provided as a comma-separated list."),
    limit: Any | None = Field(None, description="Maximum number of orders to return per page, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Show only orders processed at or after this date, specified in ISO 8601 format with timezone."),
    processed_at_max: Any | None = Field(None, description="Show only orders processed at or before this date, specified in ISO 8601 format with timezone."),
    attribution_app_id: Any | None = Field(None, description="Filter orders by the app that created them, specified by app ID. Use 'current' to show orders for the app making the request."),
    status: Any | None = Field(None, description="Filter orders by their fulfillment state: open (default), closed, cancelled, or any (including archived orders)."),
    financial_status: Any | None = Field(None, description="Filter orders by their payment state: any (default), authorized, pending, paid, partially_paid, refunded, voided, partially_refunded, or unpaid (authorized and partially paid combined)."),
    fulfillment_status: Any | None = Field(None, description="Filter orders by their shipment state: any (default), shipped (fully fulfilled), partial (partially shipped), unshipped (not yet shipped), or unfulfilled (unshipped or partially shipped combined)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of orders with filtering options by status, financial state, fulfillment state, and other criteria. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersRequest(
            query=_models.Deprecated202004GetOrdersRequestQuery(ids=ids, limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, attribution_app_id=attribution_app_id, status=status, financial_status=financial_status, fulfillment_status=fulfillment_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_orders_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/orders.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_orders_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_orders_v202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_orders_v202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def get_orders_count_202004(
    status: Any | None = Field(None, description="Filter orders by their fulfillment state. Defaults to 'open' if not specified. Use 'open' for unfulfilled orders, 'closed' for completed orders, or 'any' to count all orders regardless of status."),
    financial_status: Any | None = Field(None, description="Filter orders by their payment state. Defaults to 'any' if not specified. Valid states include 'authorized', 'pending', 'paid', 'refunded', 'voided', or 'any' to include all payment statuses."),
    fulfillment_status: Any | None = Field(None, description="Filter orders by their shipment state. Defaults to 'any' if not specified. Use 'shipped' for fully delivered orders, 'partial' for partially shipped, 'unshipped' for orders not yet sent, 'unfulfilled' for orders with no or partial shipments, or 'any' for all fulfillment states."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the count of orders filtered by status, financial status, and fulfillment status. Useful for obtaining order statistics without fetching full order details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersCountRequest(
            query=_models.Deprecated202004GetOrdersCountRequestQuery(status=status, financial_status=financial_status, fulfillment_status=fulfillment_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_orders_count_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/orders/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_orders_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_orders_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_orders_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def get_order(order_id: str = Field(..., description="The unique identifier of the order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific order by its ID. Use this to fetch complete order information including items, customer details, and fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def update_order_v2020_04(order_id: str = Field(..., description="The unique identifier of the order to update. This is a required string value that specifies which order will be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing order in Shopify. Use this operation to modify order details such as status, customer information, or other order attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateOrdersParamOrderIdRequest(
            path=_models.Deprecated202004UpdateOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_v2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_v2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_v2020_04", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_v2020_04",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def delete_order_v2(order_id: str = Field(..., description="The unique identifier of the order to delete. This is a required numeric ID that identifies which order should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes an order from the store. Note that orders connected to online payment gateways cannot be deleted due to payment processing requirements."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteOrdersParamOrderIdRequest(
            path=_models.Deprecated202004DeleteOrdersParamOrderIdRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_v2: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_v2")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_v2", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_v2",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def cancel_order_v2(
    order_id: str = Field(..., description="The unique identifier of the order to cancel."),
    amount: Any | None = Field(None, description="The monetary amount to refund. When provided, Shopify attempts to void or refund the payment based on its current status. For manual gateway transactions, refunds are recorded in Shopify but the customer must be refunded separately."),
    currency: Any | None = Field(None, description="The currency code for the refund amount. Required for orders with multiple currencies whenever the amount parameter is provided."),
    reason: Any | None = Field(None, description="The reason for cancellation. Valid options are: customer, inventory, fraud, declined, or other. Defaults to other if not specified."),
    email: Any | None = Field(None, description="Whether to send a cancellation notification email to the customer. Defaults to false if not specified."),
    refund: Any | None = Field(None, description="Refund transaction details for complex refund scenarios. Use this for advanced refund operations beyond simple amount-based refunds. See the Refund API documentation for structure details."),
) -> dict[str, Any] | ToolResult:
    """Cancels an order and optionally processes a refund. Orders with fulfillment objects cannot be canceled. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdCancelRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdCancelRequestPath(order_id=order_id),
            query=_models.Deprecated202004CreateOrdersParamOrderIdCancelRequestQuery(amount=amount, currency=currency, reason=reason, email=email, refund=refund)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_order_v2: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/cancel.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_order_v2")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_order_v2", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_order_v2",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def close_order_v2(order_id: str = Field(..., description="The unique identifier of the order to close.")) -> dict[str, Any] | ToolResult:
    """Closes an order, preventing further modifications. This operation marks the order as closed in the system."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdCloseRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdCloseRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_order_v2: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/close.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_order_v2")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_order_v2", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_order_v2",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def list_fulfillment_orders_for_order_2020_04(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order. Fulfillment orders represent the items in an order that need to be fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders_for_order_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders_for_order_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders_for_order_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders_for_order_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def list_fulfillments_for_order_v202004(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillments."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the limit parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_order_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_order_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_order_v202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_order_v202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_for_order_legacy_202004(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment. Required to route the request to the correct order.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for specified line items in an order. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed. If no line items are specified, all unfulfilled and partially fulfilled items are fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order_legacy_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order_legacy_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order_legacy_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order_legacy_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_count_for_order_2020_04(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding fulfillment status and logistics tracking without fetching full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_count_for_order_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_count_for_order_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_count_for_order_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_count_for_order_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order. This operation returns fulfillment status, tracking information, and line items included in the shipment."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202004UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def complete_fulfillment_2020_04(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped and the order fulfillment process is finished."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent
@mcp.tool()
async def list_fulfillment_events_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all events associated with a specific fulfillment for an order. Events track status changes and milestones throughout the fulfillment lifecycle."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_events_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_events_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_events_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_events_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def open_fulfillment_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be opened."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as open."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as open, allowing it to receive additional items or changes. This operation transitions a fulfillment from a closed state back to an open state."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/fulfillments/{fulfillment_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def reopen_order_202004(order_id: str = Field(..., description="The unique identifier of the order to re-open. This must be a valid order ID from your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Re-opens a previously closed order, allowing it to be modified and processed again. This operation restores a closed order to an active state."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdOpenRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdOpenRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reopen_order_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reopen_order_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reopen_order_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reopen_order_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def list_order_refunds_202004(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="Maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays refund amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of refunds for a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202004GetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def create_order_refund_202004(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Shipping refund details. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each with the line item ID, quantity to refund, restock type (no_restock, cancel, or return), and location ID for restocking (required for cancel and return types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Should be obtained from the calculate endpoint for accuracy."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transactions to submit. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202004CreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def calculate_order_refund_2020_04(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping costs, or amount to refund a specific shipping amount (takes precedence over full_refund). Required if providing a shipping amount for multi-currency orders."),
    refund_line_items: Any | None = Field(None, description="List of line items to refund, each specifying the line item ID, quantity to refund, and restock instructions. Restock type determines inventory impact: no_restock (no inventory change), cancel (items not yet fulfilled, reduces fulfillable units), or return (items already delivered, no change to fulfillable units). Optionally specify location_id for restocking; if omitted for cancel/return types, the endpoint will suggest a suitable location."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders whenever a shipping amount is specified."),
) -> dict[str, Any] | ToolResult:
    """Calculates refund transactions for an order based on specified line items, quantities, restock instructions, and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund; the response provides suggested transactions that must be converted to actual refunds."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.Deprecated202004CreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_order_refund_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_order_refund_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_order_refund_2020_04", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_order_refund_2020_04",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def get_refund_for_order_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's native currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific refund associated with an order. Use this to view refund information including amounts, line items, and status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.Deprecated202004GetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund_for_order_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund_for_order_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund_for_order_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund_for_order_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def list_order_risks_2020_04(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve associated risks.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. This endpoint uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def create_order_risk_202004(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202004CreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def get_order_risk_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk assessment."),
    risk_id: str = Field(..., description="The unique identifier of the specific risk assessment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific risk assessment associated with an order. Use this to fetch details about a fraud or security risk that has been flagged for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202004GetOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_risk_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_risk_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_risk_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_risk_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def update_order_risk_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk for a specific order. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202004UpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def delete_order_risk_202004(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be deleted."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Deletes a specific order risk associated with an order. Note that you cannot delete an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202004DeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_codes_batch_202004(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation job object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.Deprecated202004CreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_codes_batch_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_codes_batch_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_codes_batch_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_codes_batch_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_batch_202004(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the specific batch job to retrieve status and results for."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule. Use this to check the progress and results of bulk discount code generation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.Deprecated202004GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_batch_202004(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the discount code creation batch job to retrieve codes from."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a specific discount code creation batch job. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.Deprecated202004GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_price_rule_2020_04(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.Deprecated202004GetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_for_price_rule_v2(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific discount code associated with a price rule. Use this to fetch details about an individual discount code within a price rule configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202004GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_for_price_rule_v2: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_for_price_rule_v2")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_for_price_rule_v2", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_for_price_rule_v2",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def update_discount_code_202004(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code being updated."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing discount code associated with a price rule. Modify discount code properties such as code value, usage limits, and other settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202004UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_discount_code_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_discount_code_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_discount_code_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_discount_code_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def delete_discount_code_202004(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific discount code associated with a price rule. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202004DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, productlisting, sales-channels/productlisting
@mcp.tool()
async def delete_product_listing_v2020_04(product_listing_id: str = Field(..., description="The unique identifier of the product listing to delete. This ID represents the specific product-to-channel relationship being removed.")) -> dict[str, Any] | ToolResult:
    """Remove a product listing to unpublish a product from your sales channel app. This operation deletes the listing relationship without affecting the product itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteProductListingsParamProductListingIdRequest(
            path=_models.Deprecated202004DeleteProductListingsParamProductListingIdRequestPath(product_listing_id=product_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_listing_v2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/product_listings/{product_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/product_listings/{product_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_listing_v2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_listing_v2020_04", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_listing_v2020_04",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def list_products_2020_04(
    ids: Any | None = Field(None, description="Filter results to only include products with IDs matching this comma-separated list of product identifiers."),
    limit: Any | None = Field(None, description="Maximum number of products to return per page, between 1 and 250 (defaults to 50)."),
    title: Any | None = Field(None, description="Filter results to only include products whose title contains this text."),
    vendor: Any | None = Field(None, description="Filter results to only include products from this vendor."),
    handle: Any | None = Field(None, description="Filter results to only include products with this handle (URL-friendly identifier)."),
    product_type: Any | None = Field(None, description="Filter results to only include products of this type."),
    status: Any | None = Field(None, description="Filter results by product status: active (default), archived, or draft."),
    collection_id: Any | None = Field(None, description="Filter results to only include products assigned to this collection ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published, unpublished, or any (default shows all)."),
    presentment_currencies: Any | None = Field(None, description="Return presentment prices in only these currencies, specified as a comma-separated list of ISO 4217 currency codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of products from the store, with support for filtering by various attributes and sorting options. Uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetProductsRequest(
            query=_models.Deprecated202004GetProductsRequestQuery(ids=ids, limit=limit, title=title, vendor=vendor, handle=handle, product_type=product_type, status=status, collection_id=collection_id, published_status=published_status, presentment_currencies=presentment_currencies)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_products_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_products_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_products_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_products_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_products_count_202004(
    vendor: Any | None = Field(None, description="Filter the count to include only products from a specific vendor."),
    product_type: Any | None = Field(None, description="Filter the count to include only products of a specific product type."),
    collection_id: Any | None = Field(None, description="Filter the count to include only products belonging to a specific collection by its ID."),
    published_status: Any | None = Field(None, description="Filter products by their publication status: use 'published' for only published products, 'unpublished' for only unpublished products, or 'any' to include all products regardless of status (default is 'any')."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of products in the store, with optional filtering by vendor, product type, collection, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetProductsCountRequest(
            query=_models.Deprecated202004GetProductsCountRequestQuery(vendor=vendor, product_type=product_type, collection_id=collection_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_products_count_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/products/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_products_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_products_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_products_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_product_202004(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetProductsParamProductIdRequest(
            path=_models.Deprecated202004GetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def delete_product_202004(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteProductsParamProductIdRequest(
            path=_models.Deprecated202004DeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def list_product_images_202004(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of image resources for the given product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetProductsParamProductIdImagesRequest(
            path=_models.Deprecated202004GetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def create_product_image_202004(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a specific product. The image will be associated with the product and can be used to display product visuals in the storefront."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateProductsParamProductIdImagesRequest(
            path=_models.Deprecated202004CreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_images_count_202004(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for determining how many product images exist without fetching the full image data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetProductsParamProductIdImagesCountRequest(
            path=_models.Deprecated202004GetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def update_product_image_202004(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be updated."),
    image_id: str = Field(..., description="The unique identifier of the image within the product to be updated."),
) -> dict[str, Any] | ToolResult:
    """Update the properties of an existing product image, such as alt text, position, or other image metadata."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202004UpdateProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_image_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_image_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_image_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_image_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def delete_product_image_202004(
    product_id: str = Field(..., description="The unique identifier of the product containing the image to delete."),
    image_id: str = Field(..., description="The unique identifier of the image to delete from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image associated with a product. This removes the image from the product's image gallery."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202004DeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def list_recurring_application_charges_202004() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your Shopify app. Use this to view subscription charges that have been set up for the merchant."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def get_recurring_application_charge_202004(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single recurring application charge by its ID. Use this to check the status, terms, and billing information for a specific recurring charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202004GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_recurring_application_charge_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_recurring_application_charge_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_recurring_application_charge_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_recurring_application_charge_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def delete_recurring_application_charge_2020_04(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to cancel. This ID is returned when the charge is created.")) -> dict[str, Any] | ToolResult:
    """Cancels an active recurring application charge for the app. This prevents future billing cycles for the specified charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202004DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_recurring_application_charge_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_recurring_application_charge_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_recurring_application_charge_2020_04", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_recurring_application_charge_2020_04",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def update_recurring_application_charge_capped_amount_2020_04(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to update."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount limit for the recurring charge, specified as a whole number representing the maximum billable amount."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount limit for an active recurring application charge. This allows you to modify the maximum amount that can be charged to the merchant during the billing cycle."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.Deprecated202004UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.Deprecated202004UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount_2020_04", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount_2020_04",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge_202004(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to create the usage charge.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill merchants for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202004CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def list_redirects_202004(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250 (defaults to 50)."),
    path: Any | None = Field(None, description="Filter results to show only redirects with a specific source path."),
    target: Any | None = Field(None, description="Filter results to show only redirects pointing to a specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of URL redirects configured in the online store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetRedirectsRequest(
            query=_models.Deprecated202004GetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def create_redirect_202004() -> dict[str, Any] | ToolResult:
    """Creates a URL redirect for your store. Full URLs provided in the path property are automatically converted to absolute paths (domain removed). For example, a path of 'http://www.johns-apparel.com/springwear' will be saved as 'springwear'."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/redirects.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_redirect_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_redirect_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_redirect_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirects_count_2020_04(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetRedirectsCountRequest(
            query=_models.Deprecated202004GetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirects_count_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirects_count_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirects_count_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirects_count_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirect_202004(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store. Use this to fetch details about a specific URL redirect configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202004GetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def update_redirect_v202004(redirect_id: str = Field(..., description="The unique identifier of the redirect to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect in the online store. Modify redirect settings such as the target path or status for a specific redirect by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202004UpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect_v202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect_v202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def delete_redirect_v202004(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202004DeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect_v202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect_v202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect_v202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect_v202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def create_report_legacy_202004(
    name: Any | None = Field(None, description="The display name for the report. Must be 255 characters or fewer."),
    shopify_ql: Any | None = Field(None, description="The ShopifyQL query string that defines what data the report will retrieve and analyze."),
) -> dict[str, Any] | ToolResult:
    """Creates a new custom report in the Shopify admin. Reports use ShopifyQL queries to analyze store data and can be referenced by name for future access."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004CreateReportsRequest(
            query=_models.Deprecated202004CreateReportsRequestQuery(name=name, shopify_ql=shopify_ql)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_report_legacy_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_report_legacy_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_report_legacy_202004", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_report_legacy_202004",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def get_report_202004(report_id: str = Field(..., description="The unique identifier of the report to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single report that was created by your app. Use this to fetch details about a specific report by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetReportsParamReportIdRequest(
            path=_models.Deprecated202004GetReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_report_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_report_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_report_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_report_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def delete_report_202004(report_id: str = Field(..., description="The unique identifier of the report to delete. This ID must correspond to an existing report in your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific report from the Shopify admin. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteReportsParamReportIdRequest(
            path=_models.Deprecated202004DeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def get_script_tags_count_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the store. Useful for understanding the scope of script tag usage without fetching individual records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone
@mcp.tool()
async def list_shipping_zones_202004() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop
@mcp.tool()
async def get_shop_202004() -> dict[str, Any] | ToolResult:
    """Retrieves the current shop's configuration and metadata, including store name, contact information, and plan details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance
@mcp.tool()
async def get_shopify_payments_balance_2020_04() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, including available funds and pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, dispute, shopify_payments/dispute
@mcp.tool()
async def list_shopify_payments_disputes_legacy(
    status: Any | None = Field(None, description="Filter disputes by their current status (e.g., won, lost, under_review). Only disputes matching the specified status will be returned."),
    initiated_at: Any | None = Field(None, description="Filter disputes by their initiation date in ISO 8601 format. Only disputes initiated on the specified date will be returned."),
) -> dict[str, Any] | ToolResult:
    """Retrieve all Shopify Payments disputes ordered by initiation date (most recent first). Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetShopifyPaymentsDisputesRequest(
            query=_models.Deprecated202004GetShopifyPaymentsDisputesRequestQuery(status=status, initiated_at=initiated_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_disputes_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/shopify_payments/disputes.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_disputes_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_disputes_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_disputes_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def list_shopify_payments_payouts_2020_04(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after the specified date (inclusive). Use ISO 8601 format."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before the specified date (inclusive). Use ISO 8601 format."),
    status: Any | None = Field(None, description="Filter payouts by their status (e.g., pending, completed, failed, cancelled). Only payouts matching the specified status will be returned."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all payouts from Shopify Payments, ordered by payout date with the most recent first. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetShopifyPaymentsPayoutsRequest(
            query=_models.Deprecated202004GetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_payouts_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_payouts_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_payouts_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_payouts_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def get_shopify_payments_payout_2020_04(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.Deprecated202004GetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_shopify_payments_payout_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_payout_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_payout_2020_04", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_payout_2020_04",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def list_smart_collections_202004(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Defaults to 50 and cannot exceed 250."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to only smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by the smart collection's URL handle."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from your store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetSmartCollectionsRequest(
            query=_models.Deprecated202004GetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def count_smart_collections_202004(
    title: Any | None = Field(None, description="Filter to smart collections with an exact matching title."),
    product_id: Any | None = Field(None, description="Filter to smart collections that contain the specified product ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for live collections, 'unpublished' for draft collections, or 'any' to include all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetSmartCollectionsCountRequest(
            query=_models.Deprecated202004GetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def update_smart_collection_202004(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update. This ID is required to target the specific smart collection for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing smart collection by its ID. Use this to modify smart collection properties such as title, rules, and other configuration settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202004UpdateSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_202004", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_202004",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def delete_smart_collection_202004(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202004DeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def list_themes_202004() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store, including both published and unpublished themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def create_theme_with_zip() -> dict[str, Any] | ToolResult:
    """Creates a new unpublished theme from a ZIP file hosted at a public URL. Optionally publish the theme immediately by including role: main in the request; publication completes after Shopify extracts and stores all theme files, which may take a few minutes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_theme_with_zip")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_theme_with_zip", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_theme_with_zip",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def get_theme_202004(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single theme by its ID from the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetThemesParamThemeIdRequest(
            path=_models.Deprecated202004GetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def update_theme_archived(theme_id: str = Field(..., description="The unique identifier of the theme to update. This ID is returned when a theme is created and is required to target the specific theme for modification.")) -> dict[str, Any] | ToolResult:
    """Updates the configuration and settings of an existing theme in the Shopify store. Use this to modify theme properties after creation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004UpdateThemesParamThemeIdRequest(
            path=_models.Deprecated202004UpdateThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_theme_archived: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_theme_archived")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_theme_archived", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_theme_archived",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def delete_theme_2020_04(theme_id: str = Field(..., description="The unique identifier of the theme to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the Shopify store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteThemesParamThemeIdRequest(
            path=_models.Deprecated202004DeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_2020_04: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_2020_04")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_2020_04", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_2020_04",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def delete_theme_asset_202004(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key (file path) of the asset to delete from the theme. This identifies which specific asset file to remove."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key within the theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004DeleteThemesParamThemeIdAssetsRequest(
            path=_models.Deprecated202004DeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.Deprecated202004DeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-04/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-04/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset_202004", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset_202004",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def list_webhooks_legacy_202004(
    address: Any | None = Field(None, description="Filter webhooks by the URI endpoint that receives POST requests. Only webhooks sending to this address will be returned."),
    limit: Any | None = Field(None, description="Maximum number of webhooks to return per request, between 1 and 250. Defaults to 50 if not specified."),
    topic: Any | None = Field(None, description="Filter webhooks by topic to show only subscriptions for specific events. Refer to the Shopify webhook topics documentation for valid values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of webhook subscriptions configured for your Shopify store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetWebhooksRequest(
            query=_models.Deprecated202004GetWebhooksRequestQuery(address=address, limit=limit, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_webhooks_legacy_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/webhooks.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_webhooks_legacy_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_webhooks_legacy_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhooks_legacy_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def count_webhooks_202004(
    address: Any | None = Field(None, description="Filter results to webhook subscriptions that deliver POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter results to webhook subscriptions listening to a specific event topic. Refer to the webhook topic documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the Shopify store, with optional filtering by delivery address or event topic."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202004GetWebhooksCountRequest(
            query=_models.Deprecated202004GetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_webhooks_202004: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-04/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_webhooks_202004")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_webhooks_202004", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_webhooks_202004",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def list_application_charges_202007() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all application charges for the store. This endpoint returns charges that have been created for billing purposes within the Shopify admin API."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_charges_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_charges_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_charges_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def get_application_charge_202007(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific application charge by its ID. Use this to fetch information about a charge created for your app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.Deprecated202007GetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def list_application_credits_202007() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits for the store. This endpoint returns a collection of credits that have been issued to the application by Shopify."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_articles_for_blog_v202007(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="Maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: 'published' for published articles only, 'unpublished' for unpublished articles only, or 'any' for all articles regardless of status (defaults to 'any')."),
    handle: Any | None = Field(None, description="Retrieve a single article by its URL-friendly handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those written by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Results are paginated using link headers in the response; use the provided links for navigation rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202007GetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.Deprecated202007GetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_articles_for_blog_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_articles_for_blog_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_articles_for_blog_v202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_articles_for_blog_v202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def create_article_for_blog_202007(blog_id: str = Field(..., description="The unique identifier of the blog where the article will be created.")) -> dict[str, Any] | ToolResult:
    """Creates a new article for a specified blog. The article will be added to the blog's collection of articles."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202007CreateBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_article_for_blog_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/blogs/{blog_id}/articles.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_article_for_blog_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_article_for_blog_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_article_for_blog_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def count_articles_in_blog_legacy(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to count articles."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specific blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetBlogsParamBlogIdArticlesCountRequest(
            path=_models.Deprecated202007GetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.Deprecated202007GetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_articles_in_blog_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_articles_in_blog_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_articles_in_blog_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_articles_in_blog_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_202007(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article."),
    article_id: str = Field(..., description="The unique identifier of the article to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single article from a blog by its ID. Use this to fetch detailed information about a specific article including its content, metadata, and publishing status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202007GetBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def delete_article_202007(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to delete."),
    article_id: str = Field(..., description="The unique identifier of the article to delete."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes an article from a blog. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202007DeleteBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_article_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_article_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_article_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_article_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def update_carrier_service_202007(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service. Only the app that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202007UpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts
@mcp.tool()
async def get_abandoned_checkouts_count_202007(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCheckoutsCountRequest(
            query=_models.Deprecated202007GetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, checkout, sales-channels/checkout
@mcp.tool()
async def list_shipping_rates_for_checkout_2020_07(token: str = Field(..., description="The unique identifier for the checkout. This token is used to retrieve shipping rates specific to that checkout.")) -> dict[str, Any] | ToolResult:
    """Retrieves available shipping rates for a checkout. Poll this endpoint until rates become available, then use the returned rates to display updated pricing (subtotal, tax, total) or apply a rate by updating the checkout's shipping line."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCheckoutsParamTokenShippingRatesRequest(
            path=_models.Deprecated202007GetCheckoutsParamTokenShippingRatesRequestPath(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shipping_rates_for_checkout_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/checkouts/{token}/shipping_rates.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/checkouts/{token}/shipping_rates.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_rates_for_checkout_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_rates_for_checkout_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_rates_for_checkout_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def delete_collection_listing_202007(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to delete.")) -> dict[str, Any] | ToolResult:
    """Remove a collection listing to unpublish a collection from your sales channel or app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCollectionListingsParamCollectionListingIdRequest(
            path=_models.Deprecated202007DeleteCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_collection_listing_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_collection_listing_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_collection_listing_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_collection_listing_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def list_product_ids_for_collection_listing(
    collection_listing_id: str = Field(..., description="The unique identifier for the collection listing whose published product IDs you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of product IDs to return per request, ranging from 1 to 1000 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve the product IDs that are published to a specific collection listing. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectionListingsParamCollectionListingIdProductIdsRequest(
            path=_models.Deprecated202007GetCollectionListingsParamCollectionListingIdProductIdsRequestPath(collection_listing_id=collection_listing_id),
            query=_models.Deprecated202007GetCollectionListingsParamCollectionListingIdProductIdsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_ids_for_collection_listing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collection_listings/{collection_listing_id}/product_ids.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collection_listings/{collection_listing_id}/product_ids.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_ids_for_collection_listing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_ids_for_collection_listing", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_ids_for_collection_listing",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def get_collection_202007(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID, including metadata such as name, description, and product associations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectionsParamCollectionIdRequest(
            path=_models.Deprecated202007GetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def list_collection_products_202007(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, ranging from 1 to 250 products (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted according to the collection's configured sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectionsParamCollectionIdProductsRequest(
            path=_models.Deprecated202007GetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.Deprecated202007GetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def list_collects_2020_07(limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250 items (defaults to 50).")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of collects (product-to-collection associations). Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectsRequest(
            query=_models.Deprecated202007GetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def add_product_to_collection_202007() -> dict[str, Any] | ToolResult:
    """Adds a product to a custom collection in Shopify. This operation associates an existing product with a collection, making it discoverable within that collection's product listing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/collects.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_product_to_collection_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_product_to_collection_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_product_to_collection_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collects_count_202007(collection_id: int | None = Field(None, description="Filter the count to only include collects belonging to a specific collection. If omitted, returns the total count of all collects across the store.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects, optionally filtered by a specific collection. Useful for understanding the size of a collection or the total number of product-to-collection associations in the store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectsCountRequest(
            query=_models.Deprecated202007GetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collect_202007(collect_id: str = Field(..., description="The unique identifier of the collect resource to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection by its ID. Use this to fetch details about how a product is organized within a collection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCollectsParamCollectIdRequest(
            path=_models.Deprecated202007GetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def remove_product_from_collection_2020_07(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the link between a product and a collection.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCollectsParamCollectIdRequest(
            path=_models.Deprecated202007DeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection_2020_07", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection_2020_07",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def list_countries_202007() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all countries available in the Shopify system. This endpoint provides country data for store configuration and regional settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_countries_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_countries_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_countries_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def create_country_202007() -> dict[str, Any] | ToolResult:
    """Creates a new country in the store. Note: The tax field is deprecated as of API version 2020-10 and should not be used."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_country_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_country_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_country_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def get_countries_count_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of countries available in the Shopify store. This is useful for understanding the scope of country data without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/countries/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_countries_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_countries_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_countries_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def update_country_202007(country_id: str = Field(..., description="The unique identifier of the country to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing country's configuration. Note: The tax field is deprecated as of API version 2020-10 and should not be used."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCountriesParamCountryIdRequest(
            path=_models.Deprecated202007UpdateCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_country_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_country_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_country_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_country_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def delete_country_202007(country_id: str = Field(..., description="The unique identifier of the country to delete. This is a required string value that specifies which country record to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a country from the store's configuration. This operation removes the country and all associated settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCountriesParamCountryIdRequest(
            path=_models.Deprecated202007DeleteCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_country_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_country_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_country_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_country_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def list_provinces_for_country_202007(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve provinces. This ID must correspond to a valid country in the Shopify system.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of provinces or states for a specified country. Use this to populate province/state selectors or validate province data for a given country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCountriesParamCountryIdProvincesRequest(
            path=_models.Deprecated202007GetCountriesParamCountryIdProvincesRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_provinces_for_country_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/countries/{country_id}/provinces.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/countries/{country_id}/provinces.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_provinces_for_country_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_provinces_for_country_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_provinces_for_country_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def get_province_count_for_country_202007(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve the province count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of provinces or states for a specified country. Useful for understanding administrative divisions within a country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCountriesParamCountryIdProvincesCountRequest(
            path=_models.Deprecated202007GetCountriesParamCountryIdProvincesCountRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_count_for_country_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/countries/{country_id}/provinces/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/countries/{country_id}/provinces/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_count_for_country_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_count_for_country_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_count_for_country_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def update_province_for_country_2020_07(
    country_id: str = Field(..., description="The unique identifier of the country containing the province to update."),
    province_id: str = Field(..., description="The unique identifier of the province to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing province for a specified country. Note: The tax field is deprecated as of API version 2020-10."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCountriesParamCountryIdProvincesParamProvinceIdRequest(
            path=_models.Deprecated202007UpdateCountriesParamCountryIdProvincesParamProvinceIdRequestPath(country_id=country_id, province_id=province_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_province_for_country_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/countries/{country_id}/provinces/{province_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/countries/{country_id}/provinces/{province_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_province_for_country_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_province_for_country_2020_07", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_province_for_country_2020_07",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency
@mcp.tool()
async def list_currencies_202007() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies that are enabled and available on the shop for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def count_custom_collections_2020_07(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain this specific product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' for all collections regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomCollectionsCountRequest(
            query=_models.Deprecated202007GetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def update_custom_collection_202007(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to update. This ID is required to specify which collection should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing custom collection in your Shopify store. Modify collection details such as title, description, image, and other properties by providing the collection ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.Deprecated202007UpdateCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_custom_collection_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_custom_collection_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_custom_collection_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_custom_collection_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def delete_custom_collection_legacy_202007(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a custom collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.Deprecated202007DeleteCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_custom_collection_legacy_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_custom_collection_legacy_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_custom_collection_legacy_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_custom_collection_legacy_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customersavedsearch, customers/customersavedsearch
@mcp.tool()
async def list_customers_for_saved_search_2020_07(
    customer_saved_search_id: str = Field(..., description="The unique identifier of the customer saved search to retrieve customers for."),
    order: Any | None = Field(None, description="Specifies the field and direction to sort results by (e.g., ascending or descending order). Defaults to sorting by last order date in descending order."),
    limit: Any | None = Field(None, description="The maximum number of customer records to return per request. Defaults to 50 and cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all customers matching the criteria defined in a customer saved search. Results can be ordered and paginated."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequest(
            path=_models.Deprecated202007GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestPath(customer_saved_search_id=customer_saved_search_id),
            query=_models.Deprecated202007GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestQuery(order=order, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_for_saved_search_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customer_saved_searches/{customer_saved_search_id}/customers.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customer_saved_searches/{customer_saved_search_id}/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_for_saved_search_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_for_saved_search_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_for_saved_search_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customers_202007(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer ID values."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomersRequest(
            query=_models.Deprecated202007GetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customers_count_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the Shopify store. Useful for understanding customer base size and monitoring growth."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customer_202007(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single customer by their unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomersParamCustomerIdRequest(
            path=_models.Deprecated202007GetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def update_customer_202007(customer_id: str = Field(..., description="The unique identifier of the customer to update. This ID is required to specify which customer record should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in Shopify. Modify customer details such as email, name, phone, and other profile attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCustomersParamCustomerIdRequest(
            path=_models.Deprecated202007UpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def delete_customer_v202007(customer_id: str = Field(..., description="The unique identifier of the customer to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. Note that a customer cannot be deleted if they have any existing orders."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCustomersParamCustomerIdRequest(
            path=_models.Deprecated202007DeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_v202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_v202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def generate_customer_account_activation_url_2020_07(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the account activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.Deprecated202007CreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def list_customer_addresses_202007(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202007GetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def create_customer_address_v202007(customer_id: str = Field(..., description="The unique identifier of the customer for whom the address is being created.")) -> dict[str, Any] | ToolResult:
    """Creates a new address for a customer. The address is added to the customer's address list and can be set as their default billing or shipping address."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202007CreateCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_customer_address_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_customer_address_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_customer_address_v202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_customer_address_v202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def bulk_update_customer_addresses_202007(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to target for the bulk operation. The order may be significant depending on the operation type."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., delete, set as default). Refer to API documentation for valid operation values."),
) -> dict[str, Any] | ToolResult:
    """Performs bulk operations on multiple customer addresses, allowing you to update, delete, or modify address records in a single request."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.Deprecated202007UpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.Deprecated202007UpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def get_customer_address_202007(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific customer address by customer ID and address ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202007GetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def update_customer_address_2020_07(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the specific address to update for the customer."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing customer address by customer ID and address ID. Use this to modify address details such as street, city, postal code, or other address information for a specific customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202007UpdateCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_address_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_address_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_address_2020_07", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_address_2020_07",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def delete_customer_address_202007(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific address from a customer's address list. This operation permanently deletes the address record associated with the given customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202007DeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def set_customer_default_address_202007(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Sets a specific address as the default address for a customer. This updates the customer's primary address used for shipping and billing purposes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.Deprecated202007UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_customer_default_address_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_customer_default_address_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_customer_default_address_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_customer_default_address_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customer_orders_202007(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports standard Order resource query parameters for filtering, sorting, and pagination."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetCustomersParamCustomerIdOrdersRequest(
            path=_models.Deprecated202007GetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def send_customer_invite_202007(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation email to a customer, allowing them to create or access their account on the store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.Deprecated202007CreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_location(code: str | None = Field(None, description="The discount code to look up (e.g. 'SUMMER20')")) -> dict[str, Any] | ToolResult:
    """Retrieves the location URI of a discount code. The discount code's location is returned in the HTTP Location header rather than in the response body, allowing you to fetch the full discount code resource from the returned URI."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetDiscountCodesLookupRequest(
            query=_models.Deprecated202007GetDiscountCodesLookupRequestQuery(code=code)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_location: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/discount_codes/lookup.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_location")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_location", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_location",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def list_events_202007(
    limit: Any | None = Field(None, description="Maximum number of events to return per request. Defaults to 50 if not specified; cannot exceed 250."),
    filter_: Any | None = Field(None, alias="filter", description="Filters events to show only those matching the specified criteria. Use this to narrow results to relevant event types or resources."),
    verb: Any | None = Field(None, description="Filters events to show only those of a specific action type (e.g., create, update, delete). Useful for tracking particular kinds of changes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetEventsRequest(
            query=_models.Deprecated202007GetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_events_count_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. Use this to get a quick metric of how many events exist without fetching individual event details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_event_2020_07(event_id: str = Field(..., description="The unique identifier of the event to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single event by its ID from the Shopify admin. Use this to fetch detailed information about a specific event that occurred in your store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetEventsParamEventIdRequest(
            path=_models.Deprecated202007GetEventsParamEventIdRequestPath(event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def get_fulfillment_order_202007(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID. Use this to fetch current status, line items, and fulfillment information for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.Deprecated202007GetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def cancel_fulfillment_order_2020_07(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation is used to prevent a fulfillment order from being fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def send_fulfillment_order_cancellation_request_2020_07(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order in the system."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request. This provides context to the fulfillment service about why the cancellation is being requested."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider to stop processing or cancel an already-in-progress fulfillment."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def accept_fulfillment_order_cancellation_request_2020_07(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to accept the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, confirming that the fulfillment service should cancel the specified order. Optionally include a reason message for the acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def reject_fulfillment_order_cancellation_request_2020_07(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason will be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this when you need to decline a cancellation and optionally provide a reason to the fulfillment service."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def close_fulfillment_order_2020_07(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or note explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def send_fulfillment_request_202007(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request, such as special instructions or notes for the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional array of fulfillment order line items to request for fulfillment. If omitted, all line items in the fulfillment order will be requested."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally specifying which line items to fulfill and including an optional message."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def accept_fulfillment_request_2020_07(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message providing context or reasoning for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, confirming that the fulfillment service will proceed with fulfilling the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def reject_fulfillment_request_202007(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202007CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove
@mcp.tool()
async def list_locations_for_fulfillment_order_move_2020_07(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves all locations where a fulfillment order can be moved to. The results are sorted alphabetically by location name in ascending order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.Deprecated202007GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice
@mcp.tool()
async def list_fulfillment_services_legacy_202007() -> dict[str, Any] | ToolResult:
    """Retrieve all fulfillment services available for the store. This endpoint returns a list of fulfillment service configurations that can be used to manage order fulfillment."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/fulfillment_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_services_legacy_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_services_legacy_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_services_legacy_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_202007() -> dict[str, Any] | ToolResult:
    """Creates a fulfillment for one or many fulfillment orders associated with the same order and assigned to the same location. Use this to mark items as shipped and transition fulfillment orders to fulfilled status."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_202007(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This ID references a specific fulfillment order that has been previously created.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment order. This operation marks the specified fulfillment as cancelled and updates its status accordingly."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.Deprecated202007CreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_tracking_2020_07(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates tracking information for a specific fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.Deprecated202007CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def list_gift_cards_2020_07(
    status: Any | None = Field(None, description="Filter results to gift cards with a specific status: enabled to show only active gift cards, or disabled to show only inactive gift cards. Omit to retrieve all gift cards regardless of status."),
    limit: Any | None = Field(None, description="Maximum number of gift cards to return per request, between 1 and 250 (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of gift cards, optionally filtered by status. Results are paginated using cursor-based links provided in the response header rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetGiftCardsRequest(
            query=_models.Deprecated202007GetGiftCardsRequestQuery(status=status, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_gift_cards_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/gift_cards.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_gift_cards_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_gift_cards_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_gift_cards_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def count_gift_cards_v202007(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetGiftCardsCountRequest(
            query=_models.Deprecated202007GetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_gift_cards_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_gift_cards_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_gift_cards_v202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_gift_cards_v202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def search_gift_cards_202007(
    order: Any | None = Field(None, description="The field and sort direction for ordering results. Defaults to most recently disabled gift cards first."),
    query: Any | None = Field(None, description="The search query text to match against indexed gift card fields including created_at, updated_at, disabled_at, balance, initial_value, amount_spent, email, and last_characters."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50."),
) -> dict[str, Any] | ToolResult:
    """Search for gift cards using indexed fields like balance, email, or creation date. Results are paginated and returned via link headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetGiftCardsSearchRequest(
            query=_models.Deprecated202007GetGiftCardsSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_gift_cards_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/gift_cards/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_gift_cards_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_gift_cards_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_gift_cards_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def update_gift_card_202007(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Update an existing gift card's expiry date, note, and template suffix. Note that the gift card's balance cannot be modified through the API."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateGiftCardsParamGiftCardIdRequest(
            path=_models.Deprecated202007UpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def disable_gift_card_202007(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.Deprecated202007CreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def list_inventory_items_202007(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request. Defaults to 50 items; maximum allowed is 250 items."),
    ids: int | None = Field(None, description="Filter results to only inventory items with the specified IDs. Provide one or more integer IDs to narrow the results."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetInventoryItemsRequest(
            query=_models.Deprecated202007GetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def get_inventory_item_legacy(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single inventory item by its unique identifier. Use this to fetch detailed information about a specific inventory item in your Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetInventoryItemsParamInventoryItemIdRequest(
            path=_models.Deprecated202007GetInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_inventory_item_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_inventory_item_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_inventory_item_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_inventory_item_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def list_inventory_levels_202007(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request. Defaults to 50 if not specified; cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across locations and inventory items. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetInventoryLevelsRequest(
            query=_models.Deprecated202007GetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def delete_inventory_level_202007(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted."),
    location_id: int | None = Field(None, description="The unique identifier of the location from which the inventory item should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This operation disconnects the inventory item from that location; note that every inventory item must maintain at least one inventory level, so you must connect the item to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteInventoryLevelsRequest(
            query=_models.Deprecated202007DeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_locations_202007() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. Use this to get a complete list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_locations_count_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. Useful for understanding inventory distribution across fulfillment locations."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_location_202007(location_id: str = Field(..., description="The unique identifier of the location to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single location by its ID from the Shopify inventory system. Use this to fetch detailed information about a specific location."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetLocationsParamLocationIdRequest(
            path=_models.Deprecated202007GetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_inventory_levels_for_location_202007(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves all inventory levels for a specific location. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.Deprecated202007GetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafields_count_2020_07() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields associated with a resource. Useful for understanding the volume of custom metadata attached to an object without fetching individual metafield records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order, orders/order
@mcp.tool()
async def list_orders_v202007(
    ids: Any | None = Field(None, description="Filter results to only orders with the specified IDs, provided as a comma-separated list."),
    limit: Any | None = Field(None, description="Maximum number of orders to return per page, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Show only orders processed at or after this date, specified in ISO 8601 format with timezone."),
    processed_at_max: Any | None = Field(None, description="Show only orders processed at or before this date, specified in ISO 8601 format with timezone."),
    attribution_app_id: Any | None = Field(None, description="Filter orders by the app that created them, using the app's ID or 'current' for the requesting app."),
    status: Any | None = Field(None, description="Filter orders by their fulfillment state: open (default), closed, cancelled, or any (including archived orders)."),
    financial_status: Any | None = Field(None, description="Filter orders by their payment state: authorized, pending, paid, partially_paid, refunded, voided, partially_refunded, unpaid, or any (defaults to any)."),
    fulfillment_status: Any | None = Field(None, description="Filter orders by their shipment state: shipped (fulfilled), partial, unshipped (null), unfulfilled (null or partial), or any (defaults to any)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of orders with filtering options by status, financial state, fulfillment state, and other criteria. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersRequest(
            query=_models.Deprecated202007GetOrdersRequestQuery(ids=ids, limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, attribution_app_id=attribution_app_id, status=status, financial_status=financial_status, fulfillment_status=fulfillment_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_orders_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/orders.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_orders_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_orders_v202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_orders_v202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def list_fulfillment_orders_for_order_2020_07(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order. Fulfillment orders represent the items in an order that need to be fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders_for_order_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders_for_order_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders_for_order_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders_for_order_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def list_fulfillments_for_order_v202007(
    order_id: str = Field(..., description="The unique identifier of the order whose fulfillments you want to retrieve."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the limit parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_order_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_order_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_order_v202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_order_v202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_for_order_legacy_202007(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for specified line items in an order. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order_legacy_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order_legacy_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order_legacy_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order_legacy_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_count_for_order_2020_07(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding fulfillment status and logistics tracking without fetching full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_count_for_order_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_count_for_order_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_count_for_order_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_count_for_order_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order. Use this to access fulfillment status, tracking information, and line item details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202007UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def complete_fulfillment_2020_07(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the order that should be marked as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped or delivered to the customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent
@mcp.tool()
async def list_fulfillment_events_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all events associated with a specific fulfillment for an order, including status updates and tracking information."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_events_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_events_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_events_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_events_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def open_fulfillment_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be opened."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as open."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as open, allowing it to receive additional items or changes. This operation transitions a fulfillment from a closed state back to an open state."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/fulfillments/{fulfillment_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def list_order_refunds_202007(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="Maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays refund amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of refunds for a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202007GetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def create_order_refund_202007(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Shipping refund details. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each with the line item ID, quantity to refund, restock type (no_restock, cancel, or return), and location ID for restocking (required for cancel and return types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Should be obtained from the calculate endpoint for accuracy."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transactions to submit. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202007CreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def calculate_order_refund_2020_07(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping cost. The amount property takes precedence over full_refund."),
    refund_line_items: Any | None = Field(None, description="List of line items to refund, each specifying the line item ID, quantity to refund, restock instructions (no_restock, cancel, or return), and optionally the location ID where items should be restocked. If location_id is omitted for return or cancel restocks, the endpoint will suggest an appropriate location."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required whenever a shipping amount is specified, particularly important for multi-currency orders."),
) -> dict[str, Any] | ToolResult:
    """Calculate refund transactions for an order based on line items and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund, including any necessary adjustments to restock instructions."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.Deprecated202007CreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_order_refund_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_order_refund_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_order_refund_2020_07", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_order_refund_2020_07",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def get_refund_legacy(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific refund for an order. Use this to view refund information including amounts, line items, and status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.Deprecated202007GetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def list_order_risks_2020_07(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve associated risks.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. This endpoint uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202007GetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def create_order_risk_202007(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202007CreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def update_order_risk_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk for a specific order. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202007UpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def delete_order_risk_202007(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to delete."),
    risk_id: str = Field(..., description="The unique identifier of the risk assessment to delete."),
) -> dict[str, Any] | ToolResult:
    """Removes a fraud risk assessment from an order. Note that you can only delete risks created by your application; risks created by other applications cannot be deleted."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202007DeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, policy, store-properties/policy
@mcp.tool()
async def list_policies_2020_07() -> dict[str, Any] | ToolResult:
    """Retrieves all policies configured for the shop, including return, privacy, shipping, and other store policies."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/policies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_policies_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_policies_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_policies_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_codes_batch_202007(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation job object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.Deprecated202007CreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_codes_batch_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_codes_batch_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_codes_batch_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_codes_batch_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_batch_202007(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch job."),
    batch_id: str = Field(..., description="The unique identifier of the specific batch job for discount code creation."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule. Use this to check the progress and results of bulk discount code generation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.Deprecated202007GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_batch_202007(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the batch job containing the discount codes to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a specific batch creation job for a price rule. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.Deprecated202007GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_price_rule_2020_07(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.Deprecated202007GetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_code_for_price_rule_deprecated(price_rule_id: str = Field(..., description="The unique identifier of the price rule to which this discount code will be associated.")) -> dict[str, Any] | ToolResult:
    """Creates a new discount code associated with a specific price rule. The discount code can be used by customers to apply the price rule's discounts during checkout."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreatePriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.Deprecated202007CreatePriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_code_for_price_rule_deprecated: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_code_for_price_rule_deprecated")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_code_for_price_rule_deprecated", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_code_for_price_rule_deprecated",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def delete_discount_code_202007(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific discount code associated with a price rule. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202007DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, productlisting, sales-channels/productlisting
@mcp.tool()
async def update_product_listing_v202007(product_listing_id: str = Field(..., description="The unique identifier of the product listing to update.")) -> dict[str, Any] | ToolResult:
    """Update a product listing to publish or modify a product's presence on your sales channel app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateProductListingsParamProductListingIdRequest(
            path=_models.Deprecated202007UpdateProductListingsParamProductListingIdRequestPath(product_listing_id=product_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_listing_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/product_listings/{product_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/product_listings/{product_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_listing_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_listing_v202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_listing_v202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, productlisting, sales-channels/productlisting
@mcp.tool()
async def delete_product_listing_v2020_07(product_listing_id: str = Field(..., description="The unique identifier of the product listing to delete. This ID represents the specific product-to-channel relationship you want to remove.")) -> dict[str, Any] | ToolResult:
    """Remove a product listing to unpublish a product from your sales channel app. This operation deletes the listing relationship without affecting the underlying product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteProductListingsParamProductListingIdRequest(
            path=_models.Deprecated202007DeleteProductListingsParamProductListingIdRequestPath(product_listing_id=product_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_listing_v2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/product_listings/{product_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/product_listings/{product_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_listing_v2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_listing_v2020_07", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_listing_v2020_07",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def list_products_2020_07(
    ids: Any | None = Field(None, description="Filter results to only include products with IDs matching this comma-separated list of product identifiers."),
    limit: Any | None = Field(None, description="Maximum number of products to return per page, between 1 and 250 (defaults to 50)."),
    title: Any | None = Field(None, description="Filter results to only include products whose title matches this value."),
    vendor: Any | None = Field(None, description="Filter results to only include products from this vendor."),
    handle: Any | None = Field(None, description="Filter results to only include products with this handle (URL-friendly identifier)."),
    product_type: Any | None = Field(None, description="Filter results to only include products of this type."),
    status: Any | None = Field(None, description="Filter results by product status: active (default), archived, or draft."),
    collection_id: Any | None = Field(None, description="Filter results to only include products belonging to this collection ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published, unpublished, or any (default shows all)."),
    presentment_currencies: Any | None = Field(None, description="Return prices in only these currencies, specified as a comma-separated list of ISO 4217 currency codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of products from the store, with support for filtering by various attributes and sorting options. Uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsRequest(
            query=_models.Deprecated202007GetProductsRequestQuery(ids=ids, limit=limit, title=title, vendor=vendor, handle=handle, product_type=product_type, status=status, collection_id=collection_id, published_status=published_status, presentment_currencies=presentment_currencies)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_products_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_products_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_products_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_products_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_products_count_202007(
    vendor: Any | None = Field(None, description="Filter the count to include only products from a specific vendor."),
    product_type: Any | None = Field(None, description="Filter the count to include only products of a specific product type."),
    collection_id: Any | None = Field(None, description="Filter the count to include only products belonging to a specific collection by its ID."),
    published_status: Any | None = Field(None, description="Filter products by their publication status: use 'published' for only published products, 'unpublished' for only unpublished products, or 'any' to include all products regardless of status (default is 'any')."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of products in the store, with optional filtering by vendor, product type, collection, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsCountRequest(
            query=_models.Deprecated202007GetProductsCountRequestQuery(vendor=vendor, product_type=product_type, collection_id=collection_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_products_count_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/products/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_products_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_products_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_products_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_product_202007(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsParamProductIdRequest(
            path=_models.Deprecated202007GetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def delete_product_202007(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteProductsParamProductIdRequest(
            path=_models.Deprecated202007DeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def list_product_images_202007(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of image resources for the given product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsParamProductIdImagesRequest(
            path=_models.Deprecated202007GetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def create_product_image_202007(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a specific product. The image will be associated with the product and can be used to display product visuals in the storefront."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateProductsParamProductIdImagesRequest(
            path=_models.Deprecated202007CreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_images_count_202007(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for determining how many product images exist without fetching the full image data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsParamProductIdImagesCountRequest(
            path=_models.Deprecated202007GetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_image_legacy(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image."),
    image_id: str = Field(..., description="The unique identifier of the specific image to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve a specific product image by its ID. Returns detailed metadata for the requested image associated with a product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202007GetProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_image_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_image_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_image_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_image_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def update_product_image_202007(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be updated."),
    image_id: str = Field(..., description="The unique identifier of the image within the product to be updated."),
) -> dict[str, Any] | ToolResult:
    """Update the properties of an existing product image, such as alt text, position, or other image metadata."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202007UpdateProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_image_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_image_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_image_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_image_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def delete_product_image_202007(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be deleted."),
    image_id: str = Field(..., description="The unique identifier of the image to be deleted from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image associated with a product. This removes the image from the product's image gallery."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202007DeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def list_recurring_application_charges_202007() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your Shopify app. Use this to view subscription charges that have been set up for the store."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def update_recurring_application_charge_capped_amount_2020_07(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to update."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount limit for the recurring charge, specified as a whole number representing the maximum billable amount."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount limit for an active recurring application charge. This allows you to modify the maximum amount that can be charged to the merchant for this recurring billing plan."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.Deprecated202007UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.Deprecated202007UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount_2020_07", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount_2020_07",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge_202007(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to create the usage charge.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill merchants for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202007CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def get_usage_charge_for_recurring_application_charge_202007(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge that contains the usage charge you want to retrieve."),
    usage_charge_id: str = Field(..., description="The unique identifier of the specific usage charge to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single usage charge associated with a recurring application charge. Use this to fetch details about a specific metered billing charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequest(
            path=_models.Deprecated202007GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id, usage_charge_id=usage_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_usage_charge_for_recurring_application_charge_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_usage_charge_for_recurring_application_charge_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_usage_charge_for_recurring_application_charge_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_usage_charge_for_recurring_application_charge_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def list_redirects_202007(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250 (defaults to 50)."),
    path: Any | None = Field(None, description="Filter results to show only redirects with a specific source path."),
    target: Any | None = Field(None, description="Filter results to show only redirects pointing to a specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of URL redirects configured in the online store. Results are paginated using link headers in the response rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetRedirectsRequest(
            query=_models.Deprecated202007GetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def create_redirect_202007() -> dict[str, Any] | ToolResult:
    """Creates a URL redirect for your store. Full URLs provided in the path property are automatically converted to absolute paths (domain removed). For example, a path of 'http://www.johns-apparel.com/springwear' will be saved as 'springwear'."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/redirects.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_redirect_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_redirect_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_redirect_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirects_count_2020_07(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetRedirectsCountRequest(
            query=_models.Deprecated202007GetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirects_count_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirects_count_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirects_count_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirects_count_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirect_202007(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store. Use this to fetch details about a specific URL redirect configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202007GetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def update_redirect_v202007(redirect_id: str = Field(..., description="The unique identifier of the redirect to update. This ID is required to locate and modify the specific redirect.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect in the online store. Modify redirect settings such as the target path or status by providing the redirect's unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202007UpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect_v202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect_v202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def delete_redirect_v202007(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202007DeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect_v202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect_v202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect_v202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect_v202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def create_report_legacy_202007(
    name: Any | None = Field(None, description="The display name for the report. Must be 255 characters or fewer."),
    shopify_ql: Any | None = Field(None, description="The ShopifyQL query string that defines what data the report will retrieve and analyze."),
) -> dict[str, Any] | ToolResult:
    """Creates a new custom report in the Shopify admin. Reports use ShopifyQL queries to analyze store data and can be referenced by name for future access."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007CreateReportsRequest(
            query=_models.Deprecated202007CreateReportsRequestQuery(name=name, shopify_ql=shopify_ql)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_report_legacy_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_report_legacy_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_report_legacy_202007", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_report_legacy_202007",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def get_report_202007(report_id: str = Field(..., description="The unique identifier of the report to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single report that was created by your app. Use this to fetch details about a specific report by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetReportsParamReportIdRequest(
            path=_models.Deprecated202007GetReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_report_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_report_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_report_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_report_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def delete_report_202007(report_id: str = Field(..., description="The unique identifier of the report to delete. This is a required string value that identifies which report should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific report from the Shopify admin. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteReportsParamReportIdRequest(
            path=_models.Deprecated202007DeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def get_script_tags_count_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the store. Useful for understanding the scope of script tag usage without fetching individual records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone
@mcp.tool()
async def list_shipping_zones_202007() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop
@mcp.tool()
async def get_shop_202007() -> dict[str, Any] | ToolResult:
    """Retrieves the current shop's configuration and metadata, including store name, currency, timezone, and other essential shop-level settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance
@mcp.tool()
async def get_shopify_payments_balance_2020_07() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, including available funds and pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def list_payouts(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after the specified date (inclusive)."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before the specified date (inclusive)."),
    status: Any | None = Field(None, description="Filter payouts by their status (e.g., pending, completed, failed, cancelled)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all payouts ordered by payout date, with the most recent first. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetShopifyPaymentsPayoutsRequest(
            query=_models.Deprecated202007GetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_payouts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_payouts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_payouts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_payouts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def get_shopify_payments_payout_2020_07(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.Deprecated202007GetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_shopify_payments_payout_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_payout_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_payout_2020_07", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_payout_2020_07",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def list_smart_collections_202007(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Defaults to 50 and cannot exceed 250."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to only smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by the smart collection's URL-friendly handle."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from your store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetSmartCollectionsRequest(
            query=_models.Deprecated202007GetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def create_smart_collection_legacy() -> dict[str, Any] | ToolResult:
    """Creates a new smart collection that automatically groups products based on specified rules and conditions. Smart collections dynamically update their membership as products are added, modified, or removed from your store."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/smart_collections.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_smart_collection_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_smart_collection_legacy", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_smart_collection_legacy",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def count_smart_collections_202007(
    title: Any | None = Field(None, description="Filter to only count smart collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter to only count smart collections that include the specified product."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' to include all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetSmartCollectionsCountRequest(
            query=_models.Deprecated202007GetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def get_smart_collection_legacy(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single smart collection by its ID. Smart collections are dynamically generated product collections based on defined rules."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202007GetSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_smart_collection_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_smart_collection_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_smart_collection_legacy", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_smart_collection_legacy",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def update_smart_collection_202007(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update. This ID is required to target the specific smart collection for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing smart collection by its ID. Use this to modify smart collection properties such as title, rules, and other configuration settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202007UpdateSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_202007", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_202007",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def delete_smart_collection_202007(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202007DeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tendertransaction
@mcp.tool()
async def list_tender_transactions_202007(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Filter to show only tender transactions processed on or after this date (ISO 8601 format)."),
    processed_at_max: Any | None = Field(None, description="Filter to show only tender transactions processed on or before this date (ISO 8601 format)."),
    order: Any | None = Field(None, description="Sort results by processed_at timestamp in either ascending or descending order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of tender transactions. Results are paginated using link headers in the response; use the provided links to navigate between pages."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetTenderTransactionsRequest(
            query=_models.Deprecated202007GetTenderTransactionsRequestQuery(limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tender_transactions_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/tender_transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tender_transactions_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tender_transactions_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tender_transactions_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def list_themes_202007() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store, including both published and unpublished themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def create_theme_from_archive() -> dict[str, Any] | ToolResult:
    """Creates a new unpublished theme from a ZIP file hosted at a public URL. Optionally publish the theme immediately by including role: main in the request; publication completes after Shopify extracts and stores all theme files, which may take a few minutes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_theme_from_archive")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_theme_from_archive", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_theme_from_archive",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def get_theme_202007(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single theme by its ID from the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetThemesParamThemeIdRequest(
            path=_models.Deprecated202007GetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def update_theme_legacy(theme_id: str = Field(..., description="The unique identifier of the theme to update. This ID is returned when a theme is created and is required to target the specific theme for modification.")) -> dict[str, Any] | ToolResult:
    """Updates the configuration and settings of an existing theme in the Shopify store. Use this to modify theme properties after creation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007UpdateThemesParamThemeIdRequest(
            path=_models.Deprecated202007UpdateThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_theme_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_theme_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_theme_legacy", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_theme_legacy",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def delete_theme_2020_07(theme_id: str = Field(..., description="The unique identifier of the theme to delete, returned as a string by the Shopify API.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the Shopify store. The theme must not be the currently active theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteThemesParamThemeIdRequest(
            path=_models.Deprecated202007DeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_2020_07: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_2020_07")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_2020_07", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_2020_07",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def get_theme_asset_deprecated(
    theme_id: str = Field(..., description="The unique identifier of the theme containing the asset to retrieve."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key path of the asset file to retrieve (e.g., templates/index.liquid). This parameter is required to specify which asset to fetch from the theme."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single asset file from a theme by its key identifier. The asset key must be provided as a query parameter to specify which theme asset to retrieve."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetThemesParamThemeIdAssetsRequest(
            path=_models.Deprecated202007GetThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.Deprecated202007GetThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_asset_deprecated: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_asset_deprecated")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_asset_deprecated", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_asset_deprecated",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def delete_theme_asset_202007(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key (file path) of the asset to delete from the theme. This identifies which specific asset file to remove."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key within the theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007DeleteThemesParamThemeIdAssetsRequest(
            path=_models.Deprecated202007DeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.Deprecated202007DeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-07/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-07/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset_202007", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset_202007",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def list_webhooks_legacy_202007(
    address: Any | None = Field(None, description="Filter webhook subscriptions to only those sending POST requests to this specific URI."),
    limit: Any | None = Field(None, description="Maximum number of webhook subscriptions to return per request, between 1 and 250 (defaults to 50)."),
    topic: Any | None = Field(None, description="Filter webhook subscriptions by topic name. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of webhook subscriptions configured for your Shopify store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetWebhooksRequest(
            query=_models.Deprecated202007GetWebhooksRequestQuery(address=address, limit=limit, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_webhooks_legacy_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/webhooks.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_webhooks_legacy_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_webhooks_legacy_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhooks_legacy_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def count_webhooks_202007(
    address: Any | None = Field(None, description="Filter results to webhook subscriptions that deliver POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter results to webhook subscriptions with a specific topic. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the Shopify store, with optional filtering by delivery address or topic."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202007GetWebhooksCountRequest(
            query=_models.Deprecated202007GetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_webhooks_202007: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-07/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_webhooks_202007")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_webhooks_202007", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_webhooks_202007",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge, latest_api_version
@mcp.tool()
async def list_application_charges() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all application charges for the Shopify store. Use this to view billing charges associated with your app installation."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_charges")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_charges", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_charges",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge, latest_api_version
@mcp.tool()
async def create_application_charge() -> dict[str, Any] | ToolResult:
    """Creates a new application charge for billing purposes. This initiates a charge request that the merchant must approve before payment is processed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_application_charge", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_application_charge",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge, latest_api_version
@mcp.tool()
async def get_application_charge(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific application charge by its ID, including status, price, and billing information."""

    # Construct request model with validation
    try:
        _request = _models.GetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.GetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit, latest_api_version
@mcp.tool()
async def list_application_credits() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits issued to the app by Shopify. Use this to view credit balances and transaction history for billing purposes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit, latest_api_version
@mcp.tool()
async def get_application_credit(application_credit_id: str = Field(..., description="The unique identifier of the application credit to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single application credit by its ID. Use this to fetch information about a specific credit issued to an app."""

    # Construct request model with validation
    try:
        _request = _models.GetApplicationCreditsParamApplicationCreditIdRequest(
            path=_models.GetApplicationCreditsParamApplicationCreditIdRequestPath(application_credit_id=application_credit_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_credit: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/application_credits/{application_credit_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/application_credits/{application_credit_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_credit")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_credit", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_credit",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def list_article_authors() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all article authors available in the Shopify store. Use this to populate author selections or display author information across your online store."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/articles/authors.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_article_authors")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_article_authors", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_article_authors",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def list_article_tags(
    limit: Any | None = Field(None, description="Maximum number of tags to return in the response. Limits the size of the result set."),
    popular: Any | None = Field(None, description="When included, sorts tags by popularity in descending order (most popular first). Omit this parameter to retrieve tags in default order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all tags used across articles in the online store. Results can be optionally filtered by count and sorted by popularity."""

    # Construct request model with validation
    try:
        _request = _models.GetArticlesTagsRequest(
            query=_models.GetArticlesTagsRequestQuery(limit=limit, popular=popular)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_article_tags: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/articles/tags.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_article_tags")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_article_tags", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_article_tags",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, assignedfulfillmentorder, shipping-and-fulfillment/assignedfulfillmentorder, latest_api_version
@mcp.tool()
async def list_assigned_fulfillment_orders(
    assignment_status: Any | None = Field(None, description="Filter results by the fulfillment request status: use 'cancellation_requested' for orders where the merchant has requested cancellation, 'fulfillment_requested' for pending fulfillment requests, or 'fulfillment_accepted' for orders ready to be fulfilled."),
    location_ids: Any | None = Field(None, description="Limit results to fulfillment orders assigned to specific locations by providing their location IDs as a comma-separated list or array. Omit to retrieve orders from all assigned locations."),
) -> dict[str, Any] | ToolResult:
    """Retrieves fulfillment orders assigned to your app on the shop, optionally filtered by assignment status and location. Use this to monitor fulfillment requests, acceptances, and cancellations across your assigned locations."""

    # Construct request model with validation
    try:
        _request = _models.GetAssignedFulfillmentOrdersRequest(
            query=_models.GetAssignedFulfillmentOrdersRequestQuery(assignment_status=assignment_status, location_ids=location_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_assigned_fulfillment_orders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/assigned_fulfillment_orders.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_assigned_fulfillment_orders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_assigned_fulfillment_orders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_assigned_fulfillment_orders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def list_articles(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="Maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: 'published' for published articles only, 'unpublished' for unpublished articles only, or 'any' for all articles regardless of status (defaults to 'any')."),
    handle: Any | None = Field(None, description="Retrieve a single article by its URL-friendly handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those authored by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.GetBlogsParamBlogIdArticlesRequest(
            path=_models.GetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.GetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_articles: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_articles")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_articles", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_articles",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def count_articles_in_blog(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to count articles."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specific blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.GetBlogsParamBlogIdArticlesCountRequest(
            path=_models.GetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.GetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_articles_in_blog: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_articles_in_blog")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_articles_in_blog", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_articles_in_blog",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def get_article(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article."),
    article_id: str = Field(..., description="The unique identifier of the article to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single article from a blog by its ID. Returns the complete article resource including content, metadata, and publishing details."""

    # Construct request model with validation
    try:
        _request = _models.GetBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.GetBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def update_article(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to update."),
    article_id: str = Field(..., description="The unique identifier of the article to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing article within a blog. Modify article content, metadata, and other properties by providing the blog and article identifiers."""

    # Construct request model with validation
    try:
        _request = _models.UpdateBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.UpdateBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_article: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_article")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_article", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_article",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article, latest_api_version
@mcp.tool()
async def delete_article(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to delete."),
    article_id: str = Field(..., description="The unique identifier of the article to delete."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes an article from a blog. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.DeleteBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_article: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_article")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_article", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_article",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice, latest_api_version
@mcp.tool()
async def list_carrier_services() -> dict[str, Any] | ToolResult:
    """Retrieves all carrier services available for the store. Carrier services represent shipping methods that can be offered to customers during checkout."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/carrier_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_carrier_services")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_carrier_services", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_carrier_services",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice, latest_api_version
@mcp.tool()
async def get_carrier_service(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific carrier service by its unique identifier. Use this to fetch configuration and capabilities for a single shipping carrier integration."""

    # Construct request model with validation
    try:
        _request = _models.GetCarrierServicesParamCarrierServiceIdRequest(
            path=_models.GetCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_carrier_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_carrier_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_carrier_service", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_carrier_service",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice, latest_api_version
@mcp.tool()
async def update_carrier_service(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service configuration. Only the app that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.UpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice, latest_api_version
@mcp.tool()
async def delete_carrier_service(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to delete. This ID is returned when the carrier service is created.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a carrier service from the store. This removes the shipping method and all associated configurations."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCarrierServicesParamCarrierServiceIdRequest(
            path=_models.DeleteCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_carrier_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_carrier_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_carrier_service", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_carrier_service",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts, latest_api_version
@mcp.tool()
async def get_abandoned_checkouts_count(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts, or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.GetCheckoutsCountRequest(
            query=_models.GetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting, latest_api_version
@mcp.tool()
async def get_collection_listing(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve details for a specific collection listing that is published to your sales channel app, including visibility and configuration information."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectionListingsParamCollectionListingIdRequest(
            path=_models.GetCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_listing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_listing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_listing", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_listing",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting, latest_api_version
@mcp.tool()
async def publish_collection_to_sales_channel(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to update or create for publishing to the sales channel.")) -> dict[str, Any] | ToolResult:
    """Publish a collection to your sales channel by creating a collection listing. This makes the collection visible and available through your app."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCollectionListingsParamCollectionListingIdRequest(
            path=_models.UpdateCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for publish_collection_to_sales_channel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("publish_collection_to_sales_channel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("publish_collection_to_sales_channel", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="publish_collection_to_sales_channel",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting, latest_api_version
@mcp.tool()
async def delete_collection_listing(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to delete. This ID represents the specific publication of a collection to a sales channel.")) -> dict[str, Any] | ToolResult:
    """Remove a collection listing to unpublish a collection from your sales channel or app. This operation deletes the listing relationship without affecting the underlying collection."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCollectionListingsParamCollectionListingIdRequest(
            path=_models.DeleteCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_collection_listing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_collection_listing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_collection_listing", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_collection_listing",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting, latest_api_version
@mcp.tool()
async def list_collection_product_ids(
    collection_listing_id: str = Field(..., description="The unique identifier of the collection listing whose published products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of product IDs to return per request, ranging from 1 to 1000 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve the product IDs that are published to a specific collection. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectionListingsParamCollectionListingIdProductIdsRequest(
            path=_models.GetCollectionListingsParamCollectionListingIdProductIdsRequestPath(collection_listing_id=collection_listing_id),
            query=_models.GetCollectionListingsParamCollectionListingIdProductIdsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_product_ids: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collection_listings/{collection_listing_id}/product_ids.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collection_listings/{collection_listing_id}/product_ids.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_product_ids")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_product_ids", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_product_ids",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection, latest_api_version
@mcp.tool()
async def get_collection(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID, including metadata such as name, description, and product associations."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectionsParamCollectionIdRequest(
            path=_models.GetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection, latest_api_version
@mcp.tool()
async def list_collection_products(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, ranging from 1 to 250 products (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted according to the collection's configured sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectionsParamCollectionIdProductsRequest(
            path=_models.GetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.GetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect, latest_api_version
@mcp.tool()
async def list_collects(limit: Any | None = Field(None, description="Maximum number of collects to return per request, between 1 and 250. Defaults to 50 if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of collects (product-to-collection associations) from the store. Uses cursor-based pagination via response headers; the limit parameter controls batch size."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectsRequest(
            query=_models.GetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect, latest_api_version
@mcp.tool()
async def get_collects_count(collection_id: int | None = Field(None, description="Filter the count to only include collects within a specific collection. If omitted, returns the count of all collects across the store.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects (product-to-collection associations) in the store, optionally filtered by a specific collection."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectsCountRequest(
            query=_models.GetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect, latest_api_version
@mcp.tool()
async def get_collect(collect_id: str = Field(..., description="The unique identifier of the collect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection by its ID. Use this to fetch detailed information about a single collect resource."""

    # Construct request model with validation
    try:
        _request = _models.GetCollectsParamCollectIdRequest(
            path=_models.GetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect, latest_api_version
@mcp.tool()
async def remove_product_from_collection(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the link between a product and a collection.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCollectsParamCollectIdRequest(
            path=_models.DeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country, latest_api_version
@mcp.tool()
async def list_countries() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all countries available in the Shopify store, including their codes and regional information."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_countries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_countries", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_countries",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country, latest_api_version
@mcp.tool()
async def get_countries_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of countries available in the Shopify store. This is useful for understanding the scope of country data without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/countries/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_countries_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_countries_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_countries_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country, latest_api_version
@mcp.tool()
async def get_country(country_id: str = Field(..., description="The unique identifier of the country to retrieve. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a specific country, including provinces, taxes, and shipping settings configured in the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.GetCountriesParamCountryIdRequest(
            path=_models.GetCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_country: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_country")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_country", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_country",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country, latest_api_version
@mcp.tool()
async def delete_country(country_id: str = Field(..., description="The unique identifier of the country to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a country from the store's shipping and tax configuration. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCountriesParamCountryIdRequest(
            path=_models.DeleteCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_country: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_country")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_country", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_country",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province, latest_api_version
@mcp.tool()
async def list_provinces_for_country(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve provinces. This ID must correspond to a valid country in the Shopify system.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of provinces or states for a specific country. Use this to populate province/state selectors or validate province information for a given country."""

    # Construct request model with validation
    try:
        _request = _models.GetCountriesParamCountryIdProvincesRequest(
            path=_models.GetCountriesParamCountryIdProvincesRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_provinces_for_country: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/countries/{country_id}/provinces.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/countries/{country_id}/provinces.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_provinces_for_country")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_provinces_for_country", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_provinces_for_country",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province, latest_api_version
@mcp.tool()
async def get_province_count_for_country(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve the province count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of provinces or states for a specified country. Useful for understanding administrative divisions available in a particular country."""

    # Construct request model with validation
    try:
        _request = _models.GetCountriesParamCountryIdProvincesCountRequest(
            path=_models.GetCountriesParamCountryIdProvincesCountRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_count_for_country: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/countries/{country_id}/provinces/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/countries/{country_id}/provinces/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_count_for_country")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_count_for_country", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_count_for_country",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province, latest_api_version
@mcp.tool()
async def get_province_for_country(
    country_id: str = Field(..., description="The unique identifier for the country. This ID is used to scope the province lookup to the correct country."),
    province_id: str = Field(..., description="The unique identifier for the province within the specified country. This ID identifies which province's details to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a specific province within a country. Use this to fetch province data such as name, code, and tax information for a given country."""

    # Construct request model with validation
    try:
        _request = _models.GetCountriesParamCountryIdProvincesParamProvinceIdRequest(
            path=_models.GetCountriesParamCountryIdProvincesParamProvinceIdRequestPath(country_id=country_id, province_id=province_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_for_country: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/countries/{country_id}/provinces/{province_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/countries/{country_id}/provinces/{province_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_for_country")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_for_country", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_for_country",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency, latest_api_version
@mcp.tool()
async def list_currencies() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies that are enabled and available on the shop for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection, latest_api_version
@mcp.tool()
async def list_custom_collections(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 if not specified."),
    ids: Any | None = Field(None, description="Filter results to only collections with IDs matching this comma-separated list of collection IDs."),
    title: Any | None = Field(None, description="Filter results to only collections with an exact title match."),
    product_id: Any | None = Field(None, description="Filter results to only collections that contain a specific product, identified by product ID."),
    handle: Any | None = Field(None, description="Filter results by the collection's URL-friendly handle identifier."),
    published_status: Any | None = Field(None, description="Filter by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of custom collections with optional filtering and pagination. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomCollectionsRequest(
            query=_models.GetCustomCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_custom_collections: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/custom_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_custom_collections")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_custom_collections", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_custom_collections",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection, latest_api_version
@mcp.tool()
async def count_custom_collections(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain the specified product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomCollectionsCountRequest(
            query=_models.GetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection, latest_api_version
@mcp.tool()
async def get_custom_collection(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single custom collection by its ID, including its products, metadata, and configuration."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.GetCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_custom_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_custom_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_custom_collection", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_custom_collection",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection, latest_api_version
@mcp.tool()
async def update_custom_collection(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to update. This ID is returned when the collection is created and is required to target the specific collection for modification.")) -> dict[str, Any] | ToolResult:
    """Updates the properties of an existing custom collection, such as title, description, image, or sorting rules. Use this to modify collection metadata and organization settings."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.UpdateCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_custom_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_custom_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_custom_collection", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_custom_collection",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection, latest_api_version
@mcp.tool()
async def delete_custom_collection(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a custom collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.DeleteCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_custom_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_custom_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_custom_collection", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_custom_collection",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def list_customers(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer ID values."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersRequest(
            query=_models.GetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def get_customers_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the Shopify store. Useful for understanding customer base size and pagination planning."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def search_customers(
    order: Any | None = Field(None, description="Specifies the field and direction to sort results. Use format: field_name ASC or DESC (e.g., last_order_date DESC). Defaults to sorting by last order date in descending order."),
    query: Any | None = Field(None, description="The search query text to match against customer data such as names, emails, and other customer attributes."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Searches for customers in the shop by matching a query against customer data. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersSearchRequest(
            query=_models.GetCustomersSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_customers: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/customers/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_customers")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_customers", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_customers",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def get_customer(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single customer by their unique identifier. Use this to fetch customer profile data including contact information, addresses, and account details."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersParamCustomerIdRequest(
            path=_models.GetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def update_customer(customer_id: str = Field(..., description="The unique identifier of the customer to update. This is a required string value that identifies which customer record to modify.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in Shopify. Provide the customer ID and the fields you want to modify in the request body."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCustomersParamCustomerIdRequest(
            path=_models.UpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def delete_customer(customer_id: str = Field(..., description="The unique identifier of the customer to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. Note that a customer cannot be deleted if they have any existing orders associated with their account."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCustomersParamCustomerIdRequest(
            path=_models.DeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def generate_customer_account_activation_url(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.CreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.CreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def list_customer_addresses(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; page parameters are not supported."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersParamCustomerIdAddressesRequest(
            path=_models.GetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def create_address_for_customer(customer_id: str = Field(..., description="The unique identifier of the customer for whom the address is being created.")) -> dict[str, Any] | ToolResult:
    """Creates a new address for a customer. The address will be added to the customer's address book and can be used for shipping or billing purposes."""

    # Construct request model with validation
    try:
        _request = _models.CreateCustomersParamCustomerIdAddressesRequest(
            path=_models.CreateCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_address_for_customer: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_address_for_customer")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_address_for_customer", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_address_for_customer",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def bulk_update_customer_addresses(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to target for the bulk operation. The order of IDs may affect processing sequence depending on the operation type."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., delete, set as default). Consult API documentation for valid operation values."),
) -> dict[str, Any] | ToolResult:
    """Performs bulk operations on multiple addresses for a specific customer, allowing you to update, delete, or modify address records in a single request."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.UpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.UpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def get_customer_address(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific address associated with a customer account."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.GetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def update_customer_address(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the specific address to update for the customer."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing address for a specific customer. Provide the customer ID and address ID to modify the address details."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.UpdateCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_address", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_address",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def delete_customer_address(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Permanently removes a specific address from a customer's address list. The address will no longer be available for that customer."""

    # Construct request model with validation
    try:
        _request = _models.DeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.DeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address, latest_api_version
@mcp.tool()
async def set_customer_default_address(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Designates a specific address as the default address for a customer. This address will be used as the primary contact address for the customer account."""

    # Construct request model with validation
    try:
        _request = _models.UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_customer_default_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_customer_default_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_customer_default_address", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_customer_default_address",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def list_customer_orders(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports filtering and sorting through standard Order resource query parameters."""

    # Construct request model with validation
    try:
        _request = _models.GetCustomersParamCustomerIdOrdersRequest(
            path=_models.GetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer, latest_api_version
@mcp.tool()
async def send_customer_invite(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation email to a customer, allowing them to create or access their account on the store."""

    # Construct request model with validation
    try:
        _request = _models.CreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.CreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def lookup_discount_code_location(code: str | None = Field(None, description="The discount code to look up (e.g. 'SUMMER20')")) -> dict[str, Any] | ToolResult:
    """Retrieves the location URI of a discount code. The discount code's location is returned in the HTTP Location header rather than in the response body, allowing you to fetch the full discount code resource from the returned URI."""

    # Construct request model with validation
    try:
        _request = _models.GetDiscountCodesLookupRequest(
            query=_models.GetDiscountCodesLookupRequestQuery(code=code)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for lookup_discount_code_location: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/discount_codes/lookup.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("lookup_discount_code_location")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("lookup_discount_code_location", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="lookup_discount_code_location",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event, latest_api_version
@mcp.tool()
async def list_events(
    limit: Any | None = Field(None, description="Maximum number of events to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
    filter_: Any | None = Field(None, alias="filter", description="Filters events to show only those matching the specified criteria. Use this to narrow results to specific event types or conditions."),
    verb: Any | None = Field(None, description="Filters events to show only those of a specific type or action (e.g., 'create', 'update', 'delete'). Narrows results to events matching the specified verb."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetEventsRequest(
            query=_models.GetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event, latest_api_version
@mcp.tool()
async def get_events_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. Use this to get a quick aggregate metric without fetching individual event records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event, latest_api_version
@mcp.tool()
async def get_event(event_id: str = Field(..., description="The unique identifier of the event to retrieve. This ID is returned when events are listed or created.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single event by its unique identifier. Use this to fetch detailed information about a specific event that occurred in your Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.GetEventsParamEventIdRequest(
            path=_models.GetEventsParamEventIdRequestPath(event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder, latest_api_version
@mcp.tool()
async def get_fulfillment_order(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID, including order status, line items, and fulfillment requirements."""

    # Construct request model with validation
    try:
        _request = _models.GetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.GetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder, latest_api_version
@mcp.tool()
async def cancel_fulfillment_order(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order within your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation prevents further fulfillment actions on the specified order."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest, latest_api_version
@mcp.tool()
async def send_fulfillment_order_cancellation_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order in the system."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request. This provides context to the fulfillment service about why the order is being cancelled."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider that the order should be cancelled if not yet fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest, latest_api_version
@mcp.tool()
async def accept_fulfillment_order_cancellation_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the cancellation request is being accepted."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, notifying the fulfillment service that the cancellation has been approved. Optionally include a reason for accepting the request."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest, latest_api_version
@mcp.tool()
async def reject_fulfillment_order_cancellation_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason will be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this when you need to decline a cancellation and optionally provide a reason to the fulfillment service."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder, latest_api_version
@mcp.tool()
async def close_fulfillment_order(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or note explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest, latest_api_version
@mcp.tool()
async def send_fulfillment_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request, typically for communicating special instructions or notes to the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional list of specific line items from the fulfillment order to request for fulfillment. If omitted, all unfulfilled line items in the order are included in the request."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally specifying which line items to fulfill and including a message for the fulfillment service."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest, latest_api_version
@mcp.tool()
async def accept_fulfillment_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message providing context or reasoning for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, confirming that the fulfillment service will proceed with fulfilling the order."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest, latest_api_version
@mcp.tool()
async def reject_fulfillment_request(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def list_fulfillments_for_fulfillment_order(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve associated fulfillments.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific fulfillment order, including fulfillment details and status information."""

    # Construct request model with validation
    try:
        _request = _models.GetFulfillmentOrdersParamFulfillmentOrderIdFulfillmentsRequest(
            path=_models.GetFulfillmentOrdersParamFulfillmentOrderIdFulfillmentsRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_fulfillment_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_fulfillment_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove, latest_api_version
@mcp.tool()
async def list_locations_for_fulfillment_order_move(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves all locations where a fulfillment order can be moved to, sorted alphabetically by location name. Use this to determine valid destination locations before initiating a fulfillment order transfer."""

    # Construct request model with validation
    try:
        _request = _models.GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder, latest_api_version
@mcp.tool()
async def move_fulfillment_order(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to be moved."),
    new_location_id: Any | None = Field(None, description="The unique identifier of the destination location where the fulfillment order will be transferred. Must be a merchant-managed location."),
) -> dict[str, Any] | ToolResult:
    """Relocates a fulfillment order to a different merchant-managed location, enabling inventory redistribution across fulfillment centers."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequest(
            path=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.CreateFulfillmentOrdersParamFulfillmentOrderIdMoveRequestQuery(new_location_id=new_location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for move_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_orders/{fulfillment_order_id}/move.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("move_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("move_fulfillment_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="move_fulfillment_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice, latest_api_version
@mcp.tool()
async def list_fulfillment_services() -> dict[str, Any] | ToolResult:
    """Retrieve all fulfillment services available for the store. Fulfillment services represent the various fulfillment channels and logistics providers that can be used to fulfill orders."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/fulfillment_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_services")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_services", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_services",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice, latest_api_version
@mcp.tool()
async def get_fulfillment_service(fulfillment_service_id: str = Field(..., description="The unique identifier of the fulfillment service to retrieve. This ID is assigned by Shopify when the fulfillment service is created or integrated.")) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment service by its ID. Use this to fetch configuration, capabilities, and status of a fulfillment service integrated with your Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.GetFulfillmentServicesParamFulfillmentServiceIdRequest(
            path=_models.GetFulfillmentServicesParamFulfillmentServiceIdRequestPath(fulfillment_service_id=fulfillment_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_service", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_service",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice, latest_api_version
@mcp.tool()
async def update_fulfillment_service(fulfillment_service_id: str = Field(..., description="The unique identifier of the fulfillment service to update. This ID is returned when a fulfillment service is created and is required to target the correct service for modification.")) -> dict[str, Any] | ToolResult:
    """Update the configuration and settings of a specific fulfillment service. This allows you to modify fulfillment service details such as name, callback URLs, and other operational parameters."""

    # Construct request model with validation
    try:
        _request = _models.UpdateFulfillmentServicesParamFulfillmentServiceIdRequest(
            path=_models.UpdateFulfillmentServicesParamFulfillmentServiceIdRequestPath(fulfillment_service_id=fulfillment_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_service", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_service",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice, latest_api_version
@mcp.tool()
async def delete_fulfillment_service(fulfillment_service_id: str = Field(..., description="The unique identifier of the fulfillment service to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently delete a fulfillment service from your Shopify store. This removes the service and its associated configuration."""

    # Construct request model with validation
    try:
        _request = _models.DeleteFulfillmentServicesParamFulfillmentServiceIdRequest(
            path=_models.DeleteFulfillmentServicesParamFulfillmentServiceIdRequestPath(fulfillment_service_id=fulfillment_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_fulfillment_service: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillment_services/{fulfillment_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_fulfillment_service")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_fulfillment_service", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_fulfillment_service",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def cancel_fulfillment_direct(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This must be a valid fulfillment ID from your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment, preventing further processing or shipment of the associated items."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.CreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_direct: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_direct")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_direct", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_direct",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def update_fulfillment_tracking(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates tracking information for a specific fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def list_gift_cards(
    status: Any | None = Field(None, description="Filter results by gift card status. Use 'enabled' to show only active gift cards or 'disabled' to show only inactive gift cards. Omit to retrieve all gift cards regardless of status."),
    limit: Any | None = Field(None, description="Maximum number of gift cards to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of gift cards from the store. Results are paginated using cursor-based links provided in the response header rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.GetGiftCardsRequest(
            query=_models.GetGiftCardsRequestQuery(status=status, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_gift_cards: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/gift_cards.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_gift_cards")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_gift_cards", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_gift_cards",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def get_gift_cards_count(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit this parameter to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards in the store, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.GetGiftCardsCountRequest(
            query=_models.GetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_gift_cards_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_gift_cards_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_gift_cards_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_gift_cards_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def search_gift_cards(
    order: Any | None = Field(None, description="The field and direction to sort results by. Defaults to most recently disabled gift cards first. Specify a field name followed by ASC or DESC."),
    query: Any | None = Field(None, description="The search query text to match against indexed gift card fields including created_at, updated_at, disabled_at, balance, initial_value, amount_spent, email, and last_characters."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for gift cards matching a query across indexed fields like balance, email, and creation date. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetGiftCardsSearchRequest(
            query=_models.GetGiftCardsSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_gift_cards: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/gift_cards/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_gift_cards")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_gift_cards", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_gift_cards",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def get_gift_card(gift_card_id: str = Field(..., description="The unique identifier of the gift card to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single gift card by its unique identifier. Use this to fetch information such as balance, expiration date, and creation details for a specific gift card."""

    # Construct request model with validation
    try:
        _request = _models.GetGiftCardsParamGiftCardIdRequest(
            path=_models.GetGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_gift_card: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_gift_card")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_gift_card", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_gift_card",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def update_gift_card(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing gift card's expiry date, note, and template suffix. The gift card's balance cannot be modified through this API."""

    # Construct request model with validation
    try:
        _request = _models.UpdateGiftCardsParamGiftCardIdRequest(
            path=_models.UpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard, latest_api_version
@mcp.tool()
async def disable_gift_card(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.CreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.CreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem, latest_api_version
@mcp.tool()
async def list_inventory_items(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request. Accepts values between 1 and 250, with a default of 50 if not specified."),
    ids: int | None = Field(None, description="Filter results to only inventory items with the specified IDs. Provide one or more comma-separated integer IDs to retrieve specific items."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items from the store. Results are paginated using cursor-based links provided in the response header; use the link relations to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetInventoryItemsRequest(
            query=_models.GetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem, latest_api_version
@mcp.tool()
async def get_inventory_item(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to retrieve. This ID corresponds to a specific product variant's inventory record.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a single inventory item by its unique identifier. Use this to fetch inventory tracking data for a specific product variant."""

    # Construct request model with validation
    try:
        _request = _models.GetInventoryItemsParamInventoryItemIdRequest(
            path=_models.GetInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_inventory_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_inventory_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_inventory_item", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_inventory_item",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem, latest_api_version
@mcp.tool()
async def update_inventory_item(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to update. This ID is required to target the specific inventory item for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing inventory item in your Shopify store. Modify properties like SKU, tracked status, and cost for a specific inventory item."""

    # Construct request model with validation
    try:
        _request = _models.UpdateInventoryItemsParamInventoryItemIdRequest(
            path=_models.UpdateInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_inventory_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_inventory_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_inventory_item", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_inventory_item",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel, latest_api_version
@mcp.tool()
async def list_inventory_levels(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of inventory levels to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across your store's locations. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.GetInventoryLevelsRequest(
            query=_models.GetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel, latest_api_version
@mcp.tool()
async def delete_inventory_level(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted. Required to identify which item's inventory level to remove."),
    location_id: int | None = Field(None, description="The unique identifier of the location where the inventory level should be deleted. Required to specify which location's inventory connection should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This disconnects the item from that location; note that every inventory item must maintain at least one inventory level, so you must connect the item to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.DeleteInventoryLevelsRequest(
            query=_models.DeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location, latest_api_version
@mcp.tool()
async def list_locations() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. Use this to get a complete list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location, latest_api_version
@mcp.tool()
async def get_locations_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. Use this to determine how many physical or virtual locations exist without fetching full location details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location, latest_api_version
@mcp.tool()
async def get_location(location_id: str = Field(..., description="The unique identifier of the location to retrieve. This ID is assigned by Shopify when the location is created.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a specific inventory location by its ID. Use this to fetch location details such as name, address, and fulfillment capabilities."""

    # Construct request model with validation
    try:
        _request = _models.GetLocationsParamLocationIdRequest(
            path=_models.GetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location, latest_api_version
@mcp.tool()
async def list_inventory_levels_for_location(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory levels for a specific location. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.GetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.GetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield, latest_api_version
@mcp.tool()
async def list_metafields_for_product_image(
    metafield_owner_id: int | None = Field(None, alias="metafieldowner_id", description="The unique identifier of the Product Image resource that owns the metafields. Required when filtering metafields for a specific image."),
    metafield_owner_resource: str | None = Field(None, alias="metafieldowner_resource", description="The resource type that owns the metafields. For this operation, this should be set to 'product_image' to retrieve metafields belonging to a Product Image."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all metafields associated with a specific Product Image resource. Use the owner_id and owner_resource parameters to filter metafields for a particular image."""

    # Construct request model with validation
    try:
        _request = _models.GetMetafieldsRequest(
            query=_models.GetMetafieldsRequestQuery(metafield_owner_id=metafield_owner_id, metafield_owner_resource=metafield_owner_resource)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_metafields_for_product_image: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/metafields.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_metafields_for_product_image")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_metafields_for_product_image", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_metafields_for_product_image",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield, latest_api_version
@mcp.tool()
async def get_metafields_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields associated with a resource. Use this to determine how many custom metadata fields exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield, latest_api_version
@mcp.tool()
async def get_metafield(metafield_id: str = Field(..., description="The unique identifier of the metafield to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single metafield by its ID. Use this to fetch detailed information about a specific metafield attached to a Shopify resource."""

    # Construct request model with validation
    try:
        _request = _models.GetMetafieldsParamMetafieldIdRequest(
            path=_models.GetMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_metafield: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafield")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafield", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafield",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield, latest_api_version
@mcp.tool()
async def update_metafield(metafield_id: str = Field(..., description="The unique identifier of the metafield to update. This ID is returned when a metafield is created and is required to target the specific metafield for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing metafield by its ID. Use this to modify metafield properties such as namespace, key, value, and type."""

    # Construct request model with validation
    try:
        _request = _models.UpdateMetafieldsParamMetafieldIdRequest(
            path=_models.UpdateMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_metafield: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_metafield")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_metafield", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_metafield",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield, latest_api_version
@mcp.tool()
async def delete_metafield(metafield_id: str = Field(..., description="The unique identifier of the metafield to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a metafield by its ID. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteMetafieldsParamMetafieldIdRequest(
            path=_models.DeleteMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_metafield: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_metafield")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_metafield", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_metafield",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder, latest_api_version
@mcp.tool()
async def list_fulfillment_orders(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order, including their current status and fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def list_order_fulfillments(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillments."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, ranging from 1 to 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link-based navigation provided in response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.GetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_fulfillments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_fulfillments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_fulfillments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_fulfillments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def create_fulfillment_for_order(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment. Required to specify which order's line items should be fulfilled.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for an order, marking specified line items as shipped. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.CreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def get_fulfillments_count_for_order(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding how many fulfillment records exist for an order without fetching the full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillments_count_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillments_count_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillments_count_for_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillments_count_for_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def get_fulfillment(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order, including tracking information and line item fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def update_fulfillment(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def cancel_fulfillment(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to cancel."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Cancel an active fulfillment for a specific order. This operation prevents further tracking and updates for the fulfillment."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def complete_fulfillment(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped and the order fulfillment process is finished."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent, latest_api_version
@mcp.tool()
async def list_fulfillment_events(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all events associated with a specific fulfillment for an order, including status updates and tracking information."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_events: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_events")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_events", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_events",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent, latest_api_version
@mcp.tool()
async def create_fulfillment_event(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment. This order must exist and be accessible in the store."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order. This fulfillment must exist and belong to the given order."),
) -> dict[str, Any] | ToolResult:
    """Creates a fulfillment event for a specific fulfillment within an order. Fulfillment events track status changes and milestones in the fulfillment lifecycle, such as preparation, shipment, or delivery notifications."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_event", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_event",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent, latest_api_version
@mcp.tool()
async def get_fulfillment_event(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the order."),
    event_id: str = Field(..., description="The unique identifier of the specific fulfillment event to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific event associated with a fulfillment, such as notifications about shipment status changes or delivery updates."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsParamEventIdRequest(
            path=_models.GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsParamEventIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id, event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_event", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_event",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent, latest_api_version
@mcp.tool()
async def delete_fulfillment_event(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment event to delete."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the order that contains the event to delete."),
    event_id: str = Field(..., description="The unique identifier of the fulfillment event to delete."),
) -> dict[str, Any] | ToolResult:
    """Deletes a specific fulfillment event from an order's fulfillment. This removes the event record associated with the fulfillment tracking."""

    # Construct request model with validation
    try:
        _request = _models.DeleteOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsParamEventIdRequest(
            path=_models.DeleteOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsParamEventIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id, event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_fulfillment_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_fulfillment_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_fulfillment_event", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_fulfillment_event",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment, latest_api_version
@mcp.tool()
async def open_fulfillment(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be opened."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as open."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as open, allowing it to receive additional items or changes. This operation transitions a fulfillment from a closed state back to an open state."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequest(
            path=_models.CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/fulfillments/{fulfillment_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund, latest_api_version
@mcp.tool()
async def list_order_refunds(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="The maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays refund amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of refunds for a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdRefundsRequest(
            path=_models.GetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.GetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund, latest_api_version
@mcp.tool()
async def create_order_refund(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each specifying the line_item_id, quantity to refund, restock_type (no_restock, cancel, or return), and location_id (required for cancel or return restock types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Typically generated by the calculate endpoint."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transaction amounts. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdRefundsRequest(
            path=_models.CreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.CreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund, latest_api_version
@mcp.tool()
async def calculate_order_refund(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping costs, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="Array of line items to refund, each specifying the line item ID, quantity to refund, restock instructions (no_restock, cancel, or return), and optionally the location ID where items should be restocked. The endpoint will suggest a suitable location if not provided for return or cancel restocks."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required whenever a shipping amount is specified, particularly important for multi-currency orders."),
) -> dict[str, Any] | ToolResult:
    """Calculate refund transactions for an order based on line items and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund, including any necessary adjustments to restock instructions."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.CreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.CreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_order_refund: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_order_refund")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_order_refund", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_order_refund",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund, latest_api_version
@mcp.tool()
async def get_refund(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's native currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific refund for an order, including refund amounts, line items, and transaction information."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.GetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.GetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk, latest_api_version
@mcp.tool()
async def list_order_risks(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve associated risks.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. This endpoint uses cursor-based pagination via response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdRisksRequest(
            path=_models.GetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk, latest_api_version
@mcp.tool()
async def create_order_risk(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record. This must be a valid order ID from your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.CreateOrdersParamOrderIdRisksRequest(
            path=_models.CreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk, latest_api_version
@mcp.tool()
async def get_order_risk(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk assessment."),
    risk_id: str = Field(..., description="The unique identifier of the specific risk record to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific risk assessment associated with an order. Use this to view details about a flagged risk for fraud detection or order validation purposes."""

    # Construct request model with validation
    try:
        _request = _models.GetOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.GetOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_risk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_risk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_risk", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_risk",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk, latest_api_version
@mcp.tool()
async def update_order_risk(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk for a specific order. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.UpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.UpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk, latest_api_version
@mcp.tool()
async def delete_order_risk(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be deleted."),
    risk_id: str = Field(..., description="The unique identifier of the risk to be deleted from the order."),
) -> dict[str, Any] | ToolResult:
    """Deletes a specific risk associated with an order. Note that you cannot delete an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.DeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.DeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, policy, store-properties/policy, latest_api_version
@mcp.tool()
async def list_policies() -> dict[str, Any] | ToolResult:
    """Retrieves all policies configured for the shop, including return, privacy, shipping, and other store policies."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/policies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_policies")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_policies", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_policies",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def create_discount_codes_batch(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation job object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.CreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.CreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_codes_batch: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_codes_batch")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_codes_batch", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_codes_batch",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def get_discount_code_batch(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch job."),
    batch_id: str = Field(..., description="The unique identifier of the discount code creation batch job to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule."""

    # Construct request model with validation
    try:
        _request = _models.GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def list_discount_codes_for_batch(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the discount code creation batch job to retrieve codes from."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a specific discount code creation batch job. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def list_discount_codes_for_price_rule(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.GetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def create_discount_code_for_price_rule(price_rule_id: str = Field(..., description="The unique identifier of the price rule to which this discount code will be attached. This price rule must already exist.")) -> dict[str, Any] | ToolResult:
    """Creates a new discount code associated with a specific price rule. The discount code can be used by customers to apply the price rule's discounts during checkout."""

    # Construct request model with validation
    try:
        _request = _models.CreatePriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.CreatePriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_code_for_price_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_code_for_price_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_code_for_price_rule", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_code_for_price_rule",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def get_discount_code(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single discount code associated with a specific price rule. Use this to fetch detailed information about a discount code by its ID."""

    # Construct request model with validation
    try:
        _request = _models.GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def update_discount_code(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code being updated."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing discount code associated with a price rule. Modify discount code properties such as code value, usage limits, and other configurations."""

    # Construct request model with validation
    try:
        _request = _models.UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_discount_code: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_discount_code")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_discount_code", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_discount_code",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode, latest_api_version
@mcp.tool()
async def delete_discount_code(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Permanently removes a discount code from a price rule. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def list_products(
    ids: Any | None = Field(None, description="Filter results to only include products with specific IDs. Provide as a comma-separated list of product IDs."),
    limit: Any | None = Field(None, description="Maximum number of products to return per page. Defaults to 50 and cannot exceed 250."),
    title: Any | None = Field(None, description="Filter results to only include products matching the specified title."),
    vendor: Any | None = Field(None, description="Filter results to only include products from a specific vendor."),
    handle: Any | None = Field(None, description="Filter results to only include products with the specified handle (URL-friendly identifier)."),
    product_type: Any | None = Field(None, description="Filter results to only include products of a specific type."),
    status: Any | None = Field(None, description="Filter results by product status: active (default) shows only active products, archived shows only archived products, or draft shows only draft products."),
    collection_id: Any | None = Field(None, description="Filter results to only include products belonging to a specific collection by collection ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published shows only published products, unpublished shows only unpublished products, or any (default) shows all products regardless of publication status."),
    presentment_currencies: Any | None = Field(None, description="Return presentment prices in only the specified currencies. Provide as a comma-separated list of ISO 4217 currency codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of products from your store, with support for filtering by various attributes and sorting options. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsRequest(
            query=_models.GetProductsRequestQuery(ids=ids, limit=limit, title=title, vendor=vendor, handle=handle, product_type=product_type, status=status, collection_id=collection_id, published_status=published_status, presentment_currencies=presentment_currencies)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_products: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_products")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_products", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_products",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def create_product() -> dict[str, Any] | ToolResult:
    """Creates a new product in your Shopify store. You can optionally set SEO metadata using metafields_global_title_tag and metafields_global_description_tag to control how the product appears in search results."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/products.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def get_products_count(
    vendor: Any | None = Field(None, description="Filter the count to include only products from a specific vendor."),
    product_type: Any | None = Field(None, description="Filter the count to include only products of a specific product type."),
    collection_id: Any | None = Field(None, description="Filter the count to include only products belonging to a specific collection by its ID."),
    published_status: Any | None = Field(None, description="Filter the count by product publication status: use 'published' for only published products, 'unpublished' for only unpublished products, or 'any' to include all products regardless of status (default is 'any')."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of products in the store, with optional filtering by vendor, product type, collection, or published status."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsCountRequest(
            query=_models.GetProductsCountRequestQuery(vendor=vendor, product_type=product_type, collection_id=collection_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_products_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/products/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_products_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_products_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_products_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def get_product(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsParamProductIdRequest(
            path=_models.GetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def update_product(product_id: str = Field(..., description="The unique identifier of the product to update.")) -> dict[str, Any] | ToolResult:
    """Updates a product's details, variants, images, and SEO metadata. Use metafields_global_title_tag and metafields_global_description_tag to optimize the product's search engine visibility."""

    # Construct request model with validation
    try:
        _request = _models.UpdateProductsParamProductIdRequest(
            path=_models.UpdateProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product, latest_api_version
@mcp.tool()
async def delete_product(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.DeleteProductsParamProductIdRequest(
            path=_models.DeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def list_product_images(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of image objects containing URLs, alt text, and metadata for the product."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsParamProductIdImagesRequest(
            path=_models.GetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def create_product_image(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a specific product. The image will be added to the product's image gallery and can be used for product display across storefronts."""

    # Construct request model with validation
    try:
        _request = _models.CreateProductsParamProductIdImagesRequest(
            path=_models.CreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def get_product_images_count(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for understanding product image inventory without fetching the full image list."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsParamProductIdImagesCountRequest(
            path=_models.GetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def get_product_image(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image."),
    image_id: str = Field(..., description="The unique identifier of the specific image to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve a specific product image by its ID. Returns detailed metadata for the image including URL, alt text, and position within the product's image gallery."""

    # Construct request model with validation
    try:
        _request = _models.GetProductsParamProductIdImagesParamImageIdRequest(
            path=_models.GetProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_image: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_image")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_image", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_image",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def update_product_image(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be updated."),
    image_id: str = Field(..., description="The unique identifier of the image within the product to be updated."),
) -> dict[str, Any] | ToolResult:
    """Update the properties of an existing product image, such as alt text, position, or other image metadata associated with a specific product."""

    # Construct request model with validation
    try:
        _request = _models.UpdateProductsParamProductIdImagesParamImageIdRequest(
            path=_models.UpdateProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_image: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_image")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_image", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_image",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image, latest_api_version
@mcp.tool()
async def delete_product_image(
    product_id: str = Field(..., description="The unique identifier of the product containing the image to delete."),
    image_id: str = Field(..., description="The unique identifier of the image to delete from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image from a product. This removes the image association from the product permanently."""

    # Construct request model with validation
    try:
        _request = _models.DeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.DeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge, latest_api_version
@mcp.tool()
async def list_recurring_application_charges() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your Shopify app. Use this to view billing history and current subscription status."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge, latest_api_version
@mcp.tool()
async def create_recurring_application_charge() -> dict[str, Any] | ToolResult:
    """Creates a recurring application charge for your Shopify app, establishing a billing arrangement that will be charged to the merchant on a recurring basis."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_recurring_application_charge", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_recurring_application_charge",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge, latest_api_version
@mcp.tool()
async def get_recurring_application_charge(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single recurring application charge by its ID. Use this to fetch current status, pricing, and other metadata for a specific recurring charge."""

    # Construct request model with validation
    try:
        _request = _models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_recurring_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_recurring_application_charge", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_recurring_application_charge",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge, latest_api_version
@mcp.tool()
async def delete_recurring_application_charge(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to cancel. This ID is returned when the charge is initially created.")) -> dict[str, Any] | ToolResult:
    """Cancels an active recurring application charge for a Shopify app. This operation permanently removes the recurring billing arrangement between the app and the merchant."""

    # Construct request model with validation
    try:
        _request = _models.DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_recurring_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_recurring_application_charge", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_recurring_application_charge",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge, latest_api_version
@mcp.tool()
async def update_recurring_application_charge_capped_amount(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to customize."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount for the recurring charge. This sets the maximum total amount that will be billed to the merchant for this charge."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount of an active recurring application charge. This allows you to modify the maximum billing cap for an existing charge without canceling and recreating it."""

    # Construct request model with validation
    try:
        _request = _models.UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge, latest_api_version
@mcp.tool()
async def list_usage_charges_for_recurring_application_charge(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to retrieve usage charges.")) -> dict[str, Any] | ToolResult:
    """Retrieves all usage charges associated with a specific recurring application charge. Usage charges represent variable fees billed on top of a recurring application charge."""

    # Construct request model with validation
    try:
        _request = _models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_usage_charges_for_recurring_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_usage_charges_for_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_usage_charges_for_recurring_application_charge", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_usage_charges_for_recurring_application_charge",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge, latest_api_version
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to which this usage charge will be applied. This must be a valid, active recurring application charge ID.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill merchants for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge, latest_api_version
@mcp.tool()
async def get_usage_charge_for_recurring_application_charge(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge that contains the usage charge you want to retrieve."),
    usage_charge_id: str = Field(..., description="The unique identifier of the specific usage charge to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single usage charge associated with a recurring application charge. Use this to fetch details about a specific metered billing charge."""

    # Construct request model with validation
    try:
        _request = _models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequest(
            path=_models.GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id, usage_charge_id=usage_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_usage_charge_for_recurring_application_charge: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_usage_charge_for_recurring_application_charge")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_usage_charge_for_recurring_application_charge", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_usage_charge_for_recurring_application_charge",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def list_redirects(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250. Defaults to 50 if not specified."),
    path: Any | None = Field(None, description="Filter results to show only redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter results to show only redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of URL redirects configured in the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetRedirectsRequest(
            query=_models.GetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def create_redirect() -> dict[str, Any] | ToolResult:
    """Creates a URL redirect in the online store. Full URLs provided in the path property are automatically converted to absolute paths (domain removed). For example, a path of 'http://www.johns-apparel.com/springwear' will be saved as 'springwear'."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/redirects.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_redirect")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_redirect", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_redirect",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def get_redirects_count(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.GetRedirectsCountRequest(
            query=_models.GetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirects_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirects_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirects_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirects_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def get_redirect(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store. Use this to fetch details about a specific URL redirect configuration."""

    # Construct request model with validation
    try:
        _request = _models.GetRedirectsParamRedirectIdRequest(
            path=_models.GetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def update_redirect(redirect_id: str = Field(..., description="The unique identifier of the redirect to update. This ID is returned when the redirect is created and is required to target the specific redirect for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect in the online store. Modify redirect rules such as the source path and target destination URL."""

    # Construct request model with validation
    try:
        _request = _models.UpdateRedirectsParamRedirectIdRequest(
            path=_models.UpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect, latest_api_version
@mcp.tool()
async def delete_redirect(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. Once deleted, the redirect cannot be recovered."""

    # Construct request model with validation
    try:
        _request = _models.DeleteRedirectsParamRedirectIdRequest(
            path=_models.DeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report, latest_api_version
@mcp.tool()
async def list_reports(
    ids: Any | None = Field(None, description="Filter results to specific reports by providing a comma-separated list of report IDs."),
    limit: Any | None = Field(None, description="Maximum number of reports to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of reports from the Shopify Admin API. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.GetReportsRequest(
            query=_models.GetReportsRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_reports: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_reports")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_reports", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_reports",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report, latest_api_version
@mcp.tool()
async def create_report(
    name: Any | None = Field(None, description="The display name for the report. Must not exceed 255 characters."),
    shopify_ql: Any | None = Field(None, description="The ShopifyQL query string that defines what data the report will retrieve and analyze."),
) -> dict[str, Any] | ToolResult:
    """Creates a new custom report in the Shopify admin. The report can be configured with a name and ShopifyQL query to analyze store data."""

    # Construct request model with validation
    try:
        _request = _models.CreateReportsRequest(
            query=_models.CreateReportsRequestQuery(name=name, shopify_ql=shopify_ql)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_report: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_report")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_report", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_report",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report, latest_api_version
@mcp.tool()
async def get_report(report_id: str = Field(..., description="The unique identifier of the report to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single report that was created by your app. Use this to fetch details about a specific report by its ID."""

    # Construct request model with validation
    try:
        _request = _models.GetReportsParamReportIdRequest(
            path=_models.GetReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_report: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_report")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_report", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_report",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report, latest_api_version
@mcp.tool()
async def update_report(report_id: str = Field(..., description="The unique identifier of the report to update. This ID is required to target the specific report for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing report configuration in Shopify Admin. Modify report settings and parameters by providing the report ID."""

    # Construct request model with validation
    try:
        _request = _models.UpdateReportsParamReportIdRequest(
            path=_models.UpdateReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_report: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_report")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_report", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_report",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report, latest_api_version
@mcp.tool()
async def delete_report(report_id: str = Field(..., description="The unique identifier of the report to delete. This is a required string value that identifies which report should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific report from the Shopify admin. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteReportsParamReportIdRequest(
            path=_models.DeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def list_script_tags(limit: Any | None = Field(None, description="Maximum number of script tags to return per request. Accepts values between 1 and 250, with a default of 50 results.")) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of all script tags in the store. Results are paginated using cursor-based links provided in the response header; use the link relations to navigate pages rather than the limit parameter alone."""

    # Construct request model with validation
    try:
        _request = _models.GetScriptTagsRequest(
            query=_models.GetScriptTagsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_script_tags: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/script_tags.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_script_tags")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_script_tags", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_script_tags",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def create_script_tag() -> dict[str, Any] | ToolResult:
    """Creates a new script tag in the Shopify store to inject custom JavaScript or other scripts into the storefront. Script tags are used to add third-party functionality, tracking, or custom behavior to your online store."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/script_tags.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_script_tag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_script_tag", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_script_tag",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def get_script_tags_count() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the Shopify store. Use this to quickly determine how many script tags exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def get_script_tag(script_tag_id: str = Field(..., description="The unique identifier of the script tag to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single script tag by its ID from the Shopify online store. Use this to fetch details about a specific script tag resource."""

    # Construct request model with validation
    try:
        _request = _models.GetScriptTagsParamScriptTagIdRequest(
            path=_models.GetScriptTagsParamScriptTagIdRequestPath(script_tag_id=script_tag_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_script_tag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/script_tags/{script_tag_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/script_tags/{script_tag_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tag", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tag",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def update_script_tag(script_tag_id: str = Field(..., description="The unique identifier of the script tag to update. This ID is returned when the script tag is created and is required to target the specific tag for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing script tag in the Shopify online store. Modify script tag properties such as source URL, event triggers, or display scope."""

    # Construct request model with validation
    try:
        _request = _models.UpdateScriptTagsParamScriptTagIdRequest(
            path=_models.UpdateScriptTagsParamScriptTagIdRequestPath(script_tag_id=script_tag_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_script_tag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/script_tags/{script_tag_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/script_tags/{script_tag_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_script_tag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_script_tag", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_script_tag",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag, latest_api_version
@mcp.tool()
async def delete_script_tag(script_tag_id: str = Field(..., description="The unique identifier of the script tag to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a script tag from the store. This removes the associated script injection from all store pages."""

    # Construct request model with validation
    try:
        _request = _models.DeleteScriptTagsParamScriptTagIdRequest(
            path=_models.DeleteScriptTagsParamScriptTagIdRequestPath(script_tag_id=script_tag_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_script_tag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/script_tags/{script_tag_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/script_tags/{script_tag_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_script_tag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_script_tag", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_script_tag",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone, latest_api_version
@mcp.tool()
async def list_shipping_zones() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop, latest_api_version
@mcp.tool()
async def get_shop() -> dict[str, Any] | ToolResult:
    """Retrieves the complete configuration and metadata for the shop, including store name, contact information, plan details, and other shop-level settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance, latest_api_version
@mcp.tool()
async def get_shopify_payments_balance() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, including available funds and pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout, latest_api_version
@mcp.tool()
async def list_shopify_payments_payouts(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after the specified date (inclusive). Use ISO 8601 format."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before the specified date (inclusive). Use ISO 8601 format."),
    status: Any | None = Field(None, description="Filter payouts by their current status (e.g., pending, paid, failed, cancelled)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of all Shopify Payments payouts ordered by payout date, with the most recent first. Uses cursor-based pagination via response headers."""

    # Construct request model with validation
    try:
        _request = _models.GetShopifyPaymentsPayoutsRequest(
            query=_models.GetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_payouts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_payouts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_payouts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_payouts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout, latest_api_version
@mcp.tool()
async def get_payout(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information about a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.GetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.GetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_payout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_payout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_payout", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_payout",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def list_smart_collections(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Defaults to 50 and cannot exceed 250."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to only smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by the smart collection's URL-friendly handle."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished only, or 'any' for all collections. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from your store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.GetSmartCollectionsRequest(
            query=_models.GetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def create_smart_collection() -> dict[str, Any] | ToolResult:
    """Creates a new smart collection in Shopify that automatically groups products based on specified rules. Smart collections dynamically update their contents as products are added, modified, or removed to match the defined criteria."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/smart_collections.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_smart_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_smart_collection", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_smart_collection",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def count_smart_collections(
    title: Any | None = Field(None, description="Filter to smart collections with an exact matching title."),
    product_id: Any | None = Field(None, description="Filter to smart collections that contain the specified product ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for live collections, 'unpublished' for draft collections, or 'any' to include all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.GetSmartCollectionsCountRequest(
            query=_models.GetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def get_smart_collection(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single smart collection by its ID, including its configuration, products, and metadata."""

    # Construct request model with validation
    try:
        _request = _models.GetSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.GetSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_smart_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_smart_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_smart_collection", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_smart_collection",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def update_smart_collection(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update. This ID is returned when the collection is created and is required to target the correct collection for modification.")) -> dict[str, Any] | ToolResult:
    """Updates the configuration and rules of an existing smart collection, allowing you to modify its title, conditions, sorting, and other properties."""

    # Construct request model with validation
    try:
        _request = _models.UpdateSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.UpdateSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def delete_smart_collection(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.DeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection, latest_api_version
@mcp.tool()
async def update_smart_collection_order(
    smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update."),
    products: Any | None = Field(None, description="An ordered array of product IDs that should appear at the top of the collection. Passing an empty array clears any previously applied custom sorting."),
    sort_order: Any | None = Field(None, description="The sorting method to apply to the collection (e.g., alphabetical, price, newest). If not specified, the current sorting method is retained."),
) -> dict[str, Any] | ToolResult:
    """Reorder products within a smart collection by specifying a custom product sequence and/or changing the collection's sorting method."""

    # Construct request model with validation
    try:
        _request = _models.UpdateSmartCollectionsParamSmartCollectionIdOrderRequest(
            path=_models.UpdateSmartCollectionsParamSmartCollectionIdOrderRequestPath(smart_collection_id=smart_collection_id),
            query=_models.UpdateSmartCollectionsParamSmartCollectionIdOrderRequestQuery(products=products, sort_order=sort_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/smart_collections/{smart_collection_id}/order.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/smart_collections/{smart_collection_id}/order.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_order", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_order",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: access, storefrontaccesstoken, access/storefrontaccesstoken, latest_api_version
@mcp.tool()
async def list_storefront_access_tokens() -> dict[str, Any] | ToolResult:
    """Retrieves all storefront access tokens that have been issued for your Shopify store. These tokens are used to authenticate requests to the Storefront API from client-side applications."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/storefront_access_tokens.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_storefront_access_tokens")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_storefront_access_tokens", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_storefront_access_tokens",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: access, storefrontaccesstoken, access/storefrontaccesstoken, latest_api_version
@mcp.tool()
async def create_storefront_access_token() -> dict[str, Any] | ToolResult:
    """Creates a new storefront access token for accessing the Storefront API. This token enables secure, scoped access to storefront resources without requiring full admin credentials."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/storefront_access_tokens.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_storefront_access_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_storefront_access_token", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_storefront_access_token",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: access, storefrontaccesstoken, access/storefrontaccesstoken, latest_api_version
@mcp.tool()
async def delete_storefront_access_token(storefront_access_token_id: str = Field(..., description="The unique identifier of the storefront access token to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a storefront access token by its ID. This revokes the token's access to the Storefront API."""

    # Construct request model with validation
    try:
        _request = _models.DeleteStorefrontAccessTokensParamStorefrontAccessTokenIdRequest(
            path=_models.DeleteStorefrontAccessTokensParamStorefrontAccessTokenIdRequestPath(storefront_access_token_id=storefront_access_token_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_storefront_access_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/storefront_access_tokens/{storefront_access_token_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/storefront_access_tokens/{storefront_access_token_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_storefront_access_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_storefront_access_token", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_storefront_access_token",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tendertransaction, latest_api_version
@mcp.tool()
async def list_tender_transactions(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 if not specified."),
    processed_at_min: Any | None = Field(None, description="Filter to show only tender transactions processed on or after this date (ISO 8601 format)."),
    processed_at_max: Any | None = Field(None, description="Filter to show only tender transactions processed on or before this date (ISO 8601 format)."),
    order: Any | None = Field(None, description="Sort results by processed_at timestamp in either ascending or descending order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of tender transactions processed through the store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.GetTenderTransactionsRequest(
            query=_models.GetTenderTransactionsRequestQuery(limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tender_transactions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/tender_transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tender_transactions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tender_transactions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tender_transactions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme, latest_api_version
@mcp.tool()
async def list_themes() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store, including both published and unpublished themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme, latest_api_version
@mcp.tool()
async def get_theme(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single theme by its ID, including theme metadata and configuration."""

    # Construct request model with validation
    try:
        _request = _models.GetThemesParamThemeIdRequest(
            path=_models.GetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme, latest_api_version
@mcp.tool()
async def delete_theme(theme_id: str = Field(..., description="The unique identifier of the theme to delete, returned as a string by the Shopify API.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the Shopify store. The theme must not be the currently active theme."""

    # Construct request model with validation
    try:
        _request = _models.DeleteThemesParamThemeIdRequest(
            path=_models.DeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset, latest_api_version
@mcp.tool()
async def get_theme_asset(
    theme_id: str = Field(..., description="The unique identifier of the theme from which to retrieve the asset."),
    asset_key: str | None = Field(None, alias="assetkey", description="The file path key of the asset to retrieve within the theme (e.g., templates/index.liquid, assets/style.css). Required to fetch a specific asset."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single asset file from a theme by its key. The asset key specifies the file path within the theme (e.g., templates/index.liquid)."""

    # Construct request model with validation
    try:
        _request = _models.GetThemesParamThemeIdAssetsRequest(
            path=_models.GetThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.GetThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_asset")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_asset", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_asset",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset, latest_api_version
@mcp.tool()
async def delete_theme_asset(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The asset key (file path) identifying which asset to delete from the theme, such as 'templates/product.json' or 'assets/style.css'."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key path within the theme."""

    # Construct request model with validation
    try:
        _request = _models.DeleteThemesParamThemeIdAssetsRequest(
            path=_models.DeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.DeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, user, plus/user, latest_api_version
@mcp.tool()
async def get_user(user_id: str = Field(..., description="The unique identifier of the user to retrieve. This is a required string value that identifies which user's information to fetch.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a specific user by their unique identifier. Use this to fetch user profile data, permissions, and account details."""

    # Construct request model with validation
    try:
        _request = _models.GetUsersParamUserIdRequest(
            path=_models.GetUsersParamUserIdRequestPath(user_id=user_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/users/{user_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/users/{user_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_user", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_user",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook, latest_api_version
@mcp.tool()
async def list_webhooks(
    address: Any | None = Field(None, description="Filter webhooks by the URI endpoint that receives POST requests. Only webhooks sending to this address will be returned."),
    limit: Any | None = Field(None, description="Maximum number of webhooks to return per request, between 1 and 250. Defaults to 50 if not specified."),
    topic: Any | None = Field(None, description="Filter webhooks by topic (event type). Only webhooks subscribed to the specified topic will be returned. Refer to the Shopify webhook topics documentation for valid values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of webhooks configured for your Shopify store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.GetWebhooksRequest(
            query=_models.GetWebhooksRequestQuery(address=address, limit=limit, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_webhooks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/webhooks.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_webhooks")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_webhooks", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhooks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook, latest_api_version
@mcp.tool()
async def get_webhooks_count(
    address: Any | None = Field(None, description="Filter the count to only include webhook subscriptions that send POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter the count to only include webhook subscriptions for a specific event topic. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the Shopify store, with optional filtering by destination address or event topic."""

    # Construct request model with validation
    try:
        _request = _models.GetWebhooksCountRequest(
            query=_models.GetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_webhooks_count: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2020-10/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_webhooks_count")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_webhooks_count", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_webhooks_count",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook, latest_api_version
@mcp.tool()
async def get_webhook(webhook_id: str = Field(..., description="The unique identifier of the webhook subscription to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific webhook subscription by its ID, including its configuration, topics, and delivery settings."""

    # Construct request model with validation
    try:
        _request = _models.GetWebhooksParamWebhookIdRequest(
            path=_models.GetWebhooksParamWebhookIdRequestPath(webhook_id=webhook_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_webhook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2020-10/webhooks/{webhook_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2020-10/webhooks/{webhook_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_webhook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_webhook", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_webhook",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def get_application_charge_202101(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific application charge by its ID. Use this to fetch information about a charge created for your app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.Deprecated202101GetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def list_application_credits_202101() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits for the store. This endpoint returns a collection of credits that have been issued to the application."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_blog_articles(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="The maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: published (only published articles), unpublished (only unpublished articles), or any (all articles regardless of status). Defaults to any."),
    handle: Any | None = Field(None, description="Retrieve a single article by its unique handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those written by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202101GetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.Deprecated202101GetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_blog_articles: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_blog_articles")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_blog_articles", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_blog_articles",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def create_article_for_blog_202101(blog_id: str = Field(..., description="The unique identifier of the blog where the article will be created.")) -> dict[str, Any] | ToolResult:
    """Creates a new article for a specified blog. The article will be added to the blog's collection of articles."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateBlogsParamBlogIdArticlesRequest(
            path=_models.Deprecated202101CreateBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_article_for_blog_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_article_for_blog_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_article_for_blog_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_article_for_blog_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_count_for_blog_2021_01(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to count articles."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specific blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetBlogsParamBlogIdArticlesCountRequest(
            path=_models.Deprecated202101GetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.Deprecated202101GetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_count_for_blog_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_count_for_blog_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_count_for_blog_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_count_for_blog_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_202101(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article."),
    article_id: str = Field(..., description="The unique identifier of the article to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single article from a blog by its ID. Use this to fetch detailed information about a specific article in the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202101GetBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def update_article_202101(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to update."),
    article_id: str = Field(..., description="The unique identifier of the article to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing article within a blog. Modify article content, metadata, and other properties by providing the blog and article identifiers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202101UpdateBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_article_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_article_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_article_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_article_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def delete_article_202101(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to delete."),
    article_id: str = Field(..., description="The unique identifier of the article to delete."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes an article from a blog. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.Deprecated202101DeleteBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_article_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_article_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_article_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_article_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def update_carrier_service_202101(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service. Only the app that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202101UpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def delete_carrier_service_2021_01(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a carrier service from the store. This action cannot be undone and will remove the carrier service configuration."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCarrierServicesParamCarrierServiceIdRequest(
            path=_models.Deprecated202101DeleteCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_carrier_service_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_carrier_service_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_carrier_service_2021_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_carrier_service_2021_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts
@mcp.tool()
async def get_abandoned_checkouts_count_202101(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCheckoutsCountRequest(
            query=_models.Deprecated202101GetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, checkout, sales-channels/checkout
@mcp.tool()
async def complete_checkout_2021_01(token: str = Field(..., description="The unique identifier token for the checkout to complete. This token is provided when the checkout is created and is required to identify which checkout should be finalized.")) -> dict[str, Any] | ToolResult:
    """Completes a checkout by finalizing the transaction and converting it to an order. This operation marks the checkout as complete and processes the payment."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateCheckoutsParamTokenCompleteRequest(
            path=_models.Deprecated202101CreateCheckoutsParamTokenCompleteRequestPath(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_checkout_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/checkouts/{token}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/checkouts/{token}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_checkout_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_checkout_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_checkout_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, checkout, sales-channels/checkout
@mcp.tool()
async def list_shipping_rates_for_checkout_2021_01(token: str = Field(..., description="The unique identifier for the checkout. This token is used to retrieve shipping rates specific to that checkout.")) -> dict[str, Any] | ToolResult:
    """Retrieves available shipping rates for a checkout. Poll this endpoint until rates become available, then use the returned rates to display updated pricing (subtotal, tax, total) or apply a rate by updating the checkout's shipping line."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCheckoutsParamTokenShippingRatesRequest(
            path=_models.Deprecated202101GetCheckoutsParamTokenShippingRatesRequestPath(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shipping_rates_for_checkout_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/checkouts/{token}/shipping_rates.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/checkouts/{token}/shipping_rates.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_rates_for_checkout_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_rates_for_checkout_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_rates_for_checkout_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def delete_collection_listing_202101(collection_listing_id: str = Field(..., description="The unique identifier of the collection listing to delete.")) -> dict[str, Any] | ToolResult:
    """Remove a collection listing to unpublish a collection from your sales channel or app."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCollectionListingsParamCollectionListingIdRequest(
            path=_models.Deprecated202101DeleteCollectionListingsParamCollectionListingIdRequestPath(collection_listing_id=collection_listing_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_collection_listing_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collection_listings/{collection_listing_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collection_listings/{collection_listing_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_collection_listing_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_collection_listing_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_collection_listing_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sales-channels, collectionlisting, sales-channels/collectionlisting
@mcp.tool()
async def list_product_ids_for_collection_listing_v2021(
    collection_listing_id: str = Field(..., description="The unique identifier for the collection listing whose published products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of product IDs to return per request, ranging from 1 to 1000 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve the product IDs that are published to a specific collection listing. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectionListingsParamCollectionListingIdProductIdsRequest(
            path=_models.Deprecated202101GetCollectionListingsParamCollectionListingIdProductIdsRequestPath(collection_listing_id=collection_listing_id),
            query=_models.Deprecated202101GetCollectionListingsParamCollectionListingIdProductIdsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_ids_for_collection_listing_v2021: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collection_listings/{collection_listing_id}/product_ids.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collection_listings/{collection_listing_id}/product_ids.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_ids_for_collection_listing_v2021")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_ids_for_collection_listing_v2021", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_ids_for_collection_listing_v2021",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def get_collection_202101(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID, including metadata such as name, description, and product associations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectionsParamCollectionIdRequest(
            path=_models.Deprecated202101GetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def list_collection_products_202101(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, ranging from 1 to 250 products (defaults to 50 if not specified)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted by the collection's configured sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectionsParamCollectionIdProductsRequest(
            path=_models.Deprecated202101GetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.Deprecated202101GetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def list_collects_2021_01(limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250 items. Defaults to 50 if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of collects (product-to-collection associations) from the store. Results are paginated using link-based navigation provided in response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectsRequest(
            query=_models.Deprecated202101GetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collects_count_202101(collection_id: int | None = Field(None, description="Filter the count to only collects belonging to a specific collection. Omit to get the total count across all collections.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects, optionally filtered by a specific collection. Useful for understanding collection membership size without fetching individual collect records."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectsCountRequest(
            query=_models.Deprecated202101GetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collect_202101(collect_id: str = Field(..., description="The unique identifier of the collect record to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection assignment by its ID. Use this to fetch details about how a product is organized within a collection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCollectsParamCollectIdRequest(
            path=_models.Deprecated202101GetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def remove_product_from_collection_2021_01(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the specific product-collection association being deleted.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCollectsParamCollectIdRequest(
            path=_models.Deprecated202101DeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection_2021_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection_2021_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def list_countries_202101() -> dict[str, Any] | ToolResult:
    """Retrieves a complete list of all countries available in the Shopify store, including their codes and regional information."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/countries.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_countries_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_countries_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_countries_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def get_countries_count_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of countries available in the Shopify store. This is useful for understanding the scope of country data without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/countries/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_countries_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_countries_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_countries_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, country, store-properties/country
@mcp.tool()
async def delete_country_202101(country_id: str = Field(..., description="The unique identifier of the country to delete. This is a required string value that specifies which country record to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a country from the store. This operation removes the country record and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCountriesParamCountryIdRequest(
            path=_models.Deprecated202101DeleteCountriesParamCountryIdRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_country_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/countries/{country_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/countries/{country_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_country_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_country_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_country_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def list_provinces_for_country_202101(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve provinces. This ID must correspond to a valid country in the Shopify system.")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of provinces or states for a specified country. Use this to populate province/state selectors or validate province data for a given country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCountriesParamCountryIdProvincesRequest(
            path=_models.Deprecated202101GetCountriesParamCountryIdProvincesRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_provinces_for_country_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/countries/{country_id}/provinces.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/countries/{country_id}/provinces.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_provinces_for_country_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_provinces_for_country_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_provinces_for_country_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, province, store-properties/province
@mcp.tool()
async def get_province_count_for_country_202101(country_id: str = Field(..., description="The unique identifier of the country for which to retrieve the province count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of provinces or states for a specified country. Useful for understanding administrative divisions within a country."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCountriesParamCountryIdProvincesCountRequest(
            path=_models.Deprecated202101GetCountriesParamCountryIdProvincesCountRequestPath(country_id=country_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_province_count_for_country_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/countries/{country_id}/provinces/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/countries/{country_id}/provinces/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_province_count_for_country_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_province_count_for_country_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_province_count_for_country_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency
@mcp.tool()
async def list_currencies_202101() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies that are enabled and available on the shop for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def count_custom_collections_2021_01(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain this specific product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' for all collections regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomCollectionsCountRequest(
            query=_models.Deprecated202101GetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def update_custom_collection_202101(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing custom collection in your Shopify store. Modify collection details such as title, description, image, and other properties."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.Deprecated202101UpdateCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_custom_collection_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_custom_collection_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_custom_collection_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_custom_collection_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def delete_custom_collection_legacy_202101(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a custom collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.Deprecated202101DeleteCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_custom_collection_legacy_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_custom_collection_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_custom_collection_legacy_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_custom_collection_legacy_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customersavedsearch, customers/customersavedsearch
@mcp.tool()
async def get_customer_saved_search_202101(customer_saved_search_id: str = Field(..., description="The unique identifier of the customer saved search to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single customer saved search by its ID. Use this to fetch details about a specific saved search that a customer has created."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomerSavedSearchesParamCustomerSavedSearchIdRequest(
            path=_models.Deprecated202101GetCustomerSavedSearchesParamCustomerSavedSearchIdRequestPath(customer_saved_search_id=customer_saved_search_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_saved_search_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_saved_search_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_saved_search_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_saved_search_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customersavedsearch, customers/customersavedsearch
@mcp.tool()
async def delete_customer_saved_search_202101(customer_saved_search_id: str = Field(..., description="The unique identifier of the customer saved search to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer saved search by its ID. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCustomerSavedSearchesParamCustomerSavedSearchIdRequest(
            path=_models.Deprecated202101DeleteCustomerSavedSearchesParamCustomerSavedSearchIdRequestPath(customer_saved_search_id=customer_saved_search_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_saved_search_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_saved_search_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_saved_search_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_saved_search_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customersavedsearch, customers/customersavedsearch
@mcp.tool()
async def list_customers_for_saved_search_2021_01(
    customer_saved_search_id: str = Field(..., description="The unique identifier of the customer saved search to retrieve customers for."),
    order: Any | None = Field(None, description="Specifies the field and direction to sort results by (e.g., 'last_order_date DESC'). Defaults to sorting by last order date in descending order."),
    limit: Any | None = Field(None, description="The maximum number of customer records to return per request. Defaults to 50 and cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all customers that match the criteria defined in a customer saved search. Results can be ordered and paginated."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequest(
            path=_models.Deprecated202101GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestPath(customer_saved_search_id=customer_saved_search_id),
            query=_models.Deprecated202101GetCustomerSavedSearchesParamCustomerSavedSearchIdCustomersRequestQuery(order=order, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_for_saved_search_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}/customers.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customer_saved_searches/{customer_saved_search_id}/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_for_saved_search_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_for_saved_search_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_for_saved_search_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customers_202101(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer ID values."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersRequest(
            query=_models.Deprecated202101GetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customers_count_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the Shopify store. Useful for understanding customer base size without fetching individual customer records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def search_customers_202101(
    order: Any | None = Field(None, description="Specify the field and direction to sort results. Use format: field_name ASC or DESC (default: last_order_date DESC)."),
    query: Any | None = Field(None, description="Text to search across customer data fields such as name, email, phone, and address information."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250 (default: 50)."),
) -> dict[str, Any] | ToolResult:
    """Search for customers in the shop by query text. Returns matching customer records with pagination support via response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersSearchRequest(
            query=_models.Deprecated202101GetCustomersSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_customers_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/customers/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_customers_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_customers_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_customers_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customer_202101(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single customer by their unique identifier from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersParamCustomerIdRequest(
            path=_models.Deprecated202101GetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def update_customer_202101(customer_id: str = Field(..., description="The unique identifier of the customer to update. This ID is required to specify which customer record should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in Shopify. Modify customer details such as email, name, phone, and other profile attributes."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCustomersParamCustomerIdRequest(
            path=_models.Deprecated202101UpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def delete_customer_v202101(customer_id: str = Field(..., description="The unique identifier of the customer to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. The deletion will fail if the customer has any existing orders."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCustomersParamCustomerIdRequest(
            path=_models.Deprecated202101DeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_v202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_v202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def generate_customer_account_activation_url_2021_01(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the account activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.Deprecated202101CreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def list_customer_addresses_202101(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202101GetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def create_customer_address_v202101(customer_id: str = Field(..., description="The unique identifier of the customer for whom the address is being created.")) -> dict[str, Any] | ToolResult:
    """Creates a new address for a customer. The address is added to the customer's address list and can be set as their default billing or shipping address."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateCustomersParamCustomerIdAddressesRequest(
            path=_models.Deprecated202101CreateCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_customer_address_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_customer_address_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_customer_address_v202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_customer_address_v202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def bulk_update_customer_addresses_202101(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to target for the bulk operation. The order may be significant depending on the operation type."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., delete, set as default). Refer to API documentation for valid operation values."),
) -> dict[str, Any] | ToolResult:
    """Performs bulk operations on multiple customer addresses, allowing you to update, delete, or modify address records in a single request."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.Deprecated202101UpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.Deprecated202101UpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def get_customer_address_202101(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific customer address by customer ID and address ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202101GetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def update_customer_address_2021_01(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the specific address to update for the customer."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing customer address by customer ID and address ID. Use this to modify address details such as street, city, postal code, or other address information for a specific customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202101UpdateCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_address_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_address_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_address_2021_01", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_address_2021_01",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def delete_customer_address_202101(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific address from a customer's address list. This operation permanently deletes the address record associated with the given customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.Deprecated202101DeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def set_default_customer_address(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Sets a specific address as the default address for a customer. This address will be used as the primary address for the customer's account."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.Deprecated202101UpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_default_customer_address: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_default_customer_address")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_default_customer_address", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_default_customer_address",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customer_orders_202101(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports filtering and sorting through standard Order resource query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetCustomersParamCustomerIdOrdersRequest(
            path=_models.Deprecated202101GetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def send_customer_invite_202101(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation email to a customer, allowing them to create or access their account."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.Deprecated202101CreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def list_events_202101(
    limit: Any | None = Field(None, description="Maximum number of events to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
    filter_: Any | None = Field(None, alias="filter", description="Filters events to show only those matching the specified criteria. Use this to narrow results to specific event types or conditions."),
    verb: Any | None = Field(None, description="Filters events to show only those of a specific action type or verb (e.g., create, update, delete)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from the store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetEventsRequest(
            query=_models.Deprecated202101GetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_events_count_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. This endpoint provides a quick way to determine the number of events without fetching the full event list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_event_2021_01(event_id: str = Field(..., description="The unique identifier of the event to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single event by its ID from the Shopify admin. Use this to fetch detailed information about a specific event that occurred in your store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetEventsParamEventIdRequest(
            path=_models.Deprecated202101GetEventsParamEventIdRequestPath(event_id=event_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/events/{event_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/events/{event_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def get_fulfillment_order_202101(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID. Use this to fetch current status, line items, and fulfillment information for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.Deprecated202101GetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def cancel_fulfillment_order_2021_01(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order that should be marked as cancelled.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation is useful when a fulfillment order must be abandoned before it's been fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def send_fulfillment_order_cancellation_request_2021_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order in the system."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request. This provides context to the fulfillment service about why the order is being cancelled."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider to stop processing or cancel an already-placed fulfillment order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def accept_fulfillment_order_cancellation_request_2021_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to accept the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, confirming that the fulfillment service should cancel the specified order. Optionally include a reason message for the acceptance."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def reject_fulfillment_order_cancellation_request_2021_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason will be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this when you need to decline a cancellation and optionally provide a reason to the fulfillment service."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def close_fulfillment_order_2021_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or note explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def send_fulfillment_request_202101(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request for communication with the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional array of fulfillment order line items to request for fulfillment. If omitted, all line items in the fulfillment order will be requested."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally targeting specific line items or including a message."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def accept_fulfillment_request_2021_01(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message providing context or reasoning for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, confirming that the fulfillment service will proceed with fulfilling the order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def reject_fulfillment_request_202101(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove
@mcp.tool()
async def list_locations_for_fulfillment_order_move_2021_01(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves all locations where a fulfillment order can be moved to, sorted alphabetically by location name. Use this to determine valid destination locations before initiating a fulfillment order transfer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.Deprecated202101GetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def open_fulfillment_order(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to open for processing.")) -> dict[str, Any] | ToolResult:
    """Marks a scheduled fulfillment order as ready for fulfillment, allowing merchants to begin work on it before its originally scheduled fulfill_at datetime."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdOpenRequest(
            path=_models.Deprecated202101CreateFulfillmentOrdersParamFulfillmentOrderIdOpenRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillment_orders/{fulfillment_order_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentservice, shipping-and-fulfillment/fulfillmentservice
@mcp.tool()
async def list_fulfillment_services_legacy_202101() -> dict[str, Any] | ToolResult:
    """Retrieve all fulfillment services available for the store. This endpoint returns a list of fulfillment service configurations that can be used to manage inventory and order fulfillment."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/fulfillment_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_services_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_services_legacy_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_services_legacy_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_202101(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This ID references a specific fulfillment order that has been previously created.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment order. This operation marks the specified fulfillment as cancelled and updates its status accordingly."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.Deprecated202101CreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_tracking_2021_01(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates the tracking information for a fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.Deprecated202101CreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def list_gift_cards_2021_01(
    status: Any | None = Field(None, description="Filter results to gift cards with a specific status: enabled to show only active gift cards, or disabled to show only inactive gift cards."),
    limit: Any | None = Field(None, description="Maximum number of gift cards to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of gift cards, optionally filtered by status. Results are paginated using link headers in the response rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetGiftCardsRequest(
            query=_models.Deprecated202101GetGiftCardsRequestQuery(status=status, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_gift_cards_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/gift_cards.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_gift_cards_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_gift_cards_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_gift_cards_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def count_gift_cards_v202101(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetGiftCardsCountRequest(
            query=_models.Deprecated202101GetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_gift_cards_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_gift_cards_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_gift_cards_v202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_gift_cards_v202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def search_gift_cards_202101(
    order: Any | None = Field(None, description="The field and sort direction for ordering results. Defaults to most recently disabled gift cards first."),
    query: Any | None = Field(None, description="The search query text to match against indexed gift card fields including created_at, updated_at, disabled_at, balance, initial_value, amount_spent, email, and last_characters."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for gift cards using indexed fields like balance, email, or creation date. Results are paginated and returned via link headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetGiftCardsSearchRequest(
            query=_models.Deprecated202101GetGiftCardsSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_gift_cards_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/gift_cards/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_gift_cards_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_gift_cards_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_gift_cards_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def update_gift_card_202101(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing gift card's expiry date, note, and template suffix. The gift card's balance cannot be modified through this API."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateGiftCardsParamGiftCardIdRequest(
            path=_models.Deprecated202101UpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def disable_gift_card_202101(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.Deprecated202101CreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def list_inventory_items_202101(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request, between 1 and 250 items. Defaults to 50 if not specified."),
    ids: int | None = Field(None, description="Filter results to only inventory items with the specified IDs. Provide one or more comma-separated integer IDs."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetInventoryItemsRequest(
            query=_models.Deprecated202101GetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def update_inventory_item_legacy_202101(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing inventory item in your Shopify store. Modify properties like SKU, tracked status, and cost for a specific inventory item."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateInventoryItemsParamInventoryItemIdRequest(
            path=_models.Deprecated202101UpdateInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_inventory_item_legacy_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_inventory_item_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_inventory_item_legacy_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_inventory_item_legacy_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def list_inventory_levels_202101(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across your store's locations. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetInventoryLevelsRequest(
            query=_models.Deprecated202101GetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def delete_inventory_level_202101(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted."),
    location_id: int | None = Field(None, description="The unique identifier of the location where the inventory level should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This disconnects the item from that location; note that every inventory item must maintain at least one inventory level, so you must connect the item to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteInventoryLevelsRequest(
            query=_models.Deprecated202101DeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_locations_202101() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. Use this to get a complete list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_locations_count_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. This is useful for understanding inventory distribution across fulfillment locations without fetching full location details."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_location_202101(location_id: str = Field(..., description="The unique identifier of the location to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single location by its ID from the Shopify inventory system. Use this to fetch detailed information about a specific store location."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetLocationsParamLocationIdRequest(
            path=_models.Deprecated202101GetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_inventory_levels_for_location_202101(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves all inventory levels for a specific location. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.Deprecated202101GetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafields_count_2021_01() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields associated with a resource. Use this to determine how many custom metadata fields exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def list_fulfillment_orders_for_order_2021_01(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order. Fulfillment orders represent the items in an order that need to be fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders_for_order_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders_for_order_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders_for_order_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders_for_order_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def list_fulfillments_for_order_v202101(
    order_id: str = Field(..., description="The unique identifier of the order whose fulfillments you want to retrieve."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the limit parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_order_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_order_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_order_v202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_order_v202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_for_order_legacy_202101(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for specified line items in an order. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order_legacy_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order_legacy_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order_legacy_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_count_for_order_2021_01(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding fulfillment status and logistics tracking without fetching full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_count_for_order_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_count_for_order_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_count_for_order_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_count_for_order_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order. Use this to access fulfillment status, tracking information, and line item details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.Deprecated202101UpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_order_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to cancel."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Cancel a fulfillment for a specific order. This operation marks the fulfillment as cancelled and prevents further processing of the associated shipment."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def complete_fulfillment_2021_01(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the order that should be marked as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped or delivered to the customer."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent
@mcp.tool()
async def list_fulfillment_events_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all events associated with a specific fulfillment for an order. Events track status changes and milestones throughout the fulfillment lifecycle."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_events_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_events_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_events_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_events_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def list_order_refunds_202101(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="Maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays refund amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of refunds for a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202101GetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def create_order_refund_202101(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Shipping refund details. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each with the line item ID, quantity to refund, restock type (no_restock, cancel, or return), and location ID for restocking (required for cancel and return types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Should be obtained from the calculate endpoint for accuracy."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transactions to submit. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdRefundsRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.Deprecated202101CreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def calculate_order_refund_2021_01(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Shipping refund configuration. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping cost. The amount property takes precedence over full_refund."),
    refund_line_items: Any | None = Field(None, description="List of line items to refund, each specifying the line item ID, quantity to refund, restock instructions (no_restock, cancel, or return), and optionally the location ID where items should be restocked. If location_id is omitted for return or cancel restocks, the endpoint will suggest an appropriate location."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required whenever a shipping amount is specified, particularly important for multi-currency orders."),
) -> dict[str, Any] | ToolResult:
    """Calculate refund transactions for an order based on line items and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund, including any necessary adjustments to restock instructions."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.Deprecated202101CreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_order_refund_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_order_refund_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_order_refund_2021_01", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_order_refund_2021_01",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def get_refund_for_order_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's native currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific refund associated with an order. Use this to view refund information including amounts, line items, and status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.Deprecated202101GetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund_for_order_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund_for_order_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund_for_order_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund_for_order_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def list_order_risks_2021_01(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve risk data.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def create_order_risk_202101(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateOrdersParamOrderIdRisksRequest(
            path=_models.Deprecated202101CreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def get_order_risk_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk assessment."),
    risk_id: str = Field(..., description="The unique identifier of the specific risk assessment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a specific risk assessment associated with an order. Use this to fetch details about a fraud or security risk that has been flagged for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202101GetOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_risk_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_risk_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_risk_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_risk_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def update_order_risk_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk for a specific order. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202101UpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def delete_order_risk_202101(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be deleted."),
    risk_id: str = Field(..., description="The unique identifier of the risk assessment to be deleted from the order."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific risk assessment from an order. Note that you can only delete risks created by your application; risks created by other applications cannot be deleted."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.Deprecated202101DeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, policy, store-properties/policy
@mcp.tool()
async def list_policies_2021_01() -> dict[str, Any] | ToolResult:
    """Retrieves all policies configured for the shop, including return, privacy, shipping, and other store policies."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/policies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_policies_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_policies_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_policies_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_code_batch(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.Deprecated202101CreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_code_batch: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_code_batch")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_code_batch", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_code_batch",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_batch_202101(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch."),
    batch_id: str = Field(..., description="The unique identifier of the batch job to retrieve status and results for."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule. Use this to check the progress and results of bulk discount code generation."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.Deprecated202101GetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_batch_202101(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code batch job."),
    batch_id: str = Field(..., description="The unique identifier of the batch job for which to retrieve discount codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a specific batch job within a price rule. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.Deprecated202101GetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_price_rule_2021_01(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.Deprecated202101GetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_202101(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single discount code associated with a specific price rule. Use this to fetch detailed information about a discount code by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202101GetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def update_discount_code_202101(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code being updated."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing discount code associated with a price rule. Modify discount code properties such as code value, usage limits, and other settings."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202101UpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_discount_code_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_discount_code_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_discount_code_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_discount_code_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def delete_discount_code_202101(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted."),
) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific discount code associated with a price rule. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.Deprecated202101DeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def list_products_2021_01(
    ids: Any | None = Field(None, description="Filter results to only include products with IDs matching this comma-separated list of product identifiers."),
    limit: Any | None = Field(None, description="Maximum number of products to return per page, between 1 and 250 (defaults to 50)."),
    title: Any | None = Field(None, description="Filter results to only include products whose title contains this text."),
    vendor: Any | None = Field(None, description="Filter results to only include products from this vendor."),
    handle: Any | None = Field(None, description="Filter results to only include products with this handle (URL-friendly identifier)."),
    product_type: Any | None = Field(None, description="Filter results to only include products of this type."),
    status: Any | None = Field(None, description="Filter results by product status: active (default), archived, or draft."),
    collection_id: Any | None = Field(None, description="Filter results to only include products assigned to this collection ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published, unpublished, or any (default shows all)."),
    presentment_currencies: Any | None = Field(None, description="Return prices in only these currencies, specified as a comma-separated list of ISO 4217 currency codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of products from the store, with support for filtering by various attributes and sorting options. Uses cursor-based pagination via response headers rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetProductsRequest(
            query=_models.Deprecated202101GetProductsRequestQuery(ids=ids, limit=limit, title=title, vendor=vendor, handle=handle, product_type=product_type, status=status, collection_id=collection_id, published_status=published_status, presentment_currencies=presentment_currencies)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_products_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_products_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_products_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_products_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_products_count_202101(
    vendor: Any | None = Field(None, description="Filter the count to include only products from a specific vendor."),
    product_type: Any | None = Field(None, description="Filter the count to include only products of a specific product type."),
    collection_id: Any | None = Field(None, description="Filter the count to include only products belonging to a specific collection by its ID."),
    published_status: Any | None = Field(None, description="Filter products by their publication status: use 'published' for only published products, 'unpublished' for only unpublished products, or 'any' to include all products regardless of status (default is 'any')."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of products in the store, with optional filtering by vendor, product type, collection, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetProductsCountRequest(
            query=_models.Deprecated202101GetProductsCountRequestQuery(vendor=vendor, product_type=product_type, collection_id=collection_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_products_count_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/products/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_products_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_products_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_products_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_product_202101(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetProductsParamProductIdRequest(
            path=_models.Deprecated202101GetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def delete_product_202101(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a numeric ID assigned by Shopify.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteProductsParamProductIdRequest(
            path=_models.Deprecated202101DeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def list_product_images_202101(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of image resources for the given product."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetProductsParamProductIdImagesRequest(
            path=_models.Deprecated202101GetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def create_product_image_202101(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a specific product. The image will be associated with the product and can be used to display product visuals in the storefront."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateProductsParamProductIdImagesRequest(
            path=_models.Deprecated202101CreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_images_count_202101(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for determining how many product images exist without fetching the full image data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetProductsParamProductIdImagesCountRequest(
            path=_models.Deprecated202101GetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def update_product_image_202101(
    product_id: str = Field(..., description="The unique identifier of the product that contains the image to be updated."),
    image_id: str = Field(..., description="The unique identifier of the specific image within the product to be updated."),
) -> dict[str, Any] | ToolResult:
    """Update the properties of an existing product image, such as alt text, position, or other image metadata."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202101UpdateProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_image_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_image_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_image_202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_image_202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def delete_product_image_202101(
    product_id: str = Field(..., description="The unique identifier of the product containing the image to delete."),
    image_id: str = Field(..., description="The unique identifier of the image to delete from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image associated with a product. This removes the image from the product's image gallery."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.Deprecated202101DeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def list_recurring_application_charges_202101() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your Shopify app. Use this to view subscription billing details and charge status."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def get_recurring_application_charge_202101(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single recurring application charge by its ID. Use this to fetch current status, pricing, and other metadata for a specific recurring charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_recurring_application_charge_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_recurring_application_charge_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_recurring_application_charge_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_recurring_application_charge_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def delete_recurring_application_charge_2021_01(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to cancel.")) -> dict[str, Any] | ToolResult:
    """Cancels an active recurring application charge for a Shopify app, preventing future billing cycles."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.Deprecated202101DeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_recurring_application_charge_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_recurring_application_charge_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_recurring_application_charge_2021_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_recurring_application_charge_2021_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def update_recurring_application_charge_capped_amount_2021_01(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to update."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount limit for the recurring charge, specified as a whole number representing the maximum billable amount."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount limit for an active recurring application charge. This allows you to modify the maximum amount that can be charged to the merchant for this recurring billing plan."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.Deprecated202101UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.Deprecated202101UpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount_2021_01", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount_2021_01",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def list_usage_charges_for_recurring_application_charge_2021_01(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to retrieve associated usage charges.")) -> dict[str, Any] | ToolResult:
    """Retrieves all usage charges associated with a specific recurring application charge. Usage charges represent variable fees billed on top of a recurring application charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_usage_charges_for_recurring_application_charge_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_usage_charges_for_recurring_application_charge_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_usage_charges_for_recurring_application_charge_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_usage_charges_for_recurring_application_charge_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge_202101(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to create the usage charge.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill merchants for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.Deprecated202101CreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def get_usage_charge_for_recurring_application_charge_202101(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge that contains the usage charge you want to retrieve."),
    usage_charge_id: str = Field(..., description="The unique identifier of the specific usage charge to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single usage charge associated with a recurring application charge. Use this to fetch details about a specific metered billing charge."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequest(
            path=_models.Deprecated202101GetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id, usage_charge_id=usage_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_usage_charge_for_recurring_application_charge_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_usage_charge_for_recurring_application_charge_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_usage_charge_for_recurring_application_charge_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_usage_charge_for_recurring_application_charge_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def list_redirects_202101(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250. Defaults to 50 if not specified."),
    path: Any | None = Field(None, description="Filter results to show only redirects with a specific source path."),
    target: Any | None = Field(None, description="Filter results to show only redirects pointing to a specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of URL redirects configured in the online store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRedirectsRequest(
            query=_models.Deprecated202101GetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirects_count_2021_01(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRedirectsCountRequest(
            query=_models.Deprecated202101GetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirects_count_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirects_count_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirects_count_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirects_count_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirect_202101(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202101GetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def update_redirect_v202101(redirect_id: str = Field(..., description="The unique identifier of the redirect to update. This ID is required to locate and modify the correct redirect.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect in the online store. Modify redirect settings such as the target path or status for a specific redirect by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202101UpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect_v202101", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect_v202101",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def delete_redirect_v202101(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteRedirectsParamRedirectIdRequest(
            path=_models.Deprecated202101DeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect_v202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect_v202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect_v202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect_v202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def create_report_legacy_202101(
    name: Any | None = Field(None, description="The display name for the report. Must not exceed 255 characters."),
    shopify_ql: Any | None = Field(None, description="The ShopifyQL query string that defines what data the report will retrieve and analyze."),
) -> dict[str, Any] | ToolResult:
    """Creates a new custom report in the Shopify admin. The report can be configured with a name and ShopifyQL query to analyze store data."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101CreateReportsRequest(
            query=_models.Deprecated202101CreateReportsRequestQuery(name=name, shopify_ql=shopify_ql)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_report_legacy_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_report_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_report_legacy_202101", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_report_legacy_202101",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def get_report_202101(report_id: str = Field(..., description="The unique identifier of the report to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single report that was created by your app. Use this to fetch details about a specific report by its ID."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetReportsParamReportIdRequest(
            path=_models.Deprecated202101GetReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_report_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_report_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_report_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_report_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def update_report_legacy(report_id: str = Field(..., description="The unique identifier of the report to update. This ID is used to locate and modify the specific report in the system.")) -> dict[str, Any] | ToolResult:
    """Updates an existing report configuration in the Shopify Admin API. This operation modifies report settings and properties for the specified report."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101UpdateReportsParamReportIdRequest(
            path=_models.Deprecated202101UpdateReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_report_legacy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_report_legacy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_report_legacy", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_report_legacy",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def delete_report_202101(report_id: str = Field(..., description="The unique identifier of the report to delete. This is a required string value that identifies which report should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a specific report from the Shopify admin. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteReportsParamReportIdRequest(
            path=_models.Deprecated202101DeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def get_script_tags_count_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the store. Useful for understanding the scope of script tag usage without fetching individual records."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone
@mcp.tool()
async def list_shipping_zones_202101() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop
@mcp.tool()
async def get_shop_202101() -> dict[str, Any] | ToolResult:
    """Retrieves the current shop's configuration and metadata, including store name, currency, timezone, and other essential shop-level settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance
@mcp.tool()
async def get_shopify_payments_balance_2021_01() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, showing available funds and any pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def list_shopify_payments_payouts_2021_01(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after the specified date (inclusive)."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before the specified date (inclusive)."),
    status: Any | None = Field(None, description="Filter payouts by their current status (e.g., pending, paid, failed, cancelled)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all payouts from Shopify Payments, ordered by payout date with the most recent first. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetShopifyPaymentsPayoutsRequest(
            query=_models.Deprecated202101GetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_payouts_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_payouts_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_payouts_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_payouts_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def get_shopify_payments_payout_2021_01(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.Deprecated202101GetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_shopify_payments_payout_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_payout_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_payout_2021_01", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_payout_2021_01",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def list_smart_collections_202101(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Must be between 1 and 250, defaults to 50."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by the smart collection's URL-friendly handle."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished only, or 'any' for all collections. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from your store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetSmartCollectionsRequest(
            query=_models.Deprecated202101GetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def count_smart_collections_202101(
    title: Any | None = Field(None, description="Filter to only count smart collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter to only count smart collections that include the specified product."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' to include all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetSmartCollectionsCountRequest(
            query=_models.Deprecated202101GetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def delete_smart_collection_202101(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.Deprecated202101DeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tendertransaction
@mcp.tool()
async def list_tender_transactions_202101(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Filter to show only tender transactions processed on or after this date (ISO 8601 format)."),
    processed_at_max: Any | None = Field(None, description="Filter to show only tender transactions processed on or before this date (ISO 8601 format)."),
    order: Any | None = Field(None, description="Sort results by processed_at timestamp in ascending or descending order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of tender transactions processed through the store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetTenderTransactionsRequest(
            query=_models.Deprecated202101GetTenderTransactionsRequestQuery(limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tender_transactions_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/tender_transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tender_transactions_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tender_transactions_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tender_transactions_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def list_themes_202101() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store, including both published and unpublished themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def get_theme_202101(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single theme by its ID from the Shopify online store. Use this to fetch detailed information about a specific theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetThemesParamThemeIdRequest(
            path=_models.Deprecated202101GetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def delete_theme_2021_01(theme_id: str = Field(..., description="The unique identifier of the theme to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the Shopify store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteThemesParamThemeIdRequest(
            path=_models.Deprecated202101DeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_2021_01: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_2021_01")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_2021_01", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_2021_01",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def delete_theme_asset_202101(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key (file path) of the asset to delete from the theme. This identifies which specific asset file to remove."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key within the theme."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101DeleteThemesParamThemeIdAssetsRequest(
            path=_models.Deprecated202101DeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.Deprecated202101DeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/2021-01/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/2021-01/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset_202101", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset_202101",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def list_webhooks_legacy_202101(
    address: Any | None = Field(None, description="Filter webhooks by the URI endpoint that receives POST requests. Only webhooks sending to this address will be returned."),
    limit: Any | None = Field(None, description="Maximum number of webhooks to return per request, between 1 and 250. Defaults to 50 if not specified."),
    topic: Any | None = Field(None, description="Filter webhooks by topic to return only subscriptions for specific events. Refer to the Shopify webhook topics documentation for valid values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of webhook subscriptions configured for your Shopify store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetWebhooksRequest(
            query=_models.Deprecated202101GetWebhooksRequestQuery(address=address, limit=limit, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_webhooks_legacy_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/webhooks.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_webhooks_legacy_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_webhooks_legacy_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhooks_legacy_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def count_webhooks_202101(
    address: Any | None = Field(None, description="Filter the webhook count to only subscriptions that deliver POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter the webhook count to only subscriptions listening to this specific topic. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the Shopify store, with optional filtering by delivery address or topic."""

    # Construct request model with validation
    try:
        _request = _models.Deprecated202101GetWebhooksCountRequest(
            query=_models.Deprecated202101GetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_webhooks_202101: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/2021-01/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_webhooks_202101")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_webhooks_202101", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_webhooks_202101",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def list_application_charges_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all application charges for the store. This endpoint provides access to charges created by your app, including recurring and one-time charges."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_charges_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_charges_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_charges_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcharge, billing/applicationcharge
@mcp.tool()
async def get_application_charge_unstable(application_charge_id: str = Field(..., description="The unique identifier of the application charge to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific application charge by its ID. Use this to fetch the current status and information of a billing charge associated with your app."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetApplicationChargesParamApplicationChargeIdRequest(
            path=_models.DeprecatedUnstableGetApplicationChargesParamApplicationChargeIdRequestPath(application_charge_id=application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_charge_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/application_charges/{application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/application_charges/{application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_charge_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_charge_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_charge_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def list_application_credits_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves all application credits for the store. This endpoint provides a complete list of credits that have been issued to the application."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/application_credits.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_application_credits_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_application_credits_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_application_credits_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, applicationcredit, billing/applicationcredit
@mcp.tool()
async def get_application_credit_unstable(application_credit_id: str = Field(..., description="The unique identifier of the application credit to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single application credit by its ID. Use this to fetch information about a specific credit issued to an app."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetApplicationCreditsParamApplicationCreditIdRequest(
            path=_models.DeprecatedUnstableGetApplicationCreditsParamApplicationCreditIdRequestPath(application_credit_id=application_credit_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_application_credit_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/application_credits/{application_credit_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/application_credits/{application_credit_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_application_credit_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_application_credit_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_application_credit_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_article_tags_unstable(
    limit: Any | None = Field(None, description="Maximum number of tags to return in the response. Limits the size of the result set."),
    popular: Any | None = Field(None, description="When included, sorts tags by popularity in descending order (most popular first). Omit this parameter to retrieve tags in default order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all tags used across articles in the online store. Results can be optionally limited and sorted by popularity."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetArticlesTagsRequest(
            query=_models.DeprecatedUnstableGetArticlesTagsRequestQuery(limit=limit, popular=popular)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_article_tags_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/articles/tags.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_article_tags_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_article_tags_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_article_tags_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def list_blog_articles_unstable(
    blog_id: str = Field(..., description="The unique identifier of the blog from which to retrieve articles."),
    limit: Any | None = Field(None, description="Maximum number of articles to return per request, between 1 and 250 (defaults to 50)."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: published (published articles only), unpublished (unpublished articles only), or any (all articles regardless of status, default)."),
    handle: Any | None = Field(None, description="Retrieve a single article by its unique handle identifier."),
    tag: Any | None = Field(None, description="Filter articles to only those tagged with a specific tag."),
    author: Any | None = Field(None, description="Filter articles to only those written by a specific author."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of articles from a specific blog. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetBlogsParamBlogIdArticlesRequest(
            path=_models.DeprecatedUnstableGetBlogsParamBlogIdArticlesRequestPath(blog_id=blog_id),
            query=_models.DeprecatedUnstableGetBlogsParamBlogIdArticlesRequestQuery(limit=limit, published_status=published_status, handle=handle, tag=tag, author=author)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_blog_articles_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/blogs/{blog_id}/articles.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/blogs/{blog_id}/articles.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_blog_articles_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_blog_articles_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_blog_articles_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def get_article_count_for_blog(
    blog_id: str = Field(..., description="The unique identifier of the blog for which to retrieve the article count."),
    published_status: Any | None = Field(None, description="Filter articles by publication status: use 'published' to count only published articles, 'unpublished' to count only unpublished articles, or 'any' to count all articles regardless of status. Defaults to 'any' if not specified."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of articles in a specified blog, with optional filtering by publication status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetBlogsParamBlogIdArticlesCountRequest(
            path=_models.DeprecatedUnstableGetBlogsParamBlogIdArticlesCountRequestPath(blog_id=blog_id),
            query=_models.DeprecatedUnstableGetBlogsParamBlogIdArticlesCountRequestQuery(published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_article_count_for_blog: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/blogs/{blog_id}/articles/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/blogs/{blog_id}/articles/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_article_count_for_blog")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_article_count_for_blog", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_article_count_for_blog",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, article, online-store/article
@mcp.tool()
async def update_article_unstable(
    blog_id: str = Field(..., description="The unique identifier of the blog containing the article to update."),
    article_id: str = Field(..., description="The unique identifier of the article to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an article within a specific blog. Modify article content, metadata, and other properties using this endpoint."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateBlogsParamBlogIdArticlesParamArticleIdRequest(
            path=_models.DeprecatedUnstableUpdateBlogsParamBlogIdArticlesParamArticleIdRequestPath(blog_id=blog_id, article_id=article_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_article_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/blogs/{blog_id}/articles/{article_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/blogs/{blog_id}/articles/{article_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_article_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_article_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_article_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def list_carrier_services_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all available carrier services that can be used for shipping fulfillment. This endpoint provides carrier service configurations for managing shipping options in your store."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/carrier_services.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_carrier_services_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_carrier_services_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_carrier_services_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, carrierservice, shipping-and-fulfillment/carrierservice
@mcp.tool()
async def update_carrier_service_unstable(carrier_service_id: str = Field(..., description="The unique identifier of the carrier service to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing carrier service. Only the app that originally created the carrier service can modify it."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateCarrierServicesParamCarrierServiceIdRequest(
            path=_models.DeprecatedUnstableUpdateCarrierServicesParamCarrierServiceIdRequestPath(carrier_service_id=carrier_service_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_carrier_service_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/carrier_services/{carrier_service_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/carrier_services/{carrier_service_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_carrier_service_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_carrier_service_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_carrier_service_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, abandoned-checkouts, orders/abandoned-checkouts
@mcp.tool()
async def get_abandoned_checkouts_count_unstable(status: Any | None = Field(None, description="Filter the count by checkout status. Use 'open' to count only active abandoned checkouts, or 'closed' to count only completed/closed abandoned checkouts. Defaults to 'open' if not specified.")) -> dict[str, Any] | ToolResult:
    """Retrieves the count of abandoned checkouts from the past 90 days, optionally filtered by checkout status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCheckoutsCountRequest(
            query=_models.DeprecatedUnstableGetCheckoutsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_abandoned_checkouts_count_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/checkouts/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_abandoned_checkouts_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_abandoned_checkouts_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_abandoned_checkouts_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def get_collection_unstable(collection_id: str = Field(..., description="The unique identifier of the collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single collection by its ID. Use this to fetch collection information such as name, description, and other metadata."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCollectionsParamCollectionIdRequest(
            path=_models.DeprecatedUnstableGetCollectionsParamCollectionIdRequestPath(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/collections/{collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/collections/{collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collection_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collection_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collection, products/collection
@mcp.tool()
async def list_collection_products_unstable(
    collection_id: str = Field(..., description="The unique identifier of the collection whose products you want to retrieve."),
    limit: Any | None = Field(None, description="The maximum number of products to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieve products belonging to a specific collection, sorted by the collection's configured sort order. Results are paginated using link-based navigation provided in response headers."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCollectionsParamCollectionIdProductsRequest(
            path=_models.DeprecatedUnstableGetCollectionsParamCollectionIdProductsRequestPath(collection_id=collection_id),
            query=_models.DeprecatedUnstableGetCollectionsParamCollectionIdProductsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collection_products_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/collections/{collection_id}/products.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/collections/{collection_id}/products.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collection_products_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collection_products_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collection_products_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def list_collects_unstable(limit: Any | None = Field(None, description="Maximum number of collects to return per request, between 1 and 250 (defaults to 50).")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of collects (product-to-collection associations). Uses cursor-based pagination via response header links; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCollectsRequest(
            query=_models.DeprecatedUnstableGetCollectsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_collects_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/collects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_collects_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_collects_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_collects_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collects_count_unstable(collection_id: int | None = Field(None, description="Filter the count to only include collects belonging to a specific collection. Omit this parameter to get the total count across all collections.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of collects, optionally filtered by a specific collection. Use this to determine how many products are associated with a collection without fetching individual collect records."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCollectsCountRequest(
            query=_models.DeprecatedUnstableGetCollectsCountRequestQuery(collection_id=collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collects_count_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/collects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collects_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collects_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collects_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def get_collect_unstable(collect_id: str = Field(..., description="The unique identifier of the collect record to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a specific product collection by its ID. Use this to fetch details about how a product is organized within a collection."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCollectsParamCollectIdRequest(
            path=_models.DeprecatedUnstableGetCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_collect_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_collect_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_collect_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_collect_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, collect, products/collect
@mcp.tool()
async def remove_product_from_collection_unstable(collect_id: str = Field(..., description="The unique identifier of the collect relationship to remove. This ID represents the link between a product and a collection.")) -> dict[str, Any] | ToolResult:
    """Removes a product from a collection by deleting the collect relationship. This operation unlinks a product from a specific collection without affecting the product or collection itself."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteCollectsParamCollectIdRequest(
            path=_models.DeprecatedUnstableDeleteCollectsParamCollectIdRequestPath(collect_id=collect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_product_from_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/collects/{collect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/collects/{collect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_product_from_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_product_from_collection_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_product_from_collection_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, currency, store-properties/currency
@mcp.tool()
async def list_currencies_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all currencies currently enabled on the shop. Use this to discover which currencies are available for transactions and pricing."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/currencies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_currencies_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_currencies_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_currencies_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def list_custom_collections_unstable(
    limit: Any | None = Field(None, description="Maximum number of collections to return per request, between 1 and 250. Defaults to 50 if not specified."),
    ids: Any | None = Field(None, description="Filter results to only collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to only collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to only collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results to only collections with the specified URL-friendly handle."),
    published_status: Any | None = Field(None, description="Filter by publication status: 'published' for published collections only, 'unpublished' for unpublished only, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of custom collections from your store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomCollectionsRequest(
            query=_models.DeprecatedUnstableGetCustomCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_custom_collections_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/custom_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_custom_collections_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_custom_collections_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_custom_collections_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def count_custom_collections_unstable(
    title: Any | None = Field(None, description="Filter the count to include only custom collections with this exact title."),
    product_id: Any | None = Field(None, description="Filter the count to include only custom collections that contain this specific product."),
    published_status: Any | None = Field(None, description="Filter the count by publication status: 'published' for only published collections, 'unpublished' for only unpublished collections, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of custom collections in the store, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomCollectionsCountRequest(
            query=_models.DeprecatedUnstableGetCustomCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_custom_collections_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/custom_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_custom_collections_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_custom_collections_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_custom_collections_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def get_custom_collection_unstable(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single custom collection by its ID from the Shopify admin API. Use this to fetch detailed information about a specific custom collection."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.DeprecatedUnstableGetCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_custom_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_custom_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_custom_collection_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_custom_collection_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def update_custom_collection_unstable(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to update. This ID is required to specify which collection should be modified.")) -> dict[str, Any] | ToolResult:
    """Updates an existing custom collection in your Shopify store. Modify collection details such as title, description, image, and other properties."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.DeprecatedUnstableUpdateCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_custom_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_custom_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_custom_collection_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_custom_collection_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, customcollection, products/customcollection
@mcp.tool()
async def delete_custom_collection_unstable(custom_collection_id: str = Field(..., description="The unique identifier of the custom collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a custom collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteCustomCollectionsParamCustomCollectionIdRequest(
            path=_models.DeprecatedUnstableDeleteCustomCollectionsParamCustomCollectionIdRequestPath(custom_collection_id=custom_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_custom_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/custom_collections/{custom_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/custom_collections/{custom_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_custom_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_custom_collection_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_custom_collection_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customers_unstable(
    ids: Any | None = Field(None, description="Filter results to only include customers with the specified IDs. Provide as a comma-separated list of customer ID values."),
    limit: Any | None = Field(None, description="Maximum number of customer records to return per request. Defaults to 50 if not specified; maximum allowed is 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of customers from the Shopify store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersRequest(
            query=_models.DeprecatedUnstableGetCustomersRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customers_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/customers.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customers_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customers_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customers_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customers_count_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all customers in the store. This unstable endpoint provides a quick way to determine the total number of customer records without fetching individual customer data."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/customers/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customers_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customers_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customers_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def search_customers_unstable(
    order: Any | None = Field(None, description="Specify which field to sort results by and the sort direction (ascending or descending). Defaults to sorting by most recent order date in descending order."),
    query: Any | None = Field(None, description="Text query to search across customer data fields such as name, email, phone, and address information."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request, ranging from 1 to 250. Defaults to 50 results."),
) -> dict[str, Any] | ToolResult:
    """Search for customers in the shop by query text. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersSearchRequest(
            query=_models.DeprecatedUnstableGetCustomersSearchRequestQuery(order=order, query=query, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for search_customers_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/customers/search.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("search_customers_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("search_customers_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="search_customers_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def get_customer_unstable(customer_id: str = Field(..., description="The unique identifier of the customer to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single customer by their unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersParamCustomerIdRequest(
            path=_models.DeprecatedUnstableGetCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def update_customer_unstable(customer_id: str = Field(..., description="The unique identifier of the customer to update. This is a required string value that specifies which customer record to modify.")) -> dict[str, Any] | ToolResult:
    """Updates an existing customer's information in the Shopify store. This operation modifies customer details such as name, email, phone, and other profile attributes."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateCustomersParamCustomerIdRequest(
            path=_models.DeprecatedUnstableUpdateCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_customer_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_customer_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_customer_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_customer_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def delete_customer_unstable(customer_id: str = Field(..., description="The unique identifier of the customer to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a customer from the store. The deletion will fail if the customer has any existing orders."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteCustomersParamCustomerIdRequest(
            path=_models.DeprecatedUnstableDeleteCustomersParamCustomerIdRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def generate_customer_account_activation_url_unstable(customer_id: str = Field(..., description="The unique identifier of the customer for whom to generate the account activation URL.")) -> dict[str, Any] | ToolResult:
    """Generate a one-time account activation URL for a customer whose account is not yet enabled. The URL expires after 30 days; generating a new URL invalidates any previously generated URLs."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateCustomersParamCustomerIdAccountActivationUrlRequest(
            path=_models.DeprecatedUnstableCreateCustomersParamCustomerIdAccountActivationUrlRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_customer_account_activation_url_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/account_activation_url.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/account_activation_url.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_customer_account_activation_url_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_customer_account_activation_url_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_customer_account_activation_url_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def list_customer_addresses_unstable(customer_id: str = Field(..., description="The unique identifier of the customer whose addresses you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves all addresses associated with a specific customer. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersParamCustomerIdAddressesRequest(
            path=_models.DeprecatedUnstableGetCustomersParamCustomerIdAddressesRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_addresses_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/addresses.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/addresses.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_addresses_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_addresses_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_addresses_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def bulk_update_customer_addresses_unstable(
    customer_id: str = Field(..., description="The unique identifier of the customer whose addresses will be modified."),
    address_ids: int | None = Field(None, description="Array of address IDs to include in the bulk operation. The order may be significant depending on the operation type."),
    operation: str | None = Field(None, description="The type of bulk operation to perform on the specified addresses (e.g., set as default, delete, or update)."),
) -> dict[str, Any] | ToolResult:
    """Perform bulk operations on multiple addresses for a specific customer, such as setting default addresses or updating address properties in batch."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateCustomersParamCustomerIdAddressesSetRequest(
            path=_models.DeprecatedUnstableUpdateCustomersParamCustomerIdAddressesSetRequestPath(customer_id=customer_id),
            query=_models.DeprecatedUnstableUpdateCustomersParamCustomerIdAddressesSetRequestQuery(address_ids=address_ids, operation=operation)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for bulk_update_customer_addresses_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/addresses/set.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/addresses/set.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("bulk_update_customer_addresses_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("bulk_update_customer_addresses_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="bulk_update_customer_addresses_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def get_customer_address_unstable(
    customer_id: str = Field(..., description="The unique identifier of the customer who owns the address."),
    address_id: str = Field(..., description="The unique identifier of the specific address to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific customer address by customer ID and address ID."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.DeprecatedUnstableGetCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_address_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_address_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_address_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_address_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def delete_customer_address_unstable(
    customer_id: str = Field(..., description="The unique identifier of the customer whose address is being removed."),
    address_id: str = Field(..., description="The unique identifier of the address to be deleted from the customer's address list."),
) -> dict[str, Any] | ToolResult:
    """Permanently removes a specific address from a customer's address list. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteCustomersParamCustomerIdAddressesParamAddressIdRequest(
            path=_models.DeprecatedUnstableDeleteCustomersParamCustomerIdAddressesParamAddressIdRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_customer_address_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/addresses/{address_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/addresses/{address_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_customer_address_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_customer_address_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_customer_address_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer-address, customers/customer-address
@mcp.tool()
async def set_customer_default_address_unstable(
    customer_id: str = Field(..., description="The unique identifier of the customer whose default address is being updated."),
    address_id: str = Field(..., description="The unique identifier of the address to set as the customer's default address."),
) -> dict[str, Any] | ToolResult:
    """Sets a specific address as the default address for a customer. The customer's default address is used for shipping and billing purposes unless overridden during checkout."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequest(
            path=_models.DeprecatedUnstableUpdateCustomersParamCustomerIdAddressesParamAddressIdDefaultRequestPath(customer_id=customer_id, address_id=address_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_customer_default_address_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/addresses/{address_id}/default.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/addresses/{address_id}/default.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_customer_default_address_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_customer_default_address_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_customer_default_address_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def list_customer_orders_unstable(customer_id: str = Field(..., description="The unique identifier of the customer whose orders should be retrieved.")) -> dict[str, Any] | ToolResult:
    """Retrieves all orders for a specific customer. Supports the same query parameters available for the Order resource to enable filtering, sorting, and pagination."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetCustomersParamCustomerIdOrdersRequest(
            path=_models.DeprecatedUnstableGetCustomersParamCustomerIdOrdersRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_orders_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_orders_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_orders_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_orders_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customers, customer, customers/customer
@mcp.tool()
async def send_customer_invite_unstable(customer_id: str = Field(..., description="The unique identifier of the customer who will receive the account invite.")) -> dict[str, Any] | ToolResult:
    """Sends an account invitation to a customer, allowing them to create or access their account on the store."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateCustomersParamCustomerIdSendInviteRequest(
            path=_models.DeprecatedUnstableCreateCustomersParamCustomerIdSendInviteRequestPath(customer_id=customer_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_customer_invite_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/customers/{customer_id}/send_invite.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/customers/{customer_id}/send_invite.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_customer_invite_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_customer_invite_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_customer_invite_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def list_events_unstable(
    limit: Any | None = Field(None, description="Maximum number of events to return per request, between 1 and 250. Defaults to 50 if not specified."),
    filter_: Any | None = Field(None, alias="filter", description="Filter events by specific criteria to narrow results to relevant events only."),
    verb: Any | None = Field(None, description="Filter events by type or action (e.g., 'create', 'update', 'delete') to show only events of a certain kind."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of events from your Shopify store. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the page parameter."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetEventsRequest(
            query=_models.DeprecatedUnstableGetEventsRequestQuery(limit=limit, filter_=filter_, verb=verb)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_events_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/events.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_events_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_events_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_events_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, event, events/event
@mcp.tool()
async def get_events_count_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of events in the Shopify store. This unstable endpoint provides a quick way to determine the number of events without fetching the full event list."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/events/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_events_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_events_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_events_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def get_fulfillment_order_unstable(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific fulfillment order by its ID. Use this to fetch current status, line items, and fulfillment information for a particular order."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetFulfillmentOrdersParamFulfillmentOrderIdRequest(
            path=_models.DeprecatedUnstableGetFulfillmentOrdersParamFulfillmentOrderIdRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_order_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_order_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def cancel_fulfillment_order_unstable(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID references a specific fulfillment order in the system.")) -> dict[str, Any] | ToolResult:
    """Cancels a fulfillment order, marking it as no longer needed for processing. This operation is useful when orders need to be cancelled before they are fulfilled."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancelRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def send_fulfillment_order_cancellation_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to cancel. This ID must correspond to an existing fulfillment order in the system."),
    message: Any | None = Field(None, description="An optional message explaining the reason for the cancellation request. This message is typically sent to the fulfillment service to provide context for the cancellation."),
) -> dict[str, Any] | ToolResult:
    """Sends a cancellation request to the fulfillment service for a specific fulfillment order. This notifies the fulfillment provider that the order should be cancelled if not yet processed."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_order_cancellation_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_order_cancellation_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_order_cancellation_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_order_cancellation_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def accept_fulfillment_order_cancellation_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to accept the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining the reason for accepting the cancellation request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a cancellation request for a fulfillment order, notifying the fulfillment service that the cancellation has been approved. Optionally include a reason for accepting the cancellation."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_order_cancellation_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_order_cancellation_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_order_cancellation_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_order_cancellation_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, cancellationrequest, shipping-and-fulfillment/cancellationrequest
@mcp.tool()
async def reject_fulfillment_order_cancellation_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which you are rejecting the cancellation request."),
    message: Any | None = Field(None, description="An optional message explaining why the cancellation request is being rejected. This reason may be communicated to the fulfillment service."),
) -> dict[str, Any] | ToolResult:
    """Rejects a cancellation request that was sent to a fulfillment service for a specific fulfillment order. Use this to deny a cancellation and keep the fulfillment order active."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCancellationRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_order_cancellation_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/cancellation_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_order_cancellation_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_order_cancellation_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_order_cancellation_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def close_fulfillment_order_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to close."),
    message: Any | None = Field(None, description="An optional reason or message explaining why the fulfillment order is being marked as incomplete."),
) -> dict[str, Any] | ToolResult:
    """Marks an in-progress fulfillment order as incomplete, indicating the fulfillment service cannot ship remaining items and is closing the order."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdCloseRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for close_fulfillment_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/close.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/close.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("close_fulfillment_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("close_fulfillment_order_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="close_fulfillment_order_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def send_fulfillment_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to request fulfillment for."),
    message: Any | None = Field(None, description="An optional message to include with the fulfillment request, such as special instructions or notes for the fulfillment service."),
    fulfillment_order_line_items: Any | None = Field(None, description="An optional list of specific line items from the fulfillment order to request for fulfillment. If omitted, all line items in the fulfillment order will be requested for fulfillment."),
) -> dict[str, Any] | ToolResult:
    """Sends a fulfillment request to the fulfillment service for a specific fulfillment order, optionally specifying which line items to fulfill and including an optional message."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestQuery(message=message, fulfillment_order_line_items=fulfillment_order_line_items)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_fulfillment_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_fulfillment_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_fulfillment_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_fulfillment_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def accept_fulfillment_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request is being accepted."),
    message: Any | None = Field(None, description="An optional message providing context or reasoning for accepting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Accepts a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, confirming that the fulfillment service will proceed with fulfilling the order."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestAcceptRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for accept_fulfillment_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/accept.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("accept_fulfillment_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("accept_fulfillment_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="accept_fulfillment_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentrequest, shipping-and-fulfillment/fulfillmentrequest
@mcp.tool()
async def reject_fulfillment_request_unstable(
    fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which the fulfillment request should be rejected."),
    message: Any | None = Field(None, description="An optional message explaining the reason for rejecting the fulfillment request."),
) -> dict[str, Any] | ToolResult:
    """Rejects a fulfillment request that was sent to a fulfillment service for a specific fulfillment order, optionally providing a reason for the rejection."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestPath(fulfillment_order_id=fulfillment_order_id),
            query=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdFulfillmentRequestRejectRequestQuery(message=message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reject_fulfillment_request_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/fulfillment_request/reject.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reject_fulfillment_request_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reject_fulfillment_request_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reject_fulfillment_request_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, locationsformove, shipping-and-fulfillment/locationsformove
@mcp.tool()
async def list_locations_for_fulfillment_order_move_unstable(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order for which to retrieve available move destinations.")) -> dict[str, Any] | ToolResult:
    """Retrieves an alphabetically sorted list of locations where a fulfillment order can be moved. Use this to determine valid destination locations before initiating a fulfillment order transfer."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequest(
            path=_models.DeprecatedUnstableGetFulfillmentOrdersParamFulfillmentOrderIdLocationsForMoveRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_locations_for_fulfillment_order_move_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/locations_for_move.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_for_fulfillment_order_move_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_for_fulfillment_order_move_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_for_fulfillment_order_move_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def open_fulfillment_order_unstable(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to open for fulfillment.")) -> dict[str, Any] | ToolResult:
    """Marks a scheduled fulfillment order as ready for fulfillment, allowing merchants to begin work on it before its scheduled fulfill_at datetime."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdOpenRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdOpenRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment_order_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment_order_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def reschedule_fulfillment_order(fulfillment_order_id: str = Field(..., description="The unique identifier of the fulfillment order to reschedule.")) -> dict[str, Any] | ToolResult:
    """Reschedule a fulfillment order by updating its fulfill_at time. Use this to change when a scheduled fulfillment order will be marked as ready for fulfillment."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdRescheduleRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentOrdersParamFulfillmentOrderIdRescheduleRequestPath(fulfillment_order_id=fulfillment_order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reschedule_fulfillment_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/reschedule.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillment_orders/{fulfillment_order_id}/reschedule.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reschedule_fulfillment_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reschedule_fulfillment_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reschedule_fulfillment_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_unstable(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel. This ID references a specific fulfillment order that has been previously created.")) -> dict[str, Any] | ToolResult:
    """Cancels an existing fulfillment order. This operation marks the specified fulfillment as cancelled and updates its status accordingly."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentsParamFulfillmentIdCancelRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_tracking_unstable(fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment whose tracking information should be updated.")) -> dict[str, Any] | ToolResult:
    """Updates tracking information for a specific fulfillment, allowing you to modify shipment tracking details after the fulfillment has been created."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateFulfillmentsParamFulfillmentIdUpdateTrackingRequest(
            path=_models.DeprecatedUnstableCreateFulfillmentsParamFulfillmentIdUpdateTrackingRequestPath(fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_tracking_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/fulfillments/{fulfillment_id}/update_tracking.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/fulfillments/{fulfillment_id}/update_tracking.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_tracking_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_tracking_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_tracking_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def get_gift_cards_count_unstable(status: Any | None = Field(None, description="Filter the count to only include gift cards with a specific status: enabled for active gift cards, or disabled for inactive gift cards. Omit to count all gift cards regardless of status.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of gift cards, optionally filtered by their enabled or disabled status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetGiftCardsCountRequest(
            query=_models.DeprecatedUnstableGetGiftCardsCountRequestQuery(status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_gift_cards_count_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/gift_cards/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_gift_cards_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_gift_cards_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_gift_cards_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def get_gift_card_unstable(gift_card_id: str = Field(..., description="The unique identifier of the gift card to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single gift card by its unique identifier. Use this to fetch detailed information about a specific gift card."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetGiftCardsParamGiftCardIdRequest(
            path=_models.DeprecatedUnstableGetGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_gift_card_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_gift_card_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_gift_card_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_gift_card_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def update_gift_card_unstable(gift_card_id: str = Field(..., description="The unique identifier of the gift card to update.")) -> dict[str, Any] | ToolResult:
    """Updates an existing gift card's expiry date, note, and template suffix. The gift card's balance cannot be modified through this API."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateGiftCardsParamGiftCardIdRequest(
            path=_models.DeprecatedUnstableUpdateGiftCardsParamGiftCardIdRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_gift_card_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/gift_cards/{gift_card_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/gift_cards/{gift_card_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_gift_card_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_gift_card_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_gift_card_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, giftcard, plus/giftcard
@mcp.tool()
async def disable_gift_card_unstable(gift_card_id: str = Field(..., description="The unique identifier of the gift card to disable.")) -> dict[str, Any] | ToolResult:
    """Permanently disables a gift card, preventing it from being used for future transactions. This action cannot be reversed."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateGiftCardsParamGiftCardIdDisableRequest(
            path=_models.DeprecatedUnstableCreateGiftCardsParamGiftCardIdDisableRequestPath(gift_card_id=gift_card_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for disable_gift_card_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/gift_cards/{gift_card_id}/disable.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/gift_cards/{gift_card_id}/disable.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("disable_gift_card_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("disable_gift_card_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="disable_gift_card_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def list_inventory_items_unstable(
    limit: Any | None = Field(None, description="Maximum number of inventory items to return per request, between 1 and 250 items (defaults to 50)."),
    ids: str | None = Field(None, description="Filter results to specific inventory items by their numeric IDs. Provide as a comma-separated list of integers."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of inventory items. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetInventoryItemsRequest(
            query=_models.DeprecatedUnstableGetInventoryItemsRequestQuery(limit=limit, ids=ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_items_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/inventory_items.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_items_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_items_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_items_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventoryitem, inventory/inventoryitem
@mcp.tool()
async def get_inventory_item_unstable(inventory_item_id: str = Field(..., description="The unique identifier of the inventory item to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single inventory item by its ID. Use this to fetch detailed information about a specific inventory item in your Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetInventoryItemsParamInventoryItemIdRequest(
            path=_models.DeprecatedUnstableGetInventoryItemsParamInventoryItemIdRequestPath(inventory_item_id=inventory_item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_inventory_item_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/inventory_items/{inventory_item_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/inventory_items/{inventory_item_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_inventory_item_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_inventory_item_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_inventory_item_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def list_inventory_levels_unstable(
    inventory_item_ids: Any | None = Field(None, description="Comma-separated list of inventory item IDs to filter results. Maximum of 50 IDs per request."),
    location_ids: Any | None = Field(None, description="Comma-separated list of location IDs to filter results. Maximum of 50 IDs per request. Use the Location resource to find location IDs."),
    limit: Any | None = Field(None, description="Maximum number of results to return per request. Defaults to 50 if not specified; cannot exceed 250."),
) -> dict[str, Any] | ToolResult:
    """Retrieves inventory levels across your store's locations and inventory items. You must filter by at least one inventory item ID or location ID to retrieve results."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetInventoryLevelsRequest(
            query=_models.DeprecatedUnstableGetInventoryLevelsRequestQuery(inventory_item_ids=inventory_item_ids, location_ids=location_ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, inventorylevel, inventory/inventorylevel
@mcp.tool()
async def delete_inventory_level_unstable(
    inventory_item_id: int | None = Field(None, description="The unique identifier of the inventory item whose level should be deleted."),
    location_id: int | None = Field(None, description="The unique identifier of the location from which the inventory item should be removed."),
) -> dict[str, Any] | ToolResult:
    """Removes an inventory level for a specific inventory item at a location. This disconnects the inventory item from that location; note that every inventory item must maintain at least one inventory level, so ensure the item is connected to another location before deleting its last level."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteInventoryLevelsRequest(
            query=_models.DeprecatedUnstableDeleteInventoryLevelsRequestQuery(inventory_item_id=inventory_item_id, location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_inventory_level_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/inventory_levels.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_inventory_level_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_inventory_level_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_inventory_level_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_locations_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves all inventory locations for the store. This endpoint provides access to the complete list of physical and virtual locations where inventory is managed."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/locations.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_locations_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_locations_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_locations_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_locations_count_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of locations in the Shopify store. This provides a quick way to determine how many physical or virtual locations are configured without fetching full location details."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/locations/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_locations_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_locations_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_locations_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def get_location_unstable(location_id: str = Field(..., description="The unique identifier of the location to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single location by its ID from the Shopify inventory system. Use this to fetch detailed information about a specific location."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetLocationsParamLocationIdRequest(
            path=_models.DeprecatedUnstableGetLocationsParamLocationIdRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_location_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/locations/{location_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/locations/{location_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_location_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_location_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_location_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: inventory, location, inventory/location
@mcp.tool()
async def list_inventory_levels_for_location_unstable(location_id: str = Field(..., description="The unique identifier of the location for which to retrieve inventory levels.")) -> dict[str, Any] | ToolResult:
    """Retrieves all inventory levels for a specific location. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetLocationsParamLocationIdInventoryLevelsRequest(
            path=_models.DeprecatedUnstableGetLocationsParamLocationIdInventoryLevelsRequestPath(location_id=location_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_inventory_levels_for_location_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/locations/{location_id}/inventory_levels.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/locations/{location_id}/inventory_levels.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_inventory_levels_for_location_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_inventory_levels_for_location_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_inventory_levels_for_location_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafields_count_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of metafields for a resource. Use this to determine how many metafields exist without fetching the full list."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/metafields/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafields_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafields_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafields_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def get_metafield_unstable(metafield_id: str = Field(..., description="The unique identifier of the metafield to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single metafield by its ID from the Shopify admin API. Use this to fetch detailed information about a specific metafield resource."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetMetafieldsParamMetafieldIdRequest(
            path=_models.DeprecatedUnstableGetMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_metafield_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_metafield_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_metafield_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_metafield_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: metafield
@mcp.tool()
async def update_metafield_unstable(metafield_id: str = Field(..., description="The unique identifier of the metafield to update. This ID is returned when a metafield is created and is required to target the specific metafield for modification.")) -> dict[str, Any] | ToolResult:
    """Updates an existing metafield with new values. Modify metafield properties such as namespace, key, value, and type for the specified metafield resource."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateMetafieldsParamMetafieldIdRequest(
            path=_models.DeprecatedUnstableUpdateMetafieldsParamMetafieldIdRequestPath(metafield_id=metafield_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_metafield_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/metafields/{metafield_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/metafields/{metafield_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_metafield_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_metafield_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_metafield_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentorder, shipping-and-fulfillment/fulfillmentorder
@mcp.tool()
async def list_fulfillment_orders_for_order(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve fulfillment orders.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillment orders associated with a specific order, allowing you to track fulfillment status and details across multiple fulfillment locations."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentOrdersRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentOrdersRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillment_orders_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillment_orders.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillment_orders.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillment_orders_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillment_orders_for_order", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillment_orders_for_order",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def list_fulfillments_for_order_unstable(
    order_id: str = Field(..., description="The unique identifier of the order whose fulfillments you want to retrieve."),
    limit: Any | None = Field(None, description="Maximum number of fulfillments to return per request, between 1 and 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all fulfillments associated with a specific order. Results are paginated using link headers in the response; use the provided links to navigate pages rather than the limit parameter."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id),
            query=_models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_fulfillments_for_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_fulfillments_for_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_fulfillments_for_order_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_fulfillments_for_order_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def create_fulfillment_for_order_unstable(order_id: str = Field(..., description="The unique identifier of the order for which to create the fulfillment.")) -> dict[str, Any] | ToolResult:
    """Create a fulfillment for specified line items in an order. The fulfillment status depends on the fulfillment service type: manual/custom services set status immediately, while external services queue the fulfillment with pending status until processed."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_for_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_for_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_for_order_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_for_order_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_count_for_order_unstable(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve the fulfillment count.")) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of fulfillments associated with a specific order. Useful for understanding how many fulfillment operations have been created for an order without fetching full fulfillment details."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsCountRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsCountRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_count_for_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_count_for_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_count_for_order_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_count_for_order_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def get_fulfillment_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to retrieve."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieve detailed information about a specific fulfillment for an order, including its status, line items, and tracking information."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_fulfillment_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_fulfillment_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_fulfillment_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_fulfillment_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def update_fulfillment_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to update."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to update."),
) -> dict[str, Any] | ToolResult:
    """Update fulfillment details for a specific order, such as tracking information or fulfillment status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequest(
            path=_models.DeprecatedUnstableUpdateOrdersParamOrderIdFulfillmentsParamFulfillmentIdRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_fulfillment_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_fulfillment_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_fulfillment_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_fulfillment_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def cancel_fulfillment_order_unstable_2(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to cancel."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to cancel within the specified order."),
) -> dict[str, Any] | ToolResult:
    """Cancel an active fulfillment for a specific order. This operation marks the fulfillment as cancelled and prevents further shipment processing."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCancelRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_fulfillment_order_unstable_2: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/cancel.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_fulfillment_order_unstable_2")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_fulfillment_order_unstable_2", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_fulfillment_order_unstable_2",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def complete_fulfillment_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be completed."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as complete."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as complete, indicating that all items in the fulfillment have been shipped or delivered to the customer."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdCompleteRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_fulfillment_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/complete.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_fulfillment_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_fulfillment_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_fulfillment_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillmentevent, shipping-and-fulfillment/fulfillmentevent
@mcp.tool()
async def create_fulfillment_event_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment for which to create the event."),
) -> dict[str, Any] | ToolResult:
    """Creates a fulfillment event for a specific fulfillment within an order. This records status updates and tracking information for the fulfillment."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdEventsRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_fulfillment_event_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/events.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/events.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_fulfillment_event_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_fulfillment_event_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_fulfillment_event_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shipping-and-fulfillment, fulfillment, shipping-and-fulfillment/fulfillment
@mcp.tool()
async def open_fulfillment_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the fulfillment to be opened."),
    fulfillment_id: str = Field(..., description="The unique identifier of the fulfillment to mark as open."),
) -> dict[str, Any] | ToolResult:
    """Mark a fulfillment as open, allowing it to receive additional items or changes. This operation transitions a fulfillment from a closed or pending state to an open state."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdFulfillmentsParamFulfillmentIdOpenRequestPath(order_id=order_id, fulfillment_id=fulfillment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for open_fulfillment_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/open.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/fulfillments/{fulfillment_id}/open.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("open_fulfillment_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("open_fulfillment_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="open_fulfillment_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def list_order_refunds_unstable(
    order_id: str = Field(..., description="The unique identifier of the order for which to retrieve refunds."),
    limit: Any | None = Field(None, description="Maximum number of refunds to return per request, between 1 and 250 (defaults to 50)."),
    in_shop_currency: Any | None = Field(None, description="When true, displays monetary amounts in the shop's currency for the underlying transaction; defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of refunds for a specific order. Results are paginated using link headers in the response; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdRefundsRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.DeprecatedUnstableGetOrdersParamOrderIdRefundsRequestQuery(limit=limit, in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_refunds_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_refunds_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_refunds_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_refunds_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def create_order_refund_unstable(
    order_id: str = Field(..., description="The unique identifier of the order for which to create a refund."),
    notify: Any | None = Field(None, description="Whether to send a refund notification email to the customer."),
    note: Any | None = Field(None, description="An optional note to attach to the refund for internal reference."),
    discrepancy_reason: Any | None = Field(None, description="An optional reason explaining any discrepancy between calculated and actual refund amounts. Valid values are: restock, damage, customer, or other."),
    shipping: Any | None = Field(None, description="Shipping refund details. Specify either full_refund to refund all remaining shipping, or amount to refund a specific shipping amount (takes precedence over full_refund)."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each with the line item ID, quantity to refund, restock type (no_restock, cancel, or return), and location ID (required for cancel or return restock types)."),
    transactions: Any | None = Field(None, description="A list of transactions to process as refunds. Should be obtained from the calculate endpoint for accuracy."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required for multi-currency orders when an amount property is provided."),
) -> dict[str, Any] | ToolResult:
    """Creates a refund for an order. Use the calculate endpoint first to determine the correct transactions to submit. For multi-currency orders, the currency property is required whenever an amount is specified."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsRequestPath(order_id=order_id),
            query=_models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsRequestQuery(notify=notify, note=note, discrepancy_reason=discrepancy_reason, shipping=shipping, refund_line_items=refund_line_items, transactions=transactions, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_refund_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/refunds.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/refunds.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_refund_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_refund_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_refund_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def calculate_refund_for_order(
    order_id: str = Field(..., description="The unique identifier of the order for which to calculate the refund."),
    shipping: Any | None = Field(None, description="Specifies how much shipping to refund. Provide either a full refund flag or a specific amount; if both are provided, the amount takes precedence."),
    refund_line_items: Any | None = Field(None, description="A list of line items to refund, each specifying the line item ID, quantity to refund, how it affects inventory (no_restock, cancel, or return), and optionally the location where items should be restocked. If no location is provided for return or cancel operations, the endpoint will suggest one."),
    currency: Any | None = Field(None, description="The three-letter ISO 4217 currency code for the refund. Required whenever a shipping amount is specified; essential for multi-currency orders."),
) -> dict[str, Any] | ToolResult:
    """Calculates refund transactions for an order based on specified line items, quantities, restock instructions, and shipping costs. Use this endpoint to generate accurate refund details before creating an actual refund; the response includes suggested transactions that must be converted to refund transactions in the creation request."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsCalculateRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsCalculateRequestPath(order_id=order_id),
            query=_models.DeprecatedUnstableCreateOrdersParamOrderIdRefundsCalculateRequestQuery(shipping=shipping, refund_line_items=refund_line_items, currency=currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for calculate_refund_for_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/refunds/calculate.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/refunds/calculate.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("calculate_refund_for_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("calculate_refund_for_order", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="calculate_refund_for_order",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, refund, orders/refund
@mcp.tool()
async def get_refund_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the refund."),
    refund_id: str = Field(..., description="The unique identifier of the refund to retrieve."),
    in_shop_currency: Any | None = Field(None, description="When enabled, displays all monetary amounts in the shop's native currency rather than the transaction currency. Defaults to false."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a specific refund for an order. Use this to view refund information including amounts, reason, and status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdRefundsParamRefundIdRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdRefundsParamRefundIdRequestPath(order_id=order_id, refund_id=refund_id),
            query=_models.DeprecatedUnstableGetOrdersParamOrderIdRefundsParamRefundIdRequestQuery(in_shop_currency=in_shop_currency)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_refund_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/refunds/{refund_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/refunds/{refund_id}.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_refund_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_refund_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_refund_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def list_order_risks_unstable(order_id: str = Field(..., description="The unique identifier of the order for which to retrieve risk assessments.")) -> dict[str, Any] | ToolResult:
    """Retrieves all fraud and risk assessments associated with a specific order. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdRisksRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_order_risks_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_order_risks_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_order_risks_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_order_risks_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def create_order_risk_unstable(order_id: str = Field(..., description="The unique identifier of the order for which to create the risk record. This must be a valid order ID from your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Creates a risk assessment record for a specific order. Use this to flag potential issues or concerns associated with an order that may require review or action."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateOrdersParamOrderIdRisksRequest(
            path=_models.DeprecatedUnstableCreateOrdersParamOrderIdRisksRequestPath(order_id=order_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_order_risk_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/risks.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/risks.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_order_risk_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_order_risk_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_order_risk_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def get_order_risk_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk assessment."),
    risk_id: str = Field(..., description="The unique identifier of the specific risk assessment to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single risk assessment for an order. Use this to fetch detailed information about a specific fraud or security risk flagged on an order."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.DeprecatedUnstableGetOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_order_risk_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_order_risk_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_order_risk_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_order_risk_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def update_order_risk_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be updated."),
    risk_id: str = Field(..., description="The unique identifier of the order risk to be updated."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing order risk. Note that you cannot modify an order risk that was created by another application."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.DeprecatedUnstableUpdateOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_order_risk_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_order_risk_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_order_risk_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_order_risk_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: orders, order-risk, orders/order-risk
@mcp.tool()
async def delete_order_risk_unstable(
    order_id: str = Field(..., description="The unique identifier of the order containing the risk to be deleted."),
    risk_id: str = Field(..., description="The unique identifier of the risk assessment to be deleted from the order."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific risk assessment from an order. Note that you can only delete risks created by your application; risks created by other applications cannot be deleted."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteOrdersParamOrderIdRisksParamRiskIdRequest(
            path=_models.DeprecatedUnstableDeleteOrdersParamOrderIdRisksParamRiskIdRequestPath(order_id=order_id, risk_id=risk_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_order_risk_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/orders/{order_id}/risks/{risk_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_order_risk_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_order_risk_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_order_risk_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, policy, store-properties/policy
@mcp.tool()
async def list_policies_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all policies configured for the shop, including return, privacy, shipping, and other store policies."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/policies.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_policies_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_policies_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_policies_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def create_discount_codes_batch_unstable(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which discount codes will be created.")) -> dict[str, Any] | ToolResult:
    """Asynchronously creates up to 100 discount codes for a price rule in a single batch job. Returns a discount code creation job object that can be monitored for completion status, including counts of successful and failed code creations."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreatePriceRulesParamPriceRuleIdBatchRequest(
            path=_models.DeprecatedUnstableCreatePriceRulesParamPriceRuleIdBatchRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_discount_codes_batch_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/batch.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/batch.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_discount_codes_batch_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_discount_codes_batch_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_discount_codes_batch_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_batch_unstable(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch job."),
    batch_id: str = Field(..., description="The unique identifier of the batch job to retrieve status and results for."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the status and details of a discount code creation job batch for a specific price rule."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdBatchParamBatchIdRequest(
            path=_models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdBatchParamBatchIdRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_batch_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/batch/{batch_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/batch/{batch_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_batch_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_batch_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_batch_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_batch_unstable(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule associated with the discount code batch job."),
    batch_id: str = Field(..., description="The unique identifier of the batch job that generated the discount codes."),
) -> dict[str, Any] | ToolResult:
    """Retrieves all discount codes generated from a batch creation job for a specific price rule. Results include successfully created codes with populated IDs and codes that failed with error details."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequest(
            path=_models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdBatchParamBatchIdDiscountCodesRequestPath(price_rule_id=price_rule_id, batch_id=batch_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_batch_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/batch/{batch_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_batch_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_batch_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_batch_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def list_discount_codes_for_price_rule_unstable(price_rule_id: str = Field(..., description="The unique identifier of the price rule for which to retrieve associated discount codes.")) -> dict[str, Any] | ToolResult:
    """Retrieve all discount codes associated with a specific price rule. Results are paginated using link headers in the response; use the provided links to navigate pages rather than query parameters."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdDiscountCodesRequest(
            path=_models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdDiscountCodesRequestPath(price_rule_id=price_rule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_discount_codes_for_price_rule_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/discount_codes.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/discount_codes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_discount_codes_for_price_rule_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_discount_codes_for_price_rule_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_discount_codes_for_price_rule_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def get_discount_code_unstable(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single discount code associated with a specific price rule. Use this to fetch details about a discount code by its ID."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.DeprecatedUnstableGetPriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_discount_code_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_discount_code_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_discount_code_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_discount_code_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def update_discount_code_for_price_rule(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code being updated."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to update."),
) -> dict[str, Any] | ToolResult:
    """Updates an existing discount code associated with a price rule. Modify discount code properties such as code value, usage limits, or other configurations."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.DeprecatedUnstableUpdatePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_discount_code_for_price_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_discount_code_for_price_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_discount_code_for_price_rule", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_discount_code_for_price_rule",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: discounts, discountcode, discounts/discountcode
@mcp.tool()
async def delete_discount_code_for_price_rule(
    price_rule_id: str = Field(..., description="The unique identifier of the price rule that contains the discount code to be deleted."),
    discount_code_id: str = Field(..., description="The unique identifier of the discount code to be deleted from the price rule."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific discount code associated with a price rule. This operation permanently deletes the discount code and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequest(
            path=_models.DeprecatedUnstableDeletePriceRulesParamPriceRuleIdDiscountCodesParamDiscountCodeIdRequestPath(price_rule_id=price_rule_id, discount_code_id=discount_code_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_discount_code_for_price_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/price_rules/{price_rule_id}/discount_codes/{discount_code_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_discount_code_for_price_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_discount_code_for_price_rule", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_discount_code_for_price_rule",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def get_product_unstable(product_id: str = Field(..., description="The unique identifier of the product to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves detailed information for a single product by its ID from the Shopify store."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetProductsParamProductIdRequest(
            path=_models.DeprecatedUnstableGetProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def update_product_unstable(product_id: str = Field(..., description="The unique identifier of the product to update.")) -> dict[str, Any] | ToolResult:
    """Updates a product's details, variants, images, and SEO metadata. Use metafields_global_title_tag and metafields_global_description_tag to manage SEO title and description tags."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateProductsParamProductIdRequest(
            path=_models.DeprecatedUnstableUpdateProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product, products/product
@mcp.tool()
async def delete_product_unstable(product_id: str = Field(..., description="The unique identifier of the product to delete. This is a required string value that specifies which product should be removed.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a product from the store. This action cannot be undone and will remove the product and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteProductsParamProductIdRequest(
            path=_models.DeprecatedUnstableDeleteProductsParamProductIdRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def list_product_images_unstable(product_id: str = Field(..., description="The unique identifier of the product whose images you want to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieve all images associated with a specific product. Returns a collection of product image objects for the given product ID."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetProductsParamProductIdImagesRequest(
            path=_models.DeprecatedUnstableGetProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_images_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_images_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_images_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_images_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def create_product_image_unstable(product_id: str = Field(..., description="The unique identifier of the product to which the image will be added.")) -> dict[str, Any] | ToolResult:
    """Create a new image for a product. The image will be associated with the specified product and can be used to display the product in your storefront."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateProductsParamProductIdImagesRequest(
            path=_models.DeprecatedUnstableCreateProductsParamProductIdImagesRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_image_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}/images.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}/images.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_image_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_image_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_image_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def get_product_images_count_unstable(product_id: str = Field(..., description="The unique identifier of the product for which to retrieve the image count.")) -> dict[str, Any] | ToolResult:
    """Retrieve the total count of images associated with a specific product. Useful for understanding product image inventory without fetching the full image list."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetProductsParamProductIdImagesCountRequest(
            path=_models.DeprecatedUnstableGetProductsParamProductIdImagesCountRequestPath(product_id=product_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_images_count_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}/images/count.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}/images/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_images_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_images_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_images_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, product-image, products/product-image
@mcp.tool()
async def delete_product_image_unstable(
    product_id: str = Field(..., description="The unique identifier of the product containing the image to delete."),
    image_id: str = Field(..., description="The unique identifier of the image to delete from the product."),
) -> dict[str, Any] | ToolResult:
    """Delete a specific image from a product. This operation removes the image association from the product permanently."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteProductsParamProductIdImagesParamImageIdRequest(
            path=_models.DeprecatedUnstableDeleteProductsParamProductIdImagesParamImageIdRequestPath(product_id=product_id, image_id=image_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_image_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/products/{product_id}/images/{image_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/products/{product_id}/images/{image_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_image_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_image_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_image_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def list_recurring_application_charges_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all recurring application charges for your app. Use this to view subscription charges that have been set up with the merchant."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/recurring_application_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recurring_application_charges_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recurring_application_charges_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recurring_application_charges_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def delete_recurring_application_charge_unstable(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to cancel. This ID is returned when the charge is created.")) -> dict[str, Any] | ToolResult:
    """Cancels an active recurring application charge for the app. This operation permanently removes the recurring billing arrangement between the app and the store."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequest(
            path=_models.DeprecatedUnstableDeleteRecurringApplicationChargesParamRecurringApplicationChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_recurring_application_charge_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_recurring_application_charge_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_recurring_application_charge_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_recurring_application_charge_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, recurringapplicationcharge, billing/recurringapplicationcharge
@mcp.tool()
async def update_recurring_application_charge_capped_amount_unstable(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge to customize."),
    recurring_application_charge_capped_amount: int | None = Field(None, alias="recurring_application_chargecapped_amount", description="The new capped amount in cents. This sets the maximum charge amount for the billing cycle."),
) -> dict[str, Any] | ToolResult:
    """Updates the capped amount for an active recurring application charge. This allows you to modify the maximum amount that will be charged to the merchant during the billing cycle."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequest(
            path=_models.DeprecatedUnstableUpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestPath(recurring_application_charge_id=recurring_application_charge_id),
            query=_models.DeprecatedUnstableUpdateRecurringApplicationChargesParamRecurringApplicationChargeIdCustomizeRequestQuery(recurring_application_charge_capped_amount=recurring_application_charge_capped_amount)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_recurring_application_charge_capped_amount_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/customize.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/customize.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_recurring_application_charge_capped_amount_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_recurring_application_charge_capped_amount_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_recurring_application_charge_capped_amount_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def list_usage_charges_for_recurring_application_charge_unstable(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to retrieve usage charges.")) -> dict[str, Any] | ToolResult:
    """Retrieves all usage charges associated with a specific recurring application charge. Usage charges represent variable billing amounts applied to a recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.DeprecatedUnstableGetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_usage_charges_for_recurring_application_charge_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_usage_charges_for_recurring_application_charge_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_usage_charges_for_recurring_application_charge_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_usage_charges_for_recurring_application_charge_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def create_usage_charge_for_recurring_application_charge_unstable(recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge for which to create the usage charge.")) -> dict[str, Any] | ToolResult:
    """Creates a usage charge against an existing recurring application charge. Usage charges allow you to bill customers for variable usage on top of their recurring subscription."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableCreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequest(
            path=_models.DeprecatedUnstableCreateRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesRequestPath(recurring_application_charge_id=recurring_application_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_usage_charge_for_recurring_application_charge_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_usage_charge_for_recurring_application_charge_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_usage_charge_for_recurring_application_charge_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_usage_charge_for_recurring_application_charge_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: billing, usagecharge, billing/usagecharge
@mcp.tool()
async def get_usage_charge_for_recurring_application_charge_unstable(
    recurring_application_charge_id: str = Field(..., description="The unique identifier of the recurring application charge that contains the usage charge you want to retrieve."),
    usage_charge_id: str = Field(..., description="The unique identifier of the specific usage charge to retrieve."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a single usage charge associated with a recurring application charge. Use this to fetch details about a specific metered billing charge."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequest(
            path=_models.DeprecatedUnstableGetRecurringApplicationChargesParamRecurringApplicationChargeIdUsageChargesParamUsageChargeIdRequestPath(recurring_application_charge_id=recurring_application_charge_id, usage_charge_id=usage_charge_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_usage_charge_for_recurring_application_charge_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/recurring_application_charges/{recurring_application_charge_id}/usage_charges/{usage_charge_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_usage_charge_for_recurring_application_charge_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_usage_charge_for_recurring_application_charge_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_usage_charge_for_recurring_application_charge_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def list_redirects_unstable(
    limit: Any | None = Field(None, description="Maximum number of redirects to return per request, between 1 and 250 (defaults to 50)."),
    path: Any | None = Field(None, description="Filter results to show only redirects with a specific source path."),
    target: Any | None = Field(None, description="Filter results to show only redirects pointing to a specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of URL redirects configured in the online store. Results are paginated using link headers in the response rather than page parameters."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetRedirectsRequest(
            query=_models.DeprecatedUnstableGetRedirectsRequestQuery(limit=limit, path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_redirects_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/redirects.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_redirects_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_redirects_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_redirects_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def count_redirects(
    path: Any | None = Field(None, description="Filter the count to only include redirects with this specific path value."),
    target: Any | None = Field(None, description="Filter the count to only include redirects with this specific target URL."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of URL redirects in the store, with optional filtering by path or target URL."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetRedirectsCountRequest(
            query=_models.DeprecatedUnstableGetRedirectsCountRequestQuery(path=path, target=target)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_redirects: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/redirects/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_redirects")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_redirects", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_redirects",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def get_redirect_unstable(redirect_id: str = Field(..., description="The unique identifier of the redirect to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single redirect by its ID from the Shopify online store. Use this to fetch details about a specific URL redirect configuration."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetRedirectsParamRedirectIdRequest(
            path=_models.DeprecatedUnstableGetRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_redirect_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_redirect_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_redirect_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_redirect_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def update_redirect_unstable(redirect_id: str = Field(..., description="The unique identifier of the redirect to update. This ID is required to locate and modify the specific redirect rule.")) -> dict[str, Any] | ToolResult:
    """Updates an existing redirect configuration in the online store. Modify redirect rules by providing the redirect ID and updated redirect details."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateRedirectsParamRedirectIdRequest(
            path=_models.DeprecatedUnstableUpdateRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_redirect_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_redirect_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_redirect_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_redirect_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, redirect, online-store/redirect
@mcp.tool()
async def delete_redirect_unstable(redirect_id: str = Field(..., description="The unique identifier of the redirect to delete. This is a required string value that specifies which redirect rule to remove.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a redirect from the online store. This operation removes the redirect rule and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteRedirectsParamRedirectIdRequest(
            path=_models.DeprecatedUnstableDeleteRedirectsParamRedirectIdRequestPath(redirect_id=redirect_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_redirect_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/redirects/{redirect_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/redirects/{redirect_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_redirect_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_redirect_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_redirect_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def list_reports_unstable(
    ids: Any | None = Field(None, description="Filter results by one or more report IDs using a comma-separated list."),
    limit: Any | None = Field(None, description="Maximum number of reports to return per request, ranging from 1 to 250 (defaults to 50)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of reports from the Shopify Admin API. This endpoint uses cursor-based pagination via response headers; the legacy page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetReportsRequest(
            query=_models.DeprecatedUnstableGetReportsRequestQuery(ids=ids, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_reports_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/reports.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_reports_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_reports_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_reports_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: analytics, report, analytics/report
@mcp.tool()
async def delete_report_unstable(report_id: str = Field(..., description="The unique identifier of the report to delete. This ID must correspond to an existing report in your Shopify store.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a report from the Shopify Admin. This operation removes the report and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteReportsParamReportIdRequest(
            path=_models.DeprecatedUnstableDeleteReportsParamReportIdRequestPath(report_id=report_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_report_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/reports/{report_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/reports/{report_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_report_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_report_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_report_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def list_script_tags_unstable(limit: Any | None = Field(None, description="Maximum number of script tags to return per request, between 1 and 250 (defaults to 50).")) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all script tags from the online store. Uses cursor-based pagination via response headers; the page parameter is not supported."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetScriptTagsRequest(
            query=_models.DeprecatedUnstableGetScriptTagsRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_script_tags_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/script_tags.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_script_tags_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_script_tags_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_script_tags_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, scripttag, online-store/scripttag
@mcp.tool()
async def get_script_tags_count_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the total count of all script tags in the store. Useful for understanding the volume of custom scripts deployed without fetching individual script tag details."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/script_tags/count.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_script_tags_count_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_script_tags_count_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_script_tags_count_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shippingzone, store-properties/shippingzone
@mcp.tool()
async def list_shipping_zones_unstable() -> dict[str, Any] | ToolResult:
    """Retrieve all shipping zones configured in the store. Shipping zones define geographic areas and their associated shipping rates and methods."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/shipping_zones.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shipping_zones_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shipping_zones_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shipping_zones_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: store-properties, shop, store-properties/shop
@mcp.tool()
async def get_shop_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the current shop's configuration and metadata, including store name, currency, timezone, and other essential shop-level settings."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/shop.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shop_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shop_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shop_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, balance, shopify_payments/balance
@mcp.tool()
async def get_shopify_payments_balance_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the current account balance for Shopify Payments, showing available funds and any pending amounts."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/shopify_payments/balance.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_balance_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_balance_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_balance_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def list_shopify_payments_payouts_unstable(
    date_min: Any | None = Field(None, description="Filter payouts to include only those made on or after this date (inclusive). Specify as an ISO 8601 formatted date."),
    date_max: Any | None = Field(None, description="Filter payouts to include only those made on or before this date (inclusive). Specify as an ISO 8601 formatted date."),
    status: Any | None = Field(None, description="Filter payouts by their current status (e.g., pending, completed, failed, cancelled)."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a list of all payouts from Shopify Payments, ordered by payout date with the most recent first. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetShopifyPaymentsPayoutsRequest(
            query=_models.DeprecatedUnstableGetShopifyPaymentsPayoutsRequestQuery(date_min=date_min, date_max=date_max, status=status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_shopify_payments_payouts_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/shopify_payments/payouts.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_shopify_payments_payouts_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_shopify_payments_payouts_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_shopify_payments_payouts_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: shopify_payments, payout, shopify_payments/payout
@mcp.tool()
async def get_shopify_payments_payout(payout_id: str = Field(..., description="The unique identifier of the payout to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves details for a specific Shopify Payments payout by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetShopifyPaymentsPayoutsParamPayoutIdRequest(
            path=_models.DeprecatedUnstableGetShopifyPaymentsPayoutsParamPayoutIdRequestPath(payout_id=payout_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_shopify_payments_payout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/shopify_payments/payouts/{payout_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/shopify_payments/payouts/{payout_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_shopify_payments_payout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_shopify_payments_payout", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_shopify_payments_payout",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def list_smart_collections_unstable(
    limit: Any | None = Field(None, description="Maximum number of smart collections to return per request. Defaults to 50 and cannot exceed 250."),
    ids: Any | None = Field(None, description="Filter results to only smart collections with the specified IDs. Provide as a comma-separated list of numeric IDs."),
    title: Any | None = Field(None, description="Filter results to smart collections matching the exact title."),
    product_id: Any | None = Field(None, description="Filter results to only smart collections that contain the specified product ID."),
    handle: Any | None = Field(None, description="Filter results by smart collection handle (the URL-friendly identifier)."),
    published_status: Any | None = Field(None, description="Filter results by publication status: 'published' for published collections only, 'unpublished' for unpublished collections only, or 'any' for all collections regardless of status. Defaults to 'any'."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of smart collections from the store. Results are paginated using link headers in the response."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetSmartCollectionsRequest(
            query=_models.DeprecatedUnstableGetSmartCollectionsRequestQuery(limit=limit, ids=ids, title=title, product_id=product_id, handle=handle, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_smart_collections_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/smart_collections.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_smart_collections_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_smart_collections_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_smart_collections_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def create_smart_collection_unstable() -> dict[str, Any] | ToolResult:
    """Creates a new smart collection that automatically groups products based on specified rules and conditions. Smart collections dynamically update their membership as products are added, modified, or removed from your store."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/smart_collections.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_smart_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_smart_collection_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_smart_collection_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def count_smart_collections_unstable(
    title: Any | None = Field(None, description="Filter to smart collections with an exact matching title."),
    product_id: Any | None = Field(None, description="Filter to smart collections that contain the specified product ID."),
    published_status: Any | None = Field(None, description="Filter results by publication status: published (live collections only), unpublished (draft collections only), or any (all collections). Defaults to any."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of smart collections, with optional filtering by title, product inclusion, or published status."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetSmartCollectionsCountRequest(
            query=_models.DeprecatedUnstableGetSmartCollectionsCountRequestQuery(title=title, product_id=product_id, published_status=published_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_smart_collections_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/smart_collections/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_smart_collections_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_smart_collections_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_smart_collections_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def get_smart_collection_unstable(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single smart collection by its ID. Use this to fetch configuration, rules, and metadata for a specific smart collection."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.DeprecatedUnstableGetSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_smart_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_smart_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_smart_collection_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_smart_collection_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def update_smart_collection_unstable(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update.")) -> dict[str, Any] | ToolResult:
    """Updates the configuration and settings of an existing smart collection, allowing you to modify its rules, name, and other properties."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.DeprecatedUnstableUpdateSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def delete_smart_collection_unstable(smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently removes a smart collection from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteSmartCollectionsParamSmartCollectionIdRequest(
            path=_models.DeprecatedUnstableDeleteSmartCollectionsParamSmartCollectionIdRequestPath(smart_collection_id=smart_collection_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_smart_collection_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/smart_collections/{smart_collection_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/smart_collections/{smart_collection_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_smart_collection_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_smart_collection_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_smart_collection_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: products, smartcollection, products/smartcollection
@mcp.tool()
async def update_smart_collection_order_unstable(
    smart_collection_id: str = Field(..., description="The unique identifier of the smart collection to update."),
    products: Any | None = Field(None, description="An ordered array of product IDs to pin at the top of the collection. When provided as an empty array, any previously pinned products are cleared and the collection reverts to its sort_order configuration."),
    sort_order: Any | None = Field(None, description="The sorting method to apply to the collection. Valid options include alphabetical, price-based, newest, and best-selling. If not specified, the current sort order is preserved."),
) -> dict[str, Any] | ToolResult:
    """Updates the sorting configuration for products in a smart collection, allowing you to manually order specific products at the top or apply a predefined sort type across the entire collection."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableUpdateSmartCollectionsParamSmartCollectionIdOrderRequest(
            path=_models.DeprecatedUnstableUpdateSmartCollectionsParamSmartCollectionIdOrderRequestPath(smart_collection_id=smart_collection_id),
            query=_models.DeprecatedUnstableUpdateSmartCollectionsParamSmartCollectionIdOrderRequestQuery(products=products, sort_order=sort_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_smart_collection_order_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/smart_collections/{smart_collection_id}/order.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/smart_collections/{smart_collection_id}/order.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_smart_collection_order_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_smart_collection_order_unstable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_smart_collection_order_unstable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: access, storefrontaccesstoken, access/storefrontaccesstoken
@mcp.tool()
async def delete_storefront_access_token_unstable(storefront_access_token_id: str = Field(..., description="The unique identifier of the storefront access token to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a storefront access token, revoking its access to the Storefront API. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteStorefrontAccessTokensParamStorefrontAccessTokenIdRequest(
            path=_models.DeprecatedUnstableDeleteStorefrontAccessTokensParamStorefrontAccessTokenIdRequestPath(storefront_access_token_id=storefront_access_token_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_storefront_access_token_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/storefront_access_tokens/{storefront_access_token_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/storefront_access_tokens/{storefront_access_token_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_storefront_access_token_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_storefront_access_token_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_storefront_access_token_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tendertransaction
@mcp.tool()
async def list_tender_transactions_unstable(
    limit: Any | None = Field(None, description="Maximum number of results to return per request, between 1 and 250 (defaults to 50)."),
    processed_at_min: Any | None = Field(None, description="Filter to show only tender transactions processed on or after this date (ISO 8601 format)."),
    processed_at_max: Any | None = Field(None, description="Filter to show only tender transactions processed on or before this date (ISO 8601 format)."),
    order: Any | None = Field(None, description="Sort results by processed_at timestamp in either ascending or descending order."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of tender transactions processed within a specified date range. Results are paginated using link headers provided in the response."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetTenderTransactionsRequest(
            query=_models.DeprecatedUnstableGetTenderTransactionsRequestQuery(limit=limit, processed_at_min=processed_at_min, processed_at_max=processed_at_max, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tender_transactions_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/tender_transactions.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tender_transactions_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tender_transactions_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tender_transactions_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def list_themes_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves a list of all themes available in the Shopify store. This endpoint provides access to theme metadata for managing online store themes."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_themes_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_themes_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_themes_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def create_theme_unstable() -> dict[str, Any] | ToolResult:
    """Creates a new unpublished theme from a ZIP file hosted at a public URL. Optionally publish the theme immediately by including role: main in the request; published themes may take a couple of minutes to fully extract and store."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/themes.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_theme_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_theme_unstable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_theme_unstable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def get_theme_unstable(theme_id: str = Field(..., description="The unique identifier of the theme to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single theme by its ID from the Shopify online store. Use this to fetch detailed information about a specific theme."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetThemesParamThemeIdRequest(
            path=_models.DeprecatedUnstableGetThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_theme_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_theme_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_theme_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_theme_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, theme, online-store/theme
@mcp.tool()
async def delete_theme_unstable(theme_id: str = Field(..., description="The unique identifier of the theme to delete.")) -> dict[str, Any] | ToolResult:
    """Permanently deletes a theme from the store. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteThemesParamThemeIdRequest(
            path=_models.DeprecatedUnstableDeleteThemesParamThemeIdRequestPath(theme_id=theme_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/themes/{theme_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/themes/{theme_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: online-store, asset, online-store/asset
@mcp.tool()
async def delete_theme_asset_unstable(
    theme_id: str = Field(..., description="The unique identifier of the theme from which the asset will be deleted."),
    asset_key: str | None = Field(None, alias="assetkey", description="The key (file path) of the asset to delete from the theme. This identifies which specific asset file to remove."),
) -> dict[str, Any] | ToolResult:
    """Removes a specific asset file from a Shopify theme. The asset is identified by its key within the theme."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteThemesParamThemeIdAssetsRequest(
            path=_models.DeprecatedUnstableDeleteThemesParamThemeIdAssetsRequestPath(theme_id=theme_id),
            query=_models.DeprecatedUnstableDeleteThemesParamThemeIdAssetsRequestQuery(asset_key=asset_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_theme_asset_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/themes/{theme_id}/assets.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/themes/{theme_id}/assets.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_theme_asset_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_theme_asset_unstable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_theme_asset_unstable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, user, plus/user
@mcp.tool()
async def list_users_unstable(
    limit: Any | None = Field(None, description="Maximum number of users to return per page, between 1 and 250 results (defaults to 50)."),
    page_info: Any | None = Field(None, description="Cursor token for accessing a specific page of results, obtained from the Link header of the previous response."),
) -> dict[str, Any] | ToolResult:
    """Retrieves a paginated list of all users in the system. This endpoint uses cursor-based pagination via link headers rather than page numbers."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetUsersRequest(
            query=_models.DeprecatedUnstableGetUsersRequestQuery(limit=limit, page_info=page_info)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_users_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/users.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_users_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_users_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_users_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, user, plus/user
@mcp.tool()
async def get_current_user_unstable() -> dict[str, Any] | ToolResult:
    """Retrieves the user account information associated with the access token used in this request. This operation only works when the access token was created for a specific user account rather than an app."""

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/users/current.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_current_user_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_current_user_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_current_user_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: plus, user, plus/user
@mcp.tool()
async def get_user_unstable(user_id: str = Field(..., description="The unique identifier of the user to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves a single user by their ID from the Shopify admin system. This endpoint is part of the unstable API and may change."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetUsersParamUserIdRequest(
            path=_models.DeprecatedUnstableGetUsersParamUserIdRequestPath(user_id=user_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_user_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/users/{user_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/users/{user_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_user_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_user_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_user_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def count_webhooks(
    address: Any | None = Field(None, description="Filter results to webhook subscriptions that deliver POST requests to this specific URI."),
    topic: Any | None = Field(None, description="Filter results to webhook subscriptions with a specific topic. Refer to the webhook topic property documentation for valid topic values."),
) -> dict[str, Any] | ToolResult:
    """Retrieves the total count of webhook subscriptions configured in the store, with optional filtering by delivery address or topic."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetWebhooksCountRequest(
            query=_models.DeprecatedUnstableGetWebhooksCountRequestQuery(address=address, topic=topic)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for count_webhooks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/admin/api/unstable/webhooks/count.json"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("count_webhooks")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("count_webhooks", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="count_webhooks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def get_webhook_unstable(webhook_id: str = Field(..., description="The unique identifier of the webhook subscription to retrieve.")) -> dict[str, Any] | ToolResult:
    """Retrieves the details of a single webhook subscription by its ID, including its configuration, topics, and delivery settings."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableGetWebhooksParamWebhookIdRequest(
            path=_models.DeprecatedUnstableGetWebhooksParamWebhookIdRequestPath(webhook_id=webhook_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_webhook_unstable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/webhooks/{webhook_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/webhooks/{webhook_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_webhook_unstable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_webhook_unstable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_webhook_unstable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events, webhook, events/webhook
@mcp.tool()
async def delete_webhook(webhook_id: str = Field(..., description="The unique identifier of the webhook subscription to delete.")) -> dict[str, Any] | ToolResult:
    """Delete a webhook subscription by its ID. This permanently removes the webhook and stops it from receiving events."""

    # Construct request model with validation
    try:
        _request = _models.DeprecatedUnstableDeleteWebhooksParamWebhookIdRequest(
            path=_models.DeprecatedUnstableDeleteWebhooksParamWebhookIdRequestPath(webhook_id=webhook_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_webhook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/admin/api/unstable/webhooks/{webhook_id}.json", _request.path.model_dump(by_alias=True)) if _request.path else "/admin/api/unstable/webhooks/{webhook_id}.json"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_webhook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_webhook", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_webhook",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# ============================================================================
# Schema Optimization
# ============================================================================

def _optimize_tool_schemas() -> None:
    """Simplify anyOf nullable wrappers in tool schemas for LLM token efficiency.

    Pydantic generates {"anyOf": [{"type": "T"}, {"type": "null"}], "default": null}
    for `T | None = None` params. Both Anthropic and OpenAI recommend the simpler
    {"type": "T"} with the param outside `required` for optimal tool-calling accuracy
    and minimal token overhead. Runtime validation is unaffected — Python signatures
    retain the `T | None` annotation.
    """
    from fastmcp.tools import Tool as _ToolCls
    # v3: _local_provider._components, v2: _tool_manager._tools
    _src = getattr(mcp, "_local_provider", None)
    if _src is not None:
        _tools = [v for v in _src._components.values() if isinstance(v, _ToolCls)]
    else:
        _tools = list(mcp._tool_manager._tools.values())  # type: ignore[attr-defined]
    for _tool in _tools:
        _simplify_schema(_tool.parameters)

def _simplify_schema(schema: dict) -> None:
    """Walk a JSON Schema dict and collapse anyOf-nullable patterns in place.

    Also removes discriminator objects anywhere in the tree: after FastMCP's
    DereferenceRefsMiddleware inlines $ref entries, discriminator.mapping values
    point to $defs that no longer exist. The discriminator is redundant — each
    variant already identifies itself via Literal constants on the discriminant
    field, so MCP clients and LLMs don't need the mapping.
    """
    schema.pop("discriminator", None)
    for _def_schema in schema.get("$defs", {}).values():
        _simplify_schema(_def_schema)
    _props = schema.get("properties", {})
    _required = set(schema.get("required", []))
    for _name, _prop in _props.items():
        if "anyOf" in _prop:
            _non_null = [b for b in _prop["anyOf"] if b.get("type") != "null"]
            if len(_non_null) == 1:
                _desc = _prop.get("description")
                _prop.clear()
                _prop.update(_non_null[0])
                if _desc:
                    _prop["description"] = _desc
                _required.discard(_name)
        _simplify_schema(_prop)
    if _required:
        schema["required"] = sorted(_required)
    elif "required" in schema:
        del schema["required"]
    for _key in ("anyOf", "oneOf"):
        for _branch in schema.get(_key, []):
            if isinstance(_branch, dict):
                _simplify_schema(_branch)

_optimize_tool_schemas()

# ============================================================================
# Environment Validation
# ============================================================================

def validate_environment() -> None:
    """Validate required environment variables at startup."""
    errors: list[str] = []
    warnings: list[str] = []

    required_vars: list[tuple[str, str, str | None]] = [
    ]

    for var_name, description, default in required_vars:
        value = os.getenv(var_name, default)
        if not value:
            errors.append(f"  - {var_name}: {description}")

    if errors:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Missing required environment variables", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        for error in errors:
            print(error, file=sys.stderr)
        print("\nServer startup aborted. Set required variables and restart.", file=sys.stderr)
        print("\nExample:", file=sys.stderr)
        print("  python shopify_admin_server.py", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    _validate_port()
    _validate_timeout()
    _validate_rate_limit()

    logger = logging.getLogger(__name__)
    logger.info("Environment validation passed")
    for warning in warnings:
        logger.warning(warning)

def _validate_port() -> None:
    port_str = os.getenv('PORT')
    if not port_str:
        return  # Optional, has CLI default

    port: int
    try:
        port = int(port_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid PORT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port_str}", file=sys.stderr)
        print("\nPORT must be an integer between 1 and 65535", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if port < 1 or port > 65535:
        print("=" * 70, file=sys.stderr)
        print("ERROR: PORT out of valid range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port}", file=sys.stderr)
        print("\nPORT must be between 1 and 65535", file=sys.stderr)
        print("  Common ports: 8000, 8080, 3000", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_timeout() -> None:
    timeout_str = os.getenv('API_TIMEOUT')
    if not timeout_str:
        return  # Optional, has default

    timeout: int
    try:
        timeout = int(timeout_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid API_TIMEOUT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout_str}", file=sys.stderr)
        print("\nAPI_TIMEOUT must be a positive integer (seconds)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if timeout < 1 or timeout > 3600:
        print("=" * 70, file=sys.stderr)
        print("ERROR: API_TIMEOUT out of reasonable range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout} seconds", file=sys.stderr)
        print("\nAPI_TIMEOUT must be between 1 and 3600 seconds (1 hour max)", file=sys.stderr)
        print("  Recommended: 30 (most APIs), 60 (slower APIs), 300 (long operations)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_rate_limit() -> None:
    rate_str = os.getenv('RATE_LIMIT')
    if not rate_str:
        return  # Optional, has default

    try:
        rate = float(rate_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid RATE_LIMIT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate_str}", file=sys.stderr)
        print("\nRATE_LIMIT must be a positive number (requests per second)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if rate <= 0:
        print("=" * 70, file=sys.stderr)
        print("ERROR: RATE_LIMIT must be positive", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate}", file=sys.stderr)
        print("\nRATE_LIMIT must be greater than 0", file=sys.stderr)
        print("  Recommended: 1-100 requests/second depending on API limits", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

# ============================================================================
# Main Entry Point
# ============================================================================

def main():
    """CLI entry point with runtime transport selection."""

    validate_environment()

    parser = argparse.ArgumentParser(description="Shopify Admin MCP Server")

    parser.add_argument(
        '--transport',
        choices=['stdio', 'sse', 'streamable-http'],
        default='stdio',
        help='Transport protocol: stdio (default, for Claude Code), sse (remote capable), streamable-http (production)'
    )
    parser.add_argument(
        '--host',
        default='0.0.0.0',  # noqa: S104
        help='Host address for HTTP transports (default: 0.0.0.0)'
    )
    parser.add_argument(
        '--port',
        type=int,
        default=8000,
        help='Port for HTTP transports (default: 8000)'
    )
    parser.add_argument(
        '--log-level',
        choices=['DEBUG', 'INFO', 'WARNING', 'ERROR'],
        default=LOG_LEVEL,
        help='Logging level (default: from LOG_LEVEL env var, or INFO)'
    )
    parser.add_argument(
        '--retry',
        action='store_true',
        help='Enable automatic retry with exponential backoff (default: enabled)'
    )
    parser.add_argument(
        '--no-retry',
        action='store_true',
        help='Disable automatic retry'
    )
    parser.add_argument(
        '--max-retries',
        type=int,
        default=MAX_RETRIES,
        help='Maximum retry attempts (default: from MAX_RETRIES env var, or 3)'
    )
    parser.add_argument(
        '--retry-base-delay',
        type=int,
        default=RETRY_BACKOFF_FACTOR,
        help='Base delay for exponential backoff in seconds (default: from RETRY_BACKOFF_FACTOR env var, or 2)'
    )
    parser.add_argument(
        '--retry-max-delay',
        type=int,
        default=60,
        help='Maximum retry delay in seconds (default: 60)'
    )
    parser.add_argument(
        '--timeout',
        type=int,
        default=None,
        help='API request timeout in seconds (default: 30, or API_TIMEOUT env var)'
    )
    parser.add_argument(
        '--rate-limiting',
        choices=['disabled', 'local'],
        default='local',
        help='Rate limiting mode: disabled (default) or local (in-memory token bucket)'
    )
    parser.add_argument(
        '--rate-limit',
        type=int,
        default=RATE_LIMIT_REQUESTS_PER_SECOND,
        help='Rate limit in requests per second (default: from RATE_LIMIT_REQUESTS_PER_SECOND env var, or 10)'
    )
    parser.add_argument(
        '--burst',
        type=int,
        help='Burst capacity for rate limiter (default: 2x rate-limit)'
    )
    parser.add_argument(
        '--circuit-breaker',
        action='store_true',
        help='Enable circuit breaker to prevent cascading failures'
    )
    parser.add_argument(
        '--circuit-breaker-failure-threshold',
        type=int,
        default=CIRCUIT_BREAKER_FAILURE_THRESHOLD,
        help='Failures before opening circuit (default: from CIRCUIT_BREAKER_FAILURE_THRESHOLD env var, or 5)'
    )
    parser.add_argument(
        '--circuit-breaker-timeout',
        type=float,
        default=CIRCUIT_BREAKER_TIMEOUT_SECONDS,
        help='Seconds before recovery attempt (default: from CIRCUIT_BREAKER_TIMEOUT_SECONDS env var, or 60.0)'
    )
    args = parser.parse_args()

    logging.basicConfig(
        level=getattr(logging, args.log_level),
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )

    logger = logging.getLogger(__name__)
    logger.info("Starting Shopify Admin MCP Server")
    logger.info(f"Transport: {args.transport}")

    global retry_config, rate_limiter, circuit_breaker, DEFAULT_TIMEOUT

    if args.timeout is not None:
        DEFAULT_TIMEOUT = args.timeout
    else:
        timeout_env = os.getenv('API_TIMEOUT')
        if timeout_env:
            try:
                DEFAULT_TIMEOUT = int(timeout_env)
            except ValueError:
                logger.warning(f"Invalid API_TIMEOUT '{timeout_env}', using default {DEFAULT_TIMEOUT}s")
    logger.info(f"Timeout: {DEFAULT_TIMEOUT}s")

    if not args.no_retry:
        retry_config = RetryConfig(
            max_attempts=args.max_retries,
            base_delay=args.retry_base_delay,
            max_delay=args.retry_max_delay
        )
        logger.info(
            f"Retry: Enabled (max attempts: {retry_config.max_attempts}, "
            f"base delay: {retry_config.base_delay}s, max delay: {retry_config.max_delay}s)"
        )
    else:
        logger.info("Retry: Disabled")

    rate_limiting_mode = args.rate_limiting

    if rate_limiting_mode == 'disabled':
        rate_limiter = None
        logger.info("Rate limiting: Disabled")
    elif rate_limiting_mode == 'local':
        rate_limit = args.rate_limit if args.rate_limit else 10.0
        burst = args.burst if args.burst else int(rate_limit * 2)
        rate_limiter = TokenBucket(rate=rate_limit, capacity=burst)
        logger.info(f"Rate limiting: Local (rate: {rate_limit} req/sec, burst: {burst})")
    else:
        logger.warning(f"Unknown rate limiting mode: {rate_limiting_mode}, disabling")
        rate_limiter = None

    if args.circuit_breaker:
        cb_config = CircuitBreakerConfig(
            failure_threshold=args.circuit_breaker_failure_threshold,
            timeout=args.circuit_breaker_timeout
        )
        circuit_breaker = CircuitBreaker(cb_config)
        logger.info(
            f"Circuit breaker: Enabled (failure threshold: {cb_config.failure_threshold}, "
            f"timeout: {cb_config.timeout}s)"
        )
    else:
        logger.info("Circuit breaker: Disabled")
    try:
        if args.transport == 'stdio':
            logger.info("Using stdio transport")
            mcp.run()
        elif args.transport == 'sse':
            logger.info(f"Using SSE transport on http://{args.host}:{args.port}")
            mcp.run(transport='sse', host=args.host, port=args.port)
        elif args.transport == 'streamable-http':
            logger.info(f"Using Streamable HTTP transport on http://{args.host}:{args.port}")
            mcp.run(transport='streamable-http', host=args.host, port=args.port)

    except KeyboardInterrupt:
        logger.info("Server stopped by user")
    except Exception as e:
        logger.error(f"Server error: {e}", exc_info=True)
        raise

if __name__ == "__main__":
    main()
