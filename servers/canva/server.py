#!/usr/bin/env python3
"""
Canva Connect Api MCP Server

API Info:
- API License: ©2023 All Rights Reserved
- Contact: Canva Developer Community (https://community.canva.dev/)
- Terms of Service: https://www.canva.com/trust/legal/

Generated: 2026-03-27 00:09:26 UTC
@generated by MCP Blacksmith (https://mcpblacksmith.com)
"""

from __future__ import annotations

import argparse
import asyncio
import json
import logging
import os
import random
import sys
import time
import uuid
from dataclasses import dataclass
from enum import Enum
from pathlib import Path
from typing import Any, Literal

try:
    from dotenv import load_dotenv
    _env_path = Path(__file__).parent / '.env'
    if _env_path.exists():
        load_dotenv(_env_path)
except ImportError:
    pass

import _auth
import _models
import httpx
import pydantic
from fastmcp import FastMCP
from pydantic import Field

BASE_URL = os.getenv("BASE_URL", "https://api.canva.com/rest")
SERVER_NAME = "Canva"
SERVER_VERSION = "1.0.0"

CONNECTION_POOL_SIZE = int(os.getenv("CONNECTION_POOL_SIZE", "100"))
MAX_KEEPALIVE_CONNECTIONS = int(os.getenv("MAX_KEEPALIVE_CONNECTIONS", "20"))

LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO").upper()
if LOG_LEVEL not in ("DEBUG", "INFO", "WARNING", "ERROR"):
    LOG_LEVEL = "INFO"

HTTPX_TIMEOUT = httpx.Timeout(
    connect=float(os.getenv("HTTPX_CONNECT_TIMEOUT", "10.0")),
    read=float(os.getenv("HTTPX_READ_TIMEOUT", "60.0")),
    write=float(os.getenv("HTTPX_WRITE_TIMEOUT", "30.0")),
    pool=float(os.getenv("HTTPX_POOL_TIMEOUT", "5.0"))
)

DEFAULT_TIMEOUT = float(os.getenv("TOOL_EXECUTION_TIMEOUT", "90.0"))

MAX_RETRIES = int(os.getenv("MAX_RETRIES", "3"))
RETRY_BACKOFF_FACTOR = float(os.getenv("RETRY_BACKOFF_FACTOR", "2.0"))
CIRCUIT_BREAKER_FAILURE_THRESHOLD = int(os.getenv("CIRCUIT_BREAKER_FAILURE_THRESHOLD", "5"))
CIRCUIT_BREAKER_TIMEOUT_SECONDS = float(os.getenv("CIRCUIT_BREAKER_TIMEOUT_SECONDS", "60.0"))
RATE_LIMIT_REQUESTS_PER_SECOND = int(os.getenv("RATE_LIMIT_REQUESTS_PER_SECOND", "10"))
client: httpx.AsyncClient | None = None

@dataclass
class RetryConfig:
    """Retry with exponential backoff configuration."""
    max_attempts: int = 3
    base_delay: float = 1.0
    max_delay: float = 60.0
    exponential_base: float = 2.0
    jitter: bool = True
    retry_on_status_codes: list[int] | None = None

    def __post_init__(self):
        if self.retry_on_status_codes is None:
            self.retry_on_status_codes = [429, 500, 502, 503, 504]

class TokenBucket:
    """Token bucket rate limiter."""

    def __init__(self, rate: float, capacity: int):
        self.rate = rate
        self.capacity = capacity
        self.tokens = float(capacity)
        self.last_update = time.monotonic()

    def _refill(self):
        now = time.monotonic()
        elapsed = now - self.last_update
        self.tokens = min(self.capacity, self.tokens + (elapsed * self.rate))
        self.last_update = now

    def consume(self, tokens: int = 1) -> tuple[bool, float | None]:
        self._refill()
        if self.tokens >= tokens:
            self.tokens -= tokens
            return True, None
        tokens_needed = tokens - self.tokens
        return False, tokens_needed / self.rate

class CircuitState(Enum):
    """Circuit breaker states."""
    CLOSED = "closed"      # Normal operation
    OPEN = "open"          # Blocking requests
    HALF_OPEN = "half_open"  # Testing recovery

@dataclass
class CircuitBreakerConfig:
    """Circuit breaker configuration."""
    failure_threshold: int = 5
    success_threshold: int = 2
    timeout: float = 60.0

class CircuitBreakerError(Exception):
    """Circuit breaker is open, request blocked."""
    def __init__(self, message: str, retry_after: float | None = None):
        super().__init__(message)
        self.retry_after = retry_after

class CircuitBreaker:
    """Circuit breaker to prevent cascading failures."""

    def __init__(self, config: CircuitBreakerConfig):
        self.config = config
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        self.last_failure_time: float | None = None

    def before_call(self):
        """Check if circuit allows request."""
        if self.state == CircuitState.OPEN:
            if self._should_attempt_reset():
                self._transition_to_half_open()
            else:
                retry_after = self._time_until_recovery()
                raise CircuitBreakerError(
                    "Circuit breaker OPEN due to repeated failures",
                    retry_after=retry_after
                )

    def on_success(self):
        """Handle successful request."""
        if self.state == CircuitState.HALF_OPEN:
            self.success_count += 1
            if self.success_count >= self.config.success_threshold:
                self._transition_to_closed()
        elif self.state == CircuitState.CLOSED:
            if self.failure_count > 0:
                self.failure_count = 0

    def on_failure(self, error: Exception):
        """Handle failed request."""
        logging.debug(f"Circuit breaker recorded failure: {error}")
        if self.state == CircuitState.HALF_OPEN:
            self._transition_to_open()
        elif self.state == CircuitState.CLOSED:
            self.failure_count += 1
            if self.failure_count >= self.config.failure_threshold:
                self._transition_to_open()

    def _should_attempt_reset(self) -> bool:
        """Check if timeout elapsed for recovery."""
        if self.last_failure_time is None:
            return True
        return (time.monotonic() - self.last_failure_time) >= self.config.timeout

    def _time_until_recovery(self) -> float:
        """Time remaining until recovery attempt."""
        if self.last_failure_time is None:
            return 0.0
        elapsed = time.monotonic() - self.last_failure_time
        return max(0.0, self.config.timeout - elapsed)

    def _transition_to_open(self):
        """Transition to OPEN state."""
        self.state = CircuitState.OPEN
        self.last_failure_time = time.monotonic()
        self.success_count = 0
        logging.error(f"Circuit breaker OPEN after {self.failure_count} failures")

    def _transition_to_half_open(self):
        """Transition to HALF_OPEN state."""
        self.state = CircuitState.HALF_OPEN
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker HALF_OPEN - testing recovery")

    def _transition_to_closed(self):
        """Transition to CLOSED state."""
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker CLOSED - recovered")

# Global resilience configuration
retry_config: RetryConfig | None = None
rate_limiter = None  # Initialized based on mode in main()
circuit_breaker: CircuitBreaker | None = None

# ============================================================================
# Error Formatting
# ============================================================================

def _format_api_error_message(
    tool_name: str | None,
    method: str,
    path: str,
    status_code: int,
    request_id: str | None,
    error_data: dict
) -> str:
    """
    Format API error for LLM consumption with structured, parseable details.

    Args:
        tool_name: MCP tool function name
        method: HTTP method (GET, POST, etc.)
        path: API endpoint path
        status_code: HTTP status code
        request_id: Request UUID
        error_data: Sanitized error response from API

    Returns:
        Structured error message for LLM with available details
    """
    parts = []

    # Add tool name if available
    if tool_name:
        parts.append(f"Tool: {tool_name}")

    parts.append(f"Status: {status_code}")

    # Extract error message from various API response formats
    error_message = None
    if isinstance(error_data, dict):
        # Format 1: {"message": "..."}
        if 'message' in error_data:
            error_message = error_data['message']
        # Format 2: {"error": {"message": "...", "code": ..., "errors": [...]}}  (Google APIs)
        elif 'error' in error_data:
            err = error_data['error']
            if isinstance(err, dict):
                if 'message' in err:
                    error_message = err['message']
                # Also include error code if available
                if 'code' in err and error_message:
                    error_message = f"[{err['code']}] {error_message}"
            elif isinstance(err, str):
                error_message = err
        # Format 3: {"detail": "..."} (FastAPI/OpenAPI)
        elif 'detail' in error_data:
            detail = error_data['detail']
            if isinstance(detail, str):
                error_message = detail
            elif isinstance(detail, list) and detail:
                # Validation errors: [{"loc": [...], "msg": "...", "type": "..."}]
                msgs = [d.get('msg', str(d)) for d in detail[:3]]
                error_message = "; ".join(msgs)
                if len(detail) > 3:
                    error_message += f" (+{len(detail) - 3} more)"
        # Format 4: {"errors": [...]} (GraphQL-style)
        elif 'errors' in error_data and isinstance(error_data['errors'], list):
            errors = error_data['errors']
            if errors:
                msgs = [e.get('message', str(e)) for e in errors[:3]]
                error_message = "; ".join(msgs)
                if len(errors) > 3:
                    error_message += f" (+{len(errors) - 3} more)"

    if error_message:
        parts.append(f"Error: {error_message}")

    parts.append(f"Request: {method} {path}")

    # Add request ID if available
    if request_id:
        parts.append(f"ID: {request_id}")

    return "\n".join(parts)

# ============================================================================
# Response Sanitization
# ============================================================================

# Sanitization level configuration
SANITIZATION_LEVEL = os.getenv("SANITIZATION_LEVEL", "DISABLED").upper()
if SANITIZATION_LEVEL not in ("DISABLED", "LOW", "MEDIUM", "HIGH"):
    SANITIZATION_LEVEL = "DISABLED"

# Pattern sets by level
_PATTERNS_LOW = [
    'password', 'passwd', 'pwd',
    'token', 'secret', 'private_key', 'priv_key',
]
_PATTERNS_MEDIUM = _PATTERNS_LOW + [
    'auth_token', 'access_token', 'refresh_token', 'bearer',
    'credentials', 'authorization',
]
_PATTERNS_HIGH = _PATTERNS_MEDIUM + [
    'session_id', 'sessionid', 'cookie',
    'api_key', 'apikey', 'api-key',
    'user_id', 'userid', 'account_id', 'accountid', 'internal_id',
    'ip_address', 'ipaddress', 'ip_addr',
    'hostname', 'host_name', 'server_name', 'servername',
    'internal_ip', 'private_ip',
]

def _get_sensitive_patterns() -> list:
    """Get patterns based on current sanitization level."""
    if SANITIZATION_LEVEL == "DISABLED":
        return []
    if SANITIZATION_LEVEL == "LOW":
        return _PATTERNS_LOW
    if SANITIZATION_LEVEL == "MEDIUM":
        return _PATTERNS_MEDIUM
    return _PATTERNS_HIGH

def sanitize_response(response_data: Any) -> Any:
    """Remove sensitive fields from API responses before returning to LLM.

    Controlled by SANITIZATION_LEVEL env var:
    - DISABLED: No filtering (fastest)
    - LOW: Basic secrets (password, token, secret, private_key)
    - MEDIUM: LOW + Auth fields
    - HIGH: MEDIUM + Session/IDs

    Args:
        response_data: API response (dict, list, or primitive)

    Returns:
        Sanitized response with sensitive fields removed (based on level)
    """
    sensitive_patterns = _get_sensitive_patterns()

    # Skip sanitization if disabled
    if not sensitive_patterns:
        return response_data

    def should_filter(key: str) -> bool:
        """Check if field name matches sensitive patterns."""
        key_lower = key.lower()
        return any(pattern in key_lower for pattern in sensitive_patterns)

    def sanitize_dict(data: dict) -> dict:
        """Recursively sanitize dictionary."""
        sanitized: dict[str, Any] = {}
        for key, value in data.items():
            if should_filter(key):
                # Skip sensitive fields
                logging.debug(f"Filtered sensitive field: {key}")
                continue

            # Recursively sanitize nested structures
            if isinstance(value, dict):
                sanitized[key] = sanitize_dict(value)
            elif isinstance(value, list):
                sanitized[key] = sanitize_list(value)
            else:
                sanitized[key] = value

        return sanitized

    def sanitize_list(data: list[Any]) -> list[Any]:
        """Recursively sanitize list."""
        sanitized: list[Any] = []
        for item in data:
            if isinstance(item, dict):
                sanitized.append(sanitize_dict(item))
            elif isinstance(item, list):
                sanitized.append(sanitize_list(item))
            else:
                sanitized.append(item)
        return sanitized

    # Handle different response types
    if isinstance(response_data, dict):
        return sanitize_dict(response_data)
    if isinstance(response_data, list):
        return sanitize_list(response_data)
    # Primitive types (str, int, bool, None) pass through
    return response_data

def get_safe_error_response(
    error_type: str,
    status_code: int | None = None,
    retry_after: int | None = None,
    request_id: str | None = None
) -> dict:
    """Generate safe error response for client (LLM).

    Returns generic, actionable error messages without leaking:
    - API keys, tokens, credentials
    - Internal server details, IP addresses, hostnames
    - Stack traces, exception messages
    - User data from other users

    Args:
        error_type: Type of error (auth, rate_limit, timeout, etc.)
        status_code: HTTP status code
        retry_after: Retry-After header value (seconds)
        request_id: Request correlation ID for debugging

    Returns:
        Safe error response dictionary
    """
    error_messages = {
        'AUTHENTICATION_ERROR': {
            'error': 'AUTHENTICATION_ERROR',
            'message': 'Authentication failed. Please check your credentials.',
            'retry': False
        },
        'RATE_LIMIT_ERROR': {
            'error': 'RATE_LIMIT_ERROR',
            'message': 'Rate limit exceeded. Please retry after the specified delay.',
            'retry': True
        },
        'TIMEOUT_ERROR': {
            'error': 'TIMEOUT_ERROR',
            'message': 'Request timed out. The API did not respond in time.',
            'retry': True
        },
        'VALIDATION_ERROR': {
            'error': 'VALIDATION_ERROR',
            'message': 'Invalid request parameters. Please check your input.',
            'retry': False
        },
        'NOT_FOUND': {
            'error': 'NOT_FOUND',
            'message': 'The requested resource was not found.',
            'retry': False
        },
        'PERMISSION_DENIED': {
            'error': 'PERMISSION_DENIED',
            'message': 'You do not have permission to access this resource.',
            'retry': False
        },
        'API_ERROR': {
            'error': 'API_ERROR',
            'message': 'An error occurred while calling the external API.',
            'retry': True
        },
        'INTERNAL_ERROR': {
            'error': 'INTERNAL_ERROR',
            'message': 'An internal error occurred. Please try again later.',
            'retry': False
        }
    }

    # Get base error response
    response = error_messages.get(error_type, error_messages['INTERNAL_ERROR']).copy()

    # Add optional fields
    if status_code:
        response['status_code'] = status_code

    if retry_after:
        response['retry_after_seconds'] = retry_after

    if request_id:
        response['request_id'] = request_id

    return response

async def _make_request(
    method: str,
    path: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    tool_name: str | None = None,
    request_id: str | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any], int]:
    """Make HTTP request with retry, rate limiting, and circuit breaker."""
    global client, retry_config, rate_limiter, circuit_breaker

    if client is None:
        client = httpx.AsyncClient(
            base_url=BASE_URL,
            timeout=HTTPX_TIMEOUT,
            limits=httpx.Limits(max_keepalive_connections=MAX_KEEPALIVE_CONNECTIONS, max_connections=CONNECTION_POOL_SIZE),
            cookies=None  # Disable cookie persistence for multi-tenant safety
        )

    if headers is None:
        headers = {}


    if rate_limiter is not None:
        allowed, wait_time = rate_limiter.consume()
        if not allowed:
            return {
                "error": "Rate limit exceeded",
                "retry_after": wait_time,
                "message": f"Rate limit exceeded. Wait {wait_time:.1f}s"
            }, 429

    if circuit_breaker is not None:
        try:
            circuit_breaker.before_call()
        except CircuitBreakerError as e:
            return {
                "error": "Circuit breaker open",
                "retry_after": e.retry_after,
                "message": str(e)
            }, 503

    if retry_config is not None:
        max_attempts = retry_config.max_attempts
        retry_status_codes = retry_config.retry_on_status_codes or []
    else:
        max_attempts = 1
        retry_status_codes = []

    # Append raw querystring before retry loop to avoid repeated appending
    # (OAS 3.2 in: querystring — already serialized per media type)
    if raw_querystring:
        _qs_sep = "&" if "?" in path else "?"
        path = f"{path}{_qs_sep}{raw_querystring}"

    last_error: httpx.HTTPStatusError | Exception | None = None
    _auth_retried = False  # Guard: only attempt one auth refresh per request
    for attempt in range(max_attempts):
        try:
            # Dispatch body to correct httpx kwarg based on content type
            _json = body if body_content_type is None or body_content_type == "application/json" else None
            _data = body if body_content_type in ("application/x-www-form-urlencoded", "multipart/form-data") else None
            _content = body if body_content_type is not None and body_content_type not in ("application/json", "application/x-www-form-urlencoded", "multipart/form-data") else None
            response = await client.request(
                method=method,
                url=path,
                params=params,
                json=_json,
                data=_data,
                content=_content,
                headers=headers,
                cookies=cookies
            )

            # Get status code and data before raise_for_status
            status_code = response.status_code

            # Handle empty response body (204 No Content, 205 Reset Content, or empty body)
            if status_code in (204, 205) or not response.content:
                response_data = {"status": status_code, "message": "Success"}
            else:
                # Parse response JSON (graceful fallback to text)
                try:
                    response_data = response.json()
                except Exception:
                    response_data = {"text": response.text}

            # Check for HTTP errors
            if status_code >= 400:
                last_error = httpx.HTTPStatusError(
                    message=f"HTTP {status_code}",
                    request=response.request,
                    response=response
                )

                # On 401: clear stale tokens and retry once with fresh auth
                if status_code == 401 and not _auth_retried and tool_name:
                    _auth_retried = True
                    logging.warning(f"401 Unauthorized for {tool_name} - clearing token and re-authorizing")
                    _on_auth_failure()
                    _fresh_auth = _get_auth_for_operation(tool_name)
                    if _fresh_auth.get("headers"):
                        headers = {**headers, **_fresh_auth["headers"]}
                    if _fresh_auth.get("params") and params is not None:
                        params = {**params, **_fresh_auth["params"]}
                    continue  # Retry immediately with fresh auth (no backoff)

                # Check if should retry
                if status_code not in retry_status_codes or attempt == max_attempts - 1:
                    # Don't retry or last attempt - notify circuit breaker of failure
                    if circuit_breaker is not None and status_code in [500, 502, 503, 504]:
                        circuit_breaker.on_failure(last_error)

                    # Log error information - WARNING for client errors (4xx), ERROR for server errors (5xx)
                    log_level = logging.WARNING if 400 <= status_code < 500 else logging.ERROR
                    logging.log(
                        log_level,
                        f"API request failed: {method} {path} (status: {status_code})",
                        extra={
                            "status_code": status_code,
                            "endpoint": path,
                            "method": method,
                            "attempt": attempt + 1,
                            "max_attempts": max_attempts,
                            "error_detail": str(response_data)[:500]
                        },
                        exc_info=False
                    )

                    # Sanitize response before returning/raising
                    sanitized_data = sanitize_response(response_data)

                    # Raise exception with structured error message for LLM
                    error_message = _format_api_error_message(
                        tool_name=tool_name,
                        method=method,
                        path=path,
                        status_code=status_code,
                        request_id=request_id,
                        error_data=sanitized_data
                    )
                    raise ValueError(error_message)

                # Will retry - continue to backoff logic below

            else:
                # Success - notify circuit breaker
                if circuit_breaker is not None:
                    circuit_breaker.on_success()

                # Sanitize response before returning to client
                sanitized_data = sanitize_response(response_data)
                return sanitized_data, status_code

            # Exponential backoff with jitter (for retry)
            # retry_config is guaranteed non-None here since max_attempts > 1
            assert retry_config is not None
            base_delay = retry_config.base_delay
            max_delay = retry_config.max_delay
            exponential_base = retry_config.exponential_base
            use_jitter = retry_config.jitter

            # Honor Retry-After header for 429 responses
            delay: float
            if status_code == 429:
                retry_after = response.headers.get('Retry-After')
                if retry_after:
                    try:
                        delay = float(retry_after)
                    except ValueError:
                        delay = min(base_delay * (exponential_base ** attempt), max_delay)
                else:
                    delay = min(base_delay * (exponential_base ** attempt), max_delay)
            else:
                delay = min(base_delay * (exponential_base ** attempt), max_delay)

            # Add jitter
            if use_jitter:
                delay = delay * (0.5 + random.random() * 0.5)  # noqa: S311

            logging.warning(
                f"Request failed with {status_code} (attempt {attempt + 1}/{max_attempts}). "
                f"Retrying in {delay:.2f}s"
            )
            await asyncio.sleep(delay)

        except httpx.HTTPStatusError:
            # Already handled above - shouldn't reach here
            continue

        except Exception as e:
            last_error = e

            # Network/connection errors - notify circuit breaker
            if circuit_breaker is not None:
                circuit_breaker.on_failure(e)

            # Log detailed error information for debugging (internal only)
            logging.error(
                f"Request failed with exception: {method} {path}",
                extra={
                    "endpoint": path,
                    "method": method,
                    "attempt": attempt + 1,
                    "max_attempts": max_attempts,
                    "error_type": e.__class__.__name__,
                    "error_detail": str(e)
                },
                exc_info=True
            )

            # Don't retry on non-HTTP errors - raise immediately with details
            # This ensures the LLM sees the actual error message
            error_message = (
                f"Tool: {tool_name}\n"
                f"Error: {e.__class__.__name__}\n"
                f"Request: {method} {path}\n"
                f"ID: {request_id}\n"
                f"Details: {str(e)}"
            )
            raise ConnectionError(error_message) from e

    # Log retry exhaustion with full context (internal only)
    logging.error(
        f"All retry attempts exhausted: {method} {path}",
        extra={
            "endpoint": path,
            "method": method,
            "total_attempts": max_attempts,
            "last_error_type": last_error.__class__.__name__ if last_error else "Unknown",
            "last_error_detail": str(last_error) if last_error else "Unknown"
        },
        exc_info=True
    )

    # Raise exception with error details
    if isinstance(last_error, httpx.HTTPStatusError):
        try:
            error_data = last_error.response.json()
        except Exception:
            error_data = {"error": last_error.response.text[:1000]}

        sanitized_error = sanitize_response(error_data)
        error_message = _format_api_error_message(
            tool_name=tool_name,
            method=method,
            path=path,
            status_code=last_error.response.status_code,
            request_id=request_id,
            error_data=sanitized_error
        )
        raise ValueError(error_message)

    # Network/connection error - structured format for consistency
    error_message = (
        f"Tool: {tool_name}\n"
        f"Error: Network error after {max_attempts} attempts\n"
        f"Request: {method} {path}\n"
        f"ID: {request_id}\n"
        f"Details: {str(last_error)}"
    )
    raise ConnectionError(error_message)

# ============================================================================
# Helper Functions
# ============================================================================


def _serialize_query(params: dict[str, Any], styles: dict[str, tuple[str, bool]]) -> dict[str, Any]:
    """Serialize query params per OAS style/explode rules."""
    result: dict[str, Any] = {}
    for key, value in params.items():
        if key not in styles:
            result[key] = value
            continue
        style, explode = styles[key]
        if isinstance(value, list):
            if style == "pipeDelimited":
                result[key] = "|".join(str(v) for v in value)
            elif style == "spaceDelimited":
                result[key] = " ".join(str(v) for v in value)
            elif not explode:
                result[key] = ",".join(str(v) for v in value)
            else:
                result[key] = value
        elif isinstance(value, dict):
            if style == "deepObject":
                for k, v in value.items():
                    result[f"{key}[{k}]"] = v
            elif not explode:
                result[key] = ",".join(f"{k},{v}" for k, v in value.items())
            else:
                result.update(value)
        else:
            result[key] = value
    return result

def _encode_content_params(params: dict[str, Any], encodings: dict[str, str]) -> dict[str, Any]:
    """Serialize content-encoded params per OAS Parameter Object content property."""
    import json as _json_mod
    result = dict(params)
    for key, encoding in encodings.items():
        if key in result and result[key] is not None and (encoding == "application/json" or encoding.endswith("+json")):
            result[key] = _json_mod.dumps(result[key])
    return result

def _log_tool_invocation(tool_name: str, method: str, path: str, request_id: str) -> None:
    """Log tool invocation."""
    logging.info(
        f"Tool invoked: {tool_name}",
        extra={
            "request_id": request_id,
            "tool": tool_name,
            "method": method,
            "path": path,
            "timeout": DEFAULT_TIMEOUT
        }
    )

def _build_path(
    template: str,
    path_params: dict[str, Any],
) -> str:
    """Build path from template and parameters.

    Args:
        template: Path template with {param} placeholders
        path_params: Dictionary of parameter names to values

    Returns:
        Path with all  placeholders replaced

    Example:
        _build_path("/files/{fileId}/copy", {"fileId": "abc123"})
        Returns: "/files/abc123/copy"
    """
    result = template
    for key, value in path_params.items():
        result = result.replace("{" + key + "}", str(value))
    # Normalize double slashes that occur when a path param value itself starts
    # with "/" and the template already has a preceding "/" (e.g. "/{path}" + "/foo")
    while "//" in result:
        result = result.replace("//", "/")
    return result

async def _execute_tool_request(
    tool_name: str,
    method: str,
    path: str,
    request_id: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any], int]:
    """
    Execute tool request with timeout handling and metrics recording.

    Returns:
        Tuple of (normalized_response_data, status_code).
        Response data is normalized to dict format for Pydantic validation.
        Status code: HTTP status code from the API response.
    """
    start_time = time.time()
    try:
        # Wrap API call with timeout to prevent hanging
        async with asyncio.timeout(DEFAULT_TIMEOUT):
            # Call unified _make_request() which handles all resilience + security features
            result, status_code = await _make_request(
                method=method,
                path=path,
                params=params,
                body=body,
                body_content_type=body_content_type,
                headers=headers,
                cookies=cookies,
                tool_name=tool_name,
                request_id=request_id,
                raw_querystring=raw_querystring,
            )

        latency_ms = (time.time() - start_time) * 1000.0

        logging.info(f"Tool completed: {tool_name}", extra={"request_id": request_id, "latency_ms": f"{latency_ms:.2f}"})

        # Normalize response to dict and return with status code
        # Use "value" key for consistency with generated response models
        if isinstance(result, dict):
            return result, status_code
        return {"value": result}, status_code

    except asyncio.TimeoutError as e:
        latency_ms = (time.time() - start_time) * 1000.0

        # Request timed out
        logging.error(
            f"Tool timeout: {tool_name}",
            extra={
                "request_id": request_id,
                "tool": tool_name,
                "method": method,
                "path": path,
                "timeout": DEFAULT_TIMEOUT,
                "latency_ms": f"{latency_ms:.2f}"
            }
        )

        # Raise exception with structured error message for consistency
        # FastMCP will catch this and set isError: true in MCP response
        timeout_message = (
            f"Tool: {tool_name}\n"
            f"Error: Request timed out after {DEFAULT_TIMEOUT} seconds\n"
            f"ID: {request_id}"
        )
        raise asyncio.TimeoutError(timeout_message) from e

    except ValueError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except ConnectionError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except Exception:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

# ============================================================================
# Authentication
# ============================================================================

# Authentication scheme priority (most secure first)
AUTH_SCHEME_PRIORITY = [
    'oauth2',
    'openIdConnect',
    'http',
    'apiKey',
]

# Initialize authentication handlers at server startup
_auth_handlers: dict[str, Any] = {}
try:
    _auth_handlers["oauthAuthCode"] = _auth.OAuth2Auth()
    logging.info("Authentication configured: oauthAuthCode")
except ValueError as e:
    # Extract credential names from error message (first sentence before "Leave empty")
    error_msg = str(e).split("Leave empty")[0].strip()
    logging.warning(f"Credentials for oauthAuthCode not configured: {error_msg}")
    _auth_handlers["oauthAuthCode"] = None
try:
    _auth_handlers["basicAuth"] = _auth.BasicAuth(env_var_username="BASIC_AUTH_USERNAME", env_var_password="BASIC_AUTH_PASSWORD")
    logging.info("Authentication configured: basicAuth")
except ValueError as e:
    # Extract credential names from error message (first sentence before "Leave empty")
    error_msg = str(e).split("Leave empty")[0].strip()
    logging.warning(f"Credentials for basicAuth not configured: {error_msg}")
    _auth_handlers["basicAuth"] = None

# Warn only if NO auth handlers were successfully configured
if all(handler is None for handler in _auth_handlers.values()):
    logging.warning("Server will attempt unauthenticated requests (may fail if API requires auth)")

def _on_auth_failure() -> None:
    """Clear tokens on all auth handlers that support it (called on 401 response)."""
    for scheme_name, handler in _auth_handlers.items():
        if handler is not None and hasattr(handler, 'clear_token'):
            handler.clear_token()
            logging.info(f"Cleared token for auth scheme '{scheme_name}' after 401")

def _get_auth_for_operation(operation_id: str) -> dict[str, dict[str, str]]:
    """Get authentication for specific operation (handles multi-auth with OR/AND logic).

    Args:
        operation_id: The operation ID (tool name) to get auth for

    Returns:
        Dictionary with 'headers', 'params', 'cookies' keys containing auth data
    """
    result: dict[str, dict[str, str]] = {"headers": {}, "params": {}, "cookies": {}}

    # Get auth requirements for this operation from auth module
    # Map structure: operation_id -> [[scheme1], [scheme2, scheme3]] (OR of AND groups)
    required_schemes = _auth.OPERATION_AUTH_MAP.get(operation_id)

    if required_schemes is None:
        # CRITICAL: Operation missing from OPERATION_AUTH_MAP
        # This indicates a bug in auth config generation - operation should be in map
        # Do NOT fall back to global security - this masks bugs and creates security gaps
        logging.error(
            f"SECURITY ERROR: Operation '{operation_id}' not found in OPERATION_AUTH_MAP. "
            f"This indicates a bug in auth config generation. "
            f"All operations should be explicitly mapped. "
            f"Proceeding with no authentication - REQUEST WILL LIKELY FAIL."
        )
        # Return empty auth (will likely fail at API, but surfaces the bug)
        return result

    # Sort OR groups by security priority (most secure first)
    # Each OR group is sorted internally by scheme priority
    def get_scheme_priority(scheme_name: str) -> int:
        """Get priority index for scheme (lower = higher priority)."""
        try:
            return AUTH_SCHEME_PRIORITY.index(scheme_name)
        except ValueError:
            # Unknown scheme - put at end
            return len(AUTH_SCHEME_PRIORITY)

    # Sort each OR group by priority, then sort OR groups by their best (lowest) priority
    sorted_schemes = []
    for scheme_list in required_schemes:
        # Sort schemes within AND group by priority (most secure first)
        sorted_group = sorted(scheme_list, key=get_scheme_priority)
        sorted_schemes.append(sorted_group)

    # Sort OR groups by the priority of their first (most secure) scheme
    sorted_schemes.sort(key=lambda group: get_scheme_priority(group[0]) if group else float('inf'))

    # Try each OR group in priority order (most secure first)
    for scheme_list in sorted_schemes:
        headers = {}
        params = {}
        cookies = {}
        all_succeeded = True

        # Handle AND group (multiple schemes in same list - all must succeed)
        for scheme_name in scheme_list:
            handler = _auth_handlers.get(scheme_name)
            if not handler:
                # Handler not configured (env vars missing)
                all_succeeded = False
                logging.debug(f"Auth scheme '{scheme_name}' not configured for {operation_id}")
                break

            try:
                # Try all injection methods (headers, params, cookies)
                if hasattr(handler, 'get_auth_headers'):
                    headers.update(handler.get_auth_headers())
                if hasattr(handler, 'get_auth_params'):
                    params.update(handler.get_auth_params())
                if hasattr(handler, 'get_auth_cookies'):
                    cookies.update(handler.get_auth_cookies())
            except Exception as e:
                logging.debug(f"Auth scheme '{scheme_name}' failed for {operation_id}: {e}")
                all_succeeded = False
                break

        # If all schemes in AND group succeeded, use this auth
        if all_succeeded and (headers or params or cookies):
            result["headers"] = headers
            result["params"] = params
            result["cookies"] = cookies
            logging.debug(f"Using auth for {operation_id}: schemes={scheme_list}")
            return result

    # No auth configured or all OR groups failed
    if required_schemes and required_schemes != [[]]:
        logging.warning(f"No configured auth handler found for {operation_id} (requires: {required_schemes})")

    return result

# ============================================================================
# FastMCP Server Initialization
# ============================================================================

mcp = FastMCP("Canva")

# Tags: app
@mcp.tool()
async def get_app_jwks(app_id: str = Field(..., alias="appId", description="The unique identifier of the app whose public JWKS should be retrieved. Must be 1–50 characters and may only contain alphanumeric characters, underscores, or hyphens.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the JSON Web Key Set (JWKS) containing the public keys for a specified app. These public keys are used by app backends to verify the authenticity of JWTs issued for that app."""

    # Construct request model with validation
    try:
        _request = _models.GetAppJwksRequest(
            path=_models.GetAppJwksRequestPath(app_id=app_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_app_jwks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_app_jwks", "GET", "/v1/apps/{appId}/jwks", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/apps/{appId}/jwks", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/apps/{appId}/jwks"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_app_jwks")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_app_jwks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def get_asset(asset_id: str = Field(..., alias="assetId", description="The unique identifier of the asset to retrieve. Must be 1–50 characters and may contain letters, digits, underscores, and hyphens.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the metadata for a specific asset by its unique identifier. Use this to inspect asset details such as type, status, and associated properties."""

    # Construct request model with validation
    try:
        _request = _models.GetAssetRequest(
            path=_models.GetAssetRequestPath(asset_id=asset_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_asset", "GET", "/v1/assets/{assetId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/assets/{assetId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/assets/{assetId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_asset")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_asset",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def update_asset(
    asset_id: str = Field(..., alias="assetId", description="The unique identifier of the asset to update. Must be 1–50 characters and contain only alphanumeric characters, underscores, or hyphens.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    name: str | None = Field(None, description="The display name of the asset as shown in the Canva UI. If omitted or empty, the existing name remains unchanged. Maximum 50 characters.", max_length=50, examples=['My Awesome Upload']),
    tags: list[str] | None = Field(None, description="A list of tags to assign to the asset, replacing all existing tags. If omitted, existing tags are left unchanged. Each array may contain up to 50 tags; order is not significant.", max_length=50, examples=[['image', 'holiday', 'best day ever']])
) -> dict[str, Any]:
    """Updates the name and/or tags of an existing asset identified by its ID. Providing new tags replaces all existing tags on the asset entirely."""

    # Construct request model with validation
    try:
        _request = _models.UpdateAssetRequest(
            path=_models.UpdateAssetRequestPath(asset_id=asset_id),
            body=_models.UpdateAssetRequestBody(name=name, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("update_asset", "PATCH", "/v1/assets/{assetId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/assets/{assetId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/assets/{assetId}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("update_asset")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_asset",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def delete_asset(asset_id: str = Field(..., alias="assetId", description="The unique identifier of the asset to delete. Must be 1–50 characters and contain only alphanumeric characters, underscores, or hyphens.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Moves the specified asset to the trash, mirroring the delete behavior in the Canva UI. Existing designs that reference the asset are unaffected by this operation."""

    # Construct request model with validation
    try:
        _request = _models.DeleteAssetRequest(
            path=_models.DeleteAssetRequestPath(asset_id=asset_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("delete_asset", "DELETE", "/v1/assets/{assetId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/assets/{assetId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/assets/{assetId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("delete_asset")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_asset",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def upload_asset(
    asset_upload_metadata: _models.CreateAssetUploadJobHeaderAssetUploadMetadata = Field(..., alias="Asset-Upload-Metadata", description="Metadata describing the asset being uploaded, such as its name and MIME type, sent as a header alongside the binary file body."),
    body: str | None = Field(None, description="The raw binary content of the file to upload as an asset.", json_schema_extra={'format': 'binary'})
) -> dict[str, Any]:
    """Starts an asynchronous job to upload a file as an asset to the user's Canva content library. Send the file as a raw binary request body with upload metadata provided in the Asset-Upload-Metadata header."""

    # Construct request model with validation
    try:
        _request = _models.CreateAssetUploadJobRequest(
            header=_models.CreateAssetUploadJobRequestHeader(asset_upload_metadata=asset_upload_metadata),
            body=_models.CreateAssetUploadJobRequestBody(body=body)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for upload_asset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("upload_asset", "POST", "/v1/asset-uploads", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/asset-uploads"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_body = next(iter(_http_body.values()), None) if _http_body else None
    _http_headers = _request.header.model_dump(by_alias=True, exclude_none=True) if _request.header else {}
    _http_headers = _encode_content_params(_http_headers, {
        "Asset-Upload-Metadata": "application/json",
    })

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("upload_asset")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="upload_asset",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def get_asset_upload_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the asset upload job to retrieve, obtained when the upload job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the current status and result of an asynchronous asset upload job. Poll this endpoint until a `success` or `failed` status is returned, as the job may not complete immediately."""

    # Construct request model with validation
    try:
        _request = _models.GetAssetUploadJobRequest(
            path=_models.GetAssetUploadJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_asset_upload_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_asset_upload_job", "GET", "/v1/asset-uploads/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/asset-uploads/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/asset-uploads/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_asset_upload_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_asset_upload_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def upload_asset_from_url(
    name: str = Field(..., description="A human-readable display name for the asset as it will appear in the user's content library. Must be between 1 and 255 characters.", min_length=1, max_length=255, examples=['My Awesome Asset']),
    url: str = Field(..., description="The publicly accessible URL of the file to import; the URL must be reachable from the internet without authentication. Must be between 8 and 2048 characters.", min_length=8, max_length=2048, examples=['https://example.com/my_asset_to_upload.jpg'])
) -> dict[str, Any]:
    """Starts an asynchronous job to upload an asset from a publicly accessible URL into the user's Canva content library. Video assets uploaded via URL are limited to 100MB; use the direct asset upload job API for larger files."""

    # Construct request model with validation
    try:
        _request = _models.CreateUrlAssetUploadJobRequest(
            body=_models.CreateUrlAssetUploadJobRequestBody(name=name, url=url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for upload_asset_from_url: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("upload_asset_from_url", "POST", "/v1/url-asset-uploads", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/url-asset-uploads"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("upload_asset_from_url")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="upload_asset_from_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: asset
@mcp.tool()
async def get_url_asset_upload_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the URL asset upload job to retrieve, obtained when the job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the status and result of an asynchronous asset upload job initiated via URL. Poll this endpoint until a `success` or `failed` status is returned."""

    # Construct request model with validation
    try:
        _request = _models.GetUrlAssetUploadJobRequest(
            path=_models.GetUrlAssetUploadJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_url_asset_upload_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_url_asset_upload_job", "GET", "/v1/url-asset-uploads/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/url-asset-uploads/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/url-asset-uploads/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_url_asset_upload_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_url_asset_upload_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: autofill
@mcp.tool()
async def create_autofill_job(
    brand_template_id: str | None = Field(None, description="The unique identifier of the brand template to use as the basis for the autofilled design. Retrieve available template IDs from the List brand templates endpoint.", examples=['DAFVztcvd9z']),
    title: str | None = Field(None, description="Optional display title for the newly created autofilled design. If omitted, the design inherits the brand template's title. Must be between 1 and 255 characters.", min_length=1, max_length=255),
    data: dict[str, _models.DatasetValue] | None = Field(None, description="A map of data field names to their values used to populate the template. Each key is a field name from the brand template dataset, and each value is a typed object specifying the field type and content — supported types are image (referencing an asset ID), text (plain string), and chart (tabular row/cell data). Retrieve valid field names via the Get brand template dataset endpoint.", examples=[{'cute_pet_image_of_the_day': {'type': 'image', 'asset_id': 'Msd59349ff'}, 'cute_pet_witty_pet_says': {'type': 'text', 'text': 'It was like this when I got here!'}, 'cute_pet_sales_chart': {'type': 'chart', 'chart_data': {'rows': [{'cells': [{'type': 'string', 'value': 'Geographic Region'}, {'type': 'string', 'value': 'Sales (millions AUD)'}, {'type': 'string', 'value': 'Target met?'}, {'type': 'string', 'value': 'Date met'}]}, {'cells': [{'type': 'string', 'value': 'Asia Pacific'}, {'type': 'number', 'value': 10.2}, {'type': 'boolean', 'value': True}, {'type': 'date', 'value': 1721944387}]}, {'cells': [{'type': 'string', 'value': 'EMEA'}, {'type': 'number', 'value': 13.8}, {'type': 'boolean', 'value': False}, {'type': 'date'}]}]}}}])
) -> dict[str, Any]:
    """Starts an asynchronous job to generate a new Canva design by autofilling a brand template with provided data. Supports image, text, and chart field types; poll the Get design autofill job endpoint to retrieve results once the job completes."""

    # Construct request model with validation
    try:
        _request = _models.CreateDesignAutofillJobRequest(
            body=_models.CreateDesignAutofillJobRequestBody(brand_template_id=brand_template_id, title=title, data=data)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_autofill_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("create_autofill_job", "POST", "/v1/autofills", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/autofills"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("create_autofill_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_autofill_job",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: autofill
@mcp.tool()
async def get_autofill_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the design autofill job to retrieve, as returned when the job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the status and result of a design autofill job previously created via the Create design autofill job API. Poll this endpoint until a `success` or `failed` status is returned, as the job is processed asynchronously."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignAutofillJobRequest(
            path=_models.GetDesignAutofillJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_autofill_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_autofill_job", "GET", "/v1/autofills/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/autofills/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/autofills/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_autofill_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_autofill_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: brand_template
@mcp.tool()
async def list_brand_templates(
    query: str | None = Field(None, description="Keyword search term to filter brand templates by name or content."),
    limit: int | None = Field(25, description="Maximum number of brand templates to return per request, between 1 and 100.", ge=1, le=100, json_schema_extra={'format': 'int32'}),
    ownership: Literal["any", "owned", "shared"] | None = Field('any', description="Filters results by the user's ownership relationship to the templates — all accessible templates, only templates the user owns, or only templates shared with them."),
    sort_by: Literal["relevance", "modified_descending", "modified_ascending", "title_descending", "title_ascending"] | None = Field('relevance', description="Determines the sort order of the returned brand templates, such as by relevance, last modified date, or title."),
    dataset: Literal["any", "non_empty"] | None = Field('any', description="Filters templates based on whether they have dataset definitions; use 'non_empty' to return only templates compatible with the Autofill APIs.")
) -> dict[str, Any]:
    """Retrieves a paginated list of brand templates accessible to the user within their Canva Enterprise organization. Supports filtering by ownership, dataset definitions, and sorting, with optional keyword search."""

    # Construct request model with validation
    try:
        _request = _models.ListBrandTemplatesRequest(
            query=_models.ListBrandTemplatesRequestQuery(query=query, limit=limit, ownership=ownership, sort_by=sort_by, dataset=dataset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_brand_templates: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_brand_templates", "GET", "/v1/brand-templates", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/brand-templates"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_brand_templates")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_brand_templates",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: brand_template
@mcp.tool()
async def get_brand_template(brand_template_id: str = Field(..., alias="brandTemplateId", description="The unique identifier of the brand template to retrieve. Accepts both legacy and new ID formats, though migration to new IDs is recommended following the September 2025 format change.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves metadata for a specific brand template by its ID. Requires the acting user to be a member of a Canva Enterprise organization."""

    # Construct request model with validation
    try:
        _request = _models.GetBrandTemplateRequest(
            path=_models.GetBrandTemplateRequestPath(brand_template_id=brand_template_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_brand_template: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_brand_template", "GET", "/v1/brand-templates/{brandTemplateId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/brand-templates/{brandTemplateId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/brand-templates/{brandTemplateId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_brand_template")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_brand_template",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: brand_template
@mcp.tool()
async def get_brand_template_dataset(brand_template_id: str = Field(..., alias="brandTemplateId", description="The unique identifier of the brand template whose dataset definition you want to retrieve. Accepts both legacy and new ID formats, though migration to new IDs is recommended.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the dataset definition for a brand template, returning all autofill data field names and their accepted types (images, text, or charts). Use this to understand what data fields are available before submitting a design autofill job."""

    # Construct request model with validation
    try:
        _request = _models.GetBrandTemplateDatasetRequest(
            path=_models.GetBrandTemplateDatasetRequestPath(brand_template_id=brand_template_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_brand_template_dataset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_brand_template_dataset", "GET", "/v1/brand-templates/{brandTemplateId}/dataset", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/brand-templates/{brandTemplateId}/dataset", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/brand-templates/{brandTemplateId}/dataset"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_brand_template_dataset")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_brand_template_dataset",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: comment
@mcp.tool()
async def list_thread_replies(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design containing the comment thread.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    thread_id: str = Field(..., alias="threadId", description="The unique identifier of the comment or suggestion thread whose replies you want to retrieve.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['KeAbiEAjZEj']),
    limit: int | None = Field(50, description="The maximum number of replies to return in a single response. Accepts values between 1 and 100, defaulting to 50 if not specified.", ge=1, le=100)
) -> dict[str, Any]:
    """Retrieves a paginated list of replies for a specific comment or suggestion thread on a design. Useful for reading threaded conversations associated with a given design."""

    # Construct request model with validation
    try:
        _request = _models.ListRepliesRequest(
            path=_models.ListRepliesRequestPath(design_id=design_id, thread_id=thread_id),
            query=_models.ListRepliesRequestQuery(limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_thread_replies: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_thread_replies", "GET", "/v1/designs/{designId}/comments/{threadId}/replies", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/comments/{threadId}/replies", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/comments/{threadId}/replies"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_thread_replies")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_thread_replies",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: comment
@mcp.tool()
async def create_comment_reply(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design that contains the comment thread.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    thread_id: str = Field(..., alias="threadId", description="The unique identifier of the comment thread to reply to. This ID is returned when a thread is created or can be found in the thread_id field of any existing reply within the thread.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['KeAbiEAjZEj']),
    message_plaintext: str = Field(..., description="The plaintext content of the reply as it will appear in the Canva UI. To mention a user, embed their User ID and Team ID inline using the format [user_id:team_id]. Must be between 1 and 2048 characters.", min_length=1, max_length=2048, examples=['Thanks!'])
) -> dict[str, Any]:
    """Posts a reply to an existing comment or suggestion thread on a Canva design. Each thread supports up to 100 replies, and users can be mentioned in the message using a structured ID format."""

    # Construct request model with validation
    try:
        _request = _models.CreateReplyRequest(
            path=_models.CreateReplyRequestPath(design_id=design_id, thread_id=thread_id),
            body=_models.CreateReplyRequestBody(message_plaintext=message_plaintext)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_comment_reply: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("create_comment_reply", "POST", "/v1/designs/{designId}/comments/{threadId}/replies", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/comments/{threadId}/replies", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/comments/{threadId}/replies"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("create_comment_reply")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_comment_reply",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: comment
@mcp.tool()
async def get_comment_thread(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design containing the comment thread.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    thread_id: str = Field(..., alias="threadId", description="The unique identifier of the comment thread to retrieve.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['KeAbiEAjZEj'])
) -> dict[str, Any]:
    """Retrieves a comment or suggestion thread on a specific design. To fetch replies within a thread, use the Get Reply API instead."""

    # Construct request model with validation
    try:
        _request = _models.GetThreadRequest(
            path=_models.GetThreadRequestPath(design_id=design_id, thread_id=thread_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_comment_thread: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_comment_thread", "GET", "/v1/designs/{designId}/comments/{threadId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/comments/{threadId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/comments/{threadId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_comment_thread")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_comment_thread",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: comment
@mcp.tool()
async def get_comment_reply(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design containing the comment thread.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    thread_id: str = Field(..., alias="threadId", description="The unique identifier of the comment or suggestion thread that contains the reply.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['KeAbiEAjZEj']),
    reply_id: str = Field(..., alias="replyId", description="The unique identifier of the reply to retrieve within the specified thread.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['KeAZEAjijEb'])
) -> dict[str, Any]:
    """Retrieves a specific reply from a comment or suggestion thread on a Canva design. Useful for fetching the full details of a single reply by its ID within a given thread."""

    # Construct request model with validation
    try:
        _request = _models.GetReplyRequest(
            path=_models.GetReplyRequestPath(design_id=design_id, thread_id=thread_id, reply_id=reply_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_comment_reply: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_comment_reply", "GET", "/v1/designs/{designId}/comments/{threadId}/replies/{replyId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/comments/{threadId}/replies/{replyId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/comments/{threadId}/replies/{replyId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_comment_reply")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_comment_reply",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: comment
@mcp.tool()
async def create_comment_thread(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design on which to create the comment thread.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    message_plaintext: str = Field(..., description="The plaintext body of the comment as displayed in the Canva UI. To mention a user inline, use the format `[user_id:team_id]`. If an assignee is specified, the assignee must be mentioned in this message. Must be between 1 and 2048 characters.", min_length=1, max_length=2048, examples=['Great work [oUnPjZ2k2yuhftbWF7873o:oBpVhLW22VrqtwKgaayRbP]!']),
    assignee_id: str | None = Field(None, description="The User ID of the Canva user to assign this comment to. The specified user must also be mentioned in the message body using the `[user_id:team_id]` format.", examples=['oUnPjZ2k2yuhftbWF7873o'])
) -> dict[str, Any]:
    """Creates a new comment thread on a specified Canva design. Supports mentioning users inline and optionally assigning the comment to a specific team member."""

    # Construct request model with validation
    try:
        _request = _models.CreateThreadRequest(
            path=_models.CreateThreadRequestPath(design_id=design_id),
            body=_models.CreateThreadRequestBody(message_plaintext=message_plaintext, assignee_id=assignee_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_comment_thread: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("create_comment_thread", "POST", "/v1/designs/{designId}/comments", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/comments", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/comments"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("create_comment_thread")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_comment_thread",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: connect
@mcp.tool()
async def list_webhook_signing_keys() -> dict[str, Any]:
    """Retrieves the JSON Web Keys (JWK) used by Canva to sign webhook payloads, enabling you to verify webhook authenticity and protect against replay attacks. Cache returned keys and only call this endpoint when a webhook arrives signed with an unrecognized key."""

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_webhook_signing_keys", "GET", "/v1/connect/keys", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/connect/keys"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_webhook_signing_keys")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_webhook_signing_keys",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: design
@mcp.tool()
async def list_designs(
    query: str | None = Field(None, description="A search term or phrase to filter designs by name or content, matching designs owned by or shared with the user. Accepts up to 255 characters.", max_length=255, examples=['party invites']),
    ownership: Literal["any", "owned", "shared"] | None = Field('any', description="Filters results by the user's ownership relationship to the designs — all accessible designs, only designs the user owns, or only designs shared with the user.", examples=['any']),
    sort_by: Literal["relevance", "modified_descending", "modified_ascending", "title_descending", "title_ascending"] | None = Field('relevance', description="Determines the order in which designs are returned, supporting relevance to the search query, last-modified date (ascending or descending), or alphabetical title (ascending or descending).", examples=['relevance']),
    limit: int | None = Field(25, description="The maximum number of designs to return in a single response, between 1 and 100 inclusive.", ge=1, le=100, json_schema_extra={'format': 'int32'})
) -> dict[str, Any]:
    """Retrieves metadata for all designs in the authenticated Canva user's projects, with optional filtering by search term or ownership, sorting, and result limiting."""

    # Construct request model with validation
    try:
        _request = _models.ListDesignsRequest(
            query=_models.ListDesignsRequestQuery(query=query, ownership=ownership, sort_by=sort_by, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_designs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_designs", "GET", "/v1/designs", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/designs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_designs")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_designs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: design
@mcp.tool()
async def create_design(
    design_type: str | None = Field(None, description="The preset design type to use as the basis for the new design (e.g., presentation, social media post). Use this or custom dimensions to define the design format. Pass as a JSON-encoded string with a \"type\" discriminator field, e.g. '{\"type\": \"value\", ...}'."),
    asset_id: str | None = Field(None, description="The ID of an image asset from the user's projects to insert into the new design. Only image assets are currently supported; use the List Folder Items API to retrieve asset IDs.", examples=['Msd59349ff']),
    title: str | None = Field(None, description="The display name for the new design, between 1 and 255 characters.", min_length=1, max_length=255, examples=['My Holiday Presentation'])
) -> dict[str, Any]:
    """Creates a new Canva design using either a preset design type or custom dimensions, optionally pre-populated with an image asset from the user's projects. Note that blank designs not edited within 7 days are permanently deleted, bypassing the trash."""

    # Deserialize discriminated union parameters from JSON strings (MCP passes complex types as strings)
    _design_type: Any = json.loads(design_type) if isinstance(design_type, str) else design_type

    # Construct request model with validation
    try:
        _request = _models.CreateDesignRequest(
            body=_models.CreateDesignRequestBody(design_type=_design_type, asset_id=asset_id, title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_design: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("create_design", "POST", "/v1/designs", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/designs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("create_design")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_design",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: design
@mcp.tool()
async def get_design(design_id: str = Field(..., alias="designId", description="The unique identifier of the design to retrieve. Must be 1–50 alphanumeric characters, hyphens, or underscores.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves metadata for a specific design, including owner information, edit and view URLs, and thumbnail details."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignRequest(
            path=_models.GetDesignRequestPath(design_id=design_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_design: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_design", "GET", "/v1/designs/{designId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_design")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_design",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: design
@mcp.tool()
async def list_design_pages(
    design_id: str = Field(..., alias="designId", description="The unique identifier of the design whose pages you want to retrieve.", pattern="^[a-zA-Z0-9_-]{1,50}$"),
    offset: int | None = Field(1, description="The one-based page index at which to start the returned range; the first page in a design is index 1. Accepts values between 1 and 500.", ge=1, le=500, json_schema_extra={'format': 'int32'}),
    limit: int | None = Field(50, description="The number of pages to return starting from the index specified by offset. Accepts values between 1 and 200.", ge=1, le=200, json_schema_extra={'format': 'int32'})
) -> dict[str, Any]:
    """Retrieves metadata for pages within a specified design, including page-specific thumbnails. Supports paginated access via offset and limit; note that some design types such as Canva docs do not have pages."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignPagesRequest(
            path=_models.GetDesignPagesRequestPath(design_id=design_id),
            query=_models.GetDesignPagesRequestQuery(offset=offset, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_design_pages: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_design_pages", "GET", "/v1/designs/{designId}/pages", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/pages", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/pages"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_design_pages")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_design_pages",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: design
@mcp.tool()
async def list_design_export_formats(design_id: str = Field(..., alias="designId", description="The unique identifier of the design whose supported export formats you want to retrieve.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the available file export formats for a specific design. The formats returned reflect only those supported by every page type within the design, so results may vary based on design type and content."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignExportFormatsRequest(
            path=_models.GetDesignExportFormatsRequestPath(design_id=design_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_design_export_formats: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_design_export_formats", "GET", "/v1/designs/{designId}/export-formats", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/designs/{designId}/export-formats", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/designs/{designId}/export-formats"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_design_export_formats")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_design_export_formats",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: design_import
@mcp.tool()
async def import_design(
    import_metadata: _models.CreateDesignImportJobHeaderImportMetadata = Field(..., alias="Import-Metadata", description="Metadata describing the design being imported, supplied as a request header. Includes details such as the file name and target design type."),
    body: str | None = Field(None, description="The raw binary content of the file to import as a new Canva design. Must be a supported file type as defined in the Design imports overview.", json_schema_extra={'format': 'binary'})
) -> dict[str, Any]:
    """Starts an asynchronous job to import an external file as a new design in Canva. The file is sent as a binary octet-stream body, with import details provided via the Import-Metadata header."""

    # Construct request model with validation
    try:
        _request = _models.CreateDesignImportJobRequest(
            header=_models.CreateDesignImportJobRequestHeader(import_metadata=import_metadata),
            body=_models.CreateDesignImportJobRequestBody(body=body)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for import_design: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("import_design", "POST", "/v1/imports", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/imports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_body = next(iter(_http_body.values()), None) if _http_body else None
    _http_headers = _request.header.model_dump(by_alias=True, exclude_none=True) if _request.header else {}
    _http_headers = _encode_content_params(_http_headers, {
        "Import-Metadata": "application/json",
    })

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("import_design")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="import_design",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: design_import
@mcp.tool()
async def get_design_import_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the design import job to retrieve, obtained when the import job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['f81b26fd-a33d-4c2d-9e8c-4a7aca798b17'])) -> dict[str, Any]:
    """Retrieves the current status and result of an asynchronous design import job. Poll this endpoint until a `success` or `failed` status is returned."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignImportJobRequest(
            path=_models.GetDesignImportJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_design_import_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_design_import_job", "GET", "/v1/imports/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/imports/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/imports/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_design_import_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_design_import_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: design_import
@mcp.tool()
async def import_design_from_url(
    title: str = Field(..., description="The display title for the newly created design, between 1 and 255 characters.", min_length=1, max_length=255, examples=['My Awesome Design']),
    url: str = Field(..., description="The publicly accessible URL of the file to import; must be reachable from the internet and between 1 and 2048 characters.", min_length=1, max_length=2048),
    mime_type: str | None = Field(None, description="The MIME type of the file being imported, between 1 and 100 characters; if omitted, Canva will attempt to detect the file type automatically.", min_length=1, max_length=100, examples=['application/vnd.apple.keynote'])
) -> dict[str, Any]:
    """Starts an asynchronous job to fetch a file from a public URL and import it as a new design in Canva. Use the Get URL import job API to poll for job status and retrieve the resulting design once processing is complete."""

    # Construct request model with validation
    try:
        _request = _models.CreateUrlImportJobRequest(
            body=_models.CreateUrlImportJobRequestBody(title=title, url=url, mime_type=mime_type)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for import_design_from_url: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("import_design_from_url", "POST", "/v1/url-imports", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/url-imports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("import_design_from_url")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="import_design_from_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: design_import
@mcp.tool()
async def get_url_import_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the URL import job to retrieve, obtained when the job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['f81b26fd-a33d-4c2d-9e8c-4a7aca798b17'])) -> dict[str, Any]:
    """Retrieves the current status and result of an asynchronous URL import job. Poll this endpoint until a `success` or `failed` status is returned, as the job may not complete immediately."""

    # Construct request model with validation
    try:
        _request = _models.GetUrlImportJobRequest(
            path=_models.GetUrlImportJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_url_import_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_url_import_job", "GET", "/v1/url-imports/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/url-imports/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/url-imports/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_url_import_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_url_import_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: export
@mcp.tool()
async def export_design(
    design_id: str | None = Field(None, description="The unique identifier of the Canva design to export."),
    format_: str | None = Field(None, alias="format", description="An object specifying the desired export format and any format-specific options; supported formats are PDF (`pdf`), JPG (`jpg`), PNG (`png`), GIF (`gif`), Microsoft PowerPoint (`pptx`), and MP4 (`mp4`). Pass as a JSON-encoded string with a \"type\" discriminator field, e.g. '{\"type\": \"value\", ...}'.")
) -> dict[str, Any]:
    """Starts an asynchronous job to export a Canva design to a specified file format (PDF, JPG, PNG, GIF, PPTX, or MP4), returning download URLs valid for 24 hours once the job completes. Use the Get design export job API to poll for job status and retrieve results."""

    # Deserialize discriminated union parameters from JSON strings (MCP passes complex types as strings)
    _format_: Any = json.loads(format_) if isinstance(format_, str) else format_

    # Construct request model with validation
    try:
        _request = _models.CreateDesignExportJobRequest(
            body=_models.CreateDesignExportJobRequestBody(design_id=design_id, format_=_format_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for export_design: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("export_design", "POST", "/v1/exports", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/exports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("export_design")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="export_design",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: export
@mcp.tool()
async def get_design_export_job(export_id: str = Field(..., alias="exportId", description="The unique identifier of the export job to retrieve, obtained from the Create design export job response.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the status and result of a design export job, including download URLs for each page when the job succeeds. Poll this endpoint until a 'success' or 'failed' status is returned, as export jobs are processed asynchronously."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignExportJobRequest(
            path=_models.GetDesignExportJobRequestPath(export_id=export_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_design_export_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_design_export_job", "GET", "/v1/exports/{exportId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/exports/{exportId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/exports/{exportId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_design_export_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_design_export_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def get_folder(folder_id: str = Field(..., alias="folderId", description="The unique identifier of the folder to retrieve. Accepts alphanumeric characters, hyphens, and underscores, up to 50 characters in length.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['FAF2lZtloor'])) -> dict[str, Any]:
    """Retrieves the name and metadata details of a specific folder. Use this to look up folder information by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.GetFolderRequest(
            path=_models.GetFolderRequestPath(folder_id=folder_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_folder", "GET", "/v1/folders/{folderId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/folders/{folderId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/folders/{folderId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_folder")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_folder",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def rename_folder(
    folder_id: str = Field(..., alias="folderId", description="The unique identifier of the folder to update.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['FAF2lZtloor']),
    name: str = Field(..., description="The new display name for the folder, as it will appear in the Canva UI. Must be between 1 and 255 characters.", min_length=1, max_length=255, examples=['My awesome holiday'])
) -> dict[str, Any]:
    """Updates the name of an existing folder in Canva. Currently, only the folder name can be modified."""

    # Construct request model with validation
    try:
        _request = _models.UpdateFolderRequest(
            path=_models.UpdateFolderRequestPath(folder_id=folder_id),
            body=_models.UpdateFolderRequestBody(name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for rename_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("rename_folder", "PATCH", "/v1/folders/{folderId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/folders/{folderId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/folders/{folderId}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("rename_folder")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="rename_folder",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def delete_folder(folder_id: str = Field(..., alias="folderId", description="The unique identifier of the folder to delete. Accepts alphanumeric characters, hyphens, and underscores, up to 50 characters.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['FAF2lZtloor'])) -> dict[str, Any]:
    """Permanently deletes the specified folder. The folder owner's content is moved to Trash, while content owned by other users is moved to the root level of their respective projects."""

    # Construct request model with validation
    try:
        _request = _models.DeleteFolderRequest(
            path=_models.DeleteFolderRequestPath(folder_id=folder_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("delete_folder", "DELETE", "/v1/folders/{folderId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/folders/{folderId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/folders/{folderId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("delete_folder")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_folder",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def list_folder_items(
    folder_id: str = Field(..., alias="folderId", description="The unique identifier of the folder whose contents you want to list.", pattern="^[a-zA-Z0-9_-]{1,50}$", examples=['FAF2lZtloor']),
    limit: int | None = Field(50, description="The maximum number of folder items to return per request, between 1 and 100.", ge=1, le=100, json_schema_extra={'format': 'int32'}),
    item_types: list[Literal["design", "folder", "image"]] | None = Field(None, description="Restricts results to one or more item types; accepted values are `design`, `folder`, and `image`, supplied as a comma-delimited list. Order is not significant."),
    sort_by: Literal["created_ascending", "created_descending", "modified_ascending", "modified_descending", "title_ascending", "title_descending"] | None = Field('modified_descending', description="Determines the order in which folder items are returned; choose from ascending or descending variants of creation date, modification date, or title.", examples=['title_ascending']),
    pin_status: Literal["any", "pinned"] | None = Field('any', description="Filters results by pinned status; use `pinned` to return only pinned items, or `any` to return all items regardless of pin status.")
) -> dict[str, Any]:
    """Retrieves the contents of a specified folder, including nested folders, designs, and image assets, each with its type indicated. Supports filtering by item type and pinned status, as well as sorting and pagination."""

    # Construct request model with validation
    try:
        _request = _models.ListFolderItemsRequest(
            path=_models.ListFolderItemsRequestPath(folder_id=folder_id),
            query=_models.ListFolderItemsRequestQuery(limit=limit, item_types=item_types, sort_by=sort_by, pin_status=pin_status)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_folder_items: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_folder_items", "GET", "/v1/folders/{folderId}/items", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/folders/{folderId}/items", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/folders/{folderId}/items"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_query = _serialize_query(_http_query, {
        "item_types": ("form", False),
    })
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_folder_items")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_folder_items",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def move_folder_item(
    to_folder_id: str | None = Field(None, description="The ID of the destination folder to move the item into. Use the special value `root` to move the item to the top level of the user's projects.", min_length=1, max_length=50, examples=['FAF2lZtloor']),
    item_id: str | None = Field(None, description="The ID of the item to move. Video assets are not currently supported.", min_length=1, max_length=50, examples=['Msd59349ff'])
) -> dict[str, Any]:
    """Moves an item to a specified destination folder within Canva. Note that items existing in multiple folders cannot be moved via API and must be moved through the Canva UI instead."""

    # Construct request model with validation
    try:
        _request = _models.MoveFolderItemRequest(
            body=_models.MoveFolderItemRequestBody(to_folder_id=to_folder_id, item_id=item_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for move_folder_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("move_folder_item", "POST", "/v1/folders/move", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/folders/move"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("move_folder_item")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="move_folder_item",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: folder
@mcp.tool()
async def create_folder(
    name: str = Field(..., description="The display name for the new folder, between 1 and 255 characters.", min_length=1, max_length=255, examples=['My awesome holiday']),
    parent_folder_id: str = Field(..., description="The ID of the parent folder where the new folder will be created. Use `root` to place it at the top level of the user's projects, or `uploads` to place it inside their Uploads folder; otherwise provide an existing folder's ID.", min_length=1, max_length=50, examples=['FAF2lZtloor'])
) -> dict[str, Any]:
    """Creates a new folder in a user's Canva projects, Uploads folder, or inside an existing folder. Returns the new folder's ID and metadata upon success."""

    # Construct request model with validation
    try:
        _request = _models.CreateFolderRequest(
            body=_models.CreateFolderRequestBody(name=name, parent_folder_id=parent_folder_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("create_folder", "POST", "/v1/folders", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/folders"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("create_folder")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_folder",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: oauth
@mcp.tool()
async def exchange_oauth_token(body: str | None = Field(None, description="Request body containing the OAuth grant details, including the grant type (authorization_code or refresh_token), associated code or token, PKCE verifier, and optionally client credentials if not using Basic authentication. Pass as a JSON-encoded string with a \"grant_type\" discriminator field, e.g. '{\"grant_type\": \"value\", ...}'.")) -> dict[str, Any]:
    """Exchanges an authorization code or refresh token for an access token using the OAuth 2.0 Authorization Code flow with PKCE, enabling your integration to act on behalf of a user. Must be called from your backend using client credentials via Basic authentication or body parameters."""

    # Deserialize discriminated union parameters from JSON strings (MCP passes complex types as strings)
    _body: Any = json.loads(body) if isinstance(body, str) else body

    # Construct request model with validation
    try:
        _request = _models.ExchangeAccessTokenRequest(
            body=_models.ExchangeAccessTokenRequestBody(body=_body)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for exchange_oauth_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("exchange_oauth_token", "POST", "/v1/oauth/token", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/oauth/token"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_body = next(iter(_http_body.values()), None) if _http_body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("exchange_oauth_token")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="exchange_oauth_token",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="application/x-www-form-urlencoded",
        headers=_http_headers,
    )

    return _response_data

# Tags: oauth
@mcp.tool()
async def introspect_token(token: str = Field(..., description="The OAuth access token string to validate and inspect for active status, claims, and scope information.", examples=['JagALLazU0i2ld9WW4zTO4kaG0lkvP8Y5sSO206ZwxNF4E1y3xKJKF7TzN17BXTfaNOeY0P88AeRCE6cRF7SJzvf3Sx97rA80sGHtFplFo'])) -> dict[str, Any]:
    """Validates an OAuth access token and returns its active status along with associated metadata such as claims, scopes, and validity times. Must be called from a backend integration using client credentials, as browser-based requests are blocked by CORS policy."""

    # Construct request model with validation
    try:
        _request = _models.IntrospectTokenRequest(
            body=_models.IntrospectTokenRequestBody(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for introspect_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("introspect_token", "POST", "/v1/oauth/introspect", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/oauth/introspect"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("introspect_token")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="introspect_token",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="application/x-www-form-urlencoded",
        headers=_http_headers,
    )

    return _response_data

# Tags: oauth
@mcp.tool()
async def revoke_token(token: str = Field(..., description="The OAuth access token or refresh token to revoke. Revoking a refresh token additionally revokes all associated access tokens and the user's integration consent.", examples=['agALLazU0i2ld9WW4zTO4kaG0lkvP8Y5sSO206ZwxNF4E1y3xKJKF7TzN17BXTfaNOeY0P88AeRCE6cRF7SJzvf3Sx97rA80sGHtFplFo'])) -> dict[str, Any]:
    """Revokes an OAuth access token or refresh token. Revoking a refresh token also invalidates its entire lineage of access tokens and removes the user's consent for the integration, requiring them to re-authorize via OAuth."""

    # Construct request model with validation
    try:
        _request = _models.RevokeTokensRequest(
            body=_models.RevokeTokensRequestBody(token=token)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for revoke_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("revoke_token", "POST", "/v1/oauth/revoke", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/oauth/revoke"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("revoke_token")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="revoke_token",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="application/x-www-form-urlencoded",
        headers=_http_headers,
    )

    return _response_data

# Tags: resize
@mcp.tool()
async def resize_design(
    design_id: str | None = Field(None, description="The unique identifier of the design to be resized. Note that Canva docs and Canva Code designs cannot be resized."),
    design_type: str | None = Field(None, description="The target design type for the resized copy, specified as either a preset design type or custom width and height dimensions. Cannot target Canva doc or Canva Code design types. Pass as a JSON-encoded string with a \"type\" discriminator field, e.g. '{\"type\": \"value\", ...}'.")
) -> dict[str, Any]:
    """Starts an asynchronous job to create a resized copy of an existing design, adding the new design to the user's root folder. Supports resizing via a preset design type or custom dimensions (up to 25,000,000 pixels squared); requires a Canva plan with premium features."""

    # Deserialize discriminated union parameters from JSON strings (MCP passes complex types as strings)
    _design_type: Any = json.loads(design_type) if isinstance(design_type, str) else design_type

    # Construct request model with validation
    try:
        _request = _models.CreateDesignResizeJobRequest(
            body=_models.CreateDesignResizeJobRequestBody(design_id=design_id, design_type=_design_type)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for resize_design: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("resize_design", "POST", "/v1/resizes", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/resizes"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("resize_design")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="resize_design",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: resize
@mcp.tool()
async def get_design_resize_job(job_id: str = Field(..., alias="jobId", description="The unique identifier of the design resize job to retrieve, obtained when the resize job was created.", pattern="^[a-zA-Z0-9_-]{1,50}$")) -> dict[str, Any]:
    """Retrieves the status and result of an asynchronous design resize job. Poll this endpoint until a success or failed status is returned; on success, the response includes metadata for the newly resized design."""

    # Construct request model with validation
    try:
        _request = _models.GetDesignResizeJobRequest(
            path=_models.GetDesignResizeJobRequestPath(job_id=job_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_design_resize_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_design_resize_job", "GET", "/v1/resizes/{jobId}", _request_id)

    # Extract parameters for API call
    _http_path = _build_path("/v1/resizes/{jobId}", _request.path.model_dump(by_alias=True)) if _request.path else "/v1/resizes/{jobId}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_design_resize_job")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_design_resize_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: user
@mcp.tool()
async def get_current_user() -> dict[str, Any]:
    """Retrieves the User ID and Team ID associated with the authenticated account linked to the provided access token. Useful for resolving the caller's identity without requiring explicit user credentials."""

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_current_user", "GET", "/v1/users/me", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/users/me"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_current_user")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_current_user",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: user
@mcp.tool()
async def list_user_capabilities() -> dict[str, Any]:
    """Retrieves the API capabilities available to the authenticated user account associated with the provided access token. Use this to determine which features and endpoints the current user is permitted to access."""

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("list_user_capabilities", "GET", "/v1/users/me/capabilities", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/users/me/capabilities"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("list_user_capabilities")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_user_capabilities",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: user
@mcp.tool()
async def get_current_user_profile() -> dict[str, Any]:
    """Retrieves the profile information for the authenticated user associated with the provided access token. Currently returns the display name, with additional user details planned for future inclusion."""

    # Generate request ID
    _request_id = str(uuid.uuid4())

    # Log invocation
    _log_tool_invocation("get_current_user_profile", "GET", "/v1/users/me/profile", _request_id)

    # Extract parameters for API call
    _http_path = "/v1/users/me/profile"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = _get_auth_for_operation("get_current_user_profile")
    _http_headers.update(_auth.get("headers", {}))

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_current_user_profile",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# ============================================================================
# Schema Optimization
# ============================================================================

def _optimize_tool_schemas() -> None:
    """Simplify anyOf nullable wrappers in tool schemas for LLM token efficiency.

    Pydantic generates {"anyOf": [{"type": "T"}, {"type": "null"}], "default": null}
    for `T | None = None` params. Both Anthropic and OpenAI recommend the simpler
    {"type": "T"} with the param outside `required` for optimal tool-calling accuracy
    and minimal token overhead. Runtime validation is unaffected — Python signatures
    retain the `T | None` annotation.
    """
    from fastmcp.tools import Tool as _ToolCls
    # v3: _local_provider._components, v2: _tool_manager._tools
    _src = getattr(mcp, "_local_provider", None)
    if _src is not None:
        _tools = [v for v in _src._components.values() if isinstance(v, _ToolCls)]
    else:
        _tools = list(mcp._tool_manager._tools.values())  # type: ignore[attr-defined]
    for _tool in _tools:
        _simplify_schema(_tool.parameters)

def _simplify_schema(schema: dict) -> None:
    """Walk a JSON Schema dict and collapse anyOf-nullable patterns in place."""
    for _def_schema in schema.get("$defs", {}).values():
        _simplify_schema(_def_schema)
    _props = schema.get("properties", {})
    _required = set(schema.get("required", []))
    for _name, _prop in _props.items():
        if "anyOf" in _prop:
            _non_null = [b for b in _prop["anyOf"] if b.get("type") != "null"]
            if len(_non_null) == 1:
                _desc = _prop.get("description")
                _prop.clear()
                _prop.update(_non_null[0])
                if _desc:
                    _prop["description"] = _desc
                _required.discard(_name)
    if _required:
        schema["required"] = sorted(_required)
    elif "required" in schema:
        del schema["required"]

_optimize_tool_schemas()

# ============================================================================
# Environment Validation
# ============================================================================

def validate_environment() -> None:
    """Validate required environment variables at startup."""
    errors: list[str] = []
    warnings: list[str] = []

    required_vars: list[tuple[str, str, str | None]] = [
    ]

    for var_name, description, default in required_vars:
        value = os.getenv(var_name, default)
        if not value:
            errors.append(f"  - {var_name}: {description}")

    if errors:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Missing required environment variables", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        for error in errors:
            print(error, file=sys.stderr)
        print("\nServer startup aborted. Set required variables and restart.", file=sys.stderr)
        print("\nExample:", file=sys.stderr)
        print("  python canva_connect_api_server.py", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    _validate_port()
    _validate_timeout()
    _validate_rate_limit()

    logger = logging.getLogger(__name__)
    logger.info("Environment validation passed")
    for warning in warnings:
        logger.warning(warning)

def _validate_port() -> None:
    port_str = os.getenv('PORT')
    if not port_str:
        return  # Optional, has CLI default

    port: int
    try:
        port = int(port_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid PORT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port_str}", file=sys.stderr)
        print("\nPORT must be an integer between 1 and 65535", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if port < 1 or port > 65535:
        print("=" * 70, file=sys.stderr)
        print("ERROR: PORT out of valid range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port}", file=sys.stderr)
        print("\nPORT must be between 1 and 65535", file=sys.stderr)
        print("  Common ports: 8000, 8080, 3000", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_timeout() -> None:
    timeout_str = os.getenv('API_TIMEOUT')
    if not timeout_str:
        return  # Optional, has default

    timeout: int
    try:
        timeout = int(timeout_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid API_TIMEOUT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout_str}", file=sys.stderr)
        print("\nAPI_TIMEOUT must be a positive integer (seconds)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if timeout < 1 or timeout > 3600:
        print("=" * 70, file=sys.stderr)
        print("ERROR: API_TIMEOUT out of reasonable range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout} seconds", file=sys.stderr)
        print("\nAPI_TIMEOUT must be between 1 and 3600 seconds (1 hour max)", file=sys.stderr)
        print("  Recommended: 30 (most APIs), 60 (slower APIs), 300 (long operations)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_rate_limit() -> None:
    rate_str = os.getenv('RATE_LIMIT')
    if not rate_str:
        return  # Optional, has default

    try:
        rate = float(rate_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid RATE_LIMIT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate_str}", file=sys.stderr)
        print("\nRATE_LIMIT must be a positive number (requests per second)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if rate <= 0:
        print("=" * 70, file=sys.stderr)
        print("ERROR: RATE_LIMIT must be positive", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate}", file=sys.stderr)
        print("\nRATE_LIMIT must be greater than 0", file=sys.stderr)
        print("  Recommended: 1-100 requests/second depending on API limits", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

# ============================================================================
# Main Entry Point
# ============================================================================

def main():
    """CLI entry point with runtime transport selection."""

    validate_environment()

    parser = argparse.ArgumentParser(description="Canva Connect Api MCP Server")

    parser.add_argument(
        '--transport',
        choices=['stdio', 'sse', 'streamable-http'],
        default='stdio',
        help='Transport protocol: stdio (default, for Claude Code), sse (remote capable), streamable-http (production)'
    )
    parser.add_argument(
        '--host',
        default='0.0.0.0',  # noqa: S104
        help='Host address for HTTP transports (default: 0.0.0.0)'
    )
    parser.add_argument(
        '--port',
        type=int,
        default=8000,
        help='Port for HTTP transports (default: 8000)'
    )
    parser.add_argument(
        '--log-level',
        choices=['DEBUG', 'INFO', 'WARNING', 'ERROR'],
        default=LOG_LEVEL,
        help='Logging level (default: from LOG_LEVEL env var, or INFO)'
    )
    parser.add_argument(
        '--retry',
        action='store_true',
        help='Enable automatic retry with exponential backoff (default: enabled)'
    )
    parser.add_argument(
        '--no-retry',
        action='store_true',
        help='Disable automatic retry'
    )
    parser.add_argument(
        '--max-retries',
        type=int,
        default=MAX_RETRIES,
        help='Maximum retry attempts (default: from MAX_RETRIES env var, or 3)'
    )
    parser.add_argument(
        '--retry-base-delay',
        type=int,
        default=RETRY_BACKOFF_FACTOR,
        help='Base delay for exponential backoff in seconds (default: from RETRY_BACKOFF_FACTOR env var, or 2)'
    )
    parser.add_argument(
        '--retry-max-delay',
        type=int,
        default=60,
        help='Maximum retry delay in seconds (default: 60)'
    )
    parser.add_argument(
        '--timeout',
        type=int,
        default=None,
        help='API request timeout in seconds (default: 30, or API_TIMEOUT env var)'
    )
    parser.add_argument(
        '--rate-limiting',
        choices=['disabled', 'local'],
        default='local',
        help='Rate limiting mode: disabled (default) or local (in-memory token bucket)'
    )
    parser.add_argument(
        '--rate-limit',
        type=int,
        default=RATE_LIMIT_REQUESTS_PER_SECOND,
        help='Rate limit in requests per second (default: from RATE_LIMIT_REQUESTS_PER_SECOND env var, or 10)'
    )
    parser.add_argument(
        '--burst',
        type=int,
        help='Burst capacity for rate limiter (default: 2x rate-limit)'
    )
    parser.add_argument(
        '--circuit-breaker',
        action='store_true',
        help='Enable circuit breaker to prevent cascading failures'
    )
    parser.add_argument(
        '--circuit-breaker-failure-threshold',
        type=int,
        default=CIRCUIT_BREAKER_FAILURE_THRESHOLD,
        help='Failures before opening circuit (default: from CIRCUIT_BREAKER_FAILURE_THRESHOLD env var, or 5)'
    )
    parser.add_argument(
        '--circuit-breaker-timeout',
        type=float,
        default=CIRCUIT_BREAKER_TIMEOUT_SECONDS,
        help='Seconds before recovery attempt (default: from CIRCUIT_BREAKER_TIMEOUT_SECONDS env var, or 60.0)'
    )
    args = parser.parse_args()

    logging.basicConfig(
        level=getattr(logging, args.log_level),
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )

    logger = logging.getLogger(__name__)
    logger.info("Starting Canva Connect Api MCP Server")
    logger.info(f"Transport: {args.transport}")

    global retry_config, rate_limiter, circuit_breaker, DEFAULT_TIMEOUT

    if args.timeout is not None:
        DEFAULT_TIMEOUT = args.timeout
    else:
        timeout_env = os.getenv('API_TIMEOUT')
        if timeout_env:
            try:
                DEFAULT_TIMEOUT = int(timeout_env)
            except ValueError:
                logger.warning(f"Invalid API_TIMEOUT '{timeout_env}', using default {DEFAULT_TIMEOUT}s")
    logger.info(f"Timeout: {DEFAULT_TIMEOUT}s")

    if not args.no_retry:
        retry_config = RetryConfig(
            max_attempts=args.max_retries,
            base_delay=args.retry_base_delay,
            max_delay=args.retry_max_delay
        )
        logger.info(
            f"Retry: Enabled (max attempts: {retry_config.max_attempts}, "
            f"base delay: {retry_config.base_delay}s, max delay: {retry_config.max_delay}s)"
        )
    else:
        logger.info("Retry: Disabled")

    rate_limiting_mode = args.rate_limiting

    if rate_limiting_mode == 'disabled':
        rate_limiter = None
        logger.info("Rate limiting: Disabled")
    elif rate_limiting_mode == 'local':
        rate_limit = args.rate_limit if args.rate_limit else 10.0
        burst = args.burst if args.burst else int(rate_limit * 2)
        rate_limiter = TokenBucket(rate=rate_limit, capacity=burst)
        logger.info(f"Rate limiting: Local (rate: {rate_limit} req/sec, burst: {burst})")
    else:
        logger.warning(f"Unknown rate limiting mode: {rate_limiting_mode}, disabling")
        rate_limiter = None

    if args.circuit_breaker:
        cb_config = CircuitBreakerConfig(
            failure_threshold=args.circuit_breaker_failure_threshold,
            timeout=args.circuit_breaker_timeout
        )
        circuit_breaker = CircuitBreaker(cb_config)
        logger.info(
            f"Circuit breaker: Enabled (failure threshold: {cb_config.failure_threshold}, "
            f"timeout: {cb_config.timeout}s)"
        )
    else:
        logger.info("Circuit breaker: Disabled")
    try:
        if args.transport == 'stdio':
            logger.info("Using stdio transport")
            mcp.run()
        elif args.transport == 'sse':
            logger.info(f"Using SSE transport on http://{args.host}:{args.port}")
            mcp.run(transport='sse', host=args.host, port=args.port)
        elif args.transport == 'streamable-http':
            logger.info(f"Using Streamable HTTP transport on http://{args.host}:{args.port}")
            mcp.run(transport='streamable-http', host=args.host, port=args.port)

    except KeyboardInterrupt:
        logger.info("Server stopped by user")
    except Exception as e:
        logger.error(f"Server error: {e}", exc_info=True)
        raise

if __name__ == "__main__":
    main()
