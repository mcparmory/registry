#!/usr/bin/env python3
"""
PostHog API MCP Server
Generated: 2026-04-11 17:53:40 UTC
Generator: MCP Blacksmith v1.1.0 (https://mcpblacksmith.com)
"""

from __future__ import annotations

import argparse
import asyncio
import json
import logging
import os
import random
import sys
import time
import uuid
from dataclasses import dataclass
from enum import Enum
from pathlib import Path
from typing import Any, Literal, overload

try:
    from dotenv import load_dotenv
    _env_path = Path(__file__).parent / '.env'
    if _env_path.exists():
        load_dotenv(_env_path)
except ImportError:
    pass

import _auth
import _models
import httpx
import pydantic
from fastmcp import FastMCP
from fastmcp.server.middleware import Middleware
from pydantic import Field

BASE_URL = os.getenv("BASE_URL", "")
SERVER_NAME = "PostHog API"
SERVER_VERSION = "1.0.0"

CONNECTION_POOL_SIZE = int(os.getenv("CONNECTION_POOL_SIZE", "100"))
MAX_KEEPALIVE_CONNECTIONS = int(os.getenv("MAX_KEEPALIVE_CONNECTIONS", "20"))

LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO").upper()
if LOG_LEVEL not in ("DEBUG", "INFO", "WARNING", "ERROR"):
    LOG_LEVEL = "INFO"

HTTPX_TIMEOUT = httpx.Timeout(
    connect=float(os.getenv("HTTPX_CONNECT_TIMEOUT", "10.0")),
    read=float(os.getenv("HTTPX_READ_TIMEOUT", "60.0")),
    write=float(os.getenv("HTTPX_WRITE_TIMEOUT", "30.0")),
    pool=float(os.getenv("HTTPX_POOL_TIMEOUT", "5.0"))
)

DEFAULT_TIMEOUT = float(os.getenv("TOOL_EXECUTION_TIMEOUT", "90.0"))

MAX_RETRIES = int(os.getenv("MAX_RETRIES", "3"))
RETRY_BACKOFF_FACTOR = float(os.getenv("RETRY_BACKOFF_FACTOR", "2.0"))
CIRCUIT_BREAKER_FAILURE_THRESHOLD = int(os.getenv("CIRCUIT_BREAKER_FAILURE_THRESHOLD", "5"))
CIRCUIT_BREAKER_TIMEOUT_SECONDS = float(os.getenv("CIRCUIT_BREAKER_TIMEOUT_SECONDS", "60.0"))
RATE_LIMIT_REQUESTS_PER_SECOND = int(os.getenv("RATE_LIMIT_REQUESTS_PER_SECOND", "10"))
client: httpx.AsyncClient | None = None

@dataclass
class RetryConfig:
    """Retry with exponential backoff configuration."""
    max_attempts: int = 3
    base_delay: float = 1.0
    max_delay: float = 60.0
    exponential_base: float = 2.0
    jitter: bool = True
    retry_on_status_codes: list[int] | None = None

    def __post_init__(self):
        if self.retry_on_status_codes is None:
            self.retry_on_status_codes = [429, 500, 502, 503, 504]

class TokenBucket:
    """Token bucket rate limiter."""

    def __init__(self, rate: float, capacity: int):
        self.rate = rate
        self.capacity = capacity
        self.tokens = float(capacity)
        self.last_update = time.monotonic()

    def _refill(self):
        now = time.monotonic()
        elapsed = now - self.last_update
        self.tokens = min(self.capacity, self.tokens + (elapsed * self.rate))
        self.last_update = now

    def consume(self, tokens: int = 1) -> tuple[bool, float | None]:
        self._refill()
        if self.tokens >= tokens:
            self.tokens -= tokens
            return True, None
        tokens_needed = tokens - self.tokens
        return False, tokens_needed / self.rate

class CircuitState(Enum):
    """Circuit breaker states."""
    CLOSED = "closed"      # Normal operation
    OPEN = "open"          # Blocking requests
    HALF_OPEN = "half_open"  # Testing recovery

@dataclass
class CircuitBreakerConfig:
    """Circuit breaker configuration."""
    failure_threshold: int = 5
    success_threshold: int = 2
    timeout: float = 60.0

class CircuitBreakerError(Exception):
    """Circuit breaker is open, request blocked."""
    def __init__(self, message: str, retry_after: float | None = None):
        super().__init__(message)
        self.retry_after = retry_after

class CircuitBreaker:
    """Circuit breaker to prevent cascading failures."""

    def __init__(self, config: CircuitBreakerConfig):
        self.config = config
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        self.last_failure_time: float | None = None

    def before_call(self):
        """Check if circuit allows request."""
        if self.state == CircuitState.OPEN:
            if self._should_attempt_reset():
                self._transition_to_half_open()
            else:
                retry_after = self._time_until_recovery()
                raise CircuitBreakerError(
                    "Circuit breaker OPEN due to repeated failures",
                    retry_after=retry_after
                )

    def on_success(self):
        """Handle successful request."""
        if self.state == CircuitState.HALF_OPEN:
            self.success_count += 1
            if self.success_count >= self.config.success_threshold:
                self._transition_to_closed()
        elif self.state == CircuitState.CLOSED:
            if self.failure_count > 0:
                self.failure_count = 0

    def on_failure(self, error: Exception):
        """Handle failed request."""
        logging.debug(f"Circuit breaker recorded failure: {error}")
        if self.state == CircuitState.HALF_OPEN:
            self._transition_to_open()
        elif self.state == CircuitState.CLOSED:
            self.failure_count += 1
            if self.failure_count >= self.config.failure_threshold:
                self._transition_to_open()

    def _should_attempt_reset(self) -> bool:
        """Check if timeout elapsed for recovery."""
        if self.last_failure_time is None:
            return True
        return (time.monotonic() - self.last_failure_time) >= self.config.timeout

    def _time_until_recovery(self) -> float:
        """Time remaining until recovery attempt."""
        if self.last_failure_time is None:
            return 0.0
        elapsed = time.monotonic() - self.last_failure_time
        return max(0.0, self.config.timeout - elapsed)

    def _transition_to_open(self):
        """Transition to OPEN state."""
        self.state = CircuitState.OPEN
        self.last_failure_time = time.monotonic()
        self.success_count = 0
        logging.error(f"Circuit breaker OPEN after {self.failure_count} failures")

    def _transition_to_half_open(self):
        """Transition to HALF_OPEN state."""
        self.state = CircuitState.HALF_OPEN
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker HALF_OPEN - testing recovery")

    def _transition_to_closed(self):
        """Transition to CLOSED state."""
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.success_count = 0
        logging.info("Circuit breaker CLOSED - recovered")

# Global resilience configuration
retry_config: RetryConfig | None = None
rate_limiter = None  # Initialized based on mode in main()
circuit_breaker: CircuitBreaker | None = None

# ============================================================================
# Error Formatting
# ============================================================================

def _format_api_error_message(
    tool_name: str | None,
    method: str,
    path: str,
    status_code: int,
    request_id: str | None,
    error_data: dict
) -> str:
    """
    Format API error for LLM consumption with structured, parseable details.

    Args:
        tool_name: MCP tool function name
        method: HTTP method (GET, POST, etc.)
        path: API endpoint path
        status_code: HTTP status code
        request_id: Request UUID
        error_data: Sanitized error response from API

    Returns:
        Structured error message for LLM with available details
    """
    parts = []

    # Add tool name if available
    if tool_name:
        parts.append(f"Tool: {tool_name}")

    parts.append(f"Status: {status_code}")

    # Extract error message from various API response formats
    error_message = None
    if isinstance(error_data, dict):
        # Format 1: {"message": "..."}
        if 'message' in error_data:
            error_message = error_data['message']
        # Format 2: {"error": {"message": "...", "code": ..., "errors": [...]}}  (Google APIs)
        elif 'error' in error_data:
            err = error_data['error']
            if isinstance(err, dict):
                if 'message' in err:
                    error_message = err['message']
                # Also include error code if available
                if 'code' in err and error_message:
                    error_message = f"[{err['code']}] {error_message}"
            elif isinstance(err, str):
                error_message = err
        # Format 3: {"detail": "..."} (FastAPI/OpenAPI)
        elif 'detail' in error_data:
            detail = error_data['detail']
            if isinstance(detail, str):
                error_message = detail
            elif isinstance(detail, list) and detail:
                # Validation errors: [{"loc": [...], "msg": "...", "type": "..."}]
                msgs = [d.get('msg', str(d)) if isinstance(d, dict) else str(d) for d in detail[:3]]
                error_message = "; ".join(msgs)
                if len(detail) > 3:
                    error_message += f" (+{len(detail) - 3} more)"
        # Format 4: {"errors": [...]} (GraphQL-style)
        elif 'errors' in error_data and isinstance(error_data['errors'], list):
            errors = error_data['errors']
            if errors:
                msgs = [e.get('message', str(e)) if isinstance(e, dict) else str(e) for e in errors[:3]]
                error_message = "; ".join(msgs)
                if len(errors) > 3:
                    error_message += f" (+{len(errors) - 3} more)"

    if error_message:
        parts.append(f"Error: {error_message}")

    parts.append(f"Request: {method} {path}")

    # Add request ID if available
    if request_id:
        parts.append(f"ID: {request_id}")

    return "\n".join(parts)

# ============================================================================
# Response Sanitization
# ============================================================================

# Sanitization level configuration
SANITIZATION_LEVEL = os.getenv("SANITIZATION_LEVEL", "DISABLED").upper()
if SANITIZATION_LEVEL not in ("DISABLED", "LOW", "MEDIUM", "HIGH"):
    SANITIZATION_LEVEL = "DISABLED"

# Pattern sets by level
_PATTERNS_LOW = [
    'password', 'passwd', 'pwd',
    'token', 'secret', 'private_key', 'priv_key',
]
_PATTERNS_MEDIUM = _PATTERNS_LOW + [
    'auth_token', 'access_token', 'refresh_token', 'bearer',
    'credentials', 'authorization',
]
_PATTERNS_HIGH = _PATTERNS_MEDIUM + [
    'session_id', 'sessionid', 'cookie',
    'api_key', 'apikey', 'api-key',
    'user_id', 'userid', 'account_id', 'accountid', 'internal_id',
    'ip_address', 'ipaddress', 'ip_addr',
    'hostname', 'host_name', 'server_name', 'servername',
    'internal_ip', 'private_ip',
]

def _get_sensitive_patterns() -> list:
    """Get patterns based on current sanitization level."""
    if SANITIZATION_LEVEL == "DISABLED":
        return []
    if SANITIZATION_LEVEL == "LOW":
        return _PATTERNS_LOW
    if SANITIZATION_LEVEL == "MEDIUM":
        return _PATTERNS_MEDIUM
    return _PATTERNS_HIGH

def sanitize_response(response_data: Any) -> Any:
    """Remove sensitive fields from API responses before returning to LLM.

    Controlled by SANITIZATION_LEVEL env var:
    - DISABLED: No filtering (fastest)
    - LOW: Basic secrets (password, token, secret, private_key)
    - MEDIUM: LOW + Auth fields
    - HIGH: MEDIUM + Session/IDs

    Args:
        response_data: API response (dict, list, or primitive)

    Returns:
        Sanitized response with sensitive fields removed (based on level)
    """
    sensitive_patterns = _get_sensitive_patterns()

    # Skip sanitization if disabled
    if not sensitive_patterns:
        return response_data

    def should_filter(key: str) -> bool:
        """Check if field name matches sensitive patterns."""
        key_lower = key.lower()
        return any(pattern in key_lower for pattern in sensitive_patterns)

    def sanitize_dict(data: dict) -> dict:
        """Recursively sanitize dictionary."""
        sanitized: dict[str, Any] = {}
        for key, value in data.items():
            if should_filter(key):
                # Skip sensitive fields
                logging.debug(f"Filtered sensitive field: {key}")
                continue

            # Recursively sanitize nested structures
            if isinstance(value, dict):
                sanitized[key] = sanitize_dict(value)
            elif isinstance(value, list):
                sanitized[key] = sanitize_list(value)
            else:
                sanitized[key] = value

        return sanitized

    def sanitize_list(data: list[Any]) -> list[Any]:
        """Recursively sanitize list."""
        sanitized: list[Any] = []
        for item in data:
            if isinstance(item, dict):
                sanitized.append(sanitize_dict(item))
            elif isinstance(item, list):
                sanitized.append(sanitize_list(item))
            else:
                sanitized.append(item)
        return sanitized

    # Handle different response types
    if isinstance(response_data, dict):
        return sanitize_dict(response_data)
    if isinstance(response_data, list):
        return sanitize_list(response_data)
    # Primitive types (str, int, bool, None) pass through
    return response_data

def get_safe_error_response(
    error_type: str,
    status_code: int | None = None,
    retry_after: int | None = None,
    request_id: str | None = None
) -> dict:
    """Generate safe error response for client (LLM).

    Returns generic, actionable error messages without leaking:
    - API keys, tokens, credentials
    - Internal server details, IP addresses, hostnames
    - Stack traces, exception messages
    - User data from other users

    Args:
        error_type: Type of error (auth, rate_limit, timeout, etc.)
        status_code: HTTP status code
        retry_after: Retry-After header value (seconds)
        request_id: Request correlation ID for debugging

    Returns:
        Safe error response dictionary
    """
    error_messages = {
        'AUTHENTICATION_ERROR': {
            'error': 'AUTHENTICATION_ERROR',
            'message': 'Authentication failed. Please check your credentials.',
            'retry': False
        },
        'RATE_LIMIT_ERROR': {
            'error': 'RATE_LIMIT_ERROR',
            'message': 'Rate limit exceeded. Please retry after the specified delay.',
            'retry': True
        },
        'TIMEOUT_ERROR': {
            'error': 'TIMEOUT_ERROR',
            'message': 'Request timed out. The API did not respond in time.',
            'retry': True
        },
        'VALIDATION_ERROR': {
            'error': 'VALIDATION_ERROR',
            'message': 'Invalid request parameters. Please check your input.',
            'retry': False
        },
        'NOT_FOUND': {
            'error': 'NOT_FOUND',
            'message': 'The requested resource was not found.',
            'retry': False
        },
        'PERMISSION_DENIED': {
            'error': 'PERMISSION_DENIED',
            'message': 'You do not have permission to access this resource.',
            'retry': False
        },
        'API_ERROR': {
            'error': 'API_ERROR',
            'message': 'An error occurred while calling the external API.',
            'retry': True
        },
        'INTERNAL_ERROR': {
            'error': 'INTERNAL_ERROR',
            'message': 'An internal error occurred. Please try again later.',
            'retry': False
        }
    }

    # Get base error response
    response = error_messages.get(error_type, error_messages['INTERNAL_ERROR']).copy()

    # Add optional fields
    if status_code:
        response['status_code'] = status_code

    if retry_after:
        response['retry_after_seconds'] = retry_after

    if request_id:
        response['request_id'] = request_id

    return response

async def _make_request(
    method: str,
    path: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    tool_name: str | None = None,
    request_id: str | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any], int]:
    """Make HTTP request with retry, rate limiting, and circuit breaker."""
    global client, retry_config, rate_limiter, circuit_breaker

    if client is None:
        client = httpx.AsyncClient(
            base_url=BASE_URL,
            timeout=HTTPX_TIMEOUT,
            limits=httpx.Limits(max_keepalive_connections=MAX_KEEPALIVE_CONNECTIONS, max_connections=CONNECTION_POOL_SIZE),
            cookies=None,
            follow_redirects=True,
        )

    if headers is None:
        headers = {}
    headers.setdefault("Accept", "application/json")
    if method.upper() in ("POST", "PUT", "PATCH") and (body_content_type is None or body_content_type == "application/json"):
        headers.setdefault("Content-Type", "application/json")


    if rate_limiter is not None:
        allowed, wait_time = rate_limiter.consume()
        if not allowed:
            return {
                "error": "Rate limit exceeded",
                "retry_after": wait_time,
                "message": f"Rate limit exceeded. Wait {wait_time:.1f}s"
            }, 429

    if circuit_breaker is not None:
        try:
            circuit_breaker.before_call()
        except CircuitBreakerError as e:
            return {
                "error": "Circuit breaker open",
                "retry_after": e.retry_after,
                "message": str(e)
            }, 503

    if retry_config is not None:
        max_attempts = retry_config.max_attempts
        retry_status_codes = retry_config.retry_on_status_codes or []
    else:
        max_attempts = 1
        retry_status_codes = []

    # Append raw querystring before retry loop to avoid repeated appending
    # (OAS 3.2 in: querystring — already serialized per media type)
    if raw_querystring:
        _qs_sep = "&" if "?" in path else "?"
        path = f"{path}{_qs_sep}{raw_querystring}"

    last_error: httpx.HTTPStatusError | Exception | None = None
    _auth_retried = False  # Guard: only attempt one auth refresh per request
    for attempt in range(max_attempts):
        try:
            # Dispatch body to correct httpx kwarg based on content type
            _json = body if body_content_type is None or body_content_type == "application/json" else None
            _data = body if body_content_type in ("application/x-www-form-urlencoded", "multipart/form-data") else None
            _content = None
            if body_content_type is not None and body_content_type not in ("application/json", "application/x-www-form-urlencoded", "multipart/form-data"):
                _raw = body
                _content = json.dumps(_raw).encode() if isinstance(_raw, (dict, list)) else _raw
            response = await client.request(
                method=method,
                url=path,
                params=params,
                json=_json,
                data=_data,
                content=_content,
                headers=headers,
                cookies=cookies
            )

            # Get status code and data before raise_for_status
            status_code = response.status_code

            # Handle empty response body (204 No Content, 205 Reset Content, or empty body)
            if status_code in (204, 205) or not response.content:
                response_data = {"status": status_code, "message": "Success"}
            else:
                # Parse response JSON (graceful fallback to text)
                try:
                    response_data = response.json()
                except Exception:
                    response_data = {"text": response.text}

            # Check for HTTP errors
            if status_code >= 400:
                last_error = httpx.HTTPStatusError(
                    message=f"HTTP {status_code}",
                    request=response.request,
                    response=response
                )

                # On 401: clear stale tokens and retry once with fresh auth
                if status_code == 401 and not _auth_retried and tool_name:
                    _auth_retried = True
                    logging.warning(f"401 Unauthorized for {tool_name} - clearing token and re-authorizing")
                    _on_auth_failure()
                    _fresh_auth = await _get_auth_for_operation(tool_name)
                    if _fresh_auth.get("headers"):
                        headers = {**headers, **_fresh_auth["headers"]}
                    if _fresh_auth.get("params") and params is not None:
                        params = {**params, **_fresh_auth["params"]}
                    continue  # Retry immediately with fresh auth (no backoff)

                # Check if should retry
                if status_code not in retry_status_codes or attempt == max_attempts - 1:
                    # Don't retry or last attempt - notify circuit breaker of failure
                    if circuit_breaker is not None and status_code in [500, 502, 503, 504]:
                        circuit_breaker.on_failure(last_error)

                    # Log error information - WARNING for client errors (4xx), ERROR for server errors (5xx)
                    log_level = logging.WARNING if 400 <= status_code < 500 else logging.ERROR
                    logging.log(
                        log_level,
                        f"API request failed: {method} {path} (status: {status_code})",
                        extra={
                            "status_code": status_code,
                            "endpoint": path,
                            "method": method,
                            "attempt": attempt + 1,
                            "max_attempts": max_attempts,
                            "error_detail": str(response_data)[:500]
                        },
                        exc_info=False
                    )

                    # Sanitize response before returning/raising
                    sanitized_data = sanitize_response(response_data)

                    # Raise exception with structured error message for LLM
                    error_message = _format_api_error_message(
                        tool_name=tool_name,
                        method=method,
                        path=path,
                        status_code=status_code,
                        request_id=request_id,
                        error_data=sanitized_data
                    )
                    raise ValueError(error_message)

                # Will retry - continue to backoff logic below

            else:
                # Success - notify circuit breaker
                if circuit_breaker is not None:
                    circuit_breaker.on_success()

                # Sanitize response before returning to client
                sanitized_data = sanitize_response(response_data)
                return sanitized_data, status_code

            # Exponential backoff with jitter (for retry)
            # retry_config is guaranteed non-None here since max_attempts > 1
            assert retry_config is not None
            base_delay = retry_config.base_delay
            max_delay = retry_config.max_delay
            exponential_base = retry_config.exponential_base
            use_jitter = retry_config.jitter

            # Honor Retry-After header for 429 responses
            delay: float
            if status_code == 429:
                retry_after = response.headers.get('Retry-After')
                if retry_after:
                    try:
                        delay = float(retry_after)
                    except ValueError:
                        delay = min(base_delay * (exponential_base ** attempt), max_delay)
                else:
                    delay = min(base_delay * (exponential_base ** attempt), max_delay)
            else:
                delay = min(base_delay * (exponential_base ** attempt), max_delay)

            # Add jitter
            if use_jitter:
                delay = delay * (0.5 + random.random() * 0.5)  # noqa: S311

            logging.warning(
                f"Request failed with {status_code} (attempt {attempt + 1}/{max_attempts}). "
                f"Retrying in {delay:.2f}s"
            )
            await asyncio.sleep(delay)

        except httpx.HTTPStatusError:
            # Already handled above - shouldn't reach here
            continue

        except Exception as e:
            last_error = e

            # Network/connection errors - notify circuit breaker
            if circuit_breaker is not None:
                circuit_breaker.on_failure(e)

            # Log detailed error information for debugging (internal only)
            logging.error(
                f"Request failed with exception: {method} {path}",
                extra={
                    "endpoint": path,
                    "method": method,
                    "attempt": attempt + 1,
                    "max_attempts": max_attempts,
                    "error_type": e.__class__.__name__,
                    "error_detail": str(e)
                },
                exc_info=True
            )

            # Don't retry on non-HTTP errors - raise immediately with details
            # This ensures the LLM sees the actual error message
            error_message = (
                f"Tool: {tool_name}\n"
                f"Error: {e.__class__.__name__}\n"
                f"Request: {method} {path}\n"
                f"ID: {request_id}\n"
                f"Details: {str(e)}"
            )
            raise ConnectionError(error_message) from e

    # Log retry exhaustion with full context (internal only)
    logging.error(
        f"All retry attempts exhausted: {method} {path}",
        extra={
            "endpoint": path,
            "method": method,
            "total_attempts": max_attempts,
            "last_error_type": last_error.__class__.__name__ if last_error else "Unknown",
            "last_error_detail": str(last_error) if last_error else "Unknown"
        },
        exc_info=True
    )

    # Raise exception with error details
    if isinstance(last_error, httpx.HTTPStatusError):
        try:
            error_data = last_error.response.json()
        except Exception:
            error_data = {"error": last_error.response.text[:1000]}

        sanitized_error = sanitize_response(error_data)
        error_message = _format_api_error_message(
            tool_name=tool_name,
            method=method,
            path=path,
            status_code=last_error.response.status_code,
            request_id=request_id,
            error_data=sanitized_error
        )
        raise ValueError(error_message)

    # Network/connection error - structured format for consistency
    error_message = (
        f"Tool: {tool_name}\n"
        f"Error: Network error after {max_attempts} attempts\n"
        f"Request: {method} {path}\n"
        f"ID: {request_id}\n"
        f"Details: {str(last_error)}"
    )
    raise ConnectionError(error_message)

# ============================================================================
# MCP Input Coercion Middleware
# ============================================================================
# Defensive middleware: some MCP clients (including Claude) may send dict/list
# arguments as JSON strings instead of native objects. This violates the MCP spec
# but is a known, widespread client-side issue. This middleware transparently
# parses stringified JSON before Pydantic validation, preventing tool call failures.
# See: https://github.com/PrefectHQ/fastmcp/issues/932

class _JsonCoercionMiddleware(Middleware):
    async def on_call_tool(self, context, call_next):
        if context.message.arguments:
            for key, value in context.message.arguments.items():
                if isinstance(value, str) and len(value) > 1 and value[0] in ('{', '['):
                    try:
                        context.message.arguments[key] = json.loads(value)
                    except (json.JSONDecodeError, ValueError):
                        pass
        return await call_next(context)


# ============================================================================
# Helper Functions
# ============================================================================

@overload
def _parse_int(v: str | int) -> int: ...
@overload
def _parse_int(v: None) -> None: ...
def _parse_int(v: str | int | None) -> int | None:
    """Convert a string representation of an integer to a Python int.

    Formatted integer parameters (int32, int64, uint64, etc.) are exposed as str
    in the tool signature to prevent JS float64 precision loss for large IDs.
    This helper converts them back to int before Pydantic model construction.
    None passes through for optional parameters.
    Raises ValueError for non-integer strings or unexpected types (e.g. bool).
    """
    if v is None:
        return None
    if isinstance(v, bool):
        raise ValueError(f"Expected an integer value, got {v!r}")
    if isinstance(v, int):
        return v
    try:
        return int(v)
    except (ValueError, TypeError) as _e:
        raise ValueError(f"Expected an integer value, got {v!r}") from _e


def _serialize_query(params: dict[str, Any], styles: dict[str, tuple[str, bool]]) -> dict[str, Any]:
    """Serialize query params per OAS style/explode rules."""
    result: dict[str, Any] = {}
    for key, value in params.items():
        if key not in styles:
            result[key] = value
            continue
        style, explode = styles[key]
        if isinstance(value, list):
            if style == "pipeDelimited":
                result[key] = "|".join(str(v) for v in value)
            elif style == "spaceDelimited":
                result[key] = " ".join(str(v) for v in value)
            elif not explode:
                result[key] = ",".join(str(v) for v in value)
            else:
                result[key] = value
        elif isinstance(value, dict):
            if style == "deepObject":
                for k, v in value.items():
                    result[f"{key}[{k}]"] = v
            elif not explode:
                result[key] = ",".join(f"{k},{v}" for k, v in value.items())
            else:
                result.update(value)
        else:
            result[key] = value
    return result

def _log_tool_invocation(tool_name: str, method: str, path: str, request_id: str, _redact: str = "") -> None:
    """Log tool invocation. If _redact is set, that value is partially masked in the logged path."""
    log_path = path
    if _redact:
        log_path = log_path.replace(_redact, _redact[:4] + "***" if len(_redact) > 4 else "***")

    logging.info(
        f"Tool invoked: {tool_name}",
        extra={
            "request_id": request_id,
            "tool": tool_name,
            "method": method,
            "path": log_path,
            "timeout": DEFAULT_TIMEOUT
        }
    )

def _build_path(
    template: str,
    path_params: dict[str, Any],
) -> str:
    """Build path from template and parameters.

    Args:
        template: Path template with {param} placeholders
        path_params: Dictionary of parameter names to values

    Returns:
        Path with all  placeholders replaced

    Example:
        _build_path("/files/{fileId}/copy", {"fileId": "abc123"})
        Returns: "/files/abc123/copy"
    """
    result = template
    for key, value in path_params.items():
        result = result.replace("{" + key + "}", str(value))
    # Normalize double slashes from path param substitution (e.g. "/{path}" + "/foo")
    # but preserve "://" in URL-valued params (e.g. siteUrl="https://example.com")
    while "//" in result:
        cleaned = result.replace("://", ":%SCHEME%")
        cleaned = cleaned.replace("//", "/")
        cleaned = cleaned.replace(":%SCHEME%", "://")
        if cleaned == result:
            break
        result = cleaned
    return result

async def _execute_tool_request(
    tool_name: str,
    method: str,
    path: str,
    request_id: str,
    params: dict[str, Any] | None = None,
    body: Any = None,
    body_content_type: str | None = None,
    headers: dict[str, str] | None = None,
    cookies: dict[str, str] | None = None,
    raw_querystring: str | None = None,
) -> tuple[dict[str, Any], int]:
    """
    Execute tool request with timeout handling and metrics recording.

    Returns:
        Tuple of (normalized_response_data, status_code).
        Response data is normalized to dict format for Pydantic validation.
        Status code: HTTP status code from the API response.
    """
    start_time = time.time()
    try:
        # Wrap API call with timeout to prevent hanging
        async with asyncio.timeout(DEFAULT_TIMEOUT):
            # Call unified _make_request() which handles all resilience + security features
            result, status_code = await _make_request(
                method=method,
                path=path,
                params=params,
                body=body,
                body_content_type=body_content_type,
                headers=headers,
                cookies=cookies,
                tool_name=tool_name,
                request_id=request_id,
                raw_querystring=raw_querystring,
            )

        latency_ms = (time.time() - start_time) * 1000.0

        logging.info(f"Tool completed: {tool_name}", extra={"request_id": request_id, "latency_ms": f"{latency_ms:.2f}"})

        # Normalize response to dict and return with status code
        # Use "value" key for consistency with generated response models
        if isinstance(result, dict):
            return result, status_code
        return {"value": result}, status_code

    except asyncio.TimeoutError as e:
        latency_ms = (time.time() - start_time) * 1000.0

        # Request timed out
        logging.error(
            f"Tool timeout: {tool_name}",
            extra={
                "request_id": request_id,
                "tool": tool_name,
                "method": method,
                "path": path,
                "timeout": DEFAULT_TIMEOUT,
                "latency_ms": f"{latency_ms:.2f}"
            }
        )

        # Raise exception with structured error message for consistency
        # FastMCP will catch this and set isError: true in MCP response
        timeout_message = (
            f"Tool: {tool_name}\n"
            f"Error: Request timed out after {DEFAULT_TIMEOUT} seconds\n"
            f"ID: {request_id}"
        )
        raise asyncio.TimeoutError(timeout_message) from e

    except ValueError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except ConnectionError:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

    except Exception:
        latency_ms = (time.time() - start_time) * 1000.0
        raise

# ============================================================================
# Authentication
# ============================================================================

# Authentication scheme priority (most secure first)
AUTH_SCHEME_PRIORITY = [
    'PersonalAPIKeyAuth',
]

# Initialize authentication handlers at server startup
_auth_handlers: dict[str, Any] = {}
try:
    _auth_handlers["PersonalAPIKeyAuth"] = _auth.BearerTokenAuth(env_var="BEARER_TOKEN", token_format="Bearer")
    logging.info("Authentication configured: PersonalAPIKeyAuth")
except ValueError as e:
    # Extract credential names from error message (first sentence before "Leave empty")
    error_msg = str(e).split("Leave empty")[0].strip()
    logging.warning(f"Credentials for PersonalAPIKeyAuth not configured: {error_msg}")
    _auth_handlers["PersonalAPIKeyAuth"] = None

# Warn only if NO auth handlers were successfully configured
if all(handler is None for handler in _auth_handlers.values()):
    logging.warning("Server will attempt unauthenticated requests (may fail if API requires auth)")

def _on_auth_failure() -> None:
    """Clear tokens on all auth handlers that support it (called on 401 response)."""
    for scheme_name, handler in _auth_handlers.items():
        if handler is not None and hasattr(handler, 'clear_token'):
            handler.clear_token()
            logging.info(f"Cleared token for auth scheme '{scheme_name}' after 401")

async def _get_auth_for_operation(operation_id: str) -> dict[str, dict[str, str]]:
    """Get authentication for specific operation (handles multi-auth with OR/AND logic).

    Args:
        operation_id: The operation ID (tool name) to get auth for

    Returns:
        Dictionary with 'headers', 'params', 'cookies', 'path_params' keys containing auth data
    """
    result: dict[str, dict[str, str]] = {"headers": {}, "params": {}, "cookies": {}, "path_params": {}}

    # Get auth requirements for this operation from auth module
    # Map structure: operation_id -> [[scheme1], [scheme2, scheme3]] (OR of AND groups)
    required_schemes = _auth.OPERATION_AUTH_MAP.get(operation_id)

    if required_schemes is None:
        # CRITICAL: Operation missing from OPERATION_AUTH_MAP
        # This indicates a bug in auth config generation - operation should be in map
        # Do NOT fall back to global security - this masks bugs and creates security gaps
        logging.error(
            f"SECURITY ERROR: Operation '{operation_id}' not found in OPERATION_AUTH_MAP. "
            f"This indicates a bug in auth config generation. "
            f"All operations should be explicitly mapped. "
            f"Proceeding with no authentication - REQUEST WILL LIKELY FAIL."
        )
        # Return empty auth (will likely fail at API, but surfaces the bug)
        return result

    # Sort OR groups by security priority (most secure first)
    # Each OR group is sorted internally by scheme priority
    def get_scheme_priority(scheme_name: str) -> int:
        """Get priority index for scheme (lower = higher priority)."""
        try:
            return AUTH_SCHEME_PRIORITY.index(scheme_name)
        except ValueError:
            # Unknown scheme - put at end
            return len(AUTH_SCHEME_PRIORITY)

    # Sort each OR group by priority, then sort OR groups by their best (lowest) priority
    sorted_schemes = []
    for scheme_list in required_schemes:
        # Sort schemes within AND group by priority (most secure first)
        sorted_group = sorted(scheme_list, key=get_scheme_priority)
        sorted_schemes.append(sorted_group)

    # Sort OR groups by the priority of their first (most secure) scheme
    sorted_schemes.sort(key=lambda group: get_scheme_priority(group[0]) if group else float('inf'))

    # Try each OR group in priority order (most secure first)
    for scheme_list in sorted_schemes:
        headers = {}
        params = {}
        cookies = {}
        path_params = {}
        all_succeeded = True

        # Handle AND group (multiple schemes in same list - all must succeed)
        for scheme_name in scheme_list:
            handler = _auth_handlers.get(scheme_name)
            if not handler:
                # Handler not configured (env vars missing)
                all_succeeded = False
                logging.debug(f"Auth scheme '{scheme_name}' not configured for {operation_id}")
                break

            try:
                # Try all injection methods (headers, params, cookies, path_params)
                # OAuth2 methods are async (token refresh/authorize); others are sync.
                import inspect as _inspect
                if hasattr(handler, 'get_auth_headers'):
                    _h = handler.get_auth_headers()
                    headers.update(await _h if _inspect.isawaitable(_h) else _h)
                if hasattr(handler, 'get_auth_params'):
                    _p = handler.get_auth_params()
                    params.update(await _p if _inspect.isawaitable(_p) else _p)
                if hasattr(handler, 'get_auth_cookies'):
                    _c = handler.get_auth_cookies()
                    cookies.update(await _c if _inspect.isawaitable(_c) else _c)
                if hasattr(handler, 'get_auth_path_params'):
                    _pp = handler.get_auth_path_params()
                    path_params.update(await _pp if _inspect.isawaitable(_pp) else _pp)
            except Exception as e:
                logging.debug(f"Auth scheme '{scheme_name}' failed for {operation_id}: {e}")
                all_succeeded = False
                break

        # If all schemes in AND group succeeded, use this auth
        if all_succeeded and (headers or params or cookies or path_params):
            result["headers"] = headers
            result["params"] = params
            result["cookies"] = cookies
            result["path_params"] = path_params
            logging.debug(f"Using auth for {operation_id}: schemes={scheme_list}")
            return result

    # No auth configured or all OR groups failed
    if required_schemes and required_schemes != [[]]:
        logging.warning(f"No configured auth handler found for {operation_id} (requires: {required_schemes})")

    return result

# ============================================================================
# FastMCP Server Initialization
# ============================================================================

mcp = FastMCP("PostHog API", middleware=[_JsonCoercionMiddleware()])

# Tags: code-invites, code
@mcp.tool()
async def redeem_invite_code(code: str = Field(..., description="The invite code to redeem. Must be 50 characters or fewer.", max_length=50)) -> dict[str, Any]:
    """Redeem a PostHog invite code to activate access. Submit a valid code to enable the associated account or workspace permissions."""

    # Construct request model with validation
    try:
        _request = _models.CodeInvitesRedeemCreateRequest(
            body=_models.CodeInvitesRedeemCreateRequestBody(code=code)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for redeem_invite_code: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/code/invites/redeem"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("redeem_invite_code")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("redeem_invite_code", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="redeem_invite_code",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: max, conversations
@mcp.tool()
async def list_conversations(
    project_id: str = Field(..., description="The unique identifier of the project containing the conversations. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of conversation records to return in a single response page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based starting position for results, used in conjunction with limit to paginate through the conversation list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of conversations for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsListRequest(
            path=_models.ConversationsListRequestPath(project_id=project_id),
            query=_models.ConversationsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_conversations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/conversations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/conversations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_conversations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_conversations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_conversations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: max, conversations
@mcp.tool()
async def send_conversation_message(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this ID by calling the /api/projects/ endpoint if needed."),
    content: str | None = Field(..., description="The message content to send. Maximum length is 40,000 characters.", max_length=40000),
    conversation: str = Field(..., description="The unique identifier of the conversation to send the message to or stream from. Must be a valid UUID."),
    trace_id: str = Field(..., description="A unique identifier for tracing this request. Must be a valid UUID."),
    contextual_tools: dict[str, Any] | None = Field(None, description="Optional tools and configurations available for the conversation to use during processing."),
    ui_context: Any | None = Field(None, description="Optional UI context information to pass along with the conversation request."),
    billing_context: Any | None = Field(None, description="Optional billing context information associated with this conversation."),
    agent_mode: Literal["product_analytics", "sql", "session_replay", "error_tracking", "plan", "execution", "survey", "research", "flags", "llm_analytics", "sandbox"] | None = Field(None, description="The operational mode or agent type for this conversation. Determines how the conversation is processed and what capabilities are available."),
) -> dict[str, Any]:
    """Send a message to a conversation or stream results from an existing conversation. Provide a message to start new conversation processing, or omit it to stream from an existing conversation."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsCreateRequest(
            path=_models.ConversationsCreateRequestPath(project_id=project_id),
            body=_models.ConversationsCreateRequestBody(content=content, conversation=conversation, contextual_tools=contextual_tools, ui_context=ui_context, billing_context=billing_context, trace_id=trace_id, agent_mode=agent_mode)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_conversation_message: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/conversations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/conversations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_conversation_message")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_conversation_message", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_conversation_message",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: max, conversations
@mcp.tool()
async def get_conversation(
    conversation: str = Field(..., description="The unique identifier (UUID) of the conversation to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the conversation. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific conversation by its UUID within a project. Use this to fetch detailed information about a single conversation."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsRetrieveRequest(
            path=_models.ConversationsRetrieveRequestPath(conversation=conversation, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_conversation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/conversations/{conversation}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/conversations/{conversation}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_conversation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_conversation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_conversation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: customer_profile_configs
@mcp.tool()
async def list_customer_profile_configs(
    project_id: str = Field(..., description="The unique identifier of the project whose customer profile configurations you want to retrieve. You can obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of customer profile configurations to return in a single page of results. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="The zero-based starting position for results, used to paginate through large result sets. Omit to start from the beginning."),
) -> dict[str, Any]:
    """Retrieve a paginated list of customer profile configurations for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.CustomerProfileConfigsListRequest(
            path=_models.CustomerProfileConfigsListRequestPath(project_id=project_id),
            query=_models.CustomerProfileConfigsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_customer_profile_configs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/customer_profile_configs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/customer_profile_configs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_customer_profile_configs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_customer_profile_configs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_customer_profile_configs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: customer_profile_configs
@mcp.tool()
async def get_customer_profile_config(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the customer profile config to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the customer profile config. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific customer profile configuration by its unique identifier within a project. Use this to fetch detailed settings and properties for a customer profile config."""

    # Construct request model with validation
    try:
        _request = _models.CustomerProfileConfigsRetrieveRequest(
            path=_models.CustomerProfileConfigsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_customer_profile_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/customer_profile_configs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/customer_profile_configs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_customer_profile_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_customer_profile_config", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_customer_profile_config",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def list_dashboard_collaborators(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard whose collaborators you want to list."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of collaborators who have access to a specific dashboard within a project. This shows all users with whom the dashboard is shared and their access permissions."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsListRequest(
            path=_models.DashboardsCollaboratorsListRequestPath(dashboard_id=dashboard_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dashboard_collaborators: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dashboard_collaborators")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dashboard_collaborators", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dashboard_collaborators",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def add_collaborator_to_dashboard(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard to which you're adding a collaborator."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve project IDs by calling the /api/projects/ endpoint."),
    level: Literal[21, 37] = Field(..., description="The permission level for the collaborator, specified as an integer between 0 and 32767. Higher values typically grant greater permissions.", ge=0, le=32767),
    user_uuid: str = Field(..., description="The UUID of the user to add as a collaborator. Must be a valid UUID format."),
) -> dict[str, Any]:
    """Grant a user access to a specific dashboard with a defined permission level. The user is identified by UUID and the access level determines their capabilities within the dashboard."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsCreateRequest(
            path=_models.DashboardsCollaboratorsCreateRequestPath(dashboard_id=dashboard_id, project_id=project_id),
            body=_models.DashboardsCollaboratorsCreateRequestBody(level=level, user_uuid=user_uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_collaborator_to_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_collaborator_to_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_collaborator_to_dashboard", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_collaborator_to_dashboard",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def remove_dashboard_collaborator(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard from which to remove the collaborator."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve your project ID by calling /api/projects/."),
    user__uuid: str = Field(..., description="The UUID of the user to remove as a collaborator from the dashboard. Must be a valid UUID format."),
) -> dict[str, Any]:
    """Remove a collaborator from a dashboard by their user UUID. This revokes the specified user's access to the dashboard."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsDestroyRequest(
            path=_models.DashboardsCollaboratorsDestroyRequestPath(dashboard_id=dashboard_id, project_id=project_id, user__uuid=user__uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_dashboard_collaborator: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators/{user__uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/dashboards/{dashboard_id}/collaborators/{user__uuid}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_dashboard_collaborator")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_dashboard_collaborator", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_dashboard_collaborator",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def list_desktop_recordings(
    project_id: str = Field(..., description="The unique identifier of the project containing the desktop recordings. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of recording results to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of desktop meeting recordings for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsListRequest(
            path=_models.DesktopRecordingsListRequestPath(project_id=project_id),
            query=_models.DesktopRecordingsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_desktop_recordings: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_desktop_recordings")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_desktop_recordings", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_desktop_recordings",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def create_desktop_recording(project_id: str = Field(..., description="The unique identifier of the project where the recording will be created. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Initiate a new desktop recording session and obtain an upload token for the Recall.ai desktop SDK to use during the recording."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsCreateRequest(
            path=_models.DesktopRecordingsCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_desktop_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_desktop_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_desktop_recording", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_desktop_recording",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def get_desktop_recording(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the desktop recording to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the desktop recording. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific desktop meeting recording by its unique identifier within a project."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsRetrieveRequest(
            path=_models.DesktopRecordingsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_desktop_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_desktop_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_desktop_recording", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_desktop_recording",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def update_desktop_recording(
    id_: str = Field(..., alias="id", description="Unique identifier (UUID) of the desktop recording to update."),
    project_id: str = Field(..., description="Project ID that owns this recording. Retrieve available project IDs from /api/projects/."),
    platform: Literal["zoom", "teams", "meet", "desktop_audio", "slack"] = Field(..., description="Meeting platform source: zoom, teams (Microsoft Teams), meet (Google Meet), desktop_audio, or slack (Slack huddle)."),
    recall_recording_id: str | None = Field(None, description="Optional UUID identifier linking to an external recall recording system."),
    meeting_title: str | None = Field(None, description="Human-readable title of the meeting, up to 255 characters.", max_length=255),
    meeting_url: str | None = Field(None, description="URL to the original meeting, up to 200 characters. Must be a valid URI.", max_length=200),
    duration_seconds: int | None = Field(None, description="Total recording duration in seconds.", ge=-2147483648, le=2147483647),
    status: Literal["recording", "uploading", "processing", "ready", "error"] | None = Field(None, description="Current processing state: recording (in progress), uploading (to storage), processing (AI analysis), ready (complete), or error (failed)."),
    notes: str | None = Field(None, description="Optional user notes or metadata about the recording."),
    error_message: str | None = Field(None, description="Error description if status is 'error', explaining why processing failed."),
    video_url: str | None = Field(None, description="URL to the processed video file, up to 200 characters. Must be a valid URI.", max_length=200),
    participants: list[str] | None = Field(None, description="Array of participant names who attended the meeting."),
    transcript_segments: list[_models.TranscriptSegment] | None = Field(None, description="Array of transcript segments, each containing text and timestamp information in chronological order."),
    summary: str | None = Field(None, description="AI-generated summary of the meeting content."),
    extracted_tasks: list[_models.Task] | None = Field(None, description="Array of actionable tasks extracted from the transcript by AI analysis."),
    started_at: str | None = Field(None, description="ISO 8601 timestamp indicating when the recording started."),
    completed_at: str | None = Field(None, description="ISO 8601 timestamp indicating when the recording and processing completed."),
) -> dict[str, Any]:
    """Update metadata and processing status for a desktop meeting recording, including transcript segments, extracted tasks, and participant information."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsUpdateRequest(
            path=_models.DesktopRecordingsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DesktopRecordingsUpdateRequestBody(recall_recording_id=recall_recording_id, platform=platform, meeting_title=meeting_title, meeting_url=meeting_url, duration_seconds=duration_seconds, status=status, notes=notes, error_message=error_message, video_url=video_url, participants=participants, transcript_segments=transcript_segments, summary=summary, extracted_tasks=extracted_tasks, started_at=started_at, completed_at=completed_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_desktop_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_desktop_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_desktop_recording", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_desktop_recording",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def update_desktop_recording_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the desktop recording to update."),
    project_id: str = Field(..., description="The project ID that owns this recording. Retrieve available project IDs from the /api/projects/ endpoint."),
    recall_recording_id: str | None = Field(None, description="The unique identifier (UUID) of the associated Recall recording, if applicable."),
    meeting_title: str | None = Field(None, description="The title or name of the meeting. Maximum 255 characters.", max_length=255),
    meeting_url: str | None = Field(None, description="The URL where the meeting took place. Must be a valid URI, maximum 200 characters.", max_length=200),
    duration_seconds: int | None = Field(None, description="The total duration of the recording in seconds. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
    status: Literal["recording", "uploading", "processing", "ready", "error"] | None = Field(None, description="The current processing state of the recording: recording (actively capturing), uploading (being transferred), processing (being analyzed), ready (available for use), or error (failed)."),
    notes: str | None = Field(None, description="Additional notes or metadata about the recording."),
    error_message: str | None = Field(None, description="Error details if the recording failed to process. Typically populated when status is 'error'."),
    video_url: str | None = Field(None, description="The URL where the processed video file is stored. Must be a valid URI, maximum 200 characters.", max_length=200),
    participants: list[str] | None = Field(None, description="An ordered list of participant names who attended the meeting."),
    transcript_segments: list[_models.TranscriptSegment] | None = Field(None, description="An ordered array of transcript segments, each containing speaker text and associated timestamps. Order reflects chronological sequence in the recording."),
    summary: str | None = Field(None, description="A text summary of the meeting content, typically generated by AI analysis."),
    extracted_tasks: list[_models.Task] | None = Field(None, description="An ordered list of actionable tasks extracted from the transcript by AI analysis."),
    started_at: str | None = Field(None, description="The timestamp when the recording started, in ISO 8601 format."),
    completed_at: str | None = Field(None, description="The timestamp when the recording completed, in ISO 8601 format."),
) -> dict[str, Any]:
    """Partially update a desktop recording's metadata, status, and extracted content. Use this to modify recording details, track processing status, store transcripts and tasks, or mark recordings as ready or failed."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsPartialUpdateRequest(
            path=_models.DesktopRecordingsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DesktopRecordingsPartialUpdateRequestBody(recall_recording_id=recall_recording_id, meeting_title=meeting_title, meeting_url=meeting_url, duration_seconds=duration_seconds, status=status, notes=notes, error_message=error_message, video_url=video_url, participants=participants, transcript_segments=transcript_segments, summary=summary, extracted_tasks=extracted_tasks, started_at=started_at, completed_at=completed_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_desktop_recording_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_desktop_recording_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_desktop_recording_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_desktop_recording_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def delete_desktop_recording(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the desktop recording to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the desktop recording. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a desktop meeting recording by its ID. This operation removes the recording and all associated data from the specified project."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsDestroyRequest(
            path=_models.DesktopRecordingsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_desktop_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_desktop_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_desktop_recording", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_desktop_recording",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: desktop_recordings, desktop_recordings
@mcp.tool()
async def append_segments_to_desktop_recording(
    id_: str = Field(..., alias="id", description="The UUID identifier of the desktop recording to append segments to."),
    project_id: str = Field(..., description="The unique identifier of the project containing the desktop recording. Retrieve available project IDs from the /api/projects/ endpoint."),
    segments: list[_models.TranscriptSegment] = Field(..., description="An array of transcript segments to append to the recording. Must contain at least one segment. Segments are processed in the order provided.", min_length=1),
) -> dict[str, Any]:
    """Append transcript segments to a desktop recording, supporting batched real-time streaming for continuous transcription updates."""

    # Construct request model with validation
    try:
        _request = _models.DesktopRecordingsAppendSegmentsCreateRequest(
            path=_models.DesktopRecordingsAppendSegmentsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.DesktopRecordingsAppendSegmentsCreateRequestBody(segments=segments)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for append_segments_to_desktop_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/desktop_recordings/{id}/append_segments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/desktop_recordings/{id}/append_segments"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("append_segments_to_desktop_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("append_segments_to_desktop_recording", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="append_segments_to_desktop_recording",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def list_error_tracking_assignment_rules(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of assignment rules to return in a single page of results. Defaults to the API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination alongside the limit parameter."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error tracking assignment rules configured for a project. Assignment rules determine how errors are automatically routed to team members."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingAssignmentRulesListRequest(
            path=_models.ErrorTrackingAssignmentRulesListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingAssignmentRulesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_assignment_rules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/assignment_rules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/assignment_rules"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_assignment_rules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_assignment_rules", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_assignment_rules",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def get_error_tracking_assignment_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking assignment rule to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the assignment rule. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking assignment rule by its unique identifier. This rule defines how errors are automatically assigned within your project."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingAssignmentRulesRetrieveRequest(
            path=_models.ErrorTrackingAssignmentRulesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_assignment_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/assignment_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/assignment_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_assignment_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_assignment_rule", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_assignment_rule",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_assignment_rule(
    id_: str = Field(..., alias="id", description="The UUID identifier of the error tracking assignment rule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the assignment rule. Retrieve project IDs from the /api/projects/ endpoint."),
    filters: Any = Field(..., description="The filter criteria that determine which errors this rule applies to. Specifies the conditions for matching errors."),
    order_key: int = Field(..., description="The sort order for this rule relative to other assignment rules. Must be an integer between -2,147,483,648 and 2,147,483,647; lower values are evaluated first.", ge=-2147483648, le=2147483647),
    assignee_id: str | None = Field(None, alias="assigneeId", description="The ID of the user or team to assign errors to. Accepts either a string UUID or an integer identifier."),
    disabled_data: Any | None = Field(None, description="Configuration for disabling or enabling specific aspects of this assignment rule."),
) -> dict[str, Any]:
    """Update an error tracking assignment rule for a project, including its filters, ordering, assignee, and enabled/disabled status."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingAssignmentRulesUpdateRequest(
            path=_models.ErrorTrackingAssignmentRulesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingAssignmentRulesUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data,
                assignee=_models.ErrorTrackingAssignmentRulesUpdateRequestBodyAssignee(id_=assignee_id) if any(v is not None for v in [assignee_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_assignment_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/assignment_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/assignment_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_assignment_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_assignment_rule", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_assignment_rule",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_assignment_rule_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking assignment rule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the assignment rule. Retrieve project IDs from the /api/projects/ endpoint."),
    assignee_id: str | None = Field(None, alias="assigneeId", description="The ID of the user or team to assign errors to. Accepts either a string UUID or an integer identifier."),
    filters: Any | None = Field(None, description="Criteria for matching errors to this assignment rule. Defines which errors should be assigned based on specified conditions."),
    order_key: int | None = Field(None, description="The sort order position for this rule among other assignment rules. Determines rule evaluation priority, with valid values ranging from -2,147,483,648 to 2,147,483,647.", ge=-2147483648, le=2147483647),
    disabled_data: Any | None = Field(None, description="Configuration for disabling or enabling this assignment rule without deletion."),
) -> dict[str, Any]:
    """Partially update an error tracking assignment rule for a project. Allows modification of assignee, filters, ordering, and enabled/disabled status."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingAssignmentRulesPartialUpdateRequest(
            path=_models.ErrorTrackingAssignmentRulesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingAssignmentRulesPartialUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data,
                assignee=_models.ErrorTrackingAssignmentRulesPartialUpdateRequestBodyAssignee(id_=assignee_id) if any(v is not None for v in [assignee_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_assignment_rule_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/assignment_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/assignment_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_assignment_rule_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_assignment_rule_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_assignment_rule_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def delete_error_tracking_assignment_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking assignment rule to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the assignment rule. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a specific error tracking assignment rule from a project. This removes the rule and stops it from being applied to new error assignments."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingAssignmentRulesDestroyRequest(
            path=_models.ErrorTrackingAssignmentRulesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_assignment_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/assignment_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/assignment_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_assignment_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_assignment_rule", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_assignment_rule",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_fingerprints(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of fingerprints to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, enabling pagination through large result sets. Combine with limit to navigate pages."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error fingerprints for a specific project. Fingerprints group similar errors together to help identify recurring issues."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingFingerprintsListRequest(
            path=_models.ErrorTrackingFingerprintsListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingFingerprintsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_fingerprints: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/fingerprints", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/fingerprints"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_fingerprints")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_fingerprints", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_fingerprints",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_fingerprint(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking fingerprint to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error fingerprint. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking fingerprint by its unique identifier within a project. Fingerprints group similar errors together for consolidated tracking and analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingFingerprintsRetrieveRequest(
            path=_models.ErrorTrackingFingerprintsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_fingerprint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/fingerprints/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/fingerprints/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_fingerprint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_fingerprint", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_fingerprint",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def list_error_tracking_grouping_rules(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of grouping rules to return in a single response page. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from. Use with limit to paginate through the full list of grouping rules."),
) -> dict[str, Any]:
    """Retrieve the error tracking grouping rules configured for a project. Grouping rules determine how errors are automatically grouped and deduplicated in error tracking."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingGroupingRulesListRequest(
            path=_models.ErrorTrackingGroupingRulesListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingGroupingRulesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_grouping_rules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/grouping_rules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/grouping_rules"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_grouping_rules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_grouping_rules", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_grouping_rules",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def get_error_tracking_grouping_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking grouping rule to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the grouping rule. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking grouping rule by its unique identifier. Grouping rules define how errors are automatically grouped and categorized within a project."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingGroupingRulesRetrieveRequest(
            path=_models.ErrorTrackingGroupingRulesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_grouping_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/grouping_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/grouping_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_grouping_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_grouping_rule", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_grouping_rule",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_grouping_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking grouping rule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the grouping rule. Retrieve available project IDs from the /api/projects/ endpoint."),
    filters: Any = Field(..., description="The filtering criteria that determine which errors are matched by this grouping rule."),
    order_key: int = Field(..., description="The sort order position for this grouping rule. Must be an integer between -2,147,483,648 and 2,147,483,647.", ge=-2147483648, le=2147483647),
    assignee_id: str | None = Field(None, alias="assigneeId", description="The ID of the user to assign to this grouping rule. Accepts either a string or integer identifier."),
    disabled_data: Any | None = Field(None, description="Optional data specifying which fields or settings should be disabled for this grouping rule."),
) -> dict[str, Any]:
    """Update an error tracking grouping rule for a project. Modify how errors are grouped together by specifying filters, ordering, and optional assignee information."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingGroupingRulesUpdateRequest(
            path=_models.ErrorTrackingGroupingRulesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingGroupingRulesUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data,
                assignee=_models.ErrorTrackingGroupingRulesUpdateRequestBodyAssignee(id_=assignee_id) if any(v is not None for v in [assignee_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_grouping_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/grouping_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/grouping_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_grouping_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_grouping_rule", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_grouping_rule",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_grouping_rule_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking grouping rule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the grouping rule. Retrieve available project IDs from the /api/projects/ endpoint."),
    assignee_id: str | None = Field(None, alias="assigneeId", description="The ID of the user or team to assign to this grouping rule. Accepts either a string or integer identifier."),
    filters: Any | None = Field(None, description="Filter criteria used to match errors for this grouping rule. Defines which errors are grouped together based on specified conditions."),
    order_key: int | None = Field(None, description="The sort order position for this grouping rule. Must be an integer within the range of a 32-bit signed integer.", ge=-2147483648, le=2147483647),
    disabled_data: Any | None = Field(None, description="Configuration for disabling or controlling the active state of this grouping rule."),
) -> dict[str, Any]:
    """Partially update an error tracking grouping rule for a project. Allows modification of rule configuration including filters, ordering, assignee, and enabled/disabled status."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingGroupingRulesPartialUpdateRequest(
            path=_models.ErrorTrackingGroupingRulesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingGroupingRulesPartialUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data,
                assignee=_models.ErrorTrackingGroupingRulesPartialUpdateRequestBodyAssignee(id_=assignee_id) if any(v is not None for v in [assignee_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_grouping_rule_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/grouping_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/grouping_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_grouping_rule_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_grouping_rule_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_grouping_rule_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def delete_error_tracking_grouping_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking grouping rule to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the grouping rule. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking grouping rule from a project. This removes the rule and stops it from being applied to future error grouping operations."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingGroupingRulesDestroyRequest(
            path=_models.ErrorTrackingGroupingRulesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_grouping_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/grouping_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/grouping_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_grouping_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_grouping_rule", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_grouping_rule",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_issues(
    project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of issues to return in a single page of results. Controls the page size for pagination."),
    offset: int | None = Field(None, description="The starting position (zero-indexed) from which to return results. Use this with limit to paginate through the issue list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error tracking issues for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesListRequest(
            path=_models.ErrorTrackingIssuesListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingIssuesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_issues: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_issues")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_issues", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_issues",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def create_error_tracking_issue(
    project_id: str = Field(..., description="The unique identifier of the project where the error tracking issue will be created. Retrieve this ID from the /api/projects/ endpoint."),
    assignee_id: str = Field(..., alias="assigneeId", description="The ID of the user to assign this issue to. Accepts either a string or integer identifier."),
    first_seen: str = Field(..., description="The timestamp when this error was first observed, formatted as an ISO 8601 date-time string."),
    external_issues: list[_models.ErrorTrackingExternalReference] = Field(..., description="An array of external issue references (e.g., links to bug trackers, support tickets) associated with this error."),
    cohort_id: int | None = Field(None, alias="cohortId", description="Optional cohort identifier to group this issue with related errors."),
    status: Literal["archived", "active", "resolved", "pending_release", "suppressed"] | None = Field(None, description="The initial status of the issue. Must be one of: active (currently occurring), resolved (fixed), archived (no longer relevant), pending_release (fix awaiting deployment), or suppressed (intentionally ignored)."),
    name: str | None = Field(None, description="A descriptive name or title for the error tracking issue."),
    description: str | None = Field(None, description="Additional context or details about the error, such as reproduction steps or impact assessment."),
) -> dict[str, Any]:
    """Create a new error tracking issue in a project. Requires specifying the assignee, initial status, and when the error was first detected."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesCreateRequest(
            path=_models.ErrorTrackingIssuesCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingIssuesCreateRequestBody(status=status, name=name, description=description, first_seen=first_seen, external_issues=external_issues,
                assignee=_models.ErrorTrackingIssuesCreateRequestBodyAssignee(id_=assignee_id),
                cohort=_models.ErrorTrackingIssuesCreateRequestBodyCohort(id_=cohort_id) if any(v is not None for v in [cohort_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_error_tracking_issue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_error_tracking_issue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_error_tracking_issue", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_error_tracking_issue",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_issue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking issue to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking issue. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific error tracking issue within a project, including its status, occurrences, and metadata."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesRetrieveRequest(
            path=_models.ErrorTrackingIssuesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_issue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_issue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_issue", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_issue",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_issue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking issue to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this error tracking issue. Retrieve project IDs from the /api/projects/ endpoint."),
    assignee_id: str = Field(..., alias="assigneeId", description="The ID of the user or team to assign this issue to. Accepts either a string or integer identifier."),
    first_seen: str = Field(..., description="The timestamp when this error was first observed, formatted as an ISO 8601 date-time string."),
    external_issues: list[_models.ErrorTrackingExternalReference] = Field(..., description="An array of external issue references (e.g., Jira tickets, GitHub issues) linked to this error for cross-system tracking."),
    cohort_id: int | None = Field(None, alias="cohortId", description="The ID of the cohort to associate with this issue for grouping and analysis."),
    status: Literal["archived", "active", "resolved", "pending_release", "suppressed"] | None = Field(None, description="The current status of the issue. Valid statuses are: archived (no longer tracked), active (ongoing), resolved (fixed), pending_release (awaiting deployment), or suppressed (intentionally hidden)."),
    name: str | None = Field(None, description="A human-readable name or title for the error tracking issue."),
    description: str | None = Field(None, description="Detailed description or notes about the error, its cause, or resolution steps."),
) -> dict[str, Any]:
    """Update an error tracking issue's status, assignment, cohort, and metadata. Modify issue details such as name, description, and linked external issues."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesUpdateRequest(
            path=_models.ErrorTrackingIssuesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingIssuesUpdateRequestBody(status=status, name=name, description=description, first_seen=first_seen, external_issues=external_issues,
                assignee=_models.ErrorTrackingIssuesUpdateRequestBodyAssignee(id_=assignee_id),
                cohort=_models.ErrorTrackingIssuesUpdateRequestBodyCohort(id_=cohort_id) if any(v is not None for v in [cohort_id]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_issue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_issue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_issue", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_issue",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_issue_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking issue to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this error tracking issue. Retrieve project IDs via the /api/projects/ endpoint."),
    assignee_id: str = Field(..., alias="assigneeId", description="The ID of the user to assign this issue to. Accepts either a string or integer identifier."),
    status: Literal["archived", "active", "resolved", "pending_release", "suppressed"] | None = Field(None, description="The current status of the issue. Valid statuses are: archived, active, resolved, pending_release, or suppressed."),
    name: str | None = Field(None, description="The display name or title for this error tracking issue."),
    description: str | None = Field(None, description="A detailed description or notes about this error tracking issue."),
    external_issues: list[_models.ErrorTrackingExternalReference] | None = Field(None, description="An array of external issue references linked to this error tracking issue, such as tickets in external bug tracking systems."),
) -> dict[str, Any]:
    """Partially update an error tracking issue, allowing you to modify its status, assignee, name, description, and linked external issues."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesPartialUpdateRequest(
            path=_models.ErrorTrackingIssuesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingIssuesPartialUpdateRequestBody(status=status, name=name, description=description, external_issues=external_issues,
                assignee=_models.ErrorTrackingIssuesPartialUpdateRequestBodyAssignee(id_=assignee_id))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_issue_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_issue_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_issue_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_issue_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_issue_activity(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking issue whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking issue. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific error tracking issue, including all events and changes associated with that issue."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesActivityRetrieve2Request(
            path=_models.ErrorTrackingIssuesActivityRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_issue_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_issue_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_issue_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_issue_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_issues_activity(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the activity log for error tracking issues within a specific project. This endpoint returns a timeline of events and changes related to tracked errors."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingIssuesActivityRetrieveRequest(
            path=_models.ErrorTrackingIssuesActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_issues_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/issues/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/issues/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_issues_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_issues_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_issues_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_releases(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if you don't have it."),
    limit: int | None = Field(None, description="Maximum number of releases to return in a single response page. Defaults to the API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of releases for error tracking in a specific project. Use this to view all tracked releases and their associated error data."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesListRequest(
            path=_models.ErrorTrackingReleasesListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingReleasesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_releases: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_releases")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_releases", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_releases",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def create_error_tracking_release(
    project_id: str = Field(..., description="The unique identifier of the project where the release will be tracked. Retrieve this ID from the /api/projects/ endpoint if needed."),
    hash_id: str = Field(..., description="A unique hash identifier for the release, used to distinguish this release from others in the error tracking system."),
    version: str = Field(..., description="The version string or number identifying this release (e.g., semantic version format like '1.2.3' or any custom versioning scheme)."),
    project: str = Field(..., description="The project identifier or name associated with this release for organizational and tracking purposes."),
    metadata: Any | None = Field(None, description="Optional metadata object containing additional information about the release, such as build details, deployment information, or custom tags."),
) -> dict[str, Any]:
    """Create a new release entry for error tracking in a specified project. This registers a release version with associated metadata for tracking errors across different application versions."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesCreateRequest(
            path=_models.ErrorTrackingReleasesCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingReleasesCreateRequestBody(hash_id=hash_id, metadata=metadata, version=version, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_error_tracking_release: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_error_tracking_release")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_error_tracking_release", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_error_tracking_release",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_release(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific error tracking release for a project, including its metadata and associated error data."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesRetrieveRequest(
            path=_models.ErrorTrackingReleasesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_release: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_release")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_release", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_release",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_release(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to update."),
    project_id: str = Field(..., description="The project ID that contains the error tracking release. Find your project ID by calling the /api/projects/ endpoint."),
    hash_id: str = Field(..., description="A unique hash identifier for the release, used to distinguish different release versions."),
    version: str = Field(..., description="The version string for the release (e.g., semantic version, build number, or commit hash)."),
    project: str = Field(..., description="The project identifier or slug associated with this error tracking release."),
    metadata: Any | None = Field(None, description="Optional metadata object containing additional information about the release, such as build details, deployment info, or custom tags."),
) -> dict[str, Any]:
    """Update an error tracking release for a project, including its version, hash identifier, and associated metadata."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesUpdateRequest(
            path=_models.ErrorTrackingReleasesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingReleasesUpdateRequestBody(hash_id=hash_id, metadata=metadata, version=version, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_release: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_release")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_release", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_release",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_release_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. Retrieve project IDs from the /api/projects/ endpoint."),
    hash_id: str | None = Field(None, description="An optional identifier hash for the release, used for alternative reference or deduplication purposes."),
    metadata: Any | None = Field(None, description="Optional metadata object to attach or update custom data associated with the release."),
    project: str | None = Field(None, description="Optional project identifier or reference to associate or reassign the release to a different project."),
) -> dict[str, Any]:
    """Partially update an error tracking release for a project. Use this to modify specific fields of an existing release without affecting other attributes."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesPartialUpdateRequest(
            path=_models.ErrorTrackingReleasesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingReleasesPartialUpdateRequestBody(hash_id=hash_id, metadata=metadata, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_release_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_release_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_release_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_release_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def delete_error_tracking_release(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking release from a project. This removes the release and all associated error tracking data."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesDestroyRequest(
            path=_models.ErrorTrackingReleasesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_release: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_release")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_release", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_release",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_release_by_hash(
    hash_id: str = Field(..., description="The unique hash identifier of the release to retrieve. This identifier is used to look up the specific release record in the error tracking system."),
    project_id: str = Field(..., description="The unique identifier of the project containing the release. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific error tracking release using its hash identifier. This operation fetches release metadata and associated error tracking data for a given project."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesHashRetrieveRequest(
            path=_models.ErrorTrackingReleasesHashRetrieveRequestPath(hash_id=hash_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_release_by_hash: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/releases/hash/{hash_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/releases/hash/{hash_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_release_by_hash")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_release_by_hash", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_release_by_hash",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_spike_events(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if needed."),
    limit: int | None = Field(None, description="Maximum number of spike events to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error tracking spike events for a specific project. Spike events represent sudden increases in error occurrences that warrant attention."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSpikeEventsListRequest(
            path=_models.ErrorTrackingSpikeEventsListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingSpikeEventsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_spike_events: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/spike_events", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/spike_events"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_spike_events")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_spike_events", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_spike_events",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def list_error_tracking_suppression_rules(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of suppression rules to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for returning results, enabling pagination through large result sets. Use with limit to control which page of results is returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error tracking suppression rules configured for a specific project. Suppression rules allow you to filter out or ignore certain errors from being tracked."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSuppressionRulesListRequest(
            path=_models.ErrorTrackingSuppressionRulesListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingSuppressionRulesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_suppression_rules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/suppression_rules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/suppression_rules"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_suppression_rules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_suppression_rules", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_suppression_rules",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def get_error_tracking_suppression_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking suppression rule to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the suppression rule. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking suppression rule by its ID. Use this to fetch details about an existing suppression rule configured for your project."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSuppressionRulesRetrieveRequest(
            path=_models.ErrorTrackingSuppressionRulesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_suppression_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/suppression_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/suppression_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_suppression_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_suppression_rule", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_suppression_rule",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_suppression_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the suppression rule to update."),
    project_id: str = Field(..., description="The project ID containing the suppression rule. Find your project ID by calling the /api/projects/ endpoint."),
    filters: Any = Field(..., description="Filter conditions that determine which errors this rule applies to. Specifies the matching criteria for error suppression."),
    order_key: int = Field(..., description="The priority order for this rule relative to other suppression rules. Must be an integer value; lower values indicate higher priority.", ge=-2147483648, le=2147483647),
    disabled_data: Any | None = Field(None, description="Optional flag to disable this suppression rule without deleting it. When set, the rule will not be applied to incoming errors."),
    sampling_rate: float | None = Field(None, description="Optional sampling rate for suppressed errors, expressed as a decimal between 0 and 1. Controls what percentage of matching errors are actually suppressed."),
) -> dict[str, Any]:
    """Update an existing error tracking suppression rule for a project. Modify filtering criteria, ordering, and sampling behavior to control which errors are suppressed."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSuppressionRulesUpdateRequest(
            path=_models.ErrorTrackingSuppressionRulesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSuppressionRulesUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data, sampling_rate=sampling_rate)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_suppression_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/suppression_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/suppression_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_suppression_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_suppression_rule", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_suppression_rule",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def update_error_tracking_suppression_rule_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking suppression rule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the suppression rule. Retrieve project IDs from the /api/projects/ endpoint."),
    filters: Any | None = Field(None, description="Filter criteria to apply for error suppression. Defines which errors match this suppression rule."),
    order_key: int | None = Field(None, description="Numeric ordering key to control the sequence in which suppression rules are evaluated. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
    disabled_data: Any | None = Field(None, description="Configuration for disabling the suppression rule. When set, the rule will not be applied to incoming errors."),
    sampling_rate: float | None = Field(None, description="Sampling rate as a decimal value between 0 and 1, controlling what percentage of matching errors are suppressed (e.g., 0.5 suppresses 50% of matches)."),
) -> dict[str, Any]:
    """Partially update an error tracking suppression rule for a project. Allows modification of filtering criteria, ordering, disabled status, and sampling configuration."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSuppressionRulesPartialUpdateRequest(
            path=_models.ErrorTrackingSuppressionRulesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSuppressionRulesPartialUpdateRequestBody(filters=filters, order_key=order_key, disabled_data=disabled_data, sampling_rate=sampling_rate)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_suppression_rule_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/suppression_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/suppression_rules/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_suppression_rule_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_suppression_rule_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_suppression_rule_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking
@mcp.tool()
async def delete_error_tracking_suppression_rule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking suppression rule to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the suppression rule. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking suppression rule from a project. This removes the rule and stops it from suppressing matching errors."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSuppressionRulesDestroyRequest(
            path=_models.ErrorTrackingSuppressionRulesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_suppression_rule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/suppression_rules/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/suppression_rules/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_suppression_rule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_suppression_rule", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_suppression_rule",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_symbol_sets(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of symbol sets to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, allowing you to paginate through large result sets. Use with limit to control pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of symbol sets for error tracking in a specific project. Symbol sets are used to map obfuscated stack traces back to original source code."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsListRequest(
            path=_models.ErrorTrackingSymbolSetsListRequestPath(project_id=project_id),
            query=_models.ErrorTrackingSymbolSetsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_symbol_sets: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_symbol_sets")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_symbol_sets", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_symbol_sets",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def create_error_tracking_symbol_set(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier for the symbol set, typically used to associate the symbol set with a specific build, version, or release of your application."),
) -> dict[str, Any]:
    """Create a new symbol set for error tracking in a project. Symbol sets are used to map obfuscated or minified code back to original source code for better error analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsCreateRequest(
            path=_models.ErrorTrackingSymbolSetsCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsCreateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_error_tracking_symbol_set: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_error_tracking_symbol_set")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_error_tracking_symbol_set", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_error_tracking_symbol_set",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_symbol_set(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking symbol set by its unique identifier. Symbol sets contain debug information used to map obfuscated stack traces back to original source code."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsRetrieveRequest(
            path=_models.ErrorTrackingSymbolSetsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_symbol_set: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_symbol_set")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_symbol_set", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_symbol_set",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_symbol_set(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier or version marker for the symbol set update. This typically indicates which version or release of symbols is being updated."),
) -> dict[str, Any]:
    """Update an error tracking symbol set for a project. Symbol sets are used to map obfuscated or minified code back to original source for better error tracking and debugging."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsUpdateRequest(
            path=_models.ErrorTrackingSymbolSetsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsUpdateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_symbol_set: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_symbol_set")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_symbol_set", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_symbol_set",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def delete_error_tracking_symbol_set(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking symbol set from a project. This removes the symbol set and all associated symbol data used for error tracking."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsDestroyRequest(
            path=_models.ErrorTrackingSymbolSetsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_symbol_set: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_symbol_set")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_symbol_set", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_symbol_set",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def finish_symbol_set_upload(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to finalize."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. Retrieve available project IDs from the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier or token required to complete the upload operation."),
) -> dict[str, Any]:
    """Completes the upload process for an error tracking symbol set, finalizing the symbols for use in error tracking and analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsFinishUploadUpdateRequest(
            path=_models.ErrorTrackingSymbolSetsFinishUploadUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsFinishUploadUpdateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for finish_symbol_set_upload: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/{id}/finish_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/{id}/finish_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("finish_symbol_set_upload")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("finish_symbol_set_upload", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="finish_symbol_set_upload",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def finish_symbol_set_bulk_upload(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set upload is being finalized. Retrieve this ID from the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier for the bulk upload session being completed. This reference is typically provided when the upload was initiated."),
) -> dict[str, Any]:
    """Completes a bulk symbol set upload for error tracking in a project. This finalizes the upload process after all symbol files have been transferred."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsBulkFinishUploadCreateRequest(
            path=_models.ErrorTrackingSymbolSetsBulkFinishUploadCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsBulkFinishUploadCreateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for finish_symbol_set_bulk_upload: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/bulk_finish_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/bulk_finish_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("finish_symbol_set_bulk_upload")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("finish_symbol_set_bulk_upload", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="finish_symbol_set_bulk_upload",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def start_symbol_sets_bulk_upload(
    project_id: str = Field(..., description="The unique identifier of the project where symbol sets will be uploaded. Retrieve this ID from the /api/projects/ endpoint if needed."),
    ref: str = Field(..., description="A reference identifier or token that associates this bulk upload session with a specific upload batch or transaction, enabling tracking and management of the upload process."),
) -> dict[str, Any]:
    """Initiate a bulk upload process for error tracking symbol sets. This operation prepares the system to receive multiple symbol set files and returns upload credentials or a session reference for subsequent file uploads."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsBulkStartUploadCreateRequest(
            path=_models.ErrorTrackingSymbolSetsBulkStartUploadCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsBulkStartUploadCreateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for start_symbol_sets_bulk_upload: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/bulk_start_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/bulk_start_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("start_symbol_sets_bulk_upload")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("start_symbol_sets_bulk_upload", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="start_symbol_sets_bulk_upload",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def start_symbol_set_upload(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set will be uploaded. Retrieve this ID from the /api/projects/ endpoint if needed."),
    ref: str = Field(..., description="A reference identifier for the symbol set upload session, used to correlate and track the upload across multiple requests."),
) -> dict[str, Any]:
    """Initiates a symbol set upload for error tracking in a specified project. This operation prepares the system to receive symbol files and returns upload credentials or a session identifier for subsequent file transfers."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsStartUploadCreateRequest(
            path=_models.ErrorTrackingSymbolSetsStartUploadCreateRequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsStartUploadCreateRequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for start_symbol_set_upload: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/error_tracking/symbol_sets/start_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/error_tracking/symbol_sets/start_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("start_symbol_set_upload")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("start_symbol_set_upload", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="start_symbol_set_upload",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluation_runs
@mcp.tool()
async def create_evaluation_run(project_id: str = Field(..., description="The unique identifier of the project in which to create the evaluation run. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Create and enqueue a new evaluation run for a project. The request is validated and a Temporal workflow is asynchronously triggered to execute the evaluation."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationRunsCreateRequest(
            path=_models.EvaluationRunsCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_evaluation_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluation_runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluation_runs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_evaluation_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_evaluation_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_evaluation_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def list_evaluations(
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluations. Obtain this ID by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Filter results to show only enabled or disabled evaluations."),
    id__in: list[str] | None = Field(None, description="Filter results to include only evaluations with IDs matching the provided list. Separate multiple IDs with commas."),
    limit: int | None = Field(None, description="Maximum number of evaluations to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    order_by: list[Literal["-created_at", "-name", "-updated_at", "created_at", "name", "updated_at"]] | None = Field(None, description="Sort results by one or more fields. Prefix field names with a hyphen to sort in descending order. Available fields: created_at, updated_at, and name."),
) -> dict[str, Any]:
    """Retrieve a paginated list of evaluations for a specific project, with optional filtering by enabled status, evaluation IDs, and sorting capabilities."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsListRequest(
            path=_models.EvaluationsListRequestPath(project_id=project_id),
            query=_models.EvaluationsListRequestQuery(enabled=enabled, id__in=id__in, limit=limit, offset=offset, order_by=order_by)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_evaluations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_query = _serialize_query(_http_query, {
        "id__in": ("form", False),
        "order_by": ("form", False),
    })
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_evaluations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_evaluations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_evaluations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def create_evaluation(
    project_id: str = Field(..., description="The unique identifier of the project where the evaluation will be created. Retrieve project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the evaluation. Maximum length is 400 characters.", max_length=400),
    evaluation_type: Literal["llm_judge", "hog"] = Field(..., description="The evaluation methodology to use. Choose 'llm_judge' to use an LLM as a judge, or 'hog' for Hog-based evaluation."),
    output_type: Literal["boolean"] = Field(..., description="The format of the evaluation result. Currently only 'boolean' is supported, returning a pass/fail outcome."),
    description: str | None = Field(None, description="An optional detailed description explaining the purpose and scope of the evaluation."),
    enabled: bool | None = Field(None, description="Optional flag to enable or disable the evaluation. When disabled, the evaluation will not be executed."),
    evaluation_config: Any | None = Field(None, description="Optional configuration object specific to the selected evaluation type, containing parameters that customize how the evaluation is performed."),
    output_config: Any | None = Field(None, description="Optional configuration object that defines how the evaluation output is processed, formatted, or mapped to the output type."),
    conditions: Any | None = Field(None, description="Optional conditions or rules that determine when the evaluation should be triggered or how results should be interpreted."),
    model_configuration: _models.EvaluationsCreateBodyModelConfiguration | None = Field(None, description="Optional configuration for the language model used in LLM-based evaluations, including model selection and inference parameters."),
) -> dict[str, Any]:
    """Create a new evaluation for a project to assess model outputs or behaviors. Evaluations can use LLM-based judging or Hog-based analysis and return boolean pass/fail results."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsCreateRequest(
            path=_models.EvaluationsCreateRequestPath(project_id=project_id),
            body=_models.EvaluationsCreateRequestBody(name=name, description=description, enabled=enabled, evaluation_type=evaluation_type, evaluation_config=evaluation_config, output_type=output_type, output_config=output_config, conditions=conditions, model_configuration=model_configuration)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_evaluation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_evaluation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_evaluation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_evaluation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def get_evaluation(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the evaluation to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluation. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific evaluation by its ID within a project. Returns detailed information about the evaluation including its configuration, results, and metadata."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsRetrieveRequest(
            path=_models.EvaluationsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_evaluation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_evaluation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_evaluation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_evaluation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def update_evaluation(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the evaluation to update."),
    project_id: str = Field(..., description="The project ID containing the evaluation. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the evaluation. Must not exceed 400 characters.", max_length=400),
    evaluation_type: Literal["llm_judge", "hog"] = Field(..., description="The evaluation methodology: 'llm_judge' uses an LLM to assess responses, 'hog' uses the Hog algorithm."),
    output_type: Literal["boolean"] = Field(..., description="The format of evaluation results. Currently supports 'boolean' for pass/fail outcomes."),
    description: str | None = Field(None, description="Optional detailed description of the evaluation's purpose and scope."),
    enabled: bool | None = Field(None, description="Optional flag to enable or disable the evaluation without deleting it."),
    evaluation_config: Any | None = Field(None, description="Optional configuration object specific to the selected evaluation_type, containing type-specific parameters and settings."),
    output_config: Any | None = Field(None, description="Optional configuration object that defines how evaluation results are formatted and processed."),
    conditions: Any | None = Field(None, description="Optional array of conditions that determine when the evaluation should be applied or how results are interpreted."),
    model_configuration: _models.EvaluationsUpdateBodyModelConfiguration | None = Field(None, description="Optional configuration object for LLM model selection and parameters when using 'llm_judge' evaluation type."),
) -> dict[str, Any]:
    """Update an existing evaluation configuration within a project. Modify evaluation criteria, type, output settings, and associated conditions to refine how responses are assessed."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsUpdateRequest(
            path=_models.EvaluationsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EvaluationsUpdateRequestBody(name=name, description=description, enabled=enabled, evaluation_type=evaluation_type, evaluation_config=evaluation_config, output_type=output_type, output_config=output_config, conditions=conditions, model_configuration=model_configuration)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_evaluation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_evaluation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_evaluation", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_evaluation",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def update_evaluation_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the evaluation to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluation. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable description of the evaluation's purpose or details."),
    enabled: bool | None = Field(None, description="Whether the evaluation is active and should be executed. Set to true to enable or false to disable."),
    evaluation_type: Literal["llm_judge", "hog"] | None = Field(None, description="The evaluation methodology to use. Choose 'llm_judge' to use an LLM as a judge, or 'hog' for Hog-based evaluation."),
    evaluation_config: Any | None = Field(None, description="Configuration object specific to the selected evaluation type, defining how the evaluation should be executed."),
    output_type: Literal["boolean"] | None = Field(None, description="The format of the evaluation output. Currently supports 'boolean' for pass/fail results."),
    output_config: Any | None = Field(None, description="Configuration object that defines how the evaluation output should be formatted and processed."),
    conditions: Any | None = Field(None, description="Conditional logic or rules that determine when and how the evaluation should be applied."),
    model_configuration: _models.EvaluationsPartialUpdateBodyModelConfiguration | None = Field(None, description="Configuration settings for the language model used in the evaluation, including model selection and parameters."),
) -> dict[str, Any]:
    """Partially update an evaluation configuration within a project. Allows modification of evaluation settings such as type, enabled status, description, and associated configurations."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsPartialUpdateRequest(
            path=_models.EvaluationsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EvaluationsPartialUpdateRequestBody(description=description, enabled=enabled, evaluation_type=evaluation_type, evaluation_config=evaluation_config, output_type=output_type, output_config=output_config, conditions=conditions, model_configuration=model_configuration)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_evaluation_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_evaluation_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_evaluation_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_evaluation_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: evaluations
@mcp.tool()
async def test_hog_evaluation(
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluation environment. Retrieve this ID from the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the evaluation. Must not exceed 400 characters.", max_length=400),
    evaluation_type: Literal["llm_judge", "hog"] = Field(..., description="The type of evaluation logic to use. Must be either 'llm_judge' (LLM as a judge) or 'hog' (Hog code evaluation)."),
    output_type: Literal["boolean"] = Field(..., description="The format of the evaluation result. Currently supports 'boolean' for pass/fail outcomes."),
    description: str | None = Field(None, description="An optional detailed explanation of what this evaluation measures or validates."),
    enabled: bool | None = Field(None, description="Optional flag to indicate whether this evaluation should be active when saved."),
    evaluation_config: Any | None = Field(None, description="Optional configuration object specific to the chosen evaluation type, containing settings that control how the evaluation executes."),
    output_config: Any | None = Field(None, description="Optional configuration object that defines how the evaluation output should be structured or processed."),
    conditions: Any | None = Field(None, description="Optional conditions or filters that determine when this evaluation should be applied to events."),
    model_configuration: _models.EvaluationsTestHogCreateBodyModelConfiguration | None = Field(None, description="Optional configuration for model-specific settings, applicable when using LLM-based evaluation types."),
) -> dict[str, Any]:
    """Validate Hog evaluation code against sample events to verify logic correctness before saving. This operation executes the evaluation without persisting it, allowing you to test and iterate on your evaluation rules."""

    # Construct request model with validation
    try:
        _request = _models.EvaluationsTestHogCreateRequest(
            path=_models.EvaluationsTestHogCreateRequestPath(project_id=project_id),
            body=_models.EvaluationsTestHogCreateRequestBody(name=name, description=description, enabled=enabled, evaluation_type=evaluation_type, evaluation_config=evaluation_config, output_type=output_type, output_config=output_config, conditions=conditions, model_configuration=model_configuration)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for test_hog_evaluation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/evaluations/test_hog", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/evaluations/test_hog"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("test_hog_evaluation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("test_hog_evaluation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="test_hog_evaluation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: health_issues
@mcp.tool()
async def list_health_issues(
    project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of health issues to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for retrieving results, enabling pagination through large result sets. Use with limit to control which page of results is returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of health issues for a specific project. Health issues represent potential problems or warnings detected in the project's environment."""

    # Construct request model with validation
    try:
        _request = _models.HealthIssuesListRequest(
            path=_models.HealthIssuesListRequestPath(project_id=project_id),
            query=_models.HealthIssuesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_health_issues: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/health_issues", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/health_issues"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_health_issues")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_health_issues", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_health_issues",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: health_issues
@mcp.tool()
async def get_health_issue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the health issue to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the health issue. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific health issue by its unique identifier within a project. Returns detailed information about the health issue including its status, severity, and related metadata."""

    # Construct request model with validation
    try:
        _request = _models.HealthIssuesRetrieveRequest(
            path=_models.HealthIssuesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_health_issue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/health_issues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/health_issues/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_health_issue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_health_issue", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_health_issue",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: health_issues
@mcp.tool()
async def update_health_issue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the health issue to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the health issue. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    dismissed: bool | None = Field(None, description="Mark the health issue as dismissed (true) or active (false) to track resolution status."),
) -> dict[str, Any]:
    """Partially update a health issue within a project, allowing you to modify specific fields such as the dismissed status without affecting other properties."""

    # Construct request model with validation
    try:
        _request = _models.HealthIssuesPartialUpdateRequest(
            path=_models.HealthIssuesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.HealthIssuesPartialUpdateRequestBody(dismissed=dismissed)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_health_issue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/health_issues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/health_issues/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_health_issue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_health_issue", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_health_issue",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def list_clustering_jobs(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of clustering jobs to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index from which to start returning results. Use with limit to paginate through the job list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM analytics clustering job configurations for a project. Each team can maintain up to 5 clustering job configurations."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsListRequest(
            path=_models.LlmAnalyticsClusteringJobsListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsClusteringJobsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_clustering_jobs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_clustering_jobs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_clustering_jobs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_clustering_jobs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def create_llm_analytics_clustering_job(
    project_id: str = Field(..., description="The unique identifier of the project where the clustering job will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the clustering job. Must not exceed 100 characters.", max_length=100),
    analysis_level: Literal["trace", "generation"] = Field(..., description="The granularity level at which clustering analysis will be performed. Choose either 'trace' for trace-level analysis or 'generation' for generation-level analysis."),
    event_filters: Any | None = Field(None, description="Optional filters to refine which events are included in the clustering analysis. Specify filter criteria as needed to target relevant data."),
    enabled: bool | None = Field(None, description="Optional flag to enable or disable the clustering job. When enabled, the job will actively process and cluster data according to its configuration."),
) -> dict[str, Any]:
    """Create a new clustering job configuration for LLM analytics. Each team can have a maximum of 5 active clustering jobs."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsCreateRequest(
            path=_models.LlmAnalyticsClusteringJobsCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsClusteringJobsCreateRequestBody(name=name, analysis_level=analysis_level, event_filters=event_filters, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_llm_analytics_clustering_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_llm_analytics_clustering_job")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_llm_analytics_clustering_job", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_llm_analytics_clustering_job",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def get_clustering_job(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the clustering job to retrieve."),
    project_id: str = Field(..., description="The project ID that owns the clustering job. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific LLM analytics clustering job configuration by its unique identifier. Each team can maintain up to 5 clustering job configurations."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsRetrieveRequest(
            path=_models.LlmAnalyticsClusteringJobsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_clustering_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_clustering_job")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_clustering_job", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_clustering_job",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def update_clustering_job(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the clustering job to update."),
    project_id: str = Field(..., description="The project ID containing this clustering job. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the clustering job, up to 100 characters.", max_length=100),
    analysis_level: Literal["trace", "generation"] = Field(..., description="The granularity level for clustering analysis: 'trace' for full trace-level clustering or 'generation' for individual generation-level clustering."),
    event_filters: Any | None = Field(None, description="Optional filters to apply to events included in the clustering analysis. Specify as a structured filter object."),
    enabled: bool | None = Field(None, description="Optional flag to enable or disable this clustering job without deleting it."),
) -> dict[str, Any]:
    """Update an existing LLM analytics clustering job configuration. Each team can maintain up to 5 clustering jobs for analyzing trace or generation-level data."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsUpdateRequest(
            path=_models.LlmAnalyticsClusteringJobsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsClusteringJobsUpdateRequestBody(name=name, analysis_level=analysis_level, event_filters=event_filters, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_clustering_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_clustering_job")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_clustering_job", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_clustering_job",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def update_clustering_job_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the clustering job to update."),
    project_id: str = Field(..., description="The project ID containing the clustering job. Retrieve available project IDs from the /api/projects/ endpoint."),
    analysis_level: Literal["trace", "generation"] | None = Field(None, description="The granularity level for clustering analysis: either 'trace' for trace-level clustering or 'generation' for generation-level clustering."),
    event_filters: Any | None = Field(None, description="Filters to apply when selecting events for clustering analysis. Specify filter criteria to narrow the dataset used in the clustering job."),
    enabled: bool | None = Field(None, description="Whether the clustering job is active. Set to true to enable the job or false to disable it."),
) -> dict[str, Any]:
    """Partially update an LLM analytics clustering job configuration. Allows modification of analysis level, event filters, and enabled status for an existing clustering job within a project."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsPartialUpdateRequest(
            path=_models.LlmAnalyticsClusteringJobsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsClusteringJobsPartialUpdateRequestBody(analysis_level=analysis_level, event_filters=event_filters, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_clustering_job_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_clustering_job_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_clustering_job_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_clustering_job_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def delete_clustering_job(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the clustering job to delete."),
    project_id: str = Field(..., description="The project ID that contains the clustering job. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a clustering job configuration. Each team can maintain a maximum of 5 clustering jobs; removing one frees capacity for new configurations."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsClusteringJobsDestroyRequest(
            path=_models.LlmAnalyticsClusteringJobsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_clustering_job: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/clustering_jobs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_clustering_job")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_clustering_job", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_clustering_job",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def get_llm_analytics_evaluation_config(project_id: str = Field(..., description="The unique identifier of the project whose evaluation configuration you want to retrieve. You can obtain this ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the LLM analytics evaluation configuration for a specific project, which defines how evaluation metrics and criteria are applied to LLM outputs."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsEvaluationConfigRetrieveRequest(
            path=_models.LlmAnalyticsEvaluationConfigRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_llm_analytics_evaluation_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/evaluation_config", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/evaluation_config"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_llm_analytics_evaluation_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_llm_analytics_evaluation_config", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_llm_analytics_evaluation_config",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: LLM Analytics, llm_analytics
@mcp.tool()
async def generate_llm_evaluation_summary(
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluation. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    evaluation_id: str = Field(..., description="The UUID of the evaluation configuration to analyze and summarize."),
    filter_: Literal["all", "pass", "fail", "na"] | None = Field(None, alias="filter", description="Filter the evaluation results to analyze: 'all' includes all results, 'pass' includes only passing evaluations, 'fail' includes only failing evaluations, and 'na' includes results with no applicable status. Defaults to 'all' if not specified."),
    generation_ids: list[str] | None = Field(None, description="Optional list of specific generation IDs to include in the summary analysis. If provided, the summary will focus only on these generations. Maximum of 250 IDs allowed.", max_length=250),
) -> dict[str, Any]:
    """Generate an AI-powered analysis of evaluation results that identifies patterns in passing and failing evaluations and provides actionable recommendations for improving LLM response quality."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsEvaluationSummaryCreateRequest(
            path=_models.LlmAnalyticsEvaluationSummaryCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsEvaluationSummaryCreateRequestBody(evaluation_id=evaluation_id, filter_=filter_, generation_ids=generation_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_llm_evaluation_summary: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/evaluation_summary", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/evaluation_summary"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_llm_evaluation_summary")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_llm_evaluation_summary", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_llm_evaluation_summary",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def list_llm_analytics_provider_keys(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if you don't have it."),
    limit: int | None = Field(None, description="Maximum number of provider keys to return in a single page of results. Omit to use the default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM analytics provider API keys configured for a specific project. Use this to view all provider credentials and their configurations."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysListRequest(
            path=_models.LlmAnalyticsProviderKeysListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsProviderKeysListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_llm_analytics_provider_keys: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_llm_analytics_provider_keys")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_llm_analytics_provider_keys", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_llm_analytics_provider_keys",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def get_llm_provider_key(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the provider key. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific LLM provider key configuration by its unique identifier within a project. This returns the details of a single provider key used for LLM analytics."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysRetrieveRequest(
            path=_models.LlmAnalyticsProviderKeysRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_llm_provider_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_llm_provider_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_llm_provider_key", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_llm_provider_key",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def update_llm_provider_key(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to update."),
    project_id: str = Field(..., description="The project ID that owns this LLM provider key. Retrieve available project IDs from the /api/projects/ endpoint."),
    provider: Literal["openai", "anthropic", "gemini", "openrouter", "fireworks"] = Field(..., description="The LLM provider type for this key. Must be one of: openai, anthropic, gemini, openrouter, or fireworks."),
    name: str = Field(..., description="A display name for this provider key configuration. Maximum 255 characters.", max_length=255),
) -> dict[str, Any]:
    """Update an LLM provider API key configuration for analytics tracking. Modify the provider type and display name for an existing key credential."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysUpdateRequest(
            path=_models.LlmAnalyticsProviderKeysUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsProviderKeysUpdateRequestBody(provider=provider, name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_llm_provider_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_llm_provider_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_llm_provider_key", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_llm_provider_key",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def update_llm_provider_key_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the provider key. Retrieve available project IDs from the /api/projects/ endpoint."),
    provider: Literal["openai", "anthropic", "gemini", "openrouter", "fireworks"] | None = Field(None, description="The LLM provider type for this key. Must be one of: openai, anthropic, gemini, openrouter, or fireworks."),
) -> dict[str, Any]:
    """Partially update an LLM provider API key configuration for a specific project. Allows modification of the provider type or other key attributes without replacing the entire configuration."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysPartialUpdateRequest(
            path=_models.LlmAnalyticsProviderKeysPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsProviderKeysPartialUpdateRequestBody(provider=provider)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_llm_provider_key_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_llm_provider_key_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_llm_provider_key_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_llm_provider_key_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def delete_llm_provider_key(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the provider key. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an LLM provider API key from a project. This removes the stored credentials and disables any analytics collection using this key."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysDestroyRequest(
            path=_models.LlmAnalyticsProviderKeysDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_llm_provider_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_llm_provider_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_llm_provider_key", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_llm_provider_key",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def assign_llm_provider_key_to_evaluations(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to assign."),
    project_id: str = Field(..., description="The unique identifier of the project containing the evaluations. Retrieve available project IDs from the /api/projects/ endpoint."),
    provider: Literal["openai", "anthropic", "gemini", "openrouter", "fireworks"] = Field(..., description="The LLM provider associated with this key. Must be one of: openai, anthropic, gemini, openrouter, or fireworks."),
    name: str = Field(..., description="A descriptive name for this key assignment. Maximum 255 characters.", max_length=255),
) -> dict[str, Any]:
    """Assign an LLM provider API key to evaluations within a project, optionally re-enabling them for use. This operation links a stored provider key to evaluation workflows."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysAssignCreateRequest(
            path=_models.LlmAnalyticsProviderKeysAssignCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsProviderKeysAssignCreateRequestBody(provider=provider, name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for assign_llm_provider_key_to_evaluations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}/assign", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}/assign"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("assign_llm_provider_key_to_evaluations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("assign_llm_provider_key_to_evaluations", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="assign_llm_provider_key_to_evaluations",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def list_dependent_configs_for_provider_key(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to retrieve dependent configurations for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the provider key. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all evaluations and configurations that depend on a specific LLM provider key, along with alternative keys that can be used as replacements."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysDependentConfigsRetrieveRequest(
            path=_models.LlmAnalyticsProviderKeysDependentConfigsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dependent_configs_for_provider_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}/dependent_configs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}/dependent_configs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dependent_configs_for_provider_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dependent_configs_for_provider_key", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dependent_configs_for_provider_key",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics
@mcp.tool()
async def validate_llm_provider_key(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the LLM provider key to validate."),
    project_id: str = Field(..., description="The unique identifier of the project containing the provider key. Retrieve this ID from the /api/projects/ endpoint if needed."),
    provider: Literal["openai", "anthropic", "gemini", "openrouter", "fireworks"] = Field(..., description="The LLM provider associated with the key. Must be one of: openai, anthropic, gemini, openrouter, or fireworks."),
    name: str = Field(..., description="A descriptive name for the provider key. Maximum length is 255 characters.", max_length=255),
) -> dict[str, Any]:
    """Validate an LLM provider API key to ensure it is correctly configured and has proper permissions. This operation tests the key's authenticity and functionality with the specified provider."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsProviderKeysValidateCreateRequest(
            path=_models.LlmAnalyticsProviderKeysValidateCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsProviderKeysValidateCreateRequestBody(provider=provider, name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for validate_llm_provider_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/provider_keys/{id}/validate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/provider_keys/{id}/validate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("validate_llm_provider_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("validate_llm_provider_key", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="validate_llm_provider_key",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def list_review_queue_items(
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue items. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of items to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index from which to start returning results. Use with limit to paginate through large result sets."),
    order_by: str | None = Field(None, description="Sort results by creation or modification timestamp. Use '-created_at' for newest first or 'created_at' for oldest first; similarly for 'updated_at'."),
    queue_id: str | None = Field(None, description="Filter results to a specific review queue by its UUID identifier."),
) -> dict[str, Any]:
    """Retrieve a paginated list of items in LLM analytics review queues for a specific project, with optional filtering and sorting capabilities."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueueItemsListRequest(
            path=_models.LlmAnalyticsReviewQueueItemsListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsReviewQueueItemsListRequestQuery(limit=limit, offset=offset, order_by=order_by, queue_id=queue_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_review_queue_items: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queue_items", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queue_items"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_review_queue_items")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_review_queue_items", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_review_queue_items",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def add_trace_to_review_queue(
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue. Retrieve this ID from the /api/projects/ endpoint if needed."),
    queue_id: str = Field(..., description="The unique identifier (UUID format) of the review queue that will own this trace."),
    trace_id: str = Field(..., description="The unique identifier of the trace to add to the review queue. Must be 255 characters or fewer.", max_length=255),
) -> dict[str, Any]:
    """Add a trace to an LLM analytics review queue for manual inspection and evaluation. This operation associates a specific trace with a review queue to enable team review workflows."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueueItemsCreateRequest(
            path=_models.LlmAnalyticsReviewQueueItemsCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsReviewQueueItemsCreateRequestBody(queue_id=queue_id, trace_id=trace_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_trace_to_review_queue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queue_items", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queue_items"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_trace_to_review_queue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_trace_to_review_queue", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_trace_to_review_queue",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def get_review_queue_item(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue item to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the review queue item. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific LLM analytics review queue item by its unique identifier within a project."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueueItemsRetrieveRequest(
            path=_models.LlmAnalyticsReviewQueueItemsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_review_queue_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queue_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queue_items/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_review_queue_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_review_queue_item", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_review_queue_item",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def update_review_queue_item(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue item to update."),
    project_id: str = Field(..., description="The project ID that owns the review queue item. Find your project ID by calling the /api/projects/ endpoint."),
    queue_id: str | None = Field(None, description="The UUID of the review queue that should own this pending trace. Omit to keep the current queue assignment."),
) -> dict[str, Any]:
    """Partially update a review queue item by reassigning it to a different review queue or modifying its metadata. Use this to move pending traces between queues or update queue ownership."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueueItemsPartialUpdateRequest(
            path=_models.LlmAnalyticsReviewQueueItemsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsReviewQueueItemsPartialUpdateRequestBody(queue_id=queue_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_review_queue_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queue_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queue_items/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_review_queue_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_review_queue_item", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_review_queue_item",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def delete_llm_analytics_review_queue_item(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue item to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue item. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific review queue item from LLM analytics. This removes the item and its associated data from the review queue."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueueItemsDestroyRequest(
            path=_models.LlmAnalyticsReviewQueueItemsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_llm_analytics_review_queue_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queue_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queue_items/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_llm_analytics_review_queue_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_llm_analytics_review_queue_item", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_llm_analytics_review_queue_item",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def list_llm_analytics_review_queues(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if needed."),
    limit: int | None = Field(None, description="Maximum number of review queues to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    order_by: str | None = Field(None, description="Sort the results by queue name (alphabetical), creation timestamp, or last update timestamp."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM analytics review queues for a specific project. Results can be sorted by name, creation date, or last update time."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueuesListRequest(
            path=_models.LlmAnalyticsReviewQueuesListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsReviewQueuesListRequestQuery(limit=limit, offset=offset, order_by=order_by)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_llm_analytics_review_queues: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queues", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queues"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_llm_analytics_review_queues")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_llm_analytics_review_queues", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_llm_analytics_review_queues",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def create_llm_analytics_review_queue(
    project_id: str = Field(..., description="The unique identifier of the project where the review queue will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the review queue. Must be 255 characters or fewer.", max_length=255),
) -> dict[str, Any]:
    """Create a new review queue for LLM analytics within a specified project. Review queues organize and manage items for human review in the LLM analytics pipeline."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueuesCreateRequest(
            path=_models.LlmAnalyticsReviewQueuesCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsReviewQueuesCreateRequestBody(name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_llm_analytics_review_queue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queues", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queues"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_llm_analytics_review_queue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_llm_analytics_review_queue", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_llm_analytics_review_queue",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def get_review_queue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific LLM analytics review queue by its unique identifier within a project."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueuesRetrieveRequest(
            path=_models.LlmAnalyticsReviewQueuesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_review_queue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queues/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_review_queue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_review_queue", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_review_queue",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def update_review_queue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Partially update an LLM analytics review queue configuration within a specific project. Use this to modify queue settings, filters, or other properties without replacing the entire queue."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueuesPartialUpdateRequest(
            path=_models.LlmAnalyticsReviewQueuesPartialUpdateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_review_queue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queues/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_review_queue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_review_queue", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_review_queue",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def delete_review_queue(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the review queue to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the review queue. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific LLM analytics review queue from a project. This operation removes the queue and all associated data."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsReviewQueuesDestroyRequest(
            path=_models.LlmAnalyticsReviewQueuesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_review_queue: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/review_queues/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/review_queues/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_review_queue")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_review_queue", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_review_queue",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def list_llm_analytics_score_definitions(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if needed."),
    kind: str | None = Field(None, description="Filter results to include only score definitions of a specific scorer kind, such as 'categorical' for categorical scorers."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    order_by: str | None = Field(None, description="Sort results by one of: name, kind, created_at, updated_at, or current_version."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM analytics score definitions for a project, with optional filtering by scorer kind and sorting capabilities."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsScoreDefinitionsListRequest(
            path=_models.LlmAnalyticsScoreDefinitionsListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsScoreDefinitionsListRequestQuery(kind=kind, limit=limit, offset=offset, order_by=order_by)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_llm_analytics_score_definitions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/score_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/score_definitions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_llm_analytics_score_definitions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_llm_analytics_score_definitions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_llm_analytics_score_definitions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def create_llm_analytics_score_definition(
    project_id: str = Field(..., description="The unique identifier of the project where the score definition will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the scorer, up to 255 characters. This name identifies the score definition in analytics dashboards and reports.", max_length=255),
    kind: Literal["categorical", "numeric", "boolean"] = Field(..., description="The type of scoring metric: categorical (discrete categories), numeric (continuous values), or boolean (true/false). This cannot be changed after the score definition is created."),
    config: _models.CategoricalScoreDefinitionConfig | _models.NumericScoreDefinitionConfig | _models.BooleanScoreDefinitionConfig = Field(..., description="The initial configuration for the scorer, which defines its behavior and cannot be modified after creation. Configuration structure depends on the selected scorer kind."),
    description: str | None = Field(None, description="An optional human-readable explanation of what this scorer measures and how it should be applied."),
) -> dict[str, Any]:
    """Create a new LLM analytics score definition for evaluating model outputs. Score definitions are immutable after creation and define how responses will be scored using categorical, numeric, or boolean metrics."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsScoreDefinitionsCreateRequest(
            path=_models.LlmAnalyticsScoreDefinitionsCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsScoreDefinitionsCreateRequestBody(name=name, description=description, kind=kind, config=config)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_llm_analytics_score_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/score_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/score_definitions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_llm_analytics_score_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_llm_analytics_score_definition", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_llm_analytics_score_definition",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def get_llm_analytics_score_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the score definition to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the score definition. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific LLM analytics score definition by its unique identifier within a project. Use this to fetch configuration and metadata for a particular scoring metric."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsScoreDefinitionsRetrieveRequest(
            path=_models.LlmAnalyticsScoreDefinitionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_llm_analytics_score_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/score_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/score_definitions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_llm_analytics_score_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_llm_analytics_score_definition", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_llm_analytics_score_definition",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def update_llm_analytics_score_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the score definition to update."),
    project_id: str = Field(..., description="The project ID that contains the score definition. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    description: str | None = Field(None, description="Updated text description for the scorer, providing context about its purpose or behavior."),
) -> dict[str, Any]:
    """Partially update an LLM analytics score definition within a project. Allows modification of scorer metadata such as description while preserving other configuration details."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsScoreDefinitionsPartialUpdateRequest(
            path=_models.LlmAnalyticsScoreDefinitionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsScoreDefinitionsPartialUpdateRequestBody(description=description)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_llm_analytics_score_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/score_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/score_definitions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_llm_analytics_score_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_llm_analytics_score_definition", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_llm_analytics_score_definition",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def create_score_definition_new_version(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the score definition to create a new version for."),
    project_id: str = Field(..., description="The project ID that contains the score definition. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    config: _models.CategoricalScoreDefinitionConfig | _models.NumericScoreDefinitionConfig | _models.BooleanScoreDefinitionConfig = Field(..., description="The next immutable scorer configuration object that defines the scoring behavior for this new version."),
) -> dict[str, Any]:
    """Create a new version of an existing LLM analytics score definition with an updated immutable scorer configuration. This allows you to evolve scoring logic while maintaining version history."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsScoreDefinitionsNewVersionCreateRequest(
            path=_models.LlmAnalyticsScoreDefinitionsNewVersionCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsScoreDefinitionsNewVersionCreateRequestBody(config=config)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_score_definition_new_version: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/score_definitions/{id}/new_version", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/score_definitions/{id}/new_version"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_score_definition_new_version")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_score_definition_new_version", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_score_definition_new_version",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: LLM Analytics, llm_analytics
@mcp.tool()
async def analyze_sentiment_for_llm_outputs(
    project_id: str = Field(..., description="The unique identifier of the project containing the LLM outputs to analyze. Retrieve your project ID from the /api/projects/ endpoint."),
    ids: list[str] = Field(..., description="Array of output IDs to analyze for sentiment. Must contain between 1 and 5 IDs. Order is preserved in the analysis results.", min_length=1, max_length=5),
    analysis_level: Literal["trace", "generation"] | None = Field(None, description="Granularity level for sentiment analysis results. Defaults to trace-level analysis if not specified."),
) -> dict[str, Any]:
    """Analyze sentiment for one or more LLM outputs within a project. Performs sentiment analysis on the specified outputs at the requested analysis level (defaults to trace-level detail)."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsSentimentCreateRequest(
            path=_models.LlmAnalyticsSentimentCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsSentimentCreateRequestBody(ids=ids, analysis_level=analysis_level)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for analyze_sentiment_for_llm_outputs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/sentiment", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/sentiment"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("analyze_sentiment_for_llm_outputs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("analyze_sentiment_for_llm_outputs", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="analyze_sentiment_for_llm_outputs",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: LLM Analytics, llm_analytics
@mcp.tool()
async def batch_check_cached_summaries(
    project_id: str = Field(..., description="The project ID containing the traces. Find your project ID by calling the /api/projects/ endpoint."),
    trace_ids: list[str] = Field(..., description="List of trace IDs to check for cached summaries. Up to 100 trace IDs can be checked in a single request.", max_length=100),
    mode: Literal["minimal", "detailed"] | None = Field(None, description="The summary detail level to check for: either minimal (brief summaries) or detailed (comprehensive summaries). Defaults to minimal if not specified."),
    model: str | None = Field(None, description="The LLM model identifier used to generate the cached summaries. Use this to filter results by a specific model if multiple models have generated summaries."),
) -> dict[str, Any]:
    """Check which traces have cached summaries available in batch. This endpoint validates multiple trace IDs against the summary cache, returning only those with existing summaries to avoid redundant LLM processing."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsSummarizationBatchCheckCreateRequest(
            path=_models.LlmAnalyticsSummarizationBatchCheckCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsSummarizationBatchCheckCreateRequestBody(trace_ids=trace_ids, mode=mode, model=model)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for batch_check_cached_summaries: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/summarization/batch_check", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/summarization/batch_check"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("batch_check_cached_summaries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("batch_check_cached_summaries", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="batch_check_cached_summaries",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def list_trace_reviews(
    project_id: str = Field(..., description="The unique identifier of the project containing the trace reviews. Obtain this ID by calling the /api/projects/ endpoint."),
    definition_id: str | None = Field(None, description="Filter results to only include trace reviews associated with a specific scorer definition, specified as a UUID."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position from which to begin returning results, used for pagination."),
    order_by: str | None = Field(None, description="Sort results by either creation time or last update time. Use `-updated_at` for newest first or `updated_at` for oldest first; similarly for `created_at`."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM trace reviews for a project, optionally filtered by scorer definition and sorted by creation or update time."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsTraceReviewsListRequest(
            path=_models.LlmAnalyticsTraceReviewsListRequestPath(project_id=project_id),
            query=_models.LlmAnalyticsTraceReviewsListRequestQuery(definition_id=definition_id, limit=limit, offset=offset, order_by=order_by)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_trace_reviews: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/trace_reviews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/trace_reviews"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_trace_reviews")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_trace_reviews", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_trace_reviews",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def create_trace_review(
    project_id: str = Field(..., description="The unique identifier of the project containing the trace. Retrieve this ID from the /api/projects/ endpoint."),
    trace_id: str = Field(..., description="The unique identifier of the trace being reviewed. Must be 255 characters or fewer. Only one active review per trace and team is permitted.", max_length=255),
    comment: str | None = Field(None, description="Optional human-readable comment or reasoning explaining the review decision."),
    scores: list[_models.TraceReviewScoreWrite] | None = Field(None, description="Optional array of score objects for this review. Include only the scorers you want to populate; omit any you wish to leave blank. Order and format depend on your scoring schema."),
    queue_id: str | None = Field(None, description="Optional UUID of the review queue this review belongs to. When provided, the corresponding pending queue item is automatically cleared after the review is saved. If omitted, any pending queue item for this trace is cleared regardless."),
) -> dict[str, Any]:
    """Create a review for an LLM trace, recording human feedback, scores, and optional reasoning. Only one active review can exist per trace and team."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsTraceReviewsCreateRequest(
            path=_models.LlmAnalyticsTraceReviewsCreateRequestPath(project_id=project_id),
            body=_models.LlmAnalyticsTraceReviewsCreateRequestBody(trace_id=trace_id, comment=comment, scores=scores, queue_id=queue_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_trace_review: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/trace_reviews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/trace_reviews"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_trace_review")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_trace_review", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_trace_review",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def get_trace_review(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the trace review to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the trace review. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific trace review by its unique identifier within a project. Use this to fetch detailed information about a single LLM analytics trace review."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsTraceReviewsRetrieveRequest(
            path=_models.LlmAnalyticsTraceReviewsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_trace_review: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/trace_reviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/trace_reviews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_trace_review")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_trace_review", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_trace_review",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def update_trace_review(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the trace review to update."),
    project_id: str = Field(..., description="The project ID containing the trace review. Retrieve available project IDs from the /api/projects/ endpoint."),
    comment: str | None = Field(None, description="Optional human-readable comment or reasoning documenting the review decision."),
    scores: list[_models.TraceReviewScoreWrite] | None = Field(None, description="Optional array of score objects representing the complete desired score set for this review. Omit any scorers you want to leave unchanged or blank. Array order and item structure should match the API's score format specification."),
    queue_id: str | None = Field(None, description="Optional review queue ID (UUID format) to associate this review with a specific queue context. When provided, the matching pending queue item is cleared after the review is saved. If omitted, any pending queue item for the same trace is automatically cleared."),
) -> dict[str, Any]:
    """Partially update a trace review with new scores, comments, or queue context. Clears associated pending queue items upon save."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsTraceReviewsPartialUpdateRequest(
            path=_models.LlmAnalyticsTraceReviewsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LlmAnalyticsTraceReviewsPartialUpdateRequestBody(comment=comment, scores=scores, queue_id=queue_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_trace_review: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/trace_reviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/trace_reviews/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_trace_review")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_trace_review", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_trace_review",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_analytics
@mcp.tool()
async def delete_trace_review(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the trace review to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the trace review. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific trace review from an LLM analytics project. This operation removes the trace review record and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.LlmAnalyticsTraceReviewsDestroyRequest(
            path=_models.LlmAnalyticsTraceReviewsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_trace_review: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_analytics/trace_reviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_analytics/trace_reviews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_trace_review")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_trace_review", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_trace_review",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def list_llm_prompts(
    project_id: str = Field(..., description="The unique identifier of the project containing the LLM prompts. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or set to default for standard pagination."),
    offset: int | None = Field(None, description="Zero-based starting position for results. Use this with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of LLM prompts for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsListRequest(
            path=_models.LlmPromptsListRequestPath(project_id=project_id),
            query=_models.LlmPromptsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_llm_prompts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_llm_prompts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_llm_prompts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_llm_prompts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def create_llm_prompt(
    project_id: str = Field(..., description="The unique identifier of the project where the prompt will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the prompt using only letters, numbers, hyphens, and underscores. Maximum length is 255 characters.", max_length=255),
    prompt: Any = Field(..., description="The prompt content provided as either a JSON object or string. This defines the actual prompt template or instructions for the LLM."),
) -> dict[str, Any]:
    """Create a new LLM prompt in a project. The prompt can be defined as either a JSON object or string data and must have a unique name within the project."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsCreateRequest(
            path=_models.LlmPromptsCreateRequestPath(project_id=project_id),
            body=_models.LlmPromptsCreateRequestBody(name=name, prompt=prompt)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_llm_prompt: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_llm_prompt")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_llm_prompt", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_llm_prompt",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def get_llm_prompt_by_name(
    project_id: str = Field(..., description="The unique identifier of the project containing the LLM prompt. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    prompt_name: str = Field(..., description="The name of the LLM prompt to retrieve. The name cannot contain forward slashes and must be a valid prompt identifier within the project.", pattern="^[^/]+$"),
) -> dict[str, Any]:
    """Retrieve a specific LLM prompt configuration by its name within a project. Use this to fetch prompt details for execution or inspection."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsNameRetrieveRequest(
            path=_models.LlmPromptsNameRetrieveRequestPath(project_id=project_id, prompt_name=prompt_name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_llm_prompt_by_name: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts/name/{prompt_name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts/name/{prompt_name}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_llm_prompt_by_name")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_llm_prompt_by_name", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_llm_prompt_by_name",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def update_llm_prompt_name(
    project_id: str = Field(..., description="The unique identifier of the project containing the prompt. Retrieve available project IDs from the /api/projects/ endpoint."),
    prompt_name: str = Field(..., description="The current name of the LLM prompt to update. Must not contain forward slashes.", pattern="^[^/]+$"),
    base_version: int | None = Field(None, description="The latest version number of the prompt you are editing from, used to prevent concurrent modification conflicts. Must be at least 1.", ge=1),
) -> dict[str, Any]:
    """Partially update the name of an LLM prompt within a project. Supports optimistic concurrency control via base version tracking."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsNamePartialUpdateRequest(
            path=_models.LlmPromptsNamePartialUpdateRequestPath(project_id=project_id, prompt_name=prompt_name),
            body=_models.LlmPromptsNamePartialUpdateRequestBody(base_version=base_version)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_llm_prompt_name: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts/name/{prompt_name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts/name/{prompt_name}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_llm_prompt_name")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_llm_prompt_name", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_llm_prompt_name",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def archive_llm_prompt_by_name(
    project_id: str = Field(..., description="The unique identifier of the project containing the prompt. Retrieve this ID by calling the /api/projects/ endpoint."),
    prompt_name: str = Field(..., description="The name of the prompt to archive. Must not contain forward slashes.", pattern="^[^/]+$"),
    name: str = Field(..., description="The unique identifier for the prompt using only letters, numbers, hyphens, and underscores. Maximum length is 255 characters.", max_length=255),
    prompt: Any = Field(..., description="The prompt content and configuration as a JSON object or string. This payload defines the prompt's behavior and parameters."),
) -> dict[str, Any]:
    """Archive an LLM prompt by its name within a specific project. This operation creates an archived version of the prompt, preserving it for historical reference while removing it from active use."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsNameArchiveCreateRequest(
            path=_models.LlmPromptsNameArchiveCreateRequestPath(project_id=project_id, prompt_name=prompt_name),
            body=_models.LlmPromptsNameArchiveCreateRequestBody(name=name, prompt=prompt)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for archive_llm_prompt_by_name: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts/name/{prompt_name}/archive", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts/name/{prompt_name}/archive"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("archive_llm_prompt_by_name")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("archive_llm_prompt_by_name", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="archive_llm_prompt_by_name",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def duplicate_llm_prompt(
    project_id: str = Field(..., description="The unique identifier of the project containing the prompt to duplicate. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    prompt_name: str = Field(..., description="The name of the existing LLM prompt to duplicate. The name cannot contain forward slashes.", pattern="^[^/]+$"),
    new_name: str = Field(..., description="The name for the new duplicated prompt. Must be unique within the project and contain only letters, numbers, hyphens, and underscores. Maximum length is 255 characters.", max_length=255),
) -> dict[str, Any]:
    """Creates a duplicate copy of an existing LLM prompt with a new unique name. The duplicated prompt retains all configuration and settings from the original."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsNameDuplicateCreateRequest(
            path=_models.LlmPromptsNameDuplicateCreateRequestPath(project_id=project_id, prompt_name=prompt_name),
            body=_models.LlmPromptsNameDuplicateCreateRequestBody(new_name=new_name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for duplicate_llm_prompt: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts/name/{prompt_name}/duplicate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts/name/{prompt_name}/duplicate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("duplicate_llm_prompt")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("duplicate_llm_prompt", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="duplicate_llm_prompt",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, llm_prompts
@mcp.tool()
async def get_llm_prompt_by_name_resolved(
    project_id: str = Field(..., description="The unique identifier of the project containing the LLM prompt. Obtain this ID by calling the projects endpoint."),
    prompt_name: str = Field(..., description="The name of the LLM prompt to retrieve. Must not contain forward slashes.", pattern="^[^/]+$"),
    before_version: int | None = Field(None, description="Retrieve only prompt versions older than this version number (must be 1 or greater). Cannot be used together with offset for pagination.", ge=1),
    limit: int | None = Field(None, description="Maximum number of versions to return per page. Accepts values between 1 and 100, defaults to 50 if not specified.", ge=1, le=100),
    offset: int | None = Field(None, description="Zero-based position in the version history for pagination. Cannot be used together with before_version. Useful for iterating through large version histories.", ge=0),
) -> dict[str, Any]:
    """Retrieve a specific LLM prompt by name within a project, with support for version history pagination. Returns the prompt definition and metadata, optionally filtered to a specific version range."""

    # Construct request model with validation
    try:
        _request = _models.LlmPromptsResolveNameRetrieveRequest(
            path=_models.LlmPromptsResolveNameRetrieveRequestPath(project_id=project_id, prompt_name=prompt_name),
            query=_models.LlmPromptsResolveNameRetrieveRequestQuery(before_version=before_version, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_llm_prompt_by_name_resolved: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/llm_prompts/resolve/name/{prompt_name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/llm_prompts/resolve/name/{prompt_name}"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_llm_prompt_by_name_resolved")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_llm_prompt_by_name_resolved", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_llm_prompt_by_name_resolved",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def explain_log_with_ai(
    project_id: str = Field(..., description="The unique identifier of the project containing the log entry. Retrieve this ID from the /api/projects/ endpoint if needed."),
    uuid_: str = Field(..., alias="uuid", description="The unique identifier (UUID) of the log entry you want to explain."),
    timestamp: str = Field(..., description="The timestamp when the log entry was created, formatted as an ISO 8601 date-time string. This enables efficient lookup of the log entry."),
) -> dict[str, Any]:
    """Generate an AI-powered explanation for a specific log entry to help understand its content and context."""

    # Construct request model with validation
    try:
        _request = _models.LogsExplainLogWithAiCreateRequest(
            path=_models.LogsExplainLogWithAiCreateRequestPath(project_id=project_id),
            body=_models.LogsExplainLogWithAiCreateRequestBody(uuid_=uuid_, timestamp=timestamp)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for explain_log_with_ai: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/explainLogWithAI", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/explainLogWithAI"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("explain_log_with_ai")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("explain_log_with_ai", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="explain_log_with_ai",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def list_log_views(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value by calling the /api/projects/ endpoint if you don't have it."),
    limit: int | None = Field(None, description="Maximum number of log views to return in a single response page. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of log views configured for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsListRequest(
            path=_models.LogsViewsListRequestPath(project_id=project_id),
            query=_models.LogsViewsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_log_views: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_log_views")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_log_views", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_log_views",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def create_logs_view(
    project_id: str = Field(..., description="The unique identifier of the project where the logs view will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name for the logs view. Must be 400 characters or fewer.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Optional filter criteria to apply to the logs view. Can include severity levels, service names, search terms, filter groups, date ranges, and other filtering options supported by the logs viewer."),
    pinned: bool | None = Field(None, description="Optional flag to pin this logs view for quick access. When set to true, the view will be highlighted or prioritized in the view list."),
) -> dict[str, Any]:
    """Create a new logs view for a project with optional filtering criteria and pinning. Views allow you to save and organize log queries with custom filters and display preferences."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsCreateRequest(
            path=_models.LogsViewsCreateRequestPath(project_id=project_id),
            body=_models.LogsViewsCreateRequestBody(name=name, filters=filters, pinned=pinned)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_logs_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_logs_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_logs_view", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_logs_view",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def get_log_view(
    project_id: str = Field(..., description="The unique identifier of the project containing the log view. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the log view to retrieve."),
) -> dict[str, Any]:
    """Retrieve a specific log view configuration by its short identifier within a project. This returns the details and settings for a saved log view."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsRetrieveRequest(
            path=_models.LogsViewsRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_log_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views/{short_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_log_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_log_view", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_log_view",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def update_logs_view(
    project_id: str = Field(..., description="The unique identifier of the project containing the logs view. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the logs view to update."),
    name: str = Field(..., description="The display name for the logs view. Must not exceed 400 characters.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Filter criteria to apply to the logs view. Supports severityLevels, serviceNames, searchTerm, filterGroup, dateRange, and other filter properties to narrow log results."),
    pinned: bool | None = Field(None, description="Whether to pin this logs view for quick access. When true, the view appears in a pinned section for easy reference."),
) -> dict[str, Any]:
    """Update an existing logs view configuration for a project, including its name, filter criteria, and pinned status."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsUpdateRequest(
            path=_models.LogsViewsUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.LogsViewsUpdateRequestBody(name=name, filters=filters, pinned=pinned)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_logs_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_logs_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_logs_view", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_logs_view",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def update_logs_view_partial(
    project_id: str = Field(..., description="The unique identifier of the project containing the logs view. Retrieve this ID from the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the logs view to update."),
    filters: dict[str, Any] | None = Field(None, description="Filter criteria to apply to the logs view. May include severityLevels, serviceNames, searchTerm, filterGroup, dateRange, and other filter keys to narrow down displayed logs."),
    pinned: bool | None = Field(None, description="Whether to pin this logs view for quick access. Set to true to pin or false to unpin."),
) -> dict[str, Any]:
    """Partially update a logs view configuration for a project, allowing you to modify filter criteria and pinned status without replacing the entire view."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsPartialUpdateRequest(
            path=_models.LogsViewsPartialUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.LogsViewsPartialUpdateRequestBody(filters=filters, pinned=pinned)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_logs_view_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_logs_view_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_logs_view_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_logs_view_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def delete_log_view(
    project_id: str = Field(..., description="The unique identifier of the project containing the log view. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the log view to delete."),
) -> dict[str, Any]:
    """Permanently delete a log view from a project. This operation removes the specified log view and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.LogsViewsDestroyRequest(
            path=_models.LogsViewsDestroyRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_log_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/logs/views/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/logs/views/{short_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_log_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_log_view", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_log_view",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: max_tools
@mcp.tool()
async def create_and_query_insight(project_id: str = Field(..., description="The unique identifier of the project where the insight will be created and queried. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Create a new insight and execute a query against it in a single operation. This combines insight creation with immediate querying to streamline analysis workflows."""

    # Construct request model with validation
    try:
        _request = _models.MaxToolsCreateAndQueryInsightCreateRequest(
            path=_models.MaxToolsCreateAndQueryInsightCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_and_query_insight: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/max_tools/create_and_query_insight", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/max_tools/create_and_query_insight"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_and_query_insight")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_and_query_insight", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_and_query_insight",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_summaries
@mcp.tool()
async def generate_session_summaries(
    project_id: str = Field(..., description="The unique identifier of the project containing the sessions to summarize. Retrieve this ID from the /api/projects/ endpoint if needed."),
    session_ids: list[str] = Field(..., description="An array of session IDs to include in the summary analysis. Provide between 1 and 300 session IDs; order is preserved for processing.", min_length=1, max_length=300),
    focus_area: str | None = Field(None, description="An optional focus area to guide the summarization analysis, such as a specific user behavior, feature, or metric of interest. Limited to 500 characters.", max_length=500),
) -> dict[str, Any]:
    """Generate an AI-powered summary across multiple session recordings to identify patterns and insights, producing a structured notebook of findings."""

    # Construct request model with validation
    try:
        _request = _models.CreateSessionSummariesRequest(
            path=_models.CreateSessionSummariesRequestPath(project_id=project_id),
            body=_models.CreateSessionSummariesRequestBody(session_ids=session_ids, focus_area=focus_area)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_session_summaries: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/session_summaries/create_session_summaries", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/session_summaries/create_session_summaries"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_session_summaries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_session_summaries", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_session_summaries",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_summaries
@mcp.tool()
async def generate_individual_session_summaries(
    project_id: str = Field(..., description="The unique identifier of the project containing the sessions. Retrieve this ID from the /api/projects/ endpoint if needed."),
    session_ids: list[str] = Field(..., description="Array of session IDs to summarize. Accepts between 1 and 300 session IDs per request. Order is preserved in processing.", min_length=1, max_length=300),
    focus_area: str | None = Field(None, description="Optional thematic focus for the summarization analysis. When provided, summaries will emphasize this area. Limited to 500 characters.", max_length=500),
) -> dict[str, Any]:
    """Generate AI-powered summaries for each session individually without grouping them together. Useful for analyzing session-specific insights with optional focus areas."""

    # Construct request model with validation
    try:
        _request = _models.CreateSessionSummariesIndividuallyRequest(
            path=_models.CreateSessionSummariesIndividuallyRequestPath(project_id=project_id),
            body=_models.CreateSessionSummariesIndividuallyRequestBody(session_ids=session_ids, focus_area=focus_area)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_individual_session_summaries: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/session_summaries/create_session_summaries_individually", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/session_summaries/create_session_summaries_individually"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_individual_session_summaries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_individual_session_summaries", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_individual_session_summaries",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def list_user_interviews(
    project_id: str = Field(..., description="The unique identifier of the project containing the user interviews. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of interview records to return in a single response page. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based starting position for results, used in conjunction with limit to paginate through the interview list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of user interviews for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsListRequest(
            path=_models.UserInterviewsListRequestPath(project_id=project_id),
            query=_models.UserInterviewsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_user_interviews: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_user_interviews")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_user_interviews", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_user_interviews",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def create_user_interview(
    project_id: str = Field(..., description="The unique identifier of the project where the interview will be created. Retrieve this ID by calling the /api/projects/ endpoint."),
    audio: str = Field(..., description="Required URI pointing to the audio file recording of the interview. Must be a valid URI format."),
    interviewee_emails: list[str] | None = Field(None, description="Optional list of email addresses for the interviewees participating in this interview. Provide as an array of valid email strings."),
    summary: str | None = Field(None, description="Optional text summary or notes describing the interview content, purpose, or key takeaways."),
) -> dict[str, Any]:
    """Create a new user interview record within a project environment. Requires an audio file URI and optionally accepts interviewee email addresses and a summary description."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsCreateRequest(
            path=_models.UserInterviewsCreateRequestPath(project_id=project_id),
            body=_models.UserInterviewsCreateRequestBody(interviewee_emails=interviewee_emails, summary=summary, audio=audio)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_user_interview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_user_interview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_user_interview", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_user_interview",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def get_user_interview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the user interview to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the user interview. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific user interview by its unique identifier within a project. Returns the full details of the requested user interview."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsRetrieveRequest(
            path=_models.UserInterviewsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_user_interview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_user_interview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_user_interview", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_user_interview",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def update_user_interview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the user interview to update."),
    project_id: str = Field(..., description="The project ID that contains this user interview. Find your project ID by calling the /api/projects/ endpoint."),
    audio: str = Field(..., description="A URI pointing to the audio file or recording of the user interview."),
    interviewee_emails: list[str] | None = Field(None, description="Email addresses of the interviewees involved in this interview. Provide as an array of email strings."),
    summary: str | None = Field(None, description="A text summary or notes about the user interview content and key findings."),
) -> dict[str, Any]:
    """Update an existing user interview record with new metadata, interviewee information, or audio content. Requires a valid interview ID and project context."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsUpdateRequest(
            path=_models.UserInterviewsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.UserInterviewsUpdateRequestBody(interviewee_emails=interviewee_emails, summary=summary, audio=audio)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_user_interview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_user_interview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_user_interview", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_user_interview",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def update_user_interview_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the user interview to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the user interview. Retrieve available project IDs from the /api/projects/ endpoint."),
    interviewee_emails: list[str] | None = Field(None, description="A list of email addresses for the interviewees associated with this interview. Order and format should match standard email address conventions."),
    summary: str | None = Field(None, description="A text summary or notes describing the key points, findings, or outcomes from the user interview."),
) -> dict[str, Any]:
    """Partially update a user interview record, allowing you to modify interviewee emails and summary information for a specific interview within a project."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsPartialUpdateRequest(
            path=_models.UserInterviewsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.UserInterviewsPartialUpdateRequestBody(interviewee_emails=interviewee_emails, summary=summary)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_user_interview_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_user_interview_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_user_interview_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_user_interview_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: user_interviews, user_interviews
@mcp.tool()
async def delete_user_interview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the user interview to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the user interview. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a user interview from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.UserInterviewsDestroyRequest(
            path=_models.UserInterviewsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_user_interview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/user_interviews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/user_interviews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_user_interview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_user_interview", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_user_interview",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_vitals
@mcp.tool()
async def get_web_vitals_for_pathname(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    pathname: str = Field(..., description="The URL pathname to filter web vitals data by (e.g., '/home', '/products/details'). Only metrics for this specific pathname will be returned."),
) -> dict[str, Any]:
    """Retrieve Core Web Vitals metrics for a specific pathname within a project. Use this to analyze performance data for individual pages or routes."""

    # Construct request model with validation
    try:
        _request = _models.WebVitalsRetrieveRequest(
            path=_models.WebVitalsRetrieveRequestPath(project_id=project_id),
            query=_models.WebVitalsRetrieveRequestQuery(pathname=pathname)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_web_vitals_for_pathname: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/environments/{project_id}/web_vitals", _request.path.model_dump(by_alias=True)) if _request.path else "/api/environments/{project_id}/web_vitals"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_web_vitals_for_pathname")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_web_vitals_for_pathname", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_web_vitals_for_pathname",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def list_organizations(
    limit: int | None = Field(None, description="Maximum number of organizations to return in a single response page. Controls the size of each paginated result set."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to navigate through paginated results."),
) -> dict[str, Any]:
    """Retrieve a paginated list of organizations. Use limit and offset parameters to control pagination and navigate through results."""

    # Construct request model with validation
    try:
        _request = _models.ListRequest(
            query=_models.ListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_organizations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/organizations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_organizations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_organizations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_organizations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def create_organization(
    name: str = Field(..., description="The name of the organization. Must be 64 characters or fewer.", max_length=64),
    logo_media_id: str | None = Field(None, description="UUID of a media asset to use as the organization's logo."),
    is_member_join_email_enabled: bool | None = Field(None, description="Whether to send email notifications when new members join the organization."),
    enforce_2fa: bool | None = Field(None, description="Whether to require two-factor authentication for all organization members."),
    members_can_invite: bool | None = Field(None, description="Whether organization members are permitted to invite other users to join."),
    members_can_use_personal_api_keys: bool | None = Field(None, description="Whether organization members can create and use personal API keys for authentication."),
    allow_publicly_shared_resources: bool | None = Field(None, description="Whether resources within this organization can be shared publicly outside the organization."),
    is_ai_data_processing_approved: bool | None = Field(None, description="Whether the organization has approved AI-based data processing for its projects and experiments."),
    default_experiment_stats_method: Literal["bayesian", "frequentist"] | Literal[""] | None = Field(None, description="The default statistical method for analyzing experiment results in new projects. Choose between Bayesian or Frequentist approaches."),
    default_anonymize_ips: bool | None = Field(None, description="Whether to automatically discard client IP address data by default for new projects created in this organization."),
    default_role_id: str | None = Field(None, description="The role ID to automatically assign to new members when they join the organization, establishing their default permissions."),
) -> dict[str, Any]:
    """Create a new organization with configurable security, member management, and experiment analysis settings. The organization name is required and will serve as the primary identifier."""

    # Construct request model with validation
    try:
        _request = _models.CreateRequest(
            body=_models.CreateRequestBody(name=name, logo_media_id=logo_media_id, is_member_join_email_enabled=is_member_join_email_enabled, enforce_2fa=enforce_2fa, members_can_invite=members_can_invite, members_can_use_personal_api_keys=members_can_use_personal_api_keys, allow_publicly_shared_resources=allow_publicly_shared_resources, is_ai_data_processing_approved=is_ai_data_processing_approved, default_experiment_stats_method=default_experiment_stats_method, default_anonymize_ips=default_anonymize_ips, default_role_id=default_role_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/organizations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_organization", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_organization",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def get_organization(id_: str = Field(..., alias="id", description="The unique identifier of the organization, provided as a UUID string.")) -> dict[str, Any]:
    """Retrieve detailed information about a specific organization by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.RetrieveRequest(
            path=_models.RetrieveRequestPath(id_=id_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_organization", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_organization",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def update_organization(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the organization to update."),
    name: str = Field(..., description="The organization's display name. Must not exceed 64 characters.", max_length=64),
    logo_media_id: str | None = Field(None, description="UUID identifier of the media asset to use as the organization's logo."),
    is_member_join_email_enabled: bool | None = Field(None, description="Whether to send email notifications when new members join the organization."),
    enforce_2fa: bool | None = Field(None, description="Whether to require two-factor authentication for all organization members."),
    members_can_invite: bool | None = Field(None, description="Whether organization members can invite other users to join."),
    members_can_use_personal_api_keys: bool | None = Field(None, description="Whether organization members can create and use personal API keys."),
    allow_publicly_shared_resources: bool | None = Field(None, description="Whether resources can be shared publicly outside the organization."),
    is_ai_data_processing_approved: bool | None = Field(None, description="Whether the organization has approved AI data processing for analytics and insights."),
    default_experiment_stats_method: Literal["bayesian", "frequentist"] | Literal[""] | None = Field(None, description="The default statistical method (Bayesian or Frequentist) to use for analyzing new experiments created in this organization."),
    default_anonymize_ips: bool | None = Field(None, description="Whether to discard client IP addresses by default for new projects created in this organization."),
    default_role_id: str | None = Field(None, description="UUID of the role to automatically assign to new members when they join the organization."),
) -> dict[str, Any]:
    """Update an organization's settings, including branding, security policies, member permissions, and default configurations for experiments and projects."""

    # Construct request model with validation
    try:
        _request = _models.UpdateRequest(
            path=_models.UpdateRequestPath(id_=id_),
            body=_models.UpdateRequestBody(name=name, logo_media_id=logo_media_id, is_member_join_email_enabled=is_member_join_email_enabled, enforce_2fa=enforce_2fa, members_can_invite=members_can_invite, members_can_use_personal_api_keys=members_can_use_personal_api_keys, allow_publicly_shared_resources=allow_publicly_shared_resources, is_ai_data_processing_approved=is_ai_data_processing_approved, default_experiment_stats_method=default_experiment_stats_method, default_anonymize_ips=default_anonymize_ips, default_role_id=default_role_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_organization", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_organization",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def update_organization_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the organization to update."),
    logo_media_id: str | None = Field(None, description="UUID of the media asset to use as the organization's logo."),
    is_member_join_email_enabled: bool | None = Field(None, description="Enable or disable email notifications when new members join the organization."),
    enforce_2fa: bool | None = Field(None, description="Require all organization members to use two-factor authentication for account access."),
    members_can_invite: bool | None = Field(None, description="Allow organization members to invite other users to join."),
    members_can_use_personal_api_keys: bool | None = Field(None, description="Allow organization members to create and use personal API keys for authentication."),
    allow_publicly_shared_resources: bool | None = Field(None, description="Allow resources within the organization to be shared publicly outside the organization."),
    is_ai_data_processing_approved: bool | None = Field(None, description="Confirm that the organization approves AI-based data processing for analytics and insights."),
    default_experiment_stats_method: Literal["bayesian", "frequentist"] | Literal[""] | None = Field(None, description="Default statistical method for analyzing experiment results in new experiments. Choose between Bayesian or Frequentist approaches."),
    default_anonymize_ips: bool | None = Field(None, description="Default setting for whether to discard client IP addresses in data collection for new projects created in this organization."),
    default_role_id: str | None = Field(None, description="ID of the default role to automatically assign to new members when they join the organization."),
) -> dict[str, Any]:
    """Partially update organization settings including security policies, member permissions, experiment defaults, and role assignments."""

    # Construct request model with validation
    try:
        _request = _models.PartialUpdateRequest(
            path=_models.PartialUpdateRequestPath(id_=id_),
            body=_models.PartialUpdateRequestBody(logo_media_id=logo_media_id, is_member_join_email_enabled=is_member_join_email_enabled, enforce_2fa=enforce_2fa, members_can_invite=members_can_invite, members_can_use_personal_api_keys=members_can_use_personal_api_keys, allow_publicly_shared_resources=allow_publicly_shared_resources, is_ai_data_processing_approved=is_ai_data_processing_approved, default_experiment_stats_method=default_experiment_stats_method, default_anonymize_ips=default_anonymize_ips, default_role_id=default_role_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_organization_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_organization_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_organization_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_organization_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, organizations
@mcp.tool()
async def delete_organization(id_: str = Field(..., alias="id", description="The unique identifier of the organization to delete, provided as a UUID string.")) -> dict[str, Any]:
    """Permanently delete an organization and all associated data. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DestroyRequest(
            path=_models.DestroyRequestPath(id_=id_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_organization", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_organization",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def list_batch_exports(
    organization_id: str = Field(..., description="The unique identifier of the organization whose batch exports should be retrieved."),
    limit: int | None = Field(None, description="Maximum number of batch export records to return in a single page of results. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of batch export jobs for the specified organization. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsListRequest(
            path=_models.BatchExportsListRequestPath(organization_id=organization_id),
            query=_models.BatchExportsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_batch_exports: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_batch_exports")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_batch_exports", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_batch_exports",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def create_batch_export(
    organization_id: str = Field(..., description="The unique identifier of the organization that will own this batch export."),
    name: str = Field(..., description="A human-readable name to identify this batch export job."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, BigQuery, Redshift, Databricks), databases (Postgres), HTTP endpoints, or internal workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="How frequently the export should run: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data to export: events, persons, or sessions. If not specified, defaults to events."),
    paused: bool | None = Field(None, description="Whether this batch export is currently paused. When paused, no new export runs will be triggered."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="An optional set of filters to narrow down which data is included in the export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling export runs and interpreting start/end times."),
    offset_day: int | None = Field(None, description="The number of days (0-6) to offset the export schedule. Useful for staggering exports across a week.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour of the day (0-23) to offset the export schedule. Useful for running exports at specific times.", ge=0, le=23),
) -> dict[str, Any]:
    """Create a new batch export job to periodically export data (events, persons, or sessions) to a supported destination. The export will run on a specified interval until paused or the end date is reached."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsCreateRequest(
            path=_models.BatchExportsCreateRequestPath(organization_id=organization_id),
            body=_models.BatchExportsCreateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsCreateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_batch_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_batch_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to retrieve."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the batch export."),
) -> dict[str, Any]:
    """Retrieve a specific batch export by its unique identifier within an organization. Use this to check the status, details, and results of a previously created batch export job."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRetrieveRequest(
            path=_models.BatchExportsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def update_batch_export(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to update."),
    organization_id: str = Field(..., description="The organization ID that owns this batch export."),
    name: str = Field(..., description="A human-readable name for this batch export, used for identification and display purposes."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported: S3, Snowflake, Postgres, Redshift, BigQuery, Databricks, Azure Blob, Workflows, HTTP, or NoOp (for testing)."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which this batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The data model to export: events (raw event data), persons (user/person records), or sessions (aggregated session data)."),
    paused: bool | None = Field(None, description="Set to true to pause this batch export, preventing any scheduled runs until resumed."),
    start_at: str | None = Field(None, description="ISO 8601 formatted datetime. Batch export runs will not be triggered before this time."),
    end_at: str | None = Field(None, description="ISO 8601 formatted datetime. Batch export runs will not be triggered after this time."),
    hogql_query: str | None = Field(None, description="Optional HogQL query to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to apply to the exported data, further refining what records are included."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs, affecting when intervals are calculated."),
    offset_day: int | None = Field(None, description="Day offset (0-6) for scheduling: 0 = Sunday through 6 = Saturday. Used to align weekly exports to a specific day.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Hour offset (0-23) for scheduling. Used to align exports to a specific hour of the day.", ge=0, le=23),
) -> dict[str, Any]:
    """Update an existing batch export configuration, including its schedule, destination type, data model, and filtering criteria. Changes take effect on the next scheduled run."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsUpdateRequest(
            path=_models.BatchExportsUpdateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.BatchExportsUpdateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsUpdateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_batch_export", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_batch_export",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def update_batch_export_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to update."),
    organization_id: str = Field(..., description="The organization ID that owns this batch export."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination type for the batch export. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, Postgres, Redshift, BigQuery, Databricks), HTTP endpoints, and internal Workflows."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The data model being exported: Events, Persons, or Sessions."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] | None = Field(None, description="The export frequency: hourly, daily, weekly, or every 5/15 minutes."),
    paused: bool | None = Field(None, description="Whether the batch export is currently paused. Set to true to suspend scheduled runs without deleting the configuration."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="A HogQL query string to filter the data being exported."),
    filters: Any | None = Field(None, description="Filter criteria to narrow the scope of exported data."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for scheduling batch export runs."),
    offset_day: int | None = Field(None, description="Day offset for scheduling (0-6), where 0 represents the current day. Used to adjust when exports are triggered relative to the interval.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Hour offset for scheduling (0-23). Specifies the hour of the day when the export should run.", ge=0, le=23),
) -> dict[str, Any]:
    """Partially update a batch export configuration for an organization. Modify export settings such as destination type, schedule, filters, and timing constraints."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsPartialUpdateRequest(
            path=_models.BatchExportsPartialUpdateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.BatchExportsPartialUpdateRequestBody(model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsPartialUpdateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_batch_export_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_batch_export_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_batch_export_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_batch_export_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def delete_batch_export(
    id_: str = Field(..., alias="id", description="The unique identifier of the batch export to delete, provided as a UUID string."),
    organization_id: str = Field(..., description="The organization ID that owns the batch export being deleted."),
) -> dict[str, Any]:
    """Permanently delete a batch export job from an organization. This operation removes the batch export record and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsDestroyRequest(
            path=_models.BatchExportsDestroyRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_batch_export", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_batch_export",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export_logs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export job whose logs you want to retrieve."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the batch export job."),
) -> dict[str, Any]:
    """Retrieve the logs for a specific batch export job, including processing details and any errors or warnings that occurred during execution."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsLogsRetrieveRequest(
            path=_models.BatchExportsLogsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def pause_batch_export(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to pause."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns this batch export."),
    name: str = Field(..., description="A human-readable name for the batch export, used for identification and display purposes."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported: S3, Snowflake, Postgres, Redshift, BigQuery, Databricks, Azure Blob Storage, Workflows, HTTP, or NoOp (no-operation/test mode)."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data being exported: Events (raw event data), Persons (user/person profiles), or Sessions (aggregated session data)."),
    paused: bool | None = Field(None, description="Whether the batch export is currently paused (true) or active (false). Set to true to pause the export."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) at which batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) after which batch export runs will not be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="An optional HogQL query string to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to apply to the exported data, further refining which records are included in the batch export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs, affecting when interval-based exports are triggered."),
    offset_day: int | None = Field(None, description="The day offset (0-6) for scheduling, where 0 is the current day. Used to adjust the start day of weekly or daily exports.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour offset (0-23) for scheduling, specifying which hour of the day the batch export should run.", ge=0, le=23),
) -> dict[str, Any]:
    """Pause an active batch export job, temporarily halting scheduled data exports without deleting the configuration. The batch export can be resumed later."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsPauseCreateRequest(
            path=_models.BatchExportsPauseCreateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.BatchExportsPauseCreateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsPauseCreateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for pause_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}/pause", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}/pause"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("pause_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("pause_batch_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="pause_batch_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def create_batch_export_test_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to test."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns this batch export."),
    name: str = Field(..., description="A human-readable name for this batch export configuration."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination type for the batch export. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, Postgres, Redshift, BigQuery, Databricks), HTTP endpoints, and internal Workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which this batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data model to export: Events, Persons, or Sessions."),
    paused: bool | None = Field(None, description="Whether this batch export is currently paused and should not trigger scheduled runs."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) at which batch export runs can be triggered. Runs will not occur before this time."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) at which batch export runs can be triggered. Runs will not occur after this time."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to apply to the exported data, further refining what records are included in the batch export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs and interpreting time-based intervals."),
    offset_day: int | None = Field(None, description="The day offset (0-6) to apply when scheduling runs, where 0 represents the current day and higher values represent future days.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour offset (0-23) to apply when scheduling runs, determining the hour of the day when exports are triggered.", ge=0, le=23),
) -> dict[str, Any]:
    """Execute a test run for a batch export configuration to validate the export settings and destination connectivity before scheduling production runs."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunTestStepCreateRequest(
            path=_models.BatchExportsRunTestStepCreateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.BatchExportsRunTestStepCreateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsRunTestStepCreateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_batch_export_test_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}/run_test_step", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}/run_test_step"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_batch_export_test_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_batch_export_test_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_batch_export_test_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def unpause_batch_export(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to unpause."),
    organization_id: str = Field(..., description="The organization ID that owns this batch export."),
    name: str = Field(..., description="A human-readable name for this batch export."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported: S3, Snowflake, Postgres, Redshift, BigQuery, Databricks, Azure Blob, Workflows, HTTP, or NoOp."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data being exported: Events, Persons, or Sessions."),
    paused: bool | None = Field(None, description="Whether this batch export is paused. Set to false to unpause."),
    start_at: str | None = Field(None, description="The earliest time (ISO 8601 format) at which batch export runs can be triggered. Runs will not start before this time."),
    end_at: str | None = Field(None, description="The latest time (ISO 8601 format) after which batch export runs will not be triggered. Runs will not start after this time."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to apply to the exported data."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone in which the batch export schedule operates."),
    offset_day: int | None = Field(None, description="The day offset (0-6) for scheduling, where 0 is the current day and 6 is six days in the future.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour offset (0-23) for scheduling, representing the hour of the day in 24-hour format.", ge=0, le=23),
) -> dict[str, Any]:
    """Resume a paused batch export job, allowing it to continue running on its configured schedule. The batch export will resume exporting data according to its interval and timezone settings."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsUnpauseCreateRequest(
            path=_models.BatchExportsUnpauseCreateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.BatchExportsUnpauseCreateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsUnpauseCreateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for unpause_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/{id}/unpause", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/{id}/unpause"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("unpause_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("unpause_batch_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="unpause_batch_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def create_batch_export_test_run_new(
    organization_id: str = Field(..., description="The unique identifier of the organization that will own this batch export."),
    name: str = Field(..., description="A human-readable name to identify this batch export job."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, BigQuery, Redshift, Databricks, Postgres), HTTP endpoints, or internal Workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which batch exports will run: hourly, daily, weekly, or at 5 or 15-minute intervals."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data to export: Events (event records), Persons (user profiles), or Sessions (user sessions)."),
    paused: bool | None = Field(None, description="Set to true to pause this batch export, preventing any scheduled runs from executing."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to refine which records are included in the batch export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs and interpreting time-based parameters."),
    offset_day: int | None = Field(None, description="Number of days (0-6) to offset the batch export schedule, useful for staggering exports across a week.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Number of hours (0-23) to offset the batch export schedule within a day.", ge=0, le=23),
) -> dict[str, Any]:
    """Create and test a new batch export configuration for a specified organization. This operation validates the export settings by running a test step before full deployment."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunTestStepNewCreateRequest(
            path=_models.BatchExportsRunTestStepNewCreateRequestPath(organization_id=organization_id),
            body=_models.BatchExportsRunTestStepNewCreateRequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsRunTestStepNewCreateRequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_batch_export_test_run_new: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/run_test_step_new", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/run_test_step_new"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_batch_export_test_run_new")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_batch_export_test_run_new", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_batch_export_test_run_new",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_exports_test(organization_id: str = Field(..., description="The unique identifier of the organization for which to retrieve batch export test information.")) -> dict[str, Any]:
    """Retrieve test batch export configuration or status for an organization. This endpoint allows you to verify batch export settings and test connectivity before running production exports."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsTestRetrieveRequest(
            path=_models.BatchExportsTestRetrieveRequestPath(organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_exports_test: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/batch_exports/test", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/batch_exports/test"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_exports_test")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_exports_test", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_exports_test",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def list_domains(
    organization_id: str = Field(..., description="The unique identifier of the organization whose domains should be retrieved."),
    limit: int | None = Field(None, description="Maximum number of domain records to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through the domain list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of all domains associated with a specific organization."""

    # Construct request model with validation
    try:
        _request = _models.DomainsListRequest(
            path=_models.DomainsListRequestPath(organization_id=organization_id),
            query=_models.DomainsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_domains: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_domains")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_domains", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_domains",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def create_domain(
    organization_id: str = Field(..., description="The unique identifier of the organization that will own this domain."),
    domain: str = Field(..., description="The domain name to register (e.g., example.com). Maximum 128 characters.", max_length=128),
    jit_provisioning_enabled: bool | None = Field(None, description="Enable just-in-time user provisioning, allowing users to be automatically created on first SSO login."),
    sso_enforcement: str | None = Field(None, description="SSO enforcement policy for this domain. Maximum 28 characters. Controls whether SSO is optional or required for users in this domain.", max_length=28),
    saml_entity_id: str | None = Field(None, description="SAML identity provider entity ID for this domain. Maximum 512 characters. Used to identify the IdP in SAML authentication flows.", max_length=512),
    saml_acs_url: str | None = Field(None, description="SAML Assertion Consumer Service (ACS) URL where the IdP will send authentication responses. Maximum 512 characters.", max_length=512),
    saml_x509_cert: str | None = Field(None, description="SAML X.509 certificate in PEM format from your identity provider. Used to validate signed SAML assertions."),
    scim_enabled: bool | None = Field(None, description="Enable SCIM 2.0 provisioning for automated user and group management from your identity provider."),
) -> dict[str, Any]:
    """Create a new domain for an organization to enable SSO, SCIM provisioning, and identity management. This configures SAML authentication settings and user provisioning policies for the domain."""

    # Construct request model with validation
    try:
        _request = _models.DomainsCreateRequest(
            path=_models.DomainsCreateRequestPath(organization_id=organization_id),
            body=_models.DomainsCreateRequestBody(domain=domain, jit_provisioning_enabled=jit_provisioning_enabled, sso_enforcement=sso_enforcement, saml_entity_id=saml_entity_id, saml_acs_url=saml_acs_url, saml_x509_cert=saml_x509_cert, scim_enabled=scim_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_domain", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_domain",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def get_domain(
    id_: str = Field(..., alias="id", description="The unique identifier of the domain to retrieve, formatted as a UUID."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the domain, formatted as a UUID."),
) -> dict[str, Any]:
    """Retrieve a specific domain by its ID within an organization. Returns detailed information about the domain configuration and settings."""

    # Construct request model with validation
    try:
        _request = _models.DomainsRetrieveRequest(
            path=_models.DomainsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_domain", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_domain",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def update_domain(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the domain to update."),
    organization_id: str = Field(..., description="The organization that owns this domain."),
    domain: str = Field(..., description="The domain name, up to 128 characters.", max_length=128),
    jit_provisioning_enabled: bool | None = Field(None, description="Enable or disable just-in-time user provisioning for this domain."),
    sso_enforcement: str | None = Field(None, description="SSO enforcement policy for the domain, up to 28 characters (e.g., 'required', 'optional').", max_length=28),
    saml_entity_id: str | None = Field(None, description="SAML entity identifier URL, up to 512 characters. Used to identify the service provider in SAML authentication flows.", max_length=512),
    saml_acs_url: str | None = Field(None, description="SAML Assertion Consumer Service URL, up to 512 characters. The endpoint where the identity provider sends SAML responses.", max_length=512),
    saml_x509_cert: str | None = Field(None, description="SAML X.509 certificate in PEM format for validating SAML assertions from the identity provider."),
    scim_enabled: bool | None = Field(None, description="Enable or disable SCIM (System for Cross-domain Identity Management) for automated user provisioning and deprovisioning."),
) -> dict[str, Any]:
    """Update domain configuration settings including SSO enforcement, SAML authentication, and SCIM provisioning for an organization."""

    # Construct request model with validation
    try:
        _request = _models.DomainsUpdateRequest(
            path=_models.DomainsUpdateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.DomainsUpdateRequestBody(domain=domain, jit_provisioning_enabled=jit_provisioning_enabled, sso_enforcement=sso_enforcement, saml_entity_id=saml_entity_id, saml_acs_url=saml_acs_url, saml_x509_cert=saml_x509_cert, scim_enabled=scim_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_domain", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_domain",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def update_domain_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the domain to update."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns this domain."),
    domain: str | None = Field(None, description="The domain name. Must not exceed 128 characters.", max_length=128),
    jit_provisioning_enabled: bool | None = Field(None, description="Enable or disable just-in-time user provisioning for this domain."),
    sso_enforcement: str | None = Field(None, description="SSO enforcement policy for the domain. Must not exceed 28 characters.", max_length=28),
    saml_entity_id: str | None = Field(None, description="SAML entity identifier for this domain. Must not exceed 512 characters.", max_length=512),
    saml_acs_url: str | None = Field(None, description="SAML Assertion Consumer Service (ACS) URL endpoint. Must not exceed 512 characters.", max_length=512),
    saml_x509_cert: str | None = Field(None, description="SAML X.509 certificate for validating SAML assertions from the identity provider."),
    scim_enabled: bool | None = Field(None, description="Enable or disable SCIM (System for Cross-domain Identity Management) provisioning for this domain."),
) -> dict[str, Any]:
    """Partially update domain configuration settings for an organization, including SAML authentication, SSO enforcement, and SCIM provisioning options."""

    # Construct request model with validation
    try:
        _request = _models.DomainsPartialUpdateRequest(
            path=_models.DomainsPartialUpdateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.DomainsPartialUpdateRequestBody(domain=domain, jit_provisioning_enabled=jit_provisioning_enabled, sso_enforcement=sso_enforcement, saml_entity_id=saml_entity_id, saml_acs_url=saml_acs_url, saml_x509_cert=saml_x509_cert, scim_enabled=scim_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_domain_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_domain_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_domain_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_domain_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def delete_domain(
    id_: str = Field(..., alias="id", description="The unique identifier of the domain to delete, provided as a UUID string."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the domain, provided as a UUID string."),
) -> dict[str, Any]:
    """Permanently delete a domain from an organization. This operation removes the domain and all associated configuration."""

    # Construct request model with validation
    try:
        _request = _models.DomainsDestroyRequest(
            path=_models.DomainsDestroyRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_domain", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_domain",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def list_scim_logs_for_domain(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the domain for which to retrieve SCIM logs."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the domain."),
) -> dict[str, Any]:
    """Retrieve SCIM (System for Cross-domain Identity Management) operation logs for a specific domain within an organization. This endpoint provides audit trail information for identity synchronization activities."""

    # Construct request model with validation
    try:
        _request = _models.DomainsScimLogsRetrieveRequest(
            path=_models.DomainsScimLogsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_scim_logs_for_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}/scim/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}/scim/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_scim_logs_for_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_scim_logs_for_domain", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_scim_logs_for_domain",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, domains
@mcp.tool()
async def verify_domain(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the domain to verify."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the domain."),
    domain: str = Field(..., description="The domain name to verify, up to 128 characters.", max_length=128),
    jit_provisioning_enabled: bool | None = Field(None, description="Enable just-in-time user provisioning for this domain, allowing automatic user creation on first login."),
    sso_enforcement: str | None = Field(None, description="SSO enforcement policy for the domain, up to 28 characters (e.g., 'optional', 'required').", max_length=28),
    saml_entity_id: str | None = Field(None, description="SAML entity identifier for the identity provider, up to 512 characters.", max_length=512),
    saml_acs_url: str | None = Field(None, description="SAML Assertion Consumer Service URL where the identity provider sends authentication responses, up to 512 characters.", max_length=512),
    saml_x509_cert: str | None = Field(None, description="SAML X.509 certificate in PEM format for validating identity provider signatures."),
    scim_enabled: bool | None = Field(None, description="Enable SCIM (System for Cross-domain Identity Management) for automated user and group provisioning."),
) -> dict[str, Any]:
    """Verify and configure a domain for an organization, enabling SSO and SCIM provisioning settings. This operation validates the domain and applies identity provider configuration."""

    # Construct request model with validation
    try:
        _request = _models.DomainsVerifyCreateRequest(
            path=_models.DomainsVerifyCreateRequestPath(id_=id_, organization_id=organization_id),
            body=_models.DomainsVerifyCreateRequestBody(domain=domain, jit_provisioning_enabled=jit_provisioning_enabled, sso_enforcement=sso_enforcement, saml_entity_id=saml_entity_id, saml_acs_url=saml_acs_url, saml_x509_cert=saml_x509_cert, scim_enabled=scim_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for verify_domain: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/domains/{id}/verify", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/domains/{id}/verify"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("verify_domain")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("verify_domain", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="verify_domain",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, integrations
@mcp.tool()
async def list_organization_integrations(
    organization_id: str = Field(..., description="The unique identifier of the organization whose integrations you want to retrieve."),
    limit: int | None = Field(None, description="Maximum number of integration results to return in a single page of results."),
    offset: int | None = Field(None, description="The zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of organization-level integrations (such as Vercel, AWS Marketplace) that are scoped to the entire organization. This is read-only access; creation occurs through integration installation flows and deletion requires support contact due to billing implications."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsListRequest(
            path=_models.IntegrationsListRequestPath(organization_id=organization_id),
            query=_models.IntegrationsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_organization_integrations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/integrations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/integrations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_organization_integrations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_organization_integrations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_organization_integrations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, integrations
@mcp.tool()
async def get_organization_integration(
    id_: str = Field(..., alias="id", description="The unique identifier of the organization integration to retrieve, formatted as a UUID."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the integration, formatted as a UUID."),
) -> dict[str, Any]:
    """Retrieve a specific organization-level integration by ID. Returns read-only details about integrations scoped to the entire organization, such as Vercel or AWS Marketplace integrations."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsRetrieveRequest(
            path=_models.IntegrationsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_organization_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/integrations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/integrations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_organization_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_organization_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_organization_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, integrations
@mcp.tool()
async def update_integration_environment_mapping(
    id_: str = Field(..., alias="id", description="The unique identifier of the organization integration to update, provided as a UUID string."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the integration, provided as a UUID string."),
) -> dict[str, Any]:
    """Partially update the environment mapping configuration for an organization-level integration. This allows you to modify how environments are mapped for integrations like Vercel or AWS Marketplace without replacing the entire configuration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsEnvironmentMappingPartialUpdateRequest(
            path=_models.IntegrationsEnvironmentMappingPartialUpdateRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_integration_environment_mapping: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/integrations/{id}/environment-mapping", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/integrations/{id}/environment-mapping"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_integration_environment_mapping")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_integration_environment_mapping", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_integration_environment_mapping",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, invites
@mcp.tool()
async def list_organization_invites(
    organization_id: str = Field(..., description="The unique identifier of the organization whose invitations you want to retrieve."),
    limit: int | None = Field(None, description="Maximum number of invitation records to return in a single response. Controls page size for pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of pending invitations for an organization. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.InvitesListRequest(
            path=_models.InvitesListRequestPath(organization_id=organization_id),
            query=_models.InvitesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_organization_invites: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/invites", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/invites"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_organization_invites")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_organization_invites", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_organization_invites",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, invites
@mcp.tool()
async def send_organization_invite(
    organization_id: str = Field(..., description="The unique identifier of the organization to which the user is being invited."),
    target_email: str = Field(..., description="The email address of the invitee. Must be a valid email format with a maximum length of 254 characters.", max_length=254),
    first_name: str | None = Field(None, description="The first name of the invitee. Maximum length is 30 characters.", max_length=30),
    level: Literal[1, 8, 15] | None = Field(None, description="The access level or role for the invitee within the organization. Must be a value between 0 and 32767.", ge=0, le=32767),
    message: str | None = Field(None, description="An optional custom message to include in the invitation sent to the recipient."),
    private_project_access: Any | None = Field(None, description="A mapping of team IDs to access levels that grants the invitee access to specific private projects within the organization."),
    combine_pending_invites: bool | None = Field(None, description="If true, combines this invitation with any existing pending invitations for the same email address. Defaults to false."),
) -> dict[str, Any]:
    """Send an invitation to join an organization to a specified email address, with optional role assignment and private project access configuration."""

    # Construct request model with validation
    try:
        _request = _models.InvitesCreateRequest(
            path=_models.InvitesCreateRequestPath(organization_id=organization_id),
            body=_models.InvitesCreateRequestBody(target_email=target_email, first_name=first_name, level=level, message=message, private_project_access=private_project_access, combine_pending_invites=combine_pending_invites)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_organization_invite: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/invites", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/invites"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_organization_invite")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_organization_invite", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_organization_invite",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, invites
@mcp.tool()
async def delete_organization_invite(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the organization invite to delete."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the invite."),
) -> dict[str, Any]:
    """Revoke or delete a pending organization invite by its UUID. This removes the invitation and prevents the recipient from joining the organization using this invite."""

    # Construct request model with validation
    try:
        _request = _models.InvitesDestroyRequest(
            path=_models.InvitesDestroyRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_organization_invite: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/invites/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/invites/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_organization_invite")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_organization_invite", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_organization_invite",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, invites
@mcp.tool()
async def send_bulk_invites(
    organization_id: str = Field(..., description="The unique identifier of the organization to which users will be invited."),
    target_email: str = Field(..., description="The email address of the user to invite. Must be a valid email address with a maximum length of 254 characters.", max_length=254),
    first_name: str | None = Field(None, description="The first name of the invited user. Maximum length is 30 characters.", max_length=30),
    level: Literal[1, 8, 15] | None = Field(None, description="The permission level to grant the invited user within the organization. Must be a non-negative integer between 0 and 32767.", ge=0, le=32767),
    message: str | None = Field(None, description="An optional custom message to include in the invitation sent to the user."),
    private_project_access: Any | None = Field(None, description="A mapping of team IDs to access levels that grants the invited user access to specified private projects. Structure defines which private projects the user can access and at what permission level."),
    combine_pending_invites: bool | None = Field(None, description="If true, consolidates any existing pending invitations for the target email address with this new invitation. Defaults to false."),
) -> dict[str, Any]:
    """Send bulk invitations to users to join an organization, with optional access control to private projects and the ability to consolidate pending invites."""

    # Construct request model with validation
    try:
        _request = _models.InvitesBulkCreateRequest(
            path=_models.InvitesBulkCreateRequestPath(organization_id=organization_id),
            body=_models.InvitesBulkCreateRequestBody(target_email=target_email, first_name=first_name, level=level, message=message, private_project_access=private_project_access, combine_pending_invites=combine_pending_invites)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_bulk_invites: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/invites/bulk", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/invites/bulk"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_bulk_invites")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_bulk_invites", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_bulk_invites",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, members
@mcp.tool()
async def list_organization_members(
    organization_id: str = Field(..., description="The unique identifier of the organization whose members you want to retrieve."),
    limit: int | None = Field(None, description="Maximum number of member records to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through the member list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of members belonging to a specific organization. Use limit and offset parameters to control pagination through the member roster."""

    # Construct request model with validation
    try:
        _request = _models.MembersListRequest(
            path=_models.MembersListRequestPath(organization_id=organization_id),
            query=_models.MembersListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_organization_members: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/members", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/members"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_organization_members")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_organization_members", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_organization_members",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, members
@mcp.tool()
async def update_organization_member(
    organization_id: str = Field(..., description="The unique identifier of the organization containing the member to update."),
    user__uuid: str = Field(..., description="The UUID of the user whose membership level should be updated."),
    level: Literal[1, 8, 15] | None = Field(None, description="The access level to assign to the member, ranging from 0 to 32767 where higher values indicate greater permissions.", ge=0, le=32767),
) -> dict[str, Any]:
    """Update a member's access level within an organization. Modifies the permission level for a specific user in the organization."""

    # Construct request model with validation
    try:
        _request = _models.MembersUpdateRequest(
            path=_models.MembersUpdateRequestPath(organization_id=organization_id, user__uuid=user__uuid),
            body=_models.MembersUpdateRequestBody(level=level)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_organization_member: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/members/{user__uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/members/{user__uuid}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_organization_member")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_organization_member", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_organization_member",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, members
@mcp.tool()
async def update_organization_member_level(
    organization_id: str = Field(..., description="The unique identifier of the organization containing the member to update."),
    user__uuid: str = Field(..., description="The UUID of the user whose membership details should be updated."),
    level: Literal[1, 8, 15] | None = Field(None, description="The access or permission level for the member, represented as a numeric value between 0 and 32767.", ge=0, le=32767),
) -> dict[str, Any]:
    """Partially update a member's information within an organization, such as their access level or role permissions."""

    # Construct request model with validation
    try:
        _request = _models.MembersPartialUpdateRequest(
            path=_models.MembersPartialUpdateRequestPath(organization_id=organization_id, user__uuid=user__uuid),
            body=_models.MembersPartialUpdateRequestBody(level=level)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_organization_member_level: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/members/{user__uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/members/{user__uuid}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_organization_member_level")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_organization_member_level", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_organization_member_level",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, members
@mcp.tool()
async def remove_member_from_organization(
    organization_id: str = Field(..., description="The unique identifier of the organization from which the member will be removed."),
    user__uuid: str = Field(..., description="The UUID of the user to be removed from the organization. Must be a valid UUID format."),
) -> dict[str, Any]:
    """Remove a member from an organization by deleting their user account association. This operation permanently revokes the user's access to the organization."""

    # Construct request model with validation
    try:
        _request = _models.MembersDestroyRequest(
            path=_models.MembersDestroyRequestPath(organization_id=organization_id, user__uuid=user__uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_member_from_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/members/{user__uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/members/{user__uuid}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_member_from_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_member_from_organization", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_member_from_organization",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, members
@mcp.tool()
async def list_scoped_api_keys_for_member(
    organization_id: str = Field(..., description="The unique identifier of the organization. This must be a valid UUID that identifies which organization's member to query."),
    user__uuid: str = Field(..., description="The unique identifier of the member user. This must be a valid UUID format that specifies which member's scoped API keys to retrieve."),
) -> dict[str, Any]:
    """Retrieve all scoped API keys associated with a specific member within an organization. Scoped API keys are restricted credentials that grant limited access to organization resources."""

    # Construct request model with validation
    try:
        _request = _models.MembersScopedApiKeysRetrieveRequest(
            path=_models.MembersScopedApiKeysRetrieveRequestPath(organization_id=organization_id, user__uuid=user__uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_scoped_api_keys_for_member: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/members/{user__uuid}/scoped_api_keys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/members/{user__uuid}/scoped_api_keys"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_scoped_api_keys_for_member")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_scoped_api_keys_for_member", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_scoped_api_keys_for_member",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, oauth_applications
@mcp.tool()
async def list_oauth_applications(
    organization_id: str = Field(..., description="The unique identifier of the organization whose OAuth applications you want to retrieve."),
    limit: int | None = Field(None, description="The maximum number of OAuth applications to return in a single page of results. Use this to control pagination size."),
    offset: int | None = Field(None, description="The zero-based index position from which to start returning results. Use this with the limit parameter to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of OAuth applications configured for the specified organization. This is a read-only operation that returns all OAuth applications associated with the organization."""

    # Construct request model with validation
    try:
        _request = _models.OauthApplicationsListRequest(
            path=_models.OauthApplicationsListRequestPath(organization_id=organization_id),
            query=_models.OauthApplicationsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_oauth_applications: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/oauth_applications", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/oauth_applications"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_oauth_applications")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_oauth_applications", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_oauth_applications",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def list_projects_for_organization(
    organization_id: str = Field(..., description="The unique identifier of the organization whose projects should be retrieved."),
    limit: int | None = Field(None, description="Maximum number of projects to return in a single response page. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of projects belonging to the specified organization."""

    # Construct request model with validation
    try:
        _request = _models.List2Request(
            path=_models.List2RequestPath(organization_id=organization_id),
            query=_models.List2RequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_projects_for_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_projects_for_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_projects_for_organization", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_projects_for_organization",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def create_project(
    organization_id: str = Field(..., description="The unique identifier of the organization where the project will be created."),
    product_description: str | None = Field(None, description="A detailed description of the product or service being tracked, up to 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of application URLs associated with this project for domain-based filtering and configuration."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending notifications and alerts, up to 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Enable IP address anonymization for privacy compliance in event tracking."),
    test_account_filters_default_checked: bool | None = Field(None, description="Pre-check the test account filters option by default in the project's analytics interface."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Set the project's primary timezone for date/time display and report generation. Supports IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of user property names to use for displaying person identities throughout the interface."),
    autocapture_opt_out: bool | None = Field(None, description="Disable automatic event capture for this project, requiring manual event instrumentation instead."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Enable automatic capture of JavaScript exceptions and error events."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Enable automatic capture of Core Web Vitals metrics (LCP, FID, CLS) for performance monitoring."),
    capture_console_log_opt_in: bool | None = Field(None, description="Enable automatic capture of browser console logs (errors, warnings, info) in session recordings."),
    capture_performance_opt_in: bool | None = Field(None, description="Enable automatic capture of performance metrics and timing data."),
    session_recording_opt_in: bool | None = Field(None, description="Enable session recording to capture user interactions and page behavior."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured, between 0 and 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When provided, this takes precedence over legacy trigger field settings."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Data retention period for session recordings: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="Enable role-based access control for managing team member permissions within the project."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Set the first day of the week for calendar and date range displays (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="Dashboard ID to set as the primary dashboard displayed when opening the project."),
    live_events_columns: list[str] | None = Field(None, description="Array of property names to display as columns in the live events feed for real-time event monitoring."),
    recording_domains: list[str | None] | None = Field(None, description="List of domain names where session recording is enabled. Restricts recording to specified domains only."),
    surveys_opt_in: bool | None = Field(None, description="Enable in-app surveys and feedback collection for this project."),
    heatmaps_opt_in: bool | None = Field(None, description="Enable heatmap generation to visualize user interaction patterns on pages."),
    flags_persistence_default: bool | None = Field(None, description="Enable persistent storage of feature flag states across sessions by default."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Specify the business model served by this project (B2B, B2C, or other) to optimize the UI layout and features."),
    conversations_enabled: bool | None = Field(None, description="Enable conversations feature for collecting and managing user feedback and support interactions."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for the conversations feature, including routing and notification preferences."),
    proactive_tasks_enabled: bool | None = Field(None, description="Enable proactive task management for automated user engagement and outreach workflows."),
) -> dict[str, Any]:
    """Create a new project within an organization. Configure analytics, session recording, surveys, and other project-level settings during creation."""

    # Construct request model with validation
    try:
        _request = _models.Create2Request(
            path=_models.Create2RequestPath(organization_id=organization_id),
            body=_models.Create2RequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def get_project(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to retrieve. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The organization ID that owns the project. Used to scope the project lookup to the correct organization context."),
) -> dict[str, Any]:
    """Retrieve a specific project by ID within the current organization. Returns detailed project information including metadata and configuration."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.Retrieve2Request(
            path=_models.Retrieve2RequestPath(id_=_id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to update. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The organization ID that owns the project. Required to scope the update to the correct organization."),
    product_description: str | None = Field(None, description="A brief description of the product or service. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of URLs where the application is deployed or hosted. Used for domain-specific configurations."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending notifications and alerts. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="When enabled, anonymizes user IP addresses in analytics and recordings for privacy compliance."),
    test_account_filters_default_checked: bool | None = Field(None, description="When enabled, test account filters are checked by default in analytics views."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for the project, used for scheduling and time-based reporting. Supports IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names throughout the application."),
    autocapture_opt_out: bool | None = Field(None, description="When enabled, disables automatic event capture on web pages."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="When enabled, captures JavaScript exceptions and errors automatically."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="When enabled, captures Core Web Vitals metrics (LCP, FID, CLS) for performance monitoring."),
    capture_console_log_opt_in: bool | None = Field(None, description="When enabled, captures browser console logs (log, warn, error) for debugging."),
    capture_performance_opt_in: bool | None = Field(None, description="When enabled, captures performance metrics and timing data from the browser."),
    session_recording_opt_in: bool | None = Field(None, description="When enabled, enables session recording functionality for user behavior analysis."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When provided, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Data retention period for session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="When enabled, activates role-based access control for the project."),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week that reports and analytics weeks start on (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to set as the primary/default dashboard for the project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed."),
    recording_domains: list[str | None] | None = Field(None, description="List of domains where session recording is enabled. Restricts recording to specified domains."),
    surveys_opt_in: bool | None = Field(None, description="When enabled, activates survey and feedback collection features."),
    heatmaps_opt_in: bool | None = Field(None, description="When enabled, activates heatmap and click tracking features."),
    flags_persistence_default: bool | None = Field(None, description="When enabled, feature flags are persisted in local storage by default."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Indicates whether the project serves B2B (business-to-business) or B2C (business-to-consumer) customers. Used to optimize UI layout and features."),
    conversations_enabled: bool | None = Field(None, description="When enabled, activates the conversations and messaging features for user communication."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for the conversations feature, including routing and notification preferences."),
    proactive_tasks_enabled: bool | None = Field(None, description="When enabled, activates proactive task management and automation features."),
) -> dict[str, Any]:
    """Update configuration settings for a project within an organization, including product details, tracking preferences, session recording, and feature flags."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.Update2Request(
            path=_models.Update2RequestPath(id_=_id_, organization_id=organization_id),
            body=_models.Update2RequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_settings(
    id_: str = Field(..., alias="id", description="The unique numeric identifier of the project to update. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The organization ID that owns the project. Used to scope the project within the correct organizational context."),
    product_description: str | None = Field(None, description="A brief description of the product or service. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of URLs where the application is deployed or accessible. Useful for domain-specific configurations and allowlisting."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending notifications and alerts. Maximum 500 characters. Must be a valid webhook endpoint.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="When enabled, anonymizes user IP addresses in analytics and session recordings for privacy compliance."),
    test_account_filters_default_checked: bool | None = Field(None, description="When enabled, test account filters are checked by default in analytics views and reports."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for the project, used for scheduling, reporting, and time-based analytics. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of user property names to use for displaying person identities throughout the application. Order determines display priority."),
    autocapture_opt_out: bool | None = Field(None, description="When enabled, disables automatic event capture on the client side. Useful for privacy-sensitive applications or when using manual event tracking."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="When enabled, captures JavaScript exceptions and errors automatically for debugging and error tracking."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="When enabled, captures Web Vitals metrics (Core Web Vitals) for performance monitoring."),
    capture_console_log_opt_in: bool | None = Field(None, description="When enabled, captures browser console logs (log, warn, error) for debugging purposes."),
    capture_performance_opt_in: bool | None = Field(None, description="When enabled, captures performance metrics and timing data from the browser's Performance API."),
    session_recording_opt_in: bool | None = Field(None, description="When enabled, activates session recording functionality to capture user interactions and page behavior."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Allows up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30,000 milliseconds (0 to 30 seconds).", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When present, takes precedence over legacy trigger field settings."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Data retention period for session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="When enabled, activates role-based access control (RBAC) for managing team member permissions and data access."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Configures which day of the week is displayed as the first day in calendar and date picker components (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to display as the primary/default dashboard when users access the project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed. Order determines column display sequence."),
    recording_domains: list[str | None] | None = Field(None, description="List of domain names where session recording is enabled. Restricts recording to specified domains for security and compliance."),
    surveys_opt_in: bool | None = Field(None, description="When enabled, activates the surveys feature for collecting user feedback and conducting in-app surveys."),
    heatmaps_opt_in: bool | None = Field(None, description="When enabled, activates the heatmaps feature for visualizing user interaction patterns on pages."),
    flags_persistence_default: bool | None = Field(None, description="When enabled, feature flags are persisted in local storage by default for offline availability."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Specifies the business model served by this project (B2B, B2C, or other) to optimize UI layout and feature recommendations."),
    conversations_enabled: bool | None = Field(None, description="When enabled, activates the conversations feature for managing user feedback and support interactions."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for the conversations feature, including routing rules and notification preferences."),
    proactive_tasks_enabled: bool | None = Field(None, description="When enabled, activates proactive task management features for automated user engagement and outreach."),
) -> dict[str, Any]:
    """Partially update project configuration settings for the specified organization. Allows modification of product metadata, tracking preferences, session recording, integrations, and UI customization options."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.PartialUpdate2Request(
            path=_models.PartialUpdate2RequestPath(id_=_id_, organization_id=organization_id),
            body=_models.PartialUpdate2RequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_settings: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_settings")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_settings", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_settings",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def delete_project(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to delete. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the project."),
) -> dict[str, Any]:
    """Permanently delete a project from the organization. This action cannot be undone and will remove all associated project data."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.Destroy2Request(
            path=_models.Destroy2RequestPath(id_=_id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_project", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_project",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def get_project_activity(
    id_: str = Field(..., alias="id", description="The unique identifier of the project. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the project."),
) -> dict[str, Any]:
    """Retrieve the activity log for a specific project within an organization. This provides a historical record of changes and events associated with the project."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.ActivityRetrieveRequest(
            path=_models.ActivityRetrieveRequestPath(id_=_id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_project_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_project_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_project_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_project_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_product_intent(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to update. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The organization ID that owns the project. Required to scope the operation to the correct organization."),
    product_description: str | None = Field(None, description="A brief description of the product. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="Array of application URLs associated with this project. Used for domain-specific tracking and configuration."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for notifications and integrations. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="When enabled, anonymizes user IP addresses in tracking data for privacy compliance."),
    test_account_filters_default_checked: bool | None = Field(None, description="When enabled, test account filters are checked by default in the UI."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for the project, used for time-based reporting and scheduling. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names in the UI."),
    autocapture_opt_out: bool | None = Field(None, description="When enabled, disables automatic event capture on the web."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="When enabled, automatically captures JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="When enabled, automatically captures Web Vitals metrics (LCP, FID, CLS)."),
    capture_console_log_opt_in: bool | None = Field(None, description="When enabled, captures console log output for debugging purposes."),
    capture_performance_opt_in: bool | None = Field(None, description="When enabled, captures performance metrics and timing data."),
    session_recording_opt_in: bool | None = Field(None, description="When enabled, enables session recording functionality for user behavior analysis."),
    session_recording_sample_rate: str | None = Field(None, description="The sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When present, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long session recordings are retained. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="When enabled, activates access control and permission management features."),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week that reports and analytics weeks start on (0-6, where 0 is Sunday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to set as the primary/default dashboard for this project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column configurations for the live events view."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domains where session recording is enabled."),
    surveys_opt_in: bool | None = Field(None, description="When enabled, activates the surveys feature for collecting user feedback."),
    heatmaps_opt_in: bool | None = Field(None, description="When enabled, activates the heatmaps feature for visualizing user interactions."),
    flags_persistence_default: bool | None = Field(None, description="When enabled, feature flags are persisted by default for users."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Indicates whether this project serves B2B (business-to-business) or B2C (business-to-consumer) customers. Used to optimize UI layout and features."),
    conversations_enabled: bool | None = Field(None, description="When enabled, activates the conversations feature for user communication and support."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for the conversations feature."),
    proactive_tasks_enabled: bool | None = Field(None, description="When enabled, activates proactive tasks and automated workflows."),
) -> dict[str, Any]:
    """Partially update product intent settings for a project within an organization. Allows configuration of product metadata, tracking preferences, session recording, and feature flags."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.AddProductIntentPartialUpdateRequest(
            path=_models.AddProductIntentPartialUpdateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.AddProductIntentPartialUpdateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_product_intent: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/add_product_intent", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/add_product_intent"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_product_intent")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_product_intent", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_product_intent",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_organization(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to update. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The ID of the organization to move the project to."),
    product_description: str | None = Field(None, description="A brief description of the product or service being tracked. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of application URLs where the tracking code is deployed. Order and format should match your deployment environment."),
    slack_incoming_webhook: str | None = Field(None, description="Webhook URL for Slack integration to receive notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="When enabled, anonymizes user IP addresses in tracked events for privacy compliance."),
    test_account_filters_default_checked: bool | None = Field(None, description="When enabled, test account filters are checked by default in the analytics interface."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for reporting and scheduling. Supports IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names in the interface. Order determines display priority."),
    autocapture_opt_out: bool | None = Field(None, description="When enabled, disables automatic event capture on web pages."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="When enabled, automatically captures JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="When enabled, automatically captures Web Vitals metrics (LCP, FID, CLS) for performance monitoring."),
    capture_console_log_opt_in: bool | None = Field(None, description="When enabled, automatically captures browser console logs (log, warn, error) for debugging."),
    capture_performance_opt_in: bool | None = Field(None, description="When enabled, automatically captures performance metrics and timing data."),
    session_recording_opt_in: bool | None = Field(None, description="When enabled, enables session recording functionality to capture user interactions."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When present, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long to retain session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="When enabled, enables role-based access control for team members and permissions management."),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week to use as the start of the week in reports and calendars (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to set as the primary/default dashboard for this project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed. Order determines column display sequence."),
    recording_domains: list[str | None] | None = Field(None, description="List of domain names where session recording is enabled. Supports wildcard patterns for subdomains."),
    surveys_opt_in: bool | None = Field(None, description="When enabled, enables survey and feedback collection features."),
    heatmaps_opt_in: bool | None = Field(None, description="When enabled, enables heatmap and click tracking features."),
    flags_persistence_default: bool | None = Field(None, description="When enabled, feature flags are persisted in local storage by default."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Indicates whether this project serves B2B (business-to-business) or B2C (business-to-consumer) customers. Used to optimize the UI layout and features."),
    conversations_enabled: bool | None = Field(None, description="When enabled, enables the conversations and messaging features for user communication."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for conversations feature including notification preferences and behavior."),
    proactive_tasks_enabled: bool | None = Field(None, description="When enabled, enables proactive task management and automation features."),
) -> dict[str, Any]:
    """Move a project to a different organization and update its configuration settings including product details, integrations, tracking preferences, and feature flags."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.ChangeOrganizationCreateRequest(
            path=_models.ChangeOrganizationCreateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.ChangeOrganizationCreateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_organization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/change_organization", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/change_organization"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_organization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_organization", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_organization",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_onboarding_configuration(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to update. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The organization ID that owns the project. Required to scope the operation to the correct organization."),
    product_description: str | None = Field(None, description="A brief description of the product. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="Array of application URLs associated with this project. Order is preserved."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Whether to anonymize user IP addresses in analytics."),
    test_account_filters_default_checked: bool | None = Field(None, description="Whether test account filters should be enabled by default in reports."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The project's primary timezone for reporting and scheduling. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names in the UI. Order determines display priority."),
    autocapture_opt_out: bool | None = Field(None, description="Whether to disable automatic event capture on web pages."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Whether to capture JavaScript exceptions and errors automatically."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Whether to capture Core Web Vitals metrics (LCP, FID, CLS) automatically."),
    capture_console_log_opt_in: bool | None = Field(None, description="Whether to capture browser console logs (log, warn, error) automatically."),
    capture_performance_opt_in: bool | None = Field(None, description="Whether to capture performance timing data automatically."),
    session_recording_opt_in: bool | None = Field(None, description="Whether to enable session recording for this project."),
    session_recording_sample_rate: str | None = Field(None, description="Fraction of sessions to record, expressed as a decimal between 0 and 1 (e.g., 0.1 for 10%, 1 for 100%). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording begins. Range: 0 to 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When provided, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long to retain session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="Whether to enable role-based access control for this project."),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week to use as the start of the week in reports and calendars (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to set as the primary/default dashboard for this project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed. Order determines column display sequence."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain patterns to allow session recording. Domains not in this list will not be recorded."),
    surveys_opt_in: bool | None = Field(None, description="Whether to enable in-app surveys for this project."),
    heatmaps_opt_in: bool | None = Field(None, description="Whether to enable heatmap tracking for this project."),
    flags_persistence_default: bool | None = Field(None, description="Whether feature flags should persist across sessions by default."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model classification for UI optimization. Choose B2B for business-to-business, B2C for business-to-consumer, or Other."),
    conversations_enabled: bool | None = Field(None, description="Whether to enable the conversations feature for this project."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for the conversations feature."),
    proactive_tasks_enabled: bool | None = Field(None, description="Whether to enable proactive tasks for this project."),
) -> dict[str, Any]:
    """Partially update product onboarding settings for a project, including analytics configuration, session recording, feature flags, and UI preferences."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.CompleteProductOnboardingPartialUpdateRequest(
            path=_models.CompleteProductOnboardingPartialUpdateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.CompleteProductOnboardingPartialUpdateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_onboarding_configuration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/complete_product_onboarding", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/complete_product_onboarding"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_onboarding_configuration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_onboarding_configuration", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_onboarding_configuration",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_settings_partial(
    id_: str = Field(..., alias="id", description="The unique numeric identifier of the project to update. Must be a valid 64-bit signed integer."),
    organization_id: str = Field(..., description="The organization identifier that owns the project."),
    product_description: str | None = Field(None, description="A brief description of the product. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="Array of URLs where the product is deployed or accessible."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for integration notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Enable automatic anonymization of user IP addresses in tracking data."),
    test_account_filters_default_checked: bool | None = Field(None, description="Whether test account filters should be enabled by default in analytics views."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Project timezone for reporting and scheduling. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user identities throughout the application."),
    autocapture_opt_out: bool | None = Field(None, description="Disable automatic event capture across the application."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Enable capture of JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Enable collection of Web Vitals metrics (Core Web Vitals and performance data)."),
    capture_console_log_opt_in: bool | None = Field(None, description="Enable capture of browser console logs (log, warn, error messages)."),
    capture_performance_opt_in: bool | None = Field(None, description="Enable collection of performance metrics and timing data."),
    session_recording_opt_in: bool | None = Field(None, description="Enable session recording functionality."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% sampling). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When provided, takes precedence over legacy trigger field settings."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Data retention period for session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="Enable role-based access control for project members."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Day of week to use as the start of reporting weeks (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="Dashboard ID to set as the primary/default dashboard for this project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column configurations for the live events view."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain patterns to include in session recording capture."),
    surveys_opt_in: bool | None = Field(None, description="Enable surveys and feedback collection features."),
    heatmaps_opt_in: bool | None = Field(None, description="Enable heatmap and click tracking features."),
    flags_persistence_default: bool | None = Field(None, description="Enable persistent storage of feature flags across sessions."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model classification for UI optimization. Select B2B for business-to-business, B2C for business-to-consumer, or Other."),
    conversations_enabled: bool | None = Field(None, description="Enable conversations and messaging features for user engagement."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for conversations functionality."),
    proactive_tasks_enabled: bool | None = Field(None, description="Enable proactive task management and automation features."),
) -> dict[str, Any]:
    """Partially update project configuration settings for the specified organization and project. Allows selective modification of project properties including product details, integrations, tracking preferences, and feature flags without affecting unspecified fields."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.DeleteSecretTokenBackupPartialUpdateRequest(
            path=_models.DeleteSecretTokenBackupPartialUpdateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.DeleteSecretTokenBackupPartialUpdateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_settings_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/delete_secret_token_backup", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/delete_secret_token_backup"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_settings_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_settings_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_settings_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def create_conversations_public_token(
    id_: str = Field(..., alias="id", description="The unique identifier of the project (64-bit integer)"),
    organization_id: str = Field(..., description="The organization ID that owns the project"),
    product_description: str | None = Field(None, description="A brief description of the product or service (up to 1000 characters)", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of application URLs where conversations will be enabled"),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending conversation notifications (up to 500 characters)", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Whether to anonymize user IP addresses in conversation data"),
    test_account_filters_default_checked: bool | None = Field(None, description="Whether test account filters should be enabled by default"),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for conversation timestamps and scheduling (IANA timezone identifier)"),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names in conversations"),
    autocapture_opt_out: bool | None = Field(None, description="Whether to disable automatic event capture for conversations"),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Whether to capture JavaScript exceptions in conversation sessions"),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Whether to capture Web Vitals metrics during conversation sessions"),
    capture_console_log_opt_in: bool | None = Field(None, description="Whether to capture browser console logs during conversation sessions"),
    capture_performance_opt_in: bool | None = Field(None, description="Whether to capture performance metrics during conversation sessions"),
    session_recording_opt_in: bool | None = Field(None, description="Whether to enable session recording for conversations"),
    session_recording_sample_rate: str | None = Field(None, description="Sample rate for session recording as a decimal between 0 and 1 (e.g., 0.5 for 50%)", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured (0-30000 ms)", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording; takes precedence over legacy trigger fields"),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long to retain session recordings: 30 days, 90 days, 1 year, or 5 years"),
    access_control: bool | None = Field(None, description="Whether to enable access control for conversation data"),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week to use as the start of the week (0-6, where 0 is Sunday)", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the primary dashboard for displaying conversation analytics"),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events view"),
    recording_domains: list[str | None] | None = Field(None, description="List of domains where conversation recording is allowed"),
    surveys_opt_in: bool | None = Field(None, description="Whether to enable surveys in conversations"),
    heatmaps_opt_in: bool | None = Field(None, description="Whether to enable heatmaps for conversation interactions"),
    flags_persistence_default: bool | None = Field(None, description="Whether feature flags should persist across conversation sessions by default"),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model type to optimize UI layout: B2B, B2C, or other"),
    conversations_enabled: bool | None = Field(None, description="Whether conversations functionality is enabled for this project"),
    conversations_settings: Any | None = Field(None, description="Configuration settings specific to conversations behavior and features"),
    proactive_tasks_enabled: bool | None = Field(None, description="Whether proactive task automation is enabled for conversations"),
) -> dict[str, Any]:
    """Generate a public token for conversations in a project, enabling secure access to conversation features with configurable settings for recording, analytics, and user tracking."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.GenerateConversationsPublicTokenCreateRequest(
            path=_models.GenerateConversationsPublicTokenCreateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.GenerateConversationsPublicTokenCreateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_conversations_public_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/generate_conversations_public_token", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/generate_conversations_public_token"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_conversations_public_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_conversations_public_token", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_conversations_public_token",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def get_project_demo_data_generation_status(
    id_: str = Field(..., alias="id", description="The unique identifier of the project. Must be a valid 64-bit signed integer."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the project."),
) -> dict[str, Any]:
    """Check whether a project is currently generating demo data. Returns the generation status for the specified project within an organization."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.IsGeneratingDemoDataRetrieveRequest(
            path=_models.IsGeneratingDemoDataRetrieveRequestPath(id_=_id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_project_demo_data_generation_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/is_generating_demo_data", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/is_generating_demo_data"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_project_demo_data_generation_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_project_demo_data_generation_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_project_demo_data_generation_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def update_project_reset_token(
    id_: str = Field(..., alias="id", description="The unique numeric identifier of the project to update. Must be a valid 64-bit signed integer."),
    organization_id: str = Field(..., description="The organization identifier that owns the project. Required to scope the operation to the correct organization."),
    product_description: str | None = Field(None, description="A brief description of the product or service this project tracks. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="Array of application URLs associated with this project. Used for domain-based filtering and configuration."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending notifications and alerts. Maximum 500 characters. Must be a valid webhook endpoint.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Enable IP address anonymization for privacy compliance. When true, user IP addresses are masked in recordings and analytics."),
    test_account_filters_default_checked: bool | None = Field(None, description="Pre-check the test account filters option by default in the UI. Helps filter out internal testing traffic from analytics."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Project timezone for scheduling and time-based reporting. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user identities throughout the application. Order determines display priority."),
    autocapture_opt_out: bool | None = Field(None, description="Disable automatic event capture globally. When true, only explicitly tracked events are recorded."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Enable automatic capture of JavaScript exceptions and errors. Provides visibility into application crashes and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Enable automatic capture of Web Vitals metrics (LCP, FID, CLS). Helps monitor core web performance indicators."),
    capture_console_log_opt_in: bool | None = Field(None, description="Enable automatic capture of browser console logs. Useful for debugging and monitoring application behavior."),
    capture_performance_opt_in: bool | None = Field(None, description="Enable automatic capture of performance metrics and timing data. Provides insights into application performance."),
    session_recording_opt_in: bool | None = Field(None, description="Enable session recording functionality. When true, user sessions are recorded for playback and analysis."),
    session_recording_sample_rate: str | None = Field(None, description="Sampling rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Helps manage storage and performance.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30000 milliseconds. Filters out very short sessions.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. Defines conditions that trigger automatic session recording. Takes precedence over legacy trigger fields if present."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Data retention period for session recordings. Options: 30 days, 90 days, 1 year, or 5 years. Longer retention increases storage costs."),
    access_control: bool | None = Field(None, description="Enable role-based access control for project members. When true, granular permission management is available."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Day of week to use as the start of the week in reports and dashboards. Accepts numeric day values (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="Dashboard ID to set as the primary/default dashboard. Users see this dashboard first when accessing the project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed. Order determines column display sequence."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain patterns for which session recording is enabled. Restricts recording to specified domains only."),
    surveys_opt_in: bool | None = Field(None, description="Enable surveys and feedback collection features. When true, survey tools are available for gathering user feedback."),
    heatmaps_opt_in: bool | None = Field(None, description="Enable heatmaps and click tracking visualization. When true, visual heatmap analysis tools are available."),
    flags_persistence_default: bool | None = Field(None, description="Enable persistent flag storage by default. When true, feature flags are retained across sessions."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model classification for UI optimization. Select 'b2b' for business-to-business, 'b2c' for business-to-consumer, or 'other' for alternative models."),
    conversations_enabled: bool | None = Field(None, description="Enable conversations and user communication features. When true, in-app messaging and support tools are available."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for conversations feature. Defines behavior and options for user communication tools."),
    proactive_tasks_enabled: bool | None = Field(None, description="Enable proactive task management and automation. When true, automated task creation and workflow features are available."),
) -> dict[str, Any]:
    """Partially update project configuration and reset its authentication token. Allows modification of project settings such as product description, URLs, integrations, and feature flags while invalidating the current token."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.ResetTokenPartialUpdateRequest(
            path=_models.ResetTokenPartialUpdateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.ResetTokenPartialUpdateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_reset_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/reset_token", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/reset_token"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_reset_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_reset_token", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_reset_token",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, projects
@mcp.tool()
async def rotate_project_secret_token(
    id_: str = Field(..., alias="id", description="The unique identifier of the project to rotate the secret token for. Must be a valid 64-bit integer."),
    organization_id: str = Field(..., description="The unique identifier of the organization that owns the project."),
    product_description: str | None = Field(None, description="A brief description of the product. Maximum 1000 characters.", max_length=1000),
    app_urls: list[str | None] | None = Field(None, description="List of URLs where the product is deployed or accessible."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for sending notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Whether to anonymize user IP addresses in analytics."),
    test_account_filters_default_checked: bool | None = Field(None, description="Whether test account filters should be enabled by default in the UI."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for the project, used for time-based reporting and scheduling. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user names in the UI."),
    autocapture_opt_out: bool | None = Field(None, description="Whether to disable automatic event capture on the frontend."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Whether to enable capture of JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Whether to enable capture of Web Vitals metrics (Core Web Vitals)."),
    capture_console_log_opt_in: bool | None = Field(None, description="Whether to enable capture of browser console logs."),
    capture_performance_opt_in: bool | None = Field(None, description="Whether to enable capture of performance metrics."),
    session_recording_opt_in: bool | None = Field(None, description="Whether to enable session recording."),
    session_recording_sample_rate: str | None = Field(None, description="The sample rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Must be between 0 and 30000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When provided, this takes precedence over legacy trigger field settings."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long to retain session recordings. Options: 30 days, 90 days, 1 year, or 5 years."),
    access_control: bool | None = Field(None, description="Whether to enable access control and permission management for this project."),
    week_start_day: Literal[0, 1] | None = Field(None, description="The day of the week to use as the start of the week in reports and calendars (0-6, where 0 is Sunday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="The ID of the dashboard to set as the primary/default dashboard for this project."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events viewer."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain names where session recording is enabled."),
    surveys_opt_in: bool | None = Field(None, description="Whether to enable surveys and feedback collection."),
    heatmaps_opt_in: bool | None = Field(None, description="Whether to enable heatmaps and click tracking."),
    flags_persistence_default: bool | None = Field(None, description="Whether feature flags should persist across sessions by default."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="The business model for this project. Use 'b2b' for business-to-business, 'b2c' for business-to-consumer, or 'other' for different models. This optimizes the UI layout accordingly."),
    conversations_enabled: bool | None = Field(None, description="Whether to enable conversations and user feedback features."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for conversations and feedback collection."),
    proactive_tasks_enabled: bool | None = Field(None, description="Whether to enable proactive task management and automation."),
) -> dict[str, Any]:
    """Rotate the secret token for a project within an organization. This operation performs a partial update to project settings while rotating the authentication token used for API requests."""

    _id_ = _parse_int(id_)

    # Construct request model with validation
    try:
        _request = _models.RotateSecretTokenPartialUpdateRequest(
            path=_models.RotateSecretTokenPartialUpdateRequestPath(id_=_id_, organization_id=organization_id),
            body=_models.RotateSecretTokenPartialUpdateRequestBody(product_description=product_description, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, access_control=access_control, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for rotate_project_secret_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/projects/{id}/rotate_secret_token", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/projects/{id}/rotate_secret_token"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("rotate_project_secret_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("rotate_project_secret_token", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="rotate_project_secret_token",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: reverse_proxy, proxy_records
@mcp.tool()
async def list_proxy_records(organization_id: str = Field(..., description="The unique identifier of the organization whose proxy records should be retrieved.")) -> dict[str, Any]:
    """Retrieve all reverse proxy configurations for an organization, including details about each proxy record and the maximum number of proxies allowed under the current plan."""

    # Construct request model with validation
    try:
        _request = _models.ProxyRecordsListRequest(
            path=_models.ProxyRecordsListRequestPath(organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_proxy_records: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/proxy_records", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/proxy_records"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_proxy_records")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_proxy_records", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_proxy_records",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: reverse_proxy, proxy_records
@mcp.tool()
async def create_proxy_record(
    organization_id: str = Field(..., description="The unique identifier of the organization that will own this proxy record."),
    domain: str = Field(..., description="The custom subdomain to proxy through (e.g., 'e.example.com'). Must be a valid subdomain that you control."),
) -> dict[str, Any]:
    """Create a new managed reverse proxy for a custom domain. After creation, you'll receive a CNAME target to add to your DNS records. The proxy will be automatically verified and provisioned once the DNS configuration is in place."""

    # Construct request model with validation
    try:
        _request = _models.ProxyRecordsCreateRequest(
            path=_models.ProxyRecordsCreateRequestPath(organization_id=organization_id),
            body=_models.ProxyRecordsCreateRequestBody(domain=domain)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_proxy_record: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/proxy_records", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/proxy_records"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_proxy_record")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_proxy_record", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_proxy_record",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: reverse_proxy, proxy_records
@mcp.tool()
async def get_proxy_record(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the proxy record to retrieve."),
    organization_id: str = Field(..., description="The unique identifier (UUID) of the organization that owns the proxy record."),
) -> dict[str, Any]:
    """Retrieve the complete configuration details of a specific reverse proxy record, including its domain, CNAME target, and current provisioning status."""

    # Construct request model with validation
    try:
        _request = _models.ProxyRecordsRetrieveRequest(
            path=_models.ProxyRecordsRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_proxy_record: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/proxy_records/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/proxy_records/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_proxy_record")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_proxy_record", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_proxy_record",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: reverse_proxy, proxy_records
@mcp.tool()
async def delete_proxy_record(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the proxy record to delete."),
    organization_id: str = Field(..., description="The unique identifier (UUID) of the organization that owns the proxy record."),
) -> dict[str, Any]:
    """Delete a reverse proxy record. For proxies in waiting, erroring, or timed-out status, the record is deleted immediately. For active proxies, a deletion workflow is initiated to safely clean up provisioned infrastructure."""

    # Construct request model with validation
    try:
        _request = _models.ProxyRecordsDestroyRequest(
            path=_models.ProxyRecordsDestroyRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_proxy_record: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/proxy_records/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/proxy_records/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_proxy_record")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_proxy_record", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_proxy_record",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, roles
@mcp.tool()
async def get_role(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the role to retrieve."),
    organization_id: str = Field(..., description="The unique identifier of the organization that contains the role."),
) -> dict[str, Any]:
    """Retrieve a specific role within an organization by its unique identifier. Returns the role's details including permissions and metadata."""

    # Construct request model with validation
    try:
        _request = _models.RolesRetrieveRequest(
            path=_models.RolesRetrieveRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_role: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_role")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_role", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_role",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, roles
@mcp.tool()
async def update_role(
    id_: str = Field(..., alias="id", description="The unique identifier of the role to update, formatted as a UUID string."),
    organization_id: str = Field(..., description="The unique identifier of the organization that contains the role."),
) -> dict[str, Any]:
    """Partially update a role within an organization. Allows modification of specific role properties without requiring all fields to be provided."""

    # Construct request model with validation
    try:
        _request = _models.RolesPartialUpdateRequest(
            path=_models.RolesPartialUpdateRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_role: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_role")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_role", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_role",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, roles
@mcp.tool()
async def delete_role(
    id_: str = Field(..., alias="id", description="The unique identifier of the role to delete, provided as a UUID string."),
    organization_id: str = Field(..., description="The unique identifier of the organization that contains the role, provided as a UUID string."),
) -> dict[str, Any]:
    """Permanently delete a role from an organization. This operation removes the specified role and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.RolesDestroyRequest(
            path=_models.RolesDestroyRequestPath(id_=id_, organization_id=organization_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_role: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_role")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_role", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_role",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, roles
@mcp.tool()
async def list_role_memberships(
    organization_id: str = Field(..., description="The unique identifier of the organization containing the role."),
    role_id: str = Field(..., description="The unique identifier (UUID format) of the role whose memberships you want to retrieve."),
    limit: int | None = Field(None, description="Maximum number of results to return per page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position (zero-indexed) from which to return results. Use with limit for pagination."),
) -> dict[str, Any]:
    """Retrieve all members assigned to a specific role within an organization. Returns a paginated list of role memberships for the given role."""

    # Construct request model with validation
    try:
        _request = _models.RolesRoleMembershipsListRequest(
            path=_models.RolesRoleMembershipsListRequestPath(organization_id=organization_id, role_id=role_id),
            query=_models.RolesRoleMembershipsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_role_memberships: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{role_id}/role_memberships", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{role_id}/role_memberships"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_role_memberships")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_role_memberships", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_role_memberships",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, roles
@mcp.tool()
async def add_user_to_role(
    organization_id: str = Field(..., description="The unique identifier of the organization where the role membership is being created."),
    role_id: str = Field(..., description="The unique identifier (UUID format) of the role to which the user is being added."),
    user_uuid: str = Field(..., description="The unique identifier (UUID format) of the user to be added to the role."),
) -> dict[str, Any]:
    """Adds a user to a role within an organization, granting them the permissions and responsibilities associated with that role."""

    # Construct request model with validation
    try:
        _request = _models.RolesRoleMembershipsCreateRequest(
            path=_models.RolesRoleMembershipsCreateRequestPath(organization_id=organization_id, role_id=role_id),
            body=_models.RolesRoleMembershipsCreateRequestBody(user_uuid=user_uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_user_to_role: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{role_id}/role_memberships", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{role_id}/role_memberships"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_user_to_role")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_user_to_role", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_user_to_role",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, roles
@mcp.tool()
async def get_role_membership(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the role membership to retrieve."),
    organization_id: str = Field(..., description="The unique identifier of the organization that contains the role and role membership."),
    role_id: str = Field(..., description="The unique identifier (UUID) of the role that contains the role membership."),
) -> dict[str, Any]:
    """Retrieve a specific role membership by its unique identifier within an organization and role context."""

    # Construct request model with validation
    try:
        _request = _models.RolesRoleMembershipsRetrieveRequest(
            path=_models.RolesRoleMembershipsRetrieveRequestPath(id_=id_, organization_id=organization_id, role_id=role_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_role_membership: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{role_id}/role_memberships/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{role_id}/role_memberships/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_role_membership")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_role_membership", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_role_membership",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: organizations, roles
@mcp.tool()
async def remove_role_membership(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the role membership to remove."),
    organization_id: str = Field(..., description="The unique identifier of the organization containing the role."),
    role_id: str = Field(..., description="The unique identifier (UUID) of the role from which the membership will be removed."),
) -> dict[str, Any]:
    """Remove a user or entity from a role within an organization. This operation deletes the specified role membership, effectively revoking the associated role assignment."""

    # Construct request model with validation
    try:
        _request = _models.RolesRoleMembershipsDestroyRequest(
            path=_models.RolesRoleMembershipsDestroyRequestPath(id_=id_, organization_id=organization_id, role_id=role_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_role_membership: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/organizations/{organization_id}/roles/{role_id}/role_memberships/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/organizations/{organization_id}/roles/{role_id}/role_memberships/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_role_membership")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_role_membership", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_role_membership",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def list_actions(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of actions to return in a single response page. Defaults to the API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of actions for a specific project. Use pagination parameters to control the number of results and navigate through large result sets."""

    # Construct request model with validation
    try:
        _request = _models.ActionsListRequest(
            path=_models.ActionsListRequestPath(project_id=project_id),
            query=_models.ActionsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_actions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_actions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_actions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_actions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def create_action(
    project_id: str = Field(..., description="The unique identifier of the project where the action will be created. Retrieve available project IDs by calling the projects list endpoint."),
    description: str | None = Field(None, description="A human-readable explanation of the action's purpose and behavior."),
    tags: list[Any] | None = Field(None, description="Labels or categories to organize and filter actions. Provide as an array of string values."),
    post_to_slack: bool | None = Field(None, description="Enable automatic Slack notifications when this action is triggered."),
    slack_message_format: str | None = Field(None, description="Template string for customizing Slack notification content using event properties. Maximum 1200 characters.", max_length=1200),
    steps: list[_models.ActionStepJson] | None = Field(None, description="Array of trigger conditions that define when the action executes. Each step matches events by name, properties, URL, or element attributes. Multiple steps are combined with OR logic, so the action triggers if any step matches."),
    pinned_at: str | None = Field(None, description="ISO 8601 formatted timestamp indicating when the action was pinned for visibility. Set a timestamp to pin the action, or null to unpin it."),
) -> dict[str, Any]:
    """Create a new action in a project that triggers automated workflows based on event matching. Actions can send Slack notifications and execute defined steps when specified conditions are met."""

    # Construct request model with validation
    try:
        _request = _models.ActionsCreateRequest(
            path=_models.ActionsCreateRequestPath(project_id=project_id),
            body=_models.ActionsCreateRequestBody(description=description, tags=tags, post_to_slack=post_to_slack, slack_message_format=slack_message_format, steps=steps, pinned_at=pinned_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_action: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_action")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_action", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_action",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def get_action(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the action to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the action. You can obtain project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific action by its ID within a project. Use this to fetch detailed information about a single action."""

    # Construct request model with validation
    try:
        _request = _models.ActionsRetrieveRequest(
            path=_models.ActionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_action: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_action")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_action", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_action",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def update_action(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the action to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the action. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable description explaining what this action does and when it triggers."),
    tags: list[Any] | None = Field(None, description="Labels or keywords for organizing and categorizing this action. Provided as an array of tag values."),
    post_to_slack: bool | None = Field(None, description="Enable or disable automatic Slack notifications when this action is triggered."),
    slack_message_format: str | None = Field(None, description="Custom message template for Slack notifications, supporting dynamic substitution of event properties. Limited to 1200 characters.", max_length=1200),
    steps: list[_models.ActionStepJson] | None = Field(None, description="Array of trigger conditions that define when this action executes. Each step matches events by name, properties, URL, or element attributes. Multiple steps are combined with OR logic, so the action triggers if any step matches."),
    pinned_at: str | None = Field(None, description="ISO 8601 formatted timestamp indicating when the action was pinned for quick access. Set to any value to pin the action, or null to unpin it."),
) -> dict[str, Any]:
    """Update an action's configuration, including its description, trigger steps, Slack notifications, and pinned status within a project."""

    # Construct request model with validation
    try:
        _request = _models.ActionsUpdateRequest(
            path=_models.ActionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ActionsUpdateRequestBody(description=description, tags=tags, post_to_slack=post_to_slack, slack_message_format=slack_message_format, steps=steps, pinned_at=pinned_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_action: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_action")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_action", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_action",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def update_action_partial(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the action to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the action. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable description explaining what this action does and when it triggers."),
    tags: list[Any] | None = Field(None, description="An array of tags for categorizing and organizing the action."),
    post_to_slack: bool | None = Field(None, description="Enable or disable Slack notifications when this action is triggered."),
    slack_message_format: str | None = Field(None, description="Custom message template for Slack notifications, supporting dynamic event properties. Limited to 1200 characters.", max_length=1200),
    steps: list[_models.ActionStepJson] | None = Field(None, description="An array of trigger steps that define when the action executes. Each step matches events by name, properties, URL, or element attributes. Multiple steps are combined with OR logic, so the action triggers if any step matches."),
    pinned_at: str | None = Field(None, description="ISO 8601 formatted timestamp indicating when the action was pinned, or null to unpin. Set any value to pin the action."),
) -> dict[str, Any]:
    """Partially update an action's configuration, including its description, trigger steps, Slack notifications, and pinned status within a specific project."""

    # Construct request model with validation
    try:
        _request = _models.ActionsPartialUpdateRequest(
            path=_models.ActionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ActionsPartialUpdateRequestBody(description=description, tags=tags, post_to_slack=post_to_slack, slack_message_format=slack_message_format, steps=steps, pinned_at=pinned_at)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_action_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_action_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_action_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_action_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def list_action_references(
    id_: int = Field(..., alias="id", description="The unique identifier of the action whose references you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the action. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all references to a specific action within a project. References indicate where and how the action is being used or linked throughout the project."""

    # Construct request model with validation
    try:
        _request = _models.ActionsReferencesListRequest(
            path=_models.ActionsReferencesListRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_action_references: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions/{id}/references", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions/{id}/references"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_action_references")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_action_references", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_action_references",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: activity_logs, activity_log
@mcp.tool()
async def list_activity_logs(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    item_id: str | None = Field(None, description="Filter activity logs to only those affecting a specific resource by its ID. Must be at least 1 character long.", min_length=1),
    page: int | None = Field(None, description="The page number for pagination, starting from 1. Use this to navigate through paginated results ordered by most recent activity first.", ge=1),
    page_size: int | None = Field(None, description="The number of results to return per page. Defaults to 100 if not specified. Maximum allowed is 1000 results per page.", ge=1, le=1000),
    scopes: list[Literal["Cohort", "FeatureFlag", "Person", "Group", "Insight", "Plugin", "PluginConfig", "HogFunction", "HogFlow", "DataManagement", "EventDefinition", "PropertyDefinition", "Notebook", "Endpoint", "EndpointVersion", "Dashboard", "Replay", "Experiment", "ExperimentHoldout", "ExperimentSavedMetric", "Survey", "EarlyAccessFeature", "SessionRecordingPlaylist", "Comment", "Team", "Project", "ErrorTrackingIssue", "DataWarehouseSavedQuery", "Organization", "OrganizationDomain", "OrganizationMembership", "Role", "UserGroup", "BatchExport", "BatchImport", "Integration", "Annotation", "Tag", "TaggedItem", "Subscription", "PersonalAPIKey", "ProjectSecretAPIKey", "User", "Action", "AlertConfiguration", "Threshold", "AlertSubscription", "ExternalDataSource", "ExternalDataSchema", "LLMTrace", "WebAnalyticsFilterPreset", "CustomerProfileConfig", "Log", "LogsAlertConfiguration", "ProductTour", "Ticket"]] | None = Field(None, description="Filter activity logs by one or more activity scopes using comma-separated valid ActivityScope enum values (e.g., FeatureFlag,Insight). Only logs matching the specified scopes will be returned."),
) -> dict[str, Any]:
    """Retrieve activity logs for a project, with optional filtering by resource and activity scope. Results are paginated and ordered by most recent first."""

    # Construct request model with validation
    try:
        _request = _models.ActivityLogListRequest(
            path=_models.ActivityLogListRequestPath(project_id=project_id),
            query=_models.ActivityLogListRequestQuery(item_id=item_id, page=page, page_size=page_size, scopes=scopes)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_activity_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/activity_log", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/activity_log"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_activity_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_activity_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_activity_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: advanced_activity_logs
@mcp.tool()
async def list_advanced_activity_logs(project_id: str = Field(..., description="The unique identifier of the project for which to retrieve activity logs. You can obtain the project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve a list of advanced activity logs for a specific project, providing detailed records of project activities and events."""

    # Construct request model with validation
    try:
        _request = _models.AdvancedActivityLogsListRequest(
            path=_models.AdvancedActivityLogsListRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_advanced_activity_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/advanced_activity_logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/advanced_activity_logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_advanced_activity_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_advanced_activity_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_advanced_activity_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def list_alerts(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of alerts to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based starting position for results. Use with limit to paginate through alerts."),
) -> dict[str, Any]:
    """Retrieve a paginated list of alerts for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.AlertsListRequest(
            path=_models.AlertsListRequestPath(project_id=project_id),
            query=_models.AlertsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_alerts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_alerts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_alerts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_alerts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def create_alert(
    project_id: str = Field(..., description="The unique identifier of the project containing the insight to monitor. Retrieve available project IDs from the /api/projects/ endpoint."),
    insight: int = Field(..., description="The unique identifier of the insight to be monitored by this alert. The response will include the full insight details."),
    subscribed_users: list[int] = Field(..., description="A list of user IDs who will receive notifications when this alert triggers. The response will include full user details for each subscriber."),
    threshold: _models.AlertsCreateBodyThreshold = Field(..., description="Configuration object specifying the threshold bounds and evaluation type (e.g., numeric value, percentage) used to determine when the alert should trigger."),
    condition: _models.AlertsCreateBodyCondition | None = Field(None, description="The evaluation method for comparing the monitored value against the threshold: absolute_value (direct comparison), relative_increase (percentage growth), or relative_decrease (percentage decline). Defaults to absolute_value if not specified."),
    enabled: bool | None = Field(None, description="Boolean flag indicating whether the alert is actively being evaluated. Set to false to disable the alert without deleting it."),
    detector_config: _models.EnsembleDetectorConfig | _models.ZScoreDetectorConfig | _models.MadDetectorConfig | _models.IqrDetectorConfig | _models.ThresholdDetectorConfig | _models.EcodDetectorConfig | _models.CopodDetectorConfig | _models.IsolationForestDetectorConfig | _models.KnnDetectorConfig | _models.HbosDetectorConfig | _models.LofDetectorConfig | _models.OcsvmDetectorConfig | _models.PcaDetectorConfig | None = Field(None, description="Advanced configuration object for customizing the detection algorithm or sensitivity parameters used to evaluate the alert condition."),
    calculation_interval: Literal["hourly", "daily", "weekly", "monthly"] | None = Field(None, description="The frequency at which the alert is evaluated: hourly, daily, weekly, or monthly. Determines how often the threshold is checked against current data."),
    snoozed_until: str | None = Field(None, description="Temporarily suppress alert notifications until the specified time. Accepts relative date strings (e.g., '2h' for 2 hours, '1d' for 1 day) or null to immediately unsnooze an active alert."),
    skip_weekend: bool | None = Field(None, description="Boolean flag to exclude weekends (Saturday and Sunday) from alert evaluation. Useful for business-focused metrics that are not relevant on non-working days."),
) -> dict[str, Any]:
    """Create a new alert for an insight that monitors a metric against a threshold and notifies subscribed users when the condition is met."""

    # Construct request model with validation
    try:
        _request = _models.AlertsCreateRequest(
            path=_models.AlertsCreateRequestPath(project_id=project_id),
            body=_models.AlertsCreateRequestBody(insight=insight, subscribed_users=subscribed_users, threshold=threshold, condition=condition, enabled=enabled, detector_config=detector_config, calculation_interval=calculation_interval, snoozed_until=snoozed_until, skip_weekend=skip_weekend)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_alert", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_alert",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def get_alert(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the alert configuration to retrieve."),
    project_id: str = Field(..., description="The ID of the project containing the alert. Use /api/projects/ to discover available project IDs."),
    checks_date_from: str | None = Field(None, description="Optional relative date string marking the start of the check history window (e.g., '-24h', '-7d', '-14d'). Returns checks created after this time. Historical data is retained for a maximum of 14 days."),
    checks_date_to: str | None = Field(None, description="Optional relative date string marking the end of the check history window (e.g., '-1h', '-1d'). Defaults to the current time if not specified."),
    checks_limit: int | None = Field(None, description="Optional maximum number of check results to return, applied after date filtering. Defaults to 5 with a maximum of 500."),
) -> dict[str, Any]:
    """Retrieve a specific alert configuration by ID, optionally including historical check results within a specified time window."""

    # Construct request model with validation
    try:
        _request = _models.AlertsRetrieveRequest(
            path=_models.AlertsRetrieveRequestPath(id_=id_, project_id=project_id),
            query=_models.AlertsRetrieveRequestQuery(checks_date_from=checks_date_from, checks_date_to=checks_date_to, checks_limit=checks_limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts/{id}"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_alert", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_alert",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def update_alert(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the alert configuration to update."),
    project_id: str = Field(..., description="The project ID containing this alert. Retrieve available project IDs from the /api/projects/ endpoint."),
    insight: int = Field(..., description="The ID of the insight this alert monitors. The response will include the full insight details."),
    subscribed_users: list[int] = Field(..., description="List of user IDs to subscribe to alert notifications. The response will include full user details for each subscriber."),
    threshold: _models.AlertsUpdateBodyThreshold = Field(..., description="Threshold configuration object that defines the bounds and evaluation type for triggering the alert."),
    condition: _models.AlertsUpdateBodyCondition | None = Field(None, description="How the alert value is evaluated against the threshold: absolute_value (fixed threshold), relative_increase (percentage increase), or relative_decrease (percentage decrease)."),
    enabled: bool | None = Field(None, description="Whether the alert is actively being monitored and evaluated."),
    detector_config: _models.EnsembleDetectorConfig | _models.ZScoreDetectorConfig | _models.MadDetectorConfig | _models.IqrDetectorConfig | _models.ThresholdDetectorConfig | _models.EcodDetectorConfig | _models.CopodDetectorConfig | _models.IsolationForestDetectorConfig | _models.KnnDetectorConfig | _models.HbosDetectorConfig | _models.LofDetectorConfig | _models.OcsvmDetectorConfig | _models.PcaDetectorConfig | None = Field(None, description="Configuration object for the alert detection mechanism."),
    calculation_interval: Literal["hourly", "daily", "weekly", "monthly"] | None = Field(None, description="How frequently the alert is evaluated: hourly, daily, weekly, or monthly."),
    snoozed_until: str | None = Field(None, description="Temporarily disable the alert until a specified time. Accepts relative date strings (e.g., '2h', '1d') or null to remove the snooze."),
    skip_weekend: bool | None = Field(None, description="When enabled, prevents alert evaluation on weekends (Saturday and Sunday)."),
) -> dict[str, Any]:
    """Update an alert configuration for a specific insight, including threshold settings, subscribed users, and evaluation parameters."""

    # Construct request model with validation
    try:
        _request = _models.AlertsUpdateRequest(
            path=_models.AlertsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.AlertsUpdateRequestBody(insight=insight, subscribed_users=subscribed_users, threshold=threshold, condition=condition, enabled=enabled, detector_config=detector_config, calculation_interval=calculation_interval, snoozed_until=snoozed_until, skip_weekend=skip_weekend)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_alert", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_alert",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def update_alert_configuration(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the alert configuration to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the alert. Retrieve available project IDs from the /api/projects/ endpoint."),
    insight: int | None = Field(None, description="The ID of the insight to monitor with this alert. The response will include the full insight object details."),
    subscribed_users: list[int] | None = Field(None, description="A list of user IDs to subscribe to alert notifications. The response will include full user object details for each subscriber."),
    condition: _models.AlertsPartialUpdateBodyCondition | None = Field(None, description="The evaluation method for the alert threshold: absolute_value (fixed threshold), relative_increase (percentage growth), or relative_decrease (percentage decline)."),
    enabled: bool | None = Field(None, description="Enable or disable active evaluation of this alert. When disabled, the alert will not trigger notifications."),
    detector_config: _models.EnsembleDetectorConfig | _models.ZScoreDetectorConfig | _models.MadDetectorConfig | _models.IqrDetectorConfig | _models.ThresholdDetectorConfig | _models.EcodDetectorConfig | _models.CopodDetectorConfig | _models.IsolationForestDetectorConfig | _models.KnnDetectorConfig | _models.HbosDetectorConfig | _models.LofDetectorConfig | _models.OcsvmDetectorConfig | _models.PcaDetectorConfig | None = Field(None, description="Configuration settings for the alert detection mechanism."),
    calculation_interval: Literal["hourly", "daily", "weekly", "monthly"] | None = Field(None, description="The frequency at which the alert is evaluated: hourly, daily, weekly, or monthly."),
    snoozed_until: str | None = Field(None, description="Temporarily suppress alert notifications until the specified time. Accepts relative date strings (e.g., '2h', '1d') or null to remove the snooze."),
    skip_weekend: bool | None = Field(None, description="When enabled, skip alert evaluation on weekends (Saturday and Sunday)."),
) -> dict[str, Any]:
    """Partially update an alert configuration for a specific project. Modify alert settings such as enabled status, notification subscribers, evaluation frequency, and snooze state."""

    # Construct request model with validation
    try:
        _request = _models.AlertsPartialUpdateRequest(
            path=_models.AlertsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.AlertsPartialUpdateRequestBody(insight=insight, subscribed_users=subscribed_users, condition=condition, enabled=enabled, detector_config=detector_config, calculation_interval=calculation_interval, snoozed_until=snoozed_until, skip_weekend=skip_weekend)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_alert_configuration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_alert_configuration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_alert_configuration", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_alert_configuration",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def delete_alert(
    id_: str = Field(..., alias="id", description="The unique identifier of the alert configuration to delete, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the alert. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a specific alert configuration from a project. This permanently removes the alert and its associated settings."""

    # Construct request model with validation
    try:
        _request = _models.AlertsDestroyRequest(
            path=_models.AlertsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_alert", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_alert",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: alerts
@mcp.tool()
async def simulate_detector_on_insight(
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Retrieve available project IDs from the /api/projects/ endpoint."),
    insight: int = Field(..., description="The unique identifier of the insight whose historical data will be used for the detector simulation."),
    detector_config: _models.EnsembleDetectorConfig | _models.ZScoreDetectorConfig | _models.MadDetectorConfig | _models.IqrDetectorConfig | _models.ThresholdDetectorConfig | _models.EcodDetectorConfig | _models.CopodDetectorConfig | _models.IsolationForestDetectorConfig | _models.KnnDetectorConfig | _models.HbosDetectorConfig | _models.LofDetectorConfig | _models.OcsvmDetectorConfig | _models.PcaDetectorConfig = Field(..., description="The detector configuration object to test. This defines the detection rules and parameters to simulate against the insight's data."),
    series_index: int | None = Field(None, description="The zero-based index of the time series within the insight to analyze. Defaults to 0 if not specified."),
) -> dict[str, Any]:
    """Test a detector configuration against historical data from an insight without creating any alert records. Useful for validating detector behavior before deployment."""

    # Construct request model with validation
    try:
        _request = _models.AlertsSimulateCreateRequest(
            path=_models.AlertsSimulateCreateRequestPath(project_id=project_id),
            body=_models.AlertsSimulateCreateRequestBody(insight=insight, detector_config=detector_config, series_index=series_index)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for simulate_detector_on_insight: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/alerts/simulate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/alerts/simulate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("simulate_detector_on_insight")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("simulate_detector_on_insight", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="simulate_detector_on_insight",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def list_annotations(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of annotations to return per page. Use this to control pagination size."),
    offset: int | None = Field(None, description="The starting position for results (zero-indexed). Use this with limit to paginate through annotations."),
) -> dict[str, Any]:
    """Retrieve a paginated list of annotations for a specific project. Annotations are used to mark and document important events or changes in your project's data."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsListRequest(
            path=_models.AnnotationsListRequestPath(project_id=project_id),
            query=_models.AnnotationsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_annotations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_annotations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_annotations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_annotations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def create_annotation(
    project_id: str = Field(..., description="The unique identifier of the project where you want to create the annotation. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    date_marker: str | None = Field(None, description="The timestamp when this annotation occurred, formatted as an ISO 8601 datetime string. This positions the annotation on your charts at the correct point in time."),
    dashboard_item: int | None = Field(None, description="The ID of a specific dashboard item (chart or insight) to attach this annotation to. Use this to associate the annotation with a particular visualization."),
    dashboard_id: int | None = Field(None, description="The ID of a dashboard to attach this annotation to. Use this to associate the annotation with an entire dashboard rather than a specific item."),
) -> dict[str, Any]:
    """Create a new annotation to mark important events or insights on your PostHog charts and dashboards. Annotations help you correlate product changes with metrics and share context with your team."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsCreateRequest(
            path=_models.AnnotationsCreateRequestPath(project_id=project_id),
            body=_models.AnnotationsCreateRequestBody(date_marker=date_marker, dashboard_item=dashboard_item, dashboard_id=dashboard_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_annotation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_annotation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_annotation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_annotation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def get_annotation(
    id_: int = Field(..., alias="id", description="The unique identifier of the annotation to retrieve. This is a positive integer assigned by PostHog when the annotation was created."),
    project_id: str = Field(..., description="The unique identifier of the PostHog project containing the annotation. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific annotation by its ID from a PostHog project. Annotations are used to mark and document important events or changes in your data."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsRetrieveRequest(
            path=_models.AnnotationsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_annotation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_annotation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_annotation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_annotation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def update_annotation(
    id_: int = Field(..., alias="id", description="The unique identifier of the annotation to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the annotation. Retrieve available project IDs from the /api/projects/ endpoint."),
    date_marker: str | None = Field(None, description="The date and time when this annotation occurred, formatted as an ISO 8601 timestamp. This positions the annotation on charts and timelines."),
    dashboard_item: int | None = Field(None, description="The ID of a specific dashboard item (chart or visualization) to associate this annotation with."),
    dashboard_id: int | None = Field(None, description="The ID of a dashboard to associate this annotation with."),
) -> dict[str, Any]:
    """Update an existing annotation in a project, including its timestamp and optional dashboard associations. Use this to modify when an annotation occurred or which dashboard it's linked to."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsUpdateRequest(
            path=_models.AnnotationsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.AnnotationsUpdateRequestBody(date_marker=date_marker, dashboard_item=dashboard_item, dashboard_id=dashboard_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_annotation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_annotation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_annotation", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_annotation",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def update_annotation_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the annotation to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the annotation. Retrieve available project IDs from the /api/projects/ endpoint."),
    date_marker: str | None = Field(None, description="The timestamp when this annotation occurred, formatted as an ISO 8601 datetime string. Used to position the annotation on charts."),
    dashboard_item: int | None = Field(None, description="The unique identifier of the dashboard item (e.g., chart or graph) to associate this annotation with."),
    dashboard_id: int | None = Field(None, description="The unique identifier of the dashboard to associate this annotation with."),
) -> dict[str, Any]:
    """Partially update an annotation in a project. Modify annotation metadata such as the date marker, or reassign it to a different dashboard or dashboard item."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsPartialUpdateRequest(
            path=_models.AnnotationsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.AnnotationsPartialUpdateRequestBody(date_marker=date_marker, dashboard_item=dashboard_item, dashboard_id=dashboard_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_annotation_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_annotation_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_annotation_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_annotation_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: app_metrics
@mcp.tool()
async def get_app_metrics(
    id_: int = Field(..., alias="id", description="The unique identifier of the app metrics record to retrieve. This is a positive integer that uniquely identifies the metrics configuration within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the app metrics. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed metrics for a specific app configuration within a project. Use this to fetch performance, usage, or diagnostic data associated with a particular app metrics record."""

    # Construct request model with validation
    try:
        _request = _models.AppMetricsRetrieveRequest(
            path=_models.AppMetricsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_app_metrics: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/app_metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/app_metrics/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_app_metrics")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_app_metrics", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_app_metrics",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: app_metrics
@mcp.tool()
async def list_app_metrics_historical_exports(
    plugin_config_id: int = Field(..., description="The unique identifier of the plugin configuration whose historical metric exports you want to retrieve. This ID specifies which plugin's metrics to query."),
    project_id: str = Field(..., description="The unique identifier of the project containing the plugin configuration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve historical export records for app metrics associated with a specific plugin configuration within a project. Use this to access previously generated metric exports and their metadata."""

    # Construct request model with validation
    try:
        _request = _models.AppMetricsHistoricalExportsRetrieveRequest(
            path=_models.AppMetricsHistoricalExportsRetrieveRequestPath(plugin_config_id=plugin_config_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_app_metrics_historical_exports: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/app_metrics/{plugin_config_id}/historical_exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/app_metrics/{plugin_config_id}/historical_exports"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_app_metrics_historical_exports")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_app_metrics_historical_exports", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_app_metrics_historical_exports",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: app_metrics
@mcp.tool()
async def get_historical_export(
    id_: str = Field(..., alias="id", description="The unique identifier of the historical export record to retrieve."),
    plugin_config_id: int = Field(..., description="The plugin configuration ID associated with the app metrics for which the export was generated."),
    project_id: str = Field(..., description="The unique identifier of the project containing the app metrics. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific historical export record for app metrics within a project. This operation fetches details about a previously generated metrics export by its unique identifier."""

    # Construct request model with validation
    try:
        _request = _models.AppMetricsHistoricalExportsRetrieve2Request(
            path=_models.AppMetricsHistoricalExportsRetrieve2RequestPath(id_=id_, plugin_config_id=plugin_config_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_historical_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/app_metrics/{plugin_config_id}/historical_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/app_metrics/{plugin_config_id}/historical_exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_historical_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_historical_export", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_historical_export",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def list_batch_exports_project(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of batch exports to return in a single page of results. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of batch exports for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsList2Request(
            path=_models.BatchExportsList2RequestPath(project_id=project_id),
            query=_models.BatchExportsList2RequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_batch_exports_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_batch_exports_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_batch_exports_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_batch_exports_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def create_batch_export_project(
    project_id: str = Field(..., description="The unique identifier of the project where the batch export will be created. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for this batch export job to help identify it in your project."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, BigQuery, Redshift, Databricks, Postgres), HTTP endpoints, or internal Workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data model to export: events (raw event data), persons (user profiles), or sessions (user session data). Defaults to events if not specified."),
    paused: bool | None = Field(None, description="Set to true to pause this batch export without deleting it. Paused exports will not trigger new runs until resumed."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="Optional HogQL query to filter or transform the data being exported. If provided, only data matching this query will be included in the export."),
    filters: Any | None = Field(None, description="Optional filters to narrow the dataset being exported. Structure and format depend on the selected model type."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs. Affects when hourly, daily, and weekly intervals are triggered."),
    offset_day: int | None = Field(None, description="Number of days (0-6) to offset the batch export schedule. Useful for staggering exports across a week.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Hour of the day (0-23) to offset the batch export schedule in 24-hour format. Useful for scheduling exports at specific times.", ge=0, le=23),
) -> dict[str, Any]:
    """Create a new batch export job to automatically export data (events, persons, or sessions) to a supported destination on a recurring schedule."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsCreate2Request(
            path=_models.BatchExportsCreate2RequestPath(project_id=project_id),
            body=_models.BatchExportsCreate2RequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsCreate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_batch_export_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_batch_export_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports
@mcp.tool()
async def list_backfills_for_batch_export(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains the backfills you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    ordering: str | None = Field(None, description="The field name to sort results by. Specify which backfill attribute should be used to order the returned list."),
) -> dict[str, Any]:
    """Retrieve all backfills associated with a specific batch export. Backfills allow you to reprocess historical data for a batch export job."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsBackfillsListRequest(
            path=_models.BatchExportsBackfillsListRequestPath(batch_export_id=batch_export_id, project_id=project_id),
            query=_models.BatchExportsBackfillsListRequestQuery(ordering=ordering)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_backfills_for_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_backfills_for_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_backfills_for_batch_export", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_backfills_for_batch_export",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports
@mcp.tool()
async def create_backfill_for_batch_export(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the BatchExport this backfill belongs to."),
    project_id: str = Field(..., description="The unique identifier of the project containing the BatchExport. Retrieve available project IDs from the /api/projects/ endpoint."),
    status: Literal["Cancelled", "Completed", "ContinuedAsNew", "Failed", "FailedRetryable", "Terminated", "TimedOut", "Running", "Starting"] = Field(..., description="The initial status of the backfill. Valid values are: Cancelled, Completed, ContinuedAsNew, Failed, FailedRetryable, Terminated, TimedOut, Running, or Starting."),
    team: int = Field(..., description="The numeric identifier of the team that owns this backfill."),
    batch_export: str = Field(..., description="The unique identifier (UUID) of the BatchExport this backfill belongs to. Must match the batch_export_id parameter."),
    start_at: str | None = Field(None, description="The start of the data interval to backfill, specified as an ISO 8601 datetime. If omitted, defaults to the beginning of available data."),
    end_at: str | None = Field(None, description="The end of the data interval to backfill, specified as an ISO 8601 datetime. If omitted, defaults to the current time."),
) -> dict[str, Any]:
    """Create a new backfill for a BatchExport to reprocess data over a specified time interval. Specify the data interval boundaries and desired backfill status."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsBackfillsCreateRequest(
            path=_models.BatchExportsBackfillsCreateRequestPath(batch_export_id=batch_export_id, project_id=project_id),
            body=_models.BatchExportsBackfillsCreateRequestBody(start_at=start_at, end_at=end_at, status=status, team=team, batch_export=batch_export)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_backfill_for_batch_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_backfill_for_batch_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_backfill_for_batch_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_backfill_for_batch_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports
@mcp.tool()
async def get_batch_export_backfill(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains this backfill."),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export backfill to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific backfill record for a batch export. Backfills allow you to re-export historical data for a given batch export configuration."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsBackfillsRetrieveRequest(
            path=_models.BatchExportsBackfillsRetrieveRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_backfill: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_backfill")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_backfill", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_backfill",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports
@mcp.tool()
async def cancel_batch_export_backfill(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains the backfill to cancel."),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export backfill to cancel."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export backfill."),
    status: Literal["Cancelled", "Completed", "ContinuedAsNew", "Failed", "FailedRetryable", "Terminated", "TimedOut", "Running", "Starting"] = Field(..., description="The current status of the backfill. Must be one of: Cancelled, Completed, ContinuedAsNew, Failed, FailedRetryable, Terminated, TimedOut, Running, or Starting."),
    team: int = Field(..., description="The numeric identifier of the team that owns this backfill."),
    batch_export: str = Field(..., description="The unique identifier (UUID) of the batch export this backfill belongs to."),
    start_at: str | None = Field(None, description="The start of the data interval for the backfill, specified as an ISO 8601 datetime. Optional; if provided, helps identify the specific backfill period."),
    end_at: str | None = Field(None, description="The end of the data interval for the backfill, specified as an ISO 8601 datetime. Optional; if provided, helps identify the specific backfill period."),
) -> dict[str, Any]:
    """Cancel an in-progress batch export backfill for a specific project. This operation terminates the backfill task and updates its status to Cancelled."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsBackfillsCancelCreateRequest(
            path=_models.BatchExportsBackfillsCancelCreateRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id),
            body=_models.BatchExportsBackfillsCancelCreateRequestBody(start_at=start_at, end_at=end_at, status=status, team=team, batch_export=batch_export)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_batch_export_backfill: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills/{id}/cancel", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/backfills/{id}/cancel"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_batch_export_backfill")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_batch_export_backfill", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_batch_export_backfill",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def list_batch_export_runs(
    batch_export_id: str = Field(..., description="The unique identifier (UUID format) of the batch export job whose runs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    ordering: str | None = Field(None, description="The field name to sort results by. Specify which attribute of the runs should be used for ordering the returned list."),
) -> dict[str, Any]:
    """Retrieve all runs associated with a specific batch export job. Use this to monitor the execution history and status of individual runs within a batch export."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunsListRequest(
            path=_models.BatchExportsRunsListRequestPath(batch_export_id=batch_export_id, project_id=project_id),
            query=_models.BatchExportsRunsListRequestQuery(ordering=ordering)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_batch_export_runs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/runs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_batch_export_runs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_batch_export_runs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_batch_export_runs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export_run(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains the run you want to retrieve."),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the specific batch export run to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve details of a specific batch export run within a project. Returns the status, configuration, and results of the identified batch export run."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunsRetrieveRequest(
            path=_models.BatchExportsRunsRetrieveRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_run", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_run",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def cancel_batch_export_run(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains the run to cancel."),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export run to cancel."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. Retrieve project IDs by calling the /api/projects/ endpoint."),
    status: Literal["Cancelled", "Completed", "ContinuedAsNew", "Failed", "FailedRetryable", "FailedBilling", "Terminated", "TimedOut", "Running", "Starting"] = Field(..., description="The current status of the batch export run. Valid statuses include: Cancelled, Completed, ContinuedAsNew, Failed, FailedRetryable, FailedBilling, Terminated, TimedOut, Running, or Starting."),
    data_interval_end: str = Field(..., description="The end timestamp of the data interval for this batch export run, specified in ISO 8601 date-time format."),
) -> dict[str, Any]:
    """Cancel an active or pending batch export run. The run must belong to the specified batch export and project."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunsCancelCreateRequest(
            path=_models.BatchExportsRunsCancelCreateRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id),
            body=_models.BatchExportsRunsCancelCreateRequestBody(status=status, data_interval_end=data_interval_end)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_batch_export_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/cancel", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/cancel"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_batch_export_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_batch_export_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_batch_export_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export_run_logs(
    batch_export_id: str = Field(..., description="The unique identifier (UUID) of the batch export that contains the run whose logs you want to retrieve."),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the specific batch export run whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the logs for a specific batch export run. Logs provide detailed information about the execution and processing of the batch export job."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunsLogsRetrieveRequest(
            path=_models.BatchExportsRunsLogsRetrieveRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_run_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_run_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_run_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_run_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def retry_batch_export_run(
    batch_export_id: str = Field(..., description="The UUID of the batch export that contains the run to retry."),
    id_: str = Field(..., alias="id", description="The UUID of the specific batch export run to retry."),
    project_id: str = Field(..., description="The UUID of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    status: Literal["Cancelled", "Completed", "ContinuedAsNew", "Failed", "FailedRetryable", "FailedBilling", "Terminated", "TimedOut", "Running", "Starting"] = Field(..., description="The current status of the batch export run. Must be one of: Cancelled, Completed, ContinuedAsNew, Failed, FailedRetryable, FailedBilling, Terminated, TimedOut, Running, or Starting."),
    data_interval_end: str = Field(..., description="The end timestamp of the data interval to reprocess, specified in ISO 8601 date-time format."),
) -> dict[str, Any]:
    """Retry a failed batch export run by reprocessing the same data interval. This operation uses the same mechanism as backfilling, allowing you to re-execute a single run that previously failed."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunsRetryCreateRequest(
            path=_models.BatchExportsRunsRetryCreateRequestPath(batch_export_id=batch_export_id, id_=id_, project_id=project_id),
            body=_models.BatchExportsRunsRetryCreateRequestBody(status=status, data_interval_end=data_interval_end)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for retry_batch_export_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/retry", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{batch_export_id}/runs/{id}/retry"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("retry_batch_export_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("retry_batch_export_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="retry_batch_export_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve details of a specific batch export job by its ID within a project. Use this to check the status, configuration, and results of a previously created batch export."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRetrieve2Request(
            path=_models.BatchExportsRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def update_batch_export_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this batch export. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for this batch export. Used for identification and display purposes."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service for exported data. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, Postgres, Redshift, BigQuery, Databricks), HTTP endpoints, and internal Workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which batch exports are triggered. Options range from every 5 minutes to weekly intervals."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data model to export: Events (raw event data), Persons (user/person profiles), or Sessions (aggregated session data)."),
    paused: bool | None = Field(None, description="Set to true to pause this batch export, preventing any scheduled runs from executing. Set to false to resume."),
    start_at: str | None = Field(None, description="ISO 8601 formatted datetime. Batch export runs will not be triggered before this time. Useful for scheduling exports to begin at a future date."),
    end_at: str | None = Field(None, description="ISO 8601 formatted datetime. Batch export runs will not be triggered after this time. Useful for setting an expiration date for the export."),
    hogql_query: str | None = Field(None, description="Optional HogQL query to filter or transform the data being exported. Allows advanced data selection beyond standard filters."),
    filters: Any | None = Field(None, description="Optional filter criteria to narrow the scope of exported data. Structure depends on the selected model type."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Optional timezone identifier (e.g., 'UTC', 'America/New_York') for scheduling batch exports. Affects when interval-based exports are triggered."),
    offset_day: int | None = Field(None, description="Optional day offset (0-6) for scheduling. Useful for adjusting when weekly exports run relative to the week start.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Optional hour offset (0-23) for scheduling. Useful for adjusting when daily or more frequent exports run relative to midnight.", ge=0, le=23),
) -> dict[str, Any]:
    """Update an existing batch export configuration for a project. Modify the export destination, schedule, filters, and other settings for continuous data exports."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsUpdate2Request(
            path=_models.BatchExportsUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.BatchExportsUpdate2RequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsUpdate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_batch_export_project", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_batch_export_project",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def update_batch_export_project_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. Retrieve project IDs from the /api/projects/ endpoint."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination type for the batch export. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, Postgres, Redshift, BigQuery, Databricks), HTTP endpoints, Workflows, or NoOp for testing."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The data model type to export: events, persons, or sessions."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] | None = Field(None, description="The export frequency: hourly, daily, weekly, or every 5/15 minutes."),
    paused: bool | None = Field(None, description="Set to true to pause the batch export, or false to resume it."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when batch export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="A HogQL query string to filter the data included in the batch export."),
    filters: Any | None = Field(None, description="Filter criteria to refine which records are included in the export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for scheduling batch export runs, affecting when intervals are calculated."),
    offset_day: int | None = Field(None, description="The day offset (0-6) for scheduling, where 0 is the current day and 6 is six days ahead.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour offset (0-23) for scheduling, representing the hour of the day in 24-hour format.", ge=0, le=23),
) -> dict[str, Any]:
    """Partially update a batch export configuration for a project. Modify export destination, schedule, filters, or other settings for an existing batch export job."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsPartialUpdate2Request(
            path=_models.BatchExportsPartialUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.BatchExportsPartialUpdate2RequestBody(model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsPartialUpdate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_batch_export_project_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_batch_export_project_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_batch_export_project_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_batch_export_project_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def delete_batch_export_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a batch export job from a project. This operation removes the batch export record and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsDestroy2Request(
            path=_models.BatchExportsDestroy2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_batch_export_project", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_batch_export_project",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def get_batch_export_logs_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export job whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the execution logs for a specific batch export job, including details about the export process and any errors or warnings that occurred."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsLogsRetrieve2Request(
            path=_models.BatchExportsLogsRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_batch_export_logs_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_batch_export_logs_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_batch_export_logs_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_batch_export_logs_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def pause_batch_export_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the BatchExport to pause."),
    project_id: str = Field(..., description="The unique identifier of the project containing the BatchExport. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the BatchExport to help identify it in your project."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported. Supported types include S3, Snowflake, Postgres, Redshift, BigQuery, Databricks, Azure Blob, Workflows, HTTP, or NoOp."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the BatchExport runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data model being exported: Events, Persons, or Sessions."),
    paused: bool | None = Field(None, description="Whether the BatchExport is currently paused. Set to true to pause the export."),
    start_at: str | None = Field(None, description="An optional ISO 8601 datetime after which no export runs will be triggered. Useful for setting a start window for exports."),
    end_at: str | None = Field(None, description="An optional ISO 8601 datetime after which no export runs will be triggered. Useful for setting an end window for exports."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter the data being exported."),
    filters: Any | None = Field(None, description="An optional set of filters to apply to the exported data."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="An optional timezone for scheduling the BatchExport runs."),
    offset_day: int | None = Field(None, description="An optional day offset (0-6) to adjust when the export runs within its interval cycle.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="An optional hour offset (0-23) to adjust the time of day when the export runs.", ge=0, le=23),
) -> dict[str, Any]:
    """Pause an active BatchExport to temporarily stop scheduled data exports without deleting the configuration. The export can be resumed later."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsPauseCreate2Request(
            path=_models.BatchExportsPauseCreate2RequestPath(id_=id_, project_id=project_id),
            body=_models.BatchExportsPauseCreate2RequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsPauseCreate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for pause_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}/pause", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}/pause"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("pause_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("pause_batch_export_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="pause_batch_export_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def run_batch_export_test_step(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to test."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the batch export configuration."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service for the batch export. Supported destinations include cloud storage (S3, Azure Blob), data warehouses (Snowflake, Postgres, Redshift, BigQuery, Databricks), HTTP endpoints, and internal Workflows."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the batch export runs: hourly, daily, weekly, or every 5/15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data model to export: Events (raw event data), Persons (user/person records), or Sessions (session-level aggregations)."),
    paused: bool | None = Field(None, description="Set to true to pause the batch export and prevent scheduled runs; false to keep it active."),
    start_at: str | None = Field(None, description="The earliest date and time (ISO 8601 format) when batch export runs can be triggered. Runs will not execute before this timestamp."),
    end_at: str | None = Field(None, description="The latest date and time (ISO 8601 format) when batch export runs can be triggered. Runs will not execute after this timestamp."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter or transform the data being exported."),
    filters: Any | None = Field(None, description="An optional set of filters to refine which records are included in the export."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone to use for scheduling batch export runs and interpreting time-based offsets."),
    offset_day: int | None = Field(None, description="The day offset (0-6) for scheduling, where 0 represents the current day and 6 represents six days ahead.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="The hour offset (0-23) for scheduling, representing the hour of the day in 24-hour format.", ge=0, le=23),
) -> dict[str, Any]:
    """Execute a test run of a batch export configuration to validate the export settings and destination connectivity before scheduling production runs."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsRunTestStepCreate2Request(
            path=_models.BatchExportsRunTestStepCreate2RequestPath(id_=id_, project_id=project_id),
            body=_models.BatchExportsRunTestStepCreate2RequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsRunTestStepCreate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for run_batch_export_test_step: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}/run_test_step", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}/run_test_step"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("run_batch_export_test_step")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("run_batch_export_test_step", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="run_batch_export_test_step",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: batch_exports, batch_exports
@mcp.tool()
async def unpause_batch_export_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the batch export to unpause."),
    project_id: str = Field(..., description="The unique identifier of the project containing the batch export. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the batch export."),
    type_: Literal["S3", "Snowflake", "Postgres", "Redshift", "BigQuery", "Databricks", "AzureBlob", "Workflows", "HTTP", "NoOp"] = Field(..., alias="type", description="The destination service where data will be exported: S3, Snowflake, Postgres, Redshift, BigQuery, Databricks, Azure Blob, Workflows, HTTP, or NoOp."),
    interval: Literal["hour", "day", "week", "every 5 minutes", "every 15 minutes"] = Field(..., description="The frequency at which the batch export runs: hourly, daily, weekly, every 5 minutes, or every 15 minutes."),
    model: Literal["events", "persons", "sessions"] | Literal[""] | None = Field(None, description="The type of data being exported: events, persons, or sessions."),
    paused: bool | None = Field(None, description="Whether the batch export is currently paused. Set to false to unpause."),
    start_at: str | None = Field(None, description="The earliest time (ISO 8601 format) at which batch export runs can be triggered. Runs scheduled before this time will not execute."),
    end_at: str | None = Field(None, description="The latest time (ISO 8601 format) at which batch export runs can be triggered. Runs scheduled after this time will not execute."),
    hogql_query: str | None = Field(None, description="An optional HogQL query to filter the data being exported."),
    filters: Any | None = Field(None, description="Optional filters to apply to the exported data."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="The timezone for scheduling batch export runs."),
    offset_day: int | None = Field(None, description="Day offset for scheduling (0-6), where 0 represents the current day.", ge=0, le=6),
    offset_hour: int | None = Field(None, description="Hour offset for scheduling (0-23), where 0 represents midnight.", ge=0, le=23),
) -> dict[str, Any]:
    """Resume a paused batch export job, allowing it to continue exporting data on its configured schedule."""

    # Construct request model with validation
    try:
        _request = _models.BatchExportsUnpauseCreate2Request(
            path=_models.BatchExportsUnpauseCreate2RequestPath(id_=id_, project_id=project_id),
            body=_models.BatchExportsUnpauseCreate2RequestBody(name=name, model=model, interval=interval, paused=paused, start_at=start_at, end_at=end_at, hogql_query=hogql_query, filters=filters, timezone_=timezone_, offset_day=offset_day, offset_hour=offset_hour,
                destination=_models.BatchExportsUnpauseCreate2RequestBodyDestination(type_=type_))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for unpause_batch_export_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/batch_exports/{id}/unpause", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/batch_exports/{id}/unpause"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("unpause_batch_export_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("unpause_batch_export_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="unpause_batch_export_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def list_cohorts(
    project_id: str = Field(..., description="The unique identifier of the project containing the cohorts. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of cohorts to return in a single response page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of cohorts for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.CohortsListRequest(
            path=_models.CohortsListRequestPath(project_id=project_id),
            query=_models.CohortsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_cohorts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_cohorts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_cohorts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_cohorts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def create_cohort(
    project_id: str = Field(..., description="The unique identifier of the project where the cohort will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    description: str | None = Field(None, description="Optional text description of the cohort's purpose or definition. Limited to 1000 characters.", max_length=1000),
    groups: Any | None = Field(None, description="Optional array defining logical groupings of filters that determine cohort membership. Groups are combined using OR logic, while filters within a group use AND logic."),
    filters: _models.CohortsCreateBodyFilters | None = Field(None, description="Optional filter criteria to define which users or persons are included in the cohort based on their attributes or behavior."),
    query: Any | None = Field(None, description="Optional query object for advanced cohort definition, typically used for complex analytical or behavioral segmentation."),
    cohort_type: Literal["static", "person_property", "behavioral", "realtime", "analytical"] | Literal[""] | None = Field(None, description="Optional classification of the cohort type based on filter complexity and update frequency. Choose from: static (fixed membership), person_property (based on user attributes), behavioral (based on user actions), realtime (continuously updated), or analytical (complex multi-criteria)."),
) -> dict[str, Any]:
    """Create a new cohort within a project to segment users based on filters, properties, or behavioral criteria. Cohorts can be static, dynamic, or real-time depending on the specified type."""

    # Construct request model with validation
    try:
        _request = _models.CohortsCreateRequest(
            path=_models.CohortsCreateRequestPath(project_id=project_id),
            body=_models.CohortsCreateRequestBody(description=description, groups=groups, filters=filters, query=query, cohort_type=cohort_type)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_cohort: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_cohort")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_cohort", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_cohort",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def get_cohort(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort to retrieve. This is a positive integer that uniquely identifies the cohort within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. You can obtain project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific cohort by its ID within a project. Returns detailed information about the cohort including its configuration and metadata."""

    # Construct request model with validation
    try:
        _request = _models.CohortsRetrieveRequest(
            path=_models.CohortsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_cohort: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_cohort")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_cohort", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_cohort",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def update_cohort(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the cohort to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the cohort's purpose or definition. Maximum 1000 characters.", max_length=1000),
    groups: Any | None = Field(None, description="Grouping configuration that defines how cohort members are organized or segmented."),
    filters: _models.CohortsUpdateBodyFilters | None = Field(None, description="Filter criteria that determine which users or entities are included in the cohort."),
    query: Any | None = Field(None, description="A query expression that defines the cohort membership logic."),
    cohort_type: Literal["static", "person_property", "behavioral", "realtime", "analytical"] | Literal[""] | None = Field(None, description="Classification of the cohort based on filter complexity: static (fixed membership), person_property (based on user attributes), behavioral (based on user actions), realtime (dynamically evaluated), or analytical (for analysis purposes)."),
) -> dict[str, Any]:
    """Update an existing cohort within a project, including its description, filters, grouping logic, and cohort type classification."""

    # Construct request model with validation
    try:
        _request = _models.CohortsUpdateRequest(
            path=_models.CohortsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.CohortsUpdateRequestBody(description=description, groups=groups, filters=filters, query=query, cohort_type=cohort_type)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_cohort: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_cohort")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_cohort", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_cohort",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def update_cohort_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the cohort's purpose or definition. Maximum 1000 characters.", max_length=1000),
    groups: Any | None = Field(None, description="Grouping configuration that defines how cohort members are organized or segmented."),
    filters: _models.CohortsPartialUpdateBodyFilters | None = Field(None, description="Filter criteria that determine which users or entities are included in the cohort."),
    query: Any | None = Field(None, description="Query logic or expression that defines the cohort membership rules."),
    cohort_type: Literal["static", "person_property", "behavioral", "realtime", "analytical"] | Literal[""] | None = Field(None, description="Classification of the cohort based on filter complexity. Options include: static (fixed membership), person_property (based on user attributes), behavioral (based on user actions), realtime (dynamically updated), or analytical (for analysis purposes)."),
) -> dict[str, Any]:
    """Partially update an existing cohort within a project. Allows modification of cohort properties such as description, filters, groups, query logic, and cohort type classification."""

    # Construct request model with validation
    try:
        _request = _models.CohortsPartialUpdateRequest(
            path=_models.CohortsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.CohortsPartialUpdateRequestBody(description=description, groups=groups, filters=filters, query=query, cohort_type=cohort_type)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_cohort_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_cohort_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_cohort_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_cohort_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def get_cohort_activity(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort whose activity you want to retrieve. This must be a positive integer."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history and events for a specific cohort within a project. This provides insights into cohort-related actions and changes over time."""

    # Construct request model with validation
    try:
        _request = _models.CohortsActivityRetrieve2Request(
            path=_models.CohortsActivityRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_cohort_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_cohort_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_cohort_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_cohort_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def add_persons_to_static_cohort(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the cohort to which persons will be added."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    person_ids: list[str] | None = Field(None, description="An array of person UUIDs to add to the cohort. Each UUID must be a valid identifier for an existing person in the project."),
) -> dict[str, Any]:
    """Add one or more persons to an existing static cohort by their unique identifiers. This operation allows bulk addition of individuals to a cohort for segmentation and analysis purposes."""

    # Construct request model with validation
    try:
        _request = _models.CohortsAddPersonsToStaticCohortPartialUpdateRequest(
            path=_models.CohortsAddPersonsToStaticCohortPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.CohortsAddPersonsToStaticCohortPartialUpdateRequestBody(person_ids=person_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_persons_to_static_cohort: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}/add_persons_to_static_cohort", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}/add_persons_to_static_cohort"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_persons_to_static_cohort")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_persons_to_static_cohort", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_persons_to_static_cohort",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def get_cohort_calculation_history(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort whose calculation history you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the calculation history for a specific cohort, including details about past computations and their results."""

    # Construct request model with validation
    try:
        _request = _models.CohortsCalculationHistoryRetrieveRequest(
            path=_models.CohortsCalculationHistoryRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_cohort_calculation_history: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}/calculation_history", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}/calculation_history"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_cohort_calculation_history")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_cohort_calculation_history", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_cohort_calculation_history",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def list_cohort_persons(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort whose members you want to retrieve. This must be a valid integer ID for a cohort that exists within the specified project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all persons belonging to a specific cohort within a project. Returns a list of individuals who are members of the cohort."""

    # Construct request model with validation
    try:
        _request = _models.CohortsPersonsRetrieveRequest(
            path=_models.CohortsPersonsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_cohort_persons: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}/persons", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}/persons"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_cohort_persons")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_cohort_persons", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_cohort_persons",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def remove_person_from_static_cohort(
    id_: int = Field(..., alias="id", description="The unique identifier of the cohort to modify. This must be a positive integer that corresponds to an existing static cohort in the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the cohort. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Remove a person from a static cohort within a project. This operation modifies the cohort membership by excluding the specified individual."""

    # Construct request model with validation
    try:
        _request = _models.CohortsRemovePersonFromStaticCohortPartialUpdateRequest(
            path=_models.CohortsRemovePersonFromStaticCohortPartialUpdateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_person_from_static_cohort: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}/remove_person_from_static_cohort", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}/remove_person_from_static_cohort"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_person_from_static_cohort")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_person_from_static_cohort", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_person_from_static_cohort",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def list_cohorts_activity(project_id: str = Field(..., description="The unique identifier of the project containing the cohorts. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve activity data for cohorts within a specific project. This endpoint provides insights into cohort engagement and interactions."""

    # Construct request model with validation
    try:
        _request = _models.CohortsActivityRetrieveRequest(
            path=_models.CohortsActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_cohorts_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_cohorts_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_cohorts_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_cohorts_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def list_conversation_tickets(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of tickets to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of conversation tickets with associated person data for a specific project."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsListRequest(
            path=_models.ConversationsTicketsListRequestPath(project_id=project_id),
            query=_models.ConversationsTicketsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_conversation_tickets: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_conversation_tickets")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_conversation_tickets", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_conversation_tickets",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def create_conversation_ticket(
    project_id: str = Field(..., description="The unique identifier of the project where the ticket will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    status: Literal["new", "open", "pending", "on_hold", "resolved"] | None = Field(None, description="The initial status of the ticket. Must be one of: new, open, pending, on_hold, or resolved. Defaults to new if not specified."),
    priority: Literal["low", "medium", "high"] | Literal[""] | None = Field(None, description="The priority level assigned to the ticket for triage and routing purposes."),
    anonymous_traits: Any | None = Field(None, description="Anonymous user characteristics or traits associated with the ticket requester."),
    ai_resolved: bool | None = Field(None, description="Boolean flag indicating whether the ticket has been resolved by an AI system."),
    escalation_reason: str | None = Field(None, description="The reason or explanation for escalating the ticket if applicable."),
    sla_due_at: str | None = Field(None, description="The date and time when the ticket's service level agreement (SLA) is due. Must be provided in ISO 8601 date-time format."),
    tags: list[Any] | None = Field(None, description="An array of tags for categorizing and organizing the ticket. Tags help with filtering and searching related tickets."),
) -> dict[str, Any]:
    """Create a new support ticket within a project's conversation system. Allows you to initialize tickets with optional metadata including status, priority, resolution state, and SLA tracking."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsCreateRequest(
            path=_models.ConversationsTicketsCreateRequestPath(project_id=project_id),
            body=_models.ConversationsTicketsCreateRequestBody(status=status, priority=priority, anonymous_traits=anonymous_traits, ai_resolved=ai_resolved, escalation_reason=escalation_reason, sla_due_at=sla_due_at, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_conversation_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_conversation_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_conversation_ticket", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_conversation_ticket",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def get_ticket(
    id_: str = Field(..., alias="id", description="The unique identifier of the ticket, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the ticket. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a single ticket by ID and automatically mark it as read by the team."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsRetrieveRequest(
            path=_models.ConversationsTicketsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_ticket", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_ticket",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def update_ticket(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the ticket to update."),
    project_id: str = Field(..., description="The project ID containing this ticket. Retrieve available project IDs from the /api/projects/ endpoint."),
    status: Literal["new", "open", "pending", "on_hold", "resolved"] | None = Field(None, description="The ticket's current workflow status: new (unreviewed), open (in progress), pending (awaiting response), on_hold (blocked), or resolved (completed)."),
    priority: Literal["low", "medium", "high"] | Literal[""] | None = Field(None, description="The ticket's priority level for triage and response ordering."),
    anonymous_traits: Any | None = Field(None, description="Anonymous visitor attributes or traits associated with the ticket."),
    ai_resolved: bool | None = Field(None, description="Boolean flag indicating whether the ticket was resolved by an AI system."),
    escalation_reason: str | None = Field(None, description="Reason or context for escalating the ticket to higher-tier support."),
    sla_due_at: str | None = Field(None, description="The deadline for SLA compliance, specified as an ISO 8601 date-time string."),
    tags: list[Any] | None = Field(None, description="Array of tag strings for categorizing and organizing the ticket."),
) -> dict[str, Any]:
    """Update a support ticket's properties including status, priority, assignment, and SLA tracking. Supports marking tickets as AI-resolved and adding escalation context."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsUpdateRequest(
            path=_models.ConversationsTicketsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ConversationsTicketsUpdateRequestBody(status=status, priority=priority, anonymous_traits=anonymous_traits, ai_resolved=ai_resolved, escalation_reason=escalation_reason, sla_due_at=sla_due_at, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_ticket", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_ticket",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def update_conversation_ticket(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the ticket to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the ticket. Retrieve available project IDs from the /api/projects/ endpoint."),
    status: Literal["new", "open", "pending", "on_hold", "resolved"] | None = Field(None, description="The ticket's current workflow state: new (unreviewed), open (active), pending (awaiting response), on_hold (paused), or resolved (completed)."),
    priority: Literal["low", "medium", "high"] | Literal[""] | None = Field(None, description="The ticket's priority level for handling and response urgency."),
    anonymous_traits: Any | None = Field(None, description="Custom attributes or traits associated with anonymous users related to this ticket."),
    ai_resolved: bool | None = Field(None, description="Boolean flag indicating whether the ticket was resolved by an AI system rather than manual intervention."),
    escalation_reason: str | None = Field(None, description="Reason or context for escalating the ticket to higher-level support or review."),
    sla_due_at: str | None = Field(None, description="The deadline for resolving the ticket, specified in ISO 8601 date-time format."),
    tags: list[Any] | None = Field(None, description="Labels or tags for categorizing and organizing the ticket. Provide as an array of tag values."),
) -> dict[str, Any]:
    """Partially update a conversation ticket's properties such as status, priority, resolution state, and metadata. Use this to modify ticket details without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsPartialUpdateRequest(
            path=_models.ConversationsTicketsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ConversationsTicketsPartialUpdateRequestBody(status=status, priority=priority, anonymous_traits=anonymous_traits, ai_resolved=ai_resolved, escalation_reason=escalation_reason, sla_due_at=sla_due_at, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_conversation_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_conversation_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_conversation_ticket", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_conversation_ticket",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def delete_conversation_ticket(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the ticket to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the ticket. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a conversation ticket from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsDestroyRequest(
            path=_models.ConversationsTicketsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_conversation_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_conversation_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_conversation_ticket", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_conversation_ticket",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: conversations
@mcp.tool()
async def suggest_reply_for_ticket(
    id_: str = Field(..., alias="id", description="The unique identifier of the ticket as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the ticket. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Generate an AI-suggested reply for a specific conversation ticket. This operation uses the ticket context to produce a helpful response draft that can be reviewed before sending."""

    # Construct request model with validation
    try:
        _request = _models.ConversationsTicketsSuggestReplyCreateRequest(
            path=_models.ConversationsTicketsSuggestReplyCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for suggest_reply_for_ticket: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/conversations/tickets/{id}/suggest_reply", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/conversations/tickets/{id}/suggest_reply"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("suggest_reply_for_ticket")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("suggest_reply_for_ticket", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="suggest_reply_for_ticket",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboard_templates
@mcp.tool()
async def list_dashboard_templates(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if needed."),
    is_featured: bool | None = Field(None, description="Filter templates by their featured status. Omit this parameter to retrieve all templates regardless of featured status."),
    limit: int | None = Field(None, description="Maximum number of templates to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    ordering: Literal["-template_name", "template_name"] | None = Field(None, description="Sort templates alphabetically by name. Use 'template_name' for ascending (A–Z) or '-template_name' for descending (Z–A) order. This parameter is ignored when using search functionality, which maintains relevance-based ranking instead."),
) -> dict[str, Any]:
    """Retrieve a paginated list of dashboard templates for a specific project, with optional filtering by featured status and sorting capabilities."""

    # Construct request model with validation
    try:
        _request = _models.DashboardTemplatesListRequest(
            path=_models.DashboardTemplatesListRequestPath(project_id=project_id),
            query=_models.DashboardTemplatesListRequestQuery(is_featured=is_featured, limit=limit, offset=offset, ordering=ordering)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dashboard_templates: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboard_templates", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboard_templates"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dashboard_templates")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dashboard_templates", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dashboard_templates",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboard_templates
@mcp.tool()
async def get_dashboard_template(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dashboard template to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard template. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific dashboard template by its ID within a project. Returns the complete template configuration for the specified dashboard."""

    # Construct request model with validation
    try:
        _request = _models.DashboardTemplatesRetrieveRequest(
            path=_models.DashboardTemplatesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_dashboard_template: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboard_templates/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboard_templates/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_dashboard_template")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_dashboard_template", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_dashboard_template",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboard_templates
@mcp.tool()
async def update_dashboard_template(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dashboard template to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard template. Retrieve available project IDs from the /api/projects/ endpoint."),
    template_name: str | None = Field(None, description="The display name for the dashboard template. Maximum 400 characters.", max_length=400),
    dashboard_description: str | None = Field(None, description="A description of the dashboard template's purpose and contents. Maximum 400 characters.", max_length=400),
    dashboard_filters: Any | None = Field(None, description="Filter configurations applied to the dashboard. Defines which data is displayed and how it can be filtered by users."),
    tags: list[str] | None = Field(None, description="Labels or keywords for organizing and categorizing the dashboard template. Provided as an array of tag values."),
    tiles: Any | None = Field(None, description="The dashboard layout and visualization components. Defines the tiles, charts, and widgets displayed on the dashboard."),
    variables: Any | None = Field(None, description="Template variables that can be parameterized for dynamic dashboard behavior. Allows users to customize dashboard content at runtime."),
    image_url: str | None = Field(None, description="URL to a preview or thumbnail image representing the dashboard template. Maximum 8201 characters.", max_length=8201),
    availability_contexts: list[str] | None = Field(None, description="Contexts or environments where this template is available. Provided as an array of context identifiers."),
    is_featured: bool | None = Field(None, description="Whether this template is manually curated and highlighted in the UI. Set to true to feature the template prominently."),
) -> dict[str, Any]:
    """Update an existing dashboard template within a project. Modify the template's name, description, layout, filters, variables, and visibility settings."""

    # Construct request model with validation
    try:
        _request = _models.DashboardTemplatesUpdateRequest(
            path=_models.DashboardTemplatesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardTemplatesUpdateRequestBody(template_name=template_name, dashboard_description=dashboard_description, dashboard_filters=dashboard_filters, tags=tags, tiles=tiles, variables=variables, image_url=image_url, availability_contexts=availability_contexts, is_featured=is_featured)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dashboard_template: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboard_templates/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboard_templates/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dashboard_template")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dashboard_template", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dashboard_template",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboard_templates
@mcp.tool()
async def update_dashboard_template_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dashboard template to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard template. Retrieve available project IDs from the /api/projects/ endpoint."),
    template_name: str | None = Field(None, description="The display name for the dashboard template. Maximum 400 characters.", max_length=400),
    dashboard_description: str | None = Field(None, description="A description of the dashboard template's purpose and contents. Maximum 400 characters.", max_length=400),
    dashboard_filters: Any | None = Field(None, description="Filter configuration for the dashboard, defining which data is displayed and how it can be filtered by users."),
    tags: list[str] | None = Field(None, description="An array of tags for categorizing and organizing the dashboard template."),
    tiles: Any | None = Field(None, description="An array of dashboard tiles defining the layout and visualization components displayed in the template."),
    variables: Any | None = Field(None, description="An object defining template variables that can be customized when instantiating or using the dashboard."),
    image_url: str | None = Field(None, description="A URL pointing to a preview or thumbnail image of the dashboard template. Maximum 8201 characters.", max_length=8201),
    availability_contexts: list[str] | None = Field(None, description="An array of availability contexts specifying where and to whom this template is accessible."),
    is_featured: bool | None = Field(None, description="A boolean flag indicating whether this template is manually curated and should be highlighted in the UI."),
) -> dict[str, Any]:
    """Partially update a dashboard template within a project. Allows modification of template metadata, layout, filters, variables, and visibility settings."""

    # Construct request model with validation
    try:
        _request = _models.DashboardTemplatesPartialUpdateRequest(
            path=_models.DashboardTemplatesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardTemplatesPartialUpdateRequestBody(template_name=template_name, dashboard_description=dashboard_description, dashboard_filters=dashboard_filters, tags=tags, tiles=tiles, variables=variables, image_url=image_url, availability_contexts=availability_contexts, is_featured=is_featured)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dashboard_template_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboard_templates/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboard_templates/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dashboard_template_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dashboard_template_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dashboard_template_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboard_templates
@mcp.tool()
async def soft_delete_dashboard_template(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dashboard template to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard template. Retrieve available project IDs by calling /api/projects/."),
) -> dict[str, Any]:
    """Mark a dashboard template as deleted without permanently removing it. Use a PATCH request to set the 'deleted' field to true instead of this endpoint, as hard deletion is not permitted."""

    # Construct request model with validation
    try:
        _request = _models.DashboardTemplatesDestroyRequest(
            path=_models.DashboardTemplatesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for soft_delete_dashboard_template: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboard_templates/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboard_templates/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("soft_delete_dashboard_template")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("soft_delete_dashboard_template", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="soft_delete_dashboard_template",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def list_dashboards(
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboards. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of dashboard results to return in a single page. Omit or adjust to control result set size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of dashboards for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsListRequest(
            path=_models.DashboardsListRequestPath(project_id=project_id),
            query=_models.DashboardsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dashboards: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dashboards")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dashboards", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dashboards",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def create_dashboard(
    project_id: str = Field(..., description="The unique identifier of the project where the dashboard will be created. Retrieve project IDs by calling the /api/projects/ endpoint."),
    description: str | None = Field(None, description="Optional text description of the dashboard's purpose or contents."),
    pinned: bool | None = Field(None, description="Optional flag to pin the dashboard for quick access in the project's dashboard list."),
    breakdown_colors: Any | None = Field(None, description="Optional custom color mapping that defines how breakdown values are visually represented in charts and visualizations."),
    data_color_theme_id: int | None = Field(None, description="Optional identifier of a predefined color theme to apply to all chart visualizations on the dashboard."),
    tags: list[Any] | None = Field(None, description="Optional list of tags for organizing and categorizing the dashboard."),
    restriction_level: Literal[21, 37] | None = Field(None, description="Optional access restriction level for the dashboard, ranging from 0 (least restrictive) to 32767 (most restrictive).", ge=0, le=32767),
    quick_filter_ids: list[str] | None = Field(None, description="Optional list of quick filter IDs to attach to the dashboard, enabling users to quickly filter data across visualizations."),
) -> dict[str, Any]:
    """Create a new dashboard within a project to visualize and organize data with customizable layouts, filters, and styling."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCreateRequest(
            path=_models.DashboardsCreateRequestPath(project_id=project_id),
            body=_models.DashboardsCreateRequestBody(description=description, pinned=pinned, breakdown_colors=breakdown_colors, data_color_theme_id=data_color_theme_id, tags=tags, restriction_level=restriction_level, quick_filter_ids=quick_filter_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_dashboard", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_dashboard",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def list_dashboard_collaborators_project(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard whose collaborators you want to list."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of collaborators who have access to a specific dashboard within a project. This shows all users with whom the dashboard is shared and their access permissions."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsList2Request(
            path=_models.DashboardsCollaboratorsList2RequestPath(dashboard_id=dashboard_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dashboard_collaborators_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dashboard_collaborators_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dashboard_collaborators_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dashboard_collaborators_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def add_collaborator_to_dashboard_project(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard to which the collaborator will be added."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve project IDs by calling /api/projects/."),
    level: Literal[21, 37] = Field(..., description="The permission level to grant the collaborator, represented as an integer between 0 and 32767 (inclusive). Higher values typically indicate greater permissions.", ge=0, le=32767),
    user_uuid: str = Field(..., description="The UUID of the user to add as a collaborator to the dashboard."),
) -> dict[str, Any]:
    """Grant a user access to a specific dashboard within a project by adding them as a collaborator with a specified permission level."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsCreate2Request(
            path=_models.DashboardsCollaboratorsCreate2RequestPath(dashboard_id=dashboard_id, project_id=project_id),
            body=_models.DashboardsCollaboratorsCreate2RequestBody(level=level, user_uuid=user_uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_collaborator_to_dashboard_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_collaborator_to_dashboard_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_collaborator_to_dashboard_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_collaborator_to_dashboard_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards
@mcp.tool()
async def remove_dashboard_collaborator_project(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard from which to remove the collaborator."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve project IDs by calling /api/projects/."),
    user__uuid: str = Field(..., description="The UUID of the user to remove as a collaborator from the dashboard. Must be a valid UUID format."),
) -> dict[str, Any]:
    """Remove a collaborator from a dashboard by their user UUID. This revokes the specified user's access to the dashboard within the project."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCollaboratorsDestroy2Request(
            path=_models.DashboardsCollaboratorsDestroy2RequestPath(dashboard_id=dashboard_id, project_id=project_id, user__uuid=user__uuid)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_dashboard_collaborator_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators/{user__uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/collaborators/{user__uuid}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_dashboard_collaborator_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_dashboard_collaborator_project", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_dashboard_collaborator_project",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def list_dashboard_sharing(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard whose sharing settings you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of users and teams with whom a dashboard has been shared, including their access permissions and sharing details."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsSharingListRequest(
            path=_models.DashboardsSharingListRequestPath(dashboard_id=dashboard_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dashboard_sharing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/sharing", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/sharing"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dashboard_sharing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dashboard_sharing", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dashboard_sharing",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def create_dashboard_sharing_password(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard for which to create a sharing password."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Whether the password protection is enabled for this sharing configuration."),
    settings: Any | None = Field(None, description="Additional security or access control settings for the password-protected sharing configuration."),
    password_required: bool | None = Field(None, description="Whether a password is required for users to access the shared dashboard."),
) -> dict[str, Any]:
    """Create a new password for dashboard sharing configuration. This enables password-protected access to shared dashboards with customizable security settings."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsSharingPasswordsCreateRequest(
            path=_models.DashboardsSharingPasswordsCreateRequestPath(dashboard_id=dashboard_id, project_id=project_id),
            body=_models.DashboardsSharingPasswordsCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_dashboard_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/passwords", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/passwords"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_dashboard_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_dashboard_sharing_password", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_dashboard_sharing_password",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def delete_dashboard_sharing_password(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard from which the sharing password will be removed."),
    password_id: str = Field(..., description="The unique identifier of the password credential to delete from the dashboard's sharing configuration."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Remove a specific password from a dashboard's sharing configuration, preventing further access via that credential."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsSharingPasswordsDestroyRequest(
            path=_models.DashboardsSharingPasswordsDestroyRequestPath(dashboard_id=dashboard_id, password_id=password_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_dashboard_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/passwords/{password_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/passwords/{password_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_dashboard_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_dashboard_sharing_password", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_dashboard_sharing_password",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def refresh_dashboard_sharing(
    dashboard_id: int = Field(..., description="The unique identifier of the dashboard whose sharing settings you want to refresh."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve project IDs by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Whether sharing is enabled for this dashboard. When true, the dashboard can be shared with others according to the configured settings."),
    settings: Any | None = Field(None, description="Configuration object containing advanced sharing options and preferences for how the dashboard is shared."),
    password_required: bool | None = Field(None, description="Whether a password is required for accessing the shared dashboard. When enabled, viewers must provide a password to view the dashboard."),
) -> dict[str, Any]:
    """Refresh and update the sharing configuration for a dashboard, allowing you to enable/disable sharing, modify sharing settings, and manage password protection."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsSharingRefreshCreateRequest(
            path=_models.DashboardsSharingRefreshCreateRequestPath(dashboard_id=dashboard_id, project_id=project_id),
            body=_models.DashboardsSharingRefreshCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for refresh_dashboard_sharing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/refresh", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{dashboard_id}/sharing/refresh"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("refresh_dashboard_sharing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("refresh_dashboard_sharing", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="refresh_dashboard_sharing",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def get_dashboard(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the dashboard to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific dashboard by its ID within a project. Returns the dashboard configuration and metadata."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsRetrieveRequest(
            path=_models.DashboardsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_dashboard", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_dashboard",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def update_dashboard(
    id_: int = Field(..., alias="id", description="The unique identifier of the dashboard to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the dashboard's purpose or content."),
    pinned: bool | None = Field(None, description="Whether the dashboard is pinned for quick access."),
    breakdown_colors: Any | None = Field(None, description="A mapping of custom colors to breakdown values for chart visualizations."),
    data_color_theme_id: int | None = Field(None, description="The identifier of the color theme applied to all chart visualizations on this dashboard."),
    tags: list[Any] | None = Field(None, description="Labels or tags for organizing and categorizing the dashboard."),
    restriction_level: Literal[21, 37] | None = Field(None, description="Access restriction level for the dashboard, ranging from 0 (least restrictive) to 32767 (most restrictive).", ge=0, le=32767),
    quick_filter_ids: list[str] | None = Field(None, description="An ordered list of quick filter identifiers to associate with this dashboard for filtering data."),
) -> dict[str, Any]:
    """Update an existing dashboard's configuration, including its description, visual settings, filters, and access restrictions within a specific project."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsUpdateRequest(
            path=_models.DashboardsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardsUpdateRequestBody(description=description, pinned=pinned, breakdown_colors=breakdown_colors, data_color_theme_id=data_color_theme_id, tags=tags, restriction_level=restriction_level, quick_filter_ids=quick_filter_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dashboard", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dashboard",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def update_dashboard_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the dashboard to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the dashboard's purpose or contents."),
    pinned: bool | None = Field(None, description="Whether the dashboard is pinned for quick access in the project."),
    breakdown_colors: Any | None = Field(None, description="A mapping of breakdown dimension values to custom colors for consistent visualization across charts."),
    data_color_theme_id: int | None = Field(None, description="The ID of the color theme applied to all chart visualizations on this dashboard."),
    tags: list[Any] | None = Field(None, description="Labels or keywords for organizing and categorizing the dashboard. Provided as an array of tag values."),
    restriction_level: Literal[21, 37] | None = Field(None, description="Access control level for the dashboard, ranging from 0 (most restrictive) to 32767 (least restrictive).", ge=0, le=32767),
    quick_filter_ids: list[str] | None = Field(None, description="An ordered list of quick filter IDs to associate with this dashboard, enabling users to quickly filter dashboard data."),
) -> dict[str, Any]:
    """Partially update a dashboard's configuration, including its metadata, visual styling, filters, and access restrictions. Only the fields provided in the request will be modified."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsPartialUpdateRequest(
            path=_models.DashboardsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardsPartialUpdateRequestBody(description=description, pinned=pinned, breakdown_colors=breakdown_colors, data_color_theme_id=data_color_theme_id, tags=tags, restriction_level=restriction_level, quick_filter_ids=quick_filter_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dashboard_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dashboard_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dashboard_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dashboard_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def soft_delete_dashboard(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the dashboard to be soft deleted."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Soft-delete a dashboard by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsDestroyRequest(
            path=_models.DashboardsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for soft_delete_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("soft_delete_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("soft_delete_dashboard", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="soft_delete_dashboard",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def copy_tile_to_dashboard(
    id_: int = Field(..., alias="id", description="The unique identifier of the destination dashboard where the tile will be copied to."),
    project_id: str = Field(..., description="The project ID containing both the source and destination dashboards. Find your project ID by calling /api/projects/."),
    from_dashboard_id: int = Field(..., alias="fromDashboardId", description="The dashboard ID where the tile currently exists and will be copied from."),
    tile_id: int = Field(..., alias="tileId", description="The unique identifier of the specific tile to copy from the source dashboard."),
) -> dict[str, Any]:
    """Copy a dashboard tile (insight or text card) from one dashboard to another, creating a new tile row in the destination dashboard."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCopyTileCreateRequest(
            path=_models.DashboardsCopyTileCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardsCopyTileCreateRequestBody(from_dashboard_id=from_dashboard_id, tile_id=tile_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for copy_tile_to_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}/copy_tile", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}/copy_tile"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("copy_tile_to_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("copy_tile_to_dashboard", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="copy_tile_to_dashboard",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def generate_dashboard_metadata(
    id_: int = Field(..., alias="id", description="The unique identifier of the dashboard for which to generate metadata."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Generate AI-suggested name and description for a dashboard by analyzing its tiles. This operation uses the dashboard's content to automatically create meaningful metadata."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsGenerateMetadataCreateRequest(
            path=_models.DashboardsGenerateMetadataCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_dashboard_metadata: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}/generate_metadata", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}/generate_metadata"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_dashboard_metadata")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_dashboard_metadata", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_dashboard_metadata",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def reorder_dashboard_tiles(
    id_: int = Field(..., alias="id", description="The unique identifier of the dashboard to reorder. This is a positive integer that uniquely identifies the dashboard within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    tile_order: list[int] = Field(..., description="An ordered array of tile IDs representing the desired display sequence from top to bottom, left to right. Must contain at least one tile ID. The order in this array determines how tiles appear on the dashboard.", min_length=1),
) -> dict[str, Any]:
    """Reorder the tiles displayed on a dashboard by specifying their desired sequence. Tiles are arranged top to bottom, left to right based on the provided order."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsReorderTilesCreateRequest(
            path=_models.DashboardsReorderTilesCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.DashboardsReorderTilesCreateRequestBody(tile_order=tile_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reorder_dashboard_tiles: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/{id}/reorder_tiles", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/{id}/reorder_tiles"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reorder_dashboard_tiles")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reorder_dashboard_tiles", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reorder_dashboard_tiles",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, dashboards
@mcp.tool()
async def create_dashboard_from_template_json(
    project_id: str = Field(..., description="The unique identifier of the project where the dashboard will be created. Retrieve this ID from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="Optional text description of the dashboard's purpose or content."),
    pinned: bool | None = Field(None, description="Optional flag to pin the dashboard to the top of the dashboard list for quick access."),
    breakdown_colors: Any | None = Field(None, description="Optional custom color mapping that defines how breakdown values are displayed in chart visualizations."),
    data_color_theme_id: int | None = Field(None, description="Optional identifier for the color theme applied to all chart visualizations on the dashboard."),
    tags: list[Any] | None = Field(None, description="Optional list of tags for organizing and categorizing the dashboard."),
    restriction_level: Literal[21, 37] | None = Field(None, description="Optional access restriction level for the dashboard, ranging from 0 (least restrictive) to 32767 (most restrictive).", ge=0, le=32767),
    quick_filter_ids: list[str] | None = Field(None, description="Optional list of quick filter IDs to be associated with and available on this dashboard for data filtering."),
) -> dict[str, Any]:
    """Creates a new dashboard in a project by applying a JSON template configuration. Allows customization of appearance, filtering, and organization settings for the dashboard."""

    # Construct request model with validation
    try:
        _request = _models.DashboardsCreateFromTemplateJsonCreateRequest(
            path=_models.DashboardsCreateFromTemplateJsonCreateRequestPath(project_id=project_id),
            body=_models.DashboardsCreateFromTemplateJsonCreateRequestBody(description=description, pinned=pinned, breakdown_colors=breakdown_colors, data_color_theme_id=data_color_theme_id, tags=tags, restriction_level=restriction_level, quick_filter_ids=quick_filter_ids)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_dashboard_from_template_json: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dashboards/create_from_template_json", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dashboards/create_from_template_json"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_dashboard_from_template_json")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_dashboard_from_template_json", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_dashboard_from_template_json",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards, data_color_themes
@mcp.tool()
async def list_data_color_themes(
    project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of color themes to return in a single response page. Omit to use the default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of data color themes available for a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.DataColorThemesListRequest(
            path=_models.DataColorThemesListRequestPath(project_id=project_id),
            query=_models.DataColorThemesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_data_color_themes: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/data_color_themes", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/data_color_themes"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_data_color_themes")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_data_color_themes", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_data_color_themes",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards, data_color_themes
@mcp.tool()
async def create_data_color_theme(
    project_id: str = Field(..., description="The unique identifier of the project where the color theme will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the color theme. Must be 100 characters or fewer.", max_length=100),
    colors: Any | None = Field(None, description="An optional collection of colors that define the theme's palette. Specify the color values and their order as needed for your visualization requirements."),
) -> dict[str, Any]:
    """Create a new data color theme for a project. Color themes define custom color palettes used for data visualization within the project."""

    # Construct request model with validation
    try:
        _request = _models.DataColorThemesCreateRequest(
            path=_models.DataColorThemesCreateRequestPath(project_id=project_id),
            body=_models.DataColorThemesCreateRequestBody(name=name, colors=colors)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_data_color_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/data_color_themes", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/data_color_themes"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_data_color_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_data_color_theme", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_data_color_theme",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards, data_color_themes
@mcp.tool()
async def get_data_color_theme(
    id_: int = Field(..., alias="id", description="The unique identifier of the data color theme to retrieve. This is a positive integer that uniquely identifies the theme within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the data color theme. You can obtain project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific data color theme configuration for a project. Use this to fetch styling and color palette details for a particular theme within your project."""

    # Construct request model with validation
    try:
        _request = _models.DataColorThemesRetrieveRequest(
            path=_models.DataColorThemesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_data_color_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/data_color_themes/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/data_color_themes/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_data_color_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_data_color_theme", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_data_color_theme",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards, data_color_themes
@mcp.tool()
async def update_data_color_theme(
    id_: int = Field(..., alias="id", description="The unique identifier of the data color theme to update. Must be a positive integer."),
    project_id: str = Field(..., description="The unique identifier of the project containing the data color theme. Retrieve available project IDs from the /api/projects/ endpoint."),
    colors: Any | None = Field(None, description="Color configuration settings for the theme. Accepts a structured object defining color values and their assignments."),
) -> dict[str, Any]:
    """Partially update a data color theme configuration for a specific project. Allows modification of color settings without replacing the entire theme."""

    # Construct request model with validation
    try:
        _request = _models.DataColorThemesPartialUpdateRequest(
            path=_models.DataColorThemesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DataColorThemesPartialUpdateRequestBody(colors=colors)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_data_color_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/data_color_themes/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/data_color_themes/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_data_color_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_data_color_theme", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_data_color_theme",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: dashboards, data_color_themes
@mcp.tool()
async def delete_data_color_theme(
    id_: int = Field(..., alias="id", description="The unique identifier of the data color theme to delete. This is a positive integer value."),
    project_id: str = Field(..., description="The unique identifier of the project containing the data color theme. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a data color theme from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.DataColorThemesDestroyRequest(
            path=_models.DataColorThemesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_data_color_theme: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/data_color_themes/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/data_color_themes/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_data_color_theme")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_data_color_theme", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_data_color_theme",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, dataset_items
@mcp.tool()
async def list_dataset_items(
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset items. Obtain this ID by calling the /api/projects/ endpoint."),
    dataset: str | None = Field(None, description="Filter results to include only items from a specific dataset by providing its unique identifier."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of dataset items for a specific project, with optional filtering by dataset ID."""

    # Construct request model with validation
    try:
        _request = _models.DatasetItemsListRequest(
            path=_models.DatasetItemsListRequestPath(project_id=project_id),
            query=_models.DatasetItemsListRequestQuery(dataset=dataset, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dataset_items: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dataset_items", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dataset_items"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dataset_items")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dataset_items", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dataset_items",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, dataset_items
@mcp.tool()
async def create_dataset_item(
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    dataset: str = Field(..., description="The unique identifier (UUID format) of the dataset where the item will be created."),
    input_: Any | None = Field(None, alias="input", description="The input data for the dataset item. Structure and format depend on your dataset's schema."),
    output: Any | None = Field(None, description="The expected output or ground truth for the dataset item. Structure and format depend on your dataset's schema."),
    metadata: Any | None = Field(None, description="Additional metadata associated with the dataset item, such as tags, annotations, or custom properties."),
) -> dict[str, Any]:
    """Create a new item in a dataset within a project. Items can include input data, expected output, and associated metadata for training or evaluation purposes."""

    # Construct request model with validation
    try:
        _request = _models.DatasetItemsCreateRequest(
            path=_models.DatasetItemsCreateRequestPath(project_id=project_id),
            body=_models.DatasetItemsCreateRequestBody(dataset=dataset, input_=input_, output=output, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_dataset_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dataset_items", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dataset_items"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_dataset_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_dataset_item", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_dataset_item",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, dataset_items
@mcp.tool()
async def get_dataset_item(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset item to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset item. You can obtain the project ID by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific dataset item by its unique identifier within a project. Returns the full details of the dataset item including its metadata and content."""

    # Construct request model with validation
    try:
        _request = _models.DatasetItemsRetrieveRequest(
            path=_models.DatasetItemsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_dataset_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dataset_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dataset_items/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_dataset_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_dataset_item", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_dataset_item",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, dataset_items
@mcp.tool()
async def update_dataset_item(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset item to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset item. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    dataset: str = Field(..., description="The unique identifier (UUID) of the dataset that contains this item."),
    input_: Any | None = Field(None, alias="input", description="The input data for the dataset item. Structure and format depend on your dataset schema."),
    output: Any | None = Field(None, description="The output data for the dataset item. Structure and format depend on your dataset schema."),
    metadata: Any | None = Field(None, description="Additional metadata associated with the dataset item, such as tags, labels, or custom properties."),
) -> dict[str, Any]:
    """Update a specific dataset item within a project, allowing modification of its input data, output data, and associated metadata."""

    # Construct request model with validation
    try:
        _request = _models.DatasetItemsUpdateRequest(
            path=_models.DatasetItemsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DatasetItemsUpdateRequestBody(dataset=dataset, input_=input_, output=output, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dataset_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dataset_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dataset_items/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dataset_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dataset_item", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dataset_item",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, dataset_items
@mcp.tool()
async def update_dataset_item_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset item to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset item. Retrieve available project IDs from the /api/projects/ endpoint."),
    dataset: str | None = Field(None, description="Optional UUID of the dataset to associate with this item."),
    input_: Any | None = Field(None, alias="input", description="Optional input data for the dataset item. Structure depends on your dataset schema."),
    output: Any | None = Field(None, description="Optional output data for the dataset item. Structure depends on your dataset schema."),
    metadata: Any | None = Field(None, description="Optional metadata object for storing additional information about the dataset item."),
) -> dict[str, Any]:
    """Partially update a dataset item within a project. Allows modification of the dataset association, input data, output data, and metadata fields."""

    # Construct request model with validation
    try:
        _request = _models.DatasetItemsPartialUpdateRequest(
            path=_models.DatasetItemsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DatasetItemsPartialUpdateRequestBody(dataset=dataset, input_=input_, output=output, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dataset_item_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/dataset_items/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/dataset_items/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dataset_item_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dataset_item_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dataset_item_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, datasets
@mcp.tool()
async def list_datasets(
    project_id: str = Field(..., description="The unique identifier of the project containing the datasets. Obtain this ID by calling the projects list endpoint."),
    id__in: list[str] | None = Field(None, description="Filter results to include only datasets with IDs matching the provided list. Specify multiple IDs as a comma-separated array."),
    limit: int | None = Field(None, description="Maximum number of datasets to return in a single response page. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from. Use with limit to paginate through large result sets."),
    order_by: list[Literal["-created_at", "-updated_at", "created_at", "updated_at"]] | None = Field(None, description="Sort results by creation or modification timestamp. Prefix with hyphen (e.g., `-created_at`) for descending order. Specify as a comma-separated array for multiple sort criteria."),
) -> dict[str, Any]:
    """Retrieve a paginated list of datasets within a specific project. Use filtering, sorting, and pagination parameters to customize the results."""

    # Construct request model with validation
    try:
        _request = _models.DatasetsListRequest(
            path=_models.DatasetsListRequestPath(project_id=project_id),
            query=_models.DatasetsListRequestQuery(id__in=id__in, limit=limit, offset=offset, order_by=order_by)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_datasets: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/datasets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/datasets"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_query = _serialize_query(_http_query, {
        "id__in": ("form", False),
        "order_by": ("form", False),
    })
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_datasets")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_datasets", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_datasets",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, datasets
@mcp.tool()
async def create_dataset(
    project_id: str = Field(..., description="The unique identifier of the project where the dataset will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the dataset. Must be 400 characters or fewer.", max_length=400),
    description: str | None = Field(None, description="An optional description providing additional context or details about the dataset's purpose and contents."),
    metadata: Any | None = Field(None, description="Optional metadata object for storing additional structured information or custom attributes associated with the dataset."),
) -> dict[str, Any]:
    """Create a new dataset within a specified project. Datasets serve as containers for organizing and managing data collections."""

    # Construct request model with validation
    try:
        _request = _models.DatasetsCreateRequest(
            path=_models.DatasetsCreateRequestPath(project_id=project_id),
            body=_models.DatasetsCreateRequestBody(name=name, description=description, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_dataset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/datasets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/datasets"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_dataset")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_dataset", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_dataset",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, datasets
@mcp.tool()
async def get_dataset(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific dataset by its ID within a project. Returns the dataset's metadata and configuration details."""

    # Construct request model with validation
    try:
        _request = _models.DatasetsRetrieveRequest(
            path=_models.DatasetsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_dataset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/datasets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/datasets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_dataset")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_dataset", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_dataset",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, datasets
@mcp.tool()
async def update_dataset(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the dataset. Must not exceed 400 characters.", max_length=400),
    description: str | None = Field(None, description="An optional description providing additional context or details about the dataset."),
    metadata: Any | None = Field(None, description="Optional custom metadata object for storing additional structured information about the dataset."),
) -> dict[str, Any]:
    """Update an existing dataset within a project, including its name, description, and custom metadata."""

    # Construct request model with validation
    try:
        _request = _models.DatasetsUpdateRequest(
            path=_models.DatasetsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DatasetsUpdateRequestBody(name=name, description=description, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dataset: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/datasets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/datasets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dataset")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dataset", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dataset",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: llm_analytics, datasets
@mcp.tool()
async def update_dataset_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the dataset to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the dataset. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the dataset. Optional field that can be updated independently of other dataset properties."),
    metadata: Any | None = Field(None, description="Custom metadata associated with the dataset. Structure and content are flexible and depend on your use case. Optional field that can be updated independently of other dataset properties."),
) -> dict[str, Any]:
    """Partially update a dataset within a project by modifying its description and/or metadata. Only the fields provided in the request will be updated."""

    # Construct request model with validation
    try:
        _request = _models.DatasetsPartialUpdateRequest(
            path=_models.DatasetsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DatasetsPartialUpdateRequestBody(description=description, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_dataset_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/datasets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/datasets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_dataset_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_dataset_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_dataset_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def list_early_access_features(
    project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of the response. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination alongside the limit parameter."),
) -> dict[str, Any]:
    """Retrieve a paginated list of early access features available for a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeatureListRequest(
            path=_models.EarlyAccessFeatureListRequestPath(project_id=project_id),
            query=_models.EarlyAccessFeatureListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_early_access_features: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_early_access_features")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_early_access_features", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_early_access_features",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def create_early_access_feature(
    project_id: str = Field(..., description="The unique identifier of the project where the early access feature will be created. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name of the early access feature, up to 200 characters. This is shown to users in the opt-in interface.", max_length=200),
    stage: Literal["draft", "concept", "alpha", "beta", "general-availability", "archived"] = Field(..., description="The lifecycle stage of the feature, which determines availability to opted-in users. Choose from: draft (development), concept (planning), alpha (early testing), beta (wider testing), general-availability (production release), or archived (deprecated). Only active stages (alpha, beta, general-availability) enable the feature flag for users who opt in."),
    description: str | None = Field(None, description="An optional longer description explaining what the feature does and its benefits, displayed to users in the opt-in UI."),
    documentation_url: str | None = Field(None, description="An optional URI pointing to external documentation for this feature, up to 800 characters. This link is displayed to users in the opt-in UI.", max_length=800),
    payload: Any | None = Field(None, description="Optional arbitrary JSON metadata to associate with this feature for custom tracking, configuration, or integration purposes."),
) -> dict[str, Any]:
    """Create a new early access feature for a project to manage feature rollout across different lifecycle stages. The feature can be toggled through opt-in UI and progressed from draft through general availability."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeatureCreateRequest(
            path=_models.EarlyAccessFeatureCreateRequestPath(project_id=project_id),
            body=_models.EarlyAccessFeatureCreateRequestBody(name=name, description=description, stage=stage, documentation_url=documentation_url, payload=payload)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_early_access_feature: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_early_access_feature")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_early_access_feature", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_early_access_feature",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def get_early_access_feature(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the early access feature to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the early access feature. You can obtain project IDs by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve details of a specific early access feature within a project. Use this to fetch configuration, status, and metadata for a feature identified by its UUID."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeatureRetrieveRequest(
            path=_models.EarlyAccessFeatureRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_early_access_feature: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_early_access_feature")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_early_access_feature", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_early_access_feature",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def update_early_access_feature(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the early access feature to update."),
    project_id: str = Field(..., description="The ID of the project containing this early access feature. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the early access feature, up to 200 characters.", max_length=200),
    stage: Literal["draft", "concept", "alpha", "beta", "general-availability", "archived"] = Field(..., description="The lifecycle stage of the feature. Valid stages are: draft, concept, alpha, beta, general-availability, or archived. Moving to an active stage (alpha, beta, or general-availability) enables the feature flag for users who have opted in."),
    description: str | None = Field(None, description="A longer description explaining what this early access feature does, displayed to users in the opt-in UI."),
    documentation_url: str | None = Field(None, description="A URL pointing to external documentation for this feature, shown to users in the opt-in UI. Must be a valid URI, up to 800 characters.", max_length=800),
) -> dict[str, Any]:
    """Update an early access feature's metadata, including its name, description, lifecycle stage, and documentation URL. Transitioning to an active stage (alpha, beta, or general-availability) enables the feature flag for opted-in users."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeatureUpdateRequest(
            path=_models.EarlyAccessFeatureUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EarlyAccessFeatureUpdateRequestBody(name=name, description=description, stage=stage, documentation_url=documentation_url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_early_access_feature: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_early_access_feature")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_early_access_feature", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_early_access_feature",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def update_early_access_feature_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the early access feature to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this feature. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A detailed description of the feature's functionality, displayed to users in the opt-in interface."),
    stage: Literal["draft", "concept", "alpha", "beta", "general-availability", "archived"] | None = Field(None, description="The feature's lifecycle stage, which controls availability to opted-in users. Valid stages are: draft (work in progress), concept (early planning), alpha (initial testing), beta (broader testing), general-availability (production ready), or archived (deprecated). Only active stages (alpha, beta, general-availability) enable the feature flag for users."),
    documentation_url: str | None = Field(None, description="A URI pointing to external documentation for this feature, displayed in the opt-in UI. Must be a valid URL with a maximum length of 800 characters.", max_length=800),
) -> dict[str, Any]:
    """Partially update an early access feature's metadata, including its description, lifecycle stage, and documentation URL. Use this to manage feature visibility and progression through development stages."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeaturePartialUpdateRequest(
            path=_models.EarlyAccessFeaturePartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EarlyAccessFeaturePartialUpdateRequestBody(description=description, stage=stage, documentation_url=documentation_url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_early_access_feature_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_early_access_feature_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_early_access_feature_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_early_access_feature_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: early_access_features, early_access_feature
@mcp.tool()
async def delete_early_access_feature(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the early access feature to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the early access feature. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an early access feature from a project. This operation removes the feature and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.EarlyAccessFeatureDestroyRequest(
            path=_models.EarlyAccessFeatureDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_early_access_feature: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/early_access_feature/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/early_access_feature/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_early_access_feature")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_early_access_feature", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_early_access_feature",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def list_elements(
    project_id: str = Field(..., description="The unique identifier of the project containing the elements. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of elements to return in a single response page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of elements within a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.ElementsListRequest(
            path=_models.ElementsListRequestPath(project_id=project_id),
            query=_models.ElementsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_elements: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_elements")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_elements", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_elements",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def create_element(
    project_id: str = Field(..., description="The unique identifier of the project where the element will be created. Retrieve available project IDs by calling the projects list endpoint."),
    text: str | None = Field(None, description="The text content of the element, up to 10,000 characters.", max_length=10000),
    tag_name: str | None = Field(None, description="The HTML tag name of the element (e.g., 'div', 'button', 'span'), up to 1,000 characters.", max_length=1000),
    attr_class: list[str] | None = Field(None, description="An array of CSS class names to assign to the element. Order may be significant for styling precedence."),
    href: str | None = Field(None, description="The href attribute value for link elements, up to 10,000 characters.", max_length=10000),
    attr_id: str | None = Field(None, description="The id attribute value for the element, up to 10,000 characters. Must be unique within the project.", max_length=10000),
    nth_child: int | None = Field(None, description="The position of the element among its siblings (nth-child selector). Use negative values or zero for special positioning logic.", ge=-2147483648, le=2147483647),
    nth_of_type: int | None = Field(None, description="The position of the element among siblings of the same type (nth-of-type selector). Use negative values or zero for special positioning logic.", ge=-2147483648, le=2147483647),
    attributes: Any | None = Field(None, description="A flexible object containing additional HTML attributes and their values for the element."),
    order: int | None = Field(None, description="The display order or z-index priority of the element within the project's element hierarchy.", ge=-2147483648, le=2147483647),
) -> dict[str, Any]:
    """Create a new DOM element within a project. This operation allows you to define page elements with CSS selectors, attributes, and structural positioning for web automation or testing purposes."""

    # Construct request model with validation
    try:
        _request = _models.ElementsCreateRequest(
            path=_models.ElementsCreateRequestPath(project_id=project_id),
            body=_models.ElementsCreateRequestBody(text=text, tag_name=tag_name, attr_class=attr_class, href=href, attr_id=attr_id, nth_child=nth_child, nth_of_type=nth_of_type, attributes=attributes, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_element: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_element")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_element", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_element",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def get_element(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the element to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the element. You can obtain project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific element from a project by its unique identifier. Use this to fetch detailed information about an individual element within your project."""

    # Construct request model with validation
    try:
        _request = _models.ElementsRetrieveRequest(
            path=_models.ElementsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_element: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_element")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_element", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_element",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def update_element(
    id_: int = Field(..., alias="id", description="The unique identifier of the element to update."),
    project_id: str = Field(..., description="The project ID containing the element. Retrieve available project IDs from the /api/projects/ endpoint."),
    text: str | None = Field(None, description="The text content of the element. Maximum 10,000 characters.", max_length=10000),
    tag_name: str | None = Field(None, description="The HTML tag name of the element (e.g., 'div', 'span', 'button'). Maximum 1,000 characters.", max_length=1000),
    attr_class: list[str] | None = Field(None, description="An array of CSS class names to apply to the element. Order may be significant for styling precedence."),
    href: str | None = Field(None, description="The href attribute value for link elements. Maximum 10,000 characters.", max_length=10000),
    attr_id: str | None = Field(None, description="The id attribute value for the element. Maximum 10,000 characters.", max_length=10000),
    nth_child: int | None = Field(None, description="The nth-child position of the element among its siblings. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
    nth_of_type: int | None = Field(None, description="The nth-of-type position of the element among siblings of the same tag type. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
    attributes: Any | None = Field(None, description="Additional custom HTML attributes as key-value pairs. Format and structure depend on the attribute types being set."),
    order: int | None = Field(None, description="The display order or z-index of the element. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
) -> dict[str, Any]:
    """Update an element within a project, modifying its HTML properties, attributes, text content, and positioning. Use this to change element metadata, styling classes, links, identifiers, and DOM hierarchy information."""

    # Construct request model with validation
    try:
        _request = _models.ElementsUpdateRequest(
            path=_models.ElementsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ElementsUpdateRequestBody(text=text, tag_name=tag_name, attr_class=attr_class, href=href, attr_id=attr_id, nth_child=nth_child, nth_of_type=nth_of_type, attributes=attributes, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_element: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_element")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_element", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_element",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def update_element_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the element to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the element. Retrieve available project IDs from the /api/projects/ endpoint."),
    text: str | None = Field(None, description="The text content of the element, up to 10,000 characters.", max_length=10000),
    tag_name: str | None = Field(None, description="The HTML tag name for this element, up to 1,000 characters (e.g., 'div', 'span', 'p').", max_length=1000),
    attr_class: list[str] | None = Field(None, description="An array of CSS class names to assign to the element."),
    href: str | None = Field(None, description="The href URL for link elements, up to 10,000 characters.", max_length=10000),
    attr_id: str | None = Field(None, description="The HTML id attribute value for the element, up to 10,000 characters.", max_length=10000),
    nth_child: int | None = Field(None, description="The nth-child position of the element among its siblings (integer value).", ge=-2147483648, le=2147483647),
    nth_of_type: int | None = Field(None, description="The nth-of-type position of the element among siblings of the same tag type (integer value).", ge=-2147483648, le=2147483647),
    attributes: Any | None = Field(None, description="A collection of custom HTML attributes to apply to the element."),
    order: int | None = Field(None, description="The display order or sequence position of the element (integer value).", ge=-2147483648, le=2147483647),
) -> dict[str, Any]:
    """Partially update an element within a project by modifying its properties such as text content, HTML tag, attributes, and positioning. Only the fields provided will be updated."""

    # Construct request model with validation
    try:
        _request = _models.ElementsPartialUpdateRequest(
            path=_models.ElementsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ElementsPartialUpdateRequestBody(text=text, tag_name=tag_name, attr_class=attr_class, href=href, attr_id=attr_id, nth_child=nth_child, nth_of_type=nth_of_type, attributes=attributes, order=order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_element_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_element_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_element_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_element_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def delete_element(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the element to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the element. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific element from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.ElementsDestroyRequest(
            path=_models.ElementsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_element: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_element")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_element", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_element",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def get_element_stats(project_id: str = Field(..., description="The unique identifier of the project for which to retrieve element statistics. You can obtain this ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve statistics for elements in a project, supporting multiple element types including autocapture, rage clicks, and dead clicks. By default returns only autocapture elements unless specific types are requested via query parameters."""

    # Construct request model with validation
    try:
        _request = _models.ElementsStatsRetrieveRequest(
            path=_models.ElementsStatsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_element_stats: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/stats", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/stats"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_element_stats")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_element_stats", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_element_stats",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_analytics, elements
@mcp.tool()
async def list_element_values(project_id: str = Field(..., description="The unique identifier of the project. You can obtain the project ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve all element values for a specific project. This operation returns the values associated with elements within the project."""

    # Construct request model with validation
    try:
        _request = _models.ElementsValuesRetrieveRequest(
            path=_models.ElementsValuesRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_element_values: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/elements/values", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/elements/values"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_element_values")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_element_values", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_element_values",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def list_endpoints(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    is_active: bool | None = Field(None, description="Filter endpoints by their active status. When true, returns only active endpoints; when false, returns only inactive endpoints. Omit to retrieve all endpoints regardless of status."),
    limit: int | None = Field(None, description="Maximum number of endpoints to return in a single response. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve all endpoints configured for a specific project. Use pagination parameters to control result size and offset."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsListRequest(
            path=_models.EndpointsListRequestPath(project_id=project_id),
            query=_models.EndpointsListRequestQuery(is_active=is_active, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_endpoints: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_endpoints")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_endpoints", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_endpoints",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def create_endpoint(
    project_id: str = Field(..., description="The unique identifier of the project where the endpoint will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    query: Any | None = Field(None, description="The HogQL or insight query that this endpoint will execute. Modifying this query automatically creates a new endpoint version."),
    description: str | None = Field(None, description="A human-readable explanation of what data or results this endpoint returns."),
    cache_age_seconds: float | None = Field(None, description="How long (in seconds) to cache query results before refreshing. Must be between 60 and 86400 seconds."),
    is_active: bool | None = Field(None, description="Set to true to make this endpoint available for API execution, or false to disable it."),
    is_materialized: bool | None = Field(None, description="Set to true to persist query results to S3 for long-term storage and faster retrieval."),
    sync_frequency: str | None = Field(None, description="How frequently materialized results should refresh, such as 'every_hour' or 'every_day'. Only applies when is_materialized is true."),
    derived_from_insight: str | None = Field(None, description="The short ID of an existing insight to base this endpoint on. The endpoint will inherit the insight's query and configuration."),
    bucket_overrides: dict[str, Any] | None = Field(None, description="A mapping of column names to bucket keys for customizing how range variables are materialized. Each key is a column name and each value specifies the bucketing strategy for that column."),
) -> dict[str, Any]:
    """Create a new API endpoint that executes a HogQL or insight query. The endpoint can be configured with caching, materialization to S3, and refresh schedules."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsCreateRequest(
            path=_models.EndpointsCreateRequestPath(project_id=project_id),
            body=_models.EndpointsCreateRequestBody(query=query, description=description, cache_age_seconds=cache_age_seconds, is_active=is_active, is_materialized=is_materialized, sync_frequency=sync_frequency, derived_from_insight=derived_from_insight, bucket_overrides=bucket_overrides)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_endpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_endpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_endpoint", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_endpoint",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def get_endpoint(
    name: str = Field(..., description="The URL-safe identifier for the endpoint. This name must match the endpoint's registered name in the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. Retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific endpoint configuration by name, optionally specifying a particular version. Use the version query parameter to access historical versions of the endpoint."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsRetrieveRequest(
            path=_models.EndpointsRetrieveRequestPath(name=name, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_endpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_endpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_endpoint", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_endpoint",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def update_endpoint(
    name: str = Field(..., description="URL-safe name of the endpoint to update. Must contain only letters, numbers, hyphens, and underscores."),
    project_id: str = Field(..., description="Project ID containing the endpoint. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name2: str | None = Field(None, alias="name", description="New URL-safe name for the endpoint. Must start with a letter, contain only letters, numbers, hyphens, and underscores, and not exceed 128 characters."),
    query: Any | None = Field(None, description="HogQL or insight query that this endpoint executes. Modifying this query automatically creates a new endpoint version."),
    description: str | None = Field(None, description="Human-readable description explaining what data or results this endpoint returns."),
    cache_age_seconds: float | None = Field(None, description="Cache time-to-live in seconds. Must be between 60 and 86400 seconds (1 minute to 24 hours)."),
    is_active: bool | None = Field(None, description="Whether this endpoint is currently available for execution via the API."),
    is_materialized: bool | None = Field(None, description="Whether query results are persisted to S3 for materialized views."),
    sync_frequency: str | None = Field(None, description="Refresh frequency for materialized query results (e.g., 'every_hour', 'every_day')."),
    derived_from_insight: str | None = Field(None, description="Short ID of the insight from which this endpoint was derived, if applicable."),
    bucket_overrides: dict[str, Any] | None = Field(None, description="Column-specific bucket configuration overrides for range variable materialization. Keys are column names; values are bucket identifiers."),
) -> dict[str, Any]:
    """Update an existing API endpoint configuration. All parameters are optional; pass version in the request body or as a query parameter to target a specific version for updates."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsUpdateRequest(
            path=_models.EndpointsUpdateRequestPath(name=name, project_id=project_id),
            body=_models.EndpointsUpdateRequestBody(name2=name2, query=query, description=description, cache_age_seconds=cache_age_seconds, is_active=is_active, is_materialized=is_materialized, sync_frequency=sync_frequency, derived_from_insight=derived_from_insight, bucket_overrides=bucket_overrides)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_endpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_endpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_endpoint", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_endpoint",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def update_endpoint_partial(
    name: str = Field(..., description="The URL-safe identifier for the endpoint being updated. Must be a valid endpoint name within the specified project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Partially update an endpoint configuration within a project. Use this to modify specific endpoint properties without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsPartialUpdateRequest(
            path=_models.EndpointsPartialUpdateRequestPath(name=name, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_endpoint_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_endpoint_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_endpoint_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_endpoint_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def delete_endpoint(
    name: str = Field(..., description="The URL-safe name identifier for the endpoint to delete. Must match the exact name of an existing endpoint in the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete an endpoint and clean up its associated materialized query. This operation permanently removes the endpoint configuration from the project."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsDestroyRequest(
            path=_models.EndpointsDestroyRequestPath(name=name, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_endpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_endpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_endpoint", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_endpoint",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def preview_endpoint_materialization(
    name: str = Field(..., description="The URL-safe identifier for the endpoint whose materialization transform you want to preview."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. Retrieve available project IDs by calling the list projects endpoint."),
    bucket_overrides: dict[str, str] | None = Field(None, description="Optional per-column overrides for bucket functions, specified as a mapping of column names to bucket function names (e.g., {\"timestamp\": \"hour\"}). Overrides the default bucketing strategy for specified columns."),
) -> dict[str, Any]:
    """Preview how an endpoint's query will be transformed after materialization, including automatic range pair detection and bucket function application. Use this to validate materialization behavior before deployment."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsMaterializationPreviewCreateRequest(
            path=_models.EndpointsMaterializationPreviewCreateRequestPath(name=name, project_id=project_id),
            body=_models.EndpointsMaterializationPreviewCreateRequestBody(bucket_overrides=bucket_overrides)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for preview_endpoint_materialization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}/materialization_preview", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}/materialization_preview"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("preview_endpoint_materialization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("preview_endpoint_materialization", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="preview_endpoint_materialization",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def get_endpoint_materialization_status(
    name: str = Field(..., description="The URL-safe name identifier for the endpoint whose materialization status you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the materialization status for a specific endpoint within a project. Optionally specify a version number using the ?version=N query parameter to check status for a particular endpoint version."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsMaterializationStatusRetrieveRequest(
            path=_models.EndpointsMaterializationStatusRetrieveRequestPath(name=name, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_endpoint_materialization_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}/materialization_status", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}/materialization_status"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_endpoint_materialization_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_endpoint_materialization_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_endpoint_materialization_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def run_endpoint(
    name: str = Field(..., description="The URL-safe name identifier for the endpoint to execute."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. Retrieve available project IDs by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Execute an endpoint and retrieve its results, optionally materializing the output. Runs the latest version of the endpoint unless a specific version is provided."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsRunRetrieveRequest(
            path=_models.EndpointsRunRetrieveRequestPath(name=name, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for run_endpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}/run", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}/run"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("run_endpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("run_endpoint", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="run_endpoint",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def run_endpoint_materialized(
    name: str = Field(..., description="The URL-safe name identifier for the endpoint to execute."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. Retrieve available project IDs from the /api/projects/ endpoint."),
    breakdown: str | list[str | int] | int | None = Field(None, description="A single breakdown dimension for segmenting results. Use with materialized endpoints to break down data by a specific property."),
    breakdowns: list[_models.Breakdown] | None = Field(None, description="Array of breakdown dimensions for multi-level result segmentation. Maximum of 3 breakdowns allowed. Use with materialized endpoints to break down data by multiple properties simultaneously.", max_length=3),
    explicit_date: bool | None = Field(None, alias="explicitDate", description="When true, treats the date as an explicit value rather than a relative reference. Relevant for date-based filtering in non-materialized endpoints."),
    properties: list[_models.EventPropertyFilter | _models.PersonPropertyFilter | _models.ElementPropertyFilter | _models.EventMetadataPropertyFilter | _models.SessionPropertyFilter | _models.CohortPropertyFilter | _models.RecordingPropertyFilter | _models.LogEntryPropertyFilter | _models.GroupPropertyFilter | _models.FeaturePropertyFilter | _models.FlagPropertyFilter | _models.HogQlPropertyFilter | _models.EmptyPropertyFilter | _models.DataWarehousePropertyFilter | _models.DataWarehousePersonPropertyFilter | _models.ErrorTrackingIssueFilter | _models.LogPropertyFilter | _models.SpanPropertyFilter | _models.RevenueAnalyticsPropertyFilter] | None = Field(None, description="Array of property names to include in the results. Filters which fields are returned in the response."),
    limit: int | None = Field(None, description="Maximum number of results to return in the response. If omitted, all results are returned."),
    offset: int | None = Field(None, description="Number of results to skip before returning data. Must be used together with limit parameter. Only supported for HogQL endpoints."),
    refresh: Literal["cache", "force", "direct"] | None = Field(None, description="Cache strategy for the query execution. Use 'cache' to return cached results if available, 'force' to bypass cache and recompute, or 'direct' for direct execution. Defaults to 'cache'."),
    variables: dict[str, Any] | None = Field(None, description="Key-value pairs to parameterize the endpoint query. For HogQL endpoints, keys must match variable code_names in the query. For non-materialized insights, use built-in variables like date_from and date_to. For materialized insights, use breakdown property names as keys. Unknown variable names will cause a 400 error."),
) -> dict[str, Any]:
    """Execute an endpoint with optional materialization and filtering. Automatically runs the latest version unless a specific version is provided via variables."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsRunCreateRequest(
            path=_models.EndpointsRunCreateRequestPath(name=name, project_id=project_id),
            body=_models.EndpointsRunCreateRequestBody(limit=limit, offset=offset, refresh=refresh, variables=variables,
                filters_override=_models.EndpointsRunCreateRequestBodyFiltersOverride(explicit_date=explicit_date, properties=properties,
                    breakdown_filter=_models.EndpointsRunCreateRequestBodyFiltersOverrideBreakdownFilter(breakdown=breakdown, breakdowns=breakdowns) if any(v is not None for v in [breakdown, breakdowns]) else None) if any(v is not None for v in [breakdown, breakdowns, explicit_date, properties]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for run_endpoint_materialized: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}/run", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}/run"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("run_endpoint_materialized")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("run_endpoint_materialized", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="run_endpoint_materialized",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: endpoints, endpoints
@mcp.tool()
async def list_endpoint_versions(
    name: str = Field(..., description="The URL-safe identifier for the endpoint whose versions you want to list."),
    project_id: str = Field(..., description="The unique identifier of the project containing the endpoint. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    is_active: bool | None = Field(None, description="Filter results to show only active versions when set to true, or only inactive versions when set to false. Omit to retrieve all versions regardless of status."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, allowing you to paginate through large result sets. Use with limit to control pagination."),
) -> dict[str, Any]:
    """Retrieve all versions for a specific endpoint within a project. Use optional filters to narrow results by active status and pagination parameters."""

    # Construct request model with validation
    try:
        _request = _models.EndpointsVersionsListRequest(
            path=_models.EndpointsVersionsListRequestPath(name=name, project_id=project_id),
            query=_models.EndpointsVersionsListRequestQuery(is_active=is_active, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_endpoint_versions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/endpoints/{name}/versions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/endpoints/{name}/versions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_endpoint_versions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_endpoint_versions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_endpoint_versions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: environments
@mcp.tool()
async def create_default_evaluation_contexts(
    id_: int = Field(..., alias="id", description="Unique integer identifier for the environment (team). Required to specify which team's evaluation contexts to configure."),
    project_id: str = Field(..., description="Project ID string identifying the project being accessed. Retrieve from /api/projects/ if unknown."),
    access_control: bool | None = Field(None, description="Enable or disable access control for this environment."),
    app_urls: list[str | None] | None = Field(None, description="Array of application URLs associated with this environment."),
    slack_incoming_webhook: str | None = Field(None, description="Slack incoming webhook URL for notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Anonymize IP addresses in analytics and session recordings."),
    test_account_filters_default_checked: bool | None = Field(None, description="Pre-check the test account filters option by default in the UI."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Timezone for this environment. Accepts IANA timezone identifiers (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user display names in the UI."),
    autocapture_opt_out: bool | None = Field(None, description="Disable automatic event capture for this environment."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Enable automatic capture of JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Enable automatic capture of Web Vitals metrics (LCP, FID, CLS)."),
    capture_console_log_opt_in: bool | None = Field(None, description="Enable automatic capture of browser console logs."),
    capture_performance_opt_in: bool | None = Field(None, description="Enable automatic capture of performance metrics."),
    session_recording_opt_in: bool | None = Field(None, description="Enable session recording for this environment."),
    session_recording_sample_rate: str | None = Field(None, description="Session recording sample rate as a decimal between 0 and 1 (e.g., 0.1 for 10% of sessions). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30,000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When present, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="Session recording retention period. Options: 30 days, 90 days, 1 year, or 5 years."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Day of week to start analytics weeks (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="ID of the dashboard to display as the primary dashboard for this environment."),
    live_events_columns: list[str] | None = Field(None, description="Array of column configurations for the live events view."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain patterns to include in session recording."),
    cookieless_server_hash_mode: Literal[0, 1, 2] | None = Field(None, description="Cookieless server-side hash mode configuration.", ge=-32768, le=32767),
    human_friendly_comparison_periods: bool | None = Field(None, description="Use human-friendly date range labels (e.g., 'Last 7 days') instead of exact date ranges in comparisons."),
    surveys_opt_in: bool | None = Field(None, description="Enable surveys feature for this environment."),
    heatmaps_opt_in: bool | None = Field(None, description="Enable heatmaps feature for this environment."),
    flags_persistence_default: bool | None = Field(None, description="Persist feature flag values in local storage by default."),
    feature_flag_confirmation_enabled: bool | None = Field(None, description="Require confirmation before deploying feature flag changes."),
    feature_flag_confirmation_message: str | None = Field(None, description="Custom message to display when confirming feature flag changes."),
    default_evaluation_contexts_enabled: bool | None = Field(None, description="Automatically apply default evaluation contexts to newly created feature flags."),
    require_evaluation_contexts: bool | None = Field(None, description="Require at least one evaluation context tag when creating new feature flags."),
    capture_dead_clicks: bool | None = Field(None, description="Enable automatic capture of dead clicks (clicks on non-interactive elements)."),
    default_data_theme: int | None = Field(None, description="ID of the default data visualization theme for this environment.", ge=-2147483648, le=2147483647),
    base_currency: Literal["AED", "AFN", "ALL", "AMD", "ANG", "AOA", "ARS", "AUD", "AWG", "AZN", "BAM", "BBD", "BDT", "BGN", "BHD", "BIF", "BMD", "BND", "BOB", "BRL", "BSD", "BTC", "BTN", "BWP", "BYN", "BZD", "CAD", "CDF", "CHF", "CLP", "CNY", "COP", "CRC", "CVE", "CZK", "DJF", "DKK", "DOP", "DZD", "EGP", "ERN", "ETB", "EUR", "FJD", "GBP", "GEL", "GHS", "GIP", "GMD", "GNF", "GTQ", "GYD", "HKD", "HNL", "HRK", "HTG", "HUF", "IDR", "ILS", "INR", "IQD", "IRR", "ISK", "JMD", "JOD", "JPY", "KES", "KGS", "KHR", "KMF", "KRW", "KWD", "KYD", "KZT", "LAK", "LBP", "LKR", "LRD", "LTL", "LVL", "LSL", "LYD", "MAD", "MDL", "MGA", "MKD", "MMK", "MNT", "MOP", "MRU", "MTL", "MUR", "MVR", "MWK", "MXN", "MYR", "MZN", "NAD", "NGN", "NIO", "NOK", "NPR", "NZD", "OMR", "PAB", "PEN", "PGK", "PHP", "PKR", "PLN", "PYG", "QAR", "RON", "RSD", "RUB", "RWF", "SAR", "SBD", "SCR", "SDG", "SEK", "SGD", "SRD", "SSP", "STN", "SYP", "SZL", "THB", "TJS", "TMT", "TND", "TOP", "TRY", "TTD", "TWD", "TZS", "UAH", "UGX", "USD", "UYU", "UZS", "VES", "VND", "VUV", "WST", "XAF", "XCD", "XOF", "XPF", "YER", "ZAR", "ZMW"] | None = Field(None, description="Base currency for financial metrics and revenue tracking. Defaults to USD."),
    events: Any | None = Field(None, description="Event configuration for tracking and analytics."),
    goals: Any | None = Field(None, description="Goals configuration for conversion tracking."),
    filter_test_accounts: bool | None = Field(None, description="Enable filtering of test accounts from analytics by default."),
    sources_map: Any | None = Field(None, description="Mapping of event sources for attribution and tracking."),
    conversion_goals: Any | None = Field(None, description="Conversion goals configuration for revenue and funnel analysis."),
    attribution_window_days: int | None = Field(None, description="Attribution window in days for crediting conversions to campaigns. Range: 1 to 90 days.", ge=1, le=90),
    attribution_mode: Literal["first_touch", "last_touch"] | None = Field(None, description="Attribution model for multi-touch attribution. Options: first_touch (credit first interaction) or last_touch (credit last interaction)."),
    campaign_name_mappings: Any | None = Field(None, description="Custom mappings for campaign name parameters across different sources."),
    custom_source_mappings: Any | None = Field(None, description="Custom mappings for event sources and their properties."),
    campaign_field_preferences: Any | None = Field(None, description="User preferences for campaign field display and prioritization."),
    activity_event: Any | None = Field(None, description="Event configuration for tracking user activity and engagement."),
    signup_pageview_event: Any | None = Field(None, description="Event configuration for tracking signup pageview events."),
    signup_event: Any | None = Field(None, description="Event configuration for tracking signup completion events."),
    subscription_event: Any | None = Field(None, description="Event configuration for tracking subscription events."),
    payment_event: Any | None = Field(None, description="Event configuration for tracking payment and transaction events."),
    onboarding_tasks: Any | None = Field(None, description="Onboarding tasks configuration for new users in this environment."),
    experiment_recalculation_time: str | None = Field(None, description="Time of day in UTC (HH:MM format) when experiment metrics should be automatically recalculated. Uses default time if not specified."),
    default_experiment_confidence_level: str | None = Field(None, description="Default statistical confidence level for new experiments in this environment. Valid values: 0.90 (90%), 0.95 (95%), or 0.99 (99%).", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    default_experiment_stats_method: Literal["bayesian", "frequentist"] | Literal[""] | None = Field(None, description="Default statistical method for analyzing new experiments. Options: bayesian (Bayesian inference) or frequentist (frequentist hypothesis testing)."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model classification for UI optimization. Options: b2b (business-to-business), b2c (business-to-consumer), or other."),
    conversations_enabled: bool | None = Field(None, description="Enable conversations feature for team collaboration and feedback."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for conversations feature behavior and display."),
    proactive_tasks_enabled: bool | None = Field(None, description="Enable proactive task suggestions and recommendations for this environment."),
) -> dict[str, Any]:
    """Create default evaluation contexts for a team environment. These contexts are automatically applied to new feature flags and can be required when creating flags."""

    # Construct request model with validation
    try:
        _request = _models.DefaultEvaluationContextsCreateRequest(
            path=_models.DefaultEvaluationContextsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.DefaultEvaluationContextsCreateRequestBody(access_control=access_control, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, cookieless_server_hash_mode=cookieless_server_hash_mode, human_friendly_comparison_periods=human_friendly_comparison_periods, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, feature_flag_confirmation_enabled=feature_flag_confirmation_enabled, feature_flag_confirmation_message=feature_flag_confirmation_message, default_evaluation_contexts_enabled=default_evaluation_contexts_enabled, require_evaluation_contexts=require_evaluation_contexts, capture_dead_clicks=capture_dead_clicks, default_data_theme=default_data_theme, base_currency=base_currency, onboarding_tasks=onboarding_tasks, experiment_recalculation_time=experiment_recalculation_time, default_experiment_confidence_level=default_experiment_confidence_level, default_experiment_stats_method=default_experiment_stats_method, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled,
                revenue_analytics_config=_models.DefaultEvaluationContextsCreateRequestBodyRevenueAnalyticsConfig(events=events, goals=goals, filter_test_accounts=filter_test_accounts) if any(v is not None for v in [events, goals, filter_test_accounts]) else None,
                marketing_analytics_config=_models.DefaultEvaluationContextsCreateRequestBodyMarketingAnalyticsConfig(sources_map=sources_map, conversion_goals=conversion_goals, attribution_window_days=attribution_window_days, attribution_mode=attribution_mode, campaign_name_mappings=campaign_name_mappings, custom_source_mappings=custom_source_mappings, campaign_field_preferences=campaign_field_preferences) if any(v is not None for v in [sources_map, conversion_goals, attribution_window_days, attribution_mode, campaign_name_mappings, custom_source_mappings, campaign_field_preferences]) else None,
                customer_analytics_config=_models.DefaultEvaluationContextsCreateRequestBodyCustomerAnalyticsConfig(activity_event=activity_event, signup_pageview_event=signup_pageview_event, signup_event=signup_event, subscription_event=subscription_event, payment_event=payment_event) if any(v is not None for v in [activity_event, signup_pageview_event, signup_event, subscription_event, payment_event]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_default_evaluation_contexts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/environments/{id}/default_evaluation_contexts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/environments/{id}/default_evaluation_contexts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_default_evaluation_contexts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_default_evaluation_contexts", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_default_evaluation_contexts",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: environments
@mcp.tool()
async def delete_default_evaluation_context(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the environment (team) whose default evaluation context should be deleted."),
    project_id: str = Field(..., description="The unique identifier of the project containing the environment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Remove a default evaluation context from an environment within a project. This operation deletes the specified evaluation context configuration for a team."""

    # Construct request model with validation
    try:
        _request = _models.DefaultEvaluationContextsDestroyRequest(
            path=_models.DefaultEvaluationContextsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_default_evaluation_context: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/environments/{id}/default_evaluation_contexts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/environments/{id}/default_evaluation_contexts"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_default_evaluation_context")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_default_evaluation_context", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_default_evaluation_context",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: environments
@mcp.tool()
async def get_environment_default_release_conditions(
    id_: int = Field(..., alias="id", description="The unique identifier of the environment (team). This is a positive integer that uniquely identifies the environment within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the environment. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve the default release conditions configured for new feature flags in a specific environment. These conditions define how new flags are rolled out to users by default."""

    # Construct request model with validation
    try:
        _request = _models.DefaultReleaseConditionsRetrieveRequest(
            path=_models.DefaultReleaseConditionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_environment_default_release_conditions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/environments/{id}/default_release_conditions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/environments/{id}/default_release_conditions"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_environment_default_release_conditions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_environment_default_release_conditions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_environment_default_release_conditions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: environments
@mcp.tool()
async def update_environment_default_release_conditions(
    id_: int = Field(..., alias="id", description="Unique integer identifier for the environment (team). Required to specify which environment to update."),
    project_id: str = Field(..., description="Project ID string identifying the project. Obtain from /api/projects/ endpoint. Required to access the correct project context."),
    access_control: bool | None = Field(None, description="Enable or disable access control features for this environment."),
    app_urls: list[str | None] | None = Field(None, description="Array of application URLs where feature flags and tracking should be active."),
    slack_incoming_webhook: str | None = Field(None, description="Slack webhook URL for notifications. Maximum 500 characters.", max_length=500),
    anonymize_ips: bool | None = Field(None, description="Automatically anonymize user IP addresses in analytics and session recordings."),
    test_account_filters_default_checked: bool | None = Field(None, description="Pre-check the test account filter checkbox by default when creating new feature flags."),
    timezone_: Literal["Africa/Abidjan", "Africa/Accra", "Africa/Addis_Ababa", "Africa/Algiers", "Africa/Asmara", "Africa/Asmera", "Africa/Bamako", "Africa/Bangui", "Africa/Banjul", "Africa/Bissau", "Africa/Blantyre", "Africa/Brazzaville", "Africa/Bujumbura", "Africa/Cairo", "Africa/Casablanca", "Africa/Ceuta", "Africa/Conakry", "Africa/Dakar", "Africa/Dar_es_Salaam", "Africa/Djibouti", "Africa/Douala", "Africa/El_Aaiun", "Africa/Freetown", "Africa/Gaborone", "Africa/Harare", "Africa/Johannesburg", "Africa/Juba", "Africa/Kampala", "Africa/Khartoum", "Africa/Kigali", "Africa/Kinshasa", "Africa/Lagos", "Africa/Libreville", "Africa/Lome", "Africa/Luanda", "Africa/Lubumbashi", "Africa/Lusaka", "Africa/Malabo", "Africa/Maputo", "Africa/Maseru", "Africa/Mbabane", "Africa/Mogadishu", "Africa/Monrovia", "Africa/Nairobi", "Africa/Ndjamena", "Africa/Niamey", "Africa/Nouakchott", "Africa/Ouagadougou", "Africa/Porto-Novo", "Africa/Sao_Tome", "Africa/Timbuktu", "Africa/Tripoli", "Africa/Tunis", "Africa/Windhoek", "America/Adak", "America/Anchorage", "America/Anguilla", "America/Antigua", "America/Araguaina", "America/Argentina/Buenos_Aires", "America/Argentina/Catamarca", "America/Argentina/ComodRivadavia", "America/Argentina/Cordoba", "America/Argentina/Jujuy", "America/Argentina/La_Rioja", "America/Argentina/Mendoza", "America/Argentina/Rio_Gallegos", "America/Argentina/Salta", "America/Argentina/San_Juan", "America/Argentina/San_Luis", "America/Argentina/Tucuman", "America/Argentina/Ushuaia", "America/Aruba", "America/Asuncion", "America/Atikokan", "America/Atka", "America/Bahia", "America/Bahia_Banderas", "America/Barbados", "America/Belem", "America/Belize", "America/Blanc-Sablon", "America/Boa_Vista", "America/Bogota", "America/Boise", "America/Buenos_Aires", "America/Cambridge_Bay", "America/Campo_Grande", "America/Cancun", "America/Caracas", "America/Catamarca", "America/Cayenne", "America/Cayman", "America/Chicago", "America/Chihuahua", "America/Ciudad_Juarez", "America/Coral_Harbour", "America/Cordoba", "America/Costa_Rica", "America/Creston", "America/Cuiaba", "America/Curacao", "America/Danmarkshavn", "America/Dawson", "America/Dawson_Creek", "America/Denver", "America/Detroit", "America/Dominica", "America/Edmonton", "America/Eirunepe", "America/El_Salvador", "America/Ensenada", "America/Fort_Nelson", "America/Fort_Wayne", "America/Fortaleza", "America/Glace_Bay", "America/Godthab", "America/Goose_Bay", "America/Grand_Turk", "America/Grenada", "America/Guadeloupe", "America/Guatemala", "America/Guayaquil", "America/Guyana", "America/Halifax", "America/Havana", "America/Hermosillo", "America/Indiana/Indianapolis", "America/Indiana/Knox", "America/Indiana/Marengo", "America/Indiana/Petersburg", "America/Indiana/Tell_City", "America/Indiana/Vevay", "America/Indiana/Vincennes", "America/Indiana/Winamac", "America/Indianapolis", "America/Inuvik", "America/Iqaluit", "America/Jamaica", "America/Jujuy", "America/Juneau", "America/Kentucky/Louisville", "America/Kentucky/Monticello", "America/Knox_IN", "America/Kralendijk", "America/La_Paz", "America/Lima", "America/Los_Angeles", "America/Louisville", "America/Lower_Princes", "America/Maceio", "America/Managua", "America/Manaus", "America/Marigot", "America/Martinique", "America/Matamoros", "America/Mazatlan", "America/Mendoza", "America/Menominee", "America/Merida", "America/Metlakatla", "America/Mexico_City", "America/Miquelon", "America/Moncton", "America/Monterrey", "America/Montevideo", "America/Montreal", "America/Montserrat", "America/Nassau", "America/New_York", "America/Nipigon", "America/Nome", "America/Noronha", "America/North_Dakota/Beulah", "America/North_Dakota/Center", "America/North_Dakota/New_Salem", "America/Nuuk", "America/Ojinaga", "America/Panama", "America/Pangnirtung", "America/Paramaribo", "America/Phoenix", "America/Port-au-Prince", "America/Port_of_Spain", "America/Porto_Acre", "America/Porto_Velho", "America/Puerto_Rico", "America/Punta_Arenas", "America/Rainy_River", "America/Rankin_Inlet", "America/Recife", "America/Regina", "America/Resolute", "America/Rio_Branco", "America/Rosario", "America/Santa_Isabel", "America/Santarem", "America/Santiago", "America/Santo_Domingo", "America/Sao_Paulo", "America/Scoresbysund", "America/Shiprock", "America/Sitka", "America/St_Barthelemy", "America/St_Johns", "America/St_Kitts", "America/St_Lucia", "America/St_Thomas", "America/St_Vincent", "America/Swift_Current", "America/Tegucigalpa", "America/Thule", "America/Thunder_Bay", "America/Tijuana", "America/Toronto", "America/Tortola", "America/Vancouver", "America/Virgin", "America/Whitehorse", "America/Winnipeg", "America/Yakutat", "America/Yellowknife", "Antarctica/Casey", "Antarctica/Davis", "Antarctica/DumontDUrville", "Antarctica/Macquarie", "Antarctica/Mawson", "Antarctica/McMurdo", "Antarctica/Palmer", "Antarctica/Rothera", "Antarctica/South_Pole", "Antarctica/Syowa", "Antarctica/Troll", "Antarctica/Vostok", "Arctic/Longyearbyen", "Asia/Aden", "Asia/Almaty", "Asia/Amman", "Asia/Anadyr", "Asia/Aqtau", "Asia/Aqtobe", "Asia/Ashgabat", "Asia/Ashkhabad", "Asia/Atyrau", "Asia/Baghdad", "Asia/Bahrain", "Asia/Baku", "Asia/Bangkok", "Asia/Barnaul", "Asia/Beirut", "Asia/Bishkek", "Asia/Brunei", "Asia/Calcutta", "Asia/Chita", "Asia/Choibalsan", "Asia/Chongqing", "Asia/Chungking", "Asia/Colombo", "Asia/Dacca", "Asia/Damascus", "Asia/Dhaka", "Asia/Dili", "Asia/Dubai", "Asia/Dushanbe", "Asia/Famagusta", "Asia/Gaza", "Asia/Harbin", "Asia/Hebron", "Asia/Ho_Chi_Minh", "Asia/Hong_Kong", "Asia/Hovd", "Asia/Irkutsk", "Asia/Istanbul", "Asia/Jakarta", "Asia/Jayapura", "Asia/Jerusalem", "Asia/Kabul", "Asia/Kamchatka", "Asia/Karachi", "Asia/Kashgar", "Asia/Kathmandu", "Asia/Katmandu", "Asia/Khandyga", "Asia/Kolkata", "Asia/Krasnoyarsk", "Asia/Kuala_Lumpur", "Asia/Kuching", "Asia/Kuwait", "Asia/Macao", "Asia/Macau", "Asia/Magadan", "Asia/Makassar", "Asia/Manila", "Asia/Muscat", "Asia/Nicosia", "Asia/Novokuznetsk", "Asia/Novosibirsk", "Asia/Omsk", "Asia/Oral", "Asia/Phnom_Penh", "Asia/Pontianak", "Asia/Pyongyang", "Asia/Qatar", "Asia/Qostanay", "Asia/Qyzylorda", "Asia/Rangoon", "Asia/Riyadh", "Asia/Saigon", "Asia/Sakhalin", "Asia/Samarkand", "Asia/Seoul", "Asia/Shanghai", "Asia/Singapore", "Asia/Srednekolymsk", "Asia/Taipei", "Asia/Tashkent", "Asia/Tbilisi", "Asia/Tehran", "Asia/Tel_Aviv", "Asia/Thimbu", "Asia/Thimphu", "Asia/Tokyo", "Asia/Tomsk", "Asia/Ujung_Pandang", "Asia/Ulaanbaatar", "Asia/Ulan_Bator", "Asia/Urumqi", "Asia/Ust-Nera", "Asia/Vientiane", "Asia/Vladivostok", "Asia/Yakutsk", "Asia/Yangon", "Asia/Yekaterinburg", "Asia/Yerevan", "Atlantic/Azores", "Atlantic/Bermuda", "Atlantic/Canary", "Atlantic/Cape_Verde", "Atlantic/Faeroe", "Atlantic/Faroe", "Atlantic/Jan_Mayen", "Atlantic/Madeira", "Atlantic/Reykjavik", "Atlantic/South_Georgia", "Atlantic/St_Helena", "Atlantic/Stanley", "Australia/ACT", "Australia/Adelaide", "Australia/Brisbane", "Australia/Broken_Hill", "Australia/Canberra", "Australia/Currie", "Australia/Darwin", "Australia/Eucla", "Australia/Hobart", "Australia/LHI", "Australia/Lindeman", "Australia/Lord_Howe", "Australia/Melbourne", "Australia/NSW", "Australia/North", "Australia/Perth", "Australia/Queensland", "Australia/South", "Australia/Sydney", "Australia/Tasmania", "Australia/Victoria", "Australia/West", "Australia/Yancowinna", "Brazil/Acre", "Brazil/DeNoronha", "Brazil/East", "Brazil/West", "CET", "CST6CDT", "Canada/Atlantic", "Canada/Central", "Canada/Eastern", "Canada/Mountain", "Canada/Newfoundland", "Canada/Pacific", "Canada/Saskatchewan", "Canada/Yukon", "Chile/Continental", "Chile/EasterIsland", "Cuba", "EET", "EST", "EST5EDT", "Egypt", "Eire", "Etc/GMT", "Etc/GMT+0", "Etc/GMT+1", "Etc/GMT+10", "Etc/GMT+11", "Etc/GMT+12", "Etc/GMT+2", "Etc/GMT+3", "Etc/GMT+4", "Etc/GMT+5", "Etc/GMT+6", "Etc/GMT+7", "Etc/GMT+8", "Etc/GMT+9", "Etc/GMT-0", "Etc/GMT-1", "Etc/GMT-10", "Etc/GMT-11", "Etc/GMT-12", "Etc/GMT-13", "Etc/GMT-14", "Etc/GMT-2", "Etc/GMT-3", "Etc/GMT-4", "Etc/GMT-5", "Etc/GMT-6", "Etc/GMT-7", "Etc/GMT-8", "Etc/GMT-9", "Etc/GMT0", "Etc/Greenwich", "Etc/UCT", "Etc/UTC", "Etc/Universal", "Etc/Zulu", "Europe/Amsterdam", "Europe/Andorra", "Europe/Astrakhan", "Europe/Athens", "Europe/Belfast", "Europe/Belgrade", "Europe/Berlin", "Europe/Bratislava", "Europe/Brussels", "Europe/Bucharest", "Europe/Budapest", "Europe/Busingen", "Europe/Chisinau", "Europe/Copenhagen", "Europe/Dublin", "Europe/Gibraltar", "Europe/Guernsey", "Europe/Helsinki", "Europe/Isle_of_Man", "Europe/Istanbul", "Europe/Jersey", "Europe/Kaliningrad", "Europe/Kiev", "Europe/Kirov", "Europe/Kyiv", "Europe/Lisbon", "Europe/Ljubljana", "Europe/London", "Europe/Luxembourg", "Europe/Madrid", "Europe/Malta", "Europe/Mariehamn", "Europe/Minsk", "Europe/Monaco", "Europe/Moscow", "Europe/Nicosia", "Europe/Oslo", "Europe/Paris", "Europe/Podgorica", "Europe/Prague", "Europe/Riga", "Europe/Rome", "Europe/Samara", "Europe/San_Marino", "Europe/Sarajevo", "Europe/Saratov", "Europe/Simferopol", "Europe/Skopje", "Europe/Sofia", "Europe/Stockholm", "Europe/Tallinn", "Europe/Tirane", "Europe/Tiraspol", "Europe/Ulyanovsk", "Europe/Uzhgorod", "Europe/Vaduz", "Europe/Vatican", "Europe/Vienna", "Europe/Vilnius", "Europe/Volgograd", "Europe/Warsaw", "Europe/Zagreb", "Europe/Zaporozhye", "Europe/Zurich", "GB", "GB-Eire", "GMT", "GMT+0", "GMT-0", "GMT0", "Greenwich", "HST", "Hongkong", "Iceland", "Indian/Antananarivo", "Indian/Chagos", "Indian/Christmas", "Indian/Cocos", "Indian/Comoro", "Indian/Kerguelen", "Indian/Mahe", "Indian/Maldives", "Indian/Mauritius", "Indian/Mayotte", "Indian/Reunion", "Iran", "Israel", "Jamaica", "Japan", "Kwajalein", "Libya", "MET", "MST", "MST7MDT", "Mexico/BajaNorte", "Mexico/BajaSur", "Mexico/General", "NZ", "NZ-CHAT", "Navajo", "PRC", "PST8PDT", "Pacific/Apia", "Pacific/Auckland", "Pacific/Bougainville", "Pacific/Chatham", "Pacific/Chuuk", "Pacific/Easter", "Pacific/Efate", "Pacific/Enderbury", "Pacific/Fakaofo", "Pacific/Fiji", "Pacific/Funafuti", "Pacific/Galapagos", "Pacific/Gambier", "Pacific/Guadalcanal", "Pacific/Guam", "Pacific/Honolulu", "Pacific/Johnston", "Pacific/Kanton", "Pacific/Kiritimati", "Pacific/Kosrae", "Pacific/Kwajalein", "Pacific/Majuro", "Pacific/Marquesas", "Pacific/Midway", "Pacific/Nauru", "Pacific/Niue", "Pacific/Norfolk", "Pacific/Noumea", "Pacific/Pago_Pago", "Pacific/Palau", "Pacific/Pitcairn", "Pacific/Pohnpei", "Pacific/Ponape", "Pacific/Port_Moresby", "Pacific/Rarotonga", "Pacific/Saipan", "Pacific/Samoa", "Pacific/Tahiti", "Pacific/Tarawa", "Pacific/Tongatapu", "Pacific/Truk", "Pacific/Wake", "Pacific/Wallis", "Pacific/Yap", "Poland", "Portugal", "ROC", "ROK", "Singapore", "Turkey", "UCT", "US/Alaska", "US/Aleutian", "US/Arizona", "US/Central", "US/East-Indiana", "US/Eastern", "US/Hawaii", "US/Indiana-Starke", "US/Michigan", "US/Mountain", "US/Pacific", "US/Samoa", "UTC", "Universal", "W-SU", "WET", "Zulu"] | None = Field(None, alias="timezone", description="Timezone for all time-based operations and reporting in this environment. Select from IANA timezone database (e.g., America/New_York, Europe/London, Asia/Tokyo)."),
    person_display_name_properties: list[str] | None = Field(None, description="Array of property names to use for displaying person/user identities throughout the interface."),
    autocapture_opt_out: bool | None = Field(None, description="Disable automatic event capture for user interactions by default."),
    autocapture_exceptions_opt_in: bool | None = Field(None, description="Enable automatic capture of JavaScript exceptions and errors."),
    autocapture_web_vitals_opt_in: bool | None = Field(None, description="Enable automatic capture of Core Web Vitals metrics (LCP, FID, CLS)."),
    capture_console_log_opt_in: bool | None = Field(None, description="Enable automatic capture of browser console logs (errors, warnings, info)."),
    capture_performance_opt_in: bool | None = Field(None, description="Enable automatic capture of performance metrics and timing data."),
    session_recording_opt_in: bool | None = Field(None, description="Enable session recording by default for new feature flags."),
    session_recording_sample_rate: str | None = Field(None, description="Sample rate for session recordings as a decimal between 0 and 1 (e.g., 0.1 for 10% sampling). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    session_recording_minimum_duration_milliseconds: int | None = Field(None, description="Minimum session duration in milliseconds before recording is captured. Range: 0 to 30,000 milliseconds.", ge=0, le=30000),
    session_recording_trigger_groups: Any | None = Field(None, description="V2 trigger groups configuration for session recording. When present, takes precedence over legacy trigger fields."),
    session_recording_retention_period: Literal["30d", "90d", "1y", "5y"] | None = Field(None, description="How long to retain session recordings: 30 days, 90 days, 1 year, or 5 years."),
    week_start_day: Literal[0, 1] | None = Field(None, description="Day of week when analytics weeks begin (0=Sunday through 6=Saturday).", ge=-32768, le=32767),
    primary_dashboard: int | None = Field(None, description="Primary dashboard ID to display by default in this environment."),
    live_events_columns: list[str] | None = Field(None, description="Array of column names to display in the live events feed."),
    recording_domains: list[str | None] | None = Field(None, description="Array of domain names where session recording is permitted."),
    cookieless_server_hash_mode: Literal[0, 1, 2] | None = Field(None, description="Hash mode for cookieless server-side tracking.", ge=-32768, le=32767),
    human_friendly_comparison_periods: bool | None = Field(None, description="Use human-friendly date range labels (e.g., 'Last 7 days') instead of exact date ranges in comparisons."),
    surveys_opt_in: bool | None = Field(None, description="Enable surveys and feedback collection features."),
    heatmaps_opt_in: bool | None = Field(None, description="Enable heatmap and click tracking features."),
    flags_persistence_default: bool | None = Field(None, description="Persist feature flag values across sessions by default for new flags."),
    feature_flag_confirmation_enabled: bool | None = Field(None, description="Require confirmation dialog when releasing feature flags to production."),
    feature_flag_confirmation_message: str | None = Field(None, description="Custom message to display in the feature flag release confirmation dialog."),
    default_evaluation_contexts_enabled: bool | None = Field(None, description="Automatically apply default evaluation contexts to new feature flags for better targeting."),
    require_evaluation_contexts: bool | None = Field(None, description="Require at least one evaluation context tag when creating new feature flags."),
    capture_dead_clicks: bool | None = Field(None, description="Enable automatic detection and tracking of dead clicks (clicks on non-interactive elements)."),
    default_data_theme: int | None = Field(None, description="Default data visualization theme ID for charts and reports.", ge=-2147483648, le=2147483647),
    base_currency: Literal["AED", "AFN", "ALL", "AMD", "ANG", "AOA", "ARS", "AUD", "AWG", "AZN", "BAM", "BBD", "BDT", "BGN", "BHD", "BIF", "BMD", "BND", "BOB", "BRL", "BSD", "BTC", "BTN", "BWP", "BYN", "BZD", "CAD", "CDF", "CHF", "CLP", "CNY", "COP", "CRC", "CVE", "CZK", "DJF", "DKK", "DOP", "DZD", "EGP", "ERN", "ETB", "EUR", "FJD", "GBP", "GEL", "GHS", "GIP", "GMD", "GNF", "GTQ", "GYD", "HKD", "HNL", "HRK", "HTG", "HUF", "IDR", "ILS", "INR", "IQD", "IRR", "ISK", "JMD", "JOD", "JPY", "KES", "KGS", "KHR", "KMF", "KRW", "KWD", "KYD", "KZT", "LAK", "LBP", "LKR", "LRD", "LTL", "LVL", "LSL", "LYD", "MAD", "MDL", "MGA", "MKD", "MMK", "MNT", "MOP", "MRU", "MTL", "MUR", "MVR", "MWK", "MXN", "MYR", "MZN", "NAD", "NGN", "NIO", "NOK", "NPR", "NZD", "OMR", "PAB", "PEN", "PGK", "PHP", "PKR", "PLN", "PYG", "QAR", "RON", "RSD", "RUB", "RWF", "SAR", "SBD", "SCR", "SDG", "SEK", "SGD", "SRD", "SSP", "STN", "SYP", "SZL", "THB", "TJS", "TMT", "TND", "TOP", "TRY", "TTD", "TWD", "TZS", "UAH", "UGX", "USD", "UYU", "UZS", "VES", "VND", "VUV", "WST", "XAF", "XCD", "XOF", "XPF", "YER", "ZAR", "ZMW"] | None = Field(None, description="Base currency for revenue and financial metrics. Defaults to USD."),
    events: Any | None = Field(None, description="Event tracking configuration for analytics."),
    goals: Any | None = Field(None, description="Goals and conversion tracking configuration."),
    filter_test_accounts: bool | None = Field(None, description="Automatically filter out test account traffic from analytics by default."),
    sources_map: Any | None = Field(None, description="Mapping of traffic sources to standardized source categories."),
    conversion_goals: Any | None = Field(None, description="Conversion goals configuration for funnel and attribution analysis."),
    attribution_window_days: int | None = Field(None, description="Attribution window in days for crediting conversions to campaigns. Range: 1 to 90 days.", ge=1, le=90),
    attribution_mode: Literal["first_touch", "last_touch"] | None = Field(None, description="Attribution model for multi-touch attribution: first_touch (credit first interaction) or last_touch (credit last interaction)."),
    campaign_name_mappings: Any | None = Field(None, description="Custom mappings for campaign name parameters across different traffic sources."),
    custom_source_mappings: Any | None = Field(None, description="Custom mappings for non-standard source and medium parameters."),
    campaign_field_preferences: Any | None = Field(None, description="User preferences for which campaign fields to display and prioritize."),
    activity_event: Any | None = Field(None, description="Event definition for tracking user activity and engagement."),
    signup_pageview_event: Any | None = Field(None, description="Event definition for tracking initial pageview during signup flow."),
    signup_event: Any | None = Field(None, description="Event definition for tracking user signup completion."),
    subscription_event: Any | None = Field(None, description="Event definition for tracking subscription or plan activation."),
    payment_event: Any | None = Field(None, description="Event definition for tracking payment transactions."),
    onboarding_tasks: Any | None = Field(None, description="Onboarding task configuration and progress tracking."),
    experiment_recalculation_time: str | None = Field(None, description="Time of day in UTC (HH:MM format) when experiment metrics should be automatically recalculated. If not set, uses the system default."),
    default_experiment_confidence_level: str | None = Field(None, description="Default statistical confidence level for new experiments: 0.90 (90%), 0.95 (95%), or 0.99 (99%). Supports up to 2 decimal places.", pattern="^-?\\d{0,1}(?:\\.\\d{0,2})?$"),
    default_experiment_stats_method: Literal["bayesian", "frequentist"] | Literal[""] | None = Field(None, description="Default statistical method for new experiments: bayesian (Bayesian inference) or frequentist (frequentist hypothesis testing)."),
    business_model: Literal["b2b", "b2c", "other"] | Literal[""] | None = Field(None, description="Business model classification: b2b (B2B SaaS), b2c (B2C consumer), or other. Used to optimize UI layout and defaults."),
    conversations_enabled: bool | None = Field(None, description="Enable in-app conversations and messaging features for user engagement."),
    conversations_settings: Any | None = Field(None, description="Configuration settings for in-app conversations and messaging behavior."),
    proactive_tasks_enabled: bool | None = Field(None, description="Enable proactive task suggestions and recommendations based on usage patterns."),
) -> dict[str, Any]:
    """Configure default release conditions and feature flag settings for new feature flags created in this environment. This includes release behavior, data collection preferences, experiment defaults, and analytics configuration."""

    # Construct request model with validation
    try:
        _request = _models.DefaultReleaseConditionsUpdateRequest(
            path=_models.DefaultReleaseConditionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.DefaultReleaseConditionsUpdateRequestBody(access_control=access_control, app_urls=app_urls, slack_incoming_webhook=slack_incoming_webhook, anonymize_ips=anonymize_ips, test_account_filters_default_checked=test_account_filters_default_checked, timezone_=timezone_, person_display_name_properties=person_display_name_properties, autocapture_opt_out=autocapture_opt_out, autocapture_exceptions_opt_in=autocapture_exceptions_opt_in, autocapture_web_vitals_opt_in=autocapture_web_vitals_opt_in, capture_console_log_opt_in=capture_console_log_opt_in, capture_performance_opt_in=capture_performance_opt_in, session_recording_opt_in=session_recording_opt_in, session_recording_sample_rate=session_recording_sample_rate, session_recording_minimum_duration_milliseconds=session_recording_minimum_duration_milliseconds, session_recording_trigger_groups=session_recording_trigger_groups, session_recording_retention_period=session_recording_retention_period, week_start_day=week_start_day, primary_dashboard=primary_dashboard, live_events_columns=live_events_columns, recording_domains=recording_domains, cookieless_server_hash_mode=cookieless_server_hash_mode, human_friendly_comparison_periods=human_friendly_comparison_periods, surveys_opt_in=surveys_opt_in, heatmaps_opt_in=heatmaps_opt_in, flags_persistence_default=flags_persistence_default, feature_flag_confirmation_enabled=feature_flag_confirmation_enabled, feature_flag_confirmation_message=feature_flag_confirmation_message, default_evaluation_contexts_enabled=default_evaluation_contexts_enabled, require_evaluation_contexts=require_evaluation_contexts, capture_dead_clicks=capture_dead_clicks, default_data_theme=default_data_theme, base_currency=base_currency, onboarding_tasks=onboarding_tasks, experiment_recalculation_time=experiment_recalculation_time, default_experiment_confidence_level=default_experiment_confidence_level, default_experiment_stats_method=default_experiment_stats_method, business_model=business_model, conversations_enabled=conversations_enabled, conversations_settings=conversations_settings, proactive_tasks_enabled=proactive_tasks_enabled,
                revenue_analytics_config=_models.DefaultReleaseConditionsUpdateRequestBodyRevenueAnalyticsConfig(events=events, goals=goals, filter_test_accounts=filter_test_accounts) if any(v is not None for v in [events, goals, filter_test_accounts]) else None,
                marketing_analytics_config=_models.DefaultReleaseConditionsUpdateRequestBodyMarketingAnalyticsConfig(sources_map=sources_map, conversion_goals=conversion_goals, attribution_window_days=attribution_window_days, attribution_mode=attribution_mode, campaign_name_mappings=campaign_name_mappings, custom_source_mappings=custom_source_mappings, campaign_field_preferences=campaign_field_preferences) if any(v is not None for v in [sources_map, conversion_goals, attribution_window_days, attribution_mode, campaign_name_mappings, custom_source_mappings, campaign_field_preferences]) else None,
                customer_analytics_config=_models.DefaultReleaseConditionsUpdateRequestBodyCustomerAnalyticsConfig(activity_event=activity_event, signup_pageview_event=signup_pageview_event, signup_event=signup_event, subscription_event=subscription_event, payment_event=payment_event) if any(v is not None for v in [activity_event, signup_pageview_event, signup_event, subscription_event, payment_event]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_environment_default_release_conditions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/environments/{id}/default_release_conditions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/environments/{id}/default_release_conditions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_environment_default_release_conditions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_environment_default_release_conditions", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_environment_default_release_conditions",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: environments
@mcp.tool()
async def get_environment_settings_at_timestamp(
    id_: int = Field(..., alias="id", description="The unique integer identifier for the environment (team) within the project."),
    project_id: str = Field(..., description="The project identifier that contains the target environment. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve team settings for a specific environment as they existed at a given point in time, with optional filtering by setting keys."""

    # Construct request model with validation
    try:
        _request = _models.SettingsAsOfRetrieveRequest(
            path=_models.SettingsAsOfRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_environment_settings_at_timestamp: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/environments/{id}/settings_as_of", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/environments/{id}/settings_as_of"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_environment_settings_at_timestamp")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_environment_settings_at_timestamp", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_environment_settings_at_timestamp",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_releases_project(
    project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of releases to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based starting position for results. Use with limit to paginate through releases."),
) -> dict[str, Any]:
    """Retrieve a paginated list of error tracking releases for a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesList2Request(
            path=_models.ErrorTrackingReleasesList2RequestPath(project_id=project_id),
            query=_models.ErrorTrackingReleasesList2RequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_releases_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_releases_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_releases_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_releases_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def create_error_tracking_release_project(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this by calling the /api/projects/ endpoint if you don't have it."),
    hash_id: str = Field(..., description="A unique hash identifier for the release, used to distinguish this release from others in the error tracking system."),
    version: str = Field(..., description="The version string or number for this release (e.g., semantic version format like '1.2.3' or any custom versioning scheme)."),
    project: str = Field(..., description="The project identifier or name associated with this release in the error tracking system."),
    metadata: Any | None = Field(None, description="Optional metadata object containing additional information about the release, such as build details, deployment info, or custom tags."),
) -> dict[str, Any]:
    """Create a new release entry in error tracking for a project. This registers a release version with associated metadata to track errors across different versions."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesCreate2Request(
            path=_models.ErrorTrackingReleasesCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingReleasesCreate2RequestBody(hash_id=hash_id, metadata=metadata, version=version, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_error_tracking_release_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_error_tracking_release_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_error_tracking_release_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_error_tracking_release_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_release_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve details for a specific error tracking release within a project. Returns metadata and information about the release for monitoring and analysis purposes."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesRetrieve2Request(
            path=_models.ErrorTrackingReleasesRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_release_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_release_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_release_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_release_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_release_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. Retrieve available project IDs from the /api/projects/ endpoint."),
    hash_id: str = Field(..., description="A hash identifier for the release, used to uniquely identify the release artifact or build."),
    version: str = Field(..., description="The version string for the release (e.g., semantic version format like '1.2.3')."),
    project: str = Field(..., description="The project identifier or slug associated with this error tracking release."),
    metadata: Any | None = Field(None, description="Optional metadata object to attach additional context or custom data to the release."),
) -> dict[str, Any]:
    """Update an error tracking release for a project with new version information, hash identifier, and optional metadata."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesUpdate2Request(
            path=_models.ErrorTrackingReleasesUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingReleasesUpdate2RequestBody(hash_id=hash_id, metadata=metadata, version=version, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_release_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_release_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_release_project", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_release_project",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_release_project_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. Find your project ID by calling the /api/projects/ endpoint."),
    hash_id: str | None = Field(None, description="An optional hash identifier for the release, typically used to track specific build or version hashes."),
    metadata: Any | None = Field(None, description="Optional metadata object to store additional information about the release, such as build details, environment variables, or custom tags."),
    project: str | None = Field(None, description="Optional project identifier to reassign the error tracking release to a different project."),
) -> dict[str, Any]:
    """Partially update an error tracking release for a project. Use this to modify specific fields like hash_id, metadata, or project association without replacing the entire release record."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesPartialUpdate2Request(
            path=_models.ErrorTrackingReleasesPartialUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingReleasesPartialUpdate2RequestBody(hash_id=hash_id, metadata=metadata, project=project)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_release_project_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_release_project_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_release_project_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_release_project_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def delete_error_tracking_release_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking release to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the error tracking release. You can retrieve project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking release from a project. This removes the release and all associated error tracking data."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesDestroy2Request(
            path=_models.ErrorTrackingReleasesDestroy2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_release_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_release_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_release_project", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_release_project",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_release_by_hash_project(
    hash_id: str = Field(..., description="The unique hash identifier of the release to retrieve. This identifier is used to look up the specific release record in the error tracking system."),
    project_id: str = Field(..., description="The unique identifier of the project containing the release. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific error tracking release using its hash identifier. This operation fetches release metadata and associated error tracking data for a given project."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingReleasesHashRetrieve2Request(
            path=_models.ErrorTrackingReleasesHashRetrieve2RequestPath(hash_id=hash_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_release_by_hash_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/releases/hash/{hash_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/releases/hash/{hash_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_release_by_hash_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_release_by_hash_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_release_by_hash_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def list_error_tracking_symbol_sets_project(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of symbol sets to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, allowing you to paginate through large result sets. Use with limit to control which page of results is returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of symbol sets for error tracking in a specific project. Symbol sets are used to map obfuscated or minified code back to original source for better error analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsList2Request(
            path=_models.ErrorTrackingSymbolSetsList2RequestPath(project_id=project_id),
            query=_models.ErrorTrackingSymbolSetsList2RequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_error_tracking_symbol_sets_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_error_tracking_symbol_sets_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_error_tracking_symbol_sets_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_error_tracking_symbol_sets_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def create_error_tracking_symbol_set_project(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier for the symbol set, typically used to associate the set with a specific build, version, or release of your application."),
) -> dict[str, Any]:
    """Create a new symbol set for error tracking in a project. Symbol sets are used to map obfuscated or minified code back to original source for better error analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsCreate2Request(
            path=_models.ErrorTrackingSymbolSetsCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsCreate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_error_tracking_symbol_set_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_error_tracking_symbol_set_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_error_tracking_symbol_set_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_error_tracking_symbol_set_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def get_error_tracking_symbol_set_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific error tracking symbol set by its unique identifier. Symbol sets contain debug information used to map obfuscated stack traces back to original source code."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsRetrieve2Request(
            path=_models.ErrorTrackingSymbolSetsRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_error_tracking_symbol_set_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_error_tracking_symbol_set_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_error_tracking_symbol_set_project", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_error_tracking_symbol_set_project",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_symbol_set_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier or version tag for the symbol set update, used to track or version the symbol set data."),
) -> dict[str, Any]:
    """Update an error tracking symbol set for a project. Symbol sets are used to map minified or obfuscated code back to original source for better error tracking and debugging."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsUpdate2Request(
            path=_models.ErrorTrackingSymbolSetsUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsUpdate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_symbol_set_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_symbol_set_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_symbol_set_project", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_symbol_set_project",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def update_error_tracking_symbol_set_project_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. Find your project ID by calling the /api/projects/ endpoint."),
    ref: str | None = Field(None, description="Optional reference parameter for tracking or filtering purposes."),
) -> dict[str, Any]:
    """Partially update an error tracking symbol set for a project. Use this to modify specific fields of an existing symbol set without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsPartialUpdate2Request(
            path=_models.ErrorTrackingSymbolSetsPartialUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsPartialUpdate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_error_tracking_symbol_set_project_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_error_tracking_symbol_set_project_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_error_tracking_symbol_set_project_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_error_tracking_symbol_set_project_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def delete_error_tracking_symbol_set_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an error tracking symbol set from a project. This removes the symbol set and all associated symbol data used for error tracking."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsDestroy2Request(
            path=_models.ErrorTrackingSymbolSetsDestroy2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_symbol_set_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_symbol_set_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_symbol_set_project", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_symbol_set_project",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def finish_symbol_set_upload_project(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the error tracking symbol set to finalize."),
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol set. Retrieve available project IDs from the /api/projects/ endpoint."),
    ref: str = Field(..., description="A reference identifier used to track or correlate this upload completion with the original upload session."),
) -> dict[str, Any]:
    """Completes the upload process for an error tracking symbol set, finalizing the symbols for use in error tracking and analysis."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsFinishUploadUpdate2Request(
            path=_models.ErrorTrackingSymbolSetsFinishUploadUpdate2RequestPath(id_=id_, project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsFinishUploadUpdate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for finish_symbol_set_upload_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/{id}/finish_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/{id}/finish_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("finish_symbol_set_upload_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("finish_symbol_set_upload_project", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="finish_symbol_set_upload_project",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def delete_error_tracking_symbol_sets(
    project_id: str = Field(..., description="The unique identifier of the project containing the symbol sets to delete. Retrieve this ID from the /api/projects/ endpoint."),
    ref: str = Field(..., description="Reference identifier specifying which symbol sets to delete in the bulk operation."),
) -> dict[str, Any]:
    """Bulk delete error tracking symbol sets for a specified project. This operation removes multiple symbol sets used for error tracking and source map resolution."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsBulkDeleteCreate2Request(
            path=_models.ErrorTrackingSymbolSetsBulkDeleteCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsBulkDeleteCreate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_error_tracking_symbol_sets: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/bulk_delete", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/bulk_delete"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_error_tracking_symbol_sets")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_error_tracking_symbol_sets", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_error_tracking_symbol_sets",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def finish_symbol_set_bulk_upload_project(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set upload is being finalized. Retrieve this ID from the /api/projects/ endpoint."),
    ref: str = Field(..., description="The reference identifier for the bulk upload session being completed. This reference was provided when the upload was initiated."),
) -> dict[str, Any]:
    """Completes a bulk symbol set upload for error tracking by finalizing the upload session identified by the provided reference."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsBulkFinishUploadCreate2Request(
            path=_models.ErrorTrackingSymbolSetsBulkFinishUploadCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsBulkFinishUploadCreate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for finish_symbol_set_bulk_upload_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/bulk_finish_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/bulk_finish_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("finish_symbol_set_bulk_upload_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("finish_symbol_set_bulk_upload_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="finish_symbol_set_bulk_upload_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def start_symbol_sets_bulk_upload_project(
    project_id: str = Field(..., description="The unique identifier of the project where symbol sets will be uploaded. Retrieve this ID from the /api/projects/ endpoint if needed."),
    ref: str = Field(..., description="A reference identifier or version tag for this bulk upload session, used to track and organize the uploaded symbol sets."),
) -> dict[str, Any]:
    """Initiate a bulk upload process for error tracking symbol sets in a project. This operation prepares the project to receive multiple symbol set files and returns upload credentials or session information."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsBulkStartUploadCreate2Request(
            path=_models.ErrorTrackingSymbolSetsBulkStartUploadCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsBulkStartUploadCreate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for start_symbol_sets_bulk_upload_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/bulk_start_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/bulk_start_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("start_symbol_sets_bulk_upload_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("start_symbol_sets_bulk_upload_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="start_symbol_sets_bulk_upload_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: error_tracking, error_tracking
@mcp.tool()
async def start_symbol_set_upload_project(
    project_id: str = Field(..., description="The unique identifier of the project where the symbol set will be uploaded. Retrieve this ID from the /api/projects/ endpoint if needed."),
    ref: str = Field(..., description="A reference identifier for the symbol set being uploaded, used to track and organize the upload session."),
) -> dict[str, Any]:
    """Initiates the upload process for a symbol set in error tracking. This operation prepares the server to receive symbol set data and returns upload credentials or a session identifier for subsequent upload requests."""

    # Construct request model with validation
    try:
        _request = _models.ErrorTrackingSymbolSetsStartUploadCreate2Request(
            path=_models.ErrorTrackingSymbolSetsStartUploadCreate2RequestPath(project_id=project_id),
            body=_models.ErrorTrackingSymbolSetsStartUploadCreate2RequestBody(ref=ref)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for start_symbol_set_upload_project: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/error_tracking/symbol_sets/start_upload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/error_tracking/symbol_sets/start_upload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("start_symbol_set_upload_project")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("start_symbol_set_upload_project", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="start_symbol_set_upload_project",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="multipart/form-data",
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def list_event_definitions(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value by calling the projects list endpoint if you don't have it."),
    limit: int | None = Field(None, description="Maximum number of event definitions to return in a single page of results. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of event definitions for a specific project. Use this to discover available events and their configurations within your project."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsListRequest(
            path=_models.EventDefinitionsListRequestPath(project_id=project_id),
            query=_models.EventDefinitionsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_event_definitions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_event_definitions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_event_definitions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_event_definitions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def create_event_definition(
    project_id: str = Field(..., description="The unique identifier of the project where the event definition will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the event definition. Must be 400 characters or fewer.", max_length=400),
    owner: int | None = Field(None, description="The user ID of the person responsible for owning and maintaining this event definition."),
    description: str | None = Field(None, description="A detailed explanation of what this event definition tracks and its purpose."),
    tags: list[Any] | None = Field(None, description="A list of labels or keywords for categorizing and organizing the event definition."),
    verified: bool | None = Field(None, description="Whether this event definition has been validated and approved for use."),
    hidden: bool | None = Field(None, description="Whether this event definition should be hidden from standard views and listings."),
    enforcement_mode: Literal["allow", "reject"] | None = Field(None, description="Controls how the system handles events matching this definition. Use 'allow' to accept matching events or 'reject' to block them."),
    post_to_slack: bool | None = Field(None, description="Whether to automatically post notifications about this event to Slack. Defaults to false."),
    default_columns: list[str] | None = Field(None, description="An ordered list of column names that should be displayed by default when viewing data for this event definition."),
) -> dict[str, Any]:
    """Create a new event definition within a project to track and manage custom events. Event definitions establish the schema and behavior for events that can be captured and analyzed."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsCreateRequest(
            path=_models.EventDefinitionsCreateRequestPath(project_id=project_id),
            body=_models.EventDefinitionsCreateRequestBody(name=name, owner=owner, description=description, tags=tags, verified=verified, hidden=hidden, enforcement_mode=enforcement_mode, post_to_slack=post_to_slack, default_columns=default_columns)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_event_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_event_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_event_definition", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_event_definition",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def get_event_definition(
    id_: str = Field(..., alias="id", description="The UUID identifier of the event definition to retrieve."),
    project_id: str = Field(..., description="The UUID of the project containing the event definition. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific event definition by its UUID within a project. Use this to fetch detailed information about a single event definition."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsRetrieveRequest(
            path=_models.EventDefinitionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_definition", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_definition",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def update_event_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the event definition to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event definition. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the event definition. Must not exceed 400 characters.", max_length=400),
    owner: int | None = Field(None, description="The numeric ID of the user who owns this event definition."),
    description: str | None = Field(None, description="A detailed description of the event definition and its purpose."),
    tags: list[Any] | None = Field(None, description="A list of tags for categorizing and organizing the event definition."),
    verified: bool | None = Field(None, description="Whether this event definition has been verified as accurate and reliable."),
    hidden: bool | None = Field(None, description="Whether this event definition should be hidden from standard views and listings."),
    enforcement_mode: Literal["allow", "reject"] | None = Field(None, description="The enforcement mode for this event definition. Use 'allow' to permit the event or 'reject' to block it."),
    post_to_slack: bool | None = Field(None, description="Whether to post notifications about this event to Slack. Defaults to false if not specified."),
    default_columns: list[str] | None = Field(None, description="An array specifying which columns should be displayed by default when viewing events of this type."),
) -> dict[str, Any]:
    """Update an event definition within a project. Modify event metadata, ownership, visibility, enforcement rules, and notification settings."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsUpdateRequest(
            path=_models.EventDefinitionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EventDefinitionsUpdateRequestBody(name=name, owner=owner, description=description, tags=tags, verified=verified, hidden=hidden, enforcement_mode=enforcement_mode, post_to_slack=post_to_slack, default_columns=default_columns)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_event_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_event_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_event_definition", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_event_definition",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def update_event_definition_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the event definition to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event definition. Retrieve available project IDs from the /api/projects/ endpoint."),
    owner: int | None = Field(None, description="The user ID of the event definition owner."),
    description: str | None = Field(None, description="A text description of the event definition's purpose and details."),
    tags: list[Any] | None = Field(None, description="An array of tag strings for categorizing and organizing the event definition."),
    verified: bool | None = Field(None, description="Boolean flag indicating whether the event definition has been verified as accurate and production-ready."),
    hidden: bool | None = Field(None, description="Boolean flag to hide the event definition from standard listings and views."),
    enforcement_mode: Literal["allow", "reject"] | None = Field(None, description="The enforcement mode for the event definition. Use 'allow' to permit the event or 'reject' to block it."),
    post_to_slack: bool | None = Field(None, description="Boolean flag to enable posting event notifications to Slack. Defaults to false if not specified."),
    default_columns: list[str] | None = Field(None, description="An array specifying which columns should be displayed by default when viewing event data."),
) -> dict[str, Any]:
    """Partially update an event definition within a project. Allows modification of metadata, verification status, enforcement rules, and notification settings for a specific event definition."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsPartialUpdateRequest(
            path=_models.EventDefinitionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EventDefinitionsPartialUpdateRequestBody(owner=owner, description=description, tags=tags, verified=verified, hidden=hidden, enforcement_mode=enforcement_mode, post_to_slack=post_to_slack, default_columns=default_columns)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_event_definition_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_event_definition_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_event_definition_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_event_definition_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def delete_event_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the event definition to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event definition. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an event definition from a project. This operation removes the event definition and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsDestroyRequest(
            path=_models.EventDefinitionsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_event_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_event_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_event_definition", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_event_definition",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def get_event_definition_by_name(
    project_id: str = Field(..., description="The unique identifier of the project containing the event definition. You can retrieve available project IDs by calling the projects list endpoint."),
    name: str = Field(..., description="The exact name of the event definition to retrieve. The lookup is case-sensitive and requires a complete match."),
) -> dict[str, Any]:
    """Retrieve a specific event definition by its exact name within a project. Use this to look up event schema and configuration details for a known event name."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsByNameRetrieveRequest(
            path=_models.EventDefinitionsByNameRetrieveRequestPath(project_id=project_id),
            query=_models.EventDefinitionsByNameRetrieveRequestQuery(name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_definition_by_name: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/by_name", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/by_name"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_definition_by_name")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_definition_by_name", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_definition_by_name",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def list_event_definitions_golang(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve all Go event definitions configured for a specific project. This returns the event schema and metadata used for Go-based event tracking within the project."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsGolangRetrieveRequest(
            path=_models.EventDefinitionsGolangRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_event_definitions_golang: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/golang", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/golang"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_event_definitions_golang")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_event_definitions_golang", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_event_definitions_golang",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def list_event_definitions_python(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list all available projects.")) -> dict[str, Any]:
    """Retrieve all Python event definitions configured for a specific project. This returns the event schemas and metadata used for tracking Python-related events in your project."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsPythonRetrieveRequest(
            path=_models.EventDefinitionsPythonRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_event_definitions_python: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/python", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/python"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_event_definitions_python")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_event_definitions_python", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_event_definitions_python",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, event_definitions
@mcp.tool()
async def get_event_definitions_typescript(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve TypeScript event definitions for a specific project. Returns the event schema and type definitions used for tracking events in TypeScript applications."""

    # Construct request model with validation
    try:
        _request = _models.EventDefinitionsTypescriptRetrieveRequest(
            path=_models.EventDefinitionsTypescriptRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event_definitions_typescript: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_definitions/typescript", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_definitions/typescript"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event_definitions_typescript")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event_definitions_typescript", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event_definitions_typescript",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: event_schemas
@mcp.tool()
async def list_event_schemas(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if you don't have it."),
    limit: int | None = Field(None, description="Maximum number of event schemas to return in a single page of results. Defaults to the API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of event schemas for a specific project. Use this to discover available event schemas and their configurations."""

    # Construct request model with validation
    try:
        _request = _models.EventSchemasListRequest(
            path=_models.EventSchemasListRequestPath(project_id=project_id),
            query=_models.EventSchemasListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_event_schemas: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_schemas", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_schemas"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_event_schemas")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_event_schemas", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_event_schemas",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: event_schemas
@mcp.tool()
async def update_event_schema(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the event schema to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event schema. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    event_definition: str = Field(..., description="The unique identifier (UUID) of the event definition to associate with this schema."),
    property_group_id: str = Field(..., description="The unique identifier (UUID) of the property group to associate with this schema."),
) -> dict[str, Any]:
    """Update an event schema within a project by associating it with a specific event definition and property group. This operation modifies the schema configuration for event tracking."""

    # Construct request model with validation
    try:
        _request = _models.EventSchemasUpdateRequest(
            path=_models.EventSchemasUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EventSchemasUpdateRequestBody(event_definition=event_definition, property_group_id=property_group_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_event_schema: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_schemas/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_schemas/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_event_schema")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_event_schema", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_event_schema",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: event_schemas
@mcp.tool()
async def update_event_schema_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the event schema to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event schema. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    event_definition: str | None = Field(None, description="The UUID of the event definition to associate with this event schema. Must be a valid UUID format if provided."),
) -> dict[str, Any]:
    """Partially update an event schema within a project. Allows modification of specific fields such as the associated event definition without replacing the entire schema."""

    # Construct request model with validation
    try:
        _request = _models.EventSchemasPartialUpdateRequest(
            path=_models.EventSchemasPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.EventSchemasPartialUpdateRequestBody(event_definition=event_definition)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_event_schema_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_schemas/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_schemas/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_event_schema_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_event_schema_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_event_schema_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: event_schemas
@mcp.tool()
async def delete_event_schema(
    id_: str = Field(..., alias="id", description="The unique identifier of the event schema to delete, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event schema. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an event schema from a project. This operation removes the schema definition and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.EventSchemasDestroyRequest(
            path=_models.EventSchemasDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_event_schema: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/event_schemas/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/event_schemas/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_event_schema")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_event_schema", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_event_schema",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: events
@mcp.tool()
async def get_event(
    id_: str = Field(..., alias="id", description="The unique identifier of the event to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the event. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific event by its ID within a project. Returns detailed information about the event including its metadata and associated data."""

    # Construct request model with validation
    try:
        _request = _models.EventsRetrieveRequest(
            path=_models.EventsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/events/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/events/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_event", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_event",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def list_experiment_holdouts(
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment holdouts. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of holdout records to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of experiment holdouts for a specific project. Holdouts are control groups or segments excluded from experiment treatment."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsListRequest(
            path=_models.ExperimentHoldoutsListRequestPath(project_id=project_id),
            query=_models.ExperimentHoldoutsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_experiment_holdouts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_experiment_holdouts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_experiment_holdouts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_experiment_holdouts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def create_experiment_holdout(
    project_id: str = Field(..., description="The unique identifier of the project where the holdout will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the holdout group. Must be 400 characters or fewer.", max_length=400),
    description: str | None = Field(None, description="An optional detailed description of the holdout's purpose or criteria. Must be 400 characters or fewer.", max_length=400),
    filters: Any | None = Field(None, description="Optional filtering criteria to define which users or segments are included in this holdout. Specify the filter structure according to the API's filter syntax."),
) -> dict[str, Any]:
    """Create a new experiment holdout group for a project. Holdouts are used to exclude specific users or segments from experiments for control or comparison purposes."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsCreateRequest(
            path=_models.ExperimentHoldoutsCreateRequestPath(project_id=project_id),
            body=_models.ExperimentHoldoutsCreateRequestBody(name=name, description=description, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_experiment_holdout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_experiment_holdout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_experiment_holdout", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_experiment_holdout",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def get_experiment_holdout(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment holdout to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment holdout. You can obtain project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific experiment holdout by its unique identifier within a project. Use this to fetch detailed information about a single holdout configuration."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsRetrieveRequest(
            path=_models.ExperimentHoldoutsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_experiment_holdout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_experiment_holdout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_experiment_holdout", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_experiment_holdout",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def update_experiment_holdout(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment holdout to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment holdout. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the experiment holdout. Must not exceed 400 characters.", max_length=400),
    description: str | None = Field(None, description="An optional description of the experiment holdout. Must not exceed 400 characters if provided.", max_length=400),
    filters: Any | None = Field(None, description="Optional filter criteria to define which entities are included in the holdout. Specify as a structured filter object."),
) -> dict[str, Any]:
    """Update an experiment holdout configuration within a project, including its name, description, and filter criteria."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsUpdateRequest(
            path=_models.ExperimentHoldoutsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentHoldoutsUpdateRequestBody(name=name, description=description, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment_holdout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment_holdout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment_holdout", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment_holdout",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def update_experiment_holdout_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment holdout to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment holdout. Retrieve available project IDs by calling /api/projects/."),
    description: str | None = Field(None, description="A text description of the experiment holdout, up to 400 characters in length.", max_length=400),
    filters: Any | None = Field(None, description="Filter criteria defining the holdout population. Specify the filter configuration as a structured object."),
) -> dict[str, Any]:
    """Partially update an experiment holdout configuration within a project, allowing you to modify its description and/or filter criteria."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsPartialUpdateRequest(
            path=_models.ExperimentHoldoutsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentHoldoutsPartialUpdateRequestBody(description=description, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment_holdout_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment_holdout_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment_holdout_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment_holdout_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_holdouts
@mcp.tool()
async def delete_experiment_holdout(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment holdout to delete. This is a positive integer that uniquely identifies the holdout within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment holdout. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific experiment holdout from a project. This removes the holdout configuration and its associated data."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentHoldoutsDestroyRequest(
            path=_models.ExperimentHoldoutsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_experiment_holdout: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_holdouts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_holdouts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_experiment_holdout")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_experiment_holdout", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_experiment_holdout",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def list_experiment_saved_metrics(
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment metrics. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or set to default for standard pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination control."),
) -> dict[str, Any]:
    """Retrieve a paginated list of saved metrics for experiments within a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsListRequest(
            path=_models.ExperimentSavedMetricsListRequestPath(project_id=project_id),
            query=_models.ExperimentSavedMetricsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_experiment_saved_metrics: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_experiment_saved_metrics")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_experiment_saved_metrics", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_experiment_saved_metrics",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def create_experiment_saved_metric(
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the saved metric. Must not exceed 400 characters.", max_length=400),
    query: Any = Field(..., description="The metric query definition that specifies how the metric should be calculated. This defines the computation logic for the saved metric."),
    description: str | None = Field(None, description="An optional detailed explanation of what this metric measures or how it's calculated. Must not exceed 400 characters if provided.", max_length=400),
    tags: list[Any] | None = Field(None, description="An optional list of tags for organizing and categorizing the saved metric. Tags help with filtering and discovery of metrics."),
) -> dict[str, Any]:
    """Create a new saved metric for an experiment in a project. Saved metrics allow you to define and reuse custom metric calculations across experiments."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsCreateRequest(
            path=_models.ExperimentSavedMetricsCreateRequestPath(project_id=project_id),
            body=_models.ExperimentSavedMetricsCreateRequestBody(name=name, description=description, query=query, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_experiment_saved_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_experiment_saved_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_experiment_saved_metric", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_experiment_saved_metric",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def get_experiment_saved_metric(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment saved metric to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment saved metric. You can obtain project IDs by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific saved metric from an experiment by its unique identifier. Use this to fetch detailed information about a previously saved metric within a project."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsRetrieveRequest(
            path=_models.ExperimentSavedMetricsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_experiment_saved_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_experiment_saved_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_experiment_saved_metric", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_experiment_saved_metric",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def update_experiment_saved_metric(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment saved metric to update."),
    project_id: str = Field(..., description="The project ID containing the experiment. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the saved metric. Must not exceed 400 characters.", max_length=400),
    query: Any = Field(..., description="The query definition that specifies how the metric is calculated or retrieved."),
    description: str | None = Field(None, description="An optional description of the saved metric's purpose or details. Must not exceed 400 characters if provided.", max_length=400),
    tags: list[Any] | None = Field(None, description="An optional list of tags for organizing and categorizing the saved metric."),
) -> dict[str, Any]:
    """Update an existing saved metric configuration for an experiment, including its name, description, query definition, and associated tags."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsUpdateRequest(
            path=_models.ExperimentSavedMetricsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentSavedMetricsUpdateRequestBody(name=name, description=description, query=query, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment_saved_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment_saved_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment_saved_metric", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment_saved_metric",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def update_experiment_saved_metric_partial(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment saved metric to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment saved metric. Retrieve available project IDs by calling /api/projects/."),
    description: str | None = Field(None, description="A text description of the saved metric, up to 400 characters in length.", max_length=400),
    query: Any | None = Field(None, description="The query definition or configuration for the saved metric."),
    tags: list[Any] | None = Field(None, description="An array of tags to categorize or label the saved metric."),
) -> dict[str, Any]:
    """Partially update an experiment saved metric within a project, allowing you to modify its description, query, and tags."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsPartialUpdateRequest(
            path=_models.ExperimentSavedMetricsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentSavedMetricsPartialUpdateRequestBody(description=description, query=query, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment_saved_metric_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment_saved_metric_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment_saved_metric_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment_saved_metric_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiment_saved_metrics
@mcp.tool()
async def delete_experiment_saved_metric(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment saved metric to delete. This is a positive integer that uniquely identifies the metric within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment saved metric. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a saved metric from an experiment. This removes the metric configuration and all associated data for the specified experiment."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentSavedMetricsDestroyRequest(
            path=_models.ExperimentSavedMetricsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_experiment_saved_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiment_saved_metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiment_saved_metrics/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_experiment_saved_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_experiment_saved_metric", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_experiment_saved_metric",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def list_experiments(
    project_id: str = Field(..., description="The unique identifier of the project containing the experiments. You can retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of experiment records to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position in the result set for pagination. Use with limit to retrieve subsequent pages of results."),
) -> dict[str, Any]:
    """Retrieve a paginated list of experiments for a specific project, with support for filtering by status and archival state."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsListRequest(
            path=_models.ExperimentsListRequestPath(project_id=project_id),
            query=_models.ExperimentsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_experiments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_experiments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_experiments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_experiments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def create_experiment(
    project_id: str = Field(..., description="The unique identifier of the project where the experiment will be created. Retrieve project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the experiment. Must not exceed 400 characters.", max_length=400),
    feature_flag_key: str = Field(..., description="The feature flag key that controls the experiment's treatment assignment and rollout."),
    description: str | None = Field(None, description="Optional description of the experiment's purpose or details. Must not exceed 400 characters if provided.", max_length=400),
    start_date: str | None = Field(None, description="Optional start date and time for the experiment in ISO 8601 format."),
    end_date: str | None = Field(None, description="Optional end date and time for the experiment in ISO 8601 format."),
    holdout_id: int | None = Field(None, description="Optional identifier for a holdout group to exclude from the experiment."),
    parameters: Any | None = Field(None, description="Optional experiment parameters for configuring treatment variations and allocation rules."),
    secondary_metrics: Any | None = Field(None, description="Optional secondary metrics to track alongside primary metrics for deeper analysis."),
    saved_metrics_ids: list[Any] | None = Field(None, description="Optional array of saved metric IDs to include in the experiment analysis."),
    filters: Any | None = Field(None, description="Optional filters to define the user population eligible for the experiment."),
    exposure_criteria: Any | None = Field(None, description="Optional criteria for determining user exposure to the experiment."),
    stats_config: Any | None = Field(None, description="Optional statistical configuration for hypothesis testing and result analysis."),
    scheduling_config: Any | None = Field(None, description="Optional scheduling configuration for automated experiment start and end times."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | Literal[""] | None = Field(None, description="Optional conclusion details summarizing the experiment results and findings."),
    conclusion_comment: str | None = Field(None, description="Optional comment providing additional context or notes about the experiment conclusion."),
    exposure_preaggregation_enabled: bool | None = Field(None, description="Optional flag to enable pre-aggregation of exposure data for improved performance."),
    only_count_matured_users: bool | None = Field(None, description="Optional flag to count only users who have matured (met stability criteria) in the analysis."),
) -> dict[str, Any]:
    """Create a new experiment in draft status for a project with optional configuration for metrics, scheduling, and statistical analysis."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsCreateRequest(
            path=_models.ExperimentsCreateRequestPath(project_id=project_id),
            body=_models.ExperimentsCreateRequestBody(name=name, description=description, start_date=start_date, end_date=end_date, feature_flag_key=feature_flag_key, holdout_id=holdout_id, parameters=parameters, secondary_metrics=secondary_metrics, saved_metrics_ids=saved_metrics_ids, filters=filters, exposure_criteria=exposure_criteria, stats_config=stats_config, scheduling_config=scheduling_config, conclusion=conclusion, conclusion_comment=conclusion_comment, exposure_preaggregation_enabled=exposure_preaggregation_enabled, only_count_matured_users=only_count_matured_users)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def get_experiment(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a single experiment by its ID, including its current status, metrics, feature flag configuration, and results metadata."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsRetrieveRequest(
            path=_models.ExperimentsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_experiment", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_experiment",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def update_experiment(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The experiment name. Maximum length is 400 characters.", max_length=400),
    feature_flag_key: str = Field(..., description="The feature flag key associated with this experiment."),
    description: str | None = Field(None, description="Optional description of the experiment. Maximum length is 400 characters.", max_length=400),
    start_date: str | None = Field(None, description="Optional experiment start date and time in ISO 8601 format."),
    end_date: str | None = Field(None, description="Optional experiment end date and time in ISO 8601 format."),
    holdout_id: int | None = Field(None, description="Optional identifier for a holdout group to exclude from the experiment."),
    parameters: Any | None = Field(None, description="Optional experiment parameters configuration."),
    secondary_metrics: Any | None = Field(None, description="Optional secondary metrics to track alongside primary metrics."),
    saved_metrics_ids: list[Any] | None = Field(None, description="Optional array of saved metric identifiers to include in the experiment analysis."),
    filters: Any | None = Field(None, description="Optional filters to define the experiment population or segments."),
    exposure_criteria: Any | None = Field(None, description="Optional exposure criteria configuration for determining user eligibility."),
    stats_config: Any | None = Field(None, description="Optional statistical analysis configuration for the experiment."),
    scheduling_config: Any | None = Field(None, description="Optional scheduling configuration for experiment timing and rollout."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | Literal[""] | None = Field(None, description="Optional conclusion details for a completed experiment."),
    conclusion_comment: str | None = Field(None, description="Optional comment documenting the experiment conclusion or decision rationale."),
    exposure_preaggregation_enabled: bool | None = Field(None, description="Optional flag to enable pre-aggregation of exposure data for improved performance."),
    only_count_matured_users: bool | None = Field(None, description="Optional flag to count only users who have matured (met stability criteria) in analysis."),
) -> dict[str, Any]:
    """Update an experiment configuration within a project. This operation supports approval workflows for changes requiring authorization, returning a 409 Conflict response if approval is required."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsUpdateRequest(
            path=_models.ExperimentsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsUpdateRequestBody(name=name, description=description, start_date=start_date, end_date=end_date, feature_flag_key=feature_flag_key, holdout_id=holdout_id, parameters=parameters, secondary_metrics=secondary_metrics, saved_metrics_ids=saved_metrics_ids, filters=filters, exposure_criteria=exposure_criteria, stats_config=stats_config, scheduling_config=scheduling_config, conclusion=conclusion, conclusion_comment=conclusion_comment, exposure_preaggregation_enabled=exposure_preaggregation_enabled, only_count_matured_users=only_count_matured_users)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def update_experiment_partial(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A brief text description of the experiment. Maximum 400 characters.", max_length=400),
    start_date: str | None = Field(None, description="The experiment start date and time in ISO 8601 format."),
    end_date: str | None = Field(None, description="The experiment end date and time in ISO 8601 format."),
    feature_flag_key: str | None = Field(None, description="The feature flag key associated with this experiment for controlling variant assignment."),
    holdout_id: int | None = Field(None, description="The unique identifier of a holdout group to exclude from the experiment."),
    parameters: Any | None = Field(None, description="Configuration parameters for the experiment variants and their allocation rules."),
    secondary_metrics: Any | None = Field(None, description="Secondary metrics to track alongside the primary metric for additional analysis."),
    saved_metrics_ids: list[Any] | None = Field(None, description="Array of saved metric IDs to include in the experiment analysis."),
    filters: Any | None = Field(None, description="Audience filters and targeting criteria that define which users are eligible for the experiment."),
    exposure_criteria: Any | None = Field(None, description="Criteria for determining user exposure to experiment variants."),
    stats_config: Any | None = Field(None, description="Statistical configuration including significance levels, power, and analysis methodology."),
    scheduling_config: Any | None = Field(None, description="Scheduling configuration for automated experiment start, end, or ramp-up behavior."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | Literal[""] | None = Field(None, description="The conclusion status or outcome determination for the experiment."),
    conclusion_comment: str | None = Field(None, description="Additional notes or reasoning documenting the experiment's conclusion."),
    exposure_preaggregation_enabled: bool | None = Field(None, description="Enable pre-aggregation of exposure events for improved performance with high-volume experiments."),
    only_count_matured_users: bool | None = Field(None, description="When enabled, only count users who have matured (met stability criteria) in the analysis."),
) -> dict[str, Any]:
    """Partially update an experiment's properties including name, description, metrics, variants, configuration, and scheduling. Supports modifying experiment timing, feature flag associations, statistical settings, and conclusion status."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsPartialUpdateRequest(
            path=_models.ExperimentsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsPartialUpdateRequestBody(description=description, start_date=start_date, end_date=end_date, feature_flag_key=feature_flag_key, holdout_id=holdout_id, parameters=parameters, secondary_metrics=secondary_metrics, saved_metrics_ids=saved_metrics_ids, filters=filters, exposure_criteria=exposure_criteria, stats_config=stats_config, scheduling_config=scheduling_config, conclusion=conclusion, conclusion_comment=conclusion_comment, exposure_preaggregation_enabled=exposure_preaggregation_enabled, only_count_matured_users=only_count_matured_users)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_experiment_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_experiment_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_experiment_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_experiment_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def mark_experiment_deleted(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to mark as deleted."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Soft-delete an experiment by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsDestroyRequest(
            path=_models.ExperimentsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for mark_experiment_deleted: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("mark_experiment_deleted")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("mark_experiment_deleted", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="mark_experiment_deleted",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def archive_experiment(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to archive."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. You can retrieve project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Archive a completed experiment to hide it from the default list view. Archived experiments can be restored at any time by setting archived=false. The experiment must have ended and not already be archived."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsArchiveCreateRequest(
            path=_models.ExperimentsArchiveCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for archive_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/archive", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/archive"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("archive_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("archive_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="archive_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def create_exposure_cohort_for_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the exposure cohort. Maximum 400 characters.", max_length=400),
    feature_flag_key: str = Field(..., description="The feature flag key that controls exposure for this experiment."),
    description: str | None = Field(None, description="Optional description of the exposure cohort's purpose or details. Maximum 400 characters.", max_length=400),
    start_date: str | None = Field(None, description="Optional start date and time for the cohort in ISO 8601 format."),
    end_date: str | None = Field(None, description="Optional end date and time for the cohort in ISO 8601 format."),
    holdout_id: int | None = Field(None, description="Optional identifier of a holdout group to exclude from the experiment."),
    parameters: Any | None = Field(None, description="Optional configuration parameters for the exposure cohort."),
    secondary_metrics: Any | None = Field(None, description="Optional array of secondary metrics to track alongside primary metrics."),
    saved_metrics_ids: list[Any] | None = Field(None, description="Optional array of saved metric identifiers to include in the experiment analysis."),
    filters: Any | None = Field(None, description="Optional filtering criteria to define the cohort population."),
    exposure_criteria: Any | None = Field(None, description="Optional criteria defining how user exposure is determined."),
    stats_config: Any | None = Field(None, description="Optional statistical configuration for analysis and significance testing."),
    scheduling_config: Any | None = Field(None, description="Optional scheduling configuration for automated experiment execution."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | Literal[""] | None = Field(None, description="Optional conclusion details for the experiment results."),
    conclusion_comment: str | None = Field(None, description="Optional comment summarizing the experiment conclusion or findings."),
    exposure_preaggregation_enabled: bool | None = Field(None, description="Optional flag to enable pre-aggregation of exposure data for improved performance."),
    only_count_matured_users: bool | None = Field(None, description="Optional flag to count only users who have reached maturity in the experiment."),
) -> dict[str, Any]:
    """Create an exposure cohort for an experiment to define the population and measurement criteria. This operation may require approval if the experiment configuration triggers approval gates."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsCreateExposureCohortForExperimentCreateRequest(
            path=_models.ExperimentsCreateExposureCohortForExperimentCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsCreateExposureCohortForExperimentCreateRequestBody(name=name, description=description, start_date=start_date, end_date=end_date, feature_flag_key=feature_flag_key, holdout_id=holdout_id, parameters=parameters, secondary_metrics=secondary_metrics, saved_metrics_ids=saved_metrics_ids, filters=filters, exposure_criteria=exposure_criteria, stats_config=stats_config, scheduling_config=scheduling_config, conclusion=conclusion, conclusion_comment=conclusion_comment, exposure_preaggregation_enabled=exposure_preaggregation_enabled, only_count_matured_users=only_count_matured_users)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_exposure_cohort_for_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/create_exposure_cohort_for_experiment", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/create_exposure_cohort_for_experiment"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_exposure_cohort_for_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_exposure_cohort_for_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_exposure_cohort_for_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def duplicate_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment to duplicate. This is a positive integer value."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="Display name for the duplicated experiment. Must not exceed 400 characters.", max_length=400),
    feature_flag_key: str = Field(..., description="The feature flag key that controls this experiment's rollout and variant assignment."),
    description: str | None = Field(None, description="Optional detailed description of the duplicated experiment. Must not exceed 400 characters if provided.", max_length=400),
    start_date: str | None = Field(None, description="Optional experiment start date and time in ISO 8601 format (e.g., 2024-01-15T10:30:00Z)."),
    end_date: str | None = Field(None, description="Optional experiment end date and time in ISO 8601 format (e.g., 2024-02-15T10:30:00Z)."),
    holdout_id: int | None = Field(None, description="Optional identifier for a holdout group to exclude from the experiment."),
    parameters: Any | None = Field(None, description="Optional experiment parameters as a structured object, typically containing configuration values for the experiment variants."),
    secondary_metrics: Any | None = Field(None, description="Optional array of secondary metrics to track alongside the primary metric, used for monitoring additional experiment outcomes."),
    saved_metrics_ids: list[Any] | None = Field(None, description="Optional array of saved metric identifiers to include in the experiment's analysis."),
    filters: Any | None = Field(None, description="Optional filtering criteria as a structured object to segment the experiment audience based on user attributes or events."),
    exposure_criteria: Any | None = Field(None, description="Optional exposure criteria as a structured object defining which users are eligible for experiment exposure."),
    stats_config: Any | None = Field(None, description="Optional statistical configuration as a structured object specifying analysis methods, significance levels, and other statistical parameters."),
    scheduling_config: Any | None = Field(None, description="Optional scheduling configuration as a structured object defining experiment timing, ramp-up schedules, or other temporal settings."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | Literal[""] | None = Field(None, description="Optional conclusion details as a structured object containing the experiment's final outcome and decision."),
    conclusion_comment: str | None = Field(None, description="Optional comment explaining the experiment's conclusion or final decision. Typically used to document reasoning for stopping or completing the experiment."),
    exposure_preaggregation_enabled: bool | None = Field(None, description="Optional boolean flag to enable pre-aggregation of exposure data for improved performance in large-scale experiments."),
    only_count_matured_users: bool | None = Field(None, description="Optional boolean flag to count only users who have reached maturity in the experiment, excluding early-stage participants from analysis."),
) -> dict[str, Any]:
    """Creates a duplicate copy of an existing experiment with customizable settings. The duplicated experiment can have modified configuration while preserving or overriding the original experiment's parameters, metrics, and scheduling."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsDuplicateCreateRequest(
            path=_models.ExperimentsDuplicateCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsDuplicateCreateRequestBody(name=name, description=description, start_date=start_date, end_date=end_date, feature_flag_key=feature_flag_key, holdout_id=holdout_id, parameters=parameters, secondary_metrics=secondary_metrics, saved_metrics_ids=saved_metrics_ids, filters=filters, exposure_criteria=exposure_criteria, stats_config=stats_config, scheduling_config=scheduling_config, conclusion=conclusion, conclusion_comment=conclusion_comment, exposure_preaggregation_enabled=exposure_preaggregation_enabled, only_count_matured_users=only_count_matured_users)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for duplicate_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/duplicate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/duplicate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("duplicate_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("duplicate_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="duplicate_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def end_experiment(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to end."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Find your project ID by calling /api/projects/."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | None = Field(None, description="The outcome classification of the experiment: won, lost, inconclusive, stopped_early, or invalid."),
    conclusion_comment: str | None = Field(None, description="An optional note explaining the reasoning behind the experiment conclusion."),
) -> dict[str, Any]:
    """End a running experiment and freeze its results window without modifying the feature flag or user variant assignments. Users continue to see their assigned variants and exposure events are still recorded, but only data up to the end date is included in experiment results."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsEndCreateRequest(
            path=_models.ExperimentsEndCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsEndCreateRequestBody(conclusion=conclusion, conclusion_comment=conclusion_comment)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for end_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/end", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/end"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("end_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("end_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="end_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def launch_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment to launch. Must reference an experiment in draft state within the specified project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Use the /api/projects/ endpoint to retrieve available project IDs."),
) -> dict[str, Any]:
    """Activate and start a draft experiment by validating its state, enabling its linked feature flag, and transitioning it to running status. The experiment must be in draft state and have a valid feature flag configuration with a control variant and at least 2 total variants."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsLaunchCreateRequest(
            path=_models.ExperimentsLaunchCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for launch_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/launch", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/launch"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("launch_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("launch_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="launch_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def pause_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment to pause. Must be a positive integer."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. Can be retrieved from the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Pause a running experiment by deactivating its linked feature flag. Users will no longer receive the experiment variant and will fall back to the application default experience, with no new exposure events recorded."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsPauseCreateRequest(
            path=_models.ExperimentsPauseCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for pause_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/pause", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/pause"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("pause_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("pause_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="pause_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def reset_experiment_to_draft(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to reset."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Reset an experiment to draft state, clearing its start/end dates, conclusion, and archived status. Previously collected events remain in the system but won't be included in results until the experiment is re-launched with a new start date."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsResetCreateRequest(
            path=_models.ExperimentsResetCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reset_experiment_to_draft: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/reset", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/reset"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reset_experiment_to_draft")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reset_experiment_to_draft", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reset_experiment_to_draft",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def resume_experiment(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the experiment to resume."),
    project_id: str = Field(..., description="The unique identifier of the project containing the experiment. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Resume a paused experiment by reactivating its linked feature flag. Users are deterministically re-bucketed into their previous variant assignments and exposure tracking resumes. The experiment must be in a paused state; returns an error if the experiment is not paused or not currently running."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsResumeCreateRequest(
            path=_models.ExperimentsResumeCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for resume_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/resume", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/resume"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("resume_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("resume_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="resume_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def ship_experiment_variant(
    id_: int = Field(..., alias="id", description="The unique identifier of the experiment to ship. Must be an integer."),
    project_id: str = Field(..., description="The ID of the project containing the experiment. Find your project ID by calling /api/projects/."),
    variant_key: str = Field(..., description="The key identifier of the variant to serve to all users. Must match an existing variant on the linked feature flag."),
    conclusion: Literal["won", "lost", "inconclusive", "stopped_early", "invalid"] | None = Field(None, description="The outcome classification for the experiment: won, lost, inconclusive, stopped_early, or invalid. Optional; use to document why the variant is being shipped."),
    conclusion_comment: str | None = Field(None, description="Optional narrative explanation of the experiment's conclusion, such as statistical significance, business rationale, or reasons for early stopping."),
) -> dict[str, Any]:
    """Deploy a winning experiment variant to 100% of users and optionally conclude the experiment. Preserves existing release conditions for easy rollback and supports both immediate shipping and deferred shipping after experiment completion."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsShipVariantCreateRequest(
            path=_models.ExperimentsShipVariantCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExperimentsShipVariantCreateRequestBody(conclusion=conclusion, conclusion_comment=conclusion_comment, variant_key=variant_key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for ship_experiment_variant: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/{id}/ship_variant", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/{id}/ship_variant"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("ship_experiment_variant")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("ship_experiment_variant", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="ship_experiment_variant",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def list_eligible_feature_flags_for_experiments(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve a paginated list of feature flags that are eligible for use in experiments. Eligible flags must be multivariate with at least 2 variants and have 'control' as the first variant key."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsEligibleFeatureFlagsRetrieveRequest(
            path=_models.ExperimentsEligibleFeatureFlagsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_eligible_feature_flags_for_experiments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/eligible_feature_flags", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/eligible_feature_flags"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_eligible_feature_flags_for_experiments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_eligible_feature_flags_for_experiments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_eligible_feature_flags_for_experiments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def list_experiments_requiring_flag_implementation(project_id: str = Field(..., description="The unique identifier of the project containing the experiments. You can obtain the project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve a list of experiments within a project that require flag implementation. This operation helps identify experiments that need feature flag setup before they can be deployed."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsRequiresFlagImplementationRetrieveRequest(
            path=_models.ExperimentsRequiresFlagImplementationRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_experiments_requiring_flag_implementation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/requires_flag_implementation", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/requires_flag_implementation"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_experiments_requiring_flag_implementation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_experiments_requiring_flag_implementation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_experiments_requiring_flag_implementation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: experiments, experiments
@mcp.tool()
async def get_experiments_stats(project_id: str = Field(..., description="The unique identifier of the project for which to retrieve experiment statistics. You can obtain the project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve aggregated statistics and metrics for all experiments within a specific project. This provides an overview of experiment performance and status across the project."""

    # Construct request model with validation
    try:
        _request = _models.ExperimentsStatsRetrieveRequest(
            path=_models.ExperimentsStatsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_experiments_stats: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/experiments/stats", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/experiments/stats"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_experiments_stats")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_experiments_stats", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_experiments_stats",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, exports
@mcp.tool()
async def list_exports(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of export records to return in a single response page. Defaults to the API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of exports for a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.ExportsListRequest(
            path=_models.ExportsListRequestPath(project_id=project_id),
            query=_models.ExportsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_exports: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/exports"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_exports")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_exports", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_exports",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, exports
@mcp.tool()
async def create_export(
    project_id: str = Field(..., description="The unique identifier of the project containing the dashboard or insight to export. Retrieve project IDs by calling the /api/projects/ endpoint."),
    export_format: Literal["image/png", "application/pdf", "text/csv", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "video/webm", "video/mp4", "image/gif", "application/json"] = Field(..., description="The desired output format for the export. Supported formats include PNG and GIF images, PDF documents, CSV and Excel spreadsheets, MP4 and WebM videos, and JSON data."),
    dashboard: int | None = Field(None, description="The ID of the dashboard to export. Either dashboard or insight must be specified, but not both."),
    insight: int | None = Field(None, description="The ID of the insight to export. Either insight or dashboard must be specified, but not both."),
    export_context: Any | None = Field(None, description="Additional context or configuration options for the export operation, such as rendering parameters or export-specific settings."),
) -> dict[str, Any]:
    """Create an export of a dashboard or insight in the specified format. Supports multiple output formats including images, documents, videos, and data files."""

    # Construct request model with validation
    try:
        _request = _models.ExportsCreateRequest(
            path=_models.ExportsCreateRequestPath(project_id=project_id),
            body=_models.ExportsCreateRequestBody(dashboard=dashboard, insight=insight, export_format=export_format, export_context=export_context)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/exports", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/exports"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, exports
@mcp.tool()
async def get_export(
    id_: int = Field(..., alias="id", description="The unique identifier of the exported asset you want to retrieve. This is a positive integer assigned when the export was created."),
    project_id: str = Field(..., description="The unique identifier of the project containing the export. You can retrieve your project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific exported asset by its ID from a project. Use this to fetch details about a previously created export."""

    # Construct request model with validation
    try:
        _request = _models.ExportsRetrieveRequest(
            path=_models.ExportsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/exports/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/exports/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_export", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_export",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, exports
@mcp.tool()
async def get_export_content(
    id_: int = Field(..., alias="id", description="The unique identifier of the exported asset to retrieve. This is a positive integer assigned when the export was created."),
    project_id: str = Field(..., description="The unique identifier of the project containing the export. You can retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve the content of a specific exported asset from a project. This operation fetches the actual exported data associated with a previously created export."""

    # Construct request model with validation
    try:
        _request = _models.ExportsContentRetrieveRequest(
            path=_models.ExportsContentRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_export_content: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/exports/{id}/content", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/exports/{id}/content"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_export_content")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_export_content", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_export_content",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def list_external_data_sources(
    project_id: str = Field(..., description="The unique identifier of the project containing the external data sources. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or set to default for standard pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination offset."),
) -> dict[str, Any]:
    """Retrieve a paginated list of external data sources configured for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesListRequest(
            path=_models.ExternalDataSourcesListRequestPath(project_id=project_id),
            query=_models.ExternalDataSourcesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_external_data_sources: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_external_data_sources")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_external_data_sources", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_external_data_sources",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_external_data_source(
    project_id: str = Field(..., description="The unique identifier of the project where the external data source will be created. Retrieve this ID by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="Secret credential required for authentication and authorization of the API request."),
    account_id: str = Field(..., description="The account identifier associated with the external data source."),
    prefix: str | None = Field(None, description="A short identifier or label for the external data source, up to 100 characters.", max_length=100),
    description: str | None = Field(None, description="A detailed explanation of the external data source's purpose and contents, up to 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Configuration object specifying input parameters and settings for the external data source job."),
) -> dict[str, Any]:
    """Create a new external data source for a project. External data sources enable integration with external systems and data repositories."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesCreateRequest(
            path=_models.ExternalDataSourcesCreateRequestPath(project_id=project_id),
            body=_models.ExternalDataSourcesCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_external_data_source", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_external_data_source",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def get_external_data_source(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Obtain this by calling the projects list endpoint if needed."),
) -> dict[str, Any]:
    """Retrieve a specific external data source by its unique identifier within a project. Use this to fetch configuration and metadata for an external data source."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesRetrieveRequest(
            path=_models.ExternalDataSourcesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_external_data_source", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_external_data_source",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def update_external_data_source(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this external data source. Retrieve available project IDs from the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="The client secret credential required to authenticate with the external data source."),
    account_id: str = Field(..., description="The account identifier associated with the external data source connection."),
    prefix: str | None = Field(None, description="An optional prefix label for the external data source, up to 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional description of the external data source's purpose or configuration, up to 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional job input parameters that define how data should be processed or retrieved from the external source."),
) -> dict[str, Any]:
    """Update an existing external data source configuration for a project, including authentication credentials and optional metadata like description and job input parameters."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesUpdateRequest(
            path=_models.ExternalDataSourcesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesUpdateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_external_data_source", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_external_data_source",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def update_external_data_source_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Retrieve available project IDs from the /api/projects/ endpoint."),
    prefix: str | None = Field(None, description="A short identifier or label for the external data source, up to 100 characters.", max_length=100),
    description: str | None = Field(None, description="A detailed description of the external data source's purpose or contents, up to 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Configuration object defining input parameters and mappings for data source job execution."),
) -> dict[str, Any]:
    """Partially update an external data source configuration within a project. Allows modification of metadata, description, and job input settings for an existing external data source."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesPartialUpdateRequest(
            path=_models.ExternalDataSourcesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesPartialUpdateRequestBody(prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_external_data_source_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_external_data_source_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_external_data_source_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_external_data_source_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def delete_external_data_source(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete an external data source from a project. This operation removes the data source and all associated configurations."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesDestroyRequest(
            path=_models.ExternalDataSourcesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_external_data_source", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_external_data_source",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_webhook_for_external_data_source(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source for which the webhook is being created."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Retrieve this from the /api/projects/ endpoint if needed."),
    client_secret: str = Field(..., description="The secret key used to authenticate and sign webhook requests from the external data source."),
    account_id: str = Field(..., description="The account identifier associated with the external data source for webhook authentication and routing."),
    prefix: str | None = Field(None, description="An optional prefix to namespace or categorize the webhook events. Maximum length is 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional human-readable description of the webhook's purpose or configuration. Maximum length is 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="An optional object containing input parameters or configuration data for webhook job processing."),
) -> dict[str, Any]:
    """Create a webhook for an external data source to enable real-time event notifications. The webhook will be configured with authentication credentials and optional metadata for routing and processing incoming events."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesCreateWebhookCreateRequest(
            path=_models.ExternalDataSourcesCreateWebhookCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesCreateWebhookCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_webhook_for_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/create_webhook", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/create_webhook"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_webhook_for_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_webhook_for_external_data_source", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_webhook_for_external_data_source",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_external_data_source_delete_webhook(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source for which you want to create the delete webhook."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="A secret key used to authenticate and validate webhook requests from the API. This should be stored securely and used to verify the authenticity of incoming webhook payloads."),
    account_id: str = Field(..., description="The account identifier associated with the webhook configuration. This links the webhook to a specific account context."),
    prefix: str | None = Field(None, description="An optional prefix to namespace or categorize the webhook. Maximum length is 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional human-readable description of the webhook's purpose or configuration. Maximum length is 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional input parameters or configuration data for the webhook job execution. Format and structure depend on your specific webhook implementation requirements."),
) -> dict[str, Any]:
    """Register a webhook endpoint that will be triggered when an external data source is deleted. This allows you to receive notifications and perform cleanup operations when a data source is removed from your project."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesDeleteWebhookCreateRequest(
            path=_models.ExternalDataSourcesDeleteWebhookCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesDeleteWebhookCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_external_data_source_delete_webhook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/delete_webhook", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/delete_webhook"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_external_data_source_delete_webhook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_external_data_source_delete_webhook", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_external_data_source_delete_webhook",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def list_external_data_source_jobs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source whose jobs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all jobs associated with a specific external data source. This allows you to monitor and track the status of data synchronization and processing tasks for that source."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesJobsRetrieveRequest(
            path=_models.ExternalDataSourcesJobsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_external_data_source_jobs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/jobs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/jobs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_external_data_source_jobs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_external_data_source_jobs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_external_data_source_jobs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def refresh_external_data_source_schemas(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to refresh schemas for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Retrieve this from the /api/projects/ endpoint if needed."),
    client_secret: str = Field(..., description="Authentication credential required to access the external data source."),
    account_id: str = Field(..., description="Account identifier for the external data source connection."),
    prefix: str | None = Field(None, description="Optional prefix to apply to discovered schema names. Maximum 100 characters.", max_length=100),
    description: str | None = Field(None, description="Optional description for the schema refresh operation. Maximum 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional configuration object containing additional inputs for the schema refresh job."),
) -> dict[str, Any]:
    """Discover and synchronize the current schema and table structure from an external data source, creating new ExternalDataSchema records for any newly detected tables without syncing actual data."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesRefreshSchemasCreateRequest(
            path=_models.ExternalDataSourcesRefreshSchemasCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesRefreshSchemasCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for refresh_external_data_source_schemas: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/refresh_schemas", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/refresh_schemas"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("refresh_external_data_source_schemas")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("refresh_external_data_source_schemas", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="refresh_external_data_source_schemas",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def reload_external_data_source(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to reload."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="The client secret credential required to authenticate the reload operation with the external data source."),
    account_id: str = Field(..., description="The account identifier associated with the external data source connection."),
    prefix: str | None = Field(None, description="An optional prefix to apply to the reloaded data. Maximum length is 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional description or notes about the reload operation. Maximum length is 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional configuration inputs for the reload job, specifying how the external data should be processed or transformed."),
) -> dict[str, Any]:
    """Trigger a reload of an external data source to refresh its data within a project. This operation refreshes the connection and synchronizes data from the configured external source."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesReloadCreateRequest(
            path=_models.ExternalDataSourcesReloadCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesReloadCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reload_external_data_source: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/reload", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/reload"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reload_external_data_source")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reload_external_data_source", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reload_external_data_source",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def update_external_data_source_revenue_analytics_config(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    prefix: str | None = Field(None, description="An optional prefix label for the revenue analytics configuration, up to 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional description of the revenue analytics configuration, up to 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional input parameters for the revenue analytics job configuration."),
) -> dict[str, Any]:
    """Partially update the revenue analytics configuration for an external data source and retrieve the complete updated external data source object."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesRevenueAnalyticsConfigPartialUpdateRequest(
            path=_models.ExternalDataSourcesRevenueAnalyticsConfigPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesRevenueAnalyticsConfigPartialUpdateRequestBody(prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_external_data_source_revenue_analytics_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/revenue_analytics_config", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/revenue_analytics_config"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_external_data_source_revenue_analytics_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_external_data_source_revenue_analytics_config", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_external_data_source_revenue_analytics_config",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_external_data_source_webhook_inputs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source to configure."),
    project_id: str = Field(..., description="The project ID containing the external data source. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="The client secret credential used to authenticate webhook requests from the external data source."),
    account_id: str = Field(..., description="The account identifier associated with the external data source for authentication and access control."),
    prefix: str | None = Field(None, description="An optional prefix to namespace or identify webhook inputs, limited to 100 characters.", max_length=100),
    description: str | None = Field(None, description="An optional description of the webhook input configuration, limited to 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional mapping of input fields from the webhook payload to job parameters. Structure and format depend on the external data source type."),
) -> dict[str, Any]:
    """Configure webhook input parameters for an external data source, enabling automated data synchronization through webhook triggers. This operation sets up authentication credentials and input mappings for the webhook endpoint."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesUpdateWebhookInputsCreateRequest(
            path=_models.ExternalDataSourcesUpdateWebhookInputsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ExternalDataSourcesUpdateWebhookInputsCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_external_data_source_webhook_inputs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/update_webhook_inputs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/update_webhook_inputs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_external_data_source_webhook_inputs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_external_data_source_webhook_inputs", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_external_data_source_webhook_inputs",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def get_external_data_source_webhook_info(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the external data source whose webhook information you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve webhook configuration and status information for a specific external data source. This includes details about how the data source is connected via webhooks to your project."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesWebhookInfoRetrieveRequest(
            path=_models.ExternalDataSourcesWebhookInfoRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_external_data_source_webhook_info: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/{id}/webhook_info", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/{id}/webhook_info"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_external_data_source_webhook_info")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_external_data_source_webhook_info", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_external_data_source_webhook_info",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def list_external_data_source_connections(
    project_id: str = Field(..., description="The unique identifier of the project containing the external data source connections. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of connection records to return in a single response page. Omit or adjust to control result set size."),
    offset: int | None = Field(None, description="Zero-based starting position for results, used with limit to implement pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of external data source connections for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesConnectionsListRequest(
            path=_models.ExternalDataSourcesConnectionsListRequestPath(project_id=project_id),
            query=_models.ExternalDataSourcesConnectionsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_external_data_source_connections: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/connections", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/connections"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_external_data_source_connections")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_external_data_source_connections", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_external_data_source_connections",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_external_data_source_database_schema(
    project_id: str = Field(..., description="The unique identifier of the project where the external data source will be created. Retrieve this ID by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="Secret credential used to authenticate the request with the external data source system."),
    account_id: str = Field(..., description="The account identifier associated with the external data source connection."),
    prefix: str | None = Field(None, description="Optional namespace or naming prefix for the database schema, up to 100 characters. Useful for organizing multiple schemas within the same project.", max_length=100),
    description: str | None = Field(None, description="Optional human-readable description of the external data source, up to 400 characters. Helps document the purpose and contents of this data source.", max_length=400),
    job_inputs: Any | None = Field(None, description="Optional configuration object containing input parameters for the database schema job, such as connection details, table mappings, or sync settings."),
) -> dict[str, Any]:
    """Create a new external data source with database schema configuration for a project. This operation registers a database connection as an external data source that can be queried within the project."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesDatabaseSchemaCreateRequest(
            path=_models.ExternalDataSourcesDatabaseSchemaCreateRequestPath(project_id=project_id),
            body=_models.ExternalDataSourcesDatabaseSchemaCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_external_data_source_database_schema: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/database_schema", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/database_schema"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_external_data_source_database_schema")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_external_data_source_database_schema", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_external_data_source_database_schema",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def create_external_data_source_prefix(
    project_id: str = Field(..., description="The unique identifier of the project where the external data source prefix will be created. Retrieve this ID by calling the /api/projects/ endpoint."),
    client_secret: str = Field(..., description="Client secret credential required for authentication with the external data source."),
    account_id: str = Field(..., description="Account identifier associated with the external data source configuration."),
    prefix: str | None = Field(None, description="A human-readable name or identifier for the prefix. Maximum length is 100 characters.", max_length=100),
    description: str | None = Field(None, description="Optional descriptive text explaining the purpose or usage of this prefix. Maximum length is 400 characters.", max_length=400),
    job_inputs: Any | None = Field(None, description="Configuration object containing input parameters for the external data source job or processing pipeline."),
) -> dict[str, Any]:
    """Create a new external data source prefix configuration for a project. This allows you to define a named prefix that can be used to organize and reference external data sources."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesSourcePrefixCreateRequest(
            path=_models.ExternalDataSourcesSourcePrefixCreateRequestPath(project_id=project_id),
            body=_models.ExternalDataSourcesSourcePrefixCreateRequestBody(client_secret=client_secret, account_id=account_id, prefix=prefix, description=description, job_inputs=job_inputs)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_external_data_source_prefix: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/source_prefix", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/source_prefix"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_external_data_source_prefix")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_external_data_source_prefix", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_external_data_source_prefix",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, external_data_sources
@mcp.tool()
async def get_external_data_sources_wizard(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve the external data sources wizard interface for a specific project, providing guidance and configuration options for managing external data source connections."""

    # Construct request model with validation
    try:
        _request = _models.ExternalDataSourcesWizardRetrieveRequest(
            path=_models.ExternalDataSourcesWizardRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_external_data_sources_wizard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/external_data_sources/wizard", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/external_data_sources/wizard"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_external_data_sources_wizard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_external_data_sources_wizard", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_external_data_sources_wizard",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def list_feature_flags(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flags. Obtain this ID by calling the /api/projects/ endpoint."),
    active: Literal["STALE", "false", "true"] | None = Field(None, description="Filter by feature flag status: 'true' for active flags, 'false' for inactive flags, or 'STALE' for flags that haven't been evaluated recently."),
    created_by_id: str | None = Field(None, description="Filter feature flags by the user ID of the person who created them."),
    evaluation_runtime: Literal["both", "client", "server"] | None = Field(None, description="Filter by where the feature flag is evaluated: 'client' for client-side evaluation, 'server' for server-side evaluation, or 'both' for flags supporting both runtimes."),
    excluded_properties: str | None = Field(None, description="A JSON-encoded list of feature flag keys to exclude from the results. Specify as a JSON array of strings."),
    has_evaluation_contexts: Literal["false", "true"] | None = Field(None, description="Filter by evaluation context presence: 'true' returns only flags with at least one evaluation context defined, 'false' returns only flags without evaluation contexts."),
    limit: int | None = Field(None, description="Maximum number of results to return per page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting index for results, used for pagination. Combine with limit to retrieve subsequent pages."),
    tags: str | None = Field(None, description="A JSON-encoded list of tag names to filter feature flags by. Only flags tagged with all specified tags will be returned. Specify as a JSON array of strings."),
) -> dict[str, Any]:
    """Retrieve a paginated list of feature flags for a project with optional filtering by status, runtime, tags, and other criteria."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsListRequest(
            path=_models.FeatureFlagsListRequestPath(project_id=project_id),
            query=_models.FeatureFlagsListRequestQuery(active=active, created_by_id=created_by_id, evaluation_runtime=evaluation_runtime, excluded_properties=excluded_properties, has_evaluation_contexts=has_evaluation_contexts, limit=limit, offset=offset, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_feature_flags: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_feature_flags")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_feature_flags", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_feature_flags",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def create_feature_flag(
    project_id: str = Field(..., description="The unique identifier of the project where the feature flag will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    key: str | None = Field(None, description="A unique identifier for the feature flag used to reference it in your application code and API calls."),
    filters: _models.FeatureFlagsCreateBodyFilters | None = Field(None, description="Targeting rules that determine which users or cohorts will see this feature flag enabled. Defines the rollout strategy and audience segmentation."),
    active: bool | None = Field(None, description="Whether the feature flag is currently active and should be evaluated. Inactive flags are not applied to users."),
    tags: list[str] | None = Field(None, description="One or more organizational tags to categorize and organize the feature flag for easier management and discovery."),
) -> dict[str, Any]:
    """Create a new feature flag in a project with targeting configuration, activation status, and organizational tags. Feature flags enable controlled rollout and A/B testing of features."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsCreateRequest(
            path=_models.FeatureFlagsCreateRequestPath(project_id=project_id),
            body=_models.FeatureFlagsCreateRequestBody(key=key, filters=filters, active=active, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_feature_flag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_feature_flag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_feature_flag", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_feature_flag",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flag(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag to retrieve. This is a positive integer assigned by the system."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific feature flag by its ID within a project. Use this to fetch detailed configuration and status information for a feature flag."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsRetrieveRequest(
            path=_models.FeatureFlagsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flag", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flag",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def update_feature_flag(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag to update."),
    project_id: str = Field(..., description="The project ID containing the feature flag. Retrieve available project IDs from the /api/projects/ endpoint."),
    key: str = Field(..., description="A unique key identifier for the feature flag, up to 400 characters. Used to reference the flag in your application code.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Targeting rules and conditions that determine which users or groups receive this feature flag. Defines the audience segmentation logic."),
    active: bool | None = Field(None, description="Whether the feature flag is currently active and should be evaluated. Inactive flags are not applied to users."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures users see consistent flag variants across sessions by persisting their assignment, preventing experience disruption."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger an automatic rollback of the feature flag if specified metrics or thresholds are breached."),
    performed_rollback: bool | None = Field(None, description="Indicates whether a rollback has been executed for this feature flag."),
    tags: list[Any] | None = Field(None, description="Labels or categories for organizing and filtering feature flags. Supports multiple tags as an array of strings."),
    analytics_dashboards: list[int] | None = Field(None, description="Analytics dashboards associated with this feature flag for monitoring performance and user impact. Array of dashboard identifiers."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies where the feature flag logic is evaluated: on the server (backend), client (frontend), or both. Determines deployment architecture."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="The user attribute used to consistently assign users to flag variants. Choose between distinct_id (user ID, default) or device_id for device-based bucketing."),
) -> dict[str, Any]:
    """Update an existing feature flag configuration, including its targeting rules, rollout strategy, and evaluation settings. Allows modification of flag state, filters, variants, and deployment parameters."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsUpdateRequest(
            path=_models.FeatureFlagsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.FeatureFlagsUpdateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_feature_flag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_feature_flag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_feature_flag", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_feature_flag",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def update_feature_flag_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag to update."),
    project_id: str = Field(..., description="The project ID containing the feature flag. Find your project ID by calling the /api/projects/ endpoint."),
    key: str | None = Field(None, description="The feature flag's unique key identifier used to reference it in your application."),
    filters: _models.FeatureFlagsPartialUpdateBodyFilters | None = Field(None, description="The targeting configuration that determines which users or groups see this feature flag. Defines the rules and conditions for flag evaluation."),
    active: bool | None = Field(None, description="Whether the feature flag is currently active and should be evaluated. Inactive flags are not applied to users."),
    tags: list[str] | None = Field(None, description="An array of organizational tags for categorizing and organizing this feature flag."),
) -> dict[str, Any]:
    """Partially update a feature flag's configuration, including its active status, targeting rules, key, and organizational tags."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsPartialUpdateRequest(
            path=_models.FeatureFlagsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.FeatureFlagsPartialUpdateRequestBody(key=key, filters=filters, active=active, tags=tags)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_feature_flag_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_feature_flag_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_feature_flag_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_feature_flag_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flag_activity(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can obtain this by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="The number of activity records to return per page. Must be at least 1, defaults to 10 records per page.", ge=1),
    page: int | None = Field(None, description="The page number of results to retrieve, starting from 1. Use this to navigate through paginated activity records.", ge=1),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific feature flag, including all changes and events. Results are paginated to allow browsing through activity records."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsActivityRetrieve2Request(
            path=_models.FeatureFlagsActivityRetrieve2RequestPath(id_=id_, project_id=project_id),
            query=_models.FeatureFlagsActivityRetrieve2RequestQuery(limit=limit, page=page)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flag_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/activity"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flag_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flag_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flag_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def create_static_cohort_for_feature_flag(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag to associate with the static cohort."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. Retrieve this from the /api/projects/ endpoint if needed."),
    key: str = Field(..., description="A unique key identifying the static cohort. Maximum length is 400 characters.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Targeting rules and conditions that define which users belong to this cohort. Specified as a JSON object with filter criteria."),
    active: bool | None = Field(None, description="Whether this cohort is currently active and should be applied to the feature flag."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures users maintain consistent feature flag experiences across sessions and devices."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger automatic rollback of the feature flag if specified thresholds are met."),
    performed_rollback: bool | None = Field(None, description="Indicates whether a rollback has already been executed for this feature flag."),
    tags: list[Any] | None = Field(None, description="Labels or tags for organizing and categorizing this cohort. Specified as an array of string values."),
    analytics_dashboards: list[int] | None = Field(None, description="Analytics dashboards to associate with this cohort for tracking and monitoring. Specified as an array of dashboard identifiers."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies where the feature flag should be evaluated: on the server, client-side, or both environments."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="The user identifier field used for bucketing users into cohort variants. Use 'distinct_id' for user IDs (default) or 'device_id' for device-based bucketing."),
) -> dict[str, Any]:
    """Create a static cohort associated with a feature flag. This enables you to define a fixed group of users for targeting or analysis purposes within a feature flag's rollout strategy."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsCreateStaticCohortForFlagCreateRequest(
            path=_models.FeatureFlagsCreateStaticCohortForFlagCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.FeatureFlagsCreateStaticCohortForFlagCreateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_static_cohort_for_feature_flag: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/create_static_cohort_for_flag", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/create_static_cohort_for_flag"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_static_cohort_for_feature_flag")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_static_cohort_for_feature_flag", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_static_cohort_for_feature_flag",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def create_feature_flag_dashboard(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the feature flag to create a dashboard for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. Retrieve available project IDs from the /api/projects/ endpoint."),
    key: str = Field(..., description="A unique key identifier for the feature flag, up to 400 characters in length. Used to reference the flag in your application code.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Targeting rules and conditions that determine which users or segments see this feature flag. Defined as a structured object with filter criteria."),
    active: bool | None = Field(None, description="Boolean flag indicating whether this feature flag is currently active and should be evaluated for users."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures users see consistent flag values across sessions to maintain a continuous experience during rollouts."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger an automatic rollback of the feature flag if specified thresholds or metrics are breached."),
    performed_rollback: bool | None = Field(None, description="Boolean flag indicating whether a rollback has been performed on this feature flag."),
    tags: list[Any] | None = Field(None, description="Array of tags for organizing and categorizing the feature flag for easier management and filtering."),
    analytics_dashboards: list[int] | None = Field(None, description="Array of analytics dashboard identifiers to associate with this feature flag for tracking and visualization purposes."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies the evaluation context for this feature flag: 'server' for backend evaluation, 'client' for frontend evaluation, or 'all' for both contexts."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="The identifier used to bucket users into flag variants and rollout groups: 'distinct_id' for user IDs (default) or 'device_id' for device-based bucketing."),
) -> dict[str, Any]:
    """Create a dashboard configuration for a feature flag, enabling analytics tracking and visualization of flag performance across your project."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsDashboardCreateRequest(
            path=_models.FeatureFlagsDashboardCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.FeatureFlagsDashboardCreateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_feature_flag_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/dashboard", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/dashboard"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_feature_flag_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_feature_flag_dashboard", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_feature_flag_dashboard",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def list_dependent_flags(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the feature flag to check for dependents."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all active feature flags that depend on the specified flag. This helps identify downstream impacts when modifying or disabling a flag."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsDependentFlagsListRequest(
            path=_models.FeatureFlagsDependentFlagsListRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_dependent_flags: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/dependent_flags", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/dependent_flags"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_dependent_flags")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_dependent_flags", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_dependent_flags",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def create_feature_flag_usage_dashboard(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the feature flag to create a usage dashboard for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. Retrieve this from the /api/projects/ endpoint if needed."),
    key: str = Field(..., description="A unique key identifier for the feature flag, up to 400 characters in length.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Targeting rules and conditions that determine which users or groups see this feature flag variant."),
    active: bool | None = Field(None, description="Whether the feature flag is currently active and should be evaluated for users."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures users see consistent flag variants across sessions to maintain experience continuity."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger an automatic rollback of the feature flag if specified thresholds are met."),
    performed_rollback: bool | None = Field(None, description="Indicates whether a rollback has been performed on this feature flag."),
    tags: list[Any] | None = Field(None, description="Labels or tags for organizing and categorizing the feature flag for easier management and discovery."),
    analytics_dashboards: list[int] | None = Field(None, description="Array of analytics dashboard identifiers to associate with this feature flag for usage tracking and visualization."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies where the feature flag logic should be evaluated: on the server, client-side, or both. Defaults to server evaluation."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="The user identifier field used for bucketing users into rollout percentages and variants. Defaults to distinct_id (user ID); can also use device_id for device-based bucketing."),
) -> dict[str, Any]:
    """Create a usage dashboard for a feature flag to track its performance and adoption metrics. This enriches the feature flag with analytics data visualization."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsEnrichUsageDashboardCreateRequest(
            path=_models.FeatureFlagsEnrichUsageDashboardCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.FeatureFlagsEnrichUsageDashboardCreateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_feature_flag_usage_dashboard: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/enrich_usage_dashboard", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/enrich_usage_dashboard"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_feature_flag_usage_dashboard")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_feature_flag_usage_dashboard", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_feature_flag_usage_dashboard",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flag_remote_config(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag. This is a positive integer that uniquely identifies the feature flag within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the remote configuration settings for a specific feature flag. This returns the configuration data associated with the feature flag that can be used to control its behavior across your application."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsRemoteConfigRetrieveRequest(
            path=_models.FeatureFlagsRemoteConfigRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flag_remote_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/remote_config", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/remote_config"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flag_remote_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flag_remote_config", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flag_remote_config",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flag_status(
    id_: int = Field(..., alias="id", description="The unique identifier of the feature flag you want to retrieve status for. This is a positive integer assigned by the system."),
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the current status and configuration of a specific feature flag within a project. Use this to check if a feature flag is enabled, its rollout percentage, and other active settings."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsStatusRetrieveRequest(
            path=_models.FeatureFlagsStatusRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flag_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/{id}/status", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/{id}/status"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flag_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flag_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flag_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def list_feature_flags_activity(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of activity records to return per page. Must be at least 1; defaults to 10 if not specified.", ge=1),
    page: int | None = Field(None, description="The page number to retrieve, starting from 1. Defaults to the first page if not specified.", ge=1),
) -> dict[str, Any]:
    """Retrieve a paginated list of feature flag activity events for a project. Use this to audit changes and track the history of feature flag modifications."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsActivityRetrieveRequest(
            path=_models.FeatureFlagsActivityRetrieveRequestPath(project_id=project_id),
            query=_models.FeatureFlagsActivityRetrieveRequestQuery(limit=limit, page=page)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_feature_flags_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/activity"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_feature_flags_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_feature_flags_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_feature_flags_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def delete_feature_flags_bulk(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flags to delete. Retrieve project IDs from the /api/projects/ endpoint."),
    key: str = Field(..., description="The feature flag key identifier to delete. Maximum length of 400 characters.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Filter object to match flags for deletion using the same filter parameters available on the list endpoint (e.g., search, active, type). Use this to delete flags matching specific criteria instead of providing explicit IDs."),
    active: bool | None = Field(None, description="Filter flags by their active status. When true, only active flags are deleted; when false, only inactive flags are deleted."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures user experience continuity during flag deletion by maintaining consistent flag state across sessions."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger automatic rollback of the flag deletion if specified criteria are met during or after the operation."),
    performed_rollback: bool | None = Field(None, description="Indicates whether a rollback of the flag deletion has already been performed."),
    tags: list[Any] | None = Field(None, description="Array of tag strings to filter flags for deletion. Only flags matching all specified tags will be deleted."),
    analytics_dashboards: list[int] | None = Field(None, description="Array of analytics dashboard identifiers associated with the flags to be deleted."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies the evaluation context for the feature flag: server-side evaluation, client-side evaluation, or both."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="Identifier used for user bucketing in rollouts and variants. Choose between distinct_id (user ID, default) or device_id (device identifier)."),
) -> dict[str, Any]:
    """Bulk delete multiple feature flags from a project using filter criteria or explicit flag IDs. Uses batched database operations for efficiency with a single cache invalidation at the end."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsBulkDeleteCreateRequest(
            path=_models.FeatureFlagsBulkDeleteCreateRequestPath(project_id=project_id),
            body=_models.FeatureFlagsBulkDeleteCreateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_feature_flags_bulk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/bulk_delete", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/bulk_delete"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_feature_flags_bulk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_feature_flags_bulk", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_feature_flags_bulk",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def create_feature_flags_bulk(
    project_id: str = Field(..., description="The unique identifier of the project where feature flags will be created. Retrieve your project ID by calling the /api/projects/ endpoint."),
    key: str = Field(..., description="The unique identifier for the feature flag. Must not exceed 400 characters.", max_length=400),
    filters: dict[str, Any] | None = Field(None, description="Optional filtering criteria to apply to the feature flags being created."),
    active: bool | None = Field(None, description="Whether the feature flag should be active and available for evaluation upon creation."),
    ensure_experience_continuity: bool | None = Field(None, description="When enabled, ensures that users maintain consistent feature flag experiences across sessions and devices."),
    rollback_conditions: Any | None = Field(None, description="Conditions that trigger automatic rollback of the feature flag if specified thresholds are met."),
    performed_rollback: bool | None = Field(None, description="Indicates whether a rollback has already been performed on this feature flag."),
    tags: list[Any] | None = Field(None, description="An array of tags for organizing and categorizing the feature flag for easier management and discovery."),
    analytics_dashboards: list[int] | None = Field(None, description="An array of analytics dashboard identifiers to connect this feature flag with specific dashboards for monitoring and reporting."),
    evaluation_runtime: Literal["server", "client", "all"] | Literal[""] | None = Field(None, description="Specifies the evaluation context for this feature flag: 'server' for server-side evaluation, 'client' for client-side evaluation, or 'all' for both contexts."),
    bucketing_identifier: Literal["distinct_id", "device_id"] | Literal[""] | None = Field(None, description="The identifier used to determine which variant a user receives. Use 'distinct_id' for user-based bucketing (default) or 'device_id' for device-based bucketing."),
) -> dict[str, Any]:
    """Create multiple feature flags in bulk by providing their keys and configuration. This operation accepts a list of feature flag definitions and returns the created flags with their assigned IDs and keys."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsBulkKeysCreateRequest(
            path=_models.FeatureFlagsBulkKeysCreateRequestPath(project_id=project_id),
            body=_models.FeatureFlagsBulkKeysCreateRequestBody(key=key, filters=filters, active=active, ensure_experience_continuity=ensure_experience_continuity, rollback_conditions=rollback_conditions, performed_rollback=performed_rollback, tags=tags, analytics_dashboards=analytics_dashboards, evaluation_runtime=evaluation_runtime, bucketing_identifier=bucketing_identifier)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_feature_flags_bulk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/bulk_keys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/bulk_keys"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_feature_flags_bulk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_feature_flags_bulk", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_feature_flags_bulk",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flag_evaluation_reasons(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flags. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    distinct_id: str = Field(..., description="The unique identifier for the user whose feature flag evaluation reasons you want to retrieve. Must be at least 1 character long.", min_length=1),
    groups: str | None = Field(None, description="Optional group memberships for the user, provided as a JSON object string. Used to evaluate feature flags that target specific groups. Defaults to an empty object if not provided."),
) -> dict[str, Any]:
    """Retrieve the evaluation reasons for feature flags for a specific user, optionally considering group memberships. This helps understand why a feature flag was enabled or disabled for that user."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsEvaluationReasonsRetrieveRequest(
            path=_models.FeatureFlagsEvaluationReasonsRetrieveRequestPath(project_id=project_id),
            query=_models.FeatureFlagsEvaluationReasonsRetrieveRequestQuery(distinct_id=distinct_id, groups=groups)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flag_evaluation_reasons: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/evaluation_reasons", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/evaluation_reasons"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flag_evaluation_reasons")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flag_evaluation_reasons", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flag_evaluation_reasons",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def get_feature_flags_for_local_evaluation(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flags. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    send_cohorts: bool | None = Field(None, description="When enabled, includes cohort definitions in the response to support flag rules that reference cohorts. Defaults to false if not specified."),
) -> dict[str, Any]:
    """Retrieve feature flags configured for local evaluation within a project. This endpoint returns flag definitions optimized for client-side evaluation, with optional cohort data inclusion."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsLocalEvaluationRetrieveRequest(
            path=_models.FeatureFlagsLocalEvaluationRetrieveRequestPath(project_id=project_id),
            query=_models.FeatureFlagsLocalEvaluationRetrieveRequestQuery(send_cohorts=send_cohorts)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_feature_flags_for_local_evaluation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/local_evaluation", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/local_evaluation"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_feature_flags_for_local_evaluation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_feature_flags_for_local_evaluation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_feature_flags_for_local_evaluation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def list_feature_flag_ids_by_filters(project_id: str = Field(..., description="The unique identifier of the project containing the feature flags. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve IDs of all feature flags in a project that match the current filter criteria. Only returns IDs for flags the user has permission to edit, using the same filtering logic as the standard feature flags list endpoint."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsMatchingIdsRetrieveRequest(
            path=_models.FeatureFlagsMatchingIdsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_feature_flag_ids_by_filters: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/matching_ids", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/matching_ids"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_feature_flag_ids_by_filters")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_feature_flag_ids_by_filters", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_feature_flag_ids_by_filters",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def list_feature_flags_for_user(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flags. Obtain this by calling the /api/projects/ endpoint if you don't have it."),
    groups: str | None = Field(None, description="Optional groups for feature flag evaluation, provided as a JSON object string. Defaults to an empty object if not specified."),
) -> dict[str, Any]:
    """Retrieve feature flags applicable to the current user or specified groups. Use this to evaluate which feature flags are enabled for a given user context, typically before making application decisions."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsMyFlagsRetrieveRequest(
            path=_models.FeatureFlagsMyFlagsRetrieveRequestPath(project_id=project_id),
            query=_models.FeatureFlagsMyFlagsRetrieveRequestQuery(groups=groups)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_feature_flags_for_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/my_flags", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/my_flags"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_feature_flags_for_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_feature_flags_for_user", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_feature_flags_for_user",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: feature_flags, feature_flags
@mcp.tool()
async def evaluate_feature_flag_user_blast_radius(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. Retrieve this ID from the /api/projects/ endpoint if unknown."),
    condition: dict[str, Any] = Field(..., description="The release condition logic to evaluate against the user. This defines the targeting rules and rollout percentage that determine flag eligibility."),
    group_type_index: int | None = Field(None, description="The group type index for evaluating group-based feature flags. Omit this for person-based flags, or provide the index when evaluating flags that target user groups."),
) -> dict[str, Any]:
    """Evaluate the blast radius (impact scope) of a feature flag for a specific user by testing a release condition. This determines which users would be affected by the flag's rollout."""

    # Construct request model with validation
    try:
        _request = _models.FeatureFlagsUserBlastRadiusCreateRequest(
            path=_models.FeatureFlagsUserBlastRadiusCreateRequestPath(project_id=project_id),
            body=_models.FeatureFlagsUserBlastRadiusCreateRequestBody(condition=condition, group_type_index=group_type_index)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for evaluate_feature_flag_user_blast_radius: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/feature_flags/user_blast_radius", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/feature_flags/user_blast_radius"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("evaluate_feature_flag_user_blast_radius")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("evaluate_feature_flag_user_blast_radius", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="evaluate_feature_flag_user_blast_radius",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, file_system
@mcp.tool()
async def update_file_system(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system resource to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    path: str | None = Field(None, description="The file system path or location identifier."),
    ref: str | None = Field(None, description="A reference identifier for the file system, limited to 100 characters maximum.", max_length=100),
    href: str | None = Field(None, description="A hyperlink reference or URL associated with the file system resource."),
    meta: Any | None = Field(None, description="Additional metadata or properties associated with the file system as a structured object."),
    shortcut: bool | None = Field(None, description="A boolean flag indicating whether this file system resource is a shortcut or alias to another resource."),
) -> dict[str, Any]:
    """Partially update a file system resource within a project. Allows modification of specific file system properties such as path, reference, href, metadata, and shortcut status."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemPartialUpdateRequest(
            path=_models.FileSystemPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.FileSystemPartialUpdateRequestBody(path=path, ref=ref, href=href, meta=meta, shortcut=shortcut)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_file_system: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_file_system")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_file_system", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_file_system",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, file_system
@mcp.tool()
async def delete_file_system(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system. Retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Permanently delete a file system from a project. This operation removes the file system resource identified by its UUID from the specified project."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemDestroyRequest(
            path=_models.FileSystemDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_file_system: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_file_system")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_file_system", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_file_system",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, file_system
@mcp.tool()
async def create_file_system_log_view(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this ID by calling the projects list endpoint if you don't have it."),
    path: str = Field(..., description="The file system path for which to create the log view. This specifies the target location or resource being logged."),
    ref: str | None = Field(None, description="An optional reference identifier or version tag for the log view, up to 100 characters in length.", max_length=100),
    href: str | None = Field(None, description="An optional hyperlink reference or URI associated with the log view."),
    meta: Any | None = Field(None, description="Optional metadata object to attach additional context or configuration details to the log view."),
    shortcut: bool | None = Field(None, description="Optional boolean flag to indicate whether this log view should be created as a shortcut or quick-access reference."),
) -> dict[str, Any]:
    """Create a new log view for a file system within a project. This operation allows you to configure and persist a customized view of file system logs with optional filtering and metadata."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemLogViewCreateRequest(
            path=_models.FileSystemLogViewCreateRequestPath(project_id=project_id),
            body=_models.FileSystemLogViewCreateRequestBody(path=path, ref=ref, href=href, meta=meta, shortcut=shortcut)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_file_system_log_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system/log_view", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system/log_view"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_file_system_log_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_file_system_log_view", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_file_system_log_view",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, file_system
@mcp.tool()
async def list_unfiled_files(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve all files that have not been organized into folders within a project. This operation returns unstructured files at the root level of the project's file system."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemUnfiledRetrieveRequest(
            path=_models.FileSystemUnfiledRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_unfiled_files: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system/unfiled", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system/unfiled"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_unfiled_files")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_unfiled_files", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_unfiled_files",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def list_file_system_shortcuts(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of shortcuts to return in a single response page. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of file system shortcuts configured for a specific project. Use pagination parameters to control result set size and offset."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutListRequest(
            path=_models.FileSystemShortcutListRequestPath(project_id=project_id),
            query=_models.FileSystemShortcutListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_file_system_shortcuts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_file_system_shortcuts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_file_system_shortcuts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_file_system_shortcuts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def create_file_system_shortcut(
    project_id: str = Field(..., description="The unique identifier of the project where the shortcut will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    path: str = Field(..., description="The file system path that the shortcut points to. This is the target location the shortcut will reference."),
    ref: str | None = Field(None, description="An optional reference identifier for the shortcut, limited to 100 characters maximum. Use this to tag or categorize the shortcut for organizational purposes.", max_length=100),
    href: str | None = Field(None, description="An optional hyperlink or URL associated with the shortcut, providing additional context or navigation information."),
) -> dict[str, Any]:
    """Creates a file system shortcut within a project, enabling quick access to a specific file path with optional reference and hyperlink metadata."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutCreateRequest(
            path=_models.FileSystemShortcutCreateRequestPath(project_id=project_id),
            body=_models.FileSystemShortcutCreateRequestBody(path=path, ref=ref, href=href)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_file_system_shortcut: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_file_system_shortcut")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_file_system_shortcut", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_file_system_shortcut",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def get_file_system_shortcut(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system shortcut to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system shortcut. You can obtain the project ID by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific file system shortcut by its unique identifier within a project. Use this to access details about a saved shortcut path or alias."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutRetrieveRequest(
            path=_models.FileSystemShortcutRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_file_system_shortcut: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_file_system_shortcut")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_file_system_shortcut", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_file_system_shortcut",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def update_file_system_shortcut(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system shortcut to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system shortcut. Retrieve available project IDs from the /api/projects/ endpoint."),
    path: str = Field(..., description="The file system path that the shortcut points to or represents."),
    ref: str | None = Field(None, description="An optional reference identifier for the shortcut, up to 100 characters in length.", max_length=100),
    href: str | None = Field(None, description="An optional hyperlink reference or URL associated with the shortcut."),
) -> dict[str, Any]:
    """Update an existing file system shortcut within a project. Modify the path, reference, or href properties of the shortcut to reflect changes in your file system configuration."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutUpdateRequest(
            path=_models.FileSystemShortcutUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.FileSystemShortcutUpdateRequestBody(path=path, ref=ref, href=href)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_file_system_shortcut: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_file_system_shortcut")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_file_system_shortcut", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_file_system_shortcut",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def update_file_system_shortcut_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system shortcut to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system shortcut. Retrieve available project IDs from the /api/projects/ endpoint."),
    path: str | None = Field(None, description="The file system path that the shortcut points to."),
    ref: str | None = Field(None, description="A reference identifier for the shortcut, up to 100 characters in length.", max_length=100),
    href: str | None = Field(None, description="The hyperlink reference or URL associated with the shortcut."),
) -> dict[str, Any]:
    """Partially update a file system shortcut within a project. Allows modification of the shortcut's path, reference, or href properties."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutPartialUpdateRequest(
            path=_models.FileSystemShortcutPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.FileSystemShortcutPartialUpdateRequestBody(path=path, ref=ref, href=href)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_file_system_shortcut_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_file_system_shortcut_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_file_system_shortcut_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_file_system_shortcut_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: file_system_shortcut
@mcp.tool()
async def delete_file_system_shortcut(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the file system shortcut to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the file system shortcut. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a file system shortcut from a project. This operation removes the shortcut configuration and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.FileSystemShortcutDestroyRequest(
            path=_models.FileSystemShortcutDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_file_system_shortcut: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/file_system_shortcut/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/file_system_shortcut/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_file_system_shortcut")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_file_system_shortcut", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_file_system_shortcut",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: flag_value
@mcp.tool()
async def list_feature_flag_values(
    project_id: str = Field(..., description="The unique identifier of the project containing the feature flag. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    key: str | None = Field(None, description="The unique identifier of the feature flag for which to retrieve possible values."),
) -> dict[str, Any]:
    """Retrieve all possible values that can be assigned to a specific feature flag within a project."""

    # Construct request model with validation
    try:
        _request = _models.FlagValueValuesRetrieveRequest(
            path=_models.FlagValueValuesRetrieveRequestPath(project_id=project_id),
            query=_models.FlagValueValuesRetrieveRequestQuery(key=key)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_feature_flag_values: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/flag_value/values", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/flag_value/values"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_feature_flag_values")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_feature_flag_values", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_feature_flag_values",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def list_groups_by_type(
    project_id: str = Field(..., description="The unique identifier of the project containing the groups. Retrieve available project IDs by calling the projects endpoint."),
    group_type_index: int = Field(..., description="The numeric index identifying which group type to list. Must correspond to a valid group type for the project; call the groups_types endpoint to see all available options."),
    search: str = Field(..., description="Filter groups by name using a search term. Matches group names containing the provided search string."),
) -> dict[str, Any]:
    """Retrieve all groups of a specified type within a project. Requires a valid group type index; use the groups_types endpoint to discover available group types for your project."""

    # Construct request model with validation
    try:
        _request = _models.GroupsListRequest(
            path=_models.GroupsListRequestPath(project_id=project_id),
            query=_models.GroupsListRequestQuery(group_type_index=group_type_index, search=search)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_groups_by_type: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_groups_by_type")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_groups_by_type", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_groups_by_type",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def create_group(
    project_id: str = Field(..., description="The unique identifier of the project where the group will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    group_type_index: int = Field(..., description="The type index that determines the group's classification or behavior. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    group_key: str = Field(..., description="A unique identifier or name for the group. Must not exceed 400 characters in length.", max_length=400),
    group_properties: Any | None = Field(None, description="Optional configuration object containing additional properties or metadata for the group. Structure and valid properties depend on the group type."),
) -> dict[str, Any]:
    """Create a new group within a project. Groups are organizational units that can be configured with specific types and properties."""

    # Construct request model with validation
    try:
        _request = _models.GroupsCreateRequest(
            path=_models.GroupsCreateRequestPath(project_id=project_id),
            body=_models.GroupsCreateRequestBody(group_type_index=group_type_index, group_key=group_key, group_properties=group_properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_group: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_group")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_group", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_group",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def list_user_group_activity(
    project_id: str = Field(..., description="The unique identifier of the project. Obtain this by calling the /api/projects/ endpoint to list available projects."),
    group_type_index: int = Field(..., description="The numeric index identifying the group type to filter activity results by. This determines which category of groups to retrieve activity for."),
    id_: str = Field(..., alias="id", description="The unique identifier of the user whose group activity should be retrieved."),
) -> dict[str, Any]:
    """Retrieve activity records for a specific user within a project group. Returns activity data filtered by the specified group type and user ID."""

    # Construct request model with validation
    try:
        _request = _models.GroupsActivityRetrieveRequest(
            path=_models.GroupsActivityRetrieveRequestPath(project_id=project_id),
            query=_models.GroupsActivityRetrieveRequestQuery(group_type_index=group_type_index, id_=id_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_user_group_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups/activity"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_user_group_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_user_group_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_user_group_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def delete_group_property(
    project_id: str = Field(..., description="The unique identifier of the project containing the group. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    group_key: str = Field(..., description="The unique key identifying the group from which to delete the property."),
    group_type_index: int = Field(..., description="The numeric type index of the group, used to distinguish between different group type classifications."),
    group_key2: str = Field(..., alias="group_key", description="The key of the group property to delete. Must not exceed 400 characters.", max_length=400),
    group_type_index2: int = Field(..., alias="group_type_index", description="The numeric type index corresponding to the group type. Must be a valid 32-bit signed integer.", ge=-2147483648, le=2147483647),
    group_properties: Any | None = Field(None, description="Optional collection of group properties to be deleted or modified as part of this operation."),
) -> dict[str, Any]:
    """Delete a property from a specific group within a project. Requires identifying the group by its key and type index."""

    # Construct request model with validation
    try:
        _request = _models.GroupsDeletePropertyCreateRequest(
            path=_models.GroupsDeletePropertyCreateRequestPath(project_id=project_id),
            query=_models.GroupsDeletePropertyCreateRequestQuery(group_key=group_key, group_type_index=group_type_index),
            body=_models.GroupsDeletePropertyCreateRequestBody(group_key2=group_key2, group_type_index2=group_type_index2, group_properties=group_properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_group_property: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups/delete_property", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups/delete_property"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_group_property")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_group_property", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_group_property",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def get_group_by_key_and_type(
    project_id: str = Field(..., description="The unique identifier of the project containing the group. Retrieve available project IDs by calling the projects list endpoint."),
    group_key: str = Field(..., description="The unique key that identifies the group within the project."),
    group_type_index: int = Field(..., description="The numeric index representing the group's type classification within the project."),
) -> dict[str, Any]:
    """Retrieve a specific group within a project by its key and type index. Use this to look up an individual group's details when you know its identifier and type classification."""

    # Construct request model with validation
    try:
        _request = _models.GroupsFindRetrieveRequest(
            path=_models.GroupsFindRetrieveRequestPath(project_id=project_id),
            query=_models.GroupsFindRetrieveRequestQuery(group_key=group_key, group_type_index=group_type_index)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_group_by_key_and_type: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups/find", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups/find"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_group_by_key_and_type")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_group_by_key_and_type", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_group_by_key_and_type",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def list_group_property_definitions(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve all property definitions for groups within a specific project. Property definitions define the custom fields and attributes available for group objects."""

    # Construct request model with validation
    try:
        _request = _models.GroupsPropertyDefinitionsRetrieveRequest(
            path=_models.GroupsPropertyDefinitionsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_group_property_definitions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups/property_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups/property_definitions"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_group_property_definitions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_group_property_definitions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_group_property_definitions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, groups
@mcp.tool()
async def list_related_groups_for_user(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    group_type_index: int = Field(..., description="The numeric index identifying the group type to filter results. Specify which category or classification of groups to retrieve."),
    id_: str = Field(..., alias="id", description="The unique identifier of the user whose related groups should be retrieved."),
) -> dict[str, Any]:
    """Retrieve groups related to a specific user within a project, filtered by group type. Use this to discover group associations for a user based on their role or group classification."""

    # Construct request model with validation
    try:
        _request = _models.GroupsRelatedRetrieveRequest(
            path=_models.GroupsRelatedRetrieveRequestPath(project_id=project_id),
            query=_models.GroupsRelatedRetrieveRequestQuery(group_type_index=group_type_index, id_=id_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_related_groups_for_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups/related", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups/related"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_related_groups_for_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_related_groups_for_user", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_related_groups_for_user",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def list_group_types(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list all available projects.")) -> dict[str, Any]:
    """Retrieve all available group types for a specific project. Group types define the categories or classifications that can be applied to groups within the project."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesListRequest(
            path=_models.GroupsTypesListRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_group_types: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_group_types")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_group_types", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_group_types",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def delete_group_type(
    group_type_index: int = Field(..., description="The index identifier of the group type to delete. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a specific group type from a project. This operation removes the group type configuration identified by its index within the specified project."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesDestroyRequest(
            path=_models.GroupsTypesDestroyRequestPath(group_type_index=group_type_index, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_group_type: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_group_type")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_group_type", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_group_type",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def list_metrics_for_group_type(
    group_type_index: int = Field(..., description="The index identifier for the group type within the project. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of metric results to return in a single page of results. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based starting position for returning results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve metrics associated with a specific group type within a project. Results are paginated and can be filtered by limit and offset parameters."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsListRequest(
            path=_models.GroupsTypesMetricsListRequestPath(group_type_index=group_type_index, project_id=project_id),
            query=_models.GroupsTypesMetricsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_metrics_for_group_type: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_metrics_for_group_type")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_metrics_for_group_type", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_metrics_for_group_type",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def create_group_type_metric(
    group_type_index: int = Field(..., description="The index identifier of the group type within the project. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name for the metric. Must not exceed 255 characters.", max_length=255),
    filters: Any = Field(..., description="Filter criteria to determine which data points are included in the metric calculation. Specify as a structured filter object."),
    interval: int | None = Field(None, description="The time interval for metric aggregation, specified in days. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    display: Literal["number", "sparkline"] | None = Field(None, description="The visual representation format for the metric. Choose 'number' for a single numeric value or 'sparkline' for a compact trend visualization."),
) -> dict[str, Any]:
    """Create a new metric for a specific group type within a project. Metrics track quantitative data over time with configurable display formats and filtering options."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsCreateRequest(
            path=_models.GroupsTypesMetricsCreateRequestPath(group_type_index=group_type_index, project_id=project_id),
            body=_models.GroupsTypesMetricsCreateRequestBody(name=name, interval=interval, display=display, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_group_type_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_group_type_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_group_type_metric", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_group_type_metric",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def get_group_type_metric(
    group_type_index: int = Field(..., description="The index identifying the group type within the project. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the group usage metric to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type. Obtain this ID by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific usage metric for a group type within a project. Returns detailed metric information identified by its UUID."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsRetrieveRequest(
            path=_models.GroupsTypesMetricsRetrieveRequestPath(group_type_index=group_type_index, id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_group_type_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_group_type_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_group_type_metric", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_group_type_metric",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def update_group_type_metric(
    group_type_index: int = Field(..., description="The index position of the group type within the project's group type collection.", ge=-2147483648, le=2147483647),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the metric to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type and metric."),
    name: str = Field(..., description="The display name for the metric. Maximum 255 characters.", max_length=255),
    filters: Any = Field(..., description="Filter criteria applied to the metric data. Defines which data points are included in the metric calculation."),
    interval: int | None = Field(None, description="The aggregation interval for the metric measurement, specified in days.", ge=-2147483648, le=2147483647),
    display: Literal["number", "sparkline"] | None = Field(None, description="The visualization format for displaying the metric: either 'number' for a single numeric value or 'sparkline' for a trend visualization."),
) -> dict[str, Any]:
    """Update a specific metric configuration for a group type within a project. Allows modification of metric display settings, aggregation intervals, and filtering criteria."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsUpdateRequest(
            path=_models.GroupsTypesMetricsUpdateRequestPath(group_type_index=group_type_index, id_=id_, project_id=project_id),
            body=_models.GroupsTypesMetricsUpdateRequestBody(name=name, interval=interval, display=display, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_group_type_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_group_type_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_group_type_metric", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_group_type_metric",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def update_group_type_metric_partial(
    group_type_index: int = Field(..., description="The index identifying the group type within the project. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the group usage metric to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the metric. Retrieve available project IDs from the /api/projects/ endpoint."),
    interval: int | None = Field(None, description="The time interval for metric aggregation, specified in days. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    display: Literal["number", "sparkline"] | None = Field(None, description="The display format for the metric: either 'number' for numeric display or 'sparkline' for a compact trend visualization."),
    filters: Any | None = Field(None, description="Filter criteria to apply to the metric data. Specify as a structured object matching the API's filter schema."),
) -> dict[str, Any]:
    """Partially update a group type metric configuration, allowing you to modify display settings, time interval, and filters for a specific metric within a project."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsPartialUpdateRequest(
            path=_models.GroupsTypesMetricsPartialUpdateRequestPath(group_type_index=group_type_index, id_=id_, project_id=project_id),
            body=_models.GroupsTypesMetricsPartialUpdateRequestBody(interval=interval, display=display, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_group_type_metric_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_group_type_metric_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_group_type_metric_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_group_type_metric_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def delete_group_type_metric(
    group_type_index: int = Field(..., description="The index identifier for the group type within the project. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the group usage metric to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the group type and metric. Retrieve available project IDs from the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a specific usage metric associated with a group type within a project. This removes the metric record permanently."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesMetricsDestroyRequest(
            path=_models.GroupsTypesMetricsDestroyRequestPath(group_type_index=group_type_index, id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_group_type_metric: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/{group_type_index}/metrics/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_group_type_metric")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_group_type_metric", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_group_type_metric",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: groups_types
@mcp.tool()
async def update_group_type_metadata(
    project_id: str = Field(..., description="The unique identifier of the project containing the group type to update. Retrieve available project IDs from the /api/projects/ endpoint."),
    name_singular: str | None = Field(None, description="The singular display name for the group type, up to 400 characters.", max_length=400),
    name_plural: str | None = Field(None, description="The plural display name for the group type, up to 400 characters.", max_length=400),
    detail_dashboard: int | None = Field(None, description="The numeric identifier for the detail dashboard configuration associated with this group type."),
    default_columns: list[str] | None = Field(None, description="An array specifying which columns should be displayed by default for this group type. Order may determine column sequence in the UI."),
) -> dict[str, Any]:
    """Partially update the metadata for a group type within a project, including display names, dashboard configuration, and column visibility settings."""

    # Construct request model with validation
    try:
        _request = _models.GroupsTypesUpdateMetadataPartialUpdateRequest(
            path=_models.GroupsTypesUpdateMetadataPartialUpdateRequestPath(project_id=project_id),
            body=_models.GroupsTypesUpdateMetadataPartialUpdateRequestBody(name_singular=name_singular, name_plural=name_plural, detail_dashboard=detail_dashboard, default_columns=default_columns)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_group_type_metadata: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/groups_types/update_metadata", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/groups_types/update_metadata"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_group_type_metadata")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_group_type_metadata", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_group_type_metadata",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: heatmap_screenshots
@mcp.tool()
async def get_heatmap_screenshot_content(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the heatmap screenshot whose content you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the heatmap screenshot. You can obtain project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the content of a saved heatmap screenshot for a specific project. Returns the heatmap image or data associated with the given screenshot ID."""

    # Construct request model with validation
    try:
        _request = _models.HeatmapScreenshotsContentRetrieveRequest(
            path=_models.HeatmapScreenshotsContentRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_heatmap_screenshot_content: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/heatmap_screenshots/{id}/content", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/heatmap_screenshots/{id}/content"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_heatmap_screenshot_content")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_heatmap_screenshot_content", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_heatmap_screenshot_content",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: heatmaps
@mcp.tool()
async def list_heatmaps(
    project_id: str = Field(..., description="The unique identifier of the project containing the heatmaps. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of heatmap records to return in a single response page."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of heatmaps for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.HeatmapsListRequest(
            path=_models.HeatmapsListRequestPath(project_id=project_id),
            query=_models.HeatmapsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_heatmaps: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/heatmaps", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/heatmaps"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_heatmaps")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_heatmaps", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_heatmaps",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def list_hog_flows(
    project_id: str = Field(..., description="The unique identifier of the project containing the HOG flows. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of HOG flows to return in a single response page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of HOG (HogQL) flows for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsListRequest(
            path=_models.HogFlowsListRequestPath(project_id=project_id),
            query=_models.HogFlowsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_flows: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_flows")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_flows", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_flows",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def create_hog_flow(
    project_id: str = Field(..., description="The unique identifier of the project where the flow will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="Required array of actions to execute within the flow. Actions are performed in sequence when flow conditions are met."),
    description: str | None = Field(None, description="Optional text description of the flow's purpose and behavior."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the flow: draft (not yet active), active (currently running), or archived (disabled and hidden)."),
    trigger: Any | None = Field(None, description="The event or condition that initiates the flow execution."),
    conversion: Any | None = Field(None, description="The goal event that marks successful completion of the flow."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the flow: on conversion completion, when trigger conditions fail, on either condition, or only at the flow's end."),
    edges: Any | None = Field(None, description="Connections between flow nodes defining the execution path. Order and structure determine flow logic."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variables available for use throughout the flow for dynamic behavior and data passing."),
) -> dict[str, Any]:
    """Create a new Hog flow within a project to define automated workflows with triggers, actions, and conversion tracking. Flows can be saved as drafts, activated, or archived."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsCreateRequest(
            path=_models.HogFlowsCreateRequestPath(project_id=project_id),
            body=_models.HogFlowsCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_hog_flow", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_hog_flow",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def get_hog_flow(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific Hog flow by its unique identifier within a project. Returns the complete configuration and metadata for the requested Hog flow."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsRetrieveRequest(
            path=_models.HogFlowsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_flow", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_flow",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def update_hog_flow(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this Hog flow. Retrieve available project IDs from the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="An ordered list of operations or tasks to execute within the flow. Each action represents a step in the workflow."),
    description: str | None = Field(None, description="A human-readable description of the Hog flow's purpose and behavior."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the flow: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The event or condition that initiates the Hog flow execution."),
    conversion: Any | None = Field(None, description="The target event or outcome that marks successful completion of the flow."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the flow: on conversion completion, when trigger conditions are no longer met, on either condition, or only at the workflow's final step."),
    edges: Any | None = Field(None, description="The connections and transitions between workflow steps, defining the flow's logical path."),
    variables: list[dict[str, str]] | None = Field(None, description="An ordered list of data variables available for use throughout the flow, supporting dynamic behavior and conditional logic."),
) -> dict[str, Any]:
    """Update an existing Hog flow configuration, including its trigger conditions, conversion criteria, exit logic, and associated actions. Allows modification of flow status, description, and workflow structure."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsUpdateRequest(
            path=_models.HogFlowsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFlowsUpdateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_flow", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_flow",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def update_hog_flow_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the Hog flow's purpose or behavior."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the Hog flow: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The event or condition that initiates the Hog flow execution."),
    conversion: Any | None = Field(None, description="The goal or outcome event that marks successful completion of the Hog flow."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when the Hog flow terminates: on conversion, when trigger conditions aren't met, on either condition, or only at the workflow's end."),
    edges: Any | None = Field(None, description="The connections between workflow nodes that define the flow's logical path and transitions."),
    actions: list[_models.HogFlowAction] | None = Field(None, description="An ordered list of operations or tasks executed as part of the Hog flow. Order determines execution sequence."),
    variables: list[dict[str, str]] | None = Field(None, description="An ordered list of data variables available throughout the Hog flow for use in conditions and actions."),
) -> dict[str, Any]:
    """Partially update a Hog flow configuration within a project. Modify workflow properties such as status, triggers, conversions, exit conditions, edges, actions, and variables."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsPartialUpdateRequest(
            path=_models.HogFlowsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFlowsPartialUpdateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_flow_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_flow_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_flow_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_flow_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def delete_hog_flow(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific Hog flow from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsDestroyRequest(
            path=_models.HogFlowsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_hog_flow", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_hog_flow",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def list_batch_jobs_for_hog_flow(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the hog flow whose batch jobs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the hog flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all batch jobs associated with a specific hog flow within a project. This operation returns a collection of batch job records for the given hog flow."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsBatchJobsRetrieveRequest(
            path=_models.HogFlowsBatchJobsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_batch_jobs_for_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/batch_jobs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/batch_jobs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_batch_jobs_for_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_batch_jobs_for_hog_flow", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_batch_jobs_for_hog_flow",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def create_batch_job_for_hog_flow(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow for which to create a batch job."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. Retrieve available project IDs from the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="An ordered list of actions to execute within the batch job workflow. Each action represents a step or operation performed on users matching the trigger conditions."),
    description: str | None = Field(None, description="Optional human-readable description of the batch job for reference and documentation purposes."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the batch job: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The trigger condition that initiates the batch job workflow for matching users or events."),
    conversion: Any | None = Field(None, description="The conversion event or goal that the batch job aims to achieve or track."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the batch job: on conversion completion, when trigger conditions are not met, on either condition, or only at the end of the workflow."),
    edges: Any | None = Field(None, description="The connections and flow paths between workflow nodes, defining how users progress through the batch job steps."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variables available for use throughout the batch job workflow, enabling dynamic configuration and personalization of actions."),
) -> dict[str, Any]:
    """Create a new batch job for a Hog flow, defining the workflow configuration including triggers, conversions, actions, and exit conditions to be executed across a set of users or events."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsBatchJobsCreateRequest(
            path=_models.HogFlowsBatchJobsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFlowsBatchJobsCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_batch_job_for_hog_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/batch_jobs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/batch_jobs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_batch_job_for_hog_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_batch_job_for_hog_flow", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_batch_job_for_hog_flow",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def create_hog_flow_invocation(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow to create an invocation for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. Retrieve available project IDs from the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="Required array of actions to execute within the flow. Each action defines a specific operation or step in the workflow sequence."),
    description: str | None = Field(None, description="Optional human-readable description of the invocation's purpose or configuration."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the invocation: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The trigger condition that initiates the flow invocation. Defines the event or condition that starts the workflow."),
    conversion: Any | None = Field(None, description="The conversion event or goal to track when the flow completes successfully."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when the invocation exits: on conversion completion, when trigger conditions aren't met, on either condition, or only at the natural end of the flow."),
    edges: Any | None = Field(None, description="The connections between flow nodes, defining the execution path and dependencies. Order and structure determine flow logic."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variables available for use throughout the flow invocation, supporting dynamic configuration and data passing between actions."),
) -> dict[str, Any]:
    """Create a new invocation for a Hog flow, defining its execution configuration including trigger conditions, conversion tracking, exit behavior, and associated actions."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsInvocationsCreateRequest(
            path=_models.HogFlowsInvocationsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFlowsInvocationsCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_hog_flow_invocation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/invocations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/invocations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_hog_flow_invocation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_hog_flow_invocation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_hog_flow_invocation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def get_hog_flow_logs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the HOG flow whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the HOG flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve execution logs for a specific HOG flow within a project. Returns detailed log entries for debugging and monitoring flow execution."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsLogsRetrieveRequest(
            path=_models.HogFlowsLogsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_flow_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_flow_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_flow_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_flow_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def get_hog_flow_metrics(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the HOG flow whose metrics you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the HOG flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve performance and execution metrics for a specific HOG flow within a project. Returns aggregated data about the flow's runs, performance characteristics, and usage patterns."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsMetricsRetrieveRequest(
            path=_models.HogFlowsMetricsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_flow_metrics: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/metrics"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_flow_metrics")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_flow_metrics", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_flow_metrics",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def get_hog_flow_metrics_totals(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the hog flow for which to retrieve metrics totals."),
    project_id: str = Field(..., description="The unique identifier of the project containing the hog flow. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve aggregated metrics totals for a specific hog flow, including performance and execution statistics."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsMetricsTotalsRetrieveRequest(
            path=_models.HogFlowsMetricsTotalsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_flow_metrics_totals: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/metrics/totals", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/metrics/totals"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_flow_metrics_totals")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_flow_metrics_totals", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_flow_metrics_totals",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def list_hog_flow_schedules(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog Flow whose schedules you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog Flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of schedule records to return in a single response page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position (zero-indexed) from which to return results. Use with limit to paginate through schedules."),
) -> dict[str, Any]:
    """Retrieve all schedules associated with a specific Hog Flow. Returns a paginated list of schedules configured for the given Hog Flow within the specified project."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsSchedulesListRequest(
            path=_models.HogFlowsSchedulesListRequestPath(id_=id_, project_id=project_id),
            query=_models.HogFlowsSchedulesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_flow_schedules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/schedules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/schedules"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_flow_schedules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_flow_schedules", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_flow_schedules",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def create_hog_flow_schedule(
    id_: str = Field(..., alias="id", description="The UUID identifier of the Hog Flow for which to create a schedule."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog Flow. Retrieve this from the /api/projects/ endpoint if needed."),
    actions: list[_models.HogFlowAction] = Field(..., description="Array of actions to execute as part of the schedule; required and order-dependent for execution sequence."),
    limit: int | None = Field(None, description="Maximum number of results to return per page for paginated responses."),
    offset: int | None = Field(None, description="Starting index for paginated results; use with limit to retrieve subsequent pages."),
    description: str | None = Field(None, description="Optional text description of the schedule's purpose or details."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The schedule's operational state: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="Trigger configuration that determines when the schedule activates; structure depends on trigger type."),
    conversion: Any | None = Field(None, description="Conversion configuration that defines the goal or success condition for the schedule."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the schedule: on conversion, when trigger conditions fail, on either event, or only at the scheduled end time."),
    edges: Any | None = Field(None, description="Graph edges defining the flow path and connections between schedule steps; order and structure determine execution flow."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variables available for use throughout the schedule's execution and actions."),
) -> dict[str, Any]:
    """Create a new schedule for a Hog Flow, defining when and how the flow executes with specified triggers, conversions, and exit conditions."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsSchedulesCreateRequest(
            path=_models.HogFlowsSchedulesCreateRequestPath(id_=id_, project_id=project_id),
            query=_models.HogFlowsSchedulesCreateRequestQuery(limit=limit, offset=offset),
            body=_models.HogFlowsSchedulesCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_hog_flow_schedule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/schedules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/schedules"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_hog_flow_schedule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_hog_flow_schedule", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_hog_flow_schedule",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def update_hog_flow_schedule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog flow containing the schedule to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flow. Retrieve available project IDs from the /api/projects/ endpoint."),
    schedule_id: str = Field(..., description="The unique identifier of the schedule within the Hog flow to update."),
    description: str | None = Field(None, description="A human-readable description of the schedule's purpose or behavior."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the schedule: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The trigger condition that initiates the schedule. Defines the event or user action that starts the flow execution."),
    conversion: Any | None = Field(None, description="The conversion goal or success metric tracked by the schedule. Defines what constitutes a successful outcome."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the schedule: on conversion, when trigger conditions aren't met, on either condition, or only at the scheduled end time."),
    edges: Any | None = Field(None, description="The connections between nodes in the flow, defining the execution path and dependencies. Order may indicate sequence of evaluation."),
    actions: list[_models.HogFlowAction] | None = Field(None, description="An ordered list of actions to execute as part of the schedule. Each action represents a step in the flow execution."),
    variables: list[dict[str, str]] | None = Field(None, description="An ordered list of variables available for use throughout the schedule, such as user properties or computed values."),
) -> dict[str, Any]:
    """Partially update a schedule configuration for a Hog flow, allowing you to modify trigger conditions, conversion rules, exit behaviors, and associated actions or variables."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsSchedulesPartialUpdateRequest(
            path=_models.HogFlowsSchedulesPartialUpdateRequestPath(id_=id_, project_id=project_id, schedule_id=schedule_id),
            body=_models.HogFlowsSchedulesPartialUpdateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_flow_schedule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/schedules/{schedule_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/schedules/{schedule_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_flow_schedule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_flow_schedule", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_flow_schedule",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def delete_hog_flow_schedule(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog Flow containing the schedule to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog Flow. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    schedule_id: str = Field(..., description="The unique identifier of the schedule to delete from the Hog Flow."),
) -> dict[str, Any]:
    """Delete a specific schedule associated with a Hog Flow. This removes the scheduled execution configuration for the given Hog Flow."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsSchedulesDestroyRequest(
            path=_models.HogFlowsSchedulesDestroyRequestPath(id_=id_, project_id=project_id, schedule_id=schedule_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_hog_flow_schedule: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/{id}/schedules/{schedule_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/{id}/schedules/{schedule_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_hog_flow_schedule")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_hog_flow_schedule", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_hog_flow_schedule",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def delete_hog_flows_bulk(
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog flows to delete. Retrieve this ID from the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="Required array of action objects that define the operations to perform on matched flows during deletion."),
    description: str | None = Field(None, description="Filter flows by their description text to narrow the deletion scope."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="Filter flows by their current status: draft (unpublished), active (running), or archived (inactive)."),
    trigger: Any | None = Field(None, description="Filter flows by their trigger configuration to target specific flow types."),
    conversion: Any | None = Field(None, description="Filter flows by their conversion event configuration to target flows with specific conversion goals."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Filter flows by their exit condition behavior: exit on conversion, trigger not matched, trigger not matched or conversion, or only at end of flow."),
    edges: Any | None = Field(None, description="Filter flows by their edge connections and workflow structure to target flows with specific routing patterns."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variable definitions used within the flows being deleted, useful for filtering flows that use specific variables."),
) -> dict[str, Any]:
    """Bulk delete multiple Hog flows from a project based on specified filter criteria. This operation removes flows matching the provided status, trigger, conversion, and exit condition parameters."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsBulkDeleteCreateRequest(
            path=_models.HogFlowsBulkDeleteCreateRequestPath(project_id=project_id),
            body=_models.HogFlowsBulkDeleteCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_hog_flows_bulk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/bulk_delete", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/bulk_delete"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_hog_flows_bulk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_hog_flows_bulk", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_hog_flows_bulk",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: workflows, hog_flows
@mcp.tool()
async def create_user_blast_radius_flow(
    project_id: str = Field(..., description="The unique identifier of the project where the flow will be created. Retrieve available project IDs from the /api/projects/ endpoint."),
    actions: list[_models.HogFlowAction] = Field(..., description="Required array of actions to execute within the flow. Each action represents a step or operation performed on users in the sequence."),
    description: str | None = Field(None, description="Optional text description of the flow's purpose and behavior."),
    status: Literal["draft", "active", "archived"] | None = Field(None, description="The operational status of the flow: draft (not yet active), active (currently running), or archived (no longer in use)."),
    trigger: Any | None = Field(None, description="The event or condition that initiates the flow for users. Defines which users enter the blast radius sequence."),
    conversion: Any | None = Field(None, description="The desired outcome or goal event that users should achieve within the flow."),
    exit_condition: Literal["exit_on_conversion", "exit_on_trigger_not_matched", "exit_on_trigger_not_matched_or_conversion", "exit_only_at_end"] | None = Field(None, description="Determines when users exit the flow: on conversion completion, when trigger conditions are no longer met, on either condition, or only at the flow's end."),
    edges: Any | None = Field(None, description="The connections and transitions between flow steps, defining the sequence and branching logic of user progression."),
    variables: list[dict[str, str]] | None = Field(None, description="Optional array of variables used throughout the flow for dynamic personalization, segmentation, or conditional logic."),
) -> dict[str, Any]:
    """Create a new user blast radius flow to define automated user engagement sequences with triggers, conversions, and exit conditions."""

    # Construct request model with validation
    try:
        _request = _models.HogFlowsUserBlastRadiusCreateRequest(
            path=_models.HogFlowsUserBlastRadiusCreateRequestPath(project_id=project_id),
            body=_models.HogFlowsUserBlastRadiusCreateRequestBody(description=description, status=status, trigger=trigger, conversion=conversion, exit_condition=exit_condition, edges=edges, actions=actions, variables=variables)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_user_blast_radius_flow: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_flows/user_blast_radius", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_flows/user_blast_radius"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_user_blast_radius_flow")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_user_blast_radius_flow", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_user_blast_radius_flow",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_function_templates, cdp, hog_function_templates
@mcp.tool()
async def list_hog_function_templates(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this value from the /api/projects/ endpoint if needed."),
    limit: int | None = Field(None, description="Maximum number of templates to return per page. Controls pagination size for the result set."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from. Use with limit to paginate through templates."),
    types: str | None = Field(None, description="Filter templates by one or more types using a comma-separated list (e.g., destination, email, sms_provider). Only templates matching the specified types will be returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of Hog function templates available for a project, optionally filtered by template type. Use this to discover available templates for creating Hog functions."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionTemplatesListRequest(
            path=_models.HogFunctionTemplatesListRequestPath(project_id=project_id),
            query=_models.HogFunctionTemplatesListRequestQuery(limit=limit, offset=offset, types=types)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_function_templates: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_function_templates", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_function_templates"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_function_templates")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_function_templates", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_function_templates",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_function_templates, cdp, hog_function_templates
@mcp.tool()
async def get_hog_function_template(
    project_id: str = Field(..., description="The unique identifier of the project containing the template. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    template_id: str = Field(..., description="The unique identifier of the Hog function template to retrieve."),
) -> dict[str, Any]:
    """Retrieve a specific Hog function template by its ID. Hog function templates are reusable function definitions within a project."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionTemplatesRetrieveRequest(
            path=_models.HogFunctionTemplatesRetrieveRequestPath(project_id=project_id, template_id=template_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function_template: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_function_templates/{template_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_function_templates/{template_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function_template")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function_template", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function_template",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def list_hog_functions(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Filter results to return only enabled or disabled Hog functions. Omit to return all functions regardless of status."),
    limit: int | None = Field(None, description="Maximum number of results to return per page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, enabling pagination through large result sets. Use with limit to control which page of results is returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of Hog functions for a specific project, with optional filtering by enabled status."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsListRequest(
            path=_models.HogFunctionsListRequestPath(project_id=project_id),
            query=_models.HogFunctionsListRequestQuery(enabled=enabled, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_functions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_functions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_functions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_functions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def create_hog_function(
    project_id: str = Field(..., description="The unique identifier of the project where this function will be created. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable explanation of the function's purpose and behavior."),
    enabled: bool | None = Field(None, description="Whether this function is active and will process incoming events. Disabled functions are not executed."),
    hog: str | None = Field(None, description="The function implementation code written in Hog language (for most function types) or TypeScript (for site_destination and site_app types)."),
    inputs_schema: list[_models.InputsSchemaItem] | None = Field(None, description="An array defining the configurable input parameters users can set when using this function. Each schema item specifies the parameter name, type, and validation rules."),
    inputs: dict[str, _models.InputsItem] | None = Field(None, description="An object containing the actual values for each input parameter defined in inputs_schema, keyed by parameter name."),
    filters: _models.HogFunctionsCreateBodyFilters | None = Field(None, description="Event filtering criteria that determine which events trigger this function. Only matching events will be processed."),
    masking: _models.HogFunctionsCreateBodyMasking | None = Field(None, description="Configuration for personally identifiable information (PII) masking, including time-to-live, detection threshold, and hash expression rules."),
    mappings: list[_models.Mappings] | None = Field(None, description="An array of field mappings that transform event data to destination-specific formats. Required and used only for destination and site_destination function types."),
    icon_url: str | None = Field(None, description="A URL pointing to an icon image displayed in the UI to visually represent this function."),
    execution_order: int | None = Field(None, description="The execution priority for transformation functions, where lower values execute first. Must be between 0 and 32767 inclusive.", ge=0, le=32767),
) -> dict[str, Any]:
    """Create a new Hog function to process events with custom logic, transformations, or integrations. Functions can filter events, mask PII, transform data, or route to destinations."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsCreateRequest(
            path=_models.HogFunctionsCreateRequestPath(project_id=project_id),
            body=_models.HogFunctionsCreateRequestBody(description=description, enabled=enabled, hog=hog, inputs_schema=inputs_schema, inputs=inputs, filters=filters, masking=masking, mappings=mappings, icon_url=icon_url, execution_order=execution_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_hog_function: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_hog_function")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_hog_function", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_hog_function",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def get_hog_function(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. You can retrieve your project ID by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific Hog function by its ID within a project. Returns the complete configuration and metadata for the requested Hog function."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsRetrieveRequest(
            path=_models.HogFunctionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def update_hog_function(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function to update."),
    project_id: str = Field(..., description="The project ID containing this Hog function. Find your project ID by calling the projects list endpoint."),
    description: str | None = Field(None, description="A human-readable description explaining what this function does and its purpose."),
    enabled: bool | None = Field(None, description="Toggle whether this function is active and processing events. Disabled functions are not executed."),
    hog: str | None = Field(None, description="The function's source code written in Hog language (or TypeScript for site_destination and site_app types)."),
    inputs_schema: list[_models.InputsSchemaItem] | None = Field(None, description="An array defining the configurable input parameters this function accepts. Each schema entry specifies parameter name, type, and validation rules."),
    inputs: dict[str, _models.InputsItem] | None = Field(None, description="An object containing the actual values for each input parameter defined in inputs_schema, keyed by parameter name."),
    filters: _models.HogFunctionsUpdateBodyFilters | None = Field(None, description="Event filtering rules that determine which events trigger this function's execution."),
    masking: _models.HogFunctionsUpdateBodyMasking | None = Field(None, description="Configuration for personally identifiable information (PII) masking, including time-to-live, detection threshold, and hash expression rules."),
    mappings: list[_models.Mappings] | None = Field(None, description="An array of field mappings between event properties and destination fields. Required and used only for destination and site_destination function types."),
    icon_url: str | None = Field(None, description="A URL pointing to an icon image displayed in the UI to represent this function visually."),
    execution_order: int | None = Field(None, description="Execution priority for transformation functions, where lower values execute first. Must be between 0 and 32767.", ge=0, le=32767),
) -> dict[str, Any]:
    """Update a Hog function's configuration, including its source code, inputs, filters, and execution settings. Changes take effect immediately for active functions."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsUpdateRequest(
            path=_models.HogFunctionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFunctionsUpdateRequestBody(description=description, enabled=enabled, hog=hog, inputs_schema=inputs_schema, inputs=inputs, filters=filters, masking=masking, mappings=mappings, icon_url=icon_url, execution_order=execution_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_function: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_function")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_function", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_function",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def update_hog_function_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this Hog function. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable description explaining the function's purpose and behavior."),
    enabled: bool | None = Field(None, description="Toggle whether this function is active and processing events. Disabled functions are not executed."),
    hog: str | None = Field(None, description="The function's source code written in Hog language (for most function types) or TypeScript (for site_destination and site_app types)."),
    inputs_schema: list[_models.InputsSchemaItem] | None = Field(None, description="An array defining the configurable input parameters this function accepts. Each schema entry specifies parameter name, type, and validation rules."),
    inputs: dict[str, _models.InputsItem] | None = Field(None, description="An object containing the actual values for each input parameter defined in inputs_schema. Keys must match the input names defined in the schema."),
    filters: _models.HogFunctionsPartialUpdateBodyFilters | None = Field(None, description="Event filtering rules that determine which events trigger this function. Supports complex filter expressions to target specific event types and properties."),
    masking: _models.HogFunctionsPartialUpdateBodyMasking | None = Field(None, description="Configuration for personally identifiable information (PII) masking, including time-to-live (TTL), detection threshold, and hash expression rules."),
    mappings: list[_models.Mappings] | None = Field(None, description="An array of field mappings that transform event data to destination-specific formats. Only applicable for destination and site_destination function types. Order matters for sequential transformations."),
    icon_url: str | None = Field(None, description="A URL pointing to an icon image displayed in the UI to visually represent this function."),
    execution_order: int | None = Field(None, description="Execution priority for transformation functions, where lower values execute first. Must be between 0 and 32767 inclusive.", ge=0, le=32767),
) -> dict[str, Any]:
    """Partially update a Hog function's configuration, including its code, inputs, filters, and execution settings. Changes take effect immediately for enabled functions."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsPartialUpdateRequest(
            path=_models.HogFunctionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFunctionsPartialUpdateRequestBody(description=description, enabled=enabled, hog=hog, inputs_schema=inputs_schema, inputs=inputs, filters=filters, masking=masking, mappings=mappings, icon_url=icon_url, execution_order=execution_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_function_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_function_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_function_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_function_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def enable_hog_function_backfills(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function to enable backfills for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A human-readable explanation of the function's purpose and behavior."),
    enabled: bool | None = Field(None, description="Whether the function is currently active and processing events."),
    hog: str | None = Field(None, description="The function's source code, written in Hog language for most function types or TypeScript for site_destination and site_app types."),
    inputs_schema: list[_models.InputsSchemaItem] | None = Field(None, description="An array defining the structure and configuration options for input parameters that users can customize when using this function."),
    inputs: dict[str, _models.InputsItem] | None = Field(None, description="An object containing the actual values assigned to each input parameter defined in inputs_schema."),
    filters: _models.HogFunctionsEnableBackfillsCreateBodyFilters | None = Field(None, description="Event filtering rules that determine which events trigger execution of this function."),
    masking: _models.HogFunctionsEnableBackfillsCreateBodyMasking | None = Field(None, description="Configuration for personally identifiable information (PII) masking, including time-to-live settings, detection thresholds, and hash expressions."),
    mappings: list[_models.Mappings] | None = Field(None, description="An array of field mappings that connect event properties to destination fields. Used only for destination and site_destination function types."),
    icon_url: str | None = Field(None, description="A URL pointing to an icon image displayed in the user interface to represent this function."),
    execution_order: int | None = Field(None, description="The execution priority for transformation functions, where lower values execute first. Must be between 0 and 32767.", ge=0, le=32767),
) -> dict[str, Any]:
    """Enable backfill processing for a Hog function, allowing it to retroactively process historical events. This operation updates the function configuration and activates backfill mode."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsEnableBackfillsCreateRequest(
            path=_models.HogFunctionsEnableBackfillsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFunctionsEnableBackfillsCreateRequestBody(description=description, enabled=enabled, hog=hog, inputs_schema=inputs_schema, inputs=inputs, filters=filters, masking=masking, mappings=mappings, icon_url=icon_url, execution_order=execution_order)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for enable_hog_function_backfills: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}/enable_backfills", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}/enable_backfills"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("enable_hog_function_backfills")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("enable_hog_function_backfills", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="enable_hog_function_backfills",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def test_hog_function_invocation(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function to test."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. Retrieve available project IDs from the /api/projects/ endpoint."),
    configuration: _models.HogFunctionsInvocationsCreateBodyConfiguration = Field(..., description="The complete Hog function configuration object to be tested, including all function code and settings."),
    globals_: dict[str, Any] | None = Field(None, alias="globals", description="Optional object containing mock global variables that will be available to the function during test execution."),
    clickhouse_event: dict[str, Any] | None = Field(None, description="Optional object containing mock ClickHouse event data to simulate as input for testing the function's event processing logic."),
    mock_async_functions: bool | None = Field(None, description="When enabled (default), asynchronous functions like fetch() are mocked rather than executed. Set to false to test actual async behavior."),
    invocation_id: str | None = Field(None, description="Optional unique identifier for correlating this test invocation with other requests or logs."),
) -> dict[str, Any]:
    """Test a Hog function by executing it with mock data and configuration. This operation allows you to validate function behavior before deployment by simulating ClickHouse events and global variables."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsInvocationsCreateRequest(
            path=_models.HogFunctionsInvocationsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.HogFunctionsInvocationsCreateRequestBody(configuration=configuration, globals_=globals_, clickhouse_event=clickhouse_event, mock_async_functions=mock_async_functions, invocation_id=invocation_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for test_hog_function_invocation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}/invocations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}/invocations"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("test_hog_function_invocation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("test_hog_function_invocation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="test_hog_function_invocation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def get_hog_function_logs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the hog function whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the hog function. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve execution logs for a specific hog function within a project. Logs provide detailed information about function invocations and their outcomes."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsLogsRetrieveRequest(
            path=_models.HogFunctionsLogsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def get_hog_function_metrics(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function whose metrics you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve performance and execution metrics for a specific Hog function within a project. Returns aggregated data about the function's invocations, performance, and behavior."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsMetricsRetrieveRequest(
            path=_models.HogFunctionsMetricsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function_metrics: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}/metrics", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}/metrics"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function_metrics")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function_metrics", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function_metrics",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def get_hog_function_metrics_totals(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the Hog function whose metrics you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve aggregated metrics totals for a specific Hog function, including execution counts and performance statistics."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsMetricsTotalsRetrieveRequest(
            path=_models.HogFunctionsMetricsTotalsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function_metrics_totals: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/{id}/metrics/totals", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/{id}/metrics/totals"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function_metrics_totals")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function_metrics_totals", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function_metrics_totals",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def get_hog_function_icon(project_id: str = Field(..., description="The unique identifier of the project containing the Hog function. You can retrieve available project IDs by calling the projects list endpoint.")) -> dict[str, Any]:
    """Retrieve the icon for a specific Hog function within a project. This endpoint returns the icon asset associated with the Hog function."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsIconRetrieveRequest(
            path=_models.HogFunctionsIconRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_hog_function_icon: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/icon", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/icon"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_hog_function_icon")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_hog_function_icon", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_hog_function_icon",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def list_hog_function_icons(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the available icons for Hog functions within a specific project. This returns a collection of icon assets that can be used to represent Hog functions in the UI."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsIconsRetrieveRequest(
            path=_models.HogFunctionsIconsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_function_icons: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/icons", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/icons"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_function_icons")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_function_icons", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_function_icons",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_functions, cdp, hog_functions
@mcp.tool()
async def update_hog_functions_execution_order(
    project_id: str = Field(..., description="The unique identifier of the project containing the HogFunctions to reorder. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    orders: dict[str, int] | None = Field(None, description="A mapping of HogFunction UUIDs to their desired execution_order values. Each entry specifies which position a function should occupy in the execution sequence."),
) -> dict[str, Any]:
    """Reorder the execution sequence of multiple HogFunctions within a project by specifying new execution order values for each function."""

    # Construct request model with validation
    try:
        _request = _models.HogFunctionsRearrangePartialUpdateRequest(
            path=_models.HogFunctionsRearrangePartialUpdateRequestPath(project_id=project_id),
            body=_models.HogFunctionsRearrangePartialUpdateRequestBody(orders=orders)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_hog_functions_execution_order: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/hog_functions/rearrange", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/hog_functions/rearrange"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_hog_functions_execution_order")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_hog_functions_execution_order", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_hog_functions_execution_order",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def list_insight_variables(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve your project ID by calling the /api/projects/ endpoint if you don't have it available."),
    page: int | None = Field(None, description="The page number to retrieve from the paginated result set. Omit to get the first page."),
) -> dict[str, Any]:
    """Retrieve a paginated list of insight variables for a specific project. Use this to discover available variables that can be used in project insights and analytics."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesListRequest(
            path=_models.InsightVariablesListRequestPath(project_id=project_id),
            query=_models.InsightVariablesListRequestQuery(page=page)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_insight_variables: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_insight_variables")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_insight_variables", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_insight_variables",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def create_insight_variable(
    project_id: str = Field(..., description="The unique identifier of the project where the insight variable will be created. Retrieve project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the insight variable. Must be a string with a maximum length of 400 characters.", max_length=400),
    type_: Literal["String", "Number", "Boolean", "List", "Date"] = Field(..., alias="type", description="The data type of the insight variable. Must be one of: String, Number, Boolean, List, or Date."),
    default_value: Any | None = Field(None, description="The default value assigned to the insight variable when no explicit value is provided. The format should match the specified type."),
    values: Any | None = Field(None, description="A collection of predefined values for the insight variable. Typically used with List or enumerated types to constrain valid options."),
) -> dict[str, Any]:
    """Create a new insight variable in a project to define custom data types and values for use in project insights and analytics."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesCreateRequest(
            path=_models.InsightVariablesCreateRequestPath(project_id=project_id),
            body=_models.InsightVariablesCreateRequestBody(name=name, type_=type_, default_value=default_value, values=values)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_insight_variable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_insight_variable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_insight_variable", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_insight_variable",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def get_insight_variable(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the insight variable to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight variable. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific insight variable by its ID within a project. Returns the configuration and metadata for the requested insight variable."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesRetrieveRequest(
            path=_models.InsightVariablesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight_variable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight_variable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight_variable", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight_variable",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def update_insight_variable(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the insight variable to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight variable. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the insight variable. Must not exceed 400 characters.", max_length=400),
    type_: Literal["String", "Number", "Boolean", "List", "Date"] = Field(..., alias="type", description="The data type of the insight variable. Must be one of: String, Number, Boolean, List, or Date."),
    default_value: Any | None = Field(None, description="The default value assigned to the insight variable when no explicit value is provided. The format should match the specified type."),
    values: Any | None = Field(None, description="A collection of allowed values for the insight variable. The format depends on the specified type (e.g., array of strings for String type, array of numbers for Number type)."),
) -> dict[str, Any]:
    """Update an existing insight variable within a project. Modify the variable's name, type, default value, and allowed values."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesUpdateRequest(
            path=_models.InsightVariablesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.InsightVariablesUpdateRequestBody(name=name, type_=type_, default_value=default_value, values=values)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_insight_variable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_insight_variable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_insight_variable", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_insight_variable",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def update_insight_variable_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the insight variable to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight variable. Retrieve available project IDs from the /api/projects/ endpoint."),
    default_value: Any | None = Field(None, description="The default value assigned to this insight variable when no explicit value is provided."),
    values: Any | None = Field(None, description="A collection of valid values or options that this insight variable can accept."),
) -> dict[str, Any]:
    """Partially update an insight variable within a project. Allows modification of specific fields like default values or value options without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesPartialUpdateRequest(
            path=_models.InsightVariablesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.InsightVariablesPartialUpdateRequestBody(default_value=default_value, values=values)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_insight_variable_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_insight_variable_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_insight_variable_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_insight_variable_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insight_variables
@mcp.tool()
async def delete_insight_variable(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the insight variable to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight variable. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific insight variable from a project. This operation removes the insight variable and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.InsightVariablesDestroyRequest(
            path=_models.InsightVariablesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_insight_variable: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insight_variables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insight_variables/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_insight_variable")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_insight_variable", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_insight_variable",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def list_insights(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    basic: bool | None = Field(None, description="When enabled, returns only basic insight metadata without computed results, providing faster response times."),
    limit: int | None = Field(None, description="Maximum number of insights to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position from which to begin returning results for pagination."),
    refresh: Literal["async", "async_except_on_cache_miss", "blocking", "force_async", "force_blocking", "force_cache", "lazy_async"] | None = Field(None, description="Controls how insights are refreshed and whether calculations happen synchronously or asynchronously. Use 'force_cache' for immediate cached results, 'blocking' for synchronous calculation with cache preference, 'async' for background calculation, 'lazy_async' for lenient cache freshness, 'force_blocking' to bypass cache entirely, or 'force_async' to always calculate in the background. Track background operations via the query_status response field."),
    short_id: str | None = Field(None, description="Optional short identifier for filtering or referencing a specific insight."),
) -> dict[str, Any]:
    """Retrieve a paginated list of insights for a project with flexible caching and refresh strategies. Results are gated behind permission checks, and cached responses are validated before being returned to the requester."""

    # Construct request model with validation
    try:
        _request = _models.InsightsListRequest(
            path=_models.InsightsListRequestPath(project_id=project_id),
            query=_models.InsightsListRequestQuery(basic=basic, limit=limit, offset=offset, refresh=refresh, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_insights: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_insights")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_insights", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_insights",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def create_insight(
    project_id: str = Field(..., description="The unique identifier of the project where the insight will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="The query definition or filter criteria for the insight. Specify the data or conditions this insight should analyze or display."),
    order: int | None = Field(None, description="The display order position for this insight within the project. Accepts any 32-bit integer value to control sorting and arrangement.", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="A text description of the insight's purpose or content. Limited to 400 characters maximum.", max_length=400),
    tags: list[Any] | None = Field(None, description="An array of tag strings for categorizing and organizing the insight. Tags help with filtering and discovery of related insights."),
    favorited: bool | None = Field(None, description="A boolean flag to mark this insight as favorited. Set to true to add it to your favorites for quick access."),
) -> dict[str, Any]:
    """Create a new insight for a project. The insight will be stored with optional metadata including query parameters, display order, description, and tags for organization and discovery."""

    # Construct request model with validation
    try:
        _request = _models.InsightsCreateRequest(
            path=_models.InsightsCreateRequestPath(project_id=project_id),
            body=_models.InsightsCreateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_insight: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_insight")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_insight", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_insight",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, insights
@mcp.tool()
async def list_insight_sharing(
    insight_id: int = Field(..., description="The unique identifier of the insight whose sharing settings you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all sharing configurations and permissions for a specific insight within a project. This shows who has access to the insight and what level of access they have."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSharingListRequest(
            path=_models.InsightsSharingListRequestPath(insight_id=insight_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_insight_sharing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/sharing", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/sharing"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_insight_sharing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_insight_sharing", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_insight_sharing",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, insights
@mcp.tool()
async def create_insight_sharing_password(
    insight_id: int = Field(..., description="The unique identifier of the insight for which you want to create a sharing password."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Whether password protection is enabled for this sharing configuration."),
    settings: Any | None = Field(None, description="Additional configuration settings for the password sharing behavior and constraints."),
    password_required: bool | None = Field(None, description="Whether a password is required for accessing the shared insight."),
) -> dict[str, Any]:
    """Create a new password-based access control for sharing an insight. This allows you to protect shared insights with password authentication."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSharingPasswordsCreateRequest(
            path=_models.InsightsSharingPasswordsCreateRequestPath(insight_id=insight_id, project_id=project_id),
            body=_models.InsightsSharingPasswordsCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_insight_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/sharing/passwords", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/sharing/passwords"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_insight_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_insight_sharing_password", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_insight_sharing_password",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, insights
@mcp.tool()
async def delete_insight_sharing_password(
    insight_id: int = Field(..., description="The unique identifier of the insight whose sharing password you want to delete."),
    password_id: str = Field(..., description="The unique identifier of the password to remove from the insight's sharing configuration."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Remove a specific password from an insight's sharing configuration, preventing further access via that password."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSharingPasswordsDestroyRequest(
            path=_models.InsightsSharingPasswordsDestroyRequestPath(insight_id=insight_id, password_id=password_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_insight_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/sharing/passwords/{password_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/sharing/passwords/{password_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_insight_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_insight_sharing_password", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_insight_sharing_password",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, insights
@mcp.tool()
async def refresh_insight_sharing(
    insight_id: int = Field(..., description="The unique identifier of the insight whose sharing settings you want to refresh or update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Whether sharing is enabled for this insight. When true, the insight can be accessed by others according to the sharing settings."),
    settings: Any | None = Field(None, description="Configuration object containing additional sharing parameters such as access levels, expiration settings, or recipient details."),
    password_required: bool | None = Field(None, description="Whether a password is required to access the shared insight. When true, viewers must provide the configured password to view the insight."),
) -> dict[str, Any]:
    """Refresh or update the sharing configuration for an insight, allowing you to modify access settings, enable/disable sharing, and manage password protection."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSharingRefreshCreateRequest(
            path=_models.InsightsSharingRefreshCreateRequestPath(insight_id=insight_id, project_id=project_id),
            body=_models.InsightsSharingRefreshCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for refresh_insight_sharing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/sharing/refresh", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/sharing/refresh"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("refresh_insight_sharing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("refresh_insight_sharing", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="refresh_insight_sharing",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def list_insight_thresholds(
    insight_id: int = Field(..., description="The unique identifier of the insight for which to retrieve thresholds."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of threshold records to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The zero-based starting position for results. Use with limit to paginate through threshold records."),
) -> dict[str, Any]:
    """Retrieve a paginated list of thresholds configured for a specific insight within a project. Thresholds define the alert conditions and limits for the insight's metrics."""

    # Construct request model with validation
    try:
        _request = _models.InsightsThresholdsListRequest(
            path=_models.InsightsThresholdsListRequestPath(insight_id=insight_id, project_id=project_id),
            query=_models.InsightsThresholdsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_insight_thresholds: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/thresholds", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/thresholds"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_insight_thresholds")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_insight_thresholds", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_insight_thresholds",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_insight_threshold(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the threshold to retrieve."),
    insight_id: int = Field(..., description="The numeric identifier of the insight that contains the threshold."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific threshold configuration for an insight within a project. Thresholds define alert or metric boundaries for monitoring insight data."""

    # Construct request model with validation
    try:
        _request = _models.InsightsThresholdsRetrieveRequest(
            path=_models.InsightsThresholdsRetrieveRequestPath(id_=id_, insight_id=insight_id, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight_threshold: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{insight_id}/thresholds/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{insight_id}/thresholds/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight_threshold")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight_threshold", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight_threshold",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_insight(
    id_: int = Field(..., alias="id", description="The unique identifier of the insight to retrieve."),
    project_id: str = Field(..., description="The project ID containing the insight. Find your project ID by calling the /api/projects/ endpoint."),
    from_dashboard: int | None = Field(None, description="Optional dashboard ID to apply that dashboard's filters and date range overrides when loading the insight."),
    refresh: Literal["async", "async_except_on_cache_miss", "blocking", "force_async", "force_blocking", "force_cache", "lazy_async"] | None = Field(None, description="Controls data freshness and calculation strategy: use 'force_cache' for immediate cached results, 'blocking' for synchronous calculation unless fresh cache exists, 'async' for background calculation unless fresh cache exists, 'lazy_async' for background calculation unless somewhat fresh cache exists, 'force_blocking' to always calculate synchronously, or 'force_async' to always calculate in background. Defaults to 'force_cache'. Track background calculations using the query_status response field."),
) -> dict[str, Any]:
    """Retrieve a specific insight with permission validation. Supports cached responses via middleware while ensuring authorization, and allows control over data freshness through configurable refresh strategies."""

    # Construct request model with validation
    try:
        _request = _models.InsightsRetrieveRequest(
            path=_models.InsightsRetrieveRequestPath(id_=id_, project_id=project_id),
            query=_models.InsightsRetrieveRequestQuery(from_dashboard=from_dashboard, refresh=refresh)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def update_insight(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Find your project ID by calling /api/projects/."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="Optional query parameter for filtering or additional request context."),
    order: int | None = Field(None, description="Optional integer value for ordering or sequencing the insight. Accepts any 32-bit signed integer value.", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="Optional text description of the insight. Maximum length is 400 characters.", max_length=400),
    tags: list[Any] | None = Field(None, description="Optional array of tags to categorize or label the insight."),
    favorited: bool | None = Field(None, description="Optional boolean flag to mark the insight as favorited or unfavorited."),
) -> dict[str, Any]:
    """Update an insight within a project. Requires proper authorization; cached responses are validated against permissions before being returned."""

    # Construct request model with validation
    try:
        _request = _models.InsightsUpdateRequest(
            path=_models.InsightsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.InsightsUpdateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_insight: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_insight")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_insight", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_insight",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def update_insight_partial(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Find your project ID by calling /api/projects/."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="Optional query parameter for filtering or searching insights."),
    order: int | None = Field(None, description="Optional integer value for ordering or sequencing the insight (valid range: -2,147,483,648 to 2,147,483,647).", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="Optional text description of the insight (maximum 400 characters).", max_length=400),
    tags: list[Any] | None = Field(None, description="Optional array of tags to categorize or label the insight."),
    favorited: bool | None = Field(None, description="Optional boolean flag to mark the insight as favorited or unfavorited."),
) -> dict[str, Any]:
    """Partially update an insight within a project. This operation supports permission-gated access to cached responses, ensuring authorization before returning coalesced data."""

    # Construct request model with validation
    try:
        _request = _models.InsightsPartialUpdateRequest(
            path=_models.InsightsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.InsightsPartialUpdateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_insight_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_insight_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_insight_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_insight_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def mark_insight_deleted(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight to be marked as deleted."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Soft-delete an insight by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.InsightsDestroyRequest(
            path=_models.InsightsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for mark_insight_deleted: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("mark_insight_deleted")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("mark_insight_deleted", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="mark_insight_deleted",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_insight_activity(
    id_: int = Field(..., alias="id", description="The unique identifier of the insight whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific insight within a project. Returns cached activity data after verifying the requester has permission to access the insight."""

    # Construct request model with validation
    try:
        _request = _models.InsightsActivityRetrieve2Request(
            path=_models.InsightsActivityRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_insight_analysis(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight to analyze."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve analysis results for a specific insight within a project. This operation enforces permission checks before returning cached analysis data to ensure authorized access."""

    # Construct request model with validation
    try:
        _request = _models.InsightsAnalyzeRetrieveRequest(
            path=_models.InsightsAnalyzeRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight_analysis: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}/analyze", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}/analyze"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight_analysis")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight_analysis", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight_analysis",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_insight_suggestions(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight for which to retrieve suggestions."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve suggestions for a specific insight within a project. The response is cached and gated behind permission checks to ensure the requesting user has access to the project and insight."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSuggestionsRetrieveRequest(
            path=_models.InsightsSuggestionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_insight_suggestions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}/suggestions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}/suggestions"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_insight_suggestions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_insight_suggestions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_insight_suggestions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def create_insight_suggestion(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the insight to add a suggestion to."),
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="Optional query parameter for filtering or searching suggestions."),
    order: int | None = Field(None, description="Optional integer value specifying the display order of the suggestion, ranging from -2,147,483,648 to 2,147,483,647.", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="Optional text description of the suggestion, up to 400 characters in length.", max_length=400),
    tags: list[Any] | None = Field(None, description="Optional array of tags to categorize or label the suggestion. Specify the order and format of tag items as needed."),
    favorited: bool | None = Field(None, description="Optional boolean flag indicating whether the suggestion should be marked as favorited."),
) -> dict[str, Any]:
    """Create a new suggestion for an insight within a project. The request is authorized before processing, ensuring proper access control even when using cached response data."""

    # Construct request model with validation
    try:
        _request = _models.InsightsSuggestionsCreateRequest(
            path=_models.InsightsSuggestionsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.InsightsSuggestionsCreateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_insight_suggestion: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/{id}/suggestions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/{id}/suggestions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_insight_suggestion")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_insight_suggestion", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_insight_suggestion",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def get_project_activity_insights(project_id: str = Field(..., description="The unique identifier of the project for which to retrieve activity insights. You can obtain the project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve activity insights for a specific project. Returns cached coalesced response data after verifying the request is authorized through authentication, permissions, and throttling checks."""

    # Construct request model with validation
    try:
        _request = _models.InsightsActivityRetrieveRequest(
            path=_models.InsightsActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_project_activity_insights: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_project_activity_insights")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_project_activity_insights", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_project_activity_insights",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def cancel_insight_creation(
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Retrieve available project IDs by calling /api/projects/."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="Optional query filter to identify the specific insight creation to cancel."),
    order: int | None = Field(None, description="Optional ordering parameter for result sorting. Accepts 32-bit signed integer values.", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="Optional description text for the insight, up to 400 characters.", max_length=400),
    tags: list[Any] | None = Field(None, description="Optional array of tags to categorize or label the insight."),
    favorited: bool | None = Field(None, description="Optional boolean flag to indicate whether the insight should be marked as favorited."),
) -> dict[str, Any]:
    """Cancel an in-progress insight creation for a project. This operation validates authorization before returning cached response data from coalesced requests."""

    # Construct request model with validation
    try:
        _request = _models.InsightsCancelCreateRequest(
            path=_models.InsightsCancelCreateRequestPath(project_id=project_id),
            body=_models.InsightsCancelCreateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_insight_creation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/cancel", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/cancel"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_insight_creation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_insight_creation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_insight_creation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def generate_insight_metadata(
    project_id: str = Field(..., description="The unique identifier of the project containing the insight. Retrieve available project IDs from the /api/projects/ endpoint."),
    query: _models.InsightVizNode | _models.DataTableNode | _models.DataVisualizationNode | _models.HogQuery | None = Field(None, description="The query configuration object that defines the insight's data source and filtering criteria. Used by the AI to generate contextually relevant metadata."),
    order: int | None = Field(None, description="The display order or priority of the insight within its collection. Accepts any 32-bit integer value.", ge=-2147483648, le=2147483647),
    description: str | None = Field(None, description="A custom description for the insight. Maximum length is 400 characters.", max_length=400),
    tags: list[Any] | None = Field(None, description="An array of tag strings for categorizing and organizing the insight. Order may indicate priority or grouping."),
    favorited: bool | None = Field(None, description="A boolean flag indicating whether the insight is marked as a favorite or pinned for quick access."),
) -> dict[str, Any]:
    """Generate AI-suggested name and description for an insight based on its query configuration and metadata. Use this to automatically populate insight titles and descriptions from query parameters."""

    # Construct request model with validation
    try:
        _request = _models.InsightsGenerateMetadataCreateRequest(
            path=_models.InsightsGenerateMetadataCreateRequestPath(project_id=project_id),
            body=_models.InsightsGenerateMetadataCreateRequestBody(query=query, order=order, description=description, tags=tags, favorited=favorited)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_insight_metadata: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/generate_metadata", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/generate_metadata"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_insight_metadata")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_insight_metadata", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_insight_metadata",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def list_insights_recently_viewed(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the projects list endpoint.")) -> dict[str, Any]:
    """Retrieves the 5 most recently viewed insights for the authenticated user within a specific project, ordered from most to least recent."""

    # Construct request model with validation
    try:
        _request = _models.InsightsMyLastViewedRetrieveRequest(
            path=_models.InsightsMyLastViewedRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_insights_recently_viewed: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/my_last_viewed", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/my_last_viewed"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_insights_recently_viewed")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_insights_recently_viewed", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_insights_recently_viewed",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: insights
@mcp.tool()
async def list_trending_insights(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the projects list endpoint.")) -> dict[str, Any]:
    """Retrieves the most viewed insights for a project over a specified time period. Returns the top 10 trending insights by default, ranked by view count over the last 7 days."""

    # Construct request model with validation
    try:
        _request = _models.InsightsTrendingRetrieveRequest(
            path=_models.InsightsTrendingRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_trending_insights: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/insights/trending", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/insights/trending"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_trending_insights")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_trending_insights", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_trending_insights",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_integrations(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of integration records to return in a single response page. Defaults to API's standard page size if not specified."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of integrations configured for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsList2Request(
            path=_models.IntegrationsList2RequestPath(project_id=project_id),
            query=_models.IntegrationsList2RequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_integrations: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_integrations")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_integrations", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_integrations",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def get_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration to retrieve. This is a positive integer assigned when the integration was created."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve details for a specific integration within a project. Use this to fetch configuration, status, and metadata for an integration by its ID."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsRetrieve2Request(
            path=_models.IntegrationsRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def delete_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration to delete. This must be a positive integer value."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently removes an integration from a project. This action cannot be undone and will disconnect the integration from all workflows."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsDestroyRequest(
            path=_models.IntegrationsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_integration", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_integration",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def get_integration_channels(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration whose channels you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can obtain this ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of channels configured for a specific integration within a project. This allows you to view all communication or notification channels associated with the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsChannelsRetrieveRequest(
            path=_models.IntegrationsChannelsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_integration_channels: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/channels", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/channels"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_integration_channels")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_integration_channels", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_integration_channels",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_clickup_lists_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration instance. This must be a positive integer that corresponds to an existing integration within the specified project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all ClickUp lists associated with a specific integration within a project. This allows you to view the ClickUp workspace lists that have been synced or configured for the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsClickupListsRetrieveRequest(
            path=_models.IntegrationsClickupListsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_clickup_lists_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/clickup_lists", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/clickup_lists"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_clickup_lists_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_clickup_lists_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_clickup_lists_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_clickup_spaces_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the ClickUp integration. This must be a positive integer that corresponds to an existing integration within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all ClickUp spaces associated with a specific ClickUp integration within a project. This allows you to view the available ClickUp workspaces that have been synced through the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsClickupSpacesRetrieveRequest(
            path=_models.IntegrationsClickupSpacesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_clickup_spaces_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/clickup_spaces", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/clickup_spaces"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_clickup_spaces_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_clickup_spaces_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_clickup_spaces_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_clickup_workspaces_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration instance. This must be a positive integer that corresponds to an existing integration in your project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all ClickUp workspaces available through a specific integration. This allows you to see which ClickUp workspaces are connected and accessible via the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsClickupWorkspacesRetrieveRequest(
            path=_models.IntegrationsClickupWorkspacesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_clickup_workspaces_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/clickup_workspaces", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/clickup_workspaces"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_clickup_workspaces_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_clickup_workspaces_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_clickup_workspaces_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def update_email_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration to update. This is a numeric ID specific to the integration record."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    kind: Literal["slack", "slack-posthog-code", "salesforce", "hubspot", "google-pubsub", "google-cloud-storage", "google-ads", "google-sheets", "google-cloud-service-account", "snapchat", "linkedin-ads", "reddit-ads", "tiktok-ads", "bing-ads", "intercom", "email", "linear", "github", "gitlab", "meta-ads", "twilio", "clickup", "vercel", "databricks", "azure-blob", "firebase", "jira", "pinterest-ads", "stripe"] | None = Field(None, description="The type of integration service. For email integrations, use 'email'. Other valid types include Slack, Salesforce, HubSpot, Google services, social media ad platforms, and development tools."),
) -> dict[str, Any]:
    """Partially update an email integration configuration for a project. Use this to modify specific fields of an existing email integration without replacing the entire configuration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsEmailPartialUpdateRequest(
            path=_models.IntegrationsEmailPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.IntegrationsEmailPartialUpdateRequestBody(kind=kind)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_email_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/email", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/email"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_email_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_email_integration", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_email_integration",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_github_branches(
    id_: int = Field(..., alias="id", description="The unique identifier of the GitHub integration within your project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the GitHub integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    repo: str = Field(..., description="The GitHub repository identifier in owner/repo format (e.g., octocat/Hello-World). Must be at least 1 character long.", min_length=1),
) -> dict[str, Any]:
    """Retrieve a list of branches from a GitHub repository that is integrated with your project. This operation requires the repository to be specified in owner/repo format."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsGithubBranchesRetrieveRequest(
            path=_models.IntegrationsGithubBranchesRetrieveRequestPath(id_=id_, project_id=project_id),
            query=_models.IntegrationsGithubBranchesRetrieveRequestQuery(repo=repo)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_github_branches: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/github_branches", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/github_branches"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_github_branches")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_github_branches", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_github_branches",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_github_repos_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the GitHub integration within the project. This must be a positive integer value."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of GitHub repositories available through a GitHub integration for a specific project. This allows you to see which repositories are connected and accessible via the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsGithubReposRetrieveRequest(
            path=_models.IntegrationsGithubReposRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_github_repos_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/github_repos", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/github_repos"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_github_repos_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_github_repos_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_github_repos_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_google_accessible_accounts(
    id_: int = Field(..., alias="id", description="The unique identifier of the Google integration. This must be a positive integer that corresponds to an existing integration in the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the list of Google accounts accessible through a specific Google integration. This returns all Google accounts that have been authorized and are available for use with the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsGoogleAccessibleAccountsRetrieveRequest(
            path=_models.IntegrationsGoogleAccessibleAccountsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_google_accessible_accounts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/google_accessible_accounts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/google_accessible_accounts"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_google_accessible_accounts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_google_accessible_accounts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_google_accessible_accounts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def get_google_conversion_actions(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration. This must be a positive integer that corresponds to a valid Google Ads integration in your project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the Google conversion actions configured for a specific integration. This returns the conversion tracking setup linked to your Google Ads account integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsGoogleConversionActionsRetrieveRequest(
            path=_models.IntegrationsGoogleConversionActionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_google_conversion_actions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/google_conversion_actions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/google_conversion_actions"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_google_conversion_actions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_google_conversion_actions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_google_conversion_actions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_jira_projects_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration instance. This must be a positive integer that corresponds to an existing integration in your project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all Jira projects connected to a specific integration within your project. This allows you to view which Jira projects are available through the configured integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsJiraProjectsRetrieveRequest(
            path=_models.IntegrationsJiraProjectsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_jira_projects_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/jira_projects", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/jira_projects"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_jira_projects_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_jira_projects_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_jira_projects_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def get_linear_teams_for_integration(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration whose Linear teams you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can obtain this by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the Linear teams associated with a specific integration within a project. This allows you to view which Linear teams are connected through this integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsLinearTeamsRetrieveRequest(
            path=_models.IntegrationsLinearTeamsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_linear_teams_for_integration: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/linear_teams", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/linear_teams"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_linear_teams_for_integration")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_linear_teams_for_integration", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_linear_teams_for_integration",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_linkedin_ads_accounts(
    id_: int = Field(..., alias="id", description="The unique identifier of the integration instance. This must be a positive integer that corresponds to an existing integration within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all LinkedIn Ads accounts connected to a specific integration within a project. This returns the list of LinkedIn Ads accounts that have been configured for the integration."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsLinkedinAdsAccountsRetrieveRequest(
            path=_models.IntegrationsLinkedinAdsAccountsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_linkedin_ads_accounts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/linkedin_ads_accounts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/linkedin_ads_accounts"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_linkedin_ads_accounts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_linkedin_ads_accounts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_linkedin_ads_accounts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_linkedin_ads_conversion_rules(
    id_: int = Field(..., alias="id", description="The unique identifier of the LinkedIn Ads integration. This must be a positive integer that corresponds to an existing integration within the specified project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all LinkedIn Ads conversion rules configured for a specific integration within a project. This allows you to view the conversion tracking rules that map LinkedIn campaign events to your project's conversion goals."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsLinkedinAdsConversionRulesRetrieveRequest(
            path=_models.IntegrationsLinkedinAdsConversionRulesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_linkedin_ads_conversion_rules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/linkedin_ads_conversion_rules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/linkedin_ads_conversion_rules"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_linkedin_ads_conversion_rules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_linkedin_ads_conversion_rules", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_linkedin_ads_conversion_rules",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, integrations
@mcp.tool()
async def list_twilio_phone_numbers(
    id_: int = Field(..., alias="id", description="The unique identifier of the Twilio integration. This must be a positive integer that corresponds to an existing integration in your project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the integration. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve all Twilio phone numbers associated with a specific integration. This allows you to view the phone numbers configured for a Twilio integration within your project."""

    # Construct request model with validation
    try:
        _request = _models.IntegrationsTwilioPhoneNumbersRetrieveRequest(
            path=_models.IntegrationsTwilioPhoneNumbersRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_twilio_phone_numbers: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/integrations/{id}/twilio_phone_numbers", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/integrations/{id}/twilio_phone_numbers"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_twilio_phone_numbers")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_twilio_phone_numbers", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_twilio_phone_numbers",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: js-snippet
@mcp.tool()
async def get_js_snippet_version(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the current JavaScript snippet version pin and its resolved version for a project. This shows which version is actively configured and what it resolves to."""

    # Construct request model with validation
    try:
        _request = _models.JsSnippetVersionRetrieveRequest(
            path=_models.JsSnippetVersionRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_js_snippet_version: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/js-snippet/version", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/js-snippet/version"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_js_snippet_version")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_js_snippet_version", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_js_snippet_version",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: js-snippet
@mcp.tool()
async def update_js_snippet_version(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Update the JavaScript snippet version pin for a project. This allows you to control which version of the snippet is deployed for your team."""

    # Construct request model with validation
    try:
        _request = _models.JsSnippetVersionPartialUpdateRequest(
            path=_models.JsSnippetVersionPartialUpdateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_js_snippet_version: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/js-snippet/version", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/js-snippet/version"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_js_snippet_version")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_js_snippet_version", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_js_snippet_version",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def list_live_debugger_breakpoints(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    filename: str | None = Field(None, description="Filter breakpoints by source filename to narrow results to a specific file."),
    limit: int | None = Field(None, description="Maximum number of breakpoints to return in a single response page."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    repository: str | None = Field(None, description="Filter breakpoints by repository to scope results to a specific codebase."),
) -> dict[str, Any]:
    """Retrieve a list of breakpoints configured for live debugging in a project. Supports filtering by filename and repository, with pagination support."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsListRequest(
            path=_models.LiveDebuggerBreakpointsListRequestPath(project_id=project_id),
            query=_models.LiveDebuggerBreakpointsListRequestQuery(filename=filename, limit=limit, offset=offset, repository=repository)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_live_debugger_breakpoints: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_live_debugger_breakpoints")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_live_debugger_breakpoints", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_live_debugger_breakpoints",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def create_live_debugger_breakpoint(
    project_id: str = Field(..., description="The unique identifier of the project where you want to create the breakpoint. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    filename: str = Field(..., description="The file path or name where the breakpoint will be set."),
    line_number: int = Field(..., description="The line number where the breakpoint will be triggered. Must be a non-negative integer.", ge=0, le=2147483647),
    repository: str | None = Field(None, description="The repository name or identifier where the file containing the breakpoint is located."),
    enabled: bool | None = Field(None, description="Whether the breakpoint is active. When disabled, the breakpoint will not trigger even if conditions are met."),
    condition: str | None = Field(None, description="An optional conditional expression that must evaluate to true for the breakpoint to trigger. If not specified, the breakpoint triggers unconditionally."),
) -> dict[str, Any]:
    """Create a breakpoint for live debugging in a specific file and line of your project. Breakpoints can be conditionally triggered and enabled or disabled as needed."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsCreateRequest(
            path=_models.LiveDebuggerBreakpointsCreateRequestPath(project_id=project_id),
            body=_models.LiveDebuggerBreakpointsCreateRequestBody(repository=repository, filename=filename, line_number=line_number, enabled=enabled, condition=condition)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_live_debugger_breakpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_live_debugger_breakpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_live_debugger_breakpoint", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_live_debugger_breakpoint",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def get_live_debugger_breakpoint(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the live debugger breakpoint to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the breakpoint. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific live debugger breakpoint by its ID. Use this to fetch detailed information about a configured breakpoint for debugging purposes."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsRetrieveRequest(
            path=_models.LiveDebuggerBreakpointsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_live_debugger_breakpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_live_debugger_breakpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_live_debugger_breakpoint", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_live_debugger_breakpoint",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def update_live_debugger_breakpoint(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the breakpoint to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the breakpoint. Retrieve available project IDs from the /api/projects/ endpoint."),
    filename: str = Field(..., description="The source file path where the breakpoint is set."),
    line_number: int = Field(..., description="The line number in the source file where the breakpoint applies. Must be a non-negative integer.", ge=0, le=2147483647),
    repository: str | None = Field(None, description="The repository where the breakpoint is located."),
    enabled: bool | None = Field(None, description="Whether the breakpoint is currently active and will trigger during execution."),
    condition: str | None = Field(None, description="An optional conditional expression that must evaluate to true for the breakpoint to trigger."),
) -> dict[str, Any]:
    """Update an existing breakpoint configuration for live debugging, including its location, enabled state, and conditional logic."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsUpdateRequest(
            path=_models.LiveDebuggerBreakpointsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LiveDebuggerBreakpointsUpdateRequestBody(repository=repository, filename=filename, line_number=line_number, enabled=enabled, condition=condition)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_live_debugger_breakpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_live_debugger_breakpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_live_debugger_breakpoint", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_live_debugger_breakpoint",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def update_live_debugger_breakpoint_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the breakpoint to update."),
    project_id: str = Field(..., description="The ID of the project containing the breakpoint. Retrieve available project IDs from the /api/projects/ endpoint."),
    repository: str | None = Field(None, description="The repository path or identifier where the breakpoint is set."),
    filename: str | None = Field(None, description="The source file path where the breakpoint is located."),
    line_number: int | None = Field(None, description="The line number in the source file where the breakpoint is set. Must be a non-negative integer.", ge=0, le=2147483647),
    enabled: bool | None = Field(None, description="Whether the breakpoint is currently active and will trigger during debugging."),
    condition: str | None = Field(None, description="An optional conditional expression that must evaluate to true for the breakpoint to trigger."),
) -> dict[str, Any]:
    """Partially update a live debugger breakpoint configuration, allowing you to modify its location, enabled status, or conditional logic without replacing the entire breakpoint."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsPartialUpdateRequest(
            path=_models.LiveDebuggerBreakpointsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LiveDebuggerBreakpointsPartialUpdateRequestBody(repository=repository, filename=filename, line_number=line_number, enabled=enabled, condition=condition)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_live_debugger_breakpoint_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_live_debugger_breakpoint_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_live_debugger_breakpoint_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_live_debugger_breakpoint_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def delete_live_debugger_breakpoint(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the breakpoint to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the breakpoint. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Remove a breakpoint from live debugging for a specific project. This operation permanently deletes the breakpoint configuration and stops any associated debugging at that location."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsDestroyRequest(
            path=_models.LiveDebuggerBreakpointsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_live_debugger_breakpoint: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_live_debugger_breakpoint")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_live_debugger_breakpoint", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_live_debugger_breakpoint",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def list_active_breakpoints(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="When set to true, only return breakpoints that are currently enabled. Omit to return all active breakpoints regardless of enabled status."),
    filename: str | None = Field(None, description="Filter results to return only breakpoints set in a specific file. Provide the filename or file path as it appears in your repository."),
    repository: str | None = Field(None, description="Filter results to return only breakpoints in a specific repository. Use the repository identifier in the format 'owner/repository' (e.g., 'PostHog/posthog')."),
) -> dict[str, Any]:
    """Retrieve the list of active breakpoints for a project using a Project API Key. External client applications can use this endpoint to fetch breakpoints and instrument their code accordingly."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsActiveRetrieveRequest(
            path=_models.LiveDebuggerBreakpointsActiveRetrieveRequestPath(project_id=project_id),
            query=_models.LiveDebuggerBreakpointsActiveRetrieveRequestQuery(enabled=enabled, filename=filename, repository=repository)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_active_breakpoints: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/active", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/active"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_active_breakpoints")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_active_breakpoints", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_active_breakpoints",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: live_debugger_breakpoints
@mcp.tool()
async def list_breakpoint_hits(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    breakpoint_ids: str | None = Field(None, description="Filter results to only include hits from specific breakpoints by providing one or more breakpoint IDs. Repeat this parameter for each breakpoint ID you want to include."),
    limit: int | None = Field(None, description="Maximum number of hit events to return in a single response. Defaults to 100 and cannot exceed 1000."),
    offset: int | None = Field(None, description="Number of results to skip before returning hits, useful for pagination through large result sets. Defaults to 0."),
) -> dict[str, Any]:
    """Retrieve breakpoint hit events from your application's runtime, including stack traces, local variables, and execution context. Results can be filtered by specific breakpoints and paginated."""

    # Construct request model with validation
    try:
        _request = _models.LiveDebuggerBreakpointsBreakpointHitsRetrieveRequest(
            path=_models.LiveDebuggerBreakpointsBreakpointHitsRetrieveRequestPath(project_id=project_id),
            query=_models.LiveDebuggerBreakpointsBreakpointHitsRetrieveRequestQuery(breakpoint_ids=breakpoint_ids, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_breakpoint_hits: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/live_debugger_breakpoints/breakpoint_hits", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/live_debugger_breakpoints/breakpoint_hits"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_breakpoint_hits")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_breakpoint_hits", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_breakpoint_hits",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def list_logs_alerts(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of alert records to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of log alerts for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsListRequest(
            path=_models.LogsAlertsListRequestPath(project_id=project_id),
            query=_models.LogsAlertsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_logs_alerts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_logs_alerts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_logs_alerts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_logs_alerts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def create_logs_alert(
    project_id: str = Field(..., description="The unique identifier of the project where the alert will be created. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A human-readable name for the alert. Limited to 255 characters.", max_length=255),
    filters: Any = Field(..., description="Filter criteria that define which logs trigger the alert. Must include at least one of: a list of severity levels (e.g., ERROR, WARN), a list of service names, or a filter group object for property-based filtering."),
    threshold_count: int = Field(..., description="The log count threshold that triggers the alert. Must be at least 1 and no more than 2,147,483,647.", ge=1, le=2147483647),
    enabled: bool | None = Field(None, description="Whether the alert is active and will trigger when conditions are met. Defaults to enabled if not specified."),
    threshold_operator: Literal["above", "below"] | None = Field(None, description="Whether the alert fires when the log count goes above or below the threshold. Defaults to 'above' if not specified."),
    window_minutes: int | None = Field(None, description="The time window in minutes over which logs are evaluated for the threshold. Can be 0 or greater.", ge=0, le=2147483647),
    evaluation_periods: int | None = Field(None, description="The total number of evaluation periods in the sliding window (M in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    datapoints_to_alarm: int | None = Field(None, description="How many periods within the evaluation window must breach the threshold to trigger the alert (N in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    cooldown_minutes: int | None = Field(None, description="The cooldown period in minutes after the alert fires during which it will not fire again, even if conditions are met. Can be 0 or greater.", ge=0, le=2147483647),
    snooze_until: str | None = Field(None, description="Temporarily suppress the alert until the specified date and time in ISO 8601 format. After this time, the alert will resume normal operation."),
) -> dict[str, Any]:
    """Create a new alert for a project's logs that triggers when log events match specified filter criteria and breach a configured threshold. Alerts can be configured with evaluation windows and cooldown periods to reduce noise."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsCreateRequest(
            path=_models.LogsAlertsCreateRequestPath(project_id=project_id),
            body=_models.LogsAlertsCreateRequestBody(name=name, enabled=enabled, filters=filters, threshold_count=threshold_count, threshold_operator=threshold_operator, window_minutes=window_minutes, evaluation_periods=evaluation_periods, datapoints_to_alarm=datapoints_to_alarm, cooldown_minutes=cooldown_minutes, snooze_until=snooze_until)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_logs_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_logs_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_logs_alert", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_logs_alert",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def get_logs_alert(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the logs alert configuration to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the logs alert. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific logs alert configuration by its unique identifier within a project."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsRetrieveRequest(
            path=_models.LogsAlertsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_logs_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_logs_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_logs_alert", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_logs_alert",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def update_logs_alert(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the logs alert configuration to update."),
    project_id: str = Field(..., description="The project ID containing the alert. Find your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the alert configuration. Maximum 255 characters.", max_length=255),
    filters: Any = Field(..., description="Filter criteria that define which logs trigger the alert. Must include at least one of: severityLevels (list of severity strings like 'error', 'warning'), serviceNames (list of service identifiers), or filterGroup (a property-based filter object)."),
    threshold_count: int = Field(..., description="The log count threshold that triggers the alert. Must be between 1 and 2,147,483,647.", ge=1, le=2147483647),
    enabled: bool | None = Field(None, description="Whether this alert is actively monitoring and firing. When disabled, the alert will not trigger notifications."),
    threshold_operator: Literal["above", "below"] | None = Field(None, description="Whether the alert fires when the log count goes above or below the threshold. Defaults to 'above'."),
    window_minutes: int | None = Field(None, description="The time window in minutes over which logs are counted for threshold evaluation. Must be between 0 and 2,147,483,647 minutes.", ge=0, le=2147483647),
    evaluation_periods: int | None = Field(None, description="Total number of evaluation periods in the sliding window (M in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    datapoints_to_alarm: int | None = Field(None, description="Number of periods within the evaluation window that must breach the threshold to fire the alert (N in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    cooldown_minutes: int | None = Field(None, description="Cooldown period in minutes after the alert fires before it can fire again. Must be between 0 and 2,147,483,647 minutes.", ge=0, le=2147483647),
    snooze_until: str | None = Field(None, description="Timestamp (ISO 8601 format) until which the alert is snoozed and will not fire. After this time, normal alerting resumes."),
) -> dict[str, Any]:
    """Update an existing logs alert configuration for a project. Modify alert criteria, thresholds, evaluation windows, and notification settings."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsUpdateRequest(
            path=_models.LogsAlertsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LogsAlertsUpdateRequestBody(name=name, enabled=enabled, filters=filters, threshold_count=threshold_count, threshold_operator=threshold_operator, window_minutes=window_minutes, evaluation_periods=evaluation_periods, datapoints_to_alarm=datapoints_to_alarm, cooldown_minutes=cooldown_minutes, snooze_until=snooze_until)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_logs_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_logs_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_logs_alert", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_logs_alert",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def update_logs_alert_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the logs alert configuration to update."),
    project_id: str = Field(..., description="The project ID containing the alert. Find your project ID by calling the /api/projects/ endpoint."),
    enabled: bool | None = Field(None, description="Enable or disable this alert configuration."),
    filters: Any | None = Field(None, description="Filter criteria to match logs for alerting. Must include at least one of: severityLevels (list of severity strings like 'error', 'warning'), serviceNames (list of service name strings), or filterGroup (a property filter group object)."),
    threshold_count: int | None = Field(None, description="The log count threshold that triggers the alert. Must be between 1 and 2,147,483,647.", ge=1, le=2147483647),
    threshold_operator: Literal["above", "below"] | None = Field(None, description="Whether the alert fires when the log count is above or below the threshold. Valid values: 'above' or 'below'. Defaults to 'above'."),
    window_minutes: int | None = Field(None, description="The time window in minutes over which logs are evaluated. Must be between 0 and 2,147,483,647 minutes.", ge=0, le=2147483647),
    evaluation_periods: int | None = Field(None, description="Total number of check periods in the sliding evaluation window (M in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    datapoints_to_alarm: int | None = Field(None, description="Number of periods within the evaluation window that must breach the threshold to fire the alert (N in N-of-M alerting). Must be between 1 and 10. Defaults to 1.", ge=1, le=10),
    cooldown_minutes: int | None = Field(None, description="Cooldown period in minutes after the alert fires before it can fire again. Must be between 0 and 2,147,483,647 minutes.", ge=0, le=2147483647),
    snooze_until: str | None = Field(None, description="Temporarily snooze this alert until the specified date and time (ISO 8601 format). After this time passes, the alert will resume normal operation."),
) -> dict[str, Any]:
    """Partially update a logs alert configuration for a project. Modify alert thresholds, filters, evaluation windows, and snooze settings to control when log-based alerts fire."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsPartialUpdateRequest(
            path=_models.LogsAlertsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.LogsAlertsPartialUpdateRequestBody(enabled=enabled, filters=filters, threshold_count=threshold_count, threshold_operator=threshold_operator, window_minutes=window_minutes, evaluation_periods=evaluation_periods, datapoints_to_alarm=datapoints_to_alarm, cooldown_minutes=cooldown_minutes, snooze_until=snooze_until)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_logs_alert_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_logs_alert_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_logs_alert_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_logs_alert_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs, logs
@mcp.tool()
async def delete_logs_alert(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the logs alert configuration to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the logs alert. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Delete a specific logs alert configuration from a project. This operation permanently removes the alert rule and stops any associated notifications."""

    # Construct request model with validation
    try:
        _request = _models.LogsAlertsDestroyRequest(
            path=_models.LogsAlertsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_logs_alert: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/alerts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/alerts/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_logs_alert")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_logs_alert", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_logs_alert",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def list_logs_attributes(project_id: str = Field(..., description="The unique identifier of the project. You can obtain the project ID by calling the /api/projects/ endpoint to list all accessible projects.")) -> dict[str, Any]:
    """Retrieve all available attributes for logs within a specific project. This operation returns the metadata about what attributes can be used to filter, search, or analyze logs in the project."""

    # Construct request model with validation
    try:
        _request = _models.LogsAttributesRetrieveRequest(
            path=_models.LogsAttributesRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_logs_attributes: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/attributes", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/attributes"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_logs_attributes")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_logs_attributes", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_logs_attributes",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def create_logs_export(project_id: str = Field(..., description="The unique identifier of the project for which to create the logs export. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Create a new logs export for a specified project. This initiates an export process to retrieve and download logs data from the project."""

    # Construct request model with validation
    try:
        _request = _models.LogsExportCreateRequest(
            path=_models.LogsExportCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_logs_export: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/export", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/export"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_logs_export")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_logs_export", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_logs_export",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def check_project_has_logs(project_id: str = Field(..., description="The unique identifier of the project to check for logs. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Check whether a project contains any logs. This operation determines if the specified project has log data available."""

    # Construct request model with validation
    try:
        _request = _models.LogsHasLogsRetrieveRequest(
            path=_models.LogsHasLogsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for check_project_has_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/has_logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/has_logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("check_project_has_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("check_project_has_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="check_project_has_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def create_logs_query(project_id: str = Field(..., description="The unique identifier of the project for which you want to create a logs query. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Create a new logs query for a specific project. This operation allows you to define and execute a query against the project's logs."""

    # Construct request model with validation
    try:
        _request = _models.LogsQueryCreateRequest(
            path=_models.LogsQueryCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_logs_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/query", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/query"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_logs_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_logs_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_logs_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def create_logs_sparkline(project_id: str = Field(..., description="The unique identifier of the project containing the logs. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Generate a sparkline visualization for logs in a specific project. This creates a compact graphical representation of log data trends over time."""

    # Construct request model with validation
    try:
        _request = _models.LogsSparklineCreateRequest(
            path=_models.LogsSparklineCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_logs_sparkline: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/sparkline", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/sparkline"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_logs_sparkline")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_logs_sparkline", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_logs_sparkline",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: logs
@mcp.tool()
async def list_log_values(project_id: str = Field(..., description="The unique identifier of the project. You can obtain the project ID by calling the /api/projects/ endpoint to list all accessible projects.")) -> dict[str, Any]:
    """Retrieve available log values for a specific project. This operation returns the distinct values that can be used for filtering or analyzing logs within the project."""

    # Construct request model with validation
    try:
        _request = _models.LogsValuesRetrieveRequest(
            path=_models.LogsValuesRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_log_values: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/logs/values", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/logs/values"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_log_values")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_log_values", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_log_values",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def list_notebooks(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebooks. Obtain this ID by calling the /api/projects/ endpoint."),
    contains: str | None = Field(None, description="Filter notebooks by specific criteria using colon-separated key-value pairs. Supports filtering by recording status (true/false) or a specific recording ID. Multiple filters can be combined using spaces or commas as separators."),
    limit: int | None = Field(None, description="Maximum number of notebook results to return in a single page of results."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of notebooks for a specific project. Supports filtering by recording status or specific recording IDs, and pagination control."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksListRequest(
            path=_models.NotebooksListRequestPath(project_id=project_id),
            query=_models.NotebooksListRequestQuery(contains=contains, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_notebooks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_notebooks")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_notebooks", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_notebooks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def create_notebook(
    project_id: str = Field(..., description="The unique identifier of the project where the notebook will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    title: str | None = Field(None, description="The display name for the notebook. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Create a new notebook within a specified project. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksCreateRequest(
            path=_models.NotebooksCreateRequestPath(project_id=project_id),
            body=_models.NotebooksCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_notebook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_notebook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_notebook", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_notebook",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def get_notebook(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to retrieve within the specified project."),
) -> dict[str, Any]:
    """Retrieve a specific notebook by its short ID within a project. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksRetrieveRequest(
            path=_models.NotebooksRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_notebook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_notebook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_notebook", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_notebook",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def update_notebook(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to update."),
    title: str | None = Field(None, description="The new title for the notebook. Must be 256 characters or fewer.", max_length=256),
) -> dict[str, Any]:
    """Update the title or other properties of an existing notebook within a project. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksUpdateRequest(
            path=_models.NotebooksUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksUpdateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_notebook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_notebook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_notebook", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_notebook",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def update_notebook_partial(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to update."),
    title: str | None = Field(None, description="The new title for the notebook. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Partially update a notebook's metadata within a project. Currently in early access; the API may have breaking changes without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksPartialUpdateRequest(
            path=_models.NotebooksPartialUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksPartialUpdateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_notebook_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_notebook_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_notebook_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_notebook_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def soft_delete_notebook(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to be marked as deleted."),
) -> dict[str, Any]:
    """Soft-delete a notebook by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksDestroyRequest(
            path=_models.NotebooksDestroyRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for soft_delete_notebook: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("soft_delete_notebook")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("soft_delete_notebook", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="soft_delete_notebook",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def get_notebook_activity(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook whose activity history you want to retrieve."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific notebook, including changes and interactions. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksActivityRetrieve2Request(
            path=_models.NotebooksActivityRetrieve2RequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_notebook_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_notebook_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_notebook_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_notebook_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def execute_notebook_hogql_query(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook in which to execute the HogQL query."),
    title: str | None = Field(None, description="Optional title for the notebook. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Execute a HogQL query within a notebook and retrieve results. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksHogqlExecuteCreateRequest(
            path=_models.NotebooksHogqlExecuteCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksHogqlExecuteCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for execute_notebook_hogql_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/hogql/execute", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/hogql/execute"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("execute_notebook_hogql_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("execute_notebook_hogql_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="execute_notebook_hogql_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def get_notebook_kernel_dataframe(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook within the specified project. This uniquely identifies which notebook kernel's dataframe to retrieve."),
) -> dict[str, Any]:
    """Retrieves the current dataframe state from a notebook kernel's execution environment. This allows you to access data that has been computed or loaded within the notebook's active kernel session."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelDataframeRetrieveRequest(
            path=_models.NotebooksKernelDataframeRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_notebook_kernel_dataframe: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/dataframe", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/dataframe"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_notebook_kernel_dataframe")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_notebook_kernel_dataframe", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_notebook_kernel_dataframe",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def execute_notebook_kernel(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to execute code in."),
    title: str | None = Field(None, description="Optional title for the notebook, up to 256 characters in length.", max_length=256),
) -> dict[str, Any]:
    """Execute code in a notebook kernel. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelExecuteCreateRequest(
            path=_models.NotebooksKernelExecuteCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksKernelExecuteCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for execute_notebook_kernel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/execute", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/execute"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("execute_notebook_kernel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("execute_notebook_kernel", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="execute_notebook_kernel",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def execute_notebook_kernel_stream(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. Retrieve this ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook to execute code in."),
    title: str | None = Field(None, description="Optional title for the notebook execution context. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Execute code in a notebook kernel and stream the results. This operation supports real-time execution of notebook cells with streaming output."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelExecuteStreamCreateRequest(
            path=_models.NotebooksKernelExecuteStreamCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksKernelExecuteStreamCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for execute_notebook_kernel_stream: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/execute/stream", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/execute/stream"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("execute_notebook_kernel_stream")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("execute_notebook_kernel_stream", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="execute_notebook_kernel_stream",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def restart_notebook_kernel(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook whose kernel should be restarted."),
    title: str | None = Field(None, description="Optional title to assign to the notebook. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Restart the kernel of a notebook, clearing its execution state and memory. This operation is useful for resetting the notebook's computational environment."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelRestartCreateRequest(
            path=_models.NotebooksKernelRestartCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksKernelRestartCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for restart_notebook_kernel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/restart", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/restart"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("restart_notebook_kernel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("restart_notebook_kernel", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="restart_notebook_kernel",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def start_notebook_kernel(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook for which to start the kernel."),
    title: str | None = Field(None, description="Optional display name for the notebook, up to 256 characters in length.", max_length=256),
) -> dict[str, Any]:
    """Start a kernel for a notebook in a project. This initializes the computational environment needed to execute notebook cells."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelStartCreateRequest(
            path=_models.NotebooksKernelStartCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksKernelStartCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for start_notebook_kernel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/start", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/start"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("start_notebook_kernel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("start_notebook_kernel", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="start_notebook_kernel",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def get_notebook_kernel_status(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook whose kernel status you want to retrieve."),
) -> dict[str, Any]:
    """Retrieve the current status of a notebook kernel. Returns kernel state information for the specified notebook within a project."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelStatusRetrieveRequest(
            path=_models.NotebooksKernelStatusRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_notebook_kernel_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/status", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/status"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_notebook_kernel_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_notebook_kernel_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_notebook_kernel_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def stop_notebook_kernel(
    project_id: str = Field(..., description="The unique identifier of the project containing the notebook. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the notebook whose kernel should be stopped."),
    title: str | None = Field(None, description="Optional title for the notebook. Must not exceed 256 characters.", max_length=256),
) -> dict[str, Any]:
    """Stops the kernel execution for a notebook in a project. This operation terminates any running computations in the notebook's kernel."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksKernelStopCreateRequest(
            path=_models.NotebooksKernelStopCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.NotebooksKernelStopCreateRequestBody(title=title)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for stop_notebook_kernel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/{short_id}/kernel/stop", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/{short_id}/kernel/stop"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("stop_notebook_kernel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("stop_notebook_kernel", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="stop_notebook_kernel",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def list_notebook_activity(project_id: str = Field(..., description="The unique identifier of the project containing the notebooks. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve activity history for notebooks within a project. This endpoint provides access to notebook-related events and changes (early access feature subject to API changes)."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksActivityRetrieveRequest(
            path=_models.NotebooksActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_notebook_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_notebook_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_notebook_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_notebook_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: notebooks
@mcp.tool()
async def list_recording_comments(project_id: str = Field(..., description="The unique identifier of the project containing the notebook recordings. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve all comments associated with notebook recordings in a project. This feature is in early access and the API may change without notice."""

    # Construct request model with validation
    try:
        _request = _models.NotebooksRecordingCommentsRetrieveRequest(
            path=_models.NotebooksRecordingCommentsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_recording_comments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/notebooks/recording_comments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/notebooks/recording_comments"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recording_comments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recording_comments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recording_comments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def list_object_media_previews(
    project_id: str = Field(..., description="The unique identifier of the project containing the media previews. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit to use the API's default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of media previews for objects within a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsListRequest(
            path=_models.ObjectMediaPreviewsListRequestPath(project_id=project_id),
            query=_models.ObjectMediaPreviewsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_object_media_previews: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_object_media_previews")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_object_media_previews", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_object_media_previews",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def create_object_media_preview(
    project_id: str = Field(..., description="The unique identifier of the project containing the object. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    metadata: Any | None = Field(None, description="Optional metadata object to attach to the media preview, such as tags, descriptions, or custom properties relevant to the preview."),
) -> dict[str, Any]:
    """Creates a media preview for an object within a specified project. This generates a preview representation of media content associated with a project object."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsCreateRequest(
            path=_models.ObjectMediaPreviewsCreateRequestPath(project_id=project_id),
            body=_models.ObjectMediaPreviewsCreateRequestBody(metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_object_media_preview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_object_media_preview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_object_media_preview", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_object_media_preview",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def get_object_media_preview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the object media preview to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the media preview. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific object media preview by its unique identifier within a project. This operation fetches metadata and preview information for a single media asset."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsRetrieveRequest(
            path=_models.ObjectMediaPreviewsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_object_media_preview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_object_media_preview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_object_media_preview", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_object_media_preview",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def update_object_media_preview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the object media preview to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the media preview. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    metadata: Any | None = Field(None, description="Additional metadata or configuration properties to apply to the media preview."),
) -> dict[str, Any]:
    """Update an object media preview for a specific project. Modify preview settings or metadata associated with a media preview resource identified by its UUID."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsUpdateRequest(
            path=_models.ObjectMediaPreviewsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ObjectMediaPreviewsUpdateRequestBody(metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_object_media_preview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_object_media_preview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_object_media_preview", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_object_media_preview",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def update_object_media_preview_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the object media preview to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the object media preview. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    metadata: Any | None = Field(None, description="Optional metadata object containing the fields to update for this object media preview."),
) -> dict[str, Any]:
    """Partially update an object media preview within a project. Use this to modify specific fields of an existing media preview without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsPartialUpdateRequest(
            path=_models.ObjectMediaPreviewsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ObjectMediaPreviewsPartialUpdateRequestBody(metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_object_media_preview_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_object_media_preview_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_object_media_preview_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_object_media_preview_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def delete_object_media_preview(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the object media preview to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the object media preview. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific object media preview from a project. This operation removes the preview resource identified by its UUID."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsDestroyRequest(
            path=_models.ObjectMediaPreviewsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_object_media_preview: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_object_media_preview")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_object_media_preview", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_object_media_preview",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: object_media_previews
@mcp.tool()
async def get_preferred_media_preview_for_event(project_id: str = Field(..., description="The unique identifier of the project containing the event. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the preferred media preview for an event definition, prioritizing the most recent user-uploaded media, followed by the most recent exported asset. Requires an event_definition query parameter to specify which event to retrieve media for."""

    # Construct request model with validation
    try:
        _request = _models.ObjectMediaPreviewsPreferredForEventRetrieveRequest(
            path=_models.ObjectMediaPreviewsPreferredForEventRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_preferred_media_preview_for_event: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/object_media_previews/preferred_for_event", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/object_media_previews/preferred_for_event"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_preferred_media_preview_for_event")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_preferred_media_preview_for_event", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_preferred_media_preview_for_event",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def list_persisted_folders(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of persisted folders for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderListRequest(
            path=_models.PersistedFolderListRequestPath(project_id=project_id),
            query=_models.PersistedFolderListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_persisted_folders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_persisted_folders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_persisted_folders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_persisted_folders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def create_persisted_folder(
    project_id: str = Field(..., description="The unique identifier of the project where the persisted folder will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    type_: Literal["home", "pinned", "custom_products"] = Field(..., alias="type", description="The folder type that determines its purpose and behavior: 'home' for the default home folder, 'pinned' for frequently accessed items, or 'custom_products' for custom product collections."),
    protocol: str | None = Field(None, description="Optional protocol identifier for the folder's storage or access mechanism, up to 64 characters in length.", max_length=64),
    path: str | None = Field(None, description="Optional path specification for the folder's location or resource reference within the project structure."),
) -> dict[str, Any]:
    """Create a new persisted folder within a project. Persisted folders organize content by type (home, pinned, or custom products) and can optionally specify a protocol and path for storage or access configuration."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderCreateRequest(
            path=_models.PersistedFolderCreateRequestPath(project_id=project_id),
            body=_models.PersistedFolderCreateRequestBody(type_=type_, protocol=protocol, path=path)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_persisted_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_persisted_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_persisted_folder", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_persisted_folder",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def get_persisted_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the persisted folder to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the persisted folder. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific persisted folder by its unique identifier within a project. Returns the folder's metadata and configuration."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderRetrieveRequest(
            path=_models.PersistedFolderRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persisted_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persisted_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persisted_folder", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persisted_folder",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def update_persisted_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the persisted folder to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the persisted folder. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    type_: Literal["home", "pinned", "custom_products"] = Field(..., alias="type", description="The classification type for the persisted folder: 'home' for the home directory, 'pinned' for pinned folders, or 'custom_products' for custom product folders."),
    protocol: str | None = Field(None, description="The storage protocol to use for the persisted folder (e.g., 's3', 'gcs', 'azure'). Maximum 64 characters.", max_length=64),
    path: str | None = Field(None, description="The file system path or location identifier for the persisted folder within the specified protocol."),
) -> dict[str, Any]:
    """Update the configuration of a persisted folder within a project, including its type classification and storage protocol settings."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderUpdateRequest(
            path=_models.PersistedFolderUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersistedFolderUpdateRequestBody(type_=type_, protocol=protocol, path=path)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_persisted_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_persisted_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_persisted_folder", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_persisted_folder",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def update_persisted_folder_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the persisted folder to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the persisted folder. Retrieve available project IDs from the /api/projects/ endpoint."),
    protocol: str | None = Field(None, description="The protocol type for the persisted folder connection, up to 64 characters in length.", max_length=64),
    path: str | None = Field(None, description="The file system path or location associated with the persisted folder."),
) -> dict[str, Any]:
    """Partially update a persisted folder's configuration within a project. Allows modification of protocol and path settings for an existing persisted folder."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderPartialUpdateRequest(
            path=_models.PersistedFolderPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersistedFolderPartialUpdateRequestBody(protocol=protocol, path=path)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_persisted_folder_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_persisted_folder_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_persisted_folder_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_persisted_folder_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persisted_folder
@mcp.tool()
async def delete_persisted_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the persisted folder to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the persisted folder. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a persisted folder from a project. This operation removes the folder and all its associated data."""

    # Construct request model with validation
    try:
        _request = _models.PersistedFolderDestroyRequest(
            path=_models.PersistedFolderDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_persisted_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persisted_folder/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persisted_folder/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_persisted_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_persisted_folder", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_persisted_folder",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def list_persons(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results."),
    offset: int | None = Field(None, description="The starting position (zero-indexed) from which to return results, used for pagination."),
    properties: list[_models.PropertyModel] | None = Field(None, description="Filter results by person properties. Provide an array of property conditions to narrow the list of returned persons."),
) -> dict[str, Any]:
    """Retrieve a paginated list of persons in a project. This endpoint supports filtering by person properties and is read-only; to create or update persons, use the capture API, property operations ($set/$unset), or SDKs."""

    # Construct request model with validation
    try:
        _request = _models.PersonsListRequest(
            path=_models.PersonsListRequestPath(project_id=project_id),
            query=_models.PersonsListRequestQuery(limit=limit, offset=offset, properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_persons: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_persons")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_persons", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_persons",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_person(
    id_: str = Field(..., alias="id", description="The unique identifier of the person to retrieve. Can be either a numeric ID or a UUID format."),
    project_id: str = Field(..., description="The unique identifier of the project containing the person. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific person in a project. This endpoint supports reading person data by numeric ID or UUID."""

    # Construct request model with validation
    try:
        _request = _models.PersonsRetrieveRequest(
            path=_models.PersonsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_person: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_person")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_person", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_person",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def update_person_properties(
    id_: str = Field(..., alias="id", description="The unique identifier of the person to update. Accepts either a numeric ID or UUID format."),
    project_id: str = Field(..., description="The ID of the project containing the person. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="A key-value map of person properties to update. Only the properties specified will be modified using set operations; omitted properties are unaffected."),
) -> dict[str, Any]:
    """Update specific properties on a person record. Only the properties provided in the request will be updated; existing properties not included will remain unchanged. To remove properties, use the delete_property endpoint instead."""

    # Construct request model with validation
    try:
        _request = _models.PersonsUpdateRequest(
            path=_models.PersonsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersonsUpdateRequestBody(properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_person_properties: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_person_properties")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_person_properties", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_person_properties",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_person_activity(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the person whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the person. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific person in a project. This endpoint provides read-only access to person activity data; to create or update person information, use the capture API, user properties ($set/$unset), or one of the PostHog SDKs."""

    # Construct request model with validation
    try:
        _request = _models.PersonsActivityRetrieve2Request(
            path=_models.PersonsActivityRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_person_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_person_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_person_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_person_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def remove_person_property(
    id_: str = Field(..., alias="id", description="The unique identifier of the person. Can be either a numeric ID or a UUID format."),
    project_id: str = Field(..., description="The ID of the project containing the person. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    unset: str = Field(..., description="The property key to remove from the person's profile. Specify the exact property name you want to delete."),
) -> dict[str, Any]:
    """Remove a specific property from a person record. This operation deletes a property key and its value from the person's profile."""

    # Construct request model with validation
    try:
        _request = _models.PersonsDeletePropertyCreateRequest(
            path=_models.PersonsDeletePropertyCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersonsDeletePropertyCreateRequestBody(unset=unset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_person_property: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}/delete_property", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}/delete_property"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_person_property")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_person_property", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_person_property",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_person_properties_timeline(
    id_: int = Field(..., alias="id", description="The unique identifier of the person whose property timeline you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the person. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the timeline of property changes for a specific person, showing how their properties have evolved over time."""

    # Construct request model with validation
    try:
        _request = _models.PersonsPropertiesTimelineRetrieveRequest(
            path=_models.PersonsPropertiesTimelineRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_person_properties_timeline: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}/properties_timeline", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}/properties_timeline"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_person_properties_timeline")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_person_properties_timeline", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_person_properties_timeline",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def split_person(
    id_: int = Field(..., alias="id", description="The unique identifier of the person record to split. Must be a positive integer."),
    project_id: str = Field(..., description="The unique identifier of the project containing the person. Retrieve available project IDs from the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="Optional key-value mapping of person properties to assign to the split identity. Properties should correspond to valid person property keys defined in your project."),
) -> dict[str, Any]:
    """Split a person record into separate identities. This operation divides a single person's data into multiple distinct person records, useful for resolving incorrectly merged user identities."""

    # Construct request model with validation
    try:
        _request = _models.PersonsSplitCreateRequest(
            path=_models.PersonsSplitCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersonsSplitCreateRequestBody(properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for split_person: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}/split", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}/split"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("split_person")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("split_person", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="split_person",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def set_person_property(
    id_: str = Field(..., alias="id", description="The unique identifier of the person to update. Accepts either a numeric ID or UUID format."),
    project_id: str = Field(..., description="The ID of the project containing the person. Retrieve available project IDs by calling /api/projects/."),
    key: str = Field(..., description="The property key name to set on the person. Use snake_case or camelCase naming conventions."),
    value: Any = Field(..., description="The value to assign to the property. Accepts strings, numbers, booleans, or objects."),
) -> dict[str, Any]:
    """Set or update a single property on a person. This endpoint modifies individual person properties; for bulk person creation or updates, use the capture API, $set/$unset properties, or SDKs instead."""

    # Construct request model with validation
    try:
        _request = _models.PersonsUpdatePropertyCreateRequest(
            path=_models.PersonsUpdatePropertyCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.PersonsUpdatePropertyCreateRequestBody(key=key, value=value)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for set_person_property: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/{id}/update_property", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/{id}/update_property"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("set_person_property")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("set_person_property", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="set_person_property",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def list_persons_activity(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve activity records for persons in a project. This endpoint supports reading and filtering person activity data; for creating or updating person information, use the capture API, user properties ($set/$unset), or PostHog SDKs."""

    # Construct request model with validation
    try:
        _request = _models.PersonsActivityRetrieveRequest(
            path=_models.PersonsActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_persons_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_persons_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_persons_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_persons_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_by_distinct_ids(
    project_id: str = Field(..., description="The unique identifier of the project you want to access. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="Optional key-value map of person properties to filter or set via $set and $set_once operations. Properties should be formatted as a JSON object with string keys and values of appropriate types."),
) -> dict[str, Any]:
    """Retrieve person records by their distinct IDs. This endpoint is optimized for reading and deleting persons; for creating or updating persons, use the capture API, $set/$unset properties, or PostHog SDKs instead."""

    # Construct request model with validation
    try:
        _request = _models.PersonsBatchByDistinctIdsCreateRequest(
            path=_models.PersonsBatchByDistinctIdsCreateRequestPath(project_id=project_id),
            body=_models.PersonsBatchByDistinctIdsCreateRequestBody(properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_by_distinct_ids: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/batch_by_distinct_ids", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/batch_by_distinct_ids"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_by_distinct_ids")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_by_distinct_ids", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_by_distinct_ids",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_by_uuids(
    project_id: str = Field(..., description="The unique identifier of the project to query. Find your project ID by calling /api/projects/."),
    properties: Any | None = Field(None, description="Optional key-value map of person properties to filter or set via $set and $set_once operations."),
) -> dict[str, Any]:
    """Retrieve multiple persons from a project by their UUIDs. This endpoint is optimized for reading and deleting persons; for creating or updating persons, use the capture API, $set/$unset properties, or SDKs instead."""

    # Construct request model with validation
    try:
        _request = _models.PersonsBatchByUuidsCreateRequest(
            path=_models.PersonsBatchByUuidsCreateRequestPath(project_id=project_id),
            body=_models.PersonsBatchByUuidsCreateRequestBody(properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_by_uuids: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/batch_by_uuids", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/batch_by_uuids"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_by_uuids")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_by_uuids", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_by_uuids",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def delete_persons_bulk(
    project_id: str = Field(..., description="The unique identifier of the project containing the persons to delete. Retrieve your project ID from the /api/projects/ endpoint."),
    delete_events: bool | None = Field(None, description="When enabled, queues all events associated with the specified persons for deletion. Defaults to false, preserving event data."),
    delete_recordings: bool | None = Field(None, description="When enabled, queues all session recordings associated with the specified persons for deletion. Defaults to false, preserving recording data."),
    keep_person: bool | None = Field(None, description="When enabled, retains the person records themselves but deletes their associated events and recordings. Defaults to false, which deletes person records entirely."),
) -> dict[str, Any]:
    """Permanently delete multiple persons from a project by their PostHog or distinct IDs, with options to also remove associated events and recordings. Supports up to 1000 IDs per request; only data captured before the request is deleted."""

    # Construct request model with validation
    try:
        _request = _models.PersonsBulkDeleteCreateRequest(
            path=_models.PersonsBulkDeleteCreateRequestPath(project_id=project_id),
            body=_models.PersonsBulkDeleteCreateRequestBody(delete_events=delete_events, delete_recordings=delete_recordings, keep_person=keep_person)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_persons_bulk: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/bulk_delete", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/bulk_delete"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_persons_bulk")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_persons_bulk", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_persons_bulk",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def list_cohorts_for_person(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    person_id: str = Field(..., description="The unique identifier or UUID of the person whose cohort memberships you want to retrieve."),
) -> dict[str, Any]:
    """Retrieve all cohorts that a specific person belongs to within a project. This is a read-only operation; to modify person data, use the capture API or property operations."""

    # Construct request model with validation
    try:
        _request = _models.PersonsCohortsRetrieveRequest(
            path=_models.PersonsCohortsRetrieveRequestPath(project_id=project_id),
            query=_models.PersonsCohortsRetrieveRequestQuery(person_id=person_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_cohorts_for_person: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/cohorts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/cohorts"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_cohorts_for_person")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_cohorts_for_person", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_cohorts_for_person",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_funnel(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve persons funnel data for a specific project. This endpoint supports reading and filtering person records; for creating or updating persons, use the capture API, user properties ($set/$unset), or PostHog SDKs."""

    # Construct request model with validation
    try:
        _request = _models.PersonsFunnelRetrieveRequest(
            path=_models.PersonsFunnelRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_funnel: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/funnel", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/funnel"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_funnel")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_funnel", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_funnel",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_funnel_correlation(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve correlation analysis data for persons within a funnel. This endpoint analyzes how user properties and events correlate with funnel progression."""

    # Construct request model with validation
    try:
        _request = _models.PersonsFunnelCorrelationRetrieveRequest(
            path=_models.PersonsFunnelCorrelationRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_funnel_correlation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/funnel/correlation", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/funnel/correlation"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_funnel_correlation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_funnel_correlation", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_funnel_correlation",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_lifecycle(project_id: str = Field(..., description="The unique identifier of the project to access. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve person lifecycle data for a project. This endpoint supports reading and deleting persons; for creating or updating persons, use the capture API, user properties ($set/$unset), or PostHog SDKs instead."""

    # Construct request model with validation
    try:
        _request = _models.PersonsLifecycleRetrieveRequest(
            path=_models.PersonsLifecycleRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_lifecycle: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/lifecycle", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/lifecycle"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_lifecycle")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_lifecycle", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_lifecycle",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_person_properties_at_time(
    project_id: str = Field(..., description="The unique identifier of the project containing the person. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    timestamp: str = Field(..., description="The point in time for which to reconstruct person properties, specified as an ISO 8601 datetime string in UTC (e.g., '2023-06-15T14:30:00Z'). Only events occurring on or before this timestamp are included."),
    include_set_once: bool | None = Field(None, description="Whether to include properties set via $set_once operations in the reconstruction. When enabled, captures properties that were only set once and never updated."),
) -> dict[str, Any]:
    """Retrieve a person's properties as they existed at a specific point in time by reconstructing their state from historical $set and $set_once events in ClickHouse."""

    # Construct request model with validation
    try:
        _request = _models.PersonsPropertiesAtTimeRetrieveRequest(
            path=_models.PersonsPropertiesAtTimeRetrieveRequestPath(project_id=project_id),
            query=_models.PersonsPropertiesAtTimeRetrieveRequestQuery(include_set_once=include_set_once, timestamp=timestamp)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_person_properties_at_time: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/properties_at_time", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/properties_at_time"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_person_properties_at_time")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_person_properties_at_time", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_person_properties_at_time",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def reset_person_distinct_id(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="Optional key-value map of person properties to set or update using $set and $set_once operations. Properties are applied during the reset operation."),
) -> dict[str, Any]:
    """Reset a distinct_id for a deleted person, making the identifier available for reuse. This operation is useful when you need to reassign a previously used distinct_id to a new person record."""

    # Construct request model with validation
    try:
        _request = _models.PersonsResetPersonDistinctIdCreateRequest(
            path=_models.PersonsResetPersonDistinctIdCreateRequestPath(project_id=project_id),
            body=_models.PersonsResetPersonDistinctIdCreateRequestBody(properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for reset_person_distinct_id: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/reset_person_distinct_id", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/reset_person_distinct_id"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("reset_person_distinct_id")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("reset_person_distinct_id", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="reset_person_distinct_id",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def get_persons_stickiness(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve stickiness metrics for persons in a project, indicating how consistently users engage with your product over time."""

    # Construct request model with validation
    try:
        _request = _models.PersonsStickinessRetrieveRequest(
            path=_models.PersonsStickinessRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_persons_stickiness: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/stickiness", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/stickiness"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_persons_stickiness")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_persons_stickiness", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_persons_stickiness",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def list_persons_trends(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve trend data for persons in a project. This endpoint provides analytics on person-related metrics and trends over time."""

    # Construct request model with validation
    try:
        _request = _models.PersonsTrendsRetrieveRequest(
            path=_models.PersonsTrendsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_persons_trends: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/trends", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/trends"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_persons_trends")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_persons_trends", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_persons_trends",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: persons, persons
@mcp.tool()
async def list_person_property_values(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    key: str = Field(..., description="The person property key to retrieve values for (e.g., 'email', 'plan', 'role'). This identifies which user property you want to inspect."),
    value: str | None = Field(None, description="Optional search filter to narrow results to values containing this substring. The search is case-insensitive and matches any part of the value."),
) -> dict[str, Any]:
    """Retrieve all distinct values for a specific person property across your project, with optional filtering. Useful for understanding the range of values users have for properties like email, plan, or role."""

    # Construct request model with validation
    try:
        _request = _models.PersonsValuesRetrieveRequest(
            path=_models.PersonsValuesRetrieveRequestPath(project_id=project_id),
            query=_models.PersonsValuesRetrieveRequestQuery(key=key, value=value)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_person_property_values: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/persons/values", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/persons/values"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_person_property_values")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_person_property_values", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_person_property_values",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: plugin_configs
@mcp.tool()
async def list_plugin_config_logs(
    plugin_config_id: int = Field(..., description="The unique identifier of the plugin configuration whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the plugin configuration. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of log entries to return in a single response page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position in the result set from which to return logs. Use with limit to paginate through results."),
) -> dict[str, Any]:
    """Retrieve execution logs for a specific plugin configuration within a project. Logs are paginated and can be filtered by offset and limit parameters."""

    # Construct request model with validation
    try:
        _request = _models.PluginConfigsLogsListRequest(
            path=_models.PluginConfigsLogsListRequestPath(plugin_config_id=plugin_config_id, project_id=project_id),
            query=_models.PluginConfigsLogsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_plugin_config_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/plugin_configs/{plugin_config_id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/plugin_configs/{plugin_config_id}/logs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_plugin_config_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_plugin_config_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_plugin_config_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def list_product_tours(
    project_id: str = Field(..., description="The unique identifier of the project containing the product tours. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of product tours to return in a single response page. Omit or adjust to control result set size."),
    offset: int | None = Field(None, description="Zero-based starting position for results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of product tours for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursListRequest(
            path=_models.ProductToursListRequestPath(project_id=project_id),
            query=_models.ProductToursListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_product_tours: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_product_tours")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_product_tours", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_product_tours",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def create_product_tour(
    project_id: str = Field(..., description="The unique identifier of the project where the product tour will be created. Retrieve project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the product tour. Must be 400 characters or fewer.", max_length=400),
    description: str | None = Field(None, description="Optional detailed description of the product tour's purpose and content."),
    auto_launch: bool | None = Field(None, description="Optional flag to enable automatic launching of the tour. When enabled, the tour will launch automatically if within the scheduled date range."),
    start_date: str | None = Field(None, description="Optional start date and time for when the tour becomes active. Use ISO 8601 format (e.g., 2024-01-15T10:30:00Z)."),
    end_date: str | None = Field(None, description="Optional end date and time for when the tour stops being active. Use ISO 8601 format (e.g., 2024-02-15T10:30:00Z)."),
) -> dict[str, Any]:
    """Create a new product tour for a project. Product tours guide users through features with interactive steps and can be scheduled to launch automatically within a specified date range."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursCreateRequest(
            path=_models.ProductToursCreateRequestPath(project_id=project_id),
            body=_models.ProductToursCreateRequestBody(name=name, description=description, auto_launch=auto_launch, start_date=start_date, end_date=end_date)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_product_tour: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_product_tour")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_product_tour", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_product_tour",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def get_product_tour(
    id_: str = Field(..., alias="id", description="The unique identifier of the product tour, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific product tour by its ID within a project. Returns detailed information about the product tour configuration and content."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursRetrieveRequest(
            path=_models.ProductToursRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_tour: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_tour")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_tour", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_tour",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def update_product_tour(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the product tour. Must not exceed 400 characters.", max_length=400),
    description: str | None = Field(None, description="A detailed description of the product tour's purpose and content."),
    auto_launch: bool | None = Field(None, description="Whether the product tour should launch automatically when conditions are met."),
    start_date: str | None = Field(None, description="The date and time when the product tour becomes active. Use ISO 8601 format."),
    end_date: str | None = Field(None, description="The date and time when the product tour expires or becomes inactive. Use ISO 8601 format."),
) -> dict[str, Any]:
    """Update an existing product tour within a project. Modify tour details such as name, description, launch behavior, and scheduling dates."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursUpdateRequest(
            path=_models.ProductToursUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProductToursUpdateRequestBody(name=name, description=description, auto_launch=auto_launch, start_date=start_date, end_date=end_date)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_tour: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_tour")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_tour", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_tour",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def update_product_tour_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="The product tour's description text."),
    auto_launch: bool | None = Field(None, description="Whether the product tour should launch automatically when conditions are met."),
    start_date: str | None = Field(None, description="The date and time when the product tour becomes active, specified in ISO 8601 format."),
    end_date: str | None = Field(None, description="The date and time when the product tour stops being active, specified in ISO 8601 format."),
) -> dict[str, Any]:
    """Partially update a product tour's configuration, including its description, auto-launch settings, and scheduling dates. Only the fields provided will be updated."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursPartialUpdateRequest(
            path=_models.ProductToursPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProductToursPartialUpdateRequestBody(description=description, auto_launch=auto_launch, start_date=start_date, end_date=end_date)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_tour_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_tour_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_tour_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_tour_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def delete_product_tour(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a product tour from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursDestroyRequest(
            path=_models.ProductToursDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_tour: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_tour")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_tour", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_tour",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def delete_product_tour_draft(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour whose draft should be discarded."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Discard and remove draft content for a product tour, reverting any unsaved changes within the specified project."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursDiscardDraftDestroyRequest(
            path=_models.ProductToursDiscardDraftDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_product_tour_draft: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}/discard_draft", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}/discard_draft"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_product_tour_draft")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_product_tour_draft", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_product_tour_draft",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def update_product_tour_draft(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. Find your project ID by calling /api/projects/."),
    description: str | None = Field(None, description="The description text for the product tour."),
    auto_launch: bool | None = Field(None, description="Whether the product tour should automatically launch when conditions are met."),
    start_date: str | None = Field(None, description="The date and time when the product tour becomes active, in ISO 8601 format."),
    end_date: str | None = Field(None, description="The date and time when the product tour expires or becomes inactive, in ISO 8601 format."),
) -> dict[str, Any]:
    """Partially update draft content for a product tour with server-side merge. Changes are saved as draft without triggering any side effects or publishing."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursDraftPartialUpdateRequest(
            path=_models.ProductToursDraftPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProductToursDraftPartialUpdateRequestBody(description=description, auto_launch=auto_launch, start_date=start_date, end_date=end_date)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_product_tour_draft: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}/draft", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}/draft"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_product_tour_draft")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_product_tour_draft", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_product_tour_draft",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def get_product_tour_draft_status(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour whose draft status you want to check."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Poll the draft status of a product tour to detect changes. This lightweight endpoint is designed for efficient change detection without retrieving the full tour data."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursDraftStatusRetrieveRequest(
            path=_models.ProductToursDraftStatusRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_product_tour_draft_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}/draft_status", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}/draft_status"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_product_tour_draft_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_product_tour_draft_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_product_tour_draft_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def generate_product_tour_steps(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the product tour to generate steps for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the product tour. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    title: str | None = Field(None, description="The title or name of the product tour, used as context for AI-generated step content."),
    goal: str | None = Field(None, description="The primary objective or goal of the product tour, used to guide the AI generation of step content and ensure alignment with tour purpose."),
    steps: list[dict[str, Any]] | None = Field(None, description="An array of tour step objects to generate content for. Each step should include relevant metadata that the AI will use to create contextual descriptions and guidance."),
) -> dict[str, Any]:
    """Generate AI-powered step content for a product tour within a specific project. Use this to automatically create tour step descriptions and guidance based on the tour's title, goal, and existing step structure."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursGenerateCreateRequest(
            path=_models.ProductToursGenerateCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProductToursGenerateCreateRequestBody(title=title, goal=goal, steps=steps)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for generate_product_tour_steps: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}/generate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}/generate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("generate_product_tour_steps")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("generate_product_tour_steps", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="generate_product_tour_steps",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: product_tours
@mcp.tool()
async def publish_product_tour_draft(
    id_: str = Field(..., alias="id", description="The UUID identifier of the product tour to publish."),
    project_id: str = Field(..., description="The project ID containing the product tour. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the product tour. Maximum 400 characters.", max_length=400),
    description: str | None = Field(None, description="An optional description providing additional context about the product tour."),
    auto_launch: bool | None = Field(None, description="An optional boolean flag to enable automatic launch of the product tour."),
    start_date: str | None = Field(None, description="An optional start date and time in ISO 8601 format when the product tour becomes active."),
    end_date: str | None = Field(None, description="An optional end date and time in ISO 8601 format when the product tour expires or becomes inactive."),
) -> dict[str, Any]:
    """Publish a product tour draft to live, applying full validation and triggering associated side effects. Optionally merge changes into the draft before publishing to save and publish in a single request."""

    # Construct request model with validation
    try:
        _request = _models.ProductToursPublishDraftCreateRequest(
            path=_models.ProductToursPublishDraftCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProductToursPublishDraftCreateRequestBody(name=name, description=description, auto_launch=auto_launch, start_date=start_date, end_date=end_date)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for publish_product_tour_draft: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/product_tours/{id}/publish_draft", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/product_tours/{id}/publish_draft"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("publish_product_tour_draft")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("publish_product_tour_draft", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="publish_product_tour_draft",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def create_project_secret_api_key(
    project_id: str = Field(..., description="The unique identifier of the project where the API key will be created. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    label: str = Field(..., description="A human-readable label for the API key to help identify its purpose. Must be 40 characters or fewer.", max_length=40),
    scopes: list[str] = Field(..., description="An array of permission scopes that define what actions this API key is authorized to perform. Each scope grants specific access permissions to the key."),
) -> dict[str, Any]:
    """Create a new secret API key for a project with specified access scopes. The key can be used to authenticate API requests on behalf of the project."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysCreateRequest(
            path=_models.ProjectSecretApiKeysCreateRequestPath(project_id=project_id),
            body=_models.ProjectSecretApiKeysCreateRequestBody(label=label, scopes=scopes)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_project_secret_api_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_project_secret_api_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_project_secret_api_key", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_project_secret_api_key",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def get_project_secret_api_key(
    id_: str = Field(..., alias="id", description="The unique identifier of the project secret API key to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the API key. You can obtain the project ID by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific secret API key for a project by its unique identifier. This operation returns the full details of the requested API key."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysRetrieveRequest(
            path=_models.ProjectSecretApiKeysRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_project_secret_api_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_project_secret_api_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_project_secret_api_key", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_project_secret_api_key",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def update_project_secret_api_key(
    id_: str = Field(..., alias="id", description="The unique identifier of the secret API key to update."),
    project_id: str = Field(..., description="The project ID containing the secret API key. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    label: str = Field(..., description="A human-readable label for the API key, up to 40 characters long. Use descriptive names to identify the key's purpose.", max_length=40),
    scopes: list[str] = Field(..., description="An array of permission scopes that define what actions this API key is authorized to perform. Scopes control the key's access level and capabilities."),
) -> dict[str, Any]:
    """Update an existing secret API key for a project, including its label and permission scopes. This allows you to modify the configuration of an already-created API key."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysUpdateRequest(
            path=_models.ProjectSecretApiKeysUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProjectSecretApiKeysUpdateRequestBody(label=label, scopes=scopes)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_secret_api_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_secret_api_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_secret_api_key", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_secret_api_key",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def update_project_secret_api_key_partial(
    id_: str = Field(..., alias="id", description="The unique identifier of the project secret API key to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the secret API key. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    label: str | None = Field(None, description="A human-readable label for the API key, up to 40 characters long. Use this to identify the key's purpose or associated application.", max_length=40),
    scopes: list[str] | None = Field(None, description="An array of permission scopes to assign to this API key. Each scope defines what actions the key is authorized to perform."),
) -> dict[str, Any]:
    """Partially update a project secret API key's metadata, such as its label or assigned scopes. Only the fields provided in the request will be modified."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysPartialUpdateRequest(
            path=_models.ProjectSecretApiKeysPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.ProjectSecretApiKeysPartialUpdateRequestBody(label=label, scopes=scopes)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_project_secret_api_key_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_project_secret_api_key_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_project_secret_api_key_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_project_secret_api_key_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def delete_project_secret_api_key(
    id_: str = Field(..., alias="id", description="The unique identifier of the project secret API key to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the API key. You can retrieve your project ID by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Permanently delete a secret API key from a project. This operation removes the specified API key and revokes any access it provided."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysDestroyRequest(
            path=_models.ProjectSecretApiKeysDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_project_secret_api_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_project_secret_api_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_project_secret_api_key", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_project_secret_api_key",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, project_secret_api_keys
@mcp.tool()
async def roll_project_secret_api_key(
    id_: str = Field(..., alias="id", description="The unique identifier of the project secret API key to rotate."),
    project_id: str = Field(..., description="The unique identifier of the project containing the API key. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Generate a new secret value for an existing project API key, invalidating the previous secret. Use this to rotate credentials for security purposes."""

    # Construct request model with validation
    try:
        _request = _models.ProjectSecretApiKeysRollCreateRequest(
            path=_models.ProjectSecretApiKeysRollCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for roll_project_secret_api_key: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/project_secret_api_keys/{id}/roll", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/project_secret_api_keys/{id}/roll"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("roll_project_secret_api_key")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("roll_project_secret_api_key", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="roll_project_secret_api_key",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def list_property_definitions(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this from the /api/projects/ endpoint if unknown."),
    event_names: str | None = Field(None, description="JSON-encoded list of event names to filter property visibility. When provided, the response will indicate which properties appear in these events.", min_length=1),
    exclude_core_properties: bool | None = Field(None, description="Set to true to exclude built-in core properties from the results."),
    exclude_hidden: bool | None = Field(None, description="Set to true to exclude properties marked as hidden."),
    excluded_properties: str | None = Field(None, description="JSON-encoded list of property names to exclude from the results.", min_length=1),
    filter_by_event_names: bool | None = Field(None, description="Set to true to return only properties that appear in the events specified by event_names."),
    group_type_index: int | None = Field(None, description="The group type index for group-type properties. Only use this parameter when filtering for group properties."),
    is_feature_flag: bool | None = Field(None, description="Set to true to return only feature flag properties, or false to exclude them."),
    is_numerical: bool | None = Field(None, description="Set to true to return only numerical properties, or false to exclude them."),
    limit: int | None = Field(None, description="Maximum number of results to return per page for pagination."),
    offset: int | None = Field(None, description="Zero-based starting index for pagination to retrieve subsequent result pages."),
    properties: str | None = Field(None, description="Comma-separated list of specific property names to retrieve.", min_length=1),
    verified: bool | None = Field(None, description="Filter by verification status: true returns only verified properties, false returns only unverified properties."),
) -> dict[str, Any]:
    """Retrieve property definitions for a project with optional filtering by event names, property types, and verification status. Use this to discover available properties and their metadata for analytics and tracking."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsListRequest(
            path=_models.PropertyDefinitionsListRequestPath(project_id=project_id),
            query=_models.PropertyDefinitionsListRequestQuery(event_names=event_names, exclude_core_properties=exclude_core_properties, exclude_hidden=exclude_hidden, excluded_properties=excluded_properties, filter_by_event_names=filter_by_event_names, group_type_index=group_type_index, is_feature_flag=is_feature_flag, is_numerical=is_numerical, limit=limit, offset=offset, properties=properties, verified=verified)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_property_definitions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_property_definitions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_property_definitions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_property_definitions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def get_property_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the property definition to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the property definition. You can retrieve your project ID by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific property definition by its UUID within a project. Property definitions define custom fields and their configurations for project resources."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsRetrieveRequest(
            path=_models.PropertyDefinitionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_property_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_property_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_property_definition", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_property_definition",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def update_property_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the property definition to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the property definition. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description of the property definition's purpose and usage."),
    tags: list[Any] | None = Field(None, description="An array of tags for categorizing and organizing the property definition. Order and format depend on your tagging system."),
    property_type: Literal["DateTime", "String", "Numeric", "Boolean", "Duration"] | Literal[""] | None = Field(None, description="The data type or category of this property (e.g., string, number, boolean, date). Consult your API documentation for valid property type values."),
    verified: bool | None = Field(None, description="A boolean flag indicating whether this property definition has been verified or validated."),
    hidden: bool | None = Field(None, description="A boolean flag indicating whether this property definition should be hidden from standard views or listings."),
) -> dict[str, Any]:
    """Update an existing property definition within a project. Modify metadata such as description, tags, type, verification status, and visibility."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsUpdateRequest(
            path=_models.PropertyDefinitionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.PropertyDefinitionsUpdateRequestBody(description=description, tags=tags, property_type=property_type, verified=verified, hidden=hidden)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_property_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_property_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_property_definition", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_property_definition",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def update_property_definition_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the property definition to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the property definition. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description providing context or details about the property definition."),
    tags: list[Any] | None = Field(None, description="An array of tags for categorizing or labeling the property definition. Order and format depend on the API's tag system."),
    property_type: Literal["DateTime", "String", "Numeric", "Boolean", "Duration"] | Literal[""] | None = Field(None, description="The data type or category of the property (e.g., string, number, boolean, date). Consult API documentation for valid property type values."),
    verified: bool | None = Field(None, description="A boolean flag indicating whether the property definition has been verified or validated."),
    hidden: bool | None = Field(None, description="A boolean flag indicating whether the property definition should be hidden from standard views or listings."),
) -> dict[str, Any]:
    """Partially update a property definition within a project. Allows modification of metadata such as description, tags, property type, and visibility settings."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsPartialUpdateRequest(
            path=_models.PropertyDefinitionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.PropertyDefinitionsPartialUpdateRequestBody(description=description, tags=tags, property_type=property_type, verified=verified, hidden=hidden)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_property_definition_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_property_definition_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_property_definition_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_property_definition_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def delete_property_definition(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the property definition to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the property definition. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a property definition from a project. This removes the property definition and any associated metadata."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsDestroyRequest(
            path=_models.PropertyDefinitionsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_property_definition: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_property_definition")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_property_definition", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_property_definition",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, property_definitions
@mcp.tool()
async def get_property_definitions_seen_together(project_id: str = Field(..., description="The unique identifier of the project to query. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieves a map indicating which properties have been observed together with specified events. Accepts a list of event names and returns a boolean for each event showing whether the given property has ever co-occurred with that event."""

    # Construct request model with validation
    try:
        _request = _models.PropertyDefinitionsSeenTogetherRetrieveRequest(
            path=_models.PropertyDefinitionsSeenTogetherRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_property_definitions_seen_together: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/property_definitions/seen_together", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/property_definitions/seen_together"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_property_definitions_seen_together")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_property_definitions_seen_together", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_property_definitions_seen_together",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def query_create(
    project_id: str = Field(..., description="The unique identifier of the project to query. Retrieve available project IDs from the /api/projects/ endpoint."),
    query: _models.EventsNode | _models.ActionsNode | _models.PersonsNode | _models.DataWarehouseNode | _models.FunnelsDataWarehouseNode | _models.LifecycleDataWarehouseNode | _models.EventsQuery | _models.SessionsQuery | _models.ActorsQuery | _models.GroupsQuery | _models.InsightActorsQuery | _models.InsightActorsQueryOptions | _models.SessionsTimelineQuery | _models.HogQuery | _models.HogQlQuery | _models.HogQlMetadata | _models.HogQlAutocomplete | _models.SessionAttributionExplorerQuery | _models.RevenueExampleEventsQuery | _models.RevenueExampleDataWarehouseTablesQuery | _models.ErrorTrackingQuery | _models.ErrorTrackingSimilarIssuesQuery | _models.ErrorTrackingBreakdownsQuery | _models.ErrorTrackingIssueCorrelationQuery | _models.ExperimentFunnelsQuery | _models.ExperimentTrendsQuery | _models.ExperimentQuery | _models.ExperimentExposureQuery | _models.DocumentSimilarityQuery | _models.WebOverviewQuery | _models.WebStatsTableQuery | _models.WebExternalClicksTableQuery | _models.WebGoalsQuery | _models.WebVitalsQuery | _models.WebVitalsPathBreakdownQuery | _models.WebPageUrlSearchQuery | _models.WebAnalyticsExternalSummaryQuery | _models.WebNotableChangesQuery | _models.RevenueAnalyticsGrossRevenueQuery | _models.RevenueAnalyticsMetricsQuery | _models.RevenueAnalyticsMrrQuery | _models.RevenueAnalyticsOverviewQuery | _models.RevenueAnalyticsTopCustomersQuery | _models.MarketingAnalyticsTableQuery | _models.MarketingAnalyticsAggregatedQuery | _models.NonIntegratedConversionsTableQuery | _models.DataVisualizationNode | _models.DataTableNode | _models.SavedInsightNode | _models.InsightVizNode | _models.TrendsQuery | _models.FunnelsQuery | _models.RetentionQuery | _models.PathsQuery | _models.StickinessQuery | _models.LifecycleQuery | _models.FunnelCorrelationQuery | _models.DatabaseSchemaQuery | _models.LogsQuery | _models.LogAttributesQuery | _models.LogValuesQuery | _models.TraceSpansQuery | _models.SuggestedQuestionsQuery | _models.TeamTaxonomyQuery | _models.EventTaxonomyQuery | _models.ActorsPropertyTaxonomyQuery | _models.TracesQuery | _models.TraceQuery | _models.TraceNeighborsQuery | _models.VectorSearchQuery | _models.UsageMetricsQuery | _models.EndpointsUsageOverviewQuery | _models.EndpointsUsageTableQuery | _models.EndpointsUsageTrendsQuery | _models.PropertyValuesQuery = Field(..., description="A JSON object representing the query to execute, typically a HogQL query. Must include 'kind' (e.g., 'HogQLQuery') and 'query' fields with the actual query string."),
    async_: bool | None = Field(None, alias="async", description="Execute the query asynchronously, returning immediately with a query status for background processing."),
    breakdown: str | list[str | int] | int | None = Field(None, description="Deprecated: use 'breakdowns' instead. Single breakdown dimension for aggregating results."),
    breakdowns: list[_models.Breakdown] | None = Field(None, description="Array of breakdown dimensions for multi-level result aggregation. Maximum of 3 breakdowns allowed.", max_length=3),
    explicit_date: bool | None = Field(None, alias="explicitDate", description="Include explicit date information in the response, useful for time-series analysis."),
    properties: list[_models.EventPropertyFilter | _models.PersonPropertyFilter | _models.ElementPropertyFilter | _models.EventMetadataPropertyFilter | _models.SessionPropertyFilter | _models.CohortPropertyFilter | _models.RecordingPropertyFilter | _models.LogEntryPropertyFilter | _models.GroupPropertyFilter | _models.FeaturePropertyFilter | _models.FlagPropertyFilter | _models.HogQlPropertyFilter | _models.EmptyPropertyFilter | _models.DataWarehousePropertyFilter | _models.DataWarehousePersonPropertyFilter | _models.ErrorTrackingIssueFilter | _models.LogPropertyFilter | _models.SpanPropertyFilter | _models.RevenueAnalyticsPropertyFilter] | None = Field(None, description="Array of event properties to filter or include in the query results."),
    limit_context: Literal["posthog_ai"] | None = Field(None, description="Specifies the client context for the query. Only 'posthog_ai' is accepted as a client-provided value; used for tracking AI-generated queries."),
    refresh: Literal["async", "async_except_on_cache_miss", "blocking", "force_async", "force_blocking", "force_cache", "lazy_async"] | None = Field(None, description="Controls execution and caching behavior: 'blocking' calculates synchronously unless fresh cache exists; 'async' returns immediately with status; 'lazy_async' uses somewhat fresh cache; 'force_blocking'/'force_async' ignore cache; 'force_cache' returns cached data only. Defaults to 'blocking'."),
    variables_override: dict[str, dict[str, Any]] | None = Field(None, description="Object containing variable overrides to substitute into the query template."),
) -> dict[str, Any]:
    """Execute a data analysis query against a PostHog project. Supports HogQL queries with configurable caching and execution modes (sync/async). Responses are gated behind permission checks and may be served from cache if available."""

    # Construct request model with validation
    try:
        _request = _models.QueryCreateRequest(
            path=_models.QueryCreateRequestPath(project_id=project_id),
            body=_models.QueryCreateRequestBody(async_=async_, limit_context=limit_context, query=query, refresh=refresh, variables_override=variables_override,
                filters_override=_models.QueryCreateRequestBodyFiltersOverride(explicit_date=explicit_date, properties=properties,
                    breakdown_filter=_models.QueryCreateRequestBodyFiltersOverrideBreakdownFilter(breakdown=breakdown, breakdowns=breakdowns) if any(v is not None for v in [breakdown, breakdowns]) else None) if any(v is not None for v in [breakdown, breakdowns, explicit_date, properties]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for query_create: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("query_create")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("query_create", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="query_create",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def get_query(
    id_: str = Field(..., alias="id", description="The unique identifier of the query to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the query. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific saved query by its ID within a project. This operation allows you to fetch the details and configuration of a previously created query."""

    # Construct request model with validation
    try:
        _request = _models.QueryRetrieveRequest(
            path=_models.QueryRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_query", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_query",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def delete_query(
    id_: str = Field(..., alias="id", description="The unique identifier of the query to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the query. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a saved query from a project. This operation removes the query and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.QueryDestroyRequest(
            path=_models.QueryDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_query", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_query",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def get_query_log(
    id_: str = Field(..., alias="id", description="The unique identifier of the query for which to retrieve log details. The query must have been issued within the last 24 hours."),
    project_id: str = Field(..., description="The unique identifier of the project containing the query. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve detailed log information for a specific query that was executed within the last 24 hours. The query log data is sourced from the query_log_archive table."""

    # Construct request model with validation
    try:
        _request = _models.QueryLogRetrieveRequest(
            path=_models.QueryLogRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_query_log: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/{id}/log", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/{id}/log"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_query_log")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_query_log", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_query_log",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def create_query(
    project_id: str = Field(..., description="The unique identifier of the project containing the query. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    query_kind: str = Field(..., description="The type of query to execute, specified in PascalCase format (must start with an uppercase letter followed by zero or more letters).", pattern="^[A-Z][A-Za-z]*$"),
) -> dict[str, Any]:
    """Execute a query operation within a project with permission validation. The operation supports response caching through middleware, ensuring all cached responses are authorized before being returned to the requester."""

    # Construct request model with validation
    try:
        _request = _models.QueryCreate2Request(
            path=_models.QueryCreate2RequestPath(project_id=project_id, query_kind=query_kind)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/{query_kind}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/{query_kind}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def get_draft_sql_query(project_id: str = Field(..., description="The unique identifier of the project containing the draft SQL query. You can obtain the project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the draft SQL query for a specific project. This operation returns cached query data after verifying the requester has appropriate permissions to access the project."""

    # Construct request model with validation
    try:
        _request = _models.QueryDraftSqlRetrieveRequest(
            path=_models.QueryDraftSqlRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_draft_sql_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/draft_sql", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/draft_sql"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_draft_sql_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_draft_sql_query", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_draft_sql_query",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: query
@mcp.tool()
async def upgrade_query(
    project_id: str = Field(..., description="The unique identifier of the project containing the query. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    query: _models.EventsNode | _models.ActionsNode | _models.PersonsNode | _models.DataWarehouseNode | _models.FunnelsDataWarehouseNode | _models.LifecycleDataWarehouseNode | _models.EventsQuery | _models.SessionsQuery | _models.ActorsQuery | _models.GroupsQuery | _models.InsightActorsQuery | _models.InsightActorsQueryOptions | _models.SessionsTimelineQuery | _models.HogQuery | _models.HogQlQuery | _models.HogQlMetadata | _models.HogQlAutocomplete | _models.SessionAttributionExplorerQuery | _models.RevenueExampleEventsQuery | _models.RevenueExampleDataWarehouseTablesQuery | _models.ErrorTrackingQuery | _models.ErrorTrackingSimilarIssuesQuery | _models.ErrorTrackingBreakdownsQuery | _models.ErrorTrackingIssueCorrelationQuery | _models.ExperimentFunnelsQuery | _models.ExperimentTrendsQuery | _models.ExperimentQuery | _models.ExperimentExposureQuery | _models.DocumentSimilarityQuery | _models.WebOverviewQuery | _models.WebStatsTableQuery | _models.WebExternalClicksTableQuery | _models.WebGoalsQuery | _models.WebVitalsQuery | _models.WebVitalsPathBreakdownQuery | _models.WebPageUrlSearchQuery | _models.WebAnalyticsExternalSummaryQuery | _models.WebNotableChangesQuery | _models.RevenueAnalyticsGrossRevenueQuery | _models.RevenueAnalyticsMetricsQuery | _models.RevenueAnalyticsMrrQuery | _models.RevenueAnalyticsOverviewQuery | _models.RevenueAnalyticsTopCustomersQuery | _models.MarketingAnalyticsTableQuery | _models.MarketingAnalyticsAggregatedQuery | _models.NonIntegratedConversionsTableQuery | _models.DataVisualizationNode | _models.DataTableNode | _models.SavedInsightNode | _models.InsightVizNode | _models.TrendsQuery | _models.FunnelsQuery | _models.RetentionQuery | _models.PathsQuery | _models.StickinessQuery | _models.LifecycleQuery | _models.FunnelCorrelationQuery | _models.DatabaseSchemaQuery | _models.LogsQuery | _models.LogAttributesQuery | _models.LogValuesQuery | _models.TraceSpansQuery | _models.SuggestedQuestionsQuery | _models.TeamTaxonomyQuery | _models.EventTaxonomyQuery | _models.ActorsPropertyTaxonomyQuery | _models.TracesQuery | _models.TraceQuery | _models.TraceNeighborsQuery | _models.VectorSearchQuery | _models.UsageMetricsQuery | _models.EndpointsUsageOverviewQuery | _models.EndpointsUsageTableQuery | _models.EndpointsUsageTrendsQuery | _models.PropertyValuesQuery = Field(..., description="The query object to be upgraded. This should contain the query structure with nodes that will be migrated to the latest version."),
) -> dict[str, Any]:
    """Upgrades a query to the latest version by migrating all nodes without executing it. Returns the upgraded query with all components updated to current specifications."""

    # Construct request model with validation
    try:
        _request = _models.QueryUpgradeCreateRequest(
            path=_models.QueryUpgradeCreateRequestPath(project_id=project_id),
            body=_models.QueryUpgradeCreateRequestBody(query=query)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for upgrade_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/query/upgrade", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/query/upgrade"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("upgrade_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("upgrade_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="upgrade_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: sandbox-environments, sandbox_environments
@mcp.tool()
async def list_sandbox_environments(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of sandbox environments to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position in the result set for pagination. Use with limit to retrieve subsequent pages of results."),
) -> dict[str, Any]:
    """Retrieve a paginated list of sandbox environments for a project. Sandbox environments control network access policies for task runs within the project."""

    # Construct request model with validation
    try:
        _request = _models.SandboxListRequest(
            path=_models.SandboxListRequestPath(project_id=project_id),
            query=_models.SandboxListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_sandbox_environments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sandbox_environments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sandbox_environments"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_sandbox_environments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_sandbox_environments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_sandbox_environments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: sandbox-environments, sandbox_environments
@mcp.tool()
async def get_sandbox_environment(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the sandbox environment to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the sandbox environment. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific sandbox environment configuration that controls network access policies for task runs within a project."""

    # Construct request model with validation
    try:
        _request = _models.SandboxRetrieveRequest(
            path=_models.SandboxRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_sandbox_environment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sandbox_environments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sandbox_environments/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_sandbox_environment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_sandbox_environment", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_sandbox_environment",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sandbox-environments, sandbox_environments
@mcp.tool()
async def update_sandbox_environment(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the sandbox environment to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this sandbox environment. Retrieve available project IDs from the /api/projects/ endpoint."),
    network_access_level: Literal["trusted", "full", "custom"] | None = Field(None, description="The network access level for the sandbox: 'trusted' for restricted access to default domains, 'full' for unrestricted network access, or 'custom' for a curated list of allowed domains."),
    allowed_domains: list[str] | None = Field(None, description="A list of domain names to allow network access to when using 'custom' network access level. Each domain should be specified as a fully qualified domain name."),
    include_default_domains: bool | None = Field(None, description="When true, includes default trusted domains (GitHub, npm, PyPI) in addition to any custom allowed domains. Only applicable with 'custom' network access level."),
    repositories: list[str] | None = Field(None, description="A list of repositories this sandbox environment applies to, specified in 'organization/repository' format. Controls which repositories can use this environment configuration."),
    private: bool | None = Field(None, description="When true, restricts visibility to only the creator; when false, makes the environment visible to all team members."),
) -> dict[str, Any]:
    """Partially update a sandbox environment's network access configuration and visibility settings. Allows modification of network access levels, allowed domains, repository associations, and team visibility."""

    # Construct request model with validation
    try:
        _request = _models.SandboxPartialUpdateRequest(
            path=_models.SandboxPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SandboxPartialUpdateRequestBody(network_access_level=network_access_level, allowed_domains=allowed_domains, include_default_domains=include_default_domains, repositories=repositories, private=private)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_sandbox_environment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sandbox_environments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sandbox_environments/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_sandbox_environment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_sandbox_environment", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_sandbox_environment",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: sandbox-environments, sandbox_environments
@mcp.tool()
async def delete_sandbox_environment(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the sandbox environment to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the sandbox environment. Retrieve available project IDs by calling the list projects endpoint."),
) -> dict[str, Any]:
    """Permanently removes a sandbox environment from a project, disabling network access controls for any associated task runs."""

    # Construct request model with validation
    try:
        _request = _models.SandboxDestroyRequest(
            path=_models.SandboxDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_sandbox_environment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sandbox_environments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sandbox_environments/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_sandbox_environment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_sandbox_environment", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_sandbox_environment",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def list_saved_items(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit to use the default page size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of saved items for a specific project. Use pagination parameters to control the number of results and starting position."""

    # Construct request model with validation
    try:
        _request = _models.SavedListRequest(
            path=_models.SavedListRequestPath(project_id=project_id),
            query=_models.SavedListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_saved_items: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_saved_items")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_saved_items", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_saved_items",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def create_saved_url(
    project_id: str = Field(..., description="The unique identifier of the project where the URL will be saved. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    url: str = Field(..., description="A valid URI to save. The URL must be properly formatted and cannot exceed 2000 characters in length.", max_length=2000),
) -> dict[str, Any]:
    """Save a URL to a project for later reference. The URL is stored as a saved item within the specified project."""

    # Construct request model with validation
    try:
        _request = _models.SavedCreateRequest(
            path=_models.SavedCreateRequestPath(project_id=project_id),
            body=_models.SavedCreateRequestBody(url=url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_saved_url: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_saved_url")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_saved_url", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_saved_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def get_saved_item(
    project_id: str = Field(..., description="The unique identifier of the project containing the saved item. You can retrieve available project IDs by calling the projects list endpoint."),
    short_id: str = Field(..., description="The short identifier of the saved item to retrieve within the specified project."),
) -> dict[str, Any]:
    """Retrieve a specific saved item from a project using its short identifier. This operation fetches a previously saved resource within the context of a given project."""

    # Construct request model with validation
    try:
        _request = _models.SavedRetrieveRequest(
            path=_models.SavedRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_saved_item: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved/{short_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_saved_item")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_saved_item", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_saved_item",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def update_saved_partial(
    project_id: str = Field(..., description="The unique identifier of the project containing the saved item. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the saved item to update within the specified project."),
    url: str | None = Field(None, description="An optional URI to associate with or update for the saved item. Must be a valid URI format and not exceed 2000 characters in length.", max_length=2000),
) -> dict[str, Any]:
    """Partially update a saved item within a project by modifying specific fields. Use this to update individual properties of a saved resource without replacing the entire object."""

    # Construct request model with validation
    try:
        _request = _models.SavedPartialUpdateRequest(
            path=_models.SavedPartialUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.SavedPartialUpdateRequestBody(url=url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_saved_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_saved_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_saved_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_saved_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def mark_saved_deleted(
    project_id: str = Field(..., description="The unique identifier of the project containing the saved item. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the saved item to be marked as deleted."),
) -> dict[str, Any]:
    """Soft-delete a saved query by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.SavedDestroyRequest(
            path=_models.SavedDestroyRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for mark_saved_deleted: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved/{short_id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("mark_saved_deleted")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("mark_saved_deleted", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="mark_saved_deleted",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: saved
@mcp.tool()
async def regenerate_saved_query(
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the saved query to regenerate."),
    url: str = Field(..., description="The URL endpoint to execute the saved query against. Must be a valid URI and not exceed 2000 characters in length.", max_length=2000),
) -> dict[str, Any]:
    """Regenerate a saved query by executing it against a new or updated URL. This operation re-runs a previously saved query configuration with the provided URL endpoint."""

    # Construct request model with validation
    try:
        _request = _models.SavedRegenerateCreateRequest(
            path=_models.SavedRegenerateCreateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.SavedRegenerateCreateRequestBody(url=url)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for regenerate_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/saved/{short_id}/regenerate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/saved/{short_id}/regenerate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("regenerate_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("regenerate_saved_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="regenerate_saved_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def list_schema_property_groups(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position (zero-indexed) from which to return results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of schema property groups for a specific project. Schema property groups organize custom properties used in your project's schema."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsListRequest(
            path=_models.SchemaPropertyGroupsListRequestPath(project_id=project_id),
            query=_models.SchemaPropertyGroupsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_schema_property_groups: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_schema_property_groups")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_schema_property_groups", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_schema_property_groups",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def create_schema_property_group(
    project_id: str = Field(..., description="The unique identifier of the project where the property group will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the property group. Must be 400 characters or fewer.", max_length=400),
    description: str | None = Field(None, description="An optional description providing additional context or details about the purpose and usage of this property group."),
    properties: list[_models.SchemaPropertyGroupProperty] | None = Field(None, description="An optional array of properties to include in this group. Properties define the schema fields and their configurations within the group."),
) -> dict[str, Any]:
    """Create a new schema property group within a project to organize and manage related schema properties. Property groups help structure schema definitions for better organization and reusability."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsCreateRequest(
            path=_models.SchemaPropertyGroupsCreateRequestPath(project_id=project_id),
            body=_models.SchemaPropertyGroupsCreateRequestBody(name=name, description=description, properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_schema_property_group: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_schema_property_group")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_schema_property_group", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_schema_property_group",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def get_schema_property_group(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the schema property group to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the schema property group. Find your project ID by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific schema property group by its unique identifier within a project. Use this to fetch detailed configuration and metadata for a property group."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsRetrieveRequest(
            path=_models.SchemaPropertyGroupsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_schema_property_group: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_schema_property_group")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_schema_property_group", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_schema_property_group",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def update_schema_property_group(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the schema property group to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the schema property group. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the schema property group. Must not exceed 400 characters.", max_length=400),
    description: str | None = Field(None, description="An optional description providing additional context or details about the schema property group."),
    properties: list[_models.SchemaPropertyGroupProperty] | None = Field(None, description="An optional array of properties to associate with this schema property group. Order and format depend on the API's property schema requirements."),
) -> dict[str, Any]:
    """Update an existing schema property group within a project. Modify the group's name, description, and associated properties."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsUpdateRequest(
            path=_models.SchemaPropertyGroupsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SchemaPropertyGroupsUpdateRequestBody(name=name, description=description, properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_schema_property_group: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_schema_property_group")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_schema_property_group", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_schema_property_group",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def update_schema_property_group_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the schema property group to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the schema property group. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description for the schema property group. Provides context about the group's purpose or usage."),
    properties: list[_models.SchemaPropertyGroupProperty] | None = Field(None, description="An ordered array of properties belonging to this schema property group. Each item defines a property within the group."),
) -> dict[str, Any]:
    """Partially update a schema property group within a project. Allows modification of the group's description and properties without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsPartialUpdateRequest(
            path=_models.SchemaPropertyGroupsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SchemaPropertyGroupsPartialUpdateRequestBody(description=description, properties=properties)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_schema_property_group_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_schema_property_group_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_schema_property_group_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_schema_property_group_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: schema_property_groups
@mcp.tool()
async def delete_schema_property_group(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the schema property group to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the schema property group. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a schema property group from a project. This operation removes the property group and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.SchemaPropertyGroupsDestroyRequest(
            path=_models.SchemaPropertyGroupsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_schema_property_group: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/schema_property_groups/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/schema_property_groups/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_schema_property_group")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_schema_property_group", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_schema_property_group",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def list_session_group_summaries(
    project_id: str = Field(..., description="The unique identifier of the project containing the session group summaries. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or set to default for standard pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination offset."),
) -> dict[str, Any]:
    """Retrieve a paginated list of stored group session summaries for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesListRequest(
            path=_models.SessionGroupSummariesListRequestPath(project_id=project_id),
            query=_models.SessionGroupSummariesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_group_summaries: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_group_summaries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_group_summaries", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_group_summaries",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def create_session_group_summary(project_id: str = Field(..., description="The unique identifier of the project where the session group summary will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Create a new group session summary for a project. This operation stores aggregated session data and insights for a group of sessions within the specified project."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesCreateRequest(
            path=_models.SessionGroupSummariesCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_session_group_summary: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_session_group_summary")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_session_group_summary", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_session_group_summary",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def get_session_group_summary(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session group summary to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the session group summary. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a stored session group summary by its unique identifier within a specific project."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesRetrieveRequest(
            path=_models.SessionGroupSummariesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_session_group_summary: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_session_group_summary")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_session_group_summary", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_session_group_summary",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def update_session_group_summary(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session group summary to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session group summary. Retrieve available project IDs from the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Update an existing session group summary for a specific project. Modify stored group session summary data by providing the summary ID and project context."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesUpdateRequest(
            path=_models.SessionGroupSummariesUpdateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_group_summary: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_group_summary")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_group_summary", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_group_summary",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def update_session_group_summary_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session group summary to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session group summary. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Partially update a stored group session summary within a project. Use this to modify specific fields of an existing session group summary without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesPartialUpdateRequest(
            path=_models.SessionGroupSummariesPartialUpdateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_group_summary_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_group_summary_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_group_summary_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_group_summary_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: session_group_summaries
@mcp.tool()
async def delete_session_group_summary(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session group summary to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session group summary. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a stored group session summary from a project. This operation removes the summary record and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.SessionGroupSummariesDestroyRequest(
            path=_models.SessionGroupSummariesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_session_group_summary: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_group_summaries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_group_summaries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_session_group_summary")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_session_group_summary", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_session_group_summary",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def list_session_recording_playlists(
    project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of playlists to return per page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting position for results, enabling pagination through large result sets."),
    short_id: str | None = Field(None, description="Filter results by the short identifier of a specific playlist."),
) -> dict[str, Any]:
    """Retrieve a list of session recording playlists for a project, including both user-created and synthetic playlists. Results are paginated and can be filtered by playlist short ID."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsListRequest(
            path=_models.SessionRecordingPlaylistsListRequestPath(project_id=project_id),
            query=_models.SessionRecordingPlaylistsListRequestQuery(limit=limit, offset=offset, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_recording_playlists: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_recording_playlists")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_recording_playlists", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_recording_playlists",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def create_session_recording_playlist(
    project_id: str = Field(..., description="The unique identifier of the project where the playlist will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A text description for the playlist to help identify its purpose and contents."),
    pinned: bool | None = Field(None, description="Boolean flag to pin the playlist for prominent visibility and quick access within the project."),
    filters: Any | None = Field(None, description="Filter criteria to automatically include session recordings in the playlist based on specified conditions. Structure and supported filter types depend on the API's filtering schema."),
) -> dict[str, Any]:
    """Create a new session recording playlist within a project to organize and group related session recordings. Playlists can be pinned for quick access and filtered based on specific criteria."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsCreateRequest(
            path=_models.SessionRecordingPlaylistsCreateRequestPath(project_id=project_id),
            body=_models.SessionRecordingPlaylistsCreateRequestBody(description=description, pinned=pinned, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_session_recording_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_session_recording_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_session_recording_playlist", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_session_recording_playlist",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def get_session_recording_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording playlist. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short ID of the session recording playlist to retrieve. This is a unique identifier for the playlist within the project."),
) -> dict[str, Any]:
    """Retrieve a specific session recording playlist by its short ID within a project. This returns the playlist details and configuration for the given project."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsRetrieveRequest(
            path=_models.SessionRecordingPlaylistsRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_session_recording_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_session_recording_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_session_recording_playlist", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_session_recording_playlist",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def update_session_recording_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing the playlist. Retrieve project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the session recording playlist to update."),
    description: str | None = Field(None, description="A text description for the playlist to help identify its purpose or contents."),
    pinned: bool | None = Field(None, description="Whether to pin this playlist for quick access or visibility in the project."),
    filters: Any | None = Field(None, description="Filter criteria to define which session recordings are included in this playlist."),
) -> dict[str, Any]:
    """Update an existing session recording playlist, including its description, pinned status, and filtering criteria."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsUpdateRequest(
            path=_models.SessionRecordingPlaylistsUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.SessionRecordingPlaylistsUpdateRequestBody(description=description, pinned=pinned, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_recording_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_recording_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_recording_playlist", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_recording_playlist",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def update_session_recording_playlist_partial(
    project_id: str = Field(..., description="The unique identifier of the project containing the playlist. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the session recording playlist to update."),
    description: str | None = Field(None, description="A text description for the playlist, providing context about its purpose or contents."),
    pinned: bool | None = Field(None, description="A boolean flag indicating whether the playlist should be pinned for quick access (true) or unpinned (false)."),
    filters: Any | None = Field(None, description="Filter criteria to define which session recordings are included in the playlist. Specify as a structured filter object."),
) -> dict[str, Any]:
    """Partially update a session recording playlist by modifying its metadata, description, pinned status, or filtering criteria within a specific project."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsPartialUpdateRequest(
            path=_models.SessionRecordingPlaylistsPartialUpdateRequestPath(project_id=project_id, short_id=short_id),
            body=_models.SessionRecordingPlaylistsPartialUpdateRequestBody(description=description, pinned=pinned, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_recording_playlist_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_recording_playlist_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_recording_playlist_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_recording_playlist_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def soft_delete_session_recording_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing the playlist. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the session recording playlist to be soft deleted."),
) -> dict[str, Any]:
    """Soft-delete a session recording playlist by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsDestroyRequest(
            path=_models.SessionRecordingPlaylistsDestroyRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for soft_delete_session_recording_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("soft_delete_session_recording_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("soft_delete_session_recording_playlist", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="soft_delete_session_recording_playlist",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def list_recordings_in_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing the playlist. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    short_id: str = Field(..., description="The short identifier of the session recording playlist from which to retrieve recordings."),
) -> dict[str, Any]:
    """Retrieve all recordings contained within a specific session recording playlist. This allows you to view the list of recordings that have been organized into a playlist for a given project."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsRecordingsRetrieveRequest(
            path=_models.SessionRecordingPlaylistsRecordingsRetrieveRequestPath(project_id=project_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_recordings_in_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_recordings_in_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_recordings_in_playlist", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_recordings_in_playlist",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def add_recording_to_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing both the playlist and recording. Retrieve available project IDs from the /api/projects/ endpoint."),
    session_recording_id: str = Field(..., description="The unique identifier of the session recording to add to the playlist."),
    short_id: str = Field(..., description="The unique short identifier of the playlist to which the recording will be added."),
    description: str | None = Field(None, description="Optional text description or notes for this recording within the playlist context."),
    pinned: bool | None = Field(None, description="Optional boolean flag to pin this recording to the top of the playlist for prominence."),
    filters: Any | None = Field(None, description="Optional filters object to apply additional criteria or conditions to the recording within the playlist."),
) -> dict[str, Any]:
    """Add a session recording to a playlist within a project. This operation allows you to include an existing recording in a curated playlist, optionally with metadata like descriptions and pinned status."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsRecordingsCreateRequest(
            path=_models.SessionRecordingPlaylistsRecordingsCreateRequestPath(project_id=project_id, session_recording_id=session_recording_id, short_id=short_id),
            body=_models.SessionRecordingPlaylistsRecordingsCreateRequestBody(description=description, pinned=pinned, filters=filters)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_recording_to_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings/{session_recording_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings/{session_recording_id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_recording_to_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_recording_to_playlist", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_recording_to_playlist",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recording_playlists
@mcp.tool()
async def remove_recording_from_playlist(
    project_id: str = Field(..., description="The unique identifier of the project containing the playlist. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    session_recording_id: str = Field(..., description="The unique identifier of the session recording to remove from the playlist."),
    short_id: str = Field(..., description="The short identifier of the session recording playlist from which the recording will be removed."),
) -> dict[str, Any]:
    """Remove a specific session recording from a session recording playlist. This operation deletes the association between a recording and a playlist without affecting the recording itself."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingPlaylistsRecordingsDestroyRequest(
            path=_models.SessionRecordingPlaylistsRecordingsDestroyRequestPath(project_id=project_id, session_recording_id=session_recording_id, short_id=short_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for remove_recording_from_playlist: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings/{session_recording_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recording_playlists/{short_id}/recordings/{session_recording_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("remove_recording_from_playlist")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("remove_recording_from_playlist", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="remove_recording_from_playlist",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recordings
@mcp.tool()
async def list_session_recordings(
    project_id: str = Field(..., description="The unique identifier of the project containing the session recordings. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of session recordings to return in a single response page. Controls pagination size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of session recordings for a specific project. Use limit and offset parameters to control pagination through the results."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsListRequest(
            path=_models.SessionRecordingsListRequestPath(project_id=project_id),
            query=_models.SessionRecordingsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_recordings: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_recordings")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_recordings", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_recordings",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recordings
@mcp.tool()
async def get_session_recording(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session recording to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific session recording by its unique identifier within a project. Returns detailed metadata and content for the requested recording."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsRetrieveRequest(
            path=_models.SessionRecordingsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_session_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_session_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_session_recording", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_session_recording",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recordings
@mcp.tool()
async def update_session_recording(
    id_: str = Field(..., alias="id", description="The unique identifier of the session recording to update, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="A key-value map of person properties to set or update on the session recording. Supports $set operations for standard updates and $set_once operations for values that should only be set if not already present."),
) -> dict[str, Any]:
    """Update metadata and properties for a specific session recording within a project. Use this to modify recording attributes such as person properties via $set and $set_once operations."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsUpdateRequest(
            path=_models.SessionRecordingsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SessionRecordingsUpdateRequestBody(person=_models.SessionRecordingsUpdateRequestBodyPerson(properties=properties) if any(v is not None for v in [properties]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_recording", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_recording",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recordings
@mcp.tool()
async def update_session_recording_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session recording to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    properties: Any | None = Field(None, description="A key-value map of properties to update on the session recording. Supports $set operations to assign values and $set_once operations to set values only if not previously set."),
) -> dict[str, Any]:
    """Partially update a session recording within a project. Use this to modify specific properties of an existing session recording without affecting other fields."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsPartialUpdateRequest(
            path=_models.SessionRecordingsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SessionRecordingsPartialUpdateRequestBody(person=_models.SessionRecordingsPartialUpdateRequestBodyPerson(properties=properties) if any(v is not None for v in [properties]) else None)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_session_recording_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_session_recording_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_session_recording_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_session_recording_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: replay, session_recordings
@mcp.tool()
async def delete_session_recording(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the session recording to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a specific session recording from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsDestroyRequest(
            path=_models.SessionRecordingsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_session_recording: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_session_recording")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_session_recording", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_session_recording",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, session_recordings
@mcp.tool()
async def list_session_recording_shares(
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    recording_id: str = Field(..., description="The unique identifier of the session recording whose sharing configurations you want to retrieve."),
) -> dict[str, Any]:
    """Retrieve a list of all sharing configurations for a specific session recording, including who the recording has been shared with and their access permissions."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsSharingListRequest(
            path=_models.SessionRecordingsSharingListRequestPath(project_id=project_id, recording_id=recording_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_recording_shares: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{recording_id}/sharing", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{recording_id}/sharing"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_recording_shares")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_recording_shares", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_recording_shares",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, session_recordings
@mcp.tool()
async def create_session_recording_sharing_password(
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    recording_id: str = Field(..., description="The unique identifier of the session recording for which to create a sharing password."),
    enabled: bool | None = Field(None, description="Whether the password protection is enabled for this sharing configuration."),
    settings: Any | None = Field(None, description="Additional configuration settings for the password sharing behavior and constraints."),
    password_required: bool | None = Field(None, description="Whether a password is required to access the shared session recording."),
) -> dict[str, Any]:
    """Create a new password for session recording sharing configuration. This enables password-protected access to shared session recordings."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsSharingPasswordsCreateRequest(
            path=_models.SessionRecordingsSharingPasswordsCreateRequestPath(project_id=project_id, recording_id=recording_id),
            body=_models.SessionRecordingsSharingPasswordsCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_session_recording_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{recording_id}/sharing/passwords", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{recording_id}/sharing/passwords"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_session_recording_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_session_recording_sharing_password", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_session_recording_sharing_password",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, session_recordings
@mcp.tool()
async def delete_session_recording_sharing_password(
    password_id: str = Field(..., description="The unique identifier of the password to be deleted from the sharing configuration."),
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    recording_id: str = Field(..., description="The unique identifier of the session recording from which the password will be removed."),
) -> dict[str, Any]:
    """Remove a password from a session recording's sharing configuration, preventing access via that specific password."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsSharingPasswordsDestroyRequest(
            path=_models.SessionRecordingsSharingPasswordsDestroyRequestPath(password_id=password_id, project_id=project_id, recording_id=recording_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_session_recording_sharing_password: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{recording_id}/sharing/passwords/{password_id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{recording_id}/sharing/passwords/{password_id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_session_recording_sharing_password")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_session_recording_sharing_password", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_session_recording_sharing_password",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, session_recordings
@mcp.tool()
async def refresh_session_recording_sharing(
    project_id: str = Field(..., description="The unique identifier of the project containing the session recording. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    recording_id: str = Field(..., description="The unique identifier of the session recording whose sharing configuration should be refreshed."),
    enabled: bool | None = Field(None, description="Whether the sharing link for this recording should be enabled or disabled. When enabled, the recording can be accessed via the sharing link; when disabled, access is revoked."),
    settings: Any | None = Field(None, description="Additional configuration settings for the sharing link, such as expiration policies, access restrictions, or other sharing preferences."),
    password_required: bool | None = Field(None, description="Whether a password is required to access the shared recording. When enabled, viewers must provide the correct password to view the session."),
) -> dict[str, Any]:
    """Refresh or update the sharing configuration for a session recording, allowing you to regenerate sharing links and modify access settings like password protection and enablement status."""

    # Construct request model with validation
    try:
        _request = _models.SessionRecordingsSharingRefreshCreateRequest(
            path=_models.SessionRecordingsSharingRefreshCreateRequestPath(project_id=project_id, recording_id=recording_id),
            body=_models.SessionRecordingsSharingRefreshCreateRequestBody(enabled=enabled, settings=settings, password_required=password_required)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for refresh_session_recording_sharing: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/session_recordings/{recording_id}/sharing/refresh", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/session_recordings/{recording_id}/sharing/refresh"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("refresh_session_recording_sharing")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("refresh_session_recording_sharing", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="refresh_session_recording_sharing",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: sessions
@mcp.tool()
async def list_session_property_definitions(project_id: str = Field(..., description="The unique identifier of the project. You can obtain the project ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve all property definitions available for sessions within a specific project. Property definitions define the custom attributes and metadata that can be attached to session records."""

    # Construct request model with validation
    try:
        _request = _models.SessionsPropertyDefinitionsRetrieveRequest(
            path=_models.SessionsPropertyDefinitionsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_property_definitions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sessions/property_definitions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sessions/property_definitions"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_property_definitions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_property_definitions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_property_definitions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: sessions
@mcp.tool()
async def list_session_values(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve all session values for a specific project. This endpoint returns the collection of values associated with sessions in the given project."""

    # Construct request model with validation
    try:
        _request = _models.SessionsValuesRetrieveRequest(
            path=_models.SessionsValuesRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_session_values: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/sessions/values", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/sessions/values"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_session_values")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_session_values", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_session_values",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def list_signal_source_configs(
    project_id: str = Field(..., description="The unique identifier of the project containing the signal source configurations. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or set to default for standard pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination offset."),
) -> dict[str, Any]:
    """Retrieve a paginated list of signal source configurations for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsListRequest(
            path=_models.SignalSourceConfigsListRequestPath(project_id=project_id),
            query=_models.SignalSourceConfigsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_signal_source_configs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_signal_source_configs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_signal_source_configs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_signal_source_configs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def create_signal_source_config(
    project_id: str = Field(..., description="The unique identifier of the project where the signal source configuration will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    source_product: Literal["session_replay", "llm_analytics", "github", "linear", "zendesk", "error_tracking"] = Field(..., description="The external product or service to integrate with: session_replay, llm_analytics, github, linear, zendesk, or error_tracking."),
    source_type: Literal["session_analysis_cluster", "evaluation", "issue", "ticket", "issue_created", "issue_reopened", "issue_spiking"] = Field(..., description="The type of data or event to capture from the source product. Valid types include session_analysis_cluster, evaluation, issue, ticket, issue_created, issue_reopened, and issue_spiking. The available types depend on the selected source_product."),
    enabled: bool | None = Field(None, description="Whether this signal source configuration is active and should process incoming data. Defaults to false if not specified."),
) -> dict[str, Any]:
    """Create a new signal source configuration for a project to integrate external data sources like session replay, LLM analytics, GitHub, Linear, Zendesk, or error tracking systems."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsCreateRequest(
            path=_models.SignalSourceConfigsCreateRequestPath(project_id=project_id),
            body=_models.SignalSourceConfigsCreateRequestBody(source_product=source_product, source_type=source_type, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_signal_source_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_signal_source_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_signal_source_config", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_signal_source_config",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def get_signal_source_config(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the signal source config to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the signal source config. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific signal source configuration by its unique identifier within a project."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsRetrieveRequest(
            path=_models.SignalSourceConfigsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_signal_source_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_signal_source_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_signal_source_config", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_signal_source_config",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def update_signal_source_config(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the signal source configuration to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing this signal source configuration. Retrieve available project IDs from the /api/projects/ endpoint."),
    source_product: Literal["session_replay", "llm_analytics", "github", "linear", "zendesk", "error_tracking"] = Field(..., description="The external product or service providing the signal data: session_replay, llm_analytics, github, linear, zendesk, or error_tracking."),
    source_type: Literal["session_analysis_cluster", "evaluation", "issue", "ticket", "issue_created", "issue_reopened", "issue_spiking"] = Field(..., description="The classification type for how this signal source data is categorized: session_analysis_cluster, evaluation, issue, ticket, issue_created, issue_reopened, or issue_spiking."),
    enabled: bool | None = Field(None, description="Whether this signal source configuration is active and processing data. When enabled, the configuration will be used to ingest and process signals from the specified source."),
) -> dict[str, Any]:
    """Update the configuration for a signal source integration, including its enabled status and source type mapping. This allows you to modify how data from external sources (like session replay, GitHub, or error tracking) is processed and categorized within your project."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsUpdateRequest(
            path=_models.SignalSourceConfigsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SignalSourceConfigsUpdateRequestBody(source_product=source_product, source_type=source_type, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_signal_source_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_signal_source_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_signal_source_config", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_signal_source_config",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def update_signal_source_config_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the signal source config to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the signal source config. Retrieve available project IDs from the /api/projects/ endpoint."),
    source_product: Literal["session_replay", "llm_analytics", "github", "linear", "zendesk", "error_tracking"] | None = Field(None, description="The product category for the signal source. Choose from: session_replay, llm_analytics, github, linear, zendesk, or error_tracking."),
    source_type: Literal["session_analysis_cluster", "evaluation", "issue", "ticket", "issue_created", "issue_reopened", "issue_spiking"] | None = Field(None, description="The type of signal source within the product. Choose from: session_analysis_cluster, evaluation, issue, ticket, issue_created, issue_reopened, or issue_spiking."),
    enabled: bool | None = Field(None, description="Whether the signal source config is active and enabled for use."),
) -> dict[str, Any]:
    """Partially update a signal source configuration for a project. Allows modification of the source product, source type, and enabled status of an existing signal source config."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsPartialUpdateRequest(
            path=_models.SignalSourceConfigsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SignalSourceConfigsPartialUpdateRequestBody(source_product=source_product, source_type=source_type, enabled=enabled)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_signal_source_config_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_signal_source_config_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_signal_source_config_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_signal_source_config_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: signal_source_configs
@mcp.tool()
async def delete_signal_source_config(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the signal source config to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the signal source config. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a signal source configuration from a project. This operation removes the specified signal source config and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.SignalSourceConfigsDestroyRequest(
            path=_models.SignalSourceConfigsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_signal_source_config: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/signal_source_configs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/signal_source_configs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_signal_source_config")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_signal_source_config", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_signal_source_config",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def list_subscriptions(
    project_id: str = Field(..., description="The unique identifier of the project containing the subscriptions. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of subscription records to return in a single response page. Omit or set to default for standard page size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination alongside limit."),
) -> dict[str, Any]:
    """Retrieve a paginated list of subscriptions for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsListRequest(
            path=_models.SubscriptionsListRequestPath(project_id=project_id),
            query=_models.SubscriptionsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_subscriptions: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_subscriptions")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_subscriptions", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_subscriptions",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def create_subscription(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve this from the /api/projects/ endpoint if unknown."),
    target_type: Literal["email", "slack", "webhook"] = Field(..., description="The delivery channel for the subscription: email, Slack, or webhook."),
    target_value: str = Field(..., description="The destination address or URL where the subscription will be delivered (email address, Slack channel, or webhook URL)."),
    frequency: Literal["daily", "weekly", "monthly", "yearly"] = Field(..., description="How often the subscription is delivered: daily, weekly, monthly, or yearly."),
    start_date: str = Field(..., description="The date and time when the subscription begins, in ISO 8601 format."),
    dashboard: int | None = Field(None, description="The ID of the dashboard to subscribe to for updates."),
    insight: int | None = Field(None, description="The ID of the insight to include in the subscription."),
    dashboard_export_insights: list[int] | None = Field(None, description="Array of insight IDs to export and include in dashboard subscription deliveries."),
    interval: int | None = Field(None, description="The interval multiplier for the frequency (e.g., interval=2 with frequency=weekly means every 2 weeks).", ge=-2147483648, le=2147483647),
    byweekday: list[Literal["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"]] | None = Field(None, description="Array of weekday numbers (0-6, where 0 is Monday) to specify which days of the week the subscription should be sent."),
    bysetpos: int | None = Field(None, description="The position within the recurrence pattern (e.g., 1 for first occurrence, -1 for last occurrence in the period).", ge=-2147483648, le=2147483647),
    count: int | None = Field(None, description="The maximum number of times the subscription will be sent before it automatically stops.", ge=-2147483648, le=2147483647),
    until_date: str | None = Field(None, description="The date and time when the subscription ends and stops being sent, in ISO 8601 format. If omitted, the subscription continues indefinitely."),
    title: str | None = Field(None, description="A display name for the subscription (maximum 100 characters).", max_length=100),
    invite_message: str | None = Field(None, description="An optional message to include in the subscription delivery to provide context or instructions to recipients."),
) -> dict[str, Any]:
    """Create a new subscription to receive automated reports or notifications for a project dashboard. Subscriptions can be delivered via email, Slack, or webhook at specified intervals."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsCreateRequest(
            path=_models.SubscriptionsCreateRequestPath(project_id=project_id),
            body=_models.SubscriptionsCreateRequestBody(dashboard=dashboard, insight=insight, dashboard_export_insights=dashboard_export_insights, target_type=target_type, target_value=target_value, frequency=frequency, interval=interval, byweekday=byweekday, bysetpos=bysetpos, count=count, start_date=start_date, until_date=until_date, title=title, invite_message=invite_message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_subscription: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_subscription")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_subscription", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_subscription",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def get_subscription(
    id_: int = Field(..., alias="id", description="The unique identifier of the subscription to retrieve. Must be a positive integer."),
    project_id: str = Field(..., description="The unique identifier of the project containing the subscription. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific subscription by its ID within a project. Returns detailed information about the subscription including its configuration and status."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsRetrieveRequest(
            path=_models.SubscriptionsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_subscription: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_subscription")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_subscription", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_subscription",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def update_subscription(
    id_: int = Field(..., alias="id", description="The unique identifier of the subscription to update."),
    project_id: str = Field(..., description="The project ID containing the subscription. Retrieve available project IDs from the /api/projects/ endpoint."),
    target_type: Literal["email", "slack", "webhook"] = Field(..., description="The delivery channel for the subscription: email, Slack, or webhook."),
    target_value: str = Field(..., description="The destination address or identifier for the subscription (email address, Slack channel/user ID, or webhook URL)."),
    frequency: Literal["daily", "weekly", "monthly", "yearly"] = Field(..., description="How often the subscription is delivered: daily, weekly, monthly, or yearly."),
    start_date: str = Field(..., description="The date and time when the subscription should begin, in ISO 8601 format."),
    dashboard: int | None = Field(None, description="The ID of the dashboard to associate with this subscription."),
    insight: int | None = Field(None, description="The ID of the insight to associate with this subscription."),
    dashboard_export_insights: list[int] | None = Field(None, description="An array of dashboard export insight IDs to include in the subscription delivery."),
    interval: int | None = Field(None, description="The interval multiplier for the frequency (e.g., interval=2 with frequency=weekly means every 2 weeks).", ge=-2147483648, le=2147483647),
    byweekday: list[Literal["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"]] | None = Field(None, description="An array of weekday numbers (0-6, where 0 is Monday) to specify which days of the week the subscription should be sent."),
    bysetpos: int | None = Field(None, description="The position within a recurrence pattern (e.g., 1 for first occurrence, -1 for last occurrence in the period).", ge=-2147483648, le=2147483647),
    count: int | None = Field(None, description="The maximum number of times the subscription should be delivered before stopping.", ge=-2147483648, le=2147483647),
    until_date: str | None = Field(None, description="The date and time when the subscription should end, in ISO 8601 format. If not specified, the subscription continues indefinitely."),
    title: str | None = Field(None, description="A human-readable title for the subscription, up to 100 characters.", max_length=100),
    invite_message: str | None = Field(None, description="An optional message to include when inviting recipients to the subscription."),
) -> dict[str, Any]:
    """Update an existing subscription for automated reports or notifications. Modify delivery settings, frequency, scheduling, and content preferences for a specific subscription within a project."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsUpdateRequest(
            path=_models.SubscriptionsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SubscriptionsUpdateRequestBody(dashboard=dashboard, insight=insight, dashboard_export_insights=dashboard_export_insights, target_type=target_type, target_value=target_value, frequency=frequency, interval=interval, byweekday=byweekday, bysetpos=bysetpos, count=count, start_date=start_date, until_date=until_date, title=title, invite_message=invite_message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_subscription: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_subscription")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_subscription", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_subscription",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def update_subscription_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the subscription to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the subscription. Retrieve project IDs from the /api/projects/ endpoint."),
    dashboard: int | None = Field(None, description="The ID of the dashboard associated with this subscription."),
    insight: int | None = Field(None, description="The ID of the insight associated with this subscription."),
    dashboard_export_insights: list[int] | None = Field(None, description="An array of dashboard export insight IDs to include in this subscription."),
    target_type: Literal["email", "slack", "webhook"] | None = Field(None, description="The delivery channel for the subscription: email, Slack, or webhook."),
    target_value: str | None = Field(None, description="The destination address or URL where the subscription will be delivered (email address, Slack channel, or webhook URL)."),
    frequency: Literal["daily", "weekly", "monthly", "yearly"] | None = Field(None, description="The recurrence frequency for the subscription: daily, weekly, monthly, or yearly."),
    interval: int | None = Field(None, description="The interval between recurrences (e.g., every 2 weeks). Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    byweekday: list[Literal["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"]] | None = Field(None, description="An array of weekdays (0-6, where 0 is Monday) on which the subscription should recur when using weekly frequency."),
    bysetpos: int | None = Field(None, description="The position within the recurrence pattern (e.g., last occurrence of the month). Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    count: int | None = Field(None, description="The maximum number of times the subscription should recur. Must be a valid 32-bit integer.", ge=-2147483648, le=2147483647),
    start_date: str | None = Field(None, description="The date and time when the subscription should begin (ISO 8601 format)."),
    until_date: str | None = Field(None, description="The date and time when the subscription should end (ISO 8601 format)."),
    title: str | None = Field(None, description="A descriptive title for the subscription (up to 100 characters).", max_length=100),
    invite_message: str | None = Field(None, description="An optional message to include when inviting recipients to the subscription."),
) -> dict[str, Any]:
    """Partially update a subscription for a project, allowing modification of delivery settings, scheduling, and notification preferences."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsPartialUpdateRequest(
            path=_models.SubscriptionsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SubscriptionsPartialUpdateRequestBody(dashboard=dashboard, insight=insight, dashboard_export_insights=dashboard_export_insights, target_type=target_type, target_value=target_value, frequency=frequency, interval=interval, byweekday=byweekday, bysetpos=bysetpos, count=count, start_date=start_date, until_date=until_date, title=title, invite_message=invite_message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_subscription_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_subscription_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_subscription_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_subscription_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, subscriptions
@mcp.tool()
async def soft_delete_subscription(
    id_: int = Field(..., alias="id", description="The unique integer identifier of the subscription to be soft-deleted."),
    project_id: str = Field(..., description="The unique identifier of the project containing the subscription. Retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Mark a subscription as deleted without permanently removing it from the database. Use this operation to soft-delete subscriptions; hard deletion is not supported."""

    # Construct request model with validation
    try:
        _request = _models.SubscriptionsDestroyRequest(
            path=_models.SubscriptionsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for soft_delete_subscription: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/subscriptions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/subscriptions/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("soft_delete_subscription")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("soft_delete_subscription", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="soft_delete_subscription",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def list_surveys(
    project_id: str = Field(..., description="The unique identifier of the project containing the surveys. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of survey results to return in a single page. Use with offset for pagination control."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of surveys for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.SurveysListRequest(
            path=_models.SurveysListRequestPath(project_id=project_id),
            query=_models.SurveysListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_surveys: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_surveys")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_surveys", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_surveys",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def create_survey(
    project_id: str = Field(..., description="The unique identifier of the project where the survey will be created. Retrieve project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name of the survey. Must be between 1 and 400 characters.", min_length=1, max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="The survey delivery method: popover (inline popup), widget (embedded component), external_survey (hosted externally), or api (programmatic delivery)."),
    description: str | None = Field(None, description="Optional text describing the survey's purpose or content."),
    schedule: Literal["once", "recurring", "always"] | None = Field(None, description="Controls how often the survey is shown to users: 'once' shows it a single time per user (default), 'recurring' repeats based on iteration settings, 'always' displays every time conditions are met (primarily for widget surveys)."),
    questions: list[_models.SurveyOpenQuestionSchema | _models.SurveyLinkQuestionSchema | _models.SurveyRatingQuestionSchema | _models.SurveySingleChoiceQuestionSchema | _models.SurveyMultipleChoiceQuestionSchema] | None = Field(None, description="An array of survey questions in any combination of types: open-ended (open), link-based (link), rating scales (rating), or multiple choice (single_choice or multiple_choice). Each question supports optional branching logic, translations, and customizable display properties."),
    conditions: _models.SurveysCreateBodyConditions | None = Field(None, description="Display and targeting conditions that determine which users see the survey and under what circumstances."),
    appearance: _models.SurveysCreateBodyAppearance | None = Field(None, description="Customization options for the survey's visual appearance, including colors, fonts, positioning, and styling."),
    start_date: str | None = Field(None, description="ISO 8601 formatted date-time to immediately launch the survey. Omit this parameter unless explicitly launching the survey now."),
    end_date: str | None = Field(None, description="ISO 8601 formatted date-time marking when the survey stops being displayed to users. Setting this value completes the survey."),
    responses_limit: int | None = Field(None, description="The maximum number of responses to collect before the survey automatically stops."),
    iteration_count: int | None = Field(None, description="For recurring surveys, the number of times the survey repeats to each user (1-500). Use 1 for 'once every X days', higher numbers for multiple repetitions. Works with iteration_frequency_days to define the schedule.", ge=1, le=500),
    iteration_frequency_days: int | None = Field(None, description="For recurring surveys, the number of days between each survey instance shown to a user (1-365). Used with iteration_count to determine the overall schedule.", ge=1, le=365),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Specifies the sampling strategy type for response collection (e.g., every nth response, random sampling)."),
    response_sampling_interval: int | None = Field(None, description="The sampling interval value defining how frequently responses are sampled (0 or higher, up to 2,147,483,647).", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="The maximum number of responses to sample before stopping collection (0 or higher, up to 2,147,483,647).", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="When true, responses are saved as soon as at least one question is answered. When false, responses are only saved after all questions are completed."),
    enable_iframe_embedding: bool | None = Field(None, description="When true, enables the survey to be embedded within an iframe on external websites."),
    translations: Any | None = Field(None, description="Multi-language translations for survey content, allowing customization of text for different locales."),
) -> dict[str, Any]:
    """Create a new survey within a project. Surveys can be configured with multiple question types, scheduling behavior, targeting conditions, and appearance customization to collect user feedback."""

    # Construct request model with validation
    try:
        _request = _models.SurveysCreateRequest(
            path=_models.SurveysCreateRequestPath(project_id=project_id),
            body=_models.SurveysCreateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_survey: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_survey")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_survey", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_survey",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def get_survey(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific survey by its ID within a project. Returns the survey's configuration, questions, and metadata."""

    # Construct request model with validation
    try:
        _request = _models.SurveysRetrieveRequest(
            path=_models.SurveysRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_survey: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_survey")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_survey", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_survey",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def update_survey(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name of the survey. Maximum 400 characters.", max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="The survey delivery method: popover (inline popup), widget (embedded component), external_survey (hosted externally), or api (programmatic access)."),
    description: str | None = Field(None, description="Optional detailed description or context for the survey."),
    schedule: str | None = Field(None, description="Optional scheduling configuration for survey deployment and timing."),
    questions: Any | None = Field(None, description="Array of survey questions in any combination of types: open-ended (open), link-based (link), rating scales (rating), or choice-based (single_choice/multiple_choice). Each question supports optional branching logic to control survey flow, translations for multiple languages, and customizable display options."),
    appearance: Any | None = Field(None, description="Optional visual and behavioral appearance settings for the survey interface."),
    start_date: str | None = Field(None, description="Optional survey start date and time in ISO 8601 format. Survey will not be active before this date."),
    end_date: str | None = Field(None, description="Optional survey end date and time in ISO 8601 format. Survey will not be active after this date."),
    responses_limit: int | None = Field(None, description="Optional maximum number of responses to collect. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    iteration_count: int | None = Field(None, description="Optional number of times to repeat the survey. Must be between 0 and 500 iterations.", ge=0, le=500),
    iteration_frequency_days: int | None = Field(None, description="Optional number of days between survey iterations. Must be a non-negative integer.", ge=0, le=2147483647),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Optional sampling strategy type for response collection (e.g., every nth response, percentage-based)."),
    response_sampling_interval: int | None = Field(None, description="Optional sampling interval value. Must be a non-negative integer up to 2,147,483,647. Meaning depends on the sampling interval type.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="Optional maximum number of responses to collect when using response sampling. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="Optional flag to allow respondents to submit incomplete surveys without answering all questions."),
    enable_iframe_embedding: bool | None = Field(None, description="Optional flag to enable embedding the survey in an iframe on external websites."),
    translations: Any | None = Field(None, description="Optional translations object mapping language codes to translated survey content and labels."),
) -> dict[str, Any]:
    """Update an existing survey with new configuration, questions, appearance settings, and scheduling options. Allows modification of survey type, questions with branching logic, display settings, and response collection parameters."""

    # Construct request model with validation
    try:
        _request = _models.SurveysUpdateRequest(
            path=_models.SurveysUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SurveysUpdateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_survey: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_survey")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_survey", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_survey",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def update_survey_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Retrieve available project IDs from the /api/projects/ endpoint."),
    description: str | None = Field(None, description="A brief text description of the survey's purpose or content."),
    schedule: Literal["once", "recurring", "always"] | None = Field(None, description="Controls how often the survey is displayed to users: 'once' shows it a single time per user (default), 'recurring' repeats based on iteration settings, and 'always' displays it every time conditions are met (typically for widget surveys)."),
    questions: list[_models.SurveyOpenQuestionSchema | _models.SurveyLinkQuestionSchema | _models.SurveyRatingQuestionSchema | _models.SurveySingleChoiceQuestionSchema | _models.SurveyMultipleChoiceQuestionSchema] | None = Field(None, description="An ordered array of survey questions. Each question must be one of four types: open-ended (type: 'open'), link-based (type: 'link'), rating scale (type: 'rating'), or multiple choice (type: 'single_choice' or 'multiple_choice'). Questions support optional branching logic to control survey flow and inline translations for multi-language support."),
    conditions: _models.SurveysPartialUpdateBodyConditions | None = Field(None, description="Targeting and display conditions that determine which users see the survey and under what circumstances."),
    appearance: _models.SurveysPartialUpdateBodyAppearance | None = Field(None, description="Visual customization options for the survey, including colors, fonts, positioning, and other UI elements."),
    start_date: str | None = Field(None, description="The date and time (ISO 8601 format) when the survey should begin displaying to users. Only set this if you want to launch the survey immediately."),
    end_date: str | None = Field(None, description="The date and time (ISO 8601 format) when the survey should stop being displayed. Setting this will mark the survey as completed."),
    responses_limit: int | None = Field(None, description="The maximum number of responses to collect before the survey automatically stops. Leave unset for unlimited responses."),
    iteration_count: int | None = Field(None, description="For recurring surveys, the number of times the survey should be shown to each user (1-500). Use 1 for 'once every X days' or higher numbers for multiple repetitions. Works with iteration_frequency_days to define the schedule.", ge=1, le=500),
    iteration_frequency_days: int | None = Field(None, description="For recurring surveys, the number of days (1-365) between each time the survey is shown to a user. Used with iteration_count to determine the overall schedule.", ge=1, le=365),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="The type of sampling strategy for response collection (e.g., every Nth response, random sampling)."),
    response_sampling_interval: int | None = Field(None, description="The sampling interval value (0 or greater, up to 2,147,483,647). Meaning depends on response_sampling_interval_type.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="The maximum number of responses to collect when using response sampling (0 or greater, up to 2,147,483,647).", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="When true, survey responses are saved as soon as at least one question is answered. When false, responses are only saved after all questions are completed."),
    enable_iframe_embedding: bool | None = Field(None, description="When true, allows the survey to be embedded and displayed within an iframe on external websites."),
    translations: Any | None = Field(None, description="Multi-language translations for survey content. Maps language codes to translated versions of questions, descriptions, labels, and other text fields."),
) -> dict[str, Any]:
    """Partially update a survey's configuration, including questions, scheduling, display conditions, and appearance settings. Use this to modify any aspect of an existing survey without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.SurveysPartialUpdateRequest(
            path=_models.SurveysPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.SurveysPartialUpdateRequestBody(description=description, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_survey_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_survey_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_survey_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_survey_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def delete_survey(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a survey from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.SurveysDestroyRequest(
            path=_models.SurveysDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_survey: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_survey")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_survey", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_survey",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def get_survey_activity(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific survey within a project. This shows all actions and changes that have occurred on the survey."""

    # Construct request model with validation
    try:
        _request = _models.SurveysActivityRetrieve2Request(
            path=_models.SurveysActivityRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_survey_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_survey_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_survey_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_survey_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def list_archived_response_uuids_for_survey(
    id_: str = Field(..., alias="id", description="The UUID of the survey for which to retrieve archived response identifiers."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a list of archived response UUIDs for a specific survey. Use these UUIDs to filter out archived responses when constructing HogQL queries on the frontend."""

    # Construct request model with validation
    try:
        _request = _models.SurveysArchivedResponseUuidsRetrieveRequest(
            path=_models.SurveysArchivedResponseUuidsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_archived_response_uuids_for_survey: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/archived-response-uuids", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/archived-response-uuids"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_archived_response_uuids_for_survey")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_archived_response_uuids_for_survey", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_archived_response_uuids_for_survey",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def duplicate_survey_to_projects(
    id_: str = Field(..., alias="id", description="The UUID of the survey to duplicate."),
    project_id: str = Field(..., description="The UUID of the source project containing the survey being duplicated."),
    name: str = Field(..., description="Display name for the duplicated survey. Maximum 400 characters.", max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="Survey delivery format: popover (embedded overlay), widget (embedded component), external_survey (standalone link), or api (programmatic access)."),
    description: str | None = Field(None, description="Optional description or notes about the survey."),
    schedule: str | None = Field(None, description="Optional schedule configuration for survey deployment timing."),
    questions: Any | None = Field(None, description="Array of survey questions. Each question must be one of four types: open-ended (open), link-based (link), rating scale (rating), or choice-based (single_choice or multiple_choice). Questions support branching logic, translations, and optional descriptions."),
    conditions: Any | None = Field(None, description="Optional conditional logic to control survey flow and visibility based on user attributes or responses."),
    appearance: Any | None = Field(None, description="Optional styling and presentation settings including colors, fonts, and layout preferences."),
    start_date: str | None = Field(None, description="Optional ISO 8601 datetime when the survey becomes active."),
    end_date: str | None = Field(None, description="Optional ISO 8601 datetime when the survey stops accepting responses."),
    responses_limit: int | None = Field(None, description="Optional maximum number of responses to collect before closing the survey. Must be a non-negative integer.", ge=0, le=2147483647),
    iteration_count: int | None = Field(None, description="Optional number of times to repeat the survey. Must be between 0 and 500.", ge=0, le=500),
    iteration_frequency_days: int | None = Field(None, description="Optional number of days between survey iterations when iteration_count is greater than 1.", ge=0, le=2147483647),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Optional sampling strategy type for response collection (e.g., every nth response, random sampling)."),
    response_sampling_interval: int | None = Field(None, description="Optional interval value for response sampling based on the selected sampling strategy.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="Optional maximum number of responses to collect when using response sampling.", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="Optional flag to allow respondents to submit incomplete surveys before answering all questions."),
    enable_iframe_embedding: bool | None = Field(None, description="Optional flag to enable embedding the survey in an iframe on external websites."),
    translations: Any | None = Field(None, description="Optional object mapping language codes to translated survey content including questions, descriptions, and labels."),
) -> dict[str, Any]:
    """Duplicate a survey to multiple projects in a single atomic transaction. All target projects receive an identical copy of the survey, or if any duplication fails, all changes are rolled back with no partial results."""

    # Construct request model with validation
    try:
        _request = _models.SurveysDuplicateToProjectsCreateRequest(
            path=_models.SurveysDuplicateToProjectsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.SurveysDuplicateToProjectsCreateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for duplicate_survey_to_projects: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/duplicate_to_projects", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/duplicate_to_projects"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("duplicate_survey_to_projects")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("duplicate_survey_to_projects", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="duplicate_survey_to_projects",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def archive_survey_response(
    id_: str = Field(..., alias="id", description="The UUID of the survey containing the response to archive. Must be a valid UUID format."),
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs from the /api/projects/ endpoint."),
    response_uuid: str = Field(..., description="The UUID of the specific survey response to archive. Must not contain forward slashes.", pattern="^[^/]+$"),
    name: str = Field(..., description="The name of the survey. Maximum length is 400 characters.", max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="The survey delivery type. Choose from: popover (embedded popup), widget (embedded component), external_survey (hosted externally), or api (programmatic access)."),
    description: str | None = Field(None, description="Optional description providing additional context about the survey."),
    schedule: str | None = Field(None, description="Optional scheduling configuration for survey deployment timing and frequency."),
    questions: Any | None = Field(None, description="Array of survey questions. Each question must be one of four types: Basic (open-ended), Link (with URL), Rating (with scale), or Multiple Choice (single or multiple selection). Questions support branching logic and inline translations for multiple languages."),
    conditions: Any | None = Field(None, description="Optional conditions that control survey display and targeting logic."),
    appearance: Any | None = Field(None, description="Optional appearance configuration controlling visual styling, colors, fonts, and layout of the survey."),
    start_date: str | None = Field(None, description="Optional survey start date and time in ISO 8601 format. Survey will not display before this date."),
    end_date: str | None = Field(None, description="Optional survey end date and time in ISO 8601 format. Survey will not display after this date."),
    responses_limit: int | None = Field(None, description="Optional maximum number of responses to collect. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    iteration_count: int | None = Field(None, description="Optional number of survey iterations or cycles to run. Must be between 0 and 500.", ge=0, le=500),
    iteration_frequency_days: int | None = Field(None, description="Optional interval in days between survey iterations. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Optional sampling strategy type for response collection (e.g., random, sequential, or time-based)."),
    response_sampling_interval: int | None = Field(None, description="Optional sampling interval value defining the frequency or rate of response sampling. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="Optional maximum number of responses to collect via sampling. Must be a non-negative integer up to 2,147,483,647.", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="Optional boolean flag to allow survey responses to be saved and resumed before completion."),
    enable_iframe_embedding: bool | None = Field(None, description="Optional boolean flag to enable embedding the survey within an iframe on external websites."),
    translations: Any | None = Field(None, description="Optional translations object mapping language codes to translated survey content fields for multi-language support."),
) -> dict[str, Any]:
    """Archive a single survey response by its UUID. This operation marks the response as archived within the specified survey and project."""

    # Construct request model with validation
    try:
        _request = _models.SurveysResponsesArchiveCreateRequest(
            path=_models.SurveysResponsesArchiveCreateRequestPath(id_=id_, project_id=project_id, response_uuid=response_uuid),
            body=_models.SurveysResponsesArchiveCreateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for archive_survey_response: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/responses/{response_uuid}/archive", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/responses/{response_uuid}/archive"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("archive_survey_response")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("archive_survey_response", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="archive_survey_response",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def unarchive_survey_response(
    id_: str = Field(..., alias="id", description="The UUID of the survey containing the response to unarchive."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Retrieve this from the /api/projects/ endpoint if needed."),
    response_uuid: str = Field(..., description="The UUID of the specific survey response to unarchive.", pattern="^[^/]+$"),
    name: str = Field(..., description="The name of the survey. Maximum 400 characters.", max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="The survey delivery method: popover (embedded modal), widget (embedded form), external_survey (hosted externally), or api (programmatic)."),
    description: str | None = Field(None, description="Optional description providing additional context about the survey."),
    schedule: str | None = Field(None, description="Optional scheduling configuration for survey deployment timing."),
    questions: Any | None = Field(None, description="Array of survey questions in any combination of types: Basic (open-ended), Link (with URL), Rating (scale-based, optionally NPS), or Multiple Choice (single or multiple selection). Each question supports branching logic (next question, end survey, response-based routing, or jump to specific question) and inline translations across multiple languages."),
    conditions: Any | None = Field(None, description="Optional conditions that control survey visibility and targeting logic."),
    appearance: Any | None = Field(None, description="Optional appearance configuration controlling survey styling, layout, and visual presentation."),
    start_date: str | None = Field(None, description="Optional start date and time for survey activation in ISO 8601 format."),
    end_date: str | None = Field(None, description="Optional end date and time for survey deactivation in ISO 8601 format."),
    responses_limit: int | None = Field(None, description="Optional maximum number of responses to collect. Must be between 0 and 2,147,483,647.", ge=0, le=2147483647),
    iteration_count: int | None = Field(None, description="Optional number of survey iterations or cycles to run. Must be between 0 and 500.", ge=0, le=500),
    iteration_frequency_days: int | None = Field(None, description="Optional interval in days between survey iterations. Must be between 0 and 2,147,483,647.", ge=0, le=2147483647),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Optional sampling strategy type for response collection (e.g., random, systematic)."),
    response_sampling_interval: int | None = Field(None, description="Optional sampling interval value controlling response collection frequency. Must be between 0 and 2,147,483,647.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="Optional maximum number of responses to collect via sampling. Must be between 0 and 2,147,483,647.", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="Optional flag to allow survey responses to be saved and resumed later without completing all questions."),
    enable_iframe_embedding: bool | None = Field(None, description="Optional flag to enable embedding the survey within an iframe on external websites."),
    translations: Any | None = Field(None, description="Optional object mapping language codes to translated survey content, allowing multi-language support for questions, descriptions, labels, and other text fields."),
) -> dict[str, Any]:
    """Restore a previously archived survey response to active status. This operation moves a single response back into the main response collection for the specified survey."""

    # Construct request model with validation
    try:
        _request = _models.SurveysResponsesUnarchiveCreateRequest(
            path=_models.SurveysResponsesUnarchiveCreateRequestPath(id_=id_, project_id=project_id, response_uuid=response_uuid),
            body=_models.SurveysResponsesUnarchiveCreateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for unarchive_survey_response: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/responses/{response_uuid}/unarchive", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/responses/{response_uuid}/unarchive"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("unarchive_survey_response")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("unarchive_survey_response", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="unarchive_survey_response",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def get_survey_statistics(
    id_: str = Field(..., alias="id", description="The UUID identifier of the survey for which to retrieve statistics."),
    project_id: str = Field(..., description="The UUID identifier of the project containing the survey. Use /api/projects/ to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve aggregated response statistics for a survey, including event counts, unique respondent metrics, and conversion rates. Optionally filter by date range and archived response status."""

    # Construct request model with validation
    try:
        _request = _models.SurveysStatsRetrieve2Request(
            path=_models.SurveysStatsRetrieve2RequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_survey_statistics: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/stats", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/stats"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_survey_statistics")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_survey_statistics", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_survey_statistics",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def create_survey_summary_headline(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the survey to create a summary headline for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the survey. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name for the survey headline. Maximum 400 characters.", max_length=400),
    type_: Literal["popover", "widget", "external_survey", "api"] = Field(..., alias="type", description="The survey delivery method: popover (inline popup), widget (embedded component), external_survey (standalone link), or api (programmatic access)."),
    description: str | None = Field(None, description="Optional detailed description providing additional context about the survey."),
    schedule: str | None = Field(None, description="Optional scheduling configuration for when the survey should be displayed or active."),
    questions: Any | None = Field(None, description="Array of survey questions in any combination of four types: Basic (open-ended), Link (with URL), Rating (scale-based, optionally NPS), or Multiple Choice (single or multiple selection). Each question supports branching logic (next question, end survey, response-based routing, or jump to specific question) and inline translations across multiple languages."),
    conditions: Any | None = Field(None, description="Optional conditional logic rules that determine when the survey should be displayed or which questions should appear based on user attributes or responses."),
    appearance: Any | None = Field(None, description="Optional styling and presentation settings including colors, fonts, positioning, and visual theme customization."),
    start_date: str | None = Field(None, description="Optional start date and time (ISO 8601 format) for when the survey becomes active."),
    end_date: str | None = Field(None, description="Optional end date and time (ISO 8601 format) for when the survey stops collecting responses."),
    responses_limit: int | None = Field(None, description="Optional maximum number of responses to collect before closing the survey. Must be a non-negative integer.", ge=0, le=2147483647),
    iteration_count: int | None = Field(None, description="Optional number of survey iterations or cycles to run. Must be between 0 and 500.", ge=0, le=500),
    iteration_frequency_days: int | None = Field(None, description="Optional interval in days between survey iterations. Must be a non-negative integer.", ge=0, le=2147483647),
    response_sampling_interval_type: Literal["day", "week", "month"] | Literal[""] | None = Field(None, description="Optional sampling strategy type that determines how responses are selected for collection (e.g., every nth response, random sampling)."),
    response_sampling_interval: int | None = Field(None, description="Optional sampling interval value used with the sampling strategy type. Must be a non-negative integer.", ge=0, le=2147483647),
    response_sampling_limit: int | None = Field(None, description="Optional maximum number of responses to collect when using response sampling. Must be a non-negative integer.", ge=0, le=2147483647),
    enable_partial_responses: bool | None = Field(None, description="Optional flag to allow users to submit incomplete surveys with unanswered questions."),
    enable_iframe_embedding: bool | None = Field(None, description="Optional flag to enable embedding the survey within an iframe on external websites."),
    translations: Any | None = Field(None, description="Optional object mapping language codes to translated versions of survey fields (name, description, and question content), enabling multi-language survey delivery."),
) -> dict[str, Any]:
    """Create a summary headline for a survey within a project. This configures the survey's display type, questions, scheduling, and response collection settings."""

    # Construct request model with validation
    try:
        _request = _models.SurveysSummaryHeadlineCreateRequest(
            path=_models.SurveysSummaryHeadlineCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.SurveysSummaryHeadlineCreateRequestBody(name=name, description=description, type_=type_, schedule=schedule, questions=questions, conditions=conditions, appearance=appearance, start_date=start_date, end_date=end_date, responses_limit=responses_limit, iteration_count=iteration_count, iteration_frequency_days=iteration_frequency_days, response_sampling_interval_type=response_sampling_interval_type, response_sampling_interval=response_sampling_interval, response_sampling_limit=response_sampling_limit, enable_partial_responses=enable_partial_responses, enable_iframe_embedding=enable_iframe_embedding, translations=translations)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_survey_summary_headline: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/{id}/summary_headline", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/{id}/summary_headline"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_survey_summary_headline")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_survey_summary_headline", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_survey_summary_headline",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def list_surveys_activity(project_id: str = Field(..., description="The unique identifier of the project. You can obtain this ID by calling the /api/projects/ endpoint to list available projects.")) -> dict[str, Any]:
    """Retrieve activity records for surveys within a specific project. This endpoint provides insights into survey-related events and interactions for the given project."""

    # Construct request model with validation
    try:
        _request = _models.SurveysActivityRetrieveRequest(
            path=_models.SurveysActivityRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_surveys_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_surveys_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_surveys_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_surveys_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def get_survey_response_counts(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve your project ID by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve the total number of responses for all surveys in a project, with optional filtering by survey IDs and archived status."""

    # Construct request model with validation
    try:
        _request = _models.SurveysResponsesCountRetrieveRequest(
            path=_models.SurveysResponsesCountRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_survey_response_counts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/responses_count", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/responses_count"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_survey_response_counts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_survey_response_counts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_survey_response_counts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: surveys, surveys
@mcp.tool()
async def get_surveys_stats(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve aggregated response statistics across all surveys in a project, with optional filtering by date range."""

    # Construct request model with validation
    try:
        _request = _models.SurveysStatsRetrieveRequest(
            path=_models.SurveysStatsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_surveys_stats: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/surveys/stats", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/surveys/stats"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_surveys_stats")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_surveys_stats", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_surveys_stats",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def list_tasks(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the projects list endpoint."),
    internal: bool | None = Field(None, description="Filter tasks by internal status. When not specified, internal tasks are excluded by default."),
    limit: int | None = Field(None, description="Maximum number of tasks to return per page for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, used for pagination."),
    organization: str | None = Field(None, description="Filter tasks by the repository organization name. Must be at least 1 character.", min_length=1),
    origin_product: str | None = Field(None, description="Filter tasks by their origin product. Must be at least 1 character.", min_length=1),
    repository: str | None = Field(None, description="Filter tasks by repository name, optionally in organization/repository format. Must be at least 1 character.", min_length=1),
    stage: str | None = Field(None, description="Filter tasks by their current run stage. Must be at least 1 character.", min_length=1),
) -> dict[str, Any]:
    """Retrieve a paginated list of tasks for a project with optional filtering by origin product, stage, organization, repository, and creator. Use filtering parameters to narrow results to specific task categories or sources."""

    # Construct request model with validation
    try:
        _request = _models.TasksListRequest(
            path=_models.TasksListRequestPath(project_id=project_id),
            query=_models.TasksListRequestQuery(internal=internal, limit=limit, offset=offset, organization=organization, origin_product=origin_product, repository=repository, stage=stage)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_tasks: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_tasks")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_tasks", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_tasks",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def create_task(
    project_id: str = Field(..., description="The unique identifier of the project where the task will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    title: str = Field(..., description="The name or title of the task. This is the primary identifier displayed to users and agents."),
    description: str | None = Field(None, description="A detailed explanation of the task's purpose, requirements, or context. Provides additional information beyond the title to guide task execution."),
    assignee: str | None = Field(None, description="The identifier of the agent or user responsible for completing this task. If not specified, the task remains unassigned."),
) -> dict[str, Any]:
    """Create a new task within a project. Tasks represent discrete units of work that can be assigned to agents for execution."""

    # Construct request model with validation
    try:
        _request = _models.TasksCreateRequest(
            path=_models.TasksCreateRequestPath(project_id=project_id),
            body=_models.TasksCreateRequestBody(title=title, description=description, assignee=assignee)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_task: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_task")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_task", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_task",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def get_task(
    id_: str = Field(..., alias="id", description="The unique identifier of the task to retrieve, formatted as a UUID."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. You can retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific task within a project by its unique identifier. Returns detailed information about the task including its status, assignments, and metadata."""

    # Construct request model with validation
    try:
        _request = _models.TasksRetrieveRequest(
            path=_models.TasksRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def update_task(
    id_: str = Field(..., alias="id", description="The unique identifier of the task to update, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    title: str = Field(..., description="The title of the task. This is the primary label for the task and should be descriptive."),
    description: str | None = Field(None, description="A detailed description of the task, including context, requirements, or instructions for completion."),
    assignee: str | None = Field(None, description="The identifier of the agent or user assigned to perform this task."),
) -> dict[str, Any]:
    """Update an existing task within a project, including its title, description, and assignee. Requires the task ID and project ID to identify the specific task to modify."""

    # Construct request model with validation
    try:
        _request = _models.TasksUpdateRequest(
            path=_models.TasksUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.TasksUpdateRequestBody(title=title, description=description, assignee=assignee)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_task: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_task")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_task", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_task",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def update_task_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. Retrieve available project IDs from the /api/projects/ endpoint."),
    title: str | None = Field(None, description="The task title. Maximum 255 characters.", max_length=255),
    title_manually_set: bool | None = Field(None, description="Whether the task title was manually set by a user (as opposed to auto-generated)."),
    description: str | None = Field(None, description="A detailed description of the task and its requirements."),
    origin_product: Literal["error_tracking", "eval_clusters", "user_created", "slack", "support_queue", "session_summaries", "signal_report"] | None = Field(None, description="The source system that originated this task. Valid sources include error tracking systems, evaluation clusters, user-created tasks, Slack, support queues, session summaries, and signal reports."),
    repository: str | None = Field(None, description="The code repository associated with this task. Maximum 255 characters.", max_length=255),
    github_integration: int | None = Field(None, description="The ID of the GitHub integration configured for this task, enabling automated GitHub interactions."),
    signal_report: str | None = Field(None, description="The unique identifier (UUID) of the signal report associated with this task, if applicable."),
    json_schema: Any | None = Field(None, description="A JSON schema that defines the structure and validation rules for task outputs. Used to validate the task's execution results."),
    internal: bool | None = Field(None, description="If true, marks this task as internal-only and excludes it from exposure to end users."),
) -> dict[str, Any]:
    """Partially update a task within a project. Allows modification of task properties such as title, description, origin source, and associated metadata."""

    # Construct request model with validation
    try:
        _request = _models.TasksPartialUpdateRequest(
            path=_models.TasksPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.TasksPartialUpdateRequestBody(title=title, title_manually_set=title_manually_set, description=description, origin_product=origin_product, repository=repository, github_integration=github_integration, signal_report=signal_report, json_schema=json_schema, internal=internal)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_task_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_task_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_task_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_task_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def delete_task(
    id_: str = Field(..., alias="id", description="The unique identifier of the task to delete, provided as a UUID string."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. Retrieve available project IDs by calling the projects list endpoint."),
) -> dict[str, Any]:
    """Permanently delete a task from a project. This operation removes the specified task and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.TasksDestroyRequest(
            path=_models.TasksDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_task: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_task")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_task", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_task",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: tasks, tasks
@mcp.tool()
async def create_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task to run."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. Retrieve available project IDs from the /api/projects/ endpoint."),
    mode: Literal["interactive", "background"] | None = Field(None, description="Execution mode determining run behavior: 'interactive' maintains a connection to the user for real-time interaction, while 'background' runs autonomously without user connection. Defaults to 'background'."),
    branch: str | None = Field(None, description="Git branch name to checkout in the sandbox environment for this run. Limited to 255 characters.", max_length=255),
    resume_from_run_id: str | None = Field(None, description="UUID of a previous run from the same task to resume execution from. Use this to continue an interrupted or paused run."),
    pending_user_message: str | None = Field(None, description="Follow-up message from the user to include in the resumed run's prompt context. Only applicable when resuming from a previous run."),
    sandbox_environment_id: str | None = Field(None, description="UUID of a specific sandbox environment to use for this cloud run. If not specified, a default environment will be allocated."),
) -> dict[str, Any]:
    """Initiate a new task run to execute the associated workflow. Specify execution mode, optional git branch, and sandbox environment to control how the task executes."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunCreateRequest(
            path=_models.TasksRunCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.TasksRunCreateRequestBody(mode=mode, branch=branch, resume_from_run_id=resume_from_run_id, pending_user_message=pending_user_message, sandbox_environment_id=sandbox_environment_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{id}/run", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{id}/run"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_task_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_task_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def list_task_runs(
    project_id: str = Field(..., description="The unique identifier of the project containing the task. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID format) of the task for which to list runs."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting index for results, allowing you to paginate through large result sets. Use with limit to control pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of all runs for a specific task within a project. Use pagination parameters to control result size and offset."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsListRequest(
            path=_models.TasksRunsListRequestPath(project_id=project_id, task_id=task_id),
            query=_models.TasksRunsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_task_runs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_task_runs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_task_runs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_task_runs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def create_task_run_record(
    project_id: str = Field(..., description="The unique identifier of the project containing the task. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID format) of the task for which to create a run."),
) -> dict[str, Any]:
    """Create a new run for a specific task within a project. A task run represents a single execution instance of a task."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsCreateRequest(
            path=_models.TasksRunsCreateRequestPath(project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_task_run_record: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_task_run_record")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_task_run_record", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_task_run_record",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task that owns this run."),
) -> dict[str, Any]:
    """Retrieve detailed information about a specific task run execution, identified by its UUID. Use this to inspect the status, results, and metadata of a particular task execution within a project."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsRetrieveRequest(
            path=_models.TasksRunsRetrieveRequestPath(id_=id_, project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task_run", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task_run",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def update_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to update."),
    project_id: str = Field(..., description="The project ID containing the task. Retrieve available project IDs from the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with this run."),
    status: Literal["not_started", "queued", "in_progress", "completed", "failed", "cancelled"] | None = Field(None, description="The current execution status of the run. Valid values are: not_started, queued, in_progress, completed, failed, or cancelled."),
    branch: str | None = Field(None, description="The Git branch name to associate with this task run execution."),
    stage: str | None = Field(None, description="The current stage or phase of the run (e.g., research, plan, build). Use to track progression through multi-stage executions."),
    output: Any | None = Field(None, description="The output data or results produced by the run execution."),
    state: Any | None = Field(None, description="The internal state object tracking the run's execution context and metadata."),
    error_message: str | None = Field(None, description="An error message describing the failure reason if the run status is failed."),
) -> dict[str, Any]:
    """Partially update a task run to modify its execution status, stage, output, or error information. Use this to track run progress, record completion results, or log failures."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsPartialUpdateRequest(
            path=_models.TasksRunsPartialUpdateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsPartialUpdateRequestBody(status=status, branch=branch, stage=stage, output=output, state=state, error_message=error_message)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_task_run", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_task_run",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def append_log_entries_to_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to append logs to."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. Retrieve available project IDs from the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
    entries: list[dict[str, Any]] = Field(..., description="An array of log entry objects to append to the task run log. Each entry should be a dictionary containing the log data. Entries are appended in the order provided."),
) -> dict[str, Any]:
    """Append one or more log entries to a task run's log history. Use this to record execution events, errors, or status updates for a specific task run."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsAppendLogCreateRequest(
            path=_models.TasksRunsAppendLogCreateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsAppendLogCreateRequestBody(entries=entries)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for append_log_entries_to_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/append_log", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/append_log"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("append_log_entries_to_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("append_log_entries_to_task_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="append_log_entries_to_task_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def create_artifacts_for_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to which artifacts will be attached."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
    artifacts: list[_models.TaskRunArtifactUpload] = Field(..., description="An ordered array of artifact objects to upload. Each artifact will be persisted to S3 and referenced in the run manifest."),
) -> dict[str, Any]:
    """Upload and persist artifacts for a specific task run, storing them in S3 and attaching them to the run manifest for later retrieval and analysis."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsArtifactsCreateRequest(
            path=_models.TasksRunsArtifactsCreateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsArtifactsCreateRequestBody(artifacts=artifacts)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_artifacts_for_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/artifacts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/artifacts"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_artifacts_for_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_artifacts_for_task_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_artifacts_for_task_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_artifact_presigned_url(
    id_: str = Field(..., alias="id", description="The UUID of the task run containing the artifact."),
    project_id: str = Field(..., description="The UUID or identifier of the project containing the task. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The UUID of the task associated with the run."),
    storage_path: str = Field(..., description="The S3 storage path of the artifact, as returned in the artifact manifest. Must not exceed 500 characters.", max_length=500),
) -> dict[str, Any]:
    """Generate a temporary presigned URL for downloading a specific artifact from a task run. The returned URL can be used to directly access the artifact without additional authentication."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsArtifactsPresignCreateRequest(
            path=_models.TasksRunsArtifactsPresignCreateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsArtifactsPresignCreateRequestBody(storage_path=storage_path)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_artifact_presigned_url: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/artifacts/presign", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/artifacts/presign"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_artifact_presigned_url")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_artifact_presigned_url", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_artifact_presigned_url",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def send_command_to_task_run(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to target with this command."),
    project_id: str = Field(..., description="The project ID containing the task. Retrieve available project IDs from the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with this run."),
    jsonrpc: Literal["2.0"] = Field(..., description="JSON-RPC protocol version. Must be '2.0' to comply with the JSON-RPC 2.0 specification."),
    method: Literal["user_message", "cancel", "close"] = Field(..., description="The command to execute on the agent server. Use 'user_message' to send input to the agent, 'cancel' to stop execution, or 'close' to terminate the connection."),
    params: dict[str, Any] | None = Field(None, description="Optional command-specific parameters as a JSON object. Structure depends on the selected method (e.g., message content for user_message)."),
) -> dict[str, Any]:
    """Send a JSON-RPC command to the agent server running in a task's sandbox environment. Supports sending user messages, canceling execution, or closing the connection."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsCommandCreateRequest(
            path=_models.TasksRunsCommandCreateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsCommandCreateRequestBody(jsonrpc=jsonrpc, method=method, params=params)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_command_to_task_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/command", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/command"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_command_to_task_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_command_to_task_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_command_to_task_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_task_run_sandbox_connection_token(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run for which to generate the connection token."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
) -> dict[str, Any]:
    """Generate a JWT token for direct sandbox connection to a specific task run. The token is valid for 24 hours and enables authenticated access to the sandbox environment."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsConnectionTokenRetrieveRequest(
            path=_models.TasksRunsConnectionTokenRetrieveRequestPath(id_=id_, project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task_run_sandbox_connection_token: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/connection_token", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/connection_token"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task_run_sandbox_connection_token")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task_run_sandbox_connection_token", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task_run_sandbox_connection_token",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_task_run_logs(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run whose logs you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
) -> dict[str, Any]:
    """Retrieve the logs for a specific task run in JSONL format. Each log entry is returned as a separate JSON object on its own line."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsLogsRetrieveRequest(
            path=_models.TasksRunsLogsRetrieveRequestPath(id_=id_, project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task_run_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/logs"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task_run_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task_run_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task_run_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def send_run_message_to_slack(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to relay the message for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. Retrieve available project IDs from the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
    text: str = Field(..., description="The message text to relay to Slack. Maximum length is 10,000 characters.", max_length=10000),
) -> dict[str, Any]:
    """Queue a Slack relay workflow to post a run message into the mapped Slack thread for a specific task run."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsRelayMessageCreateRequest(
            path=_models.TasksRunsRelayMessageCreateRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            body=_models.TasksRunsRelayMessageCreateRequestBody(text=text)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for send_run_message_to_slack: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/relay_message", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/relay_message"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("send_run_message_to_slack")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("send_run_message_to_slack", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="send_run_message_to_slack",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_task_run_session_logs(
    id_: str = Field(..., alias="id", description="The UUID of the task run to retrieve logs for."),
    project_id: str = Field(..., description="The project ID containing the task run. Retrieve available project IDs from the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The UUID of the task associated with this run."),
    after: str | None = Field(None, description="Filter to only return log entries after this ISO 8601 timestamp. Useful for retrieving logs since a specific point in time."),
    event_types: str | None = Field(None, description="Comma-separated list of event types to include in results. Only logs matching these types will be returned. Specify at least one type.", min_length=1),
    exclude_types: str | None = Field(None, description="Comma-separated list of event types to exclude from results. Logs matching these types will be filtered out. Specify at least one type.", min_length=1),
    limit: int | None = Field(None, description="Maximum number of log entries to return. Defaults to 1000 if not specified. Must be between 1 and 5000.", ge=1, le=5000),
) -> dict[str, Any]:
    """Retrieve session log entries for a specific task run with optional filtering by timestamp, event types, and result limits. Use this to audit task execution events and debug run behavior."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsSessionLogsRetrieveRequest(
            path=_models.TasksRunsSessionLogsRetrieveRequestPath(id_=id_, project_id=project_id, task_id=task_id),
            query=_models.TasksRunsSessionLogsRetrieveRequestQuery(after=after, event_types=event_types, exclude_types=exclude_types, limit=limit)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task_run_session_logs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/session_logs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/session_logs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task_run_session_logs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task_run_session_logs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task_run_session_logs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def update_task_run_output(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task run. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with the run."),
) -> dict[str, Any]:
    """Update the output field for a task run to store results such as PR URLs, commit SHAs, or other completion artifacts."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsSetOutputPartialUpdateRequest(
            path=_models.TasksRunsSetOutputPartialUpdateRequestPath(id_=id_, project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_task_run_output: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/set_output", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/set_output"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_task_run_output")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_task_run_output", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_task_run_output",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: task-runs, tasks
@mcp.tool()
async def get_task_run_stream(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the task run to retrieve stream data for."),
    project_id: str = Field(..., description="The unique identifier of the project containing the task. Use the /api/projects/ endpoint to discover available project IDs."),
    task_id: str = Field(..., description="The unique identifier (UUID) of the task associated with this run."),
) -> dict[str, Any]:
    """Retrieve the execution stream for a specific task run, providing real-time or historical output data from the task's execution."""

    # Construct request model with validation
    try:
        _request = _models.TasksRunsStreamRetrieveRequest(
            path=_models.TasksRunsStreamRetrieveRequestPath(id_=id_, project_id=project_id, task_id=task_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_task_run_stream: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/tasks/{task_id}/runs/{id}/stream", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/tasks/{task_id}/runs/{id}/stream"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_task_run_stream")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_task_run_stream", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_task_run_stream",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: uploaded_media
@mcp.tool()
async def create_uploaded_media(project_id: str = Field(..., description="The unique identifier of the project where the media will be uploaded. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Upload media files (images) to a project for use in dashboards and text cards. Uploaded files must be images (content type starting with 'image/') and not exceed 4MB in size."""

    # Construct request model with validation
    try:
        _request = _models.UploadedMediaCreateRequest(
            path=_models.UploadedMediaCreateRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_uploaded_media: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/uploaded_media", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/uploaded_media"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_uploaded_media")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_uploaded_media", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_uploaded_media",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def list_visual_review_repositories(
    project_id: str = Field(..., description="The unique identifier of the project containing the repositories. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of repositories to return in a single response page. Omit or adjust to control pagination size."),
    offset: int | None = Field(None, description="Zero-based starting position for results, used with limit to paginate through the repository list."),
) -> dict[str, Any]:
    """Retrieve all repositories available for visual review within a specific project. Use pagination parameters to control result size and offset."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewReposListRequest(
            path=_models.VisualReviewReposListRequestPath(project_id=project_id),
            query=_models.VisualReviewReposListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_visual_review_repositories: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/repos", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/repos"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_visual_review_repositories")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_visual_review_repositories", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_visual_review_repositories",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def create_visual_review_repo(
    project_id: str = Field(..., description="The unique identifier of the project where the visual review repository will be created. Retrieve this ID by calling the /api/projects/ endpoint."),
    repo_full_name: str = Field(..., description="The full name of the repository in the format owner/repo_name, used to identify and reference the repository within the visual review system."),
    repo_external_id: int | None = Field(None, description="An optional external identifier for the repository, useful for mapping to external repository systems or tracking purposes."),
) -> dict[str, Any]:
    """Create a new repository for visual review within a project. This enables tracking and managing visual assets for code review purposes."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewReposCreateRequest(
            path=_models.VisualReviewReposCreateRequestPath(project_id=project_id),
            body=_models.VisualReviewReposCreateRequestBody(repo_full_name=repo_full_name, repo_external_id=repo_external_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_visual_review_repo: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/repos", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/repos"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_visual_review_repo")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_visual_review_repo", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_visual_review_repo",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def get_visual_review_repo(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review repository to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the repository. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific visual review repository by its ID within a project. Use this to fetch detailed information about a single repo for visual review purposes."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewReposRetrieveRequest(
            path=_models.VisualReviewReposRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_visual_review_repo: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/repos/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/repos/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_visual_review_repo")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_visual_review_repo", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_visual_review_repo",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def update_visual_review_repo_settings(
    id_: str = Field(..., alias="id", description="The unique identifier of the repository to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the repository. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    baseline_file_paths: dict[str, str] | None = Field(None, description="File paths to use as baseline for visual comparisons. Specify as an object with path mappings."),
    enable_pr_comments: bool | None = Field(None, description="Enable or disable automatic pull request comments for visual review findings."),
) -> dict[str, Any]:
    """Update configuration settings for a repository in visual review, including baseline file paths and pull request comment behavior."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewReposPartialUpdateRequest(
            path=_models.VisualReviewReposPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.VisualReviewReposPartialUpdateRequestBody(baseline_file_paths=baseline_file_paths, enable_pr_comments=enable_pr_comments)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_visual_review_repo_settings: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/repos/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/repos/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_visual_review_repo_settings")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_visual_review_repo_settings", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_visual_review_repo_settings",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def list_visual_review_runs(
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review runs. You can retrieve available project IDs by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Use this to control pagination size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
    review_state: str | None = Field(None, description="Filter results to only include runs with a specific review state, allowing you to focus on runs at particular stages of the review process."),
) -> dict[str, Any]:
    """Retrieve a paginated list of visual review runs for a project, with optional filtering by review state to help you track and manage review progress across your team."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsListRequest(
            path=_models.VisualReviewRunsListRequestPath(project_id=project_id),
            query=_models.VisualReviewRunsListRequestQuery(limit=limit, offset=offset, review_state=review_state)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_visual_review_runs: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_visual_review_runs")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_visual_review_runs", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_visual_review_runs",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def create_visual_review_run(
    project_id: str = Field(..., description="The unique identifier of the project. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    repo_id: str = Field(..., description="The unique identifier (UUID format) of the repository associated with this visual review run."),
    run_type: str = Field(..., description="The type of run being created, typically indicating the CI pipeline or test category that generated this visual review."),
    commit_sha: str = Field(..., description="The commit SHA (hash) that this visual review run is associated with, identifying the exact code version being reviewed."),
    branch: str = Field(..., description="The branch name where the commit resides, providing context for the visual review within the repository's branching structure."),
    snapshots: list[_models.SnapshotManifestItem] = Field(..., description="An array of snapshot objects to be included in this run. Order is preserved and each snapshot represents a visual state to be reviewed."),
    pr_number: int | None = Field(None, description="Optional pull request number associated with this run, linking the visual review to a specific PR if applicable."),
    baseline_hashes: dict[str, str] | None = Field(None, description="Optional object containing baseline hashes for comparison, used to identify which snapshots have changed relative to a baseline version."),
    unchanged_count: int | None = Field(None, description="Optional count of snapshots that remain unchanged from the baseline, useful for summarizing review scope."),
    removed_identifiers: list[str] | None = Field(None, description="Optional array of snapshot identifiers that have been removed, indicating which visual elements are no longer present in this run."),
    purpose: str | None = Field(None, description="Optional string describing the purpose or intent of this visual review run, such as 'regression testing' or 'design validation'."),
    metadata: dict[str, Any] | None = Field(None, description="Optional object for storing additional custom metadata or tags associated with this visual review run."),
) -> dict[str, Any]:
    """Create a new visual review run from a CI manifest, capturing snapshots and baseline information for a specific commit and branch."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsCreateRequest(
            path=_models.VisualReviewRunsCreateRequestPath(project_id=project_id),
            body=_models.VisualReviewRunsCreateRequestBody(repo_id=repo_id, run_type=run_type, commit_sha=commit_sha, branch=branch, snapshots=snapshots, pr_number=pr_number, baseline_hashes=baseline_hashes, unchanged_count=unchanged_count, removed_identifiers=removed_identifiers, purpose=purpose, metadata=metadata)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_visual_review_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_visual_review_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def get_visual_review_run(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review run. You can obtain the project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the status and summary information for a specific visual review run within a project."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsRetrieveRequest(
            path=_models.VisualReviewRunsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_visual_review_run", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_visual_review_run",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def add_snapshots_to_visual_review_run(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run to which snapshots will be added."),
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review run. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    snapshots: list[_models.SnapshotManifestItem] = Field(..., description="An array of snapshot objects to add to the run. Each snapshot represents a visual capture or state to be included in the review. Order is preserved as provided."),
    baseline_hashes: dict[str, str] | None = Field(None, description="An optional mapping of baseline hashes used to associate snapshots with their baseline references for comparison purposes."),
) -> dict[str, Any]:
    """Add a batch of snapshots to a pending visual review run using shard-based distribution. This operation allows you to incrementally build up snapshot data for a run before finalizing it."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsAddSnapshotsCreateRequest(
            path=_models.VisualReviewRunsAddSnapshotsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.VisualReviewRunsAddSnapshotsCreateRequestBody(snapshots=snapshots, baseline_hashes=baseline_hashes)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for add_snapshots_to_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}/add-snapshots", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}/add-snapshots"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("add_snapshots_to_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("add_snapshots_to_visual_review_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="add_snapshots_to_visual_review_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def approve_visual_review_run(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run to approve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review run. You can retrieve your project ID by calling the /api/projects/ endpoint."),
    snapshots: list[_models.ApproveSnapshotInput] | None = Field(None, description="Array of specific snapshot identifiers to approve. When provided, only these snapshots are approved. Omit this parameter when using approve_all=true."),
    approve_all: bool | None = Field(None, description="When true, approves all changed and new snapshots in the run and returns the signed baseline YAML. Cannot be used together with specific snapshots array."),
    commit_to_github: bool | None = Field(None, description="When true, automatically commits the approved visual changes to GitHub after approval."),
) -> dict[str, Any]:
    """Approve visual changes for snapshots in a review run. Optionally approve all changed and new snapshots at once, or selectively approve specific snapshots. Can automatically commit approved changes to GitHub."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsApproveCreateRequest(
            path=_models.VisualReviewRunsApproveCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.VisualReviewRunsApproveCreateRequestBody(snapshots=snapshots, approve_all=approve_all, commit_to_github=commit_to_github)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for approve_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}/approve", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}/approve"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("approve_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("approve_visual_review_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="approve_visual_review_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def complete_visual_review_run(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run to complete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review run. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Complete a visual review run by detecting removals, verifying uploads, and triggering diff processing for the specified project and run."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsCompleteCreateRequest(
            path=_models.VisualReviewRunsCompleteCreateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for complete_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}/complete", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}/complete"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("complete_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("complete_visual_review_run", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="complete_visual_review_run",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def list_snapshot_history_for_visual_review_run(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run containing the snapshot history to retrieve."),
    project_id: str = Field(..., description="The project ID that contains the visual review runs. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    identifier: str = Field(..., description="The snapshot identifier for which to retrieve the change history across runs."),
    limit: int | None = Field(None, description="Maximum number of history records to return per page. Use with offset for pagination."),
    offset: int | None = Field(None, description="The starting index for results, enabling pagination through the history records."),
) -> dict[str, Any]:
    """Retrieve the recent change history for a specific snapshot identifier across multiple visual review runs within a project."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsSnapshotHistoryListRequest(
            path=_models.VisualReviewRunsSnapshotHistoryListRequestPath(id_=id_, project_id=project_id),
            query=_models.VisualReviewRunsSnapshotHistoryListRequestQuery(identifier=identifier, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_snapshot_history_for_visual_review_run: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}/snapshot-history", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}/snapshot-history"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_snapshot_history_for_visual_review_run")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_snapshot_history_for_visual_review_run", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_snapshot_history_for_visual_review_run",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def list_visual_review_run_snapshots(
    id_: str = Field(..., alias="id", description="The unique identifier of the visual review run for which to retrieve snapshots."),
    project_id: str = Field(..., description="The unique identifier of the project containing the visual review run. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of snapshot results to return in a single page of results. Omit to use the default page size."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve all snapshots for a visual review run, including their diff results. Use pagination parameters to control the result set size and starting position."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsSnapshotsListRequest(
            path=_models.VisualReviewRunsSnapshotsListRequestPath(id_=id_, project_id=project_id),
            query=_models.VisualReviewRunsSnapshotsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_visual_review_run_snapshots: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/{id}/snapshots", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/{id}/snapshots"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_visual_review_run_snapshots")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_visual_review_run_snapshots", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_visual_review_run_snapshots",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: visual_review, visual_review
@mcp.tool()
async def get_visual_review_runs_counts(project_id: str = Field(..., description="The unique identifier of the project. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve aggregated state counts for visual review runs within a project, providing a summary of how many runs are in each review state."""

    # Construct request model with validation
    try:
        _request = _models.VisualReviewRunsCountsRetrieveRequest(
            path=_models.VisualReviewRunsCountsRetrieveRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_visual_review_runs_counts: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/visual_review/runs/counts", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/visual_review/runs/counts"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_visual_review_runs_counts")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_visual_review_runs_counts", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_visual_review_runs_counts",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def list_warehouse_saved_queries(
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse saved queries. Obtain this ID by calling the /api/projects/ endpoint."),
    page: int | None = Field(None, description="The page number to retrieve from the paginated result set. Omit to retrieve the first page."),
) -> dict[str, Any]:
    """Retrieve a paginated list of saved queries for a warehouse project. Use this to browse all saved queries within a specific project."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesListRequest(
            path=_models.WarehouseSavedQueriesListRequestPath(project_id=project_id),
            query=_models.WarehouseSavedQueriesListRequestQuery(page=page)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_warehouse_saved_queries: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_warehouse_saved_queries")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_warehouse_saved_queries", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_warehouse_saved_queries",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def create_warehouse_saved_query(
    project_id: str = Field(..., description="The unique identifier of the project where the saved query will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the saved query that serves as the table name in HogQL queries and node name in data modeling. Must be 128 characters or fewer.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder to organize this saved query within the SQL editor sidebar for better project structure."),
    is_test: bool | None = Field(None, description="Optional flag to mark this saved query as a temporary test query that will automatically expire, useful for development and validation."),
) -> dict[str, Any]:
    """Create a new saved warehouse query (view) that can be referenced in HogQL queries and used as a node in data modeling. Saved queries are organized within a project and can optionally be marked as temporary test queries."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesCreateRequest(
            path=_models.WarehouseSavedQueriesCreateRequestPath(project_id=project_id),
            body=_models.WarehouseSavedQueriesCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_warehouse_saved_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_warehouse_saved_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def get_warehouse_saved_query(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific saved query from your data warehouse by its unique identifier. Use this to fetch the configuration and details of a previously saved query."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesRetrieveRequest(
            path=_models.WarehouseSavedQueriesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_warehouse_saved_query", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_warehouse_saved_query",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def update_warehouse_saved_query(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse saved query to update."),
    project_id: str = Field(..., description="The project ID containing this saved query. Find your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the saved query (maximum 128 characters). This name is used as the table name in HogQL queries and as the node name in data modeling.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder to organize this saved query in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this saved query is for testing purposes only and will automatically expire."),
) -> dict[str, Any]:
    """Update an existing warehouse saved query (view) with a new name, HogQL query definition, folder organization, or test status. The saved query can be referenced as a table in HogQL queries and as a node in data modeling."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesUpdateRequest(
            path=_models.WarehouseSavedQueriesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesUpdateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_saved_query", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_saved_query",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def update_warehouse_saved_query_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse saved query to update."),
    project_id: str = Field(..., description="The project ID containing the saved query. Retrieve available project IDs from the /api/projects/ endpoint."),
    query: Any | None = Field(None, description="HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder for organizing this saved query in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional boolean flag indicating whether this is a temporary test query that will automatically expire."),
) -> dict[str, Any]:
    """Partially update a warehouse saved query, allowing modifications to the HogQL query definition, folder organization, or test status."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesPartialUpdateRequest(
            path=_models.WarehouseSavedQueriesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesPartialUpdateRequestBody(query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_saved_query_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_saved_query_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_saved_query_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_saved_query_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def delete_warehouse_saved_query(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a saved query from a data warehouse. This operation removes the query definition and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesDestroyRequest(
            path=_models.WarehouseSavedQueriesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_warehouse_saved_query", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_warehouse_saved_query",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def get_warehouse_saved_query_activity(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse saved query whose activity you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse saved query. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the activity history for a specific warehouse saved query, including creation, modification, and deletion events within a project."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesActivityRetrieveRequest(
            path=_models.WarehouseSavedQueriesActivityRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_warehouse_saved_query_activity: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/activity", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/activity"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_warehouse_saved_query_activity")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_warehouse_saved_query_activity", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_warehouse_saved_query_activity",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def list_saved_query_ancestors(
    id_: str = Field(..., alias="id", description="The UUID identifier of the saved query whose ancestors you want to retrieve."),
    project_id: str = Field(..., description="The project ID containing the saved query. Find your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the view (maximum 128 characters), used as the table name in HogQL queries and node name in data modeling.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type."),
    folder_id: str | None = Field(None, description="Optional UUID identifier of a folder to organize this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is temporary for testing purposes and will automatically expire."),
) -> dict[str, Any]:
    """Retrieve the ancestor lineage of a saved query in the data warehouse. By default returns immediate parents; use the level parameter to traverse further back in the ancestor tree."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesAncestorsCreateRequest(
            path=_models.WarehouseSavedQueriesAncestorsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesAncestorsCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_saved_query_ancestors: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/ancestors", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/ancestors"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_saved_query_ancestors")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_saved_query_ancestors", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_saved_query_ancestors",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def cancel_warehouse_saved_query(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query to cancel."),
    project_id: str = Field(..., description="The project ID containing the saved query. Find your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The unique name for this view, used as the table name in HogQL queries and node name in data modeling. Must be 128 characters or fewer.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder to organize this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is temporary for testing purposes and will automatically expire."),
) -> dict[str, Any]:
    """Cancel an in-progress saved query workflow execution in the data warehouse. This stops the running query and prevents further processing."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesCancelCreateRequest(
            path=_models.WarehouseSavedQueriesCancelCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesCancelCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cancel_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/cancel", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/cancel"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cancel_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cancel_warehouse_saved_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cancel_warehouse_saved_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def get_saved_query_dependencies(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query whose dependencies you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. You can retrieve your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve the count of immediate upstream and downstream dependencies for a specific saved query in your data warehouse. This helps you understand the impact of changes to this query on other queries and workflows."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesDependenciesRetrieveRequest(
            path=_models.WarehouseSavedQueriesDependenciesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_saved_query_dependencies: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/dependencies", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/dependencies"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_saved_query_dependencies")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_saved_query_dependencies", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_saved_query_dependencies",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def list_saved_query_descendants(
    id_: str = Field(..., alias="id", description="The UUID identifier of the saved query whose descendants you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. Retrieve project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the view (maximum 128 characters). Used as the table name in HogQL queries and the node identifier in data modeling.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., HogQLQuery)."),
    folder_id: str | None = Field(None, description="Optional UUID identifier for organizing this view within the SQL editor sidebar folder structure."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is temporary for testing purposes and will automatically expire."),
) -> dict[str, Any]:
    """Retrieve the descendant saved queries in the hierarchy tree for a specified saved query. By default returns immediate children; use the level parameter to traverse deeper into the tree."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesDescendantsCreateRequest(
            path=_models.WarehouseSavedQueriesDescendantsCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesDescendantsCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_saved_query_descendants: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/descendants", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/descendants"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_saved_query_descendants")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_saved_query_descendants", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_saved_query_descendants",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def create_materialized_view(
    id_: str = Field(..., alias="id", description="The UUID identifier of the saved query to materialize."),
    project_id: str = Field(..., description="The UUID or identifier of the project containing the saved query. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="A unique name for the materialized view (up to 128 characters). This name serves as the table identifier in HogQL queries and the node name in data modeling.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder for organizing this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is temporary for testing purposes and will automatically expire."),
) -> dict[str, Any]:
    """Enable materialization for a saved query to create a persistent view that syncs automatically every 24 hours, making the data queryable via HogQL."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesMaterializeCreateRequest(
            path=_models.WarehouseSavedQueriesMaterializeCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesMaterializeCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_materialized_view: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/materialize", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/materialize"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_materialized_view")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_materialized_view", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_materialized_view",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def revert_saved_query_materialization(
    id_: str = Field(..., alias="id", description="The UUID identifier of the saved query to revert from materialization."),
    project_id: str = Field(..., description="The UUID or identifier of the project containing the saved query. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The unique name for the view, used as the table name in HogQL queries and node name in data modeling. Must be 128 characters or fewer.", max_length=128),
    query: Any | None = Field(None, description="The HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery')."),
    folder_id: str | None = Field(None, description="Optional UUID identifier of a folder for organizing this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is temporary for testing purposes and will automatically expire."),
) -> dict[str, Any]:
    """Undo materialization of a saved query by deleting the materialized table and its associated schedule, reverting it back to a standard view."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesRevertMaterializationCreateRequest(
            path=_models.WarehouseSavedQueriesRevertMaterializationCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesRevertMaterializationCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for revert_saved_query_materialization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/revert_materialization", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/revert_materialization"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("revert_saved_query_materialization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("revert_saved_query_materialization", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="revert_saved_query_materialization",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def run_warehouse_saved_query(
    id_: str = Field(..., alias="id", description="The UUID identifier of the saved query to execute."),
    project_id: str = Field(..., description="The UUID or identifier of the project containing this saved query. Retrieve available projects from /api/projects/."),
    name: str = Field(..., description="A unique name for the result view (up to 128 characters). Used as the table name in HogQL queries and node name in data modeling. Required when persisting results.", max_length=128),
    query: Any | None = Field(None, description="HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type (e.g., 'HogQLQuery'). Overrides the saved query definition if provided."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder for organizing this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="If true, creates a temporary test view that will automatically expire. Useful for validating queries without persisting results."),
) -> dict[str, Any]:
    """Execute a saved warehouse query and retrieve results. Optionally create a temporary test view or persist results to a named view."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesRunCreateRequest(
            path=_models.WarehouseSavedQueriesRunCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseSavedQueriesRunCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for run_warehouse_saved_query: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/run", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/run"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("run_warehouse_saved_query")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("run_warehouse_saved_query", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="run_warehouse_saved_query",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def get_saved_query_run_history(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query whose run history you want to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve the 5 most recent execution records for a materialized view saved query, including run status and timing information."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesRunHistoryRetrieveRequest(
            path=_models.WarehouseSavedQueriesRunHistoryRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_saved_query_run_history: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/{id}/run_history", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/{id}/run_history"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_saved_query_run_history")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_saved_query_run_history", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_saved_query_run_history",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_queries
@mcp.tool()
async def resume_materialization_schedules(
    project_id: str = Field(..., description="The project ID containing the materialized views. Find your project ID by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="Unique identifier for the view, used as the table name in HogQL queries and node name in data modeling. Must be 128 characters or fewer.", max_length=128),
    query: Any | None = Field(None, description="HogQL query definition as a JSON object containing a 'query' key with the SQL string and a 'kind' key specifying the query type."),
    folder_id: str | None = Field(None, description="Optional UUID of a folder to organize this view in the SQL editor sidebar."),
    is_test: bool | None = Field(None, description="Optional flag indicating whether this view is for testing purposes only and will auto-expire."),
) -> dict[str, Any]:
    """Resume paused materialization schedules for one or more materialized views. This operation is idempotent and safely handles already-running or non-existent schedules."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueriesResumeSchedulesCreateRequest(
            path=_models.WarehouseSavedQueriesResumeSchedulesCreateRequestPath(project_id=project_id),
            body=_models.WarehouseSavedQueriesResumeSchedulesCreateRequestBody(name=name, query=query, folder_id=folder_id, is_test=is_test)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for resume_materialization_schedules: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_queries/resume_schedules", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_queries/resume_schedules"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("resume_materialization_schedules")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("resume_materialization_schedules", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="resume_materialization_schedules",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_query_folders
@mcp.tool()
async def list_warehouse_saved_query_folders(project_id: str = Field(..., description="The unique identifier of the project containing the warehouse saved query folders. You can retrieve available project IDs by calling the /api/projects/ endpoint.")) -> dict[str, Any]:
    """Retrieve all saved query folders within a warehouse project. This lists the organizational structure for saved queries in the specified project."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueryFoldersListRequest(
            path=_models.WarehouseSavedQueryFoldersListRequestPath(project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_warehouse_saved_query_folders: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_query_folders", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_query_folders"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_warehouse_saved_query_folders")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_warehouse_saved_query_folders", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_warehouse_saved_query_folders",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_query_folders
@mcp.tool()
async def create_warehouse_saved_query_folder(
    project_id: str = Field(..., description="The unique identifier of the project where the folder will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A display name for the folder that appears in the SQL editor sidebar. Must be 128 characters or fewer.", max_length=128),
) -> dict[str, Any]:
    """Create a new folder in the warehouse saved queries section to organize and group SQL queries in the editor sidebar. Folders help structure saved queries for better discoverability and management."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueryFoldersCreateRequest(
            path=_models.WarehouseSavedQueryFoldersCreateRequestPath(project_id=project_id),
            body=_models.WarehouseSavedQueryFoldersCreateRequestBody(name=name)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_warehouse_saved_query_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_query_folders", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_query_folders"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_warehouse_saved_query_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_warehouse_saved_query_folder", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_warehouse_saved_query_folder",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_query_folders
@mcp.tool()
async def get_warehouse_saved_query_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query folder to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query folder. Find your project ID by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific saved query folder from a data warehouse by its unique identifier. Use this to access folder details, metadata, and configuration for organizing warehouse queries."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueryFoldersRetrieveRequest(
            path=_models.WarehouseSavedQueryFoldersRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_warehouse_saved_query_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_query_folders/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_query_folders/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_warehouse_saved_query_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_warehouse_saved_query_folder", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_warehouse_saved_query_folder",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_query_folders
@mcp.tool()
async def update_warehouse_saved_query_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse saved query folder to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the folder. Retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Partially update a warehouse saved query folder within a project. Use this to modify specific properties of an existing folder without replacing the entire resource."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueryFoldersPartialUpdateRequest(
            path=_models.WarehouseSavedQueryFoldersPartialUpdateRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_saved_query_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_query_folders/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_query_folders/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_saved_query_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_saved_query_folder", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_saved_query_folder",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: data_warehouse, warehouse_saved_query_folders
@mcp.tool()
async def delete_warehouse_saved_query_folder(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the saved query folder to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the saved query folder. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a saved query folder from the data warehouse. This operation removes the folder and its contents from the specified project."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseSavedQueryFoldersDestroyRequest(
            path=_models.WarehouseSavedQueryFoldersDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_warehouse_saved_query_folder: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_saved_query_folders/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_saved_query_folders/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_warehouse_saved_query_folder")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_warehouse_saved_query_folder", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_warehouse_saved_query_folder",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def list_warehouse_tables(
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse tables. Obtain this ID by calling the /api/projects/ endpoint."),
    limit: int | None = Field(None, description="Maximum number of warehouse tables to return in a single response page."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results, used for pagination."),
) -> dict[str, Any]:
    """Retrieve a paginated list of warehouse tables for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesListRequest(
            path=_models.WarehouseTablesListRequestPath(project_id=project_id),
            query=_models.WarehouseTablesListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_warehouse_tables: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_warehouse_tables")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_warehouse_tables", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_warehouse_tables",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def create_warehouse_table(
    project_id: str = Field(..., description="The unique identifier of the project where the warehouse table will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the warehouse table. Must be 128 characters or fewer.", max_length=128),
    format_: Literal["CSV", "CSVWithNames", "Parquet", "JSONEachRow", "Delta", "DeltaS3Wrapper"] = Field(..., alias="format", description="The data format of the source files. Supported formats include CSV (plain), CSVWithNames (CSV with header row), Parquet (columnar format), JSONEachRow (newline-delimited JSON), Delta (Delta Lake format), and DeltaS3Wrapper (Delta Lake with S3 backend)."),
    url_pattern: str = Field(..., description="The URL pattern or path template that specifies where the source data files are located. Supports pattern matching for multiple files. Maximum length is 500 characters.", max_length=500),
    access_key: str = Field(..., description="The access key or username required to authenticate with the storage backend. Maximum length is 500 characters.", max_length=500),
    access_secret: str = Field(..., description="The access secret or password required to authenticate with the storage backend. Maximum length is 500 characters.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Optional configuration object for additional table settings and format-specific parameters."),
) -> dict[str, Any]:
    """Create a new warehouse table in a project by specifying the data format, storage location, and access credentials. The table will be configured to read data from the specified URL pattern using the provided authentication credentials."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesCreateRequest(
            path=_models.WarehouseTablesCreateRequestPath(project_id=project_id),
            body=_models.WarehouseTablesCreateRequestBody(name=name, format_=format_, url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesCreateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_warehouse_table: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_warehouse_table")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_warehouse_table", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_warehouse_table",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def get_warehouse_table(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to retrieve."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. Use the /api/projects/ endpoint to discover available project IDs."),
) -> dict[str, Any]:
    """Retrieve a specific warehouse table by its ID within a project. Returns the complete configuration and metadata for the requested data warehouse table."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesRetrieveRequest(
            path=_models.WarehouseTablesRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_warehouse_table: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_warehouse_table")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_warehouse_table", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_warehouse_table",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def update_warehouse_table(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the warehouse table. Must not exceed 128 characters.", max_length=128),
    format_: Literal["CSV", "CSVWithNames", "Parquet", "JSONEachRow", "Delta", "DeltaS3Wrapper"] = Field(..., alias="format", description="The data format of the warehouse table. Supported formats are CSV, CSVWithNames, Parquet, JSONEachRow, Delta, and DeltaS3Wrapper."),
    url_pattern: str = Field(..., description="The URL pattern or path to the data source location. Must not exceed 500 characters.", max_length=500),
    access_key: str = Field(..., description="The access key or credential for authenticating with the data source. Must not exceed 500 characters.", max_length=500),
    access_secret: str = Field(..., description="The access secret or password for authenticating with the data source. Must not exceed 500 characters.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Optional configuration object for additional warehouse table settings or format-specific parameters."),
) -> dict[str, Any]:
    """Update an existing warehouse table configuration, including its data format, storage location, and access credentials for a specific project."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesUpdateRequest(
            path=_models.WarehouseTablesUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseTablesUpdateRequestBody(name=name, format_=format_, url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesUpdateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_table: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_table")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_table", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_table",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def update_warehouse_table_partial(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    access_key: str = Field(..., description="The access key credential for authenticating with the warehouse, up to 500 characters.", max_length=500),
    access_secret: str = Field(..., description="The access secret credential for authenticating with the warehouse, up to 500 characters.", max_length=500),
    url_pattern: str | None = Field(None, description="The URL pattern for the warehouse table connection, up to 500 characters.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Additional configuration options for the warehouse table as a JSON object."),
) -> dict[str, Any]:
    """Partially update a warehouse table configuration within a project, allowing you to modify connection credentials, URL patterns, and other table settings."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesPartialUpdateRequest(
            path=_models.WarehouseTablesPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseTablesPartialUpdateRequestBody(url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesPartialUpdateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_table_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_table_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_table_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_table_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def delete_warehouse_table(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to delete."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a warehouse table from a project. This operation removes the specified data warehouse table and cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesDestroyRequest(
            path=_models.WarehouseTablesDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_warehouse_table: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_warehouse_table")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_warehouse_table", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_warehouse_table",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def refresh_warehouse_table_schema(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to refresh."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the warehouse table. Must not exceed 128 characters.", max_length=128),
    format_: Literal["CSV", "CSVWithNames", "Parquet", "JSONEachRow", "Delta", "DeltaS3Wrapper"] = Field(..., alias="format", description="The data format of the source file. Supported formats include CSV, CSV with header names, Parquet, JSON (one object per line), Delta Lake, and Delta Lake with S3 wrapper."),
    url_pattern: str = Field(..., description="The URL pattern or path to the data source file(s). Supports wildcards for matching multiple files. Must not exceed 500 characters.", max_length=500),
    access_key: str = Field(..., description="The access key or username required to authenticate with the data source. Must not exceed 500 characters.", max_length=500),
    access_secret: str = Field(..., description="The access secret or password required to authenticate with the data source. Must not exceed 500 characters.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Optional configuration object for additional data source settings or format-specific parameters."),
) -> dict[str, Any]:
    """Refresh the schema of a warehouse table by re-scanning its data source. This operation updates the table's column definitions and metadata based on the current state of the underlying data file or storage location."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesRefreshSchemaCreateRequest(
            path=_models.WarehouseTablesRefreshSchemaCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseTablesRefreshSchemaCreateRequestBody(name=name, format_=format_, url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesRefreshSchemaCreateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for refresh_warehouse_table_schema: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}/refresh_schema", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}/refresh_schema"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("refresh_warehouse_table_schema")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("refresh_warehouse_table_schema", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="refresh_warehouse_table_schema",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def update_warehouse_table_schema(
    id_: str = Field(..., alias="id", description="The unique identifier (UUID) of the warehouse table to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the warehouse table. Retrieve available project IDs from the /api/projects/ endpoint."),
    name: str = Field(..., description="The name of the warehouse table. Must be 128 characters or fewer.", max_length=128),
    format_: Literal["CSV", "CSVWithNames", "Parquet", "JSONEachRow", "Delta", "DeltaS3Wrapper"] = Field(..., alias="format", description="The data format of the table contents. Supported formats are CSV, CSVWithNames (CSV with header row), Parquet, JSONEachRow (newline-delimited JSON), Delta, and DeltaS3Wrapper."),
    url_pattern: str = Field(..., description="The URL pattern or path to the table data in external storage. Must be 500 characters or fewer.", max_length=500),
    access_key: str = Field(..., description="The access key or username for authenticating with the external storage system. Must be 500 characters or fewer.", max_length=500),
    access_secret: str = Field(..., description="The access secret or password for authenticating with the external storage system. Must be 500 characters or fewer.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Optional configuration object for additional table settings or format-specific parameters."),
) -> dict[str, Any]:
    """Update the schema configuration for a warehouse table, including its data format, storage location, and access credentials. This operation allows you to modify how a table reads and interprets data from external storage."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesUpdateSchemaCreateRequest(
            path=_models.WarehouseTablesUpdateSchemaCreateRequestPath(id_=id_, project_id=project_id),
            body=_models.WarehouseTablesUpdateSchemaCreateRequestBody(name=name, format_=format_, url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesUpdateSchemaCreateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_warehouse_table_schema: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/{id}/update_schema", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/{id}/update_schema"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_warehouse_table_schema")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_warehouse_table_schema", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_warehouse_table_schema",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: warehouse_tables
@mcp.tool()
async def create_warehouse_table_from_file(
    project_id: str = Field(..., description="The unique identifier of the project where the warehouse table will be created. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the warehouse table. Must be 128 characters or fewer.", max_length=128),
    format_: Literal["CSV", "CSVWithNames", "Parquet", "JSONEachRow", "Delta", "DeltaS3Wrapper"] = Field(..., alias="format", description="The file format of the source data. Supported formats include CSV, CSV with header names, Parquet, JSON (one object per line), Delta, and Delta with S3 wrapper."),
    url_pattern: str = Field(..., description="The URL pattern or path to the source file location. Supports cloud storage paths and wildcards for multiple files. Maximum length is 500 characters.", max_length=500),
    access_key: str = Field(..., description="The access key or username for authenticating with the cloud storage service. Maximum length is 500 characters.", max_length=500),
    access_secret: str = Field(..., description="The secret key or password for authenticating with the cloud storage service. Maximum length is 500 characters.", max_length=500),
    options: dict[str, Any] | None = Field(None, description="Optional configuration object for additional file parsing or ingestion settings specific to the chosen format."),
) -> dict[str, Any]:
    """Create a new warehouse table by ingesting data from a file source. Specify the file format, location via URL pattern, and cloud storage credentials to establish the data connection."""

    # Construct request model with validation
    try:
        _request = _models.WarehouseTablesFileCreateRequest(
            path=_models.WarehouseTablesFileCreateRequestPath(project_id=project_id),
            body=_models.WarehouseTablesFileCreateRequestBody(name=name, format_=format_, url_pattern=url_pattern, options=options,
                credential=_models.WarehouseTablesFileCreateRequestBodyCredential(access_key=access_key, access_secret=access_secret))
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_warehouse_table_from_file: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/warehouse_tables/file", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/warehouse_tables/file"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_warehouse_table_from_file")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_warehouse_table_from_file", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_warehouse_table_from_file",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        body_content_type="application/x-www-form-urlencoded",
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def list_web_experiments(
    project_id: str = Field(..., description="The unique identifier of the project containing the web experiments. Obtain this ID by calling the projects list endpoint."),
    limit: int | None = Field(None, description="Maximum number of results to return in a single page of results. Omit or adjust to control pagination size."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
) -> dict[str, Any]:
    """Retrieve a paginated list of web experiments for a specific project. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsListRequest(
            path=_models.WebExperimentsListRequestPath(project_id=project_id),
            query=_models.WebExperimentsListRequestQuery(limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_web_experiments: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_web_experiments")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_web_experiments", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_web_experiments",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def create_web_experiment(
    project_id: str = Field(..., description="The unique identifier of the project where the web experiment will be created. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="A descriptive name for the web experiment. Must not exceed 400 characters.", max_length=400),
    variants: Any = Field(..., description="A collection of experiment variants, each containing DOM transformations, targeting conditions, and rollout percentage. Each variant specifies CSS selectors, text/HTML replacements, and the percentage of traffic to route to that variant."),
) -> dict[str, Any]:
    """Create a new web experiment for a project with specified variants and their transformations. This allows you to define A/B test configurations with different content variants and rollout percentages."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsCreateRequest(
            path=_models.WebExperimentsCreateRequestPath(project_id=project_id),
            body=_models.WebExperimentsCreateRequestBody(name=name, variants=variants)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_web_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_web_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_web_experiment", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_web_experiment",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def get_web_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the web experiment to retrieve. This is a positive integer that uniquely identifies the experiment within the project."),
    project_id: str = Field(..., description="The unique identifier of the project containing the web experiment. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Retrieve a specific web experiment by its ID within a project. Use this to fetch detailed information about a single web experiment configuration and its settings."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsRetrieveRequest(
            path=_models.WebExperimentsRetrieveRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_web_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_web_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_web_experiment", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_web_experiment",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def update_web_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the web experiment to update."),
    project_id: str = Field(..., description="The unique identifier of the project containing the web experiment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    name: str = Field(..., description="The display name for the web experiment. Must not exceed 400 characters.", max_length=400),
    variants: Any = Field(..., description="A collection of experiment variants, each containing DOM transforms (text/HTML replacements with CSS selectors), targeting conditions, and rollout percentage allocation. The control variant is typically included as a baseline."),
) -> dict[str, Any]:
    """Update an existing web experiment with new configuration, including name and variant definitions with transforms, conditions, and rollout percentages."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsUpdateRequest(
            path=_models.WebExperimentsUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WebExperimentsUpdateRequestBody(name=name, variants=variants)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_web_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_web_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_web_experiment", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_web_experiment",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def update_web_experiment_partial(
    id_: int = Field(..., alias="id", description="The unique identifier of the web experiment to update. This is a positive integer assigned when the experiment was created."),
    project_id: str = Field(..., description="The unique identifier of the project containing the web experiment. Retrieve available project IDs by calling the /api/projects/ endpoint."),
    variants: Any | None = Field(None, description="Experiment variants defining control and treatment groups. Each variant specifies DOM transformations (text/HTML replacements via CSS selectors), targeting conditions, and traffic allocation percentage. Omit this field to leave variants unchanged."),
) -> dict[str, Any]:
    """Partially update a web experiment within a project. Use this to modify specific fields like variants, targeting conditions, or rollout percentages without replacing the entire experiment configuration."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsPartialUpdateRequest(
            path=_models.WebExperimentsPartialUpdateRequestPath(id_=id_, project_id=project_id),
            body=_models.WebExperimentsPartialUpdateRequestBody(variants=variants)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_web_experiment_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments/{id}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_web_experiment_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_web_experiment_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_web_experiment_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: web_experiments
@mcp.tool()
async def delete_web_experiment(
    id_: int = Field(..., alias="id", description="The unique identifier of the web experiment to delete. This is a positive integer value."),
    project_id: str = Field(..., description="The unique identifier of the project containing the web experiment. You can retrieve available project IDs by calling the /api/projects/ endpoint."),
) -> dict[str, Any]:
    """Permanently delete a web experiment from a project. This action cannot be undone."""

    # Construct request model with validation
    try:
        _request = _models.WebExperimentsDestroyRequest(
            path=_models.WebExperimentsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_web_experiment: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/web_experiments/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/web_experiments/{id}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_web_experiment")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_web_experiment", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_web_experiment",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: hog_function_templates, cdp, public_hog_function_templates
@mcp.tool()
async def list_hog_function_templates_public(
    limit: int | None = Field(None, description="Maximum number of templates to return in a single page of results. Use with offset for pagination."),
    offset: int | None = Field(None, description="Zero-based index position to start returning results from, enabling pagination through large result sets."),
    types: str | None = Field(None, description="Filter templates by one or more types using a comma-separated list (e.g., destination, email, sms_provider). Only templates matching the specified types will be returned."),
) -> dict[str, Any]:
    """Retrieve a paginated list of available HOG function templates, optionally filtered by template type. Use this to discover and browse available templates for destinations, email, SMS providers, and other integrations."""

    # Construct request model with validation
    try:
        _request = _models.PublicHogFunctionTemplatesListRequest(
            query=_models.PublicHogFunctionTemplatesListRequestQuery(limit=limit, offset=offset, types=types)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_hog_function_templates_public: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/public_hog_function_templates"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_hog_function_templates_public")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_hog_function_templates_public", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_hog_function_templates_public",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def list_users(
    is_staff: bool | None = Field(None, description="Filter results to include only staff users when set to true, or only non-staff users when set to false. Omit to return all users regardless of staff status."),
    limit: int | None = Field(None, description="Maximum number of user records to return in a single response. Controls page size for pagination."),
    offset: int | None = Field(None, description="Zero-based index position from which to start returning results. Use with limit to paginate through the full user list."),
) -> dict[str, Any]:
    """Retrieve a paginated list of users, optionally filtered by staff status. Use limit and offset parameters to control pagination."""

    # Construct request model with validation
    try:
        _request = _models.UsersListRequest(
            query=_models.UsersListRequestQuery(is_staff=is_staff, limit=limit, offset=offset)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for list_users: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/users"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("list_users")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("list_users", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="list_users",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def get_user(uuid_: str = Field(..., alias="uuid", description="The unique identifier (UUID) of the user to retrieve. Must be a valid UUID format.")) -> dict[str, Any]:
    """Retrieve a specific user by their unique identifier (UUID). Returns the complete user profile for the requested user."""

    # Construct request model with validation
    try:
        _request = _models.UsersRetrieveRequest(
            path=_models.UsersRetrieveRequestPath(uuid_=uuid_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_user", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_user",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def update_user(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user to update, formatted as a UUID."),
    email: str = Field(..., description="The user's email address, up to 254 characters. Must be a valid email format.", max_length=254),
    password: str = Field(..., description="The user's password for authentication, up to 128 characters. Should be a strong, secure password.", max_length=128),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="Configuration object for controlling user notification preferences and delivery channels."),
    anonymize_data: bool | None = Field(None, description="When enabled, removes personally identifiable information from the user's account while preserving activity history."),
    allow_impersonation: bool | None = Field(None, description="When enabled, allows administrators to log in as this user for support and troubleshooting purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Controls the display mode and behavior of the toolbar interface."),
    is_staff: bool | None = Field(None, description="When enabled, grants this user administrative access to the site and admin panel."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Controls the visual theme preference for the user interface (e.g., light mode, dark mode)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Specifies the preferred location for displaying keyboard shortcuts and quick-access shortcuts."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's primary role or function within the organization. Choose from: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Update a user's profile information, authentication credentials, preferences, and organizational role. Requires the user's UUID and email address."""

    # Construct request model with validation
    try:
        _request = _models.UsersUpdateRequest(
            path=_models.UsersUpdateRequestPath(uuid_=uuid_),
            body=_models.UsersUpdateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_user", "PUT", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_user",
        method="PUT",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def update_user_partial(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user to update, formatted as a UUID."),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="User's notification preferences and settings as a structured object."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in the system."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other users to impersonate this account for testing or support purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="The user's preferred toolbar display mode or layout."),
    is_staff: bool | None = Field(None, description="Whether the user has administrative privileges and can access the admin site."),
    password: str | None = Field(None, description="The user's password for account authentication, up to 128 characters. Should be securely transmitted and stored.", max_length=128),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="The user's preferred color theme or visual mode for the interface."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="The user's preferred position or layout for shortcuts in the interface."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's role or function within their organization. Valid roles are: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Partially update a user's profile information, preferences, and account settings. Allows modification of personal details, notification preferences, UI customization, and organizational role."""

    # Construct request model with validation
    try:
        _request = _models.UsersPartialUpdateRequest(
            path=_models.UsersPartialUpdateRequestPath(uuid_=uuid_),
            body=_models.UsersPartialUpdateRequestBody(first_name=first_name, last_name=last_name, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_user_partial: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_user_partial")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_user_partial", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_user_partial",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def delete_user(uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user to delete, formatted as a valid UUID (universally unique identifier).")) -> dict[str, Any]:
    """Permanently delete a user account by its unique identifier. This operation removes the user and all associated data from the system."""

    # Construct request model with validation
    try:
        _request = _models.UsersDestroyRequest(
            path=_models.UsersDestroyRequestPath(uuid_=uuid_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for delete_user: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("delete_user")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("delete_user", "DELETE", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="delete_user",
        method="DELETE",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def create_user_scene_personalisation(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user as a UUID string."),
    email: str = Field(..., description="The user's email address, must be a valid email format and up to 254 characters.", max_length=254),
    password: str = Field(..., description="The user's password, up to 128 characters. Should be securely hashed before storage.", max_length=128),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="Configuration object for notification preferences and alert settings."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's data in logs and analytics."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other users to impersonate this account for testing or support purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Display mode for the toolbar interface (e.g., compact, expanded, hidden)."),
    is_staff: bool | None = Field(None, description="Whether this user has administrative access to the system."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="The visual theme preference for the user interface (e.g., light, dark, auto)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="The preferred position for quick-access shortcuts in the interface (e.g., top, left, right, bottom)."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's role or function within their organization. Valid options are: engineering, data, product, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Create or configure personalization settings for a user, including display preferences, notification settings, and organizational role. This operation sets up the user's scene customization within the system."""

    # Construct request model with validation
    try:
        _request = _models.UsersScenePersonalisationCreateRequest(
            path=_models.UsersScenePersonalisationCreateRequestPath(uuid_=uuid_),
            body=_models.UsersScenePersonalisationCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_user_scene_personalisation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/scene_personalisation", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/scene_personalisation"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_user_scene_personalisation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_user_scene_personalisation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_user_scene_personalisation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def initiate_two_factor_authentication_setup(uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user, formatted as a valid UUID.")) -> dict[str, Any]:
    """Initiates the two-factor authentication setup process for a user. Returns the necessary information to begin configuring 2FA on their account."""

    # Construct request model with validation
    try:
        _request = _models.UsersStart2faSetupRetrieveRequest(
            path=_models.UsersStart2faSetupRetrieveRequestPath(uuid_=uuid_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for initiate_two_factor_authentication_setup: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/start_2fa_setup", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/start_2fa_setup"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("initiate_two_factor_authentication_setup")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("initiate_two_factor_authentication_setup", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="initiate_two_factor_authentication_setup",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def regenerate_two_factor_backup_codes(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier (UUID) of the user for whom backup codes are being generated."),
    email: str = Field(..., description="The user's email address, must be a valid email format and no longer than 254 characters.", max_length=254),
    password: str = Field(..., description="The user's password, up to 128 characters. Must be provided for security verification.", max_length=128),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="Configuration object for the user's notification preferences."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in logs and records."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other administrators to impersonate this user account."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="The user's preferred toolbar display mode."),
    is_staff: bool | None = Field(None, description="Whether this user has administrative access to the site."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="The user's preferred color theme mode (light, dark, or auto)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="The user's preferred position for UI shortcuts."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's role or function within their organization. Valid options include: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Generate a new set of two-factor authentication backup codes for a user, automatically invalidating any previously issued codes. This operation also allows updating user profile information in the same request."""

    # Construct request model with validation
    try:
        _request = _models.UsersTwoFactorBackupCodesCreateRequest(
            path=_models.UsersTwoFactorBackupCodesCreateRequestPath(uuid_=uuid_),
            body=_models.UsersTwoFactorBackupCodesCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for regenerate_two_factor_backup_codes: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/two_factor_backup_codes", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/two_factor_backup_codes"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("regenerate_two_factor_backup_codes")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("regenerate_two_factor_backup_codes", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="regenerate_two_factor_backup_codes",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def get_two_factor_setup_initialization(uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user, formatted as a valid UUID.")) -> dict[str, Any]:
    """Retrieve two-factor authentication setup initialization data for a specific user. This endpoint prepares the user's account for enabling two-factor authentication by returning necessary setup information."""

    # Construct request model with validation
    try:
        _request = _models.UsersTwoFactorStartSetupRetrieveRequest(
            path=_models.UsersTwoFactorStartSetupRetrieveRequestPath(uuid_=uuid_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_two_factor_setup_initialization: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/two_factor_start_setup", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/two_factor_start_setup"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_two_factor_setup_initialization")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_two_factor_setup_initialization", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_two_factor_setup_initialization",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def get_user_two_factor_status(uuid_: str = Field(..., alias="uuid", description="The unique identifier of the user, formatted as a UUID.")) -> dict[str, Any]:
    """Retrieve the current two-factor authentication status for a user, including backup codes if 2FA is enabled."""

    # Construct request model with validation
    try:
        _request = _models.UsersTwoFactorStatusRetrieveRequest(
            path=_models.UsersTwoFactorStatusRetrieveRequestPath(uuid_=uuid_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for get_user_two_factor_status: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/two_factor_status", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/two_factor_status"
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("get_user_two_factor_status")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("get_user_two_factor_status", "GET", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="get_user_two_factor_status",
        method="GET",
        path=_http_path,
        request_id=_request_id,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def validate_user_two_factor(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier (UUID) of the user account to validate and update."),
    email: str = Field(..., description="The user's email address, must be a valid email format and up to 254 characters.", max_length=254),
    password: str = Field(..., description="The user's password, up to 128 characters. Should be securely hashed before storage.", max_length=128),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="User notification preferences configuration object."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in logs and analytics."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other administrators to impersonate this user account."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Display mode preference for the application toolbar."),
    is_staff: bool | None = Field(None, description="Whether this user has administrative access to the site."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Visual theme preference for the user interface."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Preferred position for keyboard shortcuts display."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's role or function within their organization. Valid options are: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Validate and update a user's two-factor authentication status while simultaneously updating their profile information including name, email, password, preferences, and organizational role."""

    # Construct request model with validation
    try:
        _request = _models.UsersTwoFactorValidateCreateRequest(
            path=_models.UsersTwoFactorValidateCreateRequestPath(uuid_=uuid_),
            body=_models.UsersTwoFactorValidateCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for validate_user_two_factor: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/two_factor_validate", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/two_factor_validate"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("validate_user_two_factor")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("validate_user_two_factor", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="validate_user_two_factor",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def validate_and_update_user_2fa(
    uuid_: str = Field(..., alias="uuid", description="The unique identifier (UUID) of the user account to validate and update."),
    email: str = Field(..., description="The user's email address, must be a valid email format and no longer than 254 characters.", max_length=254),
    password: str = Field(..., description="The user's account password, up to 128 characters. Should be securely transmitted and stored.", max_length=128),
    first_name: str | None = Field(None, description="The user's first name, up to 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="The user's last name, up to 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="Configuration object for user notification preferences (e.g., email alerts, frequency settings)."),
    anonymize_data: bool | None = Field(None, description="When enabled, removes or masks personally identifiable information from user records."),
    allow_impersonation: bool | None = Field(None, description="When enabled, allows administrators or authorized users to log in as this user for support or troubleshooting purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Controls the display mode or visibility of the user interface toolbar (e.g., compact, expanded, hidden)."),
    is_staff: bool | None = Field(None, description="When enabled, grants this user access to the administrative site and staff-level features."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Sets the user's preferred visual theme (e.g., light mode, dark mode, auto)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Specifies the preferred location or alignment of keyboard shortcuts and quick-access elements in the interface."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="The user's role or function within the organization. Valid options are: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Validate a user's two-factor authentication and update their profile settings. This operation confirms 2FA setup while allowing optional updates to user account details and preferences."""

    # Construct request model with validation
    try:
        _request = _models.UsersValidate2faCreateRequest(
            path=_models.UsersValidate2faCreateRequestPath(uuid_=uuid_),
            body=_models.UsersValidate2faCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for validate_and_update_user_2fa: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/users/{uuid}/validate_2fa", _request.path.model_dump(by_alias=True)) if _request.path else "/api/users/{uuid}/validate_2fa"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("validate_and_update_user_2fa")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("validate_and_update_user_2fa", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="validate_and_update_user_2fa",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def update_user_profile(
    first_name: str | None = Field(None, description="User's first name, up to 150 characters in length.", max_length=150),
    last_name: str | None = Field(None, description="User's last name, up to 150 characters in length.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="User's notification preferences and settings as a structured object."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in the system."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other users to impersonate this account for administrative or support purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Display mode preference for the toolbar interface."),
    is_staff: bool | None = Field(None, description="Whether the user has administrative privileges and can access the admin site."),
    password: str | None = Field(None, description="User's account password, up to 128 characters in length.", max_length=128),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Visual theme preference for the user interface (e.g., light mode, dark mode)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Preferred position or layout for shortcuts in the user interface."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="User's role or function within their organization. Valid options are: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Partially update user profile information including personal details, preferences, and organizational role. This operation allows modification of user account settings and preferences without requiring a complete profile replacement."""

    # Construct request model with validation
    try:
        _request = _models.UsersCancelEmailChangeRequestPartialUpdateRequest(
            body=_models.UsersCancelEmailChangeRequestPartialUpdateRequestBody(first_name=first_name, last_name=last_name, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for update_user_profile: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/users/cancel_email_change_request"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("update_user_profile")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("update_user_profile", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="update_user_profile",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def create_user_with_email_verification(
    email: str = Field(..., description="User's email address. Must be a valid email format and cannot exceed 254 characters.", max_length=254),
    password: str = Field(..., description="User's account password. Maximum 128 characters. Should meet security requirements (enforced server-side).", max_length=128),
    first_name: str | None = Field(None, description="User's first name. Maximum 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="User's last name. Maximum 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="User's notification preferences as a structured object. Defines how and when the user receives notifications."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in analytics and logs."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other users or administrators to impersonate this account for support or testing purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Display mode for the toolbar interface. Controls toolbar visibility and behavior in the user interface."),
    is_staff: bool | None = Field(None, description="Whether this user has administrative access to the admin site and can manage system settings."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Preferred color theme for the user interface. Controls light/dark mode and visual styling preferences."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Position or layout preference for keyboard shortcuts and quick-access elements in the interface."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="User's role or function within their organization. Select from: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Create a new user account and initiate email verification. This operation registers a user with their credentials and optional profile information, triggering a verification email to confirm their email address."""

    # Construct request model with validation
    try:
        _request = _models.UsersRequestEmailVerificationCreateRequest(
            body=_models.UsersRequestEmailVerificationCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_user_with_email_verification: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/users/request_email_verification"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_user_with_email_verification")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_user_with_email_verification", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_user_with_email_verification",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, users
@mcp.tool()
async def create_user_with_email_verification_confirmation(
    email: str = Field(..., description="User's email address. Must be a valid email format and cannot exceed 254 characters.", max_length=254),
    password: str = Field(..., description="User's account password. Maximum 128 characters. Should meet security requirements.", max_length=128),
    first_name: str | None = Field(None, description="User's first name. Maximum 150 characters.", max_length=150),
    last_name: str | None = Field(None, description="User's last name. Maximum 150 characters.", max_length=150),
    notification_settings: dict[str, Any] | None = Field(None, description="User's notification preferences and settings as a structured object."),
    anonymize_data: bool | None = Field(None, description="Whether to anonymize the user's personal data in logs and analytics."),
    allow_impersonation: bool | None = Field(None, description="Whether to allow other users to impersonate this account for testing or support purposes."),
    toolbar_mode: Literal["disabled", "toolbar"] | Literal[""] | None = Field(None, description="Display mode preference for the toolbar interface."),
    is_staff: bool | None = Field(None, description="Whether this user has administrative access to the admin site."),
    theme_mode: Literal["light", "dark", "system"] | Literal[""] | None = Field(None, description="Display theme preference for the user interface (e.g., light or dark mode)."),
    shortcut_position: Literal["above", "below", "hidden"] | Literal[""] | None = Field(None, description="Preferred position or layout for keyboard shortcuts in the interface."),
    role_at_organization: Literal["engineering", "data", "product", "founder", "leadership", "marketing", "sales", "other"] | None = Field(None, description="User's role or function within their organization. Valid options: engineering, data, product management, founder, leadership, marketing, sales/success, or other."),
) -> dict[str, Any]:
    """Create a new user account with email verification. Requires email and password; optionally configure user profile details, notification preferences, and organizational role."""

    # Construct request model with validation
    try:
        _request = _models.UsersVerifyEmailCreateRequest(
            body=_models.UsersVerifyEmailCreateRequestBody(first_name=first_name, last_name=last_name, email=email, notification_settings=notification_settings, anonymize_data=anonymize_data, allow_impersonation=allow_impersonation, toolbar_mode=toolbar_mode, is_staff=is_staff, password=password, theme_mode=theme_mode, shortcut_position=shortcut_position, role_at_organization=role_at_organization)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for create_user_with_email_verification_confirmation: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = "/api/users/verify_email"
    _http_body = _request.body.model_dump(by_alias=True, exclude_none=True) if _request.body else None
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("create_user_with_email_verification_confirmation")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("create_user_with_email_verification_confirmation", "POST", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="create_user_with_email_verification_confirmation",
        method="POST",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: actions, actions
@mcp.tool()
async def actions_destroy(
    id_: int = Field(..., alias="id", description="A unique integer value identifying this action."),
    project_id: str = Field(..., description="Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."),
    format_: Literal["csv", "json"] | None = Field(None, alias="format"),
) -> dict[str, Any]:
    """Soft-delete an action by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.ActionsDestroyRequest(
            path=_models.ActionsDestroyRequestPath(id_=id_, project_id=project_id),
            query=_models.ActionsDestroyRequestQuery(format_=format_)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for actions_destroy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/actions/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/actions/{id}"
    _http_query = _request.query.model_dump(by_alias=True, exclude_none=True) if _request.query else {}
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("actions_destroy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("actions_destroy", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="actions_destroy",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        params=_http_query,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, cohorts
@mcp.tool()
async def cohorts_destroy(
    id_: int = Field(..., alias="id", description="A unique integer value identifying this cohort."),
    project_id: str = Field(..., description="Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."),
) -> dict[str, Any]:
    """Soft-delete a cohort by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.CohortsDestroyRequest(
            path=_models.CohortsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for cohorts_destroy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/cohorts/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/cohorts/{id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("cohorts_destroy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("cohorts_destroy", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="cohorts_destroy",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# Tags: core, annotations
@mcp.tool()
async def annotations_destroy(
    id_: int = Field(..., alias="id", description="A unique integer value identifying this annotation."),
    project_id: str = Field(..., description="Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."),
) -> dict[str, Any]:
    """Soft-delete an annotation by marking it as deleted. Sends PATCH with {deleted: true} body — the underlying DELETE endpoint is not supported by the API."""

    # Construct request model with validation
    try:
        _request = _models.AnnotationsDestroyRequest(
            path=_models.AnnotationsDestroyRequestPath(id_=id_, project_id=project_id)
        )
    except pydantic.ValidationError as _validation_err:
        logging.error(f"Parameter validation failed for annotations_destroy: {_validation_err}")
        raise ValueError(f"Invalid parameters: {_validation_err.errors()}") from _validation_err

    # Extract parameters for API call
    _http_path = _build_path("/api/projects/{project_id}/annotations/{id}", _request.path.model_dump(by_alias=True)) if _request.path else "/api/projects/{project_id}/annotations/{id}"
    _http_body: dict[str, Any] = {}
    # Constant body fields (from schemas.patch.json add_constant_body_fields) — fixed values, not agent-configurable.
    # If the original schema has a body but it's null/scalar (body_is_whole_body case), coerce to empty dict first.
    if not isinstance(_http_body, dict):
        _http_body = {}
    _http_body["deleted"] = True
    _http_headers = {}

    # Inject per-operation authentication
    _auth = await _get_auth_for_operation("annotations_destroy")
    _http_headers.update(_auth.get("headers", {}))

    _request_id = str(uuid.uuid4())
    _log_tool_invocation("annotations_destroy", "PATCH", _http_path, _request_id)

    # Execute request (returns normalized dict and status code)
    _response_data, _ = await _execute_tool_request(
        tool_name="annotations_destroy",
        method="PATCH",
        path=_http_path,
        request_id=_request_id,
        body=_http_body,
        headers=_http_headers,
    )

    return _response_data

# ============================================================================
# Schema Optimization
# ============================================================================

def _optimize_tool_schemas() -> None:
    """Simplify anyOf nullable wrappers in tool schemas for LLM token efficiency.

    Pydantic generates {"anyOf": [{"type": "T"}, {"type": "null"}], "default": null}
    for `T | None = None` params. Both Anthropic and OpenAI recommend the simpler
    {"type": "T"} with the param outside `required` for optimal tool-calling accuracy
    and minimal token overhead. Runtime validation is unaffected — Python signatures
    retain the `T | None` annotation.
    """
    from fastmcp.tools import Tool as _ToolCls
    # v3: _local_provider._components, v2: _tool_manager._tools
    _src = getattr(mcp, "_local_provider", None)
    if _src is not None:
        _tools = [v for v in _src._components.values() if isinstance(v, _ToolCls)]
    else:
        _tools = list(mcp._tool_manager._tools.values())  # type: ignore[attr-defined]
    for _tool in _tools:
        _simplify_schema(_tool.parameters)

def _simplify_schema(schema: dict) -> None:
    """Walk a JSON Schema dict and collapse anyOf-nullable patterns in place.

    Also removes discriminator objects anywhere in the tree: after FastMCP's
    DereferenceRefsMiddleware inlines $ref entries, discriminator.mapping values
    point to $defs that no longer exist. The discriminator is redundant — each
    variant already identifies itself via Literal constants on the discriminant
    field, so MCP clients and LLMs don't need the mapping.
    """
    schema.pop("discriminator", None)
    for _def_schema in schema.get("$defs", {}).values():
        _simplify_schema(_def_schema)
    _props = schema.get("properties", {})
    _required = set(schema.get("required", []))
    for _name, _prop in _props.items():
        if "anyOf" in _prop:
            _non_null = [b for b in _prop["anyOf"] if b.get("type") != "null"]
            if len(_non_null) == 1:
                _desc = _prop.get("description")
                _prop.clear()
                _prop.update(_non_null[0])
                if _desc:
                    _prop["description"] = _desc
                _required.discard(_name)
        _simplify_schema(_prop)
    if _required:
        schema["required"] = sorted(_required)
    elif "required" in schema:
        del schema["required"]
    for _key in ("anyOf", "oneOf"):
        for _branch in schema.get(_key, []):
            if isinstance(_branch, dict):
                _simplify_schema(_branch)

_optimize_tool_schemas()

# ============================================================================
# Environment Validation
# ============================================================================

def validate_environment() -> None:
    """Validate required environment variables at startup."""
    errors: list[str] = []
    warnings: list[str] = []

    required_vars: list[tuple[str, str, str | None]] = [
    ]

    for var_name, description, default in required_vars:
        value = os.getenv(var_name, default)
        if not value:
            errors.append(f"  - {var_name}: {description}")

    if errors:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Missing required environment variables", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        for error in errors:
            print(error, file=sys.stderr)
        print("\nServer startup aborted. Set required variables and restart.", file=sys.stderr)
        print("\nExample:", file=sys.stderr)
        print("  python post_hog_api_server.py", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    _validate_port()
    _validate_timeout()
    _validate_rate_limit()

    logger = logging.getLogger(__name__)
    logger.info("Environment validation passed")
    for warning in warnings:
        logger.warning(warning)

def _validate_port() -> None:
    port_str = os.getenv('PORT')
    if not port_str:
        return  # Optional, has CLI default

    port: int
    try:
        port = int(port_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid PORT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port_str}", file=sys.stderr)
        print("\nPORT must be an integer between 1 and 65535", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if port < 1 or port > 65535:
        print("=" * 70, file=sys.stderr)
        print("ERROR: PORT out of valid range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {port}", file=sys.stderr)
        print("\nPORT must be between 1 and 65535", file=sys.stderr)
        print("  Common ports: 8000, 8080, 3000", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_timeout() -> None:
    timeout_str = os.getenv('API_TIMEOUT')
    if not timeout_str:
        return  # Optional, has default

    timeout: int
    try:
        timeout = int(timeout_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid API_TIMEOUT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout_str}", file=sys.stderr)
        print("\nAPI_TIMEOUT must be a positive integer (seconds)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if timeout < 1 or timeout > 3600:
        print("=" * 70, file=sys.stderr)
        print("ERROR: API_TIMEOUT out of reasonable range", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {timeout} seconds", file=sys.stderr)
        print("\nAPI_TIMEOUT must be between 1 and 3600 seconds (1 hour max)", file=sys.stderr)
        print("  Recommended: 30 (most APIs), 60 (slower APIs), 300 (long operations)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

def _validate_rate_limit() -> None:
    rate_str = os.getenv('RATE_LIMIT')
    if not rate_str:
        return  # Optional, has default

    try:
        rate = float(rate_str)
    except ValueError:
        print("=" * 70, file=sys.stderr)
        print("ERROR: Invalid RATE_LIMIT value", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate_str}", file=sys.stderr)
        print("\nRATE_LIMIT must be a positive number (requests per second)", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

    if rate <= 0:
        print("=" * 70, file=sys.stderr)
        print("ERROR: RATE_LIMIT must be positive", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        print(f"  Got: {rate}", file=sys.stderr)
        print("\nRATE_LIMIT must be greater than 0", file=sys.stderr)
        print("  Recommended: 1-100 requests/second depending on API limits", file=sys.stderr)
        print("=" * 70, file=sys.stderr)
        sys.exit(1)

# ============================================================================
# Main Entry Point
# ============================================================================

def main():
    """CLI entry point with runtime transport selection."""

    validate_environment()

    parser = argparse.ArgumentParser(description="PostHog API MCP Server")

    parser.add_argument(
        '--transport',
        choices=['stdio', 'sse', 'streamable-http'],
        default='stdio',
        help='Transport protocol: stdio (default, for Claude Code), sse (remote capable), streamable-http (production)'
    )
    parser.add_argument(
        '--host',
        default='0.0.0.0',  # noqa: S104
        help='Host address for HTTP transports (default: 0.0.0.0)'
    )
    parser.add_argument(
        '--port',
        type=int,
        default=8000,
        help='Port for HTTP transports (default: 8000)'
    )
    parser.add_argument(
        '--log-level',
        choices=['DEBUG', 'INFO', 'WARNING', 'ERROR'],
        default=LOG_LEVEL,
        help='Logging level (default: from LOG_LEVEL env var, or INFO)'
    )
    parser.add_argument(
        '--retry',
        action='store_true',
        help='Enable automatic retry with exponential backoff (default: enabled)'
    )
    parser.add_argument(
        '--no-retry',
        action='store_true',
        help='Disable automatic retry'
    )
    parser.add_argument(
        '--max-retries',
        type=int,
        default=MAX_RETRIES,
        help='Maximum retry attempts (default: from MAX_RETRIES env var, or 3)'
    )
    parser.add_argument(
        '--retry-base-delay',
        type=int,
        default=RETRY_BACKOFF_FACTOR,
        help='Base delay for exponential backoff in seconds (default: from RETRY_BACKOFF_FACTOR env var, or 2)'
    )
    parser.add_argument(
        '--retry-max-delay',
        type=int,
        default=60,
        help='Maximum retry delay in seconds (default: 60)'
    )
    parser.add_argument(
        '--timeout',
        type=int,
        default=None,
        help='API request timeout in seconds (default: 30, or API_TIMEOUT env var)'
    )
    parser.add_argument(
        '--rate-limiting',
        choices=['disabled', 'local'],
        default='local',
        help='Rate limiting mode: disabled (default) or local (in-memory token bucket)'
    )
    parser.add_argument(
        '--rate-limit',
        type=int,
        default=RATE_LIMIT_REQUESTS_PER_SECOND,
        help='Rate limit in requests per second (default: from RATE_LIMIT_REQUESTS_PER_SECOND env var, or 10)'
    )
    parser.add_argument(
        '--burst',
        type=int,
        help='Burst capacity for rate limiter (default: 2x rate-limit)'
    )
    parser.add_argument(
        '--circuit-breaker',
        action='store_true',
        help='Enable circuit breaker to prevent cascading failures'
    )
    parser.add_argument(
        '--circuit-breaker-failure-threshold',
        type=int,
        default=CIRCUIT_BREAKER_FAILURE_THRESHOLD,
        help='Failures before opening circuit (default: from CIRCUIT_BREAKER_FAILURE_THRESHOLD env var, or 5)'
    )
    parser.add_argument(
        '--circuit-breaker-timeout',
        type=float,
        default=CIRCUIT_BREAKER_TIMEOUT_SECONDS,
        help='Seconds before recovery attempt (default: from CIRCUIT_BREAKER_TIMEOUT_SECONDS env var, or 60.0)'
    )
    args = parser.parse_args()

    logging.basicConfig(
        level=getattr(logging, args.log_level),
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )

    logger = logging.getLogger(__name__)
    logger.info("Starting PostHog API MCP Server")
    logger.info(f"Transport: {args.transport}")

    global retry_config, rate_limiter, circuit_breaker, DEFAULT_TIMEOUT

    if args.timeout is not None:
        DEFAULT_TIMEOUT = args.timeout
    else:
        timeout_env = os.getenv('API_TIMEOUT')
        if timeout_env:
            try:
                DEFAULT_TIMEOUT = int(timeout_env)
            except ValueError:
                logger.warning(f"Invalid API_TIMEOUT '{timeout_env}', using default {DEFAULT_TIMEOUT}s")
    logger.info(f"Timeout: {DEFAULT_TIMEOUT}s")

    if not args.no_retry:
        retry_config = RetryConfig(
            max_attempts=args.max_retries,
            base_delay=args.retry_base_delay,
            max_delay=args.retry_max_delay
        )
        logger.info(
            f"Retry: Enabled (max attempts: {retry_config.max_attempts}, "
            f"base delay: {retry_config.base_delay}s, max delay: {retry_config.max_delay}s)"
        )
    else:
        logger.info("Retry: Disabled")

    rate_limiting_mode = args.rate_limiting

    if rate_limiting_mode == 'disabled':
        rate_limiter = None
        logger.info("Rate limiting: Disabled")
    elif rate_limiting_mode == 'local':
        rate_limit = args.rate_limit if args.rate_limit else 10.0
        burst = args.burst if args.burst else int(rate_limit * 2)
        rate_limiter = TokenBucket(rate=rate_limit, capacity=burst)
        logger.info(f"Rate limiting: Local (rate: {rate_limit} req/sec, burst: {burst})")
    else:
        logger.warning(f"Unknown rate limiting mode: {rate_limiting_mode}, disabling")
        rate_limiter = None

    if args.circuit_breaker:
        cb_config = CircuitBreakerConfig(
            failure_threshold=args.circuit_breaker_failure_threshold,
            timeout=args.circuit_breaker_timeout
        )
        circuit_breaker = CircuitBreaker(cb_config)
        logger.info(
            f"Circuit breaker: Enabled (failure threshold: {cb_config.failure_threshold}, "
            f"timeout: {cb_config.timeout}s)"
        )
    else:
        logger.info("Circuit breaker: Disabled")
    try:
        if args.transport == 'stdio':
            logger.info("Using stdio transport")
            mcp.run()
        elif args.transport == 'sse':
            logger.info(f"Using SSE transport on http://{args.host}:{args.port}")
            mcp.run(transport='sse', host=args.host, port=args.port)
        elif args.transport == 'streamable-http':
            logger.info(f"Using Streamable HTTP transport on http://{args.host}:{args.port}")
            mcp.run(transport='streamable-http', host=args.host, port=args.port)

    except KeyboardInterrupt:
        logger.info("Server stopped by user")
    except Exception as e:
        logger.error(f"Server error: {e}", exc_info=True)
        raise

if __name__ == "__main__":
    main()
